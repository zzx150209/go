# 参考站知识点内容（自动抓取）
REF_KP = {
    1: [
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 计算机系统依靠硬件和软件共同完成信息的输入、存储、处理与输出；判断设备职责时要看信息流向，判断文件身份时要看内容和用途。</li>\n<li><strong>常用判断：</strong> 用“CPU 负责处理、内存临时保存、外部存储长期保存”区分核心部件；用箭头方向区分输入与输出；用路径定位文件所在位置。</li>\n<li><strong>最小示例：</strong> 键盘输入数据 → 程序装入内存并由 CPU 执行 → 屏幕输出结果；保存的 <code>.cpp</code> 源文件仍需编译才能得到可执行程序。</li>\n<li><strong>关键易错点：</strong> 改扩展名不会改变文件内容，程序也不是一直直接在硬盘上运行；触摸屏等设备还可能同时承担输入和输出。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>计算机不只是桌面上的主机和显示器。手机、自助挂号机、汽车导航和学校成绩系统也在接收、保存、处理和输出信息。要看懂它们怎样工作，可以先记住一条主线：</p>\n<pre><code>外部信息 → 输入 → 存储与处理 → 输出 → 外部</code></pre>\n<p>本页对应 GESP C++ 一级大纲中的“计算机基础知识”，重点回答六个问题：</p>\n<ol>\n<li>计算机为什么从专门的计算工具发展成了通用信息处理工具？</li>\n<li>硬件和软件怎样配合，CPU、内存和硬盘各做什么？</li>\n<li>怎样根据信息流向判断输入设备和输出设备？</li>\n<li>为什么计算机内部使用 0 和 1 表示信息？</li>\n<li>操作系统与普通应用软件有什么区别？</li>\n<li>文件、目录、路径和扩展名分别说明什么？</li>\n</ol>\n<p>这是一页纯概念知识，不需要用 C++ 程序来证明。后文会用一个贯穿场景把概念落到具体过程：<code>answer.cpp</code> 保存在磁盘中；编译后得到可执行程序；运行时，可执行程序被装入内存，CPU 执行指令，键盘提供输入，屏幕显示输出。一级不学习硬件电路、复杂进制转换或操作系统内部算法。</p>\n<h2 id="01-计算机的发展与应用">01 计算机的发展与应用</h2>\n<h3 id="从计算数字到处理信息">从“计算数字”到“处理信息”</h3>\n<p>早期电子计算机体积大、耗电多，主要承担科学计算等专门任务。随着电子器件变小、速度提高、成本下降，计算机逐渐进入学校、家庭、医院、工厂和交通系统。它能处理的内容也从数字扩展到文字、图片、声音和网络数据。</p>\n<p>按主要电子器件粗略划分，可以看到这样的变化：</p>\n<table>\n<thead>\n<tr>\n<th>大致阶段</th>\n<th>主要电子器件或形态</th>\n<th>带来的变化</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>早期电子计算机</td>\n<td>电子管</td>\n<td>机器庞大、耗电高，主要处理专门计算</td>\n</tr>\n<tr>\n<td>晶体管计算机</td>\n<td>晶体管</td>\n<td>体积缩小，可靠性和速度提高</td>\n</tr>\n<tr>\n<td>集成电路计算机</td>\n<td>集成电路</td>\n<td>更多元件集中到芯片中，应用范围扩大</td>\n</tr>\n<tr>\n<td>微处理器与网络时代</td>\n<td>大规模、超大规模集成电路</td>\n<td>个人计算机、移动设备和网络服务普及</td>\n</tr>\n</tbody>\n</table>\n<p>1946 年公开运行的 ENIAC 是著名的早期电子数字计算机。一级不要求背诵大量年份或机器型号，理解发展方向更重要：电子器件越来越小，处理速度和可靠性不断提高，计算机能承担的任务也越来越多。</p>\n<h3 id="判断计算机应用要看信息过程">判断“计算机应用”要看信息过程</h3>\n<p>医院自助挂号机的外形不像普通台式电脑，但它会完成下面的工作：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>正在发生的事情</th>\n<th>信息处理作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>读取证件，接收患者选择的科室</td>\n<td>输入</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>查找科室和号源</td>\n<td>处理</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>保存挂号结果</td>\n<td>存储</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td>显示结果并打印凭条</td>\n<td>输出</td>\n</tr>\n</tbody>\n</table>\n<p>它能按照程序自动完成一组信息处理任务，因此属于计算机应用。判断时不要只看设备有没有键盘、机箱或大屏幕，要看它是否在程序控制下接收、保存、处理或输出信息。</p>\n<p>学校成绩系统、导航软件和工厂自动控制也遵循相同思路。它们的任务不同，处理的信息不同，但都离不开“输入—存储与处理—输出”这条主线。</p>\n<h2 id="02-计算机的组成">02 计算机的组成</h2>\n<h3 id="计算机系统需要硬件和软件共同工作">计算机系统需要硬件和软件共同工作</h3>\n<p><strong>硬件</strong>是组成计算机的实体设备，能看见或触摸到；<strong>软件</strong>是指挥硬件完成任务的程序和相关数据。只有硬件没有软件，设备不知道要完成什么任务；只有软件没有硬件，程序也没有实际运行的地方。</p>\n<p>计算机硬件常按五种基本作用来认识：</p>\n<table>\n<thead>\n<tr>\n<th>基本组成</th>\n<th>主要作用</th>\n<th>常见例子或位置</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>运算器</td>\n<td>完成算术运算和逻辑判断</td>\n<td>CPU 内部的重要部分</td>\n</tr>\n<tr>\n<td>控制器</td>\n<td>控制指令按要求执行</td>\n<td>CPU 内部的重要部分</td>\n</tr>\n<tr>\n<td>存储器</td>\n<td>保存程序和数据</td>\n<td>内存、硬盘、固态硬盘</td>\n</tr>\n<tr>\n<td>输入设备</td>\n<td>把外部信息送进计算机</td>\n<td>键盘、鼠标、麦克风</td>\n</tr>\n<tr>\n<td>输出设备</td>\n<td>把处理结果送到外部</td>\n<td>显示器、扬声器、打印机</td>\n</tr>\n</tbody>\n</table>\n<p>CPU，也就是中央处理器，通常包含运算器和控制器。它负责取出并执行程序指令、完成主要运算和控制工作。CPU 很重要，但它不是整台计算机，也不负责长期保存所有文件。</p>\n<h3 id="cpu内存和外部存储各有职责">CPU、内存和外部存储各有职责</h3>\n<p>“存储器”是一个大类。一级最容易混淆的是内存与硬盘、固态硬盘等外部存储设备：</p>\n<table>\n<thead>\n<tr>\n<th>对比项</th>\n<th>内存</th>\n<th>硬盘、固态硬盘等外部存储</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>主要用途</td>\n<td>暂时放置正在运行的程序和数据</td>\n<td>长期保存文件和程序</td>\n</tr>\n<tr>\n<td>断电后的常见情况</td>\n<td>内容通常丢失</td>\n<td>已保存内容通常仍在</td>\n</tr>\n<tr>\n<td>与运行的关系</td>\n<td>CPU 执行程序时会频繁读取内存中的指令和数据</td>\n<td>程序运行前通常先从这里加载到内存</td>\n</tr>\n<tr>\n<td>能否代替 CPU 运算</td>\n<td>不能</td>\n<td>不能</td>\n</tr>\n</tbody>\n</table>\n<p>“电脑有 16 GB 内存”和“电脑有 1 TB 固态硬盘”说的是两种不同的存储用途。内存像运行时展开材料的工作台，外部存储像长期存放材料的文件柜；这个比喻只帮助区分职责，不表示它们的内部结构真的相同。</p>\n<h3 id="一个程序真正怎样运行">一个程序真正怎样运行</h3>\n<p>假设磁盘中已经保存了源文件 <code>answer.cpp</code>，编译后得到了可执行程序。运行时会经历下面的过程：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>发生的事情</th>\n<th>主要负责者</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>answer.cpp</code> 和编译得到的可执行程序保存在磁盘中</td>\n<td>外部存储设备</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>操作系统把可执行程序和运行需要的数据装入内存</td>\n<td>操作系统、内存</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>CPU 从内存中取得指令和数据</td>\n<td>CPU、内存</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td>CPU 按指令完成计算或判断，结果暂时放回内存</td>\n<td>CPU、内存</td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td>结果通过显示器等设备交给用户</td>\n<td>输出设备</td>\n</tr>\n</tbody>\n</table>\n<p>源文件里保存的是人写的 C++ 代码，可执行程序才是编译后供计算机运行的程序。程序也不是一直直接在硬盘上执行：运行前通常要先装入内存，再由 CPU 取出指令并执行。</p>\n<p>可以用三个问题检查自己有没有混淆：谁负责执行和运算？CPU。谁暂时保存正在运行的程序和数据？内存。谁适合在断电后继续保存文件？硬盘、固态硬盘等外部存储设备。</p>\n<h2 id="03-输入设备与输出设备">03 输入设备与输出设备</h2>\n<h3 id="以计算机为中心看信息方向">以计算机为中心看信息方向</h3>\n<p>设备的名称和外形不一定能直接说明类别。更可靠的方法是先把“计算机系统”放在中间，再画信息箭头：</p>\n<pre><code>外部世界 ──信息进入──&gt; 计算机    输入外部世界 &lt;──信息离开── 计算机    输出</code></pre>\n<table>\n<thead>\n<tr>\n<th>设备</th>\n<th>信息怎样流动</th>\n<th>判断</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>键盘</td>\n<td>按键内容进入计算机</td>\n<td>输入设备</td>\n</tr>\n<tr>\n<td>鼠标</td>\n<td>移动和点击信息进入计算机</td>\n<td>输入设备</td>\n</tr>\n<tr>\n<td>麦克风</td>\n<td>声音进入计算机</td>\n<td>输入设备</td>\n</tr>\n<tr>\n<td>扫描仪</td>\n<td>纸面图像进入计算机</td>\n<td>输入设备</td>\n</tr>\n<tr>\n<td>显示器</td>\n<td>图像从计算机送给用户</td>\n<td>输出设备</td>\n</tr>\n<tr>\n<td>扬声器</td>\n<td>声音从计算机送给用户</td>\n<td>输出设备</td>\n</tr>\n<tr>\n<td>打印机</td>\n<td>文字或图像从计算机送到纸上</td>\n<td>输出设备</td>\n</tr>\n</tbody>\n</table>\n<p>这也解释了综合测试中的判断：麦克风负责采集声音，信息方向是“外部到计算机”，所以它不是输出设备。</p>\n<h3 id="同一设备可能承担两种作用">同一设备可能承担两种作用</h3>\n<p>触摸屏显示题目时，信息从计算机到用户，是输出；学生点击答案时，触摸位置进入计算机，是输入。同一块触摸屏可以同时具有输入和输出功能。</p>\n<p>多功能打印机也要按具体动作判断：扫描纸张时承担输入，打印文件时承担输出。题目只描述其中一步，就按照这一步的信息流向作答。</p>\n<p>硬盘负责存储，不要因为它既能读取又能写入，就在“计算机五大组成”的题目中机械地归为输入或输出设备。先看题目采用的是哪种分类标准，再根据设备在该过程中的主要职责判断。</p>\n<h2 id="04-信息表示与二进制">04 信息表示与二进制</h2>\n<h3 id="为什么使用-0-和-1">为什么使用 0 和 1</h3>\n<p>计算机的电子器件适合区分两种稳定状态。人们用 <code>0</code> 和 <code>1</code> 表示这两种状态，并规定怎样把它们组合起来表示数字、文字、图片和声音。二进制中的每一位只能是 0 或 1；这样的一个二进制位常称为一个“位”。</p>\n<p>这并不表示计算机只会处理数字 0 和 1。屏幕上的汉字、照片中的颜色和扬声器播放的声音，都会按照相应规则转换成二进制数据，再由计算机保存和处理。不同类型的信息使用不同的表示规则，具体编码方法会在后续知识中学习。</p>\n<h3 id="相同字符不一定表示相同数值">相同字符不一定表示相同数值</h3>\n<p>二进制采用“逢二进一”。例如二进制 <code>101</code> 可以按位置拆开：</p>\n<pre><code>二进制 101 = 1 × 4 + 0 × 2 + 1 × 1 = 十进制 5</code></pre>\n<p>二进制 <code>101</code> 与十进制 <code>101</code> 看起来都由字符 1、0、1 组成，数值却不同：前者表示十进制 5，后者表示一百零一。因此，读一个数时除了看写了哪些数字，还要确认它采用什么进制。</p>\n<p>一级只要求理解二进制由 0 和 1 组成，并能看懂这种简单的位置意义，不需要进行复杂进制转换。遇到 <code>10201</code> 这样的写法，可以立刻判断它不是合法的二进制数，因为其中出现了数字 2。</p>\n<h2 id="05-软件与操作系统">05 软件与操作系统</h2>\n<h3 id="系统软件与应用软件解决的问题不同">系统软件与应用软件解决的问题不同</h3>\n<p>软件让硬件按照确定的步骤完成任务。按主要用途，可以先分成两类：</p>\n<table>\n<thead>\n<tr>\n<th>类别</th>\n<th>主要职责</th>\n<th>常见例子</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>系统软件</td>\n<td>管理计算机资源，为其他软件提供运行环境</td>\n<td>Windows、Linux 等操作系统</td>\n</tr>\n<tr>\n<td>应用软件</td>\n<td>帮助用户完成某项具体任务</td>\n<td>浏览器、文字处理软件、编程 IDE、游戏</td>\n</tr>\n</tbody>\n</table>\n<p>操作系统属于系统软件。它不是输入设备、输出设备或编程语言。浏览器和 IDE 虽然功能很多，仍然是帮助用户完成具体任务的应用软件。</p>\n<h3 id="操作系统站在应用与硬件之间">操作系统站在应用与硬件之间</h3>\n<p>双击浏览器图标后，并不是浏览器自己直接接管整台计算机。操作系统先找到程序文件，把运行所需内容装入内存，分配处理时间，管理窗口和设备；浏览器再完成打开网页、显示内容等具体任务。</p>\n<pre><code>用户操作 → 应用软件 → 操作系统 → 硬件</code></pre>\n<table>\n<thead>\n<tr>\n<th>操作系统承担的工作</th>\n<th>在实际使用中的表现</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>管理文件和目录</td>\n<td>创建、复制、移动、重命名和删除文件</td>\n</tr>\n<tr>\n<td>管理程序</td>\n<td>启动程序、安排运行、结束无响应程序</td>\n</tr>\n<tr>\n<td>管理内存</td>\n<td>为正在运行的程序分配和回收空间</td>\n</tr>\n<tr>\n<td>管理设备</td>\n<td>让键盘、鼠标、显示器等能被程序使用</td>\n</tr>\n</tbody>\n</table>\n<p>判断软件类别时，可以问：“它主要是在管理整台计算机的共同资源，还是在帮助用户完成某项具体任务？”前者通常属于系统软件，后者通常属于应用软件。</p>\n<h2 id="06-文件目录与扩展名">06 文件、目录与扩展名</h2>\n<h3 id="文件保存内容目录组织文件路径说明位置">文件保存内容，目录组织文件，路径说明位置</h3>\n<p>这三个概念解决的是不同问题：</p>\n<table>\n<thead>\n<tr>\n<th>概念</th>\n<th>回答的问题</th>\n<th>例子</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>文件</td>\n<td>保存的内容是什么</td>\n<td><code>answer.cpp</code></td>\n</tr>\n<tr>\n<td>目录（文件夹）</td>\n<td>文件放在哪一组中</td>\n<td><code>gesp</code> 文件夹</td>\n</tr>\n<tr>\n<td>路径</td>\n<td>怎样找到这个文件</td>\n<td><code>D:\\gesp\\answer.cpp</code></td>\n</tr>\n</tbody>\n</table>\n<p>在路径 <code>D:\\gesp\\answer.cpp</code> 中：</p>\n<ul>\n<li><code>D:</code> 表示盘符；</li>\n<li><code>gesp</code> 是目录名；</li>\n<li><code>answer.cpp</code> 是文件名；</li>\n<li><code>.cpp</code> 是扩展名，<code>answer</code> 是扩展名前的主文件名。</li>\n</ul>\n<p>目录本身不是一份源代码，路径也不是文件内容。路径只是对位置的描述，就像地址帮助我们找到房间，但地址本身不是房间里的物品。</p>\n<h3 id="扩展名是线索不是变身开关">扩展名是线索，不是“变身开关”</h3>\n<table>\n<thead>\n<tr>\n<th>扩展名</th>\n<th>常见含义</th>\n<th>里面通常保存什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>.cpp</code></td>\n<td>C++ 源代码文件</td>\n<td>人能阅读和编辑的 C++ 代码文字</td>\n</tr>\n<tr>\n<td><code>.exe</code></td>\n<td>Windows 中常见的可执行文件</td>\n<td>编译后可由系统加载运行的程序</td>\n</tr>\n<tr>\n<td><code>.txt</code></td>\n<td>文本文件</td>\n<td>普通文字</td>\n</tr>\n<tr>\n<td><code>.png</code></td>\n<td>图片文件</td>\n<td>按 PNG 格式组织的图像数据</td>\n</tr>\n</tbody>\n</table>\n<p>扩展名能帮助操作系统和用户判断文件类型，但改名不会自动改变文件内部的数据。把 <code>notes.txt</code> 改成 <code>notes.cpp</code>，里面仍然是原来的文字；只有内容符合 C++ 语法，编译器才可能把它当作正确源代码处理。把图片改名为 <code>.txt</code>，也不会让图片内容自动变成文章。</p>\n<h3 id="c-源文件与可执行程序不是同一个文件">C++ 源文件与可执行程序不是同一个文件</h3>\n<table>\n<thead>\n<tr>\n<th>对比项</th>\n<th>C++ 源文件</th>\n<th>可执行程序</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>常见名称</td>\n<td><code>answer.cpp</code></td>\n<td>Windows 中常见 <code>answer.exe</code></td>\n</tr>\n<tr>\n<td>内容作用</td>\n<td>保存程序员写的 C++ 代码</td>\n<td>保存编译后供计算机运行的程序</td>\n</tr>\n<tr>\n<td>主要操作</td>\n<td>阅读、编辑、保存、编译</td>\n<td>由操作系统加载并运行</td>\n</tr>\n<tr>\n<td>修改后怎样生效</td>\n<td>修改源文件后需要重新编译</td>\n<td>运行的是最近一次成功生成的版本</td>\n</tr>\n</tbody>\n</table>\n<p>在 Windows 中，可执行文件常用 <code>.exe</code>；在 Linux 中，可执行文件不一定有固定扩展名，所以不能把“所有可执行程序都必须以 <code>.exe</code> 结尾”当成通用规则。</p>\n<p>还有一个常见现象：你改了 <code>answer.cpp</code>，却没有保存或重新编译，运行结果仍然和以前一样。原因是旧的可执行程序没有自动变成新版本。下一页“集成开发环境”会继续讲编辑、保存、编译和运行之间的区别。</p>\n<h2 id="判断计算机基础概念的统一顺序">判断计算机基础概念的统一顺序</h2>\n<p>概念题看起来很零散，其实可以按下面的顺序逐步判断：</p>\n<ol>\n<li><strong>先找信息方向。</strong> 外部信息进入计算机是输入，计算机把结果送到外部是输出。</li>\n<li><strong>再找当前职责。</strong> 执行和运算看 CPU，运行时临时保存看内存，长期保存文件看硬盘等外部存储。</li>\n<li><strong>检查软件在管什么。</strong> 管理硬件、文件和程序的是操作系统；完成浏览网页、编辑代码等具体任务的是应用软件。</li>\n<li><strong>确认对象和位置。</strong> 文件保存内容，目录组织文件，路径描述位置，扩展名提示常见类型。</li>\n<li><strong>最后检查表示规则。</strong> 二进制只能出现 0 和 1；同样的字符采用不同进制时，数值可能不同。</li>\n</ol>\n<p>遇到陌生设备或新场景时，不必死记名称。把题目中的动作放回这五步，通常就能找到判断依据。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>查询词</th>\n<th>直接结论</th>\n<th>最快自查方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>计算机应用</td>\n<td>在程序控制下接收、保存、处理或输出信息</td>\n<td>找完整的信息处理过程</td>\n</tr>\n<tr>\n<td>CPU</td>\n<td>主要负责执行指令和运算</td>\n<td>问“谁在处理”</td>\n</tr>\n<tr>\n<td>内存</td>\n<td>临时保存正在运行的程序和数据</td>\n<td>问“断电后通常还在不在”</td>\n</tr>\n<tr>\n<td>外部存储</td>\n<td>适合长期保存文件</td>\n<td>找硬盘、固态硬盘等设备</td>\n</tr>\n<tr>\n<td>输入设备</td>\n<td>把外部信息送进计算机</td>\n<td>画指向计算机的箭头</td>\n</tr>\n<tr>\n<td>输出设备</td>\n<td>把结果送到外部</td>\n<td>画离开计算机的箭头</td>\n</tr>\n<tr>\n<td>操作系统</td>\n<td>管理硬件、文件和程序</td>\n<td>区分“管理资源”和“完成任务”</td>\n</tr>\n<tr>\n<td>二进制</td>\n<td>每一位只能是 0 或 1</td>\n<td>检查是否出现其他数字</td>\n</tr>\n<tr>\n<td>路径</td>\n<td>描述文件或目录的位置</td>\n<td>逐段找到盘符、目录和文件名</td>\n</tr>\n<tr>\n<td><code>.cpp</code></td>\n<td>C++ 源代码文件</td>\n<td>与编译后的可执行程序分开</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>一名学生在 Windows 电脑上完成下面的操作：</p>\n<ol>\n<li>在 IDE 中编辑 <code>D:\\gesp\\answer.cpp</code> 并保存；</li>\n<li>编译后得到 <code>answer.exe</code>；</li>\n<li>运行程序，用键盘输入数据；</li>\n<li>程序完成处理，并把结果显示在屏幕上。</li>\n</ol>\n<p>请判断每一步涉及的主要概念。</p>\n<h3 id="先沿过程找证据">先沿过程找证据</h3>\n<table>\n<thead>\n<tr>\n<th>场景线索</th>\n<th>判断</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>D:\\gesp\\answer.cpp</code></td>\n<td>路径指向一个 C++ 源文件</td>\n<td><code>D:</code> 是盘符，<code>gesp</code> 是目录，<code>.cpp</code> 提示源文件类型</td>\n</tr>\n<tr>\n<td>点击“保存”</td>\n<td>文件内容被写入外部存储</td>\n<td>需要在断电后继续保留</td>\n</tr>\n<tr>\n<td>得到 <code>answer.exe</code></td>\n<td>产生 Windows 中常见的可执行文件</td>\n<td>它与人编辑的 <code>.cpp</code> 源文件职责不同</td>\n</tr>\n<tr>\n<td>运行 <code>answer.exe</code></td>\n<td>程序先装入内存，再由 CPU 执行</td>\n<td>内存提供运行空间，CPU 负责指令和运算</td>\n</tr>\n<tr>\n<td>键盘输入数据</td>\n<td>输入</td>\n<td>信息从学生进入计算机</td>\n</tr>\n<tr>\n<td>屏幕显示结果</td>\n<td>输出</td>\n<td>信息从计算机送给学生</td>\n</tr>\n<tr>\n<td>分配内存、管理文件和设备</td>\n<td>操作系统的工作</td>\n<td>这些是整台计算机的共同资源</td>\n</tr>\n</tbody>\n</table>\n<h3 id="再换条件验证">再换条件验证</h3>\n<ul>\n<li>把键盘换成麦克风，仍然是输入，因为信息方向没有变。</li>\n<li>把显示器换成扬声器，仍然是输出，因为结果仍由计算机送向外部。</li>\n<li>关闭电脑后，已经保存的 <code>answer.cpp</code> 通常仍在磁盘中；运行时没有另行保存的内存数据通常会丢失。</li>\n<li>只把 <code>answer.cpp</code> 改名为 <code>answer.exe</code>，不会得到真正的可执行程序，因为改名没有改变文件内部内容。</li>\n</ul>\n<p>这个例子可以同时核对硬件职责、输入输出方向、操作系统作用和文件身份。判断依据来自实际过程，不依赖设备名称的死记。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>观察“手机语音助手回答天气”这一过程，先按“输入 → 处理/存储 → 输出”填写三步：麦克风接收语音属于输入，程序和 CPU 完成识别与查询属于处理，扬声器播报属于输出。再判断查询结果若要下次继续使用，应保存到内存还是外部存储。</p>\n<p><strong>成功标准：</strong> 能为每一步写出信息方向和部件职责，并说明长期保留应使用外部存储。完成后不看本段提示，独立完成页末“整页综合测试”；提交后逐题核对答案与解析，错题必须能重新用信息流向、部件职责或文件身份说明原因。</p>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li><strong>把计算机应用理解成“使用一台台式电脑”。</strong> 自助机、导航和自动控制也能完成信息处理。检查时找输入、存储、处理和输出，不看外壳。</li>\n<li><strong>把 CPU、内存和硬盘混为一谈。</strong> CPU 负责执行和运算，内存负责运行时临时保存，硬盘等外部存储负责长期保存。用“谁处理、谁临时放、谁长期留”逐项核对。</li>\n<li><strong>认为程序一直直接在硬盘上运行。</strong> 可执行程序通常先从外部存储装入内存，再由 CPU 取出指令执行。</li>\n<li><strong>只按设备名称判断输入输出。</strong> 触摸屏、多功能打印机可能同时具有两种功能；把题目中的具体动作画成信息箭头再判断。</li>\n<li><strong>把 Windows、Linux 当成应用软件或编程语言。</strong> 它们是操作系统，负责管理硬件、文件和程序。</li>\n<li><strong>认为二进制 <code>101</code> 就是十进制 101。</strong> 进制不同，位置代表的数值不同；先确认采用什么进制。</li>\n<li><strong>认为改扩展名就能改变文件内容。</strong> 扩展名只是类型线索，改名不会完成编译，也不会把图片变成文字。</li>\n<li><strong>把 <code>.cpp</code> 源文件和可执行程序当成同一个文件。</strong> 源文件供人编辑，编译后的程序供系统加载运行；修改源文件后还要保存并重新编译。</li>\n</ol>\n<h2 id="关联学习">关联学习</h2>\n<p>下一页的<a href="/gesp/level1/ide/">集成开发环境</a>会继续使用源文件、可执行程序和操作系统中的文件操作，并讲清编辑、保存、编译、运行与调试的区别。</p>\n<section aria-labelledby="related-knowledge-computer-basics"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-computer-basics" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><p>暂无前置知识</p></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> IDE 把编辑、保存、编译、运行和调试工具放在一起，但每一步仍有独立结果；修改源码后必须保存并重新编译，运行结果才来自新版本。</li>\n<li><strong>常用流程：</strong> 编辑源码 → 保存 <code>.cpp</code> → 编译并先处理首条有效报错 → 运行代表数据 → 对照预期 → 定位并复验。</li>\n<li><strong>最小示例：</strong> 把输出从 <code>1</code> 改成 <code>2</code> 后，先保存并编译；若直接运行旧程序仍看到 <code>1</code>，说明执行的不是最新构建结果。</li>\n<li><strong>关键易错点：</strong> “能运行”不等于答案正确；不要从最后一条连锁报错开始改，也不要同时随机修改多处而失去因果证据。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应 GESP C++ 一级大纲中的“集成开发环境”。这里不要求记住某一款软件的所有按钮，而是要学会一套换到其他 IDE 也能使用的工作方法。</p>\n<p>学完本页，你应当能回答三个问题：</p>\n<ol>\n<li><strong>IDE 的各个区域分别在处理什么？</strong></li>\n<li><strong>编辑、保存、编译和运行为什么不能互相代替？</strong></li>\n<li><strong>程序出错时，怎样根据现象找到下一步检查位置？</strong></li>\n</ol>\n<p>上一页已经介绍过源文件和可执行程序：源文件保存人写的 C++ 代码，可执行程序是编译器翻译后的结果。本页把它们放进一次真实的开发过程。下一页再具体学习 C++ 程序由哪些部分组成。</p>\n<h2 id="01-编辑与保存">01 编辑与保存</h2>\n<h3 id="ide-把开发工具放在同一个环境里">IDE 把开发工具放在同一个环境里</h3>\n<p><strong>IDE</strong> 是 Integrated Development Environment 的缩写，中文叫“集成开发环境”。“集成”的意思是：它把编写、编译、运行和调试程序时常用的工具集中在一个界面里，让我们不用频繁切换软件。</p>\n<p>常见 IDE 通常包含下面几个区域。不同软件的名称和位置可能不同，但它们承担的工作相近。</p>\n<table>\n<thead>\n<tr>\n<th>常见区域</th>\n<th>主要作用</th>\n<th>在哪里找证据</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>文件或项目区</td>\n<td>查看、新建和切换源文件</td>\n<td>文件名、文件夹位置、<code>.cpp</code> 后缀</td>\n</tr>\n<tr>\n<td>代码编辑区</td>\n<td>输入和修改源代码</td>\n<td>光标所在行、行号、尚未保存的修改标记</td>\n</tr>\n<tr>\n<td>编译信息区</td>\n<td>显示编译是否成功以及错误信息</td>\n<td>第一条错误、行号、错误附近的提示</td>\n</tr>\n<tr>\n<td>运行窗口或终端</td>\n<td>向程序输入数据，查看实际输出</td>\n<td>输入内容、输出内容、程序是否正常结束</td>\n</tr>\n<tr>\n<td>调试区</td>\n<td>暂停程序并观察变量等运行状态</td>\n<td>当前执行位置、变量当时的值</td>\n</tr>\n</tbody>\n</table>\n<p>IDE 只是把工具放到了一起，并没有把几个动作变成同一件事。按下“保存”不会自动证明程序正确；看到编辑器里有新代码，也不代表正在运行的已经是新版本。</p>\n<h3 id="编辑状态和磁盘文件不是同一个状态">编辑状态和磁盘文件不是同一个状态</h3>\n<p>在编辑器里敲下文字，先改变的是<strong>编辑区中的内容</strong>。执行保存后，这些内容才写入磁盘中的 <code>.cpp</code> 源文件。</p>\n<p>可以把它理解成写作业：</p>\n<ul>\n<li>编辑，是在草稿上改字；</li>\n<li>保存，是把当前内容正式写进文件；</li>\n<li>编译，是让编译器读取文件并检查、翻译；</li>\n<li>运行，是执行最近一次成功编译得到的程序。</li>\n</ul>\n<p>这四步处理的对象不同：</p>\n<table>\n<thead>\n<tr>\n<th>动作</th>\n<th>主要处理什么</th>\n<th>成功后得到什么</th>\n<th>不能代替什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编辑</td>\n<td>编辑器中的代码文字</td>\n<td>一次尚待保存的修改</td>\n<td>不能代替保存</td>\n</tr>\n<tr>\n<td>保存</td>\n<td>当前编辑内容</td>\n<td>磁盘中的最新源文件</td>\n<td>不能检查程序逻辑</td>\n</tr>\n<tr>\n<td>编译</td>\n<td>已保存的源文件</td>\n<td>新的可执行程序，或编译错误</td>\n<td>不能证明答案正确</td>\n</tr>\n<tr>\n<td>运行</td>\n<td>最近一次成功编译的可执行程序</td>\n<td>实际输出或运行现象</td>\n<td>不能自动修改源代码</td>\n</tr>\n</tbody>\n</table>\n<h3 id="为什么改了代码结果却可能没变">为什么改了代码，结果却可能没变</h3>\n<p>假设 <code>answer.cpp</code> 原来会输出 3。你在编辑器中把它改成输出 5，却忘了保存。编译器读取磁盘时，看到的仍可能是旧文件，于是新编译的程序仍输出 3。</p>\n<p>还有另一种情况：你已经保存，却没有重新编译，直接运行了旧的可执行程序，结果也可能还是 3。</p>\n<p>所以，“代码明明改了，结果怎么没变”不能只盯着代码。先检查这两个问题：</p>\n<ol>\n<li>修改是否已经保存到源文件？</li>\n<li>保存后是否重新编译成功？</li>\n</ol>\n<h2 id="02-编译">02 编译</h2>\n<h3 id="编译器做了什么">编译器做了什么</h3>\n<p>编译器读取源代码，检查它是否符合 C++ 的规则，并把能够通过检查的代码翻译成可执行程序。</p>\n<pre><code>已保存的 .cpp 源文件          ↓ 编译器读取、检查和翻译编译成功：得到新的可执行程序编译失败：得到错误信息，不会得到这次的新程序</code></pre>\n<p>编译失败时，旧的可执行程序可能还留在电脑里。此时如果 IDE 仍允许运行，你看到的可能是旧版本结果。因此，不能把“运行窗口弹出来了”误当成“刚才的修改已经编译成功”。</p>\n<p>C++ 主要采用“先编译，再运行”的方式。有些语言会使用解释器边读取边执行；一级只需知道两者的基本区别，不需要研究解释器内部怎样工作。</p>\n<h3 id="怎样阅读编译错误">怎样阅读编译错误</h3>\n<p>少写分号、括号不配对、名字拼错、使用没有定义的变量，都可能让编译失败。面对多条报错时，可以按这个顺序检查：</p>\n<ol>\n<li>先确认编译的是自己刚刚保存的文件。</li>\n<li>从<strong>第一条错误</strong>开始看，不要急着追最后一条。</li>\n<li>根据提示找到对应行，同时检查它的上一行。</li>\n<li>只修正一个明确的问题，再重新编译。</li>\n</ol>\n<p>为什么还要看上一行？编译器按顺序读取代码。有时上一行已经出错，但它读到下一行时才确定前面的结构无法成立。</p>\n<p>下面只观察“漏写分号”怎样影响编译，不要求现在掌握两条语句的完整含义：</p>\n<pre><code>int number = 3cout &lt;&lt; number &lt;&lt; endl;</code></pre>\n<p>第一行末尾缺少 <code>;</code>。编译器可能在读取第二行时才报告错误，所以提示位置不一定就是真正需要修改的位置。正确写法是：</p>\n<pre><code>int number = 3;cout &lt;&lt; number &lt;&lt; endl;</code></pre>\n<p>语句、分号和完整程序结构会在下一页详细学习。本页先记住：<strong>错误提示是线索，不是替你直接给出的答案。</strong></p>\n<h3 id="编译成功说明了什么">编译成功说明了什么</h3>\n<p>编译成功说明编译器没有发现阻止本次翻译继续进行的错误，并且生成了新的可执行程序。它不保证：</p>\n<ul>\n<li>计算公式符合题意；</li>\n<li>所有输入都能得到正确答案；</li>\n<li>输出的空格、换行和小数位数符合要求；</li>\n<li>程序运行时一定不会出现问题。</li>\n</ul>\n<p>因此，编译成功只是进入运行验证的起点。</p>\n<h2 id="03-运行与重新编译">03 运行与重新编译</h2>\n<h3 id="运行的是可执行程序">运行的是可执行程序</h3>\n<p>运行时，操作系统加载最近一次成功编译得到的可执行程序。程序再按代码规定读取输入、进行计算并产生输出。</p>\n<p>这里有一条很重要的版本关系：</p>\n<pre><code>源文件版本 A ──编译成功──→ 可执行程序版本 A源文件改成版本 B ──只保存──→ 可执行程序仍是版本 A源文件版本 B ──重新编译成功──→ 可执行程序才变成版本 B</code></pre>\n<p>假设旧程序计算 <code>3 + 4</code>，输出 7。你把运算改成乘法并保存，但没有重新编译，运行的仍是旧可执行程序，所以仍可能输出 7。只有重新编译成功后，新程序才会输出 12。</p>\n<p>这正是修改代码后必须重复“保存 → 编译 → 运行”的原因。</p>\n<h3 id="运行时要保存三份证据">运行时要保存三份证据</h3>\n<p>验证程序时，不要只记一句“好像不对”。至少记录：</p>\n<table>\n<thead>\n<tr>\n<th>证据</th>\n<th>要记录什么</th>\n<th>有什么用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>本次输入</td>\n<td>实际输入的每一个数据</td>\n<td>保证能够重复同一次测试</td>\n</tr>\n<tr>\n<td>期望输出</td>\n<td>根据题意手算的正确结果</td>\n<td>给实际输出一个判断标准</td>\n</tr>\n<tr>\n<td>实际输出</td>\n<td>程序真正显示的内容</td>\n<td>判断数值和格式哪里不一致</td>\n</tr>\n</tbody>\n</table>\n<p>例如，任务要求输入两个整数并输出它们的和：</p>\n<pre><code>输入：3 4期望输出：7实际输出：12</code></pre>\n<p>只看 12，无法判断它为什么错；把输入和期望结果放在旁边，就能提出更具体的检查方向：程序是否误做了乘法？两个输入变量是否读反或被改动？</p>\n<h3 id="重新编译前后怎样确认版本">重新编译前后怎样确认版本</h3>\n<p>每次修改后都执行同一条短流程：</p>\n<pre><code>修改代码 → 保存文件 → 重新编译       → 确认编译成功 → 用同一组输入运行 → 对照期望输出</code></pre>\n<p>如果编译失败，就先处理编译错误，不要拿旧程序的运行结果判断刚才的修改。只有出现“本次编译成功”的证据，运行结果才属于当前源代码。</p>\n<h2 id="04-错误类型与调试">04 错误类型与调试</h2>\n<h3 id="先按现象判断错误发生在哪个阶段">先按现象判断错误发生在哪个阶段</h3>\n<p>程序出错时，先判断它停在“编译前、运行中，还是结果核对”哪个阶段。分类的目的不是背名称，而是缩小检查范围。</p>\n<table>\n<thead>\n<tr>\n<th>错误类型</th>\n<th>常见现象</th>\n<th>首先检查</th>\n<th>一个边界提醒</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编译错误</td>\n<td>本次编译失败，没有生成新程序</td>\n<td>第一条错误、错误行和上一行、分号、括号、拼写、变量定义</td>\n<td>后面的多条错误可能由第一处错误连带产生</td>\n</tr>\n<tr>\n<td>运行错误</td>\n<td>已经开始运行，但异常结束或长时间不能结束</td>\n<td>输入是否符合要求、除数是否可能为 0、循环能否结束</td>\n<td>除以 0 等问题的具体表现可能因环境而异</td>\n</tr>\n<tr>\n<td>逻辑错误</td>\n<td>程序正常结束，但计算结果不符合题意</td>\n<td>公式、条件、执行顺序、变量变化、边界情况</td>\n<td>编译器通常不知道题目真正要求什么</td>\n</tr>\n<tr>\n<td>格式错误</td>\n<td>主要数值可能正确，但输出形式不符合要求</td>\n<td>多余文字、空格、换行、小数位数</td>\n<td>给人看着相近，不代表在线评测认为相同</td>\n</tr>\n</tbody>\n</table>\n<p>同一个现象背后可能有不止一个原因。例如“没有看到结果”，既可能是程序还在等待输入，也可能是循环没有结束，还可能是程序异常终止。调试时要继续收集证据，不能只凭第一感觉下结论。</p>\n<h3 id="wa-不是一种编译错误">WA 不是一种编译错误</h3>\n<p>在线评测中的 <strong>WA（Wrong Answer，答案错误）</strong> 表示程序已经运行并提交了输出，但输出没有通过评测。它是评测结果，不是 C++ 编译器给出的语法错误名称。</p>\n<p>WA 常见原因包括：</p>\n<ul>\n<li>公式或条件写错，属于逻辑问题；</li>\n<li>漏考虑 0、相等值等边界情况；</li>\n<li>输入理解错误；</li>\n<li>输出了多余提示文字，或空格、换行不符合要求。</li>\n</ul>\n<p>遇到 WA 时，应回到具体失败数据，比较“输入—期望输出—实际输出”，而不是再次盲目点击编译。</p>\n<h2 id="05-调试方法">05 调试方法</h2>\n<h3 id="调试不是随便改代码">调试不是随便改代码</h3>\n<p><strong>调试</strong>是根据现象收集证据、提出原因、修改并重新验证的过程。一级常用的方法有：</p>\n<ol>\n<li>用简单数据手算期望结果。</li>\n<li>逐步记录关键变量的变化。</li>\n<li>在关键位置临时输出变量值。</li>\n<li>检查 0、1、相等值和临界值等边界数据。</li>\n<li>修正后保存、重新编译，再用原数据复查。</li>\n</ol>\n<p>调试按钮只是工具之一。即使暂时不会使用断点和变量窗口，也能通过手算、变量记录和临时输出来定位许多一级程序的问题。</p>\n<h3 id="从假设到验证一次只检查一件事">从假设到验证，一次只检查一件事</h3>\n<p>仍看这个现象：输入 <code>3 4</code>，期望输出 7，实际输出 12。</p>\n<p>可以按下面的顺序调查：</p>\n<table>\n<thead>\n<tr>\n<th>步骤</th>\n<th>操作</th>\n<th>得到的证据或结论</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td>手算 <code>3 + 4</code></td>\n<td>期望结果确实是 7</td>\n</tr>\n<tr>\n<td>2</td>\n<td>确认本次输入</td>\n<td>两个输入分别是 3 和 4</td>\n</tr>\n<tr>\n<td>3</td>\n<td>观察输入变量</td>\n<td>变量中保存的也是 3 和 4</td>\n</tr>\n<tr>\n<td>4</td>\n<td>检查运算表达式</td>\n<td>发现误把加法写成乘法，<code>3 × 4</code> 正好是 12</td>\n</tr>\n<tr>\n<td>5</td>\n<td>只修正这个运算符</td>\n<td>修改目标明确，不同时改无关代码</td>\n</tr>\n<tr>\n<td>6</td>\n<td>保存并重新编译</td>\n<td>生成包含修正的新程序</td>\n</tr>\n<tr>\n<td>7</td>\n<td>再用 <code>3 4</code> 运行</td>\n<td>实际输出变为 7，与期望相同</td>\n</tr>\n<tr>\n<td>8</td>\n<td>再测 <code>0 5</code></td>\n<td>实际输出应为 5，补充检查边界情况</td>\n</tr>\n</tbody>\n</table>\n<p>这个过程比“看到错就同时改很多行”可靠。一次修改太多，即使结果碰巧正确，也很难知道真正起作用的是哪一处。</p>\n<h3 id="临时输出用完要清理">临时输出用完要清理</h3>\n<p>为了观察变量，可以暂时把变量值输出到运行窗口。但这些文字也会成为程序实际输出的一部分。</p>\n<p>如果题目只要求输出一个答案，提交时却多了一行“当前变量是……”，在线评测可能给出 WA。因此：</p>\n<ol>\n<li>调试时用临时输出收集证据；</li>\n<li>确认原因并修正；</li>\n<li>删除临时输出；</li>\n<li>重新编译；</li>\n<li>最后按题目格式再运行一次。</li>\n</ol>\n<p>注释的具体写法会在下一页“程序基本结构”中讲解。现在只需知道，调试信息不能混进最终答案。</p>\n<h2 id="第一次开发的完整闭环">第一次开发的完整闭环</h2>\n<p>第一次独立编写程序时，可以照着下面的顺序操作。它既包含成功路线，也包含出错后的返回路线。</p>\n<table>\n<thead>\n<tr>\n<th>顺序</th>\n<th>要做什么</th>\n<th>通过什么证据继续</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td>新建或打开正确的 <code>.cpp</code> 文件</td>\n<td>文件名、保存位置和后缀正确</td>\n</tr>\n<tr>\n<td>2</td>\n<td>在编辑区输入或修改代码</td>\n<td>修改内容与当前任务一致</td>\n</tr>\n<tr>\n<td>3</td>\n<td>保存源文件</td>\n<td>未保存标记消失，磁盘中是最新版本</td>\n</tr>\n<tr>\n<td>4</td>\n<td>执行编译</td>\n<td>明确看到本次编译成功或第一条错误</td>\n</tr>\n<tr>\n<td>5A</td>\n<td>如果编译失败，检查第一条错误及其附近</td>\n<td>修正后回到第 3 步</td>\n</tr>\n<tr>\n<td>5B</td>\n<td>如果编译成功，运行新程序</td>\n<td>运行窗口开始接收输入或显示输出</td>\n</tr>\n<tr>\n<td>6</td>\n<td>输入自己记录好的测试数据</td>\n<td>输入数量和顺序符合任务要求</td>\n</tr>\n<tr>\n<td>7</td>\n<td>比较期望输出与实际输出</td>\n<td>完全相同则继续测其他数据，不同则进入调试</td>\n</tr>\n<tr>\n<td>8</td>\n<td>根据错误类型收集证据并只修正明确问题</td>\n<td>修正后回到第 3 步，重新走完整闭环</td>\n</tr>\n</tbody>\n</table>\n<p>可以把它压缩成一句操作口令：</p>\n<blockquote>\n<p><strong>改完先保存，保存后编译，编译成功再运行；结果不对留证据，修正以后从保存重新开始。</strong></p>\n</blockquote>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>看到的现象</th>\n<th>先判断什么</th>\n<th>下一步</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编辑区有新内容，运行结果没变</td>\n<td>是否忘记保存</td>\n<td>保存后重新编译</td>\n</tr>\n<tr>\n<td>已保存，但仍是旧结果</td>\n<td>是否运行了旧可执行程序</td>\n<td>确认本次重新编译成功</td>\n</tr>\n<tr>\n<td>出现许多编译错误</td>\n<td>第一条错误是否引发后续错误</td>\n<td>查第一条错误、当前行和上一行</td>\n</tr>\n<tr>\n<td>能运行，但答案数值不对</td>\n<td>逻辑或边界是否错误</td>\n<td>手算期望值，记录变量变化</td>\n</tr>\n<tr>\n<td>数值正确却评测不通过</td>\n<td>输出格式是否精确匹配</td>\n<td>删除提示文字，检查空格和换行</td>\n</tr>\n<tr>\n<td>调试后本地正确，提交仍错</td>\n<td>临时输出是否还在</td>\n<td>清理后重新编译、复查</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>小灯完成了一个“输入两个整数，输出它们的和”的程序。他输入 <code>3 4</code>，程序成功编译并正常结束，但输出是 <code>12</code>。他应该怎样判断和处理？</p>\n<p>分析：</p>\n<ol>\n<li>编译已经成功，所以这不是编译错误。</li>\n<li>程序能正常结束，却没有得到题目要求的 7，首先判断为逻辑错误。</li>\n<li>记录输入 <code>3 4</code>、期望输出 7、实际输出 12。</li>\n<li>检查输入变量后，继续检查运算表达式；12 与 <code>3 × 4</code> 相符，很可能把加法误写成了乘法。</li>\n<li>修正后要保存、重新编译，并再次用 <code>3 4</code> 验证，不能直接运行旧程序。</li>\n<li>再用 <code>0 5</code> 等数据复查，确认结果仍符合题意。</li>\n</ol>\n<p>结论：判断错误类型只是起点，真正完成调试还要找到证据、修正原因并重新验证。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>把程序输出从 <code>Hello</code> 改成 <code>Hi</code>，运行后仍看到 <code>Hello</code>。按顺序检查：编辑区是否已保存、本次编译是否成功、运行的可执行程序是否来自本次编译。每完成一步只记录一个结论，不同时随机改代码。</p>\n<p><strong>成功标准：</strong> 最终运行结果必须是 <code>Hi</code>，并能指出旧结果出现在哪一步。随后不看提示独立完成页末“整页综合测试”；提交后根据解析把每个错误归入编辑/保存、编译、运行、逻辑或格式阶段。</p>\n<h2 id="常见错误">常见错误</h2>\n<h3 id="把-ide-当成会自动完成一切的工具">把 IDE 当成会自动完成一切的工具</h3>\n<p><strong>错误做法：</strong> 代码改完后直接看运行结果，默认 IDE 已经保存和重新编译。</p>\n<p><strong>为什么错：</strong> 编辑、保存、编译、运行分别处理不同对象，具体 IDE 的自动行为也可能不同。</p>\n<p><strong>怎样检查：</strong> 主动执行保存和编译，并确认本次编译成功后再运行。</p>\n<h3 id="从最后一条编译错误开始修改">从最后一条编译错误开始修改</h3>\n<p><strong>错误做法：</strong> 看到十几条错误，从列表最下面逐条修改。</p>\n<p><strong>为什么错：</strong> 第一处结构错误可能让编译器误解后续代码，从而产生连带提示。</p>\n<p><strong>怎样检查：</strong> 从第一条错误开始，同时看错误行和上一行；修正后重新编译，观察其余提示是否消失。</p>\n<h3 id="只凭能运行判断程序正确">只凭“能运行”判断程序正确</h3>\n<p><strong>错误做法：</strong> 程序没有报错就立即提交。</p>\n<p><strong>为什么错：</strong> 逻辑错误和格式错误不会一定阻止程序运行。</p>\n<p><strong>怎样检查：</strong> 先手算期望输出，再用普通数据和边界数据比较实际结果。</p>\n<h3 id="随机同时改很多处">随机同时改很多处</h3>\n<p><strong>错误做法：</strong> 不记录输入和现象，一次修改多个变量、公式和输出语句。</p>\n<p><strong>为什么错：</strong> 即使结果变化，也无法确认哪一处是根因，还可能引入新错误。</p>\n<p><strong>怎样检查：</strong> 根据证据提出一个具体原因，一次只修正与它有关的位置，再用同一组数据复查。</p>\n<h3 id="忘记删除调试输出">忘记删除调试输出</h3>\n<p><strong>错误做法：</strong> 调试时输出了变量值，提交前没有清理。</p>\n<p><strong>为什么错：</strong> 在线评测会把调试文字也当作正式答案的一部分。</p>\n<p><strong>怎样检查：</strong> 清理临时输出，重新编译，再逐字核对最终输出格式。</p>\n<h2 id="关联学习">关联学习</h2>\n<p><a href="/gesp/level1/program-structure/">程序基本结构</a>会继续解释头文件、<code>main</code>、语句、分号、代码块和注释。学会这些结构后，你就能读懂编译器经常指出的具体位置。</p>\n<section aria-labelledby="related-knowledge-ide"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-ide" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/computer-basics/" class="astro-mkwt4ymd">计算机基础</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 一份一级 C++ 完整程序通常由头文件、命名空间和 <code>main</code> 代码块组成；运行从 <code>main</code> 开始，普通语句按顺序执行。</li>\n<li><strong>常用骨架：</strong> <code>#include &lt;bits/stdc++.h&gt;</code> 提供常用声明，下一行写 <code>using namespace std;</code>，再用 <code>int main() { ... return 0; }</code> 放置主程序。</li>\n<li><strong>最小示例：</strong> <code>int main() { cout &lt;&lt; "Hello" &lt;&lt; endl; return 0; }</code> 从输出语句执行到 <code>return 0;</code> 后正常结束。</li>\n<li><strong>关键易错点：</strong> 预处理指令后不写分号，普通语句通常要用分号结束；大括号必须按层配对，未结束的多行注释会遮住后续代码。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应 GESP C++ 一级大纲中的“C++ 程序基本结构”。前一页已经走过编辑、保存、编译、运行的开发流程；这一页要看懂编译器正在处理的源代码由哪些部分组成。</p>\n<p>学完本页，你应当能解决三个核心问题：</p>\n<ol>\n<li><strong>怎样写出一份结构完整、可以编译运行的 C++ 程序？</strong></li>\n<li><strong>源代码中哪些内容参与编译，程序运行时又从哪里开始执行？</strong></li>\n<li><strong>怎样检查分号、括号、代码块和注释造成的结构问题？</strong></li>\n</ol>\n<p>本页只讲一级完整程序的公共骨架，不展开自定义函数、类、头文件内部实现等内容。输入输出语句会出现在代表程序中，但它怎样读取和组织数据，留到下一页专门讲。</p>\n<p>先看一张职责图：</p>\n<pre><code>源文件顶部：准备编译需要的声明与名称规则        ↓int main()：标出程序运行入口        ↓main 的 { }：装入真正依次执行的语句        ↓return 0;：正常结束 main</code></pre>\n<p>这几层缺一处关键结构，编译器就可能无法把源文件正确分段。</p>\n<h2 id="01-头文件与命名空间">01 头文件与命名空间</h2>\n<h3 id="为什么使用头文件">为什么使用头文件</h3>\n<p>C++ 标准库已经准备了许多常用工具。使用这些工具前，源文件需要引入相应的<strong>声明</strong>，让编译器知道名称和基本用法。</p>\n<p>一级完整程序在本站统一写：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;</code></pre>\n<p>第一行是预处理指令。<code>#include</code> 表示引入头文件，尖括号中是头文件名。这一行从 <code>#</code> 开始，行末<strong>不写分号</strong>。</p>\n<p><code>&lt;bits/stdc++.h&gt;</code> 汇集了许多常用标准库头文件，在常见 GCC 和在线评测环境中通常可用。它不是 C++ 标准规定每个编译器都必须提供的头文件；换到不支持它的环境时，需要改用实际需要的标准头文件。</p>\n<p>例如，标准头文件 <code>&lt;iostream&gt;</code> 提供 <code>cin</code>、<code>cout</code>、<code>endl</code> 等输入输出工具所需的声明：</p>\n<pre><code>#include &lt;iostream&gt;</code></pre>\n<p>因此，自测题中看到 <code>#include &lt;iostream&gt;</code> 时，应判断它是在引入输入输出相关声明，而不是在定义 <code>main</code> 或创建循环。本站完整程序继续按照项目约定使用 <code>&lt;bits/stdc++.h&gt;</code>。</p>\n<h3 id="using-namespace-std-解决什么问题"><code>using namespace std;</code> 解决什么问题</h3>\n<p>标准库中的名称位于名为 <code>std</code> 的命名空间中。命名空间可以把不同来源的名称分开，减少重名冲突。</p>\n<p>写上：</p>\n<pre><code>using namespace std;</code></pre>\n<p>以后就可以在本页程序里直接写 <code>cout</code>、<code>endl</code> 等标准库名称。这一行属于 using 指令，末尾<strong>需要分号</strong>。</p>\n<p>如果省略这行，标准库并没有消失，只是通常要在名称前写出 <code>std::</code>。一级主教学先统一保留 <code>using namespace std;</code>，不展开命名空间的更多用法。</p>\n<h3 id="两行的位置和执行边界">两行的位置和执行边界</h3>\n<p>头文件与命名空间相关内容写在 <code>main</code> 之前，因为编译器需要先认识这些声明和名称规则，再检查后面的代码。</p>\n<p>它们参与源文件的处理，却不是“运行后从上到下打印到屏幕的两条语句”：</p>\n<table>\n<thead>\n<tr>\n<th>内容</th>\n<th>主要在哪个阶段起作用</th>\n<th>会不会直接产生屏幕输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>#include &lt;...&gt;</code></td>\n<td>预处理阶段，引入头文件内容</td>\n<td>不会</td>\n</tr>\n<tr>\n<td><code>using namespace std;</code></td>\n<td>编译时帮助查找名称</td>\n<td>不会</td>\n</tr>\n<tr>\n<td><code>main</code> 代码块内的输出语句</td>\n<td>程序运行阶段</td>\n<td>会按语句要求输出</td>\n</tr>\n</tbody>\n</table>\n<p>这就解释了一个常见误会：源文件的第一行是 <code>#include</code>，不表示程序运行时先“执行一次 include”。程序真正开始运行的位置是 <code>main</code>。</p>\n<h2 id="02-关键字与基本组成">02 关键字与基本组成</h2>\n<h3 id="关键字和普通名称要分开">关键字和普通名称要分开</h3>\n<p><strong>关键字</strong>是 C++ 语言已经规定用途的单词，不能拿来给变量随意命名。一级常见关键字有 <code>int</code>、<code>double</code>、<code>char</code>、<code>bool</code>、<code>if</code>、<code>else</code>、<code>while</code>、<code>for</code>、<code>using</code>、<code>namespace</code>、<code>return</code>。</p>\n<p>有些常见单词不是关键字：</p>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>属于什么</th>\n<th>为什么容易误认</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>main</code></td>\n<td>有特殊规则的程序入口名称</td>\n<td>每个普通 C++ 程序都会看到它</td>\n</tr>\n<tr>\n<td><code>cout</code></td>\n<td>标准库提供的名称</td>\n<td>经常与输出语句一起出现</td>\n</tr>\n<tr>\n<td><code>endl</code></td>\n<td>标准库提供的输出操纵符</td>\n<td>看起来像语言命令，其实不是关键字</td>\n</tr>\n<tr>\n<td><code>number</code></td>\n<td>程序员可以选择的标识符</td>\n<td>只在当前程序中承担约定的含义</td>\n</tr>\n</tbody>\n</table>\n<p>所以，在 <code>double</code>、<code>else</code>、<code>while</code>、<code>endl</code> 四个选项中，<code>endl</code> 不是 C++ 关键字。</p>\n<h3 id="从外到内拆开-int-main">从外到内拆开 <code>int main()</code></h3>\n<p>看到 <code>int main()</code> 时，不要把它背成一串不能拆开的字符。每个部分都有职责：</p>\n<table>\n<thead>\n<tr>\n<th>部分</th>\n<th>本页先这样理解</th>\n<th>书写检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int</code></td>\n<td><code>main</code> 结束时交回一个整数状态码</td>\n<td>必须小写</td>\n</tr>\n<tr>\n<td><code>main</code></td>\n<td>程序规定的入口名称</td>\n<td>必须写成小写 <code>main</code>，不能写成 <code>Main</code> 或 <code>mian</code></td>\n</tr>\n<tr>\n<td><code>()</code></td>\n<td><code>main</code> 名称后的参数列表，本页保持空</td>\n<td>左右圆括号必须成对</td>\n</tr>\n<tr>\n<td><code>{}</code></td>\n<td><code>main</code> 的函数体，也就是一个代码块</td>\n<td>左右大括号必须成对，内部统一缩进</td>\n</tr>\n</tbody>\n</table>\n<p><code>int</code> 与代码块末尾的 <code>return 0;</code> 前后呼应：<code>return 0;</code> 把整数 0 作为结束状态交回运行环境，通常表示程序正常结束。</p>\n<p>C++ 允许 <code>main</code> 执行到末尾时省略显式的 <code>return 0;</code>，其效果仍按正常返回 0 处理。不过一级学习阶段建议把它完整写出，这样入口、执行语句和结束位置更容易辨认。</p>\n<h3 id="一份完整程序有哪些层">一份完整程序有哪些层</h3>\n<p>可以把一级程序分成三层：</p>\n<table>\n<thead>\n<tr>\n<th>层次</th>\n<th>典型内容</th>\n<th>主要职责</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编译准备层</td>\n<td><code>#include &lt;bits/stdc++.h&gt;</code></td>\n<td>提供标准库声明</td>\n</tr>\n<tr>\n<td>名称规则层</td>\n<td><code>using namespace std;</code></td>\n<td>允许直接使用标准库名称</td>\n</tr>\n<tr>\n<td>程序主体层</td>\n<td><code>int main() { ... }</code></td>\n<td>给出运行入口和实际执行内容</td>\n</tr>\n</tbody>\n</table>\n<p>“完整”并不等于代码很长。只要必要结构齐全，即使 <code>main</code> 中只有 <code>return 0;</code>，也可以编译和正常结束；只是它没有安排任何屏幕输出。</p>\n<h2 id="03-main-函数与执行顺序">03 main 函数与执行顺序</h2>\n<h3 id="main-是运行入口"><code>main</code> 是运行入口</h3>\n<p>编译器检查的是整个源文件，而运行环境启动程序时会寻找 <code>main</code>，进入它的大括号，再按照控制结构规定的顺序执行其中的语句。</p>\n<p>一级程序中，普通执行语句都应写在 <code>main</code> 的代码块内。把一条输出或计算语句随意放在 <code>main</code> 外面，编译器不会把它当成“提前执行的一步”，通常会报告结构错误。</p>\n<h3 id="从源代码到-main-经历了什么">从源代码到 <code>main</code> 经历了什么</h3>\n<p>上一页学过“保存 → 编译 → 运行”，现在把程序结构放进这条链：</p>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>处理对象</th>\n<th>发生的事</th>\n<th>是否已经在执行 <code>main</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>保存</td>\n<td>编辑器中的内容</td>\n<td>写入 <code>.cpp</code> 源文件</td>\n<td>否</td>\n</tr>\n<tr>\n<td>预处理</td>\n<td><code>#include</code> 等预处理指令</td>\n<td>处理头文件相关内容</td>\n<td>否</td>\n</tr>\n<tr>\n<td>编译与链接</td>\n<td>整个处理后的程序</td>\n<td>检查结构和名称，生成可执行程序</td>\n<td>否</td>\n</tr>\n<tr>\n<td>启动运行</td>\n<td>可执行程序</td>\n<td>运行环境找到程序入口</td>\n<td>即将进入</td>\n</tr>\n<tr>\n<td>执行主体</td>\n<td><code>main</code> 的代码块</td>\n<td>按顺序执行其中的语句</td>\n<td>是</td>\n</tr>\n<tr>\n<td>正常结束</td>\n<td><code>return 0;</code> 或到达 <code>main</code> 末尾</td>\n<td>结束 <code>main</code> 并交回状态</td>\n<td>已结束</td>\n</tr>\n</tbody>\n</table>\n<p>实际开发工具可能把“预处理、编译、链接”合并显示为一次“构建”或“编译”。一级只需掌握它们都发生在运行之前；编译成功后，才有本次源代码对应的新程序可运行。</p>\n<h3 id="怎样追踪一次运行">怎样追踪一次运行</h3>\n<p>追踪程序时，先把不会作为普通语句执行的部分排除，再看 <code>main</code> 内部：</p>\n<ol>\n<li><code>#include</code> 为编译准备声明，不会在运行窗口显示文字。</li>\n<li><code>using namespace std;</code> 帮助编译器查找名称，也不会产生输出。</li>\n<li>运行从 <code>main</code> 的左大括号进入。</li>\n<li>注释只给读者看，不作为普通语句执行。</li>\n<li><code>main</code> 内的普通语句按顺序执行。</li>\n<li>执行 <code>return 0;</code> 后，<code>main</code> 结束；写在它后面的普通语句不会再执行。</li>\n</ol>\n<p>因此，追踪输出顺序时，应先找到 <code>main</code>，再沿着它的实际执行路径检查。</p>\n<h2 id="04-语句分号与代码块">04 语句、分号与代码块</h2>\n<h3 id="分号结束一条普通语句">分号结束一条普通语句</h3>\n<p>语句描述程序要完成的一项操作。很多简单语句以分号 <code>;</code> 结束，例如：</p>\n<pre><code>int age = 12;return 0;</code></pre>\n<p>第一行定义一个整数并保存 12，第二行结束 <code>main</code> 并交回状态 0。两个分号都表示对应语句已经结束。</p>\n<p>但不能总结成“C++ 每一行都写分号”。是否需要分号取决于这段内容的语法职责：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>行末是否写分号</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>#include &lt;bits/stdc++.h&gt;</code></td>\n<td>不写</td>\n<td>它是预处理指令，不是普通语句</td>\n</tr>\n<tr>\n<td><code>using namespace std;</code></td>\n<td>要写</td>\n<td>using 指令以分号结束</td>\n</tr>\n<tr>\n<td><code>int main()</code></td>\n<td>此处不写</td>\n<td>后面紧跟函数体代码块</td>\n</tr>\n<tr>\n<td><code>cout &lt;&lt; ...;</code></td>\n<td>要写</td>\n<td>这是一条输出语句</td>\n</tr>\n<tr>\n<td><code>return 0;</code></td>\n<td>要写</td>\n<td>这是一条返回语句</td>\n</tr>\n<tr>\n<td>单独的 <code>{</code> 或 <code>}</code></td>\n<td>通常不写</td>\n<td>大括号负责围成代码块</td>\n</tr>\n</tbody>\n</table>\n<p>如果 <code>return 0</code> 后漏写分号，编译器可能在下一行或右大括号附近才确定结构不完整。仍应沿用上一页的方法：先看第一条错误，再检查提示行和上一行。</p>\n<h3 id="大括号围成代码块">大括号围成代码块</h3>\n<p>左大括号 <code>{</code> 表示代码块开始，右大括号 <code>}</code> 表示代码块结束。当前最重要的代码块是 <code>main</code> 的函数体。</p>\n<pre><code>int main() {    这里写属于 main 的语句}</code></pre>\n<p>左右大括号必须配对。漏写右大括号会让 <code>main</code> 的代码块结构不完整；多写一个右大括号，也会让编译器找不到与它配对的开始位置。</p>\n<p>缩进不改变 C++ 的执行结果，但能把从属关系显示出来。本站代码统一使用 4 个空格缩进：<code>main</code> 内的语句向右缩进一级，左右大括号保持对齐。这样检查配对时，比把所有内容挤在一列更可靠。</p>\n<h3 id="用结构配对检查编译错误">用“结构配对”检查编译错误</h3>\n<p>遇到程序结构相关报错时，可以从外到内检查：</p>\n<ol>\n<li><code>#include</code> 的 <code>#</code>、尖括号和头文件名是否完整；</li>\n<li><code>using namespace std;</code> 是否拼写正确并写了分号；</li>\n<li><code>int main()</code> 的大小写与圆括号是否正确；</li>\n<li><code>main</code> 的左右大括号是否配对；</li>\n<li>块内需要分号的普通语句是否写全；</li>\n<li>注释标记是否正确结束，是否意外遮住了代码。</li>\n</ol>\n<p>这套顺序从程序外层检查到内层。外层结构恢复以后，后面的连带报错往往会一起减少。</p>\n<h2 id="05-注释">05 注释</h2>\n<h3 id="两种基本注释">两种基本注释</h3>\n<p>注释用于给读代码的人补充说明，不作为普通语句执行。C++ 常见两种注释：</p>\n<pre><code>// 单行注释：从双斜杠开始，到这一行末尾结束\n/* 多行注释：从斜杠星号开始，   到星号斜杠结束 */</code></pre>\n<p>单行注释适合说明一行代码或一个短步骤。多行注释可以跨越多行，但必须同时有开始标记 <code>/*</code> 和结束标记 <code>*/</code>，而且不能随意嵌套另一组多行注释。</p>\n<p>注释行末不因为“像一句话”就补分号。若注释写在一条语句后面，需要分号的是前面的语句本身：</p>\n<pre><code>int age = 12; // age 保存学生年龄，初始值是 12</code></pre>\n<h3 id="注释不会执行但会影响代码边界">注释不会执行，但会影响代码边界</h3>\n<p>编译器不会把注释内容当作普通 C++ 语句执行。因此，注释里写“输出答案”，屏幕不会真的出现答案。</p>\n<p>反过来，如果把真正需要执行的代码放进注释，它也不会执行。例如临时注释掉一条输出语句后，程序可能正常结束，却没有预期输出。</p>\n<p>多行注释忘记写 <code>*/</code> 时，后面一大片代码都可能被继续当作注释，报错位置也可能远离真正遗漏标记的地方。检查时应先找到最近的 <code>/*</code>，确认它是否有对应的 <code>*/</code>。</p>\n<h3 id="注释应解释为什么或这一步做什么">注释应解释“为什么”或“这一步做什么”</h3>\n<p>好的注释帮助读者理解关键职责，例如“输出问候，确认程序已经进入 main”。如果注释只是把代码逐字重复一遍，就没有增加信息。</p>\n<p>调试时可以暂时注释某一行，但要注意两点：</p>\n<ol>\n<li>不要把必要的大括号一起注释掉，破坏代码块配对；</li>\n<li>提交前恢复正式逻辑，并重新编译运行，不能只凭记忆判断已经恢复。</li>\n</ol>\n<h2 id="把各部分合成一个完整程序">把各部分合成一个完整程序</h2>\n<h3 id="完成程序">完成程序</h3>\n<p>下面是本页唯一的完整程序。它的任务很简单：在屏幕上输出一行问候，用来验证程序骨架能够正常编译、进入 <code>main</code>、执行语句并结束。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    // 输出一句问候，确认程序已经进入 main    cout &lt;&lt; "Hello, C++!" &lt;&lt; endl;    return 0;}</code></pre>\n<p>运行时不需要输入。输出为：</p>\n<pre><code>Hello, C++!</code></pre>\n<p>这里先把 <code>cout &lt;&lt; "Hello, C++!" &lt;&lt; endl;</code> 整体看作“一条输出语句”：它把问候文字送到屏幕并换行，末尾分号结束这条语句。<code>cout</code> 从左到右怎样连接内容、<code>endl</code> 为什么换行，会在下一页详细讲解。</p>\n<h3 id="从编译到结束逐步追踪">从编译到结束逐步追踪</h3>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>当前部分</th>\n<th>实际发生的事</th>\n<th>屏幕变化</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>#include &lt;bits/stdc++.h&gt;</code></td>\n<td>在运行前准备常用标准库声明</td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>using namespace std;</code></td>\n<td>让编译器能直接查找标准库名称</td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><code>int main() {</code></td>\n<td>程序运行时进入 <code>main</code> 代码块</td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>// ...</code></td>\n<td>注释只供读者理解</td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td><code>cout &lt;&lt; ...;</code></td>\n<td>执行输出语句</td>\n<td>出现 <code>Hello, C++!</code> 并换行</td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td><code>return 0;</code></td>\n<td>立即结束 <code>main</code>，交回状态 0，程序结束</td>\n<td>不再新增文字，也不再执行后续语句</td>\n</tr>\n</tbody>\n</table>\n<p>这张表同时验证了三个结论：运行入口是 <code>main</code>，注释不作为普通语句执行，<code>return 0;</code> 后本程序正常结束。</p>\n<p>表中的右大括号 <code>}</code> 是源代码里标记函数体结束的边界，不是 <code>return 0;</code> 后还会继续执行的一条语句。</p>\n<h3 id="怎样验证这份骨架">怎样验证这份骨架</h3>\n<p>不要只看代码“长得像示例”，要真正走一遍开发流程：</p>\n<ol>\n<li>把完整代码保存为 <code>.cpp</code> 文件；</li>\n<li>使用 C++14 编译，确认没有编译错误；</li>\n<li>运行时不输入任何数据；</li>\n<li>检查屏幕是否只有 <code>Hello, C++!</code> 和行末换行；</li>\n<li>再检查程序是否正常结束，没有额外调试文字。</li>\n</ol>\n<p>如果漏写输出语句末尾的分号，应该在编译阶段发现；如果把输出语句放到 <code>return 0;</code> 后面，结构可能仍能编译，但那条语句不会执行。两种现象分别说明“语法结构”和“执行顺序”是不同问题。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>职责</th>\n<th>关键边界</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>#include &lt;bits/stdc++.h&gt;</code></td>\n<td>在常见 GCC/OJ 环境引入常用标准库声明</td>\n<td>行末不写分号；有的编译器不提供</td>\n</tr>\n<tr>\n<td><code>#include &lt;iostream&gt;</code></td>\n<td>引入标准输入输出相关声明</td>\n<td>C++ 标准头文件，行末不写分号</td>\n</tr>\n<tr>\n<td><code>using namespace std;</code></td>\n<td>允许本页直接使用标准库名称</td>\n<td>行末写分号</td>\n</tr>\n<tr>\n<td><code>int main()</code></td>\n<td>定义程序运行入口</td>\n<td><code>main</code> 小写，圆括号成对</td>\n</tr>\n<tr>\n<td><code>{ }</code></td>\n<td>围成 <code>main</code> 的代码块</td>\n<td>左右大括号成对，内部缩进 4 个空格</td>\n</tr>\n<tr>\n<td><code>return 0;</code></td>\n<td>正常结束 <code>main</code></td>\n<td>是普通语句，末尾写分号</td>\n</tr>\n<tr>\n<td><code>//</code></td>\n<td>注释到当前行末</td>\n<td>下一行恢复为普通代码</td>\n</tr>\n<tr>\n<td><code>/* */</code></td>\n<td>注释两个标记之间的内容</td>\n<td>必须闭合，不随意嵌套</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>下面几种现象分别应先检查哪里？</p>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>先检查</th>\n<th>判断依据</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编译器提示 <code>main</code> 附近结构不完整</td>\n<td><code>main</code> 的圆括号、大括号和拼写</td>\n<td>入口结构必须完整，<code>main</code> 必须小写</td>\n</tr>\n<tr>\n<td>第一条错误出现在右大括号附近</td>\n<td>上一条普通语句是否漏写分号</td>\n<td>编译器可能到代码块结束才发现语句未结束</td>\n</tr>\n<tr>\n<td>程序成功运行但没有问候文字</td>\n<td>输出语句是否被注释，或是否写在 <code>return 0;</code> 后</td>\n<td>注释不执行，返回后也不继续执行后续语句</td>\n</tr>\n<tr>\n<td>出现很多连续报错</td>\n<td>第一条错误是否破坏了外层结构</td>\n<td>漏括号或未闭合注释可能引发连带错误</td>\n</tr>\n</tbody>\n</table>\n<p>解决这类题的顺序是：先找程序入口与代码块，再查普通语句边界，最后判断注释和执行顺序。不要只凭报错数量猜程序“全都写错了”。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>检查这段骨架中的三个位置：头文件行后是否误写分号、<code>cout</code> 普通语句是否有分号、<code>main</code> 的左右大括号是否成对。按“预处理 → <code>main</code> 入口 → 语句 → 正常结束”顺序逐项打勾，再编译验证。</p>\n<p><strong>成功标准：</strong> 修正后的程序能够编译，运行时只输出预定文字，并从 <code>return 0;</code> 正常结束。完成后不看提示独立完成页末“整页综合测试”；提交后用首条报错和结构配对规则复核错题。</p>\n<h2 id="常见错误">常见错误</h2>\n<h3 id="把-main-写成近似拼法">把 <code>main</code> 写成近似拼法</h3>\n<p><strong>错误：</strong> 写成 <code>Main</code>、<code>mian</code>，或者漏掉一边圆括号。</p>\n<p><strong>原因：</strong> C++ 区分大小写，程序入口的名称和结构必须准确。</p>\n<p><strong>检查：</strong> 对照 <code>int main()</code> 逐字符确认，并配对左右圆括号。</p>\n<h3 id="认为每一行末尾都要分号">认为每一行末尾都要分号</h3>\n<p><strong>错误：</strong> 不分结构地逐行补分号，或者因为 <code>int main()</code> 后没有分号就认定它错误。</p>\n<p><strong>原因：</strong> 预处理指令、函数头、代码块和普通语句的职责不同。</p>\n<p><strong>检查：</strong> 先判断这一段属于哪种结构；普通语句和 using 指令要分号，<code>#include</code> 与紧跟代码块的 <code>int main()</code> 不这样写。</p>\n<h3 id="大括号数量相同却配对层次错误">大括号数量相同，却配对层次错误</h3>\n<p><strong>错误：</strong> 只数左、右大括号个数，不看它们围住了哪些语句。</p>\n<p><strong>原因：</strong> 大括号的数量和闭合层次都必须正确。</p>\n<p><strong>检查：</strong> 从 <code>main</code> 的左大括号开始向下追踪，使用统一缩进，让配对关系可见。</p>\n<h3 id="把注释当成待执行的说明">把注释当成待执行的说明</h3>\n<p><strong>错误：</strong> 在注释里写“输出答案”，却没有真正的输出语句。</p>\n<p><strong>原因：</strong> 注释只供读者阅读，不作为普通语句执行。</p>\n<p><strong>检查：</strong> 暂时忽略注释文字，沿 <code>main</code> 内真正的语句重新追踪执行路径。</p>\n<h3 id="多行注释没有结束">多行注释没有结束</h3>\n<p><strong>错误：</strong> 写了 <code>/*</code>，忘记补上 <code>*/</code>。</p>\n<p><strong>原因：</strong> 后续内容可能继续被当作注释，真正原因与报错行相距很远。</p>\n<p><strong>检查：</strong> 为每个 <code>/*</code> 找到对应的 <code>*/</code>，并避免嵌套多行注释。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>上一页<a href="/gesp/level1/ide/">集成开发环境</a>说明了怎样保存、编译、运行和调试这份源代码。下一页<a href="/gesp/level1/input-output/">输入输出语句</a>会拆解代表程序中的 <code>cout</code> 和 <code>endl</code>，并加入从外部读取数据的方法。</p>\n<section aria-labelledby="related-knowledge-program-structure"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-program-structure" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/ide/" class="astro-mkwt4ymd">开发环境</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 输入按题目顺序把数据交给变量，输出按代码顺序形成最终文本；OJ 比较的是实际输出，空格、换行和小数格式都属于答案。</li>\n<li><strong>常用语法：</strong> C++ 流使用 <code>cin &gt;&gt;</code>、<code>cout &lt;&lt;</code>；C 风格输入输出使用 <code>scanf</code>、<code>printf</code>，格式符必须与变量类型匹配。</li>\n<li><strong>最小示例：</strong> <code>cin &gt;&gt; a &gt;&gt; b; cout &lt;&lt; a + b &lt;&lt; \'\\n\';</code> 会读入两个数并只输出它们的和与一个换行。</li>\n<li><strong>关键易错点：</strong> 程序不会自动补空格，<code>scanf</code> 读普通变量通常要写地址符 <code>&amp;</code>，保留小数位数只改变显示形式，不会提高变量精度。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>程序有了基本结构以后，还需要让题目给出的数据进入程序，再把处理结果按照指定格式送出去。这个过程分成两端：</p>\n<table>\n<thead>\n<tr>\n<th>一端</th>\n<th>要解决的问题</th>\n<th>一级常用工具</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>标准输入</td>\n<td>数据从哪里来，依次保存到什么位置</td>\n<td><code>cin</code>、<code>scanf</code></td>\n</tr>\n<tr>\n<td>标准输出</td>\n<td>哪些内容以什么顺序组成最终答案</td>\n<td><code>cout</code>、<code>printf</code></td>\n</tr>\n</tbody>\n</table>\n<p>例如，输入依次给出学号和成绩：</p>\n<pre><code>101 87</code></pre>\n<p>题目要求先输出成绩，再输出学号：</p>\n<pre><code>87 101</code></pre>\n<p>这不只是“把两个数显示出来”。程序还要读对数据的数量和顺序，知道每个数据保存在哪里，再按照题目要求改变输出顺序，准确放入空格和换行。</p>\n<p>本页集中解决三个核心问题：</p>\n<ol>\n<li><code>cin</code>、<code>cout</code> 怎样从左到右处理数据；</li>\n<li><code>scanf</code>、<code>printf</code> 的格式符怎样与数据逐项对应；</li>\n<li>怎样让最终输出的字符和题目要求完全一致。</li>\n</ol>\n<p>本页先把 <code>studentId</code>、<code>score</code> 看作已经准备好的保存位置。变量怎样定义、数据类型怎样选择、表达式怎样计算，会在后面的课程中分别讲解。</p>\n<h2 id="01-cin-标准输入">01 cin 标准输入</h2>\n<p><code>cin</code> 的职责是从标准输入中读取数据，并把读到的内容保存到变量中。同一份程序因此可以处理不同的输入，而不需要把样例数字写死在代码里。</p>\n<h3 id="输入一个数据与多个数据">输入一个数据与多个数据</h3>\n<p>输入一个数据的基本写法是：</p>\n<pre><code>cin &gt;&gt; studentId; // 读取一个数据，保存到 studentId</code></pre>\n<p>需要连续读取多个数据时，继续按照题目给出的顺序写 <code>&gt;&gt;</code>：</p>\n<pre><code>cin &gt;&gt; studentId &gt;&gt; score; // 依次读取学号和成绩</code></pre>\n<p>这条语句可以按三个部分理解：</p>\n<table>\n<thead>\n<tr>\n<th>组成部分</th>\n<th>职责</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>cin</code></td>\n<td>指明数据来自标准输入</td>\n</tr>\n<tr>\n<td><code>&gt;&gt;</code></td>\n<td>取出下一个符合要求的数据</td>\n</tr>\n<tr>\n<td>右侧变量</td>\n<td>保存本次读到的数据</td>\n</tr>\n</tbody>\n</table>\n<p>一处 <code>&gt;&gt;</code> 对应一个保存位置。需要两个数据，就要准备两个位置并写出两次读取，不能用逗号代替后面的 <code>&gt;&gt;</code>。</p>\n<h3 id="cin-从左到右怎样读取">cin 从左到右怎样读取</h3>\n<p>假设代码是：</p>\n<pre><code>cin &gt;&gt; studentId &gt;&gt; score;</code></pre>\n<p>输入为：</p>\n<pre><code>101 87</code></pre>\n<p>读取过程如下：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>当前执行部分</th>\n<th style="text-align: right">读到的数据</th>\n<th>保存结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>&gt;&gt; studentId</code></td>\n<td style="text-align: right">101</td>\n<td><code>studentId = 101</code></td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>&gt;&gt; score</code></td>\n<td style="text-align: right">87</td>\n<td><code>score = 87</code></td>\n</tr>\n</tbody>\n</table>\n<p>变量名不会让 <code>cin</code> 自动寻找“像学号的数据”。真正决定对应关系的是代码中的位置：第一个数据给第一个变量，第二个数据给第二个变量，依次类推。</p>\n<h3 id="空格换行和制表符怎样分隔数据">空格、换行和制表符怎样分隔数据</h3>\n<p>对于本页使用的普通数字输入，<code>cin &gt;&gt;</code> 会先跳过数据前面的空格、制表符和换行，再读取下一个数据。因此下面三种输入都能得到相同结果：</p>\n<pre><code>101 87</code></pre>\n<pre><code>10187</code></pre>\n<pre><code>101     87</code></pre>\n<p>所以 <code>cin &gt;&gt; a &gt;&gt; b;</code> 表示“依次读取两个数据”，不表示“每一行只能读取一个数据”。以后学习整行文字输入时，换行会有不同作用，本页暂不展开。</p>\n<h3 id="什么时候不能按预期读完">什么时候不能按预期读完</h3>\n<p>检查 <code>cin</code> 时要同时看四件事：数据数量、先后顺序、目标类型和保存位置。</p>\n<ul>\n<li>**数据不足：**程序还没有拿到后面的数据。在控制台中通常会继续等待；如果输入文件已经结束，后续读取会失败。</li>\n<li>**顺序写反：**程序可能仍能读完，但数据进入了错误的变量。</li>\n<li>**类型不匹配：**需要整数的位置遇到 <code>*</code> 之类不能读成整数的内容时，读取会失败，不能把该变量当作有效输入，也不能假定它一定自动变成 0。</li>\n<li>**数据多余：**当前语句只读取需要的数量，后面的数据会留给后续读取语句。</li>\n</ul>\n<p>例如输入 <code>1 *</code> 时，两个整数位置不能全部读取成功；若第二个位置是字符，<code>*</code> 才可以作为一个字符读入。这里先认识“类型会影响读取规则”，各种类型会在“基本数据类型”一页系统学习。</p>\n<h2 id="02-cout-标准输出">02 cout 标准输出</h2>\n<p><code>cout</code> 的职责是把内容送到标准输出。它不会替我们猜测应该输出哪些文字、空格或标点，代码写了什么，最终文本才会出现什么。</p>\n<h3 id="cout-可以连接哪些内容">cout 可以连接哪些内容</h3>\n<p>例如：</p>\n<pre><code>cout &lt;&lt; "score=" &lt;&lt; score;</code></pre>\n<p>这条语句包含两类内容：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>输出的是什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>"score="</code></td>\n<td>双引号中的固定文字 <code>score=</code></td>\n</tr>\n<tr>\n<td><code>score</code></td>\n<td>变量当前保存的值</td>\n</tr>\n</tbody>\n</table>\n<p>如果 <code>score = 91</code>，最终形成的文本是 <code>score=91</code>。变量名 <code>score</code> 本身不会出现在屏幕上，除非把它写进双引号。</p>\n<h3 id="cout-从左到右怎样形成文本">cout 从左到右怎样形成文本</h3>\n<p>继续输出学号：</p>\n<pre><code>cout &lt;&lt; "score=" &lt;&lt; score &lt;&lt; ", id=" &lt;&lt; studentId;</code></pre>\n<p>当 <code>score = 91</code>、<code>studentId = 205</code> 时：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>本次加入的内容</th>\n<th>当前文本</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>固定文字 <code>score=</code></td>\n<td><code>score=</code></td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>score</code> 的值 91</td>\n<td><code>score=91</code></td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>固定文字 <code>, id=</code></td>\n<td><code>score=91, id=</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>studentId</code> 的值 205</td>\n<td><code>score=91, id=205</code></td>\n</tr>\n</tbody>\n</table>\n<p>每一处 <code>&lt;&lt;</code> 都把右侧内容接到当前文本末尾。判断输出时，应沿代码从左到右拼接，不能只看最后一个变量。</p>\n<h2 id="03-空格换行与输出格式">03 空格、换行与输出格式</h2>\n<p>输出格式决定最终答案“长什么样”。数值相同，但空格、换行、标点或顺序不同，仍然是不同的输出。</p>\n<h3 id="程序不会自动补空格">程序不会自动补空格</h3>\n<p>假设 <code>score = 91</code>、<code>studentId = 205</code>：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>实际输出</th>\n<th>区别</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>cout &lt;&lt; score &lt;&lt; studentId;</code></td>\n<td><code>91205</code></td>\n<td>中间没有空格</td>\n</tr>\n<tr>\n<td><code>cout &lt;&lt; score &lt;&lt; " " &lt;&lt; studentId;</code></td>\n<td><code>91 205</code></td>\n<td>中间恰好一个空格</td>\n</tr>\n<tr>\n<td><code>cout &lt;&lt; score &lt;&lt; "  " &lt;&lt; studentId;</code></td>\n<td><code>91  205</code></td>\n<td>中间有两个空格</td>\n</tr>\n</tbody>\n</table>\n<p>空格也属于双引号中的固定内容。题目要求几个空格，就要写出几个；<code>cout</code> 不会因为两个变量相邻而自动加空格。</p>\n<h3 id="endl-与换行符">endl 与换行符</h3>\n<p>下面两种写法都能在一级常见输出题中结束当前行：</p>\n<pre><code>cout &lt;&lt; score &lt;&lt; endl; // 输出成绩后换行cout &lt;&lt; studentId &lt;&lt; "\\n"; // 输出学号后换行</code></pre>\n<p><code>endl</code> 是换行写法；<code>"\\n"</code> 中的 <code>\\n</code> 是转义序列，也表示换行。双引号中的普通文字、空格和标点按写法输出，<code>\\n</code> 这样的转义序列则按特殊含义执行，不会在屏幕上显示反斜杠和字母 n。</p>\n<p>换行放在哪里，下一段内容就从哪里开始：</p>\n<pre><code>cout &lt;&lt; score &lt;&lt; " " &lt;&lt; studentId &lt;&lt; "\\n"; // 同一行输出两个值cout &lt;&lt; score &lt;&lt; "\\n" &lt;&lt; studentId &lt;&lt; "\\n"; // 两个值分成两行</code></pre>\n<h3 id="oj-会检查最终文本">OJ 会检查最终文本</h3>\n<p>OJ 通常只接收题目要求的结果。题目没有要求时，不要擅自输出 <code>请输入数据</code>、<code>答案是</code> 等提示文字。</p>\n<p>提交前逐项检查：</p>\n<ol>\n<li>输出的数据数量和顺序是否正确；</li>\n<li>固定文字、标点和大小写是否完全一致；</li>\n<li>数据之间需要几个空格；</li>\n<li>每一行在哪里结束；</li>\n<li>是否要求固定的小数位数；</li>\n<li>是否混入题目没有要求的内容。</li>\n</ol>\n<h2 id="04-scanf-与-printf">04 scanf 与 printf</h2>\n<p><code>scanf</code>、<code>printf</code> 是另一套常见输入输出方式。它们把数据的格式写进第一个双引号中，再让后面的输入位置或输出数据与格式符一一对应。</p>\n<h3 id="scanf-怎样按照格式读取">scanf 怎样按照格式读取</h3>\n<p>读取两个 <code>int</code> 数据可以写成：</p>\n<pre><code>scanf("%d%d", &amp;studentId, &amp;score);</code></pre>\n<table>\n<thead>\n<tr>\n<th>组成部分</th>\n<th>职责</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>scanf</code></td>\n<td>按格式从标准输入读取数据</td>\n</tr>\n<tr>\n<td><code>"%d%d"</code></td>\n<td>两个 <code>%d</code> 依次要求两个 <code>int</code> 数据</td>\n</tr>\n<tr>\n<td><code>&amp;studentId</code></td>\n<td>第一个数据写入 <code>studentId</code></td>\n</tr>\n<tr>\n<td><code>&amp;score</code></td>\n<td>第二个数据写入 <code>score</code></td>\n</tr>\n</tbody>\n</table>\n<p><code>scanf</code> 要把数据写进变量，因此普通输入位置前要写 <code>&amp;</code>。一级阶段先把它记作必要写法，不在本页展开地址和指针。</p>\n<h3 id="printf-怎样按照格式输出">printf 怎样按照格式输出</h3>\n<p>输出成绩和学号可以写成：</p>\n<pre><code>printf("%d %d\\n", score, studentId);</code></pre>\n<p>格式串从左到右决定最终文本：</p>\n<table>\n<thead>\n<tr>\n<th>格式串中的部分</th>\n<th>实际作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>第一个 <code>%d</code></td>\n<td>输出 <code>score</code> 的值</td>\n</tr>\n<tr>\n<td>中间空格</td>\n<td>输出一个普通空格</td>\n</tr>\n<tr>\n<td>第二个 <code>%d</code></td>\n<td>输出 <code>studentId</code> 的值</td>\n</tr>\n<tr>\n<td><code>\\n</code></td>\n<td>换行</td>\n</tr>\n</tbody>\n</table>\n<p><code>printf</code> 只读取变量的值用于显示，所以输出数据前不写 <code>&amp;</code>。</p>\n<h3 id="格式符必须与类型配对">格式符必须与类型配对</h3>\n<table>\n<thead>\n<tr>\n<th>数据类型</th>\n<th><code>scanf</code> 读取</th>\n<th><code>printf</code> 输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int</code></td>\n<td><code>%d</code></td>\n<td><code>%d</code></td>\n</tr>\n<tr>\n<td><code>long long</code></td>\n<td><code>%lld</code></td>\n<td><code>%lld</code></td>\n</tr>\n<tr>\n<td><code>float</code></td>\n<td><code>%f</code></td>\n<td><code>%f</code></td>\n</tr>\n<tr>\n<td><code>double</code></td>\n<td><code>%lf</code></td>\n<td><code>%f</code></td>\n</tr>\n<tr>\n<td><code>char</code></td>\n<td><code>%c</code></td>\n<td><code>%c</code></td>\n</tr>\n</tbody>\n</table>\n<p>最容易混淆的是 <code>double</code>：<code>scanf</code> 读取时使用 <code>%lf</code>，<code>printf</code> 输出时使用 <code>%f</code>。格式符数量、顺序或类型不对应，都可能让程序无法按预期工作。</p>\n<p>普通 <code>%c</code> 会直接读取下一个字符，空格和换行也可能被读入。如果题目要求下一个非空白字符，常见写法是在 <code>%c</code> 前放一个空格：</p>\n<pre><code>scanf(" %c", &amp;grade); // 先跳过空白，再读取一个字符</code></pre>\n<p>若题目本来就要读取空格字符，则不能这样跳过空白。</p>\n<h3 id="格式串中的固定字符也要匹配">格式串中的固定字符也要匹配</h3>\n<p>例如：</p>\n<pre><code>scanf("%d,%d", &amp;studentId, &amp;score);</code></pre>\n<p>这条语句对应的输入是：</p>\n<pre><code>205,91</code></pre>\n<p>英文逗号是格式串中的固定字符，输入中也必须在对应位置出现。类似地，<code>scanf("%d:%d:%d", ...)</code> 会要求三个整数之间出现冒号。固定字符只负责匹配，不会被保存到变量中。</p>\n<p>一级阶段选择 <code>cin</code>／<code>cout</code> 或 <code>scanf</code>／<code>printf</code> 中的一套写对、写一致即可。本页不展开底层缓冲、性能基准或输入失败后的恢复操作。</p>\n<h2 id="05-格式化输出与保留小数">05 格式化输出与保留小数</h2>\n<p>格式化输出只决定结果怎样显示。它不会改变变量原来保存的值，也不能修复此前已经发生的错误计算。</p>\n<h3 id="固定显示小数位数">固定显示小数位数</h3>\n<p>数值 <code>3.5</code> 按要求显示两位小数时，应输出 <code>3.50</code>。</p>\n<p>使用 <code>cout</code>：</p>\n<pre><code>cout &lt;&lt; fixed &lt;&lt; setprecision(2) &lt;&lt; 3.5 &lt;&lt; "\\n";</code></pre>\n<p>使用 <code>printf</code>：</p>\n<pre><code>printf("%.2f\\n", 3.5);</code></pre>\n<p>两种写法都输出：</p>\n<pre><code>3.50</code></pre>\n<p><code>fixed</code> 让 <code>setprecision(2)</code> 表示小数点后两位；<code>%.2f</code> 中的 <code>.2</code> 也表示小数点后两位。若后面还有更多数字，显示时会按规则舍入。例如 <code>3.456</code> 显示两位小数时得到 <code>3.46</code>。</p>\n<h3 id="先确认数值再检查显示">先确认数值，再检查显示</h3>\n<p>判断格式化输出时按两步走：</p>\n<ol>\n<li>先确认变量中保存的值是否正确；</li>\n<li>再根据 <code>fixed</code>、<code>setprecision</code> 或 <code>printf</code> 格式检查显示结果。</li>\n</ol>\n<p>把 <code>3.5</code> 显示成 <code>3.50</code>，变量并没有永久变成“只能保存两位小数”。反过来，若前面的计算已经得到错误数值，再漂亮的输出格式也不能把它改正确。</p>\n<h2 id="输入输出的统一检查顺序">输入输出的统一检查顺序</h2>\n<p>面对一段输入输出代码，可以按下面的顺序检查：</p>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>先问什么</th>\n<th>具体做法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>输入数量</td>\n<td>题目给几个数据，代码读几个</td>\n<td>给输入数据和读取位置分别编号</td>\n</tr>\n<tr>\n<td>输入对应</td>\n<td>每个数据按什么顺序、什么类型保存</td>\n<td>沿 <code>cin</code> 或 <code>scanf</code> 从左到右连线</td>\n</tr>\n<tr>\n<td>输出内容</td>\n<td>要输出变量值还是固定文字</td>\n<td>把每一处 <code>&lt;&lt;</code> 或格式串拆成片段</td>\n</tr>\n<tr>\n<td>输出格式</td>\n<td>空格、标点、换行和小数位数怎样要求</td>\n<td>手工拼出最终文本</td>\n</tr>\n<tr>\n<td>实际验证</td>\n<td>代码是否只对样例碰巧正确</td>\n<td>运行样例，再换一组顺序敏感的数据</td>\n</tr>\n</tbody>\n</table>\n<p>可以把它压缩成一句话：<strong>先给输入和保存位置连线，再把输出逐段拼起来，最后逐字符核对。</strong></p>\n<h2 id="典型例题">典型例题</h2>\n<p>输入依次给出一名学生的学号和成绩。程序需要交换它们的输出顺序：先输出成绩，再输出学号，中间恰好一个空格，末尾换行。</p>\n<h3 id="题目与输入输出">题目与输入输出</h3>\n<p>输入两个整数：</p>\n<pre><code>studentId score</code></pre>\n<p>输出一行：</p>\n<pre><code>成绩 学号</code></pre>\n<p>输入样例：</p>\n<pre><code>101 87</code></pre>\n<p>输出样例：</p>\n<pre><code>87 101</code></pre>\n<h3 id="先手工确定数据去向">先手工确定数据去向</h3>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">输入顺序</th>\n<th style="text-align: right">数据</th>\n<th>保存位置</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td style="text-align: right">101</td>\n<td><code>studentId</code></td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td style="text-align: right">87</td>\n<td><code>score</code></td>\n</tr>\n</tbody>\n</table>\n<p>输出时先取 <code>score</code> 的值 87，再加入一个字面量空格，接着取 <code>studentId</code> 的值 101，最后换行。输入顺序和输出顺序不同，正好可以检查程序有没有把两个保存位置写反。</p>\n<h3 id="逐步写出关键语句">逐步写出关键语句</h3>\n<p>先准备两个保存位置并按题目顺序读取：</p>\n<pre><code>int studentId; // 保存学号int score; // 保存当前成绩cin &gt;&gt; studentId &gt;&gt; score; // 依次读取学号和成绩</code></pre>\n<p>再按要求组成一行输出：</p>\n<pre><code>cout &lt;&lt; score &lt;&lt; " " &lt;&lt; studentId &lt;&lt; "\\n";</code></pre>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int studentId; // 保存输入中的学号    int score; // 保存输入中的当前成绩    cin &gt;&gt; studentId &gt;&gt; score; // 按题目顺序读取两个数据\n    cout &lt;&lt; score &lt;&lt; " " &lt;&lt; studentId &lt;&lt; "\\n"; // 先输出成绩，再输出学号    return 0;}</code></pre>\n<h3 id="执行过程">执行过程</h3>\n<p>输入 <code>101 87</code> 时：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>正在执行的部分</th>\n<th>保存位置</th>\n<th>当前输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>cin &gt;&gt; studentId</code></td>\n<td><code>studentId = 101</code></td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>&gt;&gt; score</code></td>\n<td><code>score = 87</code></td>\n<td>无</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>输出 <code>score</code></td>\n<td>两个变量都不改变</td>\n<td><code>87</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td>输出字面量空格 <code>" "</code></td>\n<td>两个变量都不改变</td>\n<td><code>87 </code></td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td>输出 <code>studentId</code></td>\n<td>两个变量都不改变</td>\n<td><code>87 101</code></td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td>输出 <code>"\\n"</code></td>\n<td>两个变量都不改变</td>\n<td>当前行结束</td>\n</tr>\n</tbody>\n</table>\n<p>这张表严格沿程序从左到右展开：输入阶段依次改变两个保存位置，输出阶段依次追加成绩、空格、学号和换行，不会改动两个变量。</p>\n<h3 id="用多组数据验证">用多组数据验证</h3>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th>正确输出</th>\n<th>主要检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>101 87</code></td>\n<td><code>87 101</code>，然后换行</td>\n<td>正式样例与交换顺序</td>\n</tr>\n<tr>\n<td><code>205 91</code></td>\n<td><code>91 205</code>，然后换行</td>\n<td>更换数据后仍按保存位置输出</td>\n</tr>\n<tr>\n<td>第一行 <code>7</code>，第二行 <code>100</code></td>\n<td><code>100 7</code>，然后换行</td>\n<td>换行也能分隔两个普通整数</td>\n</tr>\n</tbody>\n</table>\n<p>若程序只在第一组样例中正确，换一组学号和成绩后就输出反了，说明代码没有真正按保存位置和题目顺序组织输出。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>补全“读入两个整数并输出一行和”的关键语句。输入 <code>3 4</code> 时，先把 3、4 分别交给两个变量，再计算和；输出不得带提示文字，只保留结果和换行。</p>\n<p><strong>成功标准：</strong> 实际输出必须逐字等于 <code>7</code> 后接一个换行，不能多空格或说明文字。完成后不看提示独立完成页末“整页综合测试”；提交后逐题核对格式符、变量地址、空格、换行和小数位数。</p>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li><strong>把输入输出符号写反。</strong> <code>cin</code> 使用 <code>&gt;&gt;</code>，<code>cout</code> 使用 <code>&lt;&lt;</code>；方向写反会导致编译或语义错误。</li>\n<li><strong>写成 <code>cin &gt;&gt; a, b;</code>。</strong> 逗号不会代替第二个 <code>&gt;&gt;</code>，这条语句只让 <code>cin</code> 读取 <code>a</code>；多个变量要写成 <code>cin &gt;&gt; a &gt;&gt; b;</code>。</li>\n<li><strong>输入数量、顺序或类型不匹配。</strong> 给数据和保存位置编号，再逐项连线；读取失败后不要继续把变量当作有效输入。</li>\n<li><strong>输出漏空格或换行位置错误。</strong> 把预期结果按字符写出来，再沿 <code>cout</code> 或格式串逐段拼接。</li>\n<li><strong>擅自输出提示文字。</strong> OJ 没有要求的 <code>答案是</code>、<code>请输入</code> 都可能让结果错误。</li>\n<li><strong><code>scanf</code> 漏写 <code>&amp;</code> 或格式符配错。</strong> 从第一个格式符开始，与后面的每个输入位置和类型一一对应。</li>\n<li><strong>只写 <code>setprecision(2)</code>，没有配合 <code>fixed</code>。</strong> 此时不一定表示小数点后固定两位。</li>\n<li><strong>认为格式化会改变原值。</strong> 格式控制发生在输出阶段，只改变显示文本。</li>\n</ol>\n<h2 id="关联学习">关联学习</h2>\n<p><a href="/gesp/level1/variables-expressions/">程序基本概念</a>会说明变量如何定义、赋值和参与表达式；<a href="/gesp/level1/data-types/">基本数据类型</a>会解释 <code>int</code>、<code>double</code>、<code>char</code> 等类型为什么具有不同的读取规则。</p>\n<section aria-labelledby="related-knowledge-input-output"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-input-output" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/program-structure/" class="astro-mkwt4ymd">程序结构</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 变量由类型、名称和当前值组成；表达式先按括号与优先级得到结果，赋值再把结果保存到左侧变量。</li>\n<li><strong>常用语法：</strong> 定义并初始化可写 <code>int count = 0;</code>，固定值可写 <code>const int DAYS = 7;</code>，后续赋值写 <code>count = count + 1;</code>。</li>\n<li><strong>最小示例：</strong> <code>int a = 2; int b = a + 3;</code> 先取得 <code>a</code> 的当前值 2，算出 5，再把 5 保存到 <code>b</code>。</li>\n<li><strong>关键易错点：</strong> 普通局部变量必须先初始化再读取；<code>=</code> 表示赋值而不是相等判断，连续赋值要从右向左解释。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应 GESP C++ 一级大纲中的“程序基本概念”。前面已经学过完整程序骨架和输入输出；现在要弄清程序里的数据叫什么、放在哪里、怎样变化。</p>\n<p>本页集中解决三个问题：</p>\n<ol>\n<li><strong>一个变量怎样由名字、类型和当前值组成？</strong></li>\n<li><strong>声明、初始化和赋值分别发生了什么？</strong></li>\n<li><strong>表达式怎样读取变量的当前值，算出结果，再把结果保存起来？</strong></li>\n</ol>\n<p>数据类型的范围、精度和转换由<a href="/gesp/level1/data-types/">基本数据类型</a>系统讲解；运算符种类、优先级和结合方向由<a href="/gesp/level1/operations/">基本运算</a>负责。本页只把变量从“建立”到“改变”的过程讲清楚。</p>\n<p>先看一句简单代码：</p>\n<pre><code>int score = 60;</code></pre>\n<p>它包含四个关键信息：</p>\n<table>\n<thead>\n<tr>\n<th>部分</th>\n<th>身份</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int</code></td>\n<td>C++ 关键字，也是这里的数据类型</td>\n<td>说明 <code>score</code> 按整数保存和解释</td>\n</tr>\n<tr>\n<td><code>score</code></td>\n<td>程序员起的标识符</td>\n<td>给这个变量一个可引用的名字</td>\n</tr>\n<tr>\n<td><code>60</code></td>\n<td>整数字面量常量</td>\n<td>提供最初要保存的值</td>\n</tr>\n<tr>\n<td><code>=</code></td>\n<td>初始化写法的一部分</td>\n<td>在变量建立时把右侧结果作为初值</td>\n</tr>\n</tbody>\n</table>\n<p>执行这条定义以后，可以把变量状态记成：<strong>名字是 <code>score</code>，类型是 <code>int</code>，当前值是 60。</strong></p>\n<h2 id="01-标识符">01 标识符</h2>\n<h3 id="标识符是程序中的名字">标识符是程序中的名字</h3>\n<p>程序需要用名字指向变量等对象，这类名字叫<strong>标识符</strong>。<code>score</code>、<code>studentCount</code>、<code>total_price</code> 都是标识符。</p>\n<p>一级题目通常按下面的规则判断：</p>\n<ol>\n<li>只使用英文字母、数字和下划线 <code>_</code>；</li>\n<li>第一个字符不能是数字；</li>\n<li>不能使用 C++ 关键字；</li>\n<li>区分大小写，<code>score</code>、<code>Score</code> 和 <code>SCORE</code> 是三个不同名字。</li>\n</ol>\n<p>逐个判断几个例子：</p>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>是否可作一级普通标识符</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>number_of_students</code></td>\n<td>可以</td>\n<td>只含字母和下划线，开头不是数字</td>\n</tr>\n<tr>\n<td><code>score2</code></td>\n<td>可以</td>\n<td>数字不在开头</td>\n</tr>\n<tr>\n<td><code>360AntiVirus</code></td>\n<td>不可以</td>\n<td>以数字开头</td>\n</tr>\n<tr>\n<td><code>Man&amp;Woman</code></td>\n<td>不可以</td>\n<td>含有 <code>&amp;</code></td>\n</tr>\n<tr>\n<td><code>student score</code></td>\n<td>不可以</td>\n<td>含有空格</td>\n</tr>\n<tr>\n<td><code>break</code></td>\n<td>不可以</td>\n<td><code>break</code> 是 C++ 关键字</td>\n</tr>\n</tbody>\n</table>\n<p>C++ 还涉及其他字符规则以及以下划线开头的保留名称。一级编程建议直接使用字母开头的清楚名称，不需要用下划线开头去试探边界。</p>\n<h3 id="三步判断一个名称是否合法">三步判断一个名称是否合法</h3>\n<p>看到一个候选名称时，可以按固定顺序检查：</p>\n<ol>\n<li>**查开头：**第一个字符是字母或下划线吗？如果是数字，立即不合法。</li>\n<li>**查其余字符：**是否只由字母、数字、下划线组成？空格、<code>&amp;</code>、<code>-</code> 等不能直接放进普通标识符。</li>\n<li>**查关键字：**整个名称是否正好是 C++ 关键字？</li>\n</ol>\n<p>例如 <code>score2</code> 三步都通过；<code>2score</code> 在第一步就失败；<code>student-score</code> 在第二步失败；<code>while</code> 在第三步失败。</p>\n<h3 id="合法还不够名字还要能表达用途">合法还不够，名字还要能表达用途</h3>\n<p><code>a</code>、<code>x1</code> 可能合法，但在较长程序中不一定容易理解。一级程序也应尽量让名字说明保存的内容：</p>\n<table>\n<thead>\n<tr>\n<th>含义</th>\n<th>较清楚的名称</th>\n<th>容易造成的问题</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>学生人数</td>\n<td><code>studentCount</code></td>\n<td>写成 <code>x</code> 后，稍后很难确认它保存什么</td>\n</tr>\n<tr>\n<td>总价</td>\n<td><code>totalPrice</code></td>\n<td>写成 <code>price</code> 容易与单价混淆</td>\n</tr>\n<tr>\n<td>是否准备好</td>\n<td><code>ready</code></td>\n<td>写成 <code>number</code> 看不出真假含义</td>\n</tr>\n</tbody>\n</table>\n<p>命名不改变计算结果，却会直接影响检查变量和调试的速度。</p>\n<h2 id="02-关键字">02 关键字</h2>\n<h3 id="关键字已经由-c-规定用途">关键字已经由 C++ 规定用途</h3>\n<p><strong>关键字</strong>是 C++ 语言保留的单词，编译器会按规定含义理解它们。一级常见关键字包括：</p>\n<table>\n<thead>\n<tr>\n<th>用途</th>\n<th>常见关键字</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>数据类型与逻辑值</td>\n<td><code>int</code>、<code>double</code>、<code>char</code>、<code>bool</code>、<code>true</code>、<code>false</code></td>\n</tr>\n<tr>\n<td>条件与循环</td>\n<td><code>if</code>、<code>else</code>、<code>switch</code>、<code>case</code>、<code>for</code>、<code>while</code>、<code>do</code></td>\n</tr>\n<tr>\n<td>流程和返回</td>\n<td><code>break</code>、<code>continue</code>、<code>return</code></td>\n</tr>\n<tr>\n<td>声明相关</td>\n<td><code>const</code>、<code>using</code>、<code>namespace</code></td>\n</tr>\n</tbody>\n</table>\n<p>关键字不能再次当作变量名。例如：</p>\n<pre><code>int int = 3;</code></pre>\n<p>第一个 <code>int</code> 表示整数类型，第二个位置本应填写变量名；但第二个 <code>int</code> 仍会被编译器当作关键字，所以这行不能通过编译。可以改为：</p>\n<pre><code>int number = 3;</code></pre>\n<h3 id="常见名称不一定是关键字">常见名称不一定是关键字</h3>\n<p>经常出现，不代表它属于 C++ 关键字：</p>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>实际身份</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>main</code></td>\n<td>具有特殊规则的程序入口名称，不是关键字</td>\n</tr>\n<tr>\n<td><code>cin</code>、<code>cout</code></td>\n<td>标准库中的输入输出对象名称</td>\n</tr>\n<tr>\n<td><code>endl</code></td>\n<td>标准库提供的输出操纵符名称</td>\n</tr>\n<tr>\n<td><code>score</code></td>\n<td>程序员自己选择的标识符</td>\n</tr>\n</tbody>\n</table>\n<p>因此，在 <code>double</code>、<code>else</code>、<code>while</code>、<code>endl</code> 中，前三个是关键字，<code>endl</code> 不是。</p>\n<p>C++ 区分大小写。把 <code>int</code> 改成 <code>Int</code> 并不会创造“同一个整数关键字”；<code>Int</code> 只是另一个名称，而且编译器不会自动把它理解成 <code>int</code>。</p>\n<h2 id="03-常量">03 常量</h2>\n<h3 id="字面量把固定值直接写进代码">字面量把固定值直接写进代码</h3>\n<p>直接写在代码中的固定值叫<strong>字面量常量</strong>，常见形式有：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>本页先这样认识</th>\n<th>书写边界</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>10</code>、<code>-3</code></td>\n<td>整数值</td>\n<td>没有小数点</td>\n</tr>\n<tr>\n<td><code>3.14</code></td>\n<td>浮点值</td>\n<td>带小数点</td>\n</tr>\n<tr>\n<td><code>\'A\'</code></td>\n<td>单个字符</td>\n<td>使用单引号</td>\n</tr>\n<tr>\n<td><code>true</code>、<code>false</code></td>\n<td>逻辑真、逻辑假</td>\n<td>两者也是 C++ 关键字</td>\n</tr>\n<tr>\n<td><code>"GESP"</code></td>\n<td>一段文字</td>\n<td>使用双引号，不能当成单个 <code>char</code></td>\n</tr>\n</tbody>\n</table>\n<p>它们可以参与初始化或表达式。例如 <code>int count = 10;</code> 中的 10 为 <code>count</code> 提供初值；<code>price * 2 + 5</code> 中的 2 和 5 直接参与计算。</p>\n<p>各种字面量具体属于什么类型、能表示多大范围，放在“基本数据类型”页理解。本页先认清写法和用途。</p>\n<h3 id="用-const-给固定值起名字">用 <code>const</code> 给固定值起名字</h3>\n<p>如果一个固定值会多次使用，可以给它一个有含义的名字，并用 <code>const</code> 限制后续修改：</p>\n<pre><code>const int bonus = 5;</code></pre>\n<p>这行建立了名为 <code>bonus</code> 的具名常量：</p>\n<ul>\n<li>名字：<code>bonus</code>；</li>\n<li>类型：<code>const int</code>；</li>\n<li>值：5；</li>\n<li>约束：建立后不能再通过赋值改变。</li>\n</ul>\n<p>本页这种局部 <code>const</code> 对象应在定义时给出初值。后面再写 <code>bonus = 8;</code> 会编译失败，因为它违背了“不允许修改”的约束。</p>\n<p><code>const</code> 没有把数字 5 变成另一种数据。它做的是：让名为 <code>bonus</code> 的对象保持建立时的值，防止程序后面误改。</p>\n<h3 id="什么时候用字面量什么时候用具名常量">什么时候用字面量，什么时候用具名常量</h3>\n<p>一次性、含义非常直接的值可以写成字面量。一个值在题目中有明确含义、会重复出现或不应被修改时，具名常量更容易检查。</p>\n<p>例如“每盒固定装 12 支笔”可以写：</p>\n<pre><code>const int pensPerBox = 12;int totalPens = boxCount * pensPerBox;</code></pre>\n<p>看到 <code>pensPerBox</code> 就知道 12 表示每盒数量；如果以后题目改成每盒 10 支，只需检查这个固定含义的位置。</p>\n<h2 id="04-变量与初始化">04 变量与初始化</h2>\n<h3 id="变量由名字类型和当前值组成">变量由名字、类型和当前值组成</h3>\n<p>变量可以理解为程序中一个有名字、能保存数据的对象。查看一个变量时，总要同时问三件事：</p>\n<table>\n<thead>\n<tr>\n<th>组成</th>\n<th>要回答的问题</th>\n<th>例子 <code>int age = 12;</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>名字</td>\n<td>后面的代码用什么名称找到它？</td>\n<td><code>age</code></td>\n</tr>\n<tr>\n<td>类型</td>\n<td>这份数据按什么规则保存和解释？</td>\n<td><code>int</code></td>\n</tr>\n<tr>\n<td>当前值</td>\n<td>执行到此刻，它里面保存什么？</td>\n<td>12</td>\n</tr>\n</tbody>\n</table>\n<p>变量的类型在定义后固定，当前值可以被后续赋值改变，也可以一直保持不变。“变量”表示它<strong>允许</strong>变化，不表示它每次运行都必须变化。</p>\n<h3 id="声明定义和初始化的区别">声明、定义和初始化的区别</h3>\n<p>在本页的简单局部变量中：</p>\n<pre><code>int age;</code></pre>\n<p>这行告诉编译器“这里有一个名为 <code>age</code>、类型为 <code>int</code> 的变量”，并建立这个变量。它既完成了声明，也完成了定义，但还没有给普通局部整数一个可安全读取的初值。</p>\n<p>下面这行同时建立变量并给出初值：</p>\n<pre><code>int age = 12;</code></pre>\n<p>这叫<strong>初始化</strong>。执行完成后，<code>age</code> 已经拥有确定状态：名字是 <code>age</code>，类型是 <code>int</code>，当前值是 12。</p>\n<p>初始化只发生在变量建立时。变量已经存在以后再写：</p>\n<pre><code>age = 13;</code></pre>\n<p>这是<strong>赋值</strong>，它把当前值从 12 改为 13，并没有重新定义一个新的 <code>age</code>。</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>发生了什么</th>\n<th>执行后的状态</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int age;</code></td>\n<td>声明并定义变量，没有给可靠初值</td>\n<td>名字和类型确定，值不可读取</td>\n</tr>\n<tr>\n<td><code>int age = 12;</code></td>\n<td>定义并初始化变量</td>\n<td><code>age</code> 当前值为 12</td>\n</tr>\n<tr>\n<td><code>age = 13;</code></td>\n<td>给已经存在的变量赋新值</td>\n<td>类型仍为 <code>int</code>，当前值变为 13</td>\n</tr>\n</tbody>\n</table>\n<h3 id="为什么不能读取未初始化的普通局部变量">为什么不能读取未初始化的普通局部变量</h3>\n<p>在 <code>main</code> 内写下 <code>int score;</code> 后，不能直接把 <code>score</code> 用于输出、比较或计算。对本页这种未初始化的普通局部整数，读取其不确定值会导致<strong>未定义行为</strong>：程序结果没有可靠保证，不能根据某次运行猜它“默认等于 0”。</p>\n<p>错误过程是：</p>\n<pre><code>int score;cout &lt;&lt; score;</code></pre>\n<p>安全做法是先让它得到确定值：</p>\n<pre><code>int score = 0;cout &lt;&lt; score;</code></pre>\n<p>检查变量时，不只看“是否写过变量名”，还要沿执行顺序确认：在第一次读取之前，它是否已经初始化或赋值。</p>\n<h2 id="05-赋值与连续赋值">05 赋值与连续赋值</h2>\n<h3 id="赋值先算右边再写左边">赋值先算右边，再写左边</h3>\n<p>赋值号 <code>=</code> 的任务是：先得到右侧结果，再把结果保存到左侧变量。</p>\n<pre><code>int score = 60;score = 85;</code></pre>\n<p>第一行建立并初始化 <code>score</code>；第二行才是后续赋值，用 85 覆盖旧值 60。类型没有改变，改变的是当前值。</p>\n<p>当左右两边都出现同一个变量时，仍按“先右后左”执行：</p>\n<pre><code>int a = 5;a = a + 2;</code></pre>\n<p>执行第二行的过程是：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>动作</th>\n<th style="text-align: right"><code>a</code> 的状态</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>读取右侧 <code>a</code> 的旧值</td>\n<td style="text-align: right">仍为 5</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>计算 <code>5 + 2</code></td>\n<td style="text-align: right">得到结果 7，尚未写回</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>把 7 保存到左侧 <code>a</code></td>\n<td style="text-align: right">当前值变为 7</td>\n</tr>\n</tbody>\n</table>\n<p>这里不是在解数学方程“<code>a</code> 等于 <code>a + 2</code>”。<code>=</code> 表示一次有先后顺序的写入操作，所以同一个变量能够由 5 变成 7。</p>\n<h3 id="赋值复制当前值不会建立自动连接">赋值复制当前值，不会建立自动连接</h3>\n<p>假设依次执行：</p>\n<pre><code>int a = 2;int b = 5;a = b;b = 8;</code></pre>\n<p>执行 <code>a = b;</code> 时，程序读取 <code>b</code> 当时的值 5，再把 5 保存给 <code>a</code>。接着把 <code>b</code> 改成 8，并不会自动把 <code>a</code> 一起改成 8。</p>\n<table>\n<thead>\n<tr>\n<th>执行后</th>\n<th style="text-align: right"><code>a</code></th>\n<th style="text-align: right"><code>b</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>初始化完成</td>\n<td style="text-align: right">2</td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>a = b;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>b = 8;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">8</td>\n</tr>\n</tbody>\n</table>\n<p>普通整数赋值复制的是当时的值，不是给两个变量拴上一根永久连线。</p>\n<h3 id="连续赋值从右边开始">连续赋值从右边开始</h3>\n<p>自测中会遇到连续赋值，因此要能识别它的执行顺序：</p>\n<pre><code>int a;int b;a = b = 3;</code></pre>\n<p>赋值运算按右结合解释，等价于分成两步：</p>\n<pre><code>b = 3;a = b;</code></pre>\n<p>第一步先让 <code>b</code> 得到 3；这一赋值表达式的结果再交给左边，于是第二步让 <code>a</code> 也得到 3。最终 <code>a = 3</code>、<code>b = 3</code>。</p>\n<p>若写成 <code>int a = b = 3;</code>，<code>b</code> 仍必须提前定义。只在这一行定义了 <code>a</code>，并不会顺便定义 <code>b</code>。</p>\n<p>一级主程序优先使用分步写法，因为执行顺序更容易检查。连续赋值需要会读，不必为了缩短一行而频繁使用。</p>\n<h3 id="-和--的职责不同"><code>=</code> 和 <code>==</code> 的职责不同</h3>\n<pre><code>a = b;a == b;</code></pre>\n<ul>\n<li><code>a = b</code>：把 <code>b</code> 的当前值写入 <code>a</code>，会修改 <code>a</code>；</li>\n<li><code>a == b</code>：判断两者是否相等，得到真或假，不负责修改 <code>a</code>、<code>b</code>。</li>\n</ul>\n<p>两者只差一个等号，程序作用却完全不同。具体关系运算放在“基本运算”页展开。</p>\n<h2 id="06-表达式">06 表达式</h2>\n<h3 id="表达式会产生一个结果">表达式会产生一个结果</h3>\n<p>表达式由常量、变量、运算符以及必要的括号组成，计算后得到一个结果。例如：</p>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th>需要读取什么</th>\n<th>得到什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>a + b</code></td>\n<td><code>a</code>、<code>b</code> 的当前值</td>\n<td>两数之和</td>\n</tr>\n<tr>\n<td><code>price * count</code></td>\n<td>当前单价和数量</td>\n<td>总价数值</td>\n</tr>\n<tr>\n<td><code>score &gt;= 60</code></td>\n<td><code>score</code> 的当前值</td>\n<td>真或假</td>\n</tr>\n<tr>\n<td><code>(a + b) * 2</code></td>\n<td><code>a</code>、<code>b</code> 的当前值</td>\n<td>先求和再乘 2 的数值</td>\n</tr>\n</tbody>\n</table>\n<p>只计算 <code>a + b</code> 不会自动修改 <code>a</code>、<code>b</code>。要保存结果，需要初始化一个新变量，或赋值给已经存在的变量。</p>\n<h3 id="从当前值到结果再保存">从当前值到结果，再保存</h3>\n<p>假设当前状态是 <code>price = 8</code>、<code>count = 3</code>、<code>bonus = 5</code>，执行：</p>\n<pre><code>int total = price * count + bonus;</code></pre>\n<p>真实过程可以拆成四步：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>当前动作</th>\n<th style="text-align: right">结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>读取 <code>price</code>、<code>count</code>、<code>bonus</code> 的当前值</td>\n<td style="text-align: right">8、3、5</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>计算 <code>8 * 3</code></td>\n<td style="text-align: right">24</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>计算 <code>24 + 5</code></td>\n<td style="text-align: right">29</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td>用结果 29 初始化 <code>total</code></td>\n<td style="text-align: right"><code>total = 29</code></td>\n</tr>\n</tbody>\n</table>\n<p>表达式先产生结果，保存动作随后发生。若只写 <code>price * count + bonus;</code>，虽然表达式可以算出 29，却没有把这个结果交给一个后续要用的变量。</p>\n<h3 id="变量变化后旧结果不会自动重算">变量变化后，旧结果不会自动重算</h3>\n<p>继续上一例。<code>total</code> 已经得到 29 后，把 <code>price</code> 赋成 10：</p>\n<pre><code>price = 10;</code></pre>\n<p>此时 <code>price</code> 变为 10，<code>total</code> 仍是之前保存的 29。只有再次执行：</p>\n<pre><code>total = price * count + bonus;</code></pre>\n<p>程序才会用当前值 <code>10、3、5</code> 重新计算，得到 35 并覆盖 <code>total</code>。</p>\n<p>这条边界很重要：变量保存的是某次执行得到的结果，不是会随公式右侧自动更新的数学符号。</p>\n<h3 id="括号和优先级决定计算顺序">括号和优先级决定计算顺序</h3>\n<p>表达式中有多个运算符时，先处理括号，再按运算符优先级和结合方向计算。例如 <code>price * count + bonus</code> 先乘后加；<code>(price + bonus) * count</code> 先算括号，结果会不同。</p>\n<p>本页追踪的重点是“取当前值 → 按规则计算 → 保存结果”。完整的算术、关系、逻辑、赋值优先级表放在“基本运算”页。拿不准时，优先补括号或拆成多条直白语句。</p>\n<h2 id="建立变量时的统一检查顺序">建立变量时的统一检查顺序</h2>\n<p>从题意建立一个变量，可以按下面的顺序检查：</p>\n<ol>\n<li><strong>它保存什么？</strong> 先确定现实含义，例如单价、数量、总价。</li>\n<li><strong>叫什么？</strong> 选择合法且能说明用途的标识符。</li>\n<li><strong>用什么类型？</strong> 根据数据形式和范围选择；具体范围查“基本数据类型”。</li>\n<li><strong>初值从哪里来？</strong> 来自固定值、输入，还是另一个表达式？</li>\n<li><strong>以后允许改变吗？</strong> 不应修改的固定值可以使用 <code>const</code>。</li>\n<li><strong>第一次读取前有确定值吗？</strong> 没有就先初始化或赋值。</li>\n</ol>\n<p>例如“每盒 12 支笔，盒数可以变化”：</p>\n<table>\n<thead>\n<tr>\n<th>题意中的量</th>\n<th>合适写法</th>\n<th>判断</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>每盒支数</td>\n<td><code>const int pensPerBox = 12;</code></td>\n<td>固定含义，不应修改</td>\n</tr>\n<tr>\n<td>盒数</td>\n<td><code>int boxCount = 3;</code></td>\n<td>当前值为 3，以后可能改变</td>\n</tr>\n<tr>\n<td>总支数</td>\n<td><code>int totalPens = pensPerBox * boxCount;</code></td>\n<td>由当前两个值计算并保存</td>\n</tr>\n</tbody>\n</table>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>概念</th>\n<th>例子</th>\n<th>关键判断</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>标识符</td>\n<td><code>studentCount</code></td>\n<td>字母开头、字符合法、不是关键字、区分大小写</td>\n</tr>\n<tr>\n<td>关键字</td>\n<td><code>int</code>、<code>return</code></td>\n<td>C++ 已规定用途，不能用作变量名</td>\n</tr>\n<tr>\n<td>字面量常量</td>\n<td><code>10</code>、<code>3.14</code>、<code>\'A\'</code>、<code>true</code></td>\n<td>把固定值直接写进代码</td>\n</tr>\n<tr>\n<td>具名常量</td>\n<td><code>const int bonus = 5;</code></td>\n<td>定义时给值，后面不再赋新值</td>\n</tr>\n<tr>\n<td>变量</td>\n<td><code>int age;</code></td>\n<td>具有名字、类型和当前值状态</td>\n</tr>\n<tr>\n<td>初始化</td>\n<td><code>int age = 12;</code></td>\n<td>建立变量时给初值</td>\n</tr>\n<tr>\n<td>赋值</td>\n<td><code>age = 13;</code></td>\n<td>先算右侧，再覆盖左侧当前值</td>\n</tr>\n<tr>\n<td>表达式</td>\n<td><code>price * count + bonus</code></td>\n<td>读取当前值，按规则计算出结果</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>下面用一个程序验证本页最容易混淆的边界：变量改变以后，之前保存的表达式结果不会自动更新。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int price = 8;    int count = 3;    const int bonus = 5;\n    int total = price * count + bonus; // 首次计算总价并把结果保存到 total    cout &lt;&lt; total &lt;&lt; endl;\n    price = 10; // 只修改 price，已经保存的 total 不会自动更新    cout &lt;&lt; total &lt;&lt; endl;\n    total = price * count + bonus; // 按当前价格重新计算，并用新结果覆盖 total    cout &lt;&lt; total &lt;&lt; endl;    return 0;}</code></pre>\n<p>程序不需要输入，输出为：</p>\n<pre><code>292935</code></pre>\n<p>逐步追踪：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>执行内容</th>\n<th style="text-align: right"><code>price</code></th>\n<th style="text-align: right"><code>count</code></th>\n<th style="text-align: right"><code>bonus</code></th>\n<th style="text-align: right"><code>total</code></th>\n<th style="text-align: right">本次输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>完成三个初始数据的建立</td>\n<td style="text-align: right">8</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">尚未建立</td>\n<td style="text-align: right">—</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>用 <code>8 * 3 + 5</code> 初始化 <code>total</code></td>\n<td style="text-align: right">8</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">29</td>\n<td style="text-align: right">—</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>输出 <code>total</code></td>\n<td style="text-align: right">8</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">29</td>\n<td style="text-align: right">29</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>price = 10;</code></td>\n<td style="text-align: right">10</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">29</td>\n<td style="text-align: right">—</td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td>再次输出尚未重算的 <code>total</code></td>\n<td style="text-align: right">10</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">29</td>\n<td style="text-align: right">29</td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td>重新计算并赋值给 <code>total</code></td>\n<td style="text-align: right">10</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">35</td>\n<td style="text-align: right">—</td>\n</tr>\n<tr>\n<td style="text-align: right">7</td>\n<td>输出新的 <code>total</code></td>\n<td style="text-align: right">10</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">35</td>\n<td style="text-align: right">35</td>\n</tr>\n</tbody>\n</table>\n<p>第二行输出仍是 29，正好证明 <code>total</code> 没有与右侧公式永久绑定。第三行变为 35，是因为重新执行了表达式，并把新结果赋给 <code>total</code>。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>依次执行 <code>int a = 2; int b = a; a = 5;</code>。每句后记录两个变量的当前值：第二句把当时的 2 复制给 <code>b</code>，第三句只修改 <code>a</code>，所以最终 <code>a = 5</code>、<code>b = 2</code>。</p>\n<p><strong>成功标准：</strong> 能说明赋值复制的是执行当时的值，不会建立自动连接。完成后不看提示独立完成页末“整页综合测试”；提交后按“右侧求值 → 左侧写回”重新推演每道错题。</p>\n<h2 id="常见错误">常见错误</h2>\n<h3 id="变量名不符合规则">变量名不符合规则</h3>\n<p><strong>错误：</strong> 使用 <code>2score</code>、<code>student-score</code>、<code>break</code> 作为变量名。</p>\n<p><strong>检查：</strong> 依次查开头、其余字符和关键字；再确认大小写是否与定义时一致。</p>\n<h3 id="读取未初始化的普通局部变量">读取未初始化的普通局部变量</h3>\n<p><strong>错误：</strong> 只写 <code>int score;</code> 就参与输出或计算。</p>\n<p><strong>后果：</strong> 读取这种不确定值会造成未定义行为，结果不能信任，也不能假定为 0。</p>\n<p><strong>检查：</strong> 沿执行路径找到第一次读取，确认此前已经初始化或赋值。</p>\n<h3 id="把初始化和赋值混成一件事">把初始化和赋值混成一件事</h3>\n<p><strong>错误理解：</strong> 认为 <code>int age = 12;</code> 与后面的 <code>age = 13;</code> 都在重新创建变量。</p>\n<p><strong>检查：</strong> 看这一行是否同时出现类型和新变量名。建立时给值是初始化；变量已经存在后改值是赋值。</p>\n<h3 id="把--当成数学等号">把 <code>=</code> 当成数学等号</h3>\n<p><strong>错误理解：</strong> 看到 <code>a = a + 2;</code> 就认为等式不可能成立。</p>\n<p><strong>检查：</strong> 按时间顺序读：取旧值、算右侧、写回左侧。相等比较使用 <code>==</code>。</p>\n<h3 id="认为赋值会让两个变量永久联动">认为赋值会让两个变量永久联动</h3>\n<p><strong>错误理解：</strong> 执行 <code>a = b;</code> 后再改 <code>b</code>，以为 <code>a</code> 也会跟着变。</p>\n<p><strong>检查：</strong> 在每次赋值后记录两个变量的当前值。普通整数赋值只复制执行当时的值。</p>\n<h3 id="从左向右误读连续赋值">从左向右误读连续赋值</h3>\n<p><strong>错误：</strong> 把 <code>a = b = 3;</code> 理解成先处理 <code>a = b</code>。</p>\n<p><strong>检查：</strong> 按右结合拆为 <code>b = 3;</code>、<code>a = b;</code>。若写 <code>int a = b = 3;</code>，还要确认 <code>b</code> 已提前定义。</p>\n<h3 id="只改公式里的变量却忘了重新计算结果">只改公式里的变量，却忘了重新计算结果</h3>\n<p><strong>错误：</strong> <code>price</code> 已变化，却直接使用上一次保存的 <code>total</code>。</p>\n<p><strong>检查：</strong> 找到产生 <code>total</code> 的表达式，确认在所需的最新变量值出现后，它是否再次执行并写回结果。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>下一页<a href="/gesp/level1/data-types/">基本数据类型</a>负责判断变量能保存什么范围和精度；随后<a href="/gesp/level1/operations/">基本运算</a>会讲表达式中的运算符与优先级；学完这两页后，<a href="/gesp/level1/sequential/">顺序结构</a>会把多条赋值和计算语句连起来，逐行追踪完整程序状态。</p>\n<section aria-labelledby="related-knowledge-variables-expressions"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-variables-expressions" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/program-structure/" class="astro-mkwt4ymd">程序结构</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 数据类型决定一个值怎样被解释、通常能保存多大以及参与运算时遵守什么规则；选择类型要同时检查输入范围和中间结果。</li>\n<li><strong>常用语法：</strong> 整数常用 <code>int</code>、较大整数用 <code>long long</code>，小数常用 <code>double</code>，单个字符用 <code>char</code>，真假状态用 <code>bool</code>。</li>\n<li><strong>最小示例：</strong> <code>int age = 12; double score = 92.5; char grade = \'A\'; bool passed = true;</code></li>\n<li><strong>关键易错点：</strong> 数值范围不等于有效精度；整数除法会舍去小数部分，普通 <code>char</code> 的符号范围还可能随平台而不同。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>变量像一个有名字的盒子，<strong>数据类型决定这个盒子准备保存什么、通常能保存多大，以及计算机怎样解释里面的数据</strong>。</p>\n<p>例如，年龄 12、平均分 92.5、等级字符 <code>\'A\'</code> 和“记录是否完成”这四项数据看起来都很短，却不能用同一种规则理解：</p>\n<table>\n<thead>\n<tr>\n<th>数据</th>\n<th>它回答的问题</th>\n<th>合适的类型</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>12</td>\n<td>有多少、是几</td>\n<td>整数类型</td>\n</tr>\n<tr>\n<td>92.5</td>\n<td>带小数的测量或计算结果</td>\n<td>浮点类型</td>\n</tr>\n<tr>\n<td><code>\'A\'</code></td>\n<td>是哪个单个字符</td>\n<td><code>char</code></td>\n</tr>\n<tr>\n<td><code>true</code></td>\n<td>某件事是否成立</td>\n<td><code>bool</code></td>\n</tr>\n</tbody>\n</table>\n<p>选类型时，先别急着背类型名。按顺序问自己：</p>\n<ol>\n<li>数据是整数、小数、单个字符，还是“真／假”状态？</li>\n<li>如果是整数，输入值和计算过程中的最大结果有多大？</li>\n<li>如果是小数，需要多大的数值范围和多少位可靠有效数字？</li>\n<li>题目中的写法是否真的与选定类型匹配？</li>\n</ol>\n<p>本页覆盖一级大纲中的整数、浮点、字符、布尔类型，并保留现有自测需要的最小类型转换知识。表中的字节数和范围以常见 GCC/OJ 环境为参考；C++ 在不同编译器和平台上的具体实现可能不同。</p>\n<h2 id="01-整数类型">01 整数类型</h2>\n<p>整数类型保存没有小数部分的数，例如人数、次数、编号和总和。一级只学习 <code>int</code> 和 <code>long long</code>：普通整数优先用 <code>int</code>，较大整数或较大中间结果用 <code>long long</code>。</p>\n<h3 id="常见大小和范围">常见大小和范围</h3>\n<table>\n<thead>\n<tr>\n<th>类型</th>\n<th style="text-align: right">常见字节数</th>\n<th>常见有符号范围</th>\n<th>初学时怎样看</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int</code></td>\n<td style="text-align: right">4 字节</td>\n<td>-2,147,483,648 到 2,147,483,647</td>\n<td>普通整数的首选</td>\n</tr>\n<tr>\n<td><code>long long</code></td>\n<td style="text-align: right">8 字节</td>\n<td>-9,223,372,036,854,775,808 到 9,223,372,036,854,775,807</td>\n<td>较大整数和较大中间结果</td>\n</tr>\n</tbody>\n</table>\n<p>表中的字节数和范围以常见 GCC/OJ 环境为参考；选择时仍要以题目数据范围为准。一级竞赛代码需要保存较大整数时，直接检查是否应使用 <code>long long</code>，不扩展其他整数类型成员。</p>\n<h3 id="普通整数为什么优先用-int">普通整数为什么优先用 int</h3>\n<p><code>int</code> 能覆盖大多数一级题目的年龄、分数、计数和较小计算结果，写法也最直白：</p>\n<pre><code>int age = 12; // 年龄是普通整数，12 远在 int 的常见范围内int studentCount = 45; // 当前学生人数也适合用 int 保存</code></pre>\n<h3 id="什么时候需要-long-long">什么时候需要 long long</h3>\n<p>要同时检查<strong>输入值</strong>和<strong>计算结果</strong>。例如 100,000 个数，每个数最大是 100,000：</p>\n<pre><code>最大总和 = 100000 × 100000 = 10000000000</code></pre>\n<p>每个输入都能放进 <code>int</code>，但总和 10,000,000,000 已经超过 <code>int</code> 的常见上限，所以保存总和的变量应使用 <code>long long</code>。</p>\n<pre><code>long long total = 100000LL * 100000; // 先按 long long 做乘法，再保存 10000000000</code></pre>\n<p><code>LL</code> 后缀说明这个整数常量按 <code>long long</code> 处理。不能只把左边改成 <code>long long</code>，却让右边先按范围较小的整数类型算完；<strong>中间结果溢出后，再放进更大的盒子也救不回来。</strong></p>\n<h3 id="怎样验证整数类型是否合适">怎样验证整数类型是否合适</h3>\n<ol>\n<li>找出题目允许的最大绝对值。</li>\n<li>写出加、减、乘过程中可能出现的最大中间结果。</li>\n<li>与类型范围比较，不要只看最后答案。</li>\n<li>用最大值附近的数据手算一次。</li>\n</ol>\n<p>例如结果最多 1,000,000，<code>int</code> 足够；结果可能到 5,000,000,000，就应考虑 <code>long long</code>。即使使用 <code>long long</code>，范围也不是无限的，继续乘很大的数仍会溢出。</p>\n<h2 id="02-浮点类型与有效精度">02 浮点类型与有效精度</h2>\n<p>浮点类型用来保存带小数的数据，例如温度、平均分、长度和计算得到的比值。一级主要认识 <code>float</code> 和 <code>double</code>。</p>\n<h3 id="范围和精度是两件事">范围和精度是两件事</h3>\n<table>\n<thead>\n<tr>\n<th>类型</th>\n<th style="text-align: right">常见字节数</th>\n<th style="text-align: right">常见最大绝对值（约）</th>\n<th style="text-align: right">可靠有效数字（约）</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>float</code></td>\n<td style="text-align: right">4 字节</td>\n<td style="text-align: right">3.4 × 10³⁸</td>\n<td style="text-align: right">6—7 位</td>\n</tr>\n<tr>\n<td><code>double</code></td>\n<td style="text-align: right">8 字节</td>\n<td style="text-align: right">1.7 × 10³⁰⁸</td>\n<td style="text-align: right">15—16 位</td>\n</tr>\n</tbody>\n</table>\n<p>“最大绝对值”回答的是<strong>数能有多大</strong>；“有效数字”回答的是<strong>能可靠保留多少位有意义的数字</strong>。范围很大，不代表每一位都精确。</p>\n<p>浮点数也能表示负数和 0。表格只用最大绝对值帮助比较数量级，没有列出所有接近 0 的表示形式。</p>\n<h3 id="什么是有效数字">什么是有效数字</h3>\n<p>有效数字从第一个非零数字开始计算，是整个数中可靠的总位数，不是“小数点后有几位”。</p>\n<table>\n<thead>\n<tr>\n<th>数</th>\n<th style="text-align: right">有效数字</th>\n<th>为什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>123.456</td>\n<td style="text-align: right">6 位</td>\n<td>从 1 数到 6</td>\n</tr>\n<tr>\n<td>0.00123456</td>\n<td style="text-align: right">6 位</td>\n<td>前导 0 只负责定位小数点，不计入</td>\n</tr>\n<tr>\n<td>120.030</td>\n<td style="text-align: right">6 位</td>\n<td>从第一个非零数字 1 开始，中间和末尾表示精度的 0 都计入</td>\n</tr>\n</tbody>\n</table>\n<p>因此，“<code>float</code> 约有 6—7 位有效数字”不能理解成“永远保留小数点后 6—7 位”。把超过可靠位数的值保存进去时，末尾可能发生舍入：</p>\n<pre><code>float roughValue = 12345678.0f; // 8 位整数超过 float 常见可靠精度，末尾可能舍入double preciseValue = 12345678.0; // double 通常能可靠保留这 8 位数字</code></pre>\n<p>小数常量 <code>3.14</code> 默认按 <code>double</code> 看待；写成 <code>3.14f</code> 才明确表示 <code>float</code> 常量。一级小数计算通常优先使用 <code>double</code>，除非题目明确要求其他类型。</p>\n<h3 id="为什么小数有时不是完全精确">为什么小数有时不是完全精确</h3>\n<p>计算机内部用二进制保存数据，很多十进制小数不能用有限位二进制准确写完。于是 <code>0.1</code>、<code>0.2</code> 这样的值在内部通常只是非常接近原数。</p>\n<p>这属于浮点表示的精度限制，并不是 <code>double</code> “算错了”。还要分清：</p>\n<ul>\n<li><strong>保存精度</strong>：变量内部能可靠保留多少位有效数字；</li>\n<li><strong>输出格式</strong>：屏幕上要求显示几位小数。</li>\n</ul>\n<p>把结果显示成两位小数，并不会让内部存储自动变成“精确到两位”；反过来，屏幕只显示两位，也不代表变量内部只有两位。</p>\n<h3 id="怎样验证浮点类型是否合适">怎样验证浮点类型是否合适</h3>\n<ol>\n<li>先确认结果确实可能有小数。</li>\n<li>检查最大数量级是否在范围内。</li>\n<li>再判断需要多少位可靠有效数字。</li>\n<li>用普通小数和带前导零的小数各检查一次。</li>\n<li>最后按题目要求核对输出位数，不把显示格式当成内部精度。</li>\n</ol>\n<h2 id="03-char-字符类型">03 char 字符类型</h2>\n<p><code>char</code> 保存一个字符，常见大小是 1 字节。字符常量必须用<strong>单引号</strong>：</p>\n<pre><code>char grade = \'A\'; // grade 保存单个等级字符 Achar digit = \'3\'; // digit 保存字符 3，不是整数 3</code></pre>\n<p>下面三种写法表示的对象不同：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>含义</th>\n<th>类型</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>\'3\'</code></td>\n<td>单个字符 3</td>\n<td><code>char</code></td>\n</tr>\n<tr>\n<td><code>3</code></td>\n<td>整数三</td>\n<td>整数类型</td>\n</tr>\n<tr>\n<td><code>"3"</code></td>\n<td>由双引号包围的文本</td>\n<td>不是普通 <code>char</code></td>\n</tr>\n</tbody>\n</table>\n<p>所以自测中问“字符常量 <code>\'3\'</code> 的类型”，应先看单引号，答案是 <code>char</code>，不能因为里面长得像数字就选 <code>int</code>。</p>\n<p>字符在计算机内部也有编号。常见 ASCII 编码中，<code>\'0\'</code> 到 <code>\'9\'</code>、<code>\'A\'</code> 到 <code>\'Z\'</code>、<code>\'a\'</code> 到 <code>\'z\'</code> 各自连续，<code>\'0\'</code> 常见编号为 48，<code>\'A\'</code> 为 65。本页只用它说明“字符和整数不是同一个值”；字符编码和字符转换会在后续内容中系统学习。</p>\n<p>普通 <code>char</code> 默认按有符号还是无符号解释，由编译器和平台决定。一级阶段把它用于普通单字符，不要依赖它保存负数，也不要把“1 字节”误解成“固定能保存 -128 到 127”。</p>\n<h2 id="04-bool-与逻辑值">04 bool 与逻辑值</h2>\n<p><code>bool</code> 只表示两种逻辑状态：</p>\n<pre><code>bool recordReady = true; // 记录已经准备好，状态成立bool answerCorrect = false; // 记录答案尚不正确，状态不成立</code></pre>\n<p><code>true</code> 和 <code>false</code> 必须小写。它们不是放在引号中的文字，也不是四种不同状态中的两个编号。</p>\n<p>在常见 GCC/OJ 环境中，<code>bool</code> 通常占 1 字节；这不表示它有 256 种逻辑状态，它的逻辑值仍只有 <code>false</code> 和 <code>true</code>。普通 <code>cout</code> 输出时，<code>false</code> 通常显示 0，<code>true</code> 通常显示 1。</p>\n<p>条件判断中，0 会被当作假，非 0 会被当作真。但写程序时应优先使用含义明确的状态名和比较条件，不要让读者猜一个数字代表什么。</p>\n<table>\n<thead>\n<tr>\n<th>需要记录的事情</th>\n<th style="text-align: center">是否适合 bool</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>是否提交完成</td>\n<td style="text-align: center">是</td>\n<td>只有完成／未完成两种状态</td>\n</tr>\n<tr>\n<td>答案是否正确</td>\n<td style="text-align: center">是</td>\n<td>只有正确／错误两种状态</td>\n</tr>\n<tr>\n<td>学生得了多少分</td>\n<td style="text-align: center">否</td>\n<td>分数有很多可能值</td>\n</tr>\n<tr>\n<td>菜单选了第几项</td>\n<td style="text-align: center">否</td>\n<td>编号不只两种</td>\n</tr>\n</tbody>\n</table>\n<p>只有当问题本身只需要回答“是／否”时，才适合选择 <code>bool</code>。</p>\n<h2 id="05-类型转换">05 类型转换</h2>\n<p>有时已有数据的类型与当前需要的结果不同，程序会发生类型转换。本页只讲现有一级自测需要的三条规则，完整运算规则在下一页继续学习。</p>\n<h3 id="浮点数转整数向-0-截去小数部分">浮点数转整数：向 0 截去小数部分</h3>\n<pre><code>double value = -3.8; // 原变量保存小数 -3.8int whole = int(value); // 转成 int 后向 0 截断，whole 得到 -3</code></pre>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">原值</th>\n<th>转换</th>\n<th style="text-align: right">结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">3.14</td>\n<td><code>int(3.14)</code></td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td style="text-align: right">3.8</td>\n<td><code>int(3.8)</code></td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td style="text-align: right">-3.8</td>\n<td><code>int(-3.8)</code></td>\n<td style="text-align: right">-3</td>\n</tr>\n</tbody>\n</table>\n<p>这不是四舍五入。转换得到的是一个新的整数值，原来的 <code>value</code> 仍保存 -3.8。</p>\n<h3 id="整数除法和浮点除法">整数除法和浮点除法</h3>\n<p>当 <code>a = 7</code>、<code>b = 2</code> 时：</p>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th>先发生什么</th>\n<th style="text-align: right">结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>a / b</code></td>\n<td>两边都是整数，先做整数除法</td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td><code>a * 1.0 / b</code></td>\n<td><code>1.0</code> 让运算中出现浮点数</td>\n<td style="text-align: right">3.5</td>\n</tr>\n<tr>\n<td><code>0.0 + a / b</code></td>\n<td>先算 <code>a / b = 3</code>，再加 0.0</td>\n<td style="text-align: right">3.0</td>\n</tr>\n<tr>\n<td><code>(0.0 + a) / b</code></td>\n<td>括号内先得到浮点数 7.0，再除以 2</td>\n<td style="text-align: right">3.5</td>\n</tr>\n</tbody>\n</table>\n<p>判断结果时，要看小数<strong>在整数除法之前还是之后</strong>参与运算。整数除法已经算完后再出现 <code>0.0</code>，丢掉的小数部分不会回来。</p>\n<h3 id="取余只用于整数">取余只用于整数</h3>\n<p><code>%</code> 的两个操作数都必须是整数：</p>\n<pre><code>int remainder = 7 % 2; // 7 除以 2 余 1，两个操作数都是整数</code></pre>\n<p><code>7 % 2.5</code>、<code>3.14 % 2</code> 和 <code>8.0 % 3.0</code> 都不符合 C++ 的取余规则。需要处理浮点运算时，不能直接套用整数取余写法。</p>\n<h2 id="怎样选择类型">怎样选择类型</h2>\n<table>\n<thead>\n<tr>\n<th>如果数据是……</th>\n<th>先选择</th>\n<th>还要检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>普通整数</td>\n<td><code>int</code></td>\n<td>输入和中间结果是否超过约 21 亿</td>\n</tr>\n<tr>\n<td>较大整数</td>\n<td><code>long long</code></td>\n<td>结果是否仍在约 ±9.22 × 10¹⁸ 内</td>\n</tr>\n<tr>\n<td>带小数的数据</td>\n<td><code>double</code></td>\n<td>范围、有效数字和题目输出要求</td>\n</tr>\n<tr>\n<td>单个字符</td>\n<td><code>char</code></td>\n<td>是否确实只有一个字符、是否用了单引号</td>\n</tr>\n<tr>\n<td>是／否状态</td>\n<td><code>bool</code></td>\n<td>问题是否真的只有两种状态</td>\n</tr>\n</tbody>\n</table>\n<p>实际选择时按三步走：<strong>先按数据含义分类，再检查范围或精度，最后用边界数据验证。</strong></p>\n<h2 id="典型例题">典型例题</h2>\n<p>一个学习平台要保存一条统计记录：当前活跃学生数、历史提交总次数、平均分、等级字符，以及这条记录是否已经准备好。五项数据属于同一条记录，但含义和范围不同。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int activeStudents; // 当前活跃人数是普通范围内的整数    long long totalSubmissions; // 历史提交总次数可能超过 int 上限    double averageScore; // 平均分可能带小数，优先使用 double    char level; // 等级只需要保存一个字符\n    cin &gt;&gt; activeStudents &gt;&gt; totalSubmissions &gt;&gt; averageScore &gt;&gt; level;    bool recordReady = true; // 四项数据读入后，把“记录已准备”保存为真\n    cout &lt;&lt; "active students: " &lt;&lt; activeStudents &lt;&lt; endl;    cout &lt;&lt; "total submissions: " &lt;&lt; totalSubmissions &lt;&lt; endl;    cout &lt;&lt; "average score: " &lt;&lt; averageScore &lt;&lt; endl;    cout &lt;&lt; "level: " &lt;&lt; level &lt;&lt; endl;    cout &lt;&lt; "record ready: " &lt;&lt; recordReady &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入：</p>\n<pre><code>45 8000000000 92.5 A</code></pre>\n<p>输出：</p>\n<pre><code>active students: 45total submissions: 8000000000average score: 92.5level: Arecord ready: 1</code></pre>\n<p>执行过程：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>读入或形成的数据</th>\n<th>为什么使用这个类型</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>activeStudents = 45</code></td>\n<td>45 是普通整数，<code>int</code> 足够</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>totalSubmissions = 8000000000</code></td>\n<td>80 亿超过 <code>int</code> 常见上限，使用 <code>long long</code></td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><code>averageScore = 92.5</code></td>\n<td>平均分带小数，使用 <code>double</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>level = \'A\'</code></td>\n<td>等级只有一个字符，使用 <code>char</code></td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td><code>recordReady = true</code></td>\n<td>只记录是否准备好，使用 <code>bool</code></td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td>依次输出五项</td>\n<td>普通输出中 <code>true</code> 显示为 1</td>\n</tr>\n</tbody>\n</table>\n<p>这段程序展示了同一条记录中怎样根据数据含义和范围选择类型。把第二项错误地改成 <code>int</code>，输入 8,000,000,000 就可能超出范围；把第三项改成 <code>int</code>，92.5 的小数部分无法按原值保存。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>为 <code>2000000000</code>、<code>3000000000</code>、<code>0.000123456789</code> 选择类型。先判断数据是整数还是小数，再检查常见范围或有效数字：第一项可用 <code>int</code>，第二项超过常见 <code>int</code> 上限应使用 <code>long long</code>，第三项需要保留较多有效数字时优先使用 <code>double</code>。</p>\n<p><strong>成功标准：</strong> 每项选择都同时写出“数据含义”和“范围或精度”依据。完成后不看本段答案，独立完成页末“整页综合测试”；提交后逐题核对解析，并能用边界值重新说明错题为什么不适合原类型。</p>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li><strong>只看最后答案，不看中间结果。</strong> 右边先溢出后，再赋给 <code>long long</code> 已经太晚；要让较大类型在计算开始前参与。</li>\n<li><strong>把浮点范围和精度混为一谈。</strong> 能表示很大的数，不等于每一位都可靠。</li>\n<li><strong>把有效数字理解成小数位数。</strong> <code>123.456</code> 与 <code>0.00123456</code> 都有 6 位有效数字。</li>\n<li><strong>字符引号写错。</strong> 单个字符写 <code>\'A\'</code>；<code>"A"</code> 不是普通 <code>char</code>。</li>\n<li><strong>把 <code>bool</code> 当成普通小整数。</strong> 它应表达“是否成立”，不能保存分数或多项菜单编号。</li>\n<li><strong>认为浮点转整数会四舍五入。</strong> <code>int(3.8)</code> 得 3，<code>int(-3.8)</code> 得 -3，都是向 0 截断。</li>\n<li><strong>看到表达式里有 0.0 就断定结果一定是 3.5。</strong> 若整数除法已经先完成，丢掉的小数部分不会被后面的浮点数补回来。</li>\n<li><strong>把 <code>%</code> 用在小数上。</strong> 取余运算的两个操作数都必须是整数。</li>\n</ol>\n<h2 id="关联学习">关联学习</h2>\n<p><a href="/gesp/level1/operations/">基本运算</a>会继续讲整数除法、取余、运算顺序和不同类型共同参与计算时的结果。</p>\n<section aria-labelledby="related-knowledge-data-types"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-data-types" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/variables-expressions/" class="astro-mkwt4ymd">程序基本概念</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 运算符按优先级和结合方向组成表达式；先得到右侧结果，再由赋值把结果写回变量，关系与逻辑运算得到真假值。</li>\n<li><strong>常用语法：</strong> 一级常用算术、关系、逻辑和赋值运算；整除与取余使用 <code>/</code>、<code>%</code>，增加 1 可写 <code>i = i + 1</code> 或单独使用 <code>i++</code>。</li>\n<li><strong>最小示例：</strong> <code>a = b = 3;</code> 先执行 <code>b = 3</code>，再执行 <code>a = b</code>，最后 <code>a</code> 和 <code>b</code> 都是 3。</li>\n<li><strong>关键易错点：</strong> <code>=</code> 不是 <code>==</code>，两个整数相除仍是整数结果；混合表达式不确定时应加括号并逐步记录中间值。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>变量保存数据，运算符则告诉程序怎样处理这些数据。看见一个表达式时，不能只认出其中的符号，还要回答三个问题：</p>\n<ol>\n<li>这个表达式会得到数值、真假，还是从两个结果中选一个？</li>\n<li>它只产生结果，还是会把新值写回变量？</li>\n<li>有多个运算符时，程序先算哪一部分？</li>\n</ol>\n<p>例如下面四个表达式都使用了变量 <code>a</code>、<code>b</code>，作用却不同：</p>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th>得到的结果</th>\n<th style="text-align: center">是否修改原变量</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>a + b</code></td>\n<td>两数之和</td>\n<td style="text-align: center">否</td>\n</tr>\n<tr>\n<td><code>a &gt; b</code></td>\n<td><code>true</code> 或 <code>false</code></td>\n<td style="text-align: center">否</td>\n</tr>\n<tr>\n<td><code>a += b</code></td>\n<td>更新后的 <code>a</code></td>\n<td style="text-align: center">是，修改 <code>a</code></td>\n</tr>\n<tr>\n<td><code>a++</code></td>\n<td>完成一次自增</td>\n<td style="text-align: center">是，修改 <code>a</code></td>\n</tr>\n</tbody>\n</table>\n<p>本页覆盖一级大纲中的算术、关系、赋值、自增自减、逻辑和条件运算，以及常用优先级和结合方向。位运算、复杂类型转换、嵌套三目运算和复杂表达式求值规则不在本页范围内。</p>\n<h2 id="01-算术运算">01 算术运算</h2>\n<p>运算符旁边参与运算的数据叫作<strong>操作数</strong>。在 <code>a + b</code> 中，<code>a</code>、<code>b</code> 是操作数，<code>+</code> 决定对它们做加法。</p>\n<h3 id="五种基本算术运算">五种基本算术运算</h3>\n<table>\n<thead>\n<tr>\n<th>运算</th>\n<th>运算符</th>\n<th>示例</th>\n<th style="text-align: right">结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>加法</td>\n<td><code>+</code></td>\n<td><code>12 + 5</code></td>\n<td style="text-align: right">17</td>\n</tr>\n<tr>\n<td>减法</td>\n<td><code>-</code></td>\n<td><code>12 - 5</code></td>\n<td style="text-align: right">7</td>\n</tr>\n<tr>\n<td>乘法</td>\n<td><code>*</code></td>\n<td><code>12 * 5</code></td>\n<td style="text-align: right">60</td>\n</tr>\n<tr>\n<td>除法</td>\n<td><code>/</code></td>\n<td><code>12 / 5</code></td>\n<td style="text-align: right">2</td>\n</tr>\n<tr>\n<td>取余</td>\n<td><code>%</code></td>\n<td><code>12 % 5</code></td>\n<td style="text-align: right">2</td>\n</tr>\n</tbody>\n</table>\n<p>C++ 中的乘法必须写成 <code>*</code>，不能写数学中的 <code>×</code>，也不能省略乘号：</p>\n<pre><code>int area = length * width; // 正确：用 * 计算长方形面积</code></pre>\n<p><code>2x</code> 不是 C++ 的乘法写法，必须写成 <code>2 * x</code>。</p>\n<h3 id="加号减号也能表示正负">加号、减号也能表示正负</h3>\n<p>当 <code>+</code> 或 <code>-</code> 只作用于一个操作数时，它们表示正号或负号：</p>\n<pre><code>int a = 5;int positive = +a; // positive 得到 5，a 不变int negative = -a; // negative 得到 -5，a 仍然不变</code></pre>\n<p><code>-a</code> 只是计算出 <code>a</code> 的相反数。若要修改 <code>a</code>，必须把结果重新写回：</p>\n<pre><code>a = -a; // 先求 -a，再把结果保存回 a</code></pre>\n<p>所以判断表达式是否改变变量时，要找有没有赋值、复合赋值、自增或自减，不能看到一次计算就认为原变量已经变化。</p>\n<h2 id="02-关系运算">02 关系运算</h2>\n<p>关系运算比较两个数据，结果只有 <code>true</code> 或 <code>false</code>。</p>\n<table>\n<thead>\n<tr>\n<th>运算符</th>\n<th>含义</th>\n<th>当 <code>a = 3</code>、<code>b = 5</code> 时</th>\n<th>结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>&gt;</code></td>\n<td>大于</td>\n<td><code>a &gt; b</code></td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>&gt;=</code></td>\n<td>大于等于</td>\n<td><code>b &gt;= 5</code></td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>&lt;</code></td>\n<td>小于</td>\n<td><code>a &lt; b</code></td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>&lt;=</code></td>\n<td>小于等于</td>\n<td><code>a &lt;= 3</code></td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>==</code></td>\n<td>等于</td>\n<td><code>a == b</code></td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>!=</code></td>\n<td>不等于</td>\n<td><code>a != b</code></td>\n<td><code>true</code></td>\n</tr>\n</tbody>\n</table>\n<p>关系运算只负责比较，不会修改参与比较的变量。普通 <code>cout</code> 输出真假值时，<code>true</code> 通常显示为 1，<code>false</code> 通常显示为 0。</p>\n<h3 id="赋值等号和相等比较不是一回事">赋值等号和相等比较不是一回事</h3>\n<pre><code>a = b;  // 赋值：把 b 的值保存到 a，会修改 aa == b; // 比较：判断 a 与 b 是否相等，不修改变量</code></pre>\n<p>读到等号时，先看是一个 <code>=</code> 还是两个 <code>==</code>。一个表示“保存”，两个表示“是否相等”。</p>\n<h3 id="c-不能照写数学中的链式比较">C++ 不能照写数学中的链式比较</h3>\n<p>数学中可以写 <code>1 ≤ x ≤ 10</code>。C++ 中不要写成：</p>\n<pre><code>1 &lt;= x &lt;= 10 // 不能按“x 在 1 到 10 之间”理解</code></pre>\n<p>C++ 会先计算 <code>1 &lt;= x</code>，结果只能是 <code>false</code> 或 <code>true</code>；参与后面的数值比较时，它们分别相当于 0 或 1。于是第二次比较实际是 <code>0 &lt;= 10</code> 或 <code>1 &lt;= 10</code>，结果都会为真，当然不能正确检查范围。</p>\n<p>正确写法是把两个关系分别写清，再用逻辑与连接：</p>\n<pre><code>(x &gt;= 1) &amp;&amp; (x &lt;= 10) // 下界和上界都满足，x 才在范围内</code></pre>\n<h2 id="03-整数除法与取余">03 整数除法与取余</h2>\n<p>上一页已经看到 <code>7 / 2</code> 和 <code>7 % 2</code>。本节把整数除法的“商”和“余数”放在一起讲清楚。</p>\n<h3 id="两个整数相除得到整数商">两个整数相除得到整数商</h3>\n<p>对于本页使用的正整数，<code>/</code> 会保留完整商，舍去不能整除的部分，不做四舍五入：</p>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th style="text-align: right">完整商</th>\n<th style="text-align: right">结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>17 / 5</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td><code>3 / 5</code></td>\n<td style="text-align: right">0</td>\n<td style="text-align: right">0</td>\n</tr>\n<tr>\n<td><code>10 / 5</code></td>\n<td style="text-align: right">2</td>\n<td style="text-align: right">2</td>\n</tr>\n</tbody>\n</table>\n<p>若要得到小数结果，必须让浮点数在除法发生前参与运算。例如 <code>7.0 / 2.0</code> 得到 3.5。整数与小数混合运算的完整类型转换规则会在后续等级继续学习。</p>\n<h3 id="取余得到没有分完的部分">取余得到没有分完的部分</h3>\n<p><code>%</code> 计算整数相除后的余数：</p>\n<pre><code>17 / 5 = 317 % 5 = 2</code></pre>\n<p>可以用下面的关系反向核对：</p>\n<pre><code>被除数 = 商 × 除数 + 余数17 = 3 × 5 + 2</code></pre>\n<p>对于正整数，余数满足 <code>0 &lt;= 余数 &lt; 除数</code>。如果算出的余数等于或大于除数，就说明商或余数算错了。</p>\n<h3 id="除法和取余的使用条件">除法和取余的使用条件</h3>\n<ul>\n<li><code>/</code> 和 <code>%</code> 的除数都不能为 0；</li>\n<li><code>%</code> 两侧必须是整数；</li>\n<li>本页主教学只处理非负整数。负数整数除法和负数取余的完整规则不在本课展开。</li>\n</ul>\n<p>取余常用于：</p>\n<pre><code>n % 2  // 检查非负整数 n 除以 2 的余数n % 10 // 取得非负整数 n 的个位数字n % k  // 计算每组 k 个时剩余多少个</code></pre>\n<p>这里的 <code>%</code> 表示取余，不是数学中的百分号。</p>\n<h2 id="04-赋值与复合赋值">04 赋值与复合赋值</h2>\n<p>赋值的执行顺序是：<strong>先计算右侧，再把结果保存到左侧变量。</strong></p>\n<pre><code>int a = 10;a = a + 3; // 右侧先用旧值 10 算出 13，再把 13 写回 a</code></pre>\n<p>执行后 <code>a</code> 从 10 变成 13。左侧必须是可以修改的变量，不能写 <code>3 = a</code>。</p>\n<h3 id="复合赋值把计算和写回合在一起">复合赋值把计算和写回合在一起</h3>\n<table>\n<thead>\n<tr>\n<th>复合赋值</th>\n<th>等价的完整写法</th>\n<th>执行后发生什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>a += b</code></td>\n<td><code>a = a + b</code></td>\n<td><code>a</code> 增加 <code>b</code></td>\n</tr>\n<tr>\n<td><code>a -= b</code></td>\n<td><code>a = a - b</code></td>\n<td><code>a</code> 减少 <code>b</code></td>\n</tr>\n<tr>\n<td><code>a *= b</code></td>\n<td><code>a = a * b</code></td>\n<td><code>a</code> 变为原来的 <code>b</code> 倍</td>\n</tr>\n<tr>\n<td><code>a /= b</code></td>\n<td><code>a = a / b</code></td>\n<td>商写回 <code>a</code></td>\n</tr>\n<tr>\n<td><code>a %= b</code></td>\n<td><code>a = a % b</code></td>\n<td>余数写回 <code>a</code></td>\n</tr>\n</tbody>\n</table>\n<p>例如：</p>\n<pre><code>int score = 20;score += 5; // score = score + 5，score 变成 25score *= 2; // score = score * 2，score 变成 50score -= 8; // score = score - 8，score 变成 42score /= 5; // 整数除法 42 / 5 得 8，score 变成 8score %= 3; // 8 % 3 得 2，score 变成 2</code></pre>\n<p>这五条语句必须从上到下执行。后一条看到的是前一条已经写回的新值，不能每次都从 20 重新计算。</p>\n<h3 id="连续赋值从右向左执行">连续赋值从右向左执行</h3>\n<pre><code>int a;int b;a = b = 3;</code></pre>\n<p>执行过程是：</p>\n<ol>\n<li>先执行 <code>b = 3</code>，此时 <code>b</code> 得到 3；</li>\n<li>再执行 <code>a = b</code>，此时 <code>a</code> 也得到 3。</li>\n</ol>\n<p>等价的直白写法是：</p>\n<pre><code>b = 3;a = b;</code></pre>\n<p><code>int a = b = 3;</code> 中的 <code>b</code> 必须提前定义，否则程序找不到要赋值的变量。一级主代码优先使用分步写法，连续赋值只要求能按从右到左读懂。</p>\n<p>还要注意：</p>\n<pre><code>a += 3; // a 在原值上增加 3a =+ 3; // 相当于 a = +3，直接把正数 3 赋给 a</code></pre>\n<p>符号顺序不同，含义完全不同。</p>\n<h2 id="05-自增与自减">05 自增与自减</h2>\n<p>单独计算 <code>i + 1</code> 不会修改 <code>i</code>：</p>\n<pre><code>int i = 5;i + 1; // 只产生结果 6，没有写回，i 仍为 5</code></pre>\n<p>要让 <code>i</code> 增加 1，可以写：</p>\n<pre><code>i = i + 1; // 完整写法i += 1;    // 复合赋值i++;       // 自增</code></pre>\n<p>相应地，<code>i--</code> 让 <code>i</code> 减少 1。循环变量每轮只增加 1 时可以优先写 <code>i++</code>；普通累计量或增加量不是 1 时，仍用完整赋值或复合赋值表达清楚。</p>\n<h3 id="单独使用时前置和后置结果相同">单独使用时，前置和后置结果相同</h3>\n<pre><code>++a; // a 先增加 1b++; // b 也增加 1--c; // c 减少 1d--; // d 也减少 1</code></pre>\n<p>当它们各自单独占一条语句时，前置和后置的最终效果相同。</p>\n<h3 id="放进表达式时取得的值不同">放进表达式时，取得的值不同</h3>\n<pre><code>int a = 5;int x = a++; // x 先取得旧值 5，然后 a 变成 6\nint b = 5;int y = ++b; // b 先变成 6，然后 y 取得新值 6</code></pre>\n<table>\n<thead>\n<tr>\n<th>语句</th>\n<th>表达式先取得什么</th>\n<th style="text-align: right">原变量最后是多少</th>\n<th style="text-align: right">接收变量是多少</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>x = a++</code></td>\n<td><code>a</code> 的旧值 5</td>\n<td style="text-align: right"><code>a = 6</code></td>\n<td style="text-align: right"><code>x = 5</code></td>\n</tr>\n<tr>\n<td><code>y = ++b</code></td>\n<td><code>b</code> 增加后的新值 6</td>\n<td style="text-align: right"><code>b = 6</code></td>\n<td style="text-align: right"><code>y = 6</code></td>\n</tr>\n</tbody>\n</table>\n<p>自减遵循相同规则。一级实际编程仍优先把 <code>i++</code>、<code>i--</code> 单独写成更新语句，不在一条复杂表达式中多次修改同一个变量。</p>\n<h2 id="06-逻辑运算">06 逻辑运算</h2>\n<p>逻辑运算组合或改变真假条件，结果仍然是 <code>true</code> 或 <code>false</code>。</p>\n<table>\n<thead>\n<tr>\n<th>运算符</th>\n<th>含义</th>\n<th>整体为真的条件</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>&amp;&amp;</code></td>\n<td>并且</td>\n<td>左右两个条件都为真</td>\n</tr>\n<tr>\n<td><code>||</code></td>\n<td>或者</td>\n<td>至少一个条件为真</td>\n</tr>\n<tr>\n<td><code>!</code></td>\n<td>取反</td>\n<td>原条件为假</td>\n</tr>\n</tbody>\n</table>\n<h3 id="真假表">真假表</h3>\n<table>\n<thead>\n<tr>\n<th>条件 A</th>\n<th>条件 B</th>\n<th><code>A &amp;&amp; B</code></th>\n<th><code>A || B</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>false</code></td>\n<td><code>false</code></td>\n<td><code>false</code></td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>false</code></td>\n<td><code>true</code></td>\n<td><code>false</code></td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>true</code></td>\n<td><code>false</code></td>\n<td><code>false</code></td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>true</code></td>\n<td><code>true</code></td>\n<td><code>true</code></td>\n<td><code>true</code></td>\n</tr>\n</tbody>\n</table>\n<p><code>!true</code> 的结果是 <code>false</code>，<code>!false</code> 的结果是 <code>true</code>。</p>\n<p>范围判断需要同时满足上下界，因此使用 <code>&amp;&amp;</code>：</p>\n<pre><code>bool inRange = (x &gt;= 1) &amp;&amp; (x &lt;= 10);</code></pre>\n<p>当 <code>x = 7</code> 时，左右条件都为真，<code>inRange</code> 得到 <code>true</code>；当 <code>x = 12</code> 时，右侧条件为假，整体得到 <code>false</code>。</p>\n<h3 id="短路决定右侧是否执行">短路决定右侧是否执行</h3>\n<p>C++ 先计算左侧条件。如果只看左侧就能确定整体结果，右侧不会继续计算。</p>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th>左侧结果</th>\n<th>是否计算右侧</th>\n<th>整体结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>A &amp;&amp; B</code></td>\n<td><code>false</code></td>\n<td>不计算</td>\n<td><code>false</code></td>\n</tr>\n<tr>\n<td><code>A &amp;&amp; B</code></td>\n<td><code>true</code></td>\n<td>继续计算</td>\n<td>由 B 决定</td>\n</tr>\n<tr>\n<td><code>A || B</code></td>\n<td><code>true</code></td>\n<td>不计算</td>\n<td><code>true</code></td>\n</tr>\n<tr>\n<td><code>A || B</code></td>\n<td><code>false</code></td>\n<td>继续计算</td>\n<td>由 B 决定</td>\n</tr>\n</tbody>\n</table>\n<p>短路还会决定右侧运算是否真的发生。假设 <code>a = 10</code>、<code>b = 0</code>：</p>\n<pre><code>bool safe = (b != 0) &amp;&amp; (a / b &gt; 2);</code></pre>\n<p>执行过程：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>当前部分</th>\n<th>结果</th>\n<th>下一步</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>b != 0</code></td>\n<td><code>false</code></td>\n<td><code>&amp;&amp;</code> 的整体结果已经确定为假</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>a / b &gt; 2</code></td>\n<td>不执行</td>\n<td>不会发生除以 0</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>保存结果</td>\n<td><code>safe = false</code></td>\n<td>表达式结束</td>\n</tr>\n</tbody>\n</table>\n<p>如果把左右两部分交换，程序就可能先执行 <code>a / b</code>，失去保护。因此短路表达式的顺序必须符合实际目的。</p>\n<h2 id="07-条件运算符">07 条件运算符</h2>\n<p>条件运算符也叫三目运算符。它根据一个条件，从两个结果中选择一个：</p>\n<pre><code>条件 ? 条件为真时的结果 : 条件为假时的结果</code></pre>\n<p>例如：</p>\n<pre><code>int maximum = (a &gt; b) ? a : b;</code></pre>\n<p>当 <code>a = 3</code>、<code>b = 5</code> 时：</p>\n<ol>\n<li>先计算条件 <code>a &gt; b</code>，结果为 <code>false</code>；</li>\n<li>条件为假，选择冒号后的 <code>b</code>；</li>\n<li><code>b</code> 的值是 5，所以 <code>maximum</code> 得到 5；</li>\n<li>没被选中的 <code>a</code> 不作为这次表达式的结果。</li>\n</ol>\n<p>一级只要求看懂这种简单二选一。需要执行多条语句时，后面的分支结构会使用更清楚的 <code>if-else</code>；本页不学习嵌套三目运算。</p>\n<h2 id="08-优先级与结合方向">08 优先级与结合方向</h2>\n<p>一个表达式中出现多个运算符时，优先级决定哪一类先算；优先级相同时，结合方向决定从哪边开始。</p>\n<h3 id="一级常用优先级">一级常用优先级</h3>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">从高到低</th>\n<th>运算符</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>()</code></td>\n<td>括号内先计算</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td>后置 <code>++</code>、<code>--</code></td>\n<td>表达式取得旧值后再更新</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>前置 <code>++</code>、<code>--</code>、<code>!</code>、单目 <code>+</code>、<code>-</code></td>\n<td>只作用于紧邻的一个对象或条件</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>*</code>、<code>/</code>、<code>%</code></td>\n<td>乘、除、取余</td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td><code>+</code>、<code>-</code></td>\n<td>加、减</td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td><code>&lt;</code>、<code>&lt;=</code>、<code>&gt;</code>、<code>&gt;=</code></td>\n<td>大小关系</td>\n</tr>\n<tr>\n<td style="text-align: right">7</td>\n<td><code>==</code>、<code>!=</code></td>\n<td>相等与不等</td>\n</tr>\n<tr>\n<td style="text-align: right">8</td>\n<td><code>&amp;&amp;</code></td>\n<td>逻辑与</td>\n</tr>\n<tr>\n<td style="text-align: right">9</td>\n<td><code>||</code></td>\n<td>逻辑或</td>\n</tr>\n<tr>\n<td style="text-align: right">10</td>\n<td><code>? :</code></td>\n<td>简单二选一</td>\n</tr>\n<tr>\n<td style="text-align: right">11</td>\n<td><code>=</code>、<code>+=</code>、<code>-=</code>、<code>*=</code>、<code>/=</code>、<code>%=</code></td>\n<td>赋值与复合赋值</td>\n</tr>\n</tbody>\n</table>\n<p>不要把整张表当成死记任务。做题时先找括号，再按“算术 → 关系 → 逻辑 → 赋值”分层；拿不准就补括号或拆成多条语句。</p>\n<h3 id="同级算术运算通常从左到右">同级算术运算通常从左到右</h3>\n<pre><code>20 / 5 * 2 = 4 * 2 = 810 - 3 - 2 = 7 - 2 = 5</code></pre>\n<p>赋值则从右向左结合，所以 <code>a = b = 3</code> 先修改 <code>b</code>，再修改 <code>a</code>。</p>\n<h3 id="一步一步计算混合表达式">一步一步计算混合表达式</h3>\n<p>计算：</p>\n<pre><code>3 + 2 * 4 &gt; 10 &amp;&amp; !false</code></pre>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>本步运算</th>\n<th>得到的结果</th>\n<th>结果类型</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>2 * 4</code></td>\n<td>8</td>\n<td>整数</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>3 + 8</code></td>\n<td>11</td>\n<td>整数</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><code>11 &gt; 10</code></td>\n<td><code>true</code></td>\n<td><code>bool</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>!false</code></td>\n<td><code>true</code></td>\n<td><code>bool</code></td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td><code>true &amp;&amp; true</code></td>\n<td><code>true</code></td>\n<td><code>bool</code></td>\n</tr>\n</tbody>\n</table>\n<p>普通输出最终结果时会看到 1。</p>\n<h3 id="比较先算赋值最后发生">比较先算，赋值最后发生</h3>\n<p>假设 <code>b = 5</code>：</p>\n<pre><code>int a;a = b &gt; 3;  // 等价于 a = (b &gt; 3)，比较为 true，a 得到 1a = b == 3; // 等价于 a = (b == 3)，比较为 false，a 得到 0</code></pre>\n<p>关系运算和相等比较都高于赋值，所以先得到 <code>true</code> 或 <code>false</code>，再把真假值转换成普通整数 1 或 0 写入 <code>a</code>。更容易阅读的写法是直接保存为 <code>bool</code>：</p>\n<pre><code>bool greaterThanThree = (b &gt; 3);bool equalToThree = (b == 3);</code></pre>\n<p>括号和准确的变量名能直接写出含义，也能减少把 <code>=</code> 与 <code>==</code> 看错的机会。</p>\n<h2 id="做运算题的统一检查顺序">做运算题的统一检查顺序</h2>\n<p>面对一个表达式，可以按下面的顺序检查：</p>\n<ol>\n<li>圈出操作数，确认它们是整数、小数还是真假值。</li>\n<li>找出会写回变量的运算：赋值、复合赋值、自增、自减。</li>\n<li>先算括号，再按优先级分层计算。</li>\n<li>每完成一步，就写下当前结果和结果类型。</li>\n<li>遇到 <code>/</code>、<code>%</code> 检查除数；遇到 <code>&amp;&amp;</code>、<code>||</code> 检查右侧是否真的执行。</li>\n</ol>\n<h2 id="典型例题">典型例题</h2>\n<p>学校有 <code>n</code> 名学生参加活动，每个完整小组安排 <code>k</code> 人，场地最多允许 <code>limit</code> 个小组。程序需要计算：</p>\n<ul>\n<li>能组成多少个完整小组，还剩多少人；</li>\n<li>场地最多提供多少个座位；</li>\n<li>容量是否足够、是否恰好分完，以及这两个条件是否同时成立。</li>\n</ul>\n<p>输入保证 <code>n</code>、<code>k</code>、<code>limit</code> 都是 <code>int</code> 范围内的正整数，<code>k</code> 不为 0，且 <code>k * limit</code> 不超过常见 <code>int</code> 上限。</p>\n<p>对于输入 <code>23 5 5</code>，先手算：</p>\n<pre><code>完整小组数 = 23 / 5 = 4剩余人数 = 23 % 5 = 3总座位数 = 5 * 5 = 25容量足够：23 &lt;= 25，为 true恰好分完：3 == 0，为 false两个条件同时成立：true &amp;&amp; false，为 false</code></pre>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    int k; // 题目保证 k 是正整数，可以作为除数    int limit;    cin &gt;&gt; n &gt;&gt; k &gt;&gt; limit;\n    int fullGroups = n / k; // 整数除法得到完整小组数    int remaining = n % k; // 取余得到未进入完整小组的人数    int totalSeats = k * limit; // 计算场地最多提供的座位数\n    bool capacityEnough = (n &lt;= totalSeats);    bool dividedExactly = (remaining == 0);    bool canArrange = capacityEnough &amp;&amp; dividedExactly;\n    cout &lt;&lt; fullGroups &lt;&lt; " " &lt;&lt; remaining &lt;&lt; endl;    cout &lt;&lt; totalSeats &lt;&lt; endl;    cout &lt;&lt; capacityEnough &lt;&lt; " " &lt;&lt; dividedExactly &lt;&lt; " " &lt;&lt; canArrange &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入：</p>\n<pre><code>23 5 5</code></pre>\n<p>输出：</p>\n<pre><code>4 3251 0 0</code></pre>\n<p>执行过程：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">步骤</th>\n<th>新完成的运算</th>\n<th>结果</th>\n<th>当前关键状态</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>读入三个数</td>\n<td>无计算结果</td>\n<td><code>n = 23</code>、<code>k = 5</code>、<code>limit = 5</code></td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>n / k</code></td>\n<td>完整商为 4</td>\n<td><code>fullGroups = 4</code></td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><code>n % k</code></td>\n<td>余数 3</td>\n<td><code>remaining = 3</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><code>k * limit</code></td>\n<td>25</td>\n<td><code>totalSeats = 25</code></td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td><code>n &lt;= totalSeats</code></td>\n<td><code>true</code></td>\n<td><code>capacityEnough = true</code></td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td><code>remaining == 0</code></td>\n<td><code>false</code></td>\n<td><code>dividedExactly = false</code></td>\n</tr>\n<tr>\n<td style="text-align: right">7</td>\n<td><code>true &amp;&amp; false</code></td>\n<td><code>false</code></td>\n<td><code>canArrange = false</code></td>\n</tr>\n<tr>\n<td style="text-align: right">8</td>\n<td>依次输出</td>\n<td><code>4 3</code>、<code>25</code>、<code>1 0 0</code></td>\n<td>程序结束</td>\n</tr>\n</tbody>\n</table>\n<p>可以用 <code>23 = 4 × 5 + 3</code> 核对：完整商是 4，余数是 3。这个等式也能帮助我们发现把商和余数写反的错误。</p>\n<p>还可以检查三个边界：</p>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th>完整组／剩余</th>\n<th style="text-align: right">座位数</th>\n<th>三个真假结果</th>\n<th>检查目的</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>23 5 5</code></td>\n<td><code>4 3</code></td>\n<td style="text-align: right">25</td>\n<td><code>1 0 0</code></td>\n<td>有余数但容量足够</td>\n</tr>\n<tr>\n<td><code>20 5 3</code></td>\n<td><code>4 0</code></td>\n<td style="text-align: right">15</td>\n<td><code>0 1 0</code></td>\n<td>恰好分组但容量不足</td>\n</tr>\n<tr>\n<td><code>20 5 4</code></td>\n<td><code>4 0</code></td>\n<td style="text-align: right">20</td>\n<td><code>1 1 1</code></td>\n<td>两个条件都成立</td>\n</tr>\n</tbody>\n</table>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>取 <code>a = 17</code>、<code>b = 5</code>，分别求 <code>a / b</code> 和 <code>a % b</code>。先判断两侧都是整数，再写出完整商 3 和余数 2，最后用 <code>17 = 3 × 5 + 2</code> 反向核对。</p>\n<p><strong>成功标准：</strong> 商、余数和反向等式必须同时成立，并确认除数不是 0。完成后不看提示独立完成页末“整页综合测试”；提交后逐题按括号、优先级、结合方向和写回顺序复演错题。</p>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li><strong>认为整数除法会自动得到小数。</strong> <code>7 / 2</code> 得 3。自查时先看除法发生前两侧的数据类型。</li>\n<li><strong>用 0 作除数。</strong> <code>/</code> 和 <code>%</code> 的右侧都不能为 0；看到这两个符号先检查除数范围。</li>\n<li><strong>对浮点数使用 <code>%</code>。</strong> <code>%</code> 两侧必须是整数，它不是小数运算符，也不是百分号。</li>\n<li><strong>把 <code>=</code> 与 <code>==</code> 混淆。</strong> 先问这一步要“保存新值”还是“判断相等”，再决定写一个或两个等号。</li>\n<li><strong>把 <code>+=</code> 错写成 <code>=+</code>。</strong> 复合赋值的算术符号写在等号前；<code>a =+ 3</code> 只是把正数 3 赋给 <code>a</code>。</li>\n<li><strong>认为 <code>i + 1</code> 会更新 <code>i</code>。</strong> 检查计算结果有没有写回，或者是否使用了 <code>+=</code>、<code>++</code>。</li>\n<li><strong>照写数学中的 <code>1 &lt;= x &lt;= 10</code>。</strong> 在 C++ 中应把上下界分别比较，再用 <code>&amp;&amp;</code> 连接。</li>\n<li><strong>把逻辑运算符写少一个符号。</strong> 并且写 <code>&amp;&amp;</code>，或者写 <code>||</code>；单个 <code>&amp;</code>、<code>|</code> 属于本页不讲的位运算。</li>\n<li><strong>短路保护顺序写反。</strong> 要先检查除数不为 0，再把除法放到 <code>&amp;&amp;</code> 右侧。</li>\n<li><strong>凭直觉猜混合表达式。</strong> 先找括号，再分算术、关系、逻辑和赋值；仍不确定就主动补括号或拆行。</li>\n</ol>\n<h2 id="关联学习">关联学习</h2>\n<p><a href="/gesp/level1/sequential/">顺序结构</a>会把表达式放进完整的输入、计算、输出过程。</p>\n<section aria-labelledby="related-knowledge-operations"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-operations" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/data-types/" class="astro-mkwt4ymd">数据类型</a></li></ul></section></div></section>',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 顺序结构按书写顺序逐条执行，每条语句都读取当时的变量值并留下新状态；程序语句不是同时成立的数学方程。</li>\n<li><strong>常用流程：</strong> 先读入数据，再按题意计算中间量，最后按要求输出；修改变量时在每条语句执行后记录一次状态快照。</li>\n<li><strong>最小示例：</strong> <code>a = 2; a = a + 3;</code> 先把 2 保存到 <code>a</code>，第二句再读出 2 并写回 5，最终 <code>a = 5</code>。</li>\n<li><strong>关键易错点：</strong> 交换两个变量时不能先覆盖旧值；单位换算要用余数继续拆分，输出也必须在正确的执行时刻读取变量。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应 GESP C++ 一级大纲中的“顺序结构”。前面已经学过变量怎样保存当前值、赋值怎样先算右侧再写左侧；现在把多条语句连成一条真实执行路线。</p>\n<p>本页集中解决三个问题：</p>\n<ol>\n<li><strong>为什么顺序结构是程序的默认执行方式？</strong></li>\n<li><strong>每执行一条语句，变量状态怎样一步步变化？</strong></li>\n<li><strong>怎样用输入、手算、状态表和实际输出验证一段顺序程序？</strong></li>\n</ol>\n<p>本页所有主过程都沿一条路线执行，不使用条件选择，也不重复执行某段代码。分支和循环会在后面的课程中分别学习。</p>\n<h2 id="01-从上到下执行">01 从上到下执行</h2>\n<h3 id="顺序结构是一条连续路线">顺序结构是一条连续路线</h3>\n<p>程序进入 <code>main</code> 后，如果没有分支、循环等结构改变路线，普通语句会按照书写顺序从上到下执行。前一条语句执行完成，下一条才开始；后一条看到的是前面已经留下的新状态。</p>\n<pre><code>进入 main   ↓执行第 1 条语句，更新程序状态   ↓执行第 2 条语句，读取更新后的状态   ↓继续执行后面的语句   ↓return 0; 结束 main</code></pre>\n<p>这里说的是 <code>main</code> 代码块内本次实际走到的普通语句。头文件、命名空间和注释不作为运行时普通语句执行；<code>return 0;</code> 一旦执行，会立即结束 <code>main</code>，不会再继续执行它后面的语句。</p>\n<h3 id="三条语句不能同时计算">三条语句不能同时计算</h3>\n<p>看下面的执行链：</p>\n<pre><code>int a = 3;a = a + 2;a = a * 4;</code></pre>\n<p>正确过程是：</p>\n<ol>\n<li>第一行建立 <code>a</code>，当前值为 3。</li>\n<li>第二行读取当时的 3，计算 <code>3 + 2 = 5</code>，再把 5 写回 <code>a</code>。</li>\n<li>第三行读取刚刚得到的 5，计算 <code>5 * 4 = 20</code>，再把 20 写回 <code>a</code>。</li>\n</ol>\n<p>最终 <code>a = 20</code>。如果第三行仍拿初值 3 计算，就会得到错误的 12；如果先算乘法再回头加 2，也违背了程序的语句顺序。</p>\n<p>顺序程序不能像一组数学方程那样同时成立。每个赋值号都是一次发生在具体时刻的写入。</p>\n<h3 id="一条语句完成后状态才交给下一条">一条语句完成后，状态才交给下一条</h3>\n<p>可以把每条语句看成一次状态转换：</p>\n<pre><code>执行前状态 ──当前语句──→ 执行后状态执行后状态 ──下一语句──→ 新的执行后状态</code></pre>\n<p>第二行执行完之前，<code>a</code> 还是 3；第二行完成后，<code>a</code> 才变成 5。第三行必须从这个最新状态开始。</p>\n<p>这种“上一行产出、下一行使用”的关系，是顺序结构最重要的因果关系。</p>\n<h2 id="02-变量变化追踪">02 变量变化追踪</h2>\n<h3 id="状态表记录每行执行后的快照">状态表记录每行执行后的快照</h3>\n<p>读一段顺序程序时，可以画<strong>变量状态表</strong>。表格的一行表示“当前语句已经完整执行以后”，而不是语句执行到一半的临时猜测。</p>\n<p>仍用前面的三条语句：</p>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th>右侧怎样计算</th>\n<th style="text-align: right">执行后 <code>a</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>int a = 3;</code></td>\n<td>用 3 初始化 <code>a</code></td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td><code>a = a + 2;</code></td>\n<td>读取旧值 3，计算 <code>3 + 2</code></td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>a = a * 4;</code></td>\n<td>读取新值 5，计算 <code>5 * 4</code></td>\n<td style="text-align: right">20</td>\n</tr>\n</tbody>\n</table>\n<p>状态表直接回答了自测题：三条语句执行完以后，<code>a</code> 是 20。</p>\n<h3 id="多个变量怎样记录">多个变量怎样记录</h3>\n<p>每个变量占一列。当前语句只修改其中一个变量时，其他列沿用上一行的值。</p>\n<p>假设初始有 <code>x = 2</code>、<code>y = 5</code>，接着执行 <code>x = y;</code>、<code>y = 8;</code>：</p>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th style="text-align: right"><code>x</code></th>\n<th style="text-align: right"><code>y</code></th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>完成初始化</td>\n<td style="text-align: right">2</td>\n<td style="text-align: right">5</td>\n<td>两个变量都有确定值</td>\n</tr>\n<tr>\n<td><code>x = y;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">5</td>\n<td>把 <code>y</code> 当时的 5 复制给 <code>x</code></td>\n</tr>\n<tr>\n<td><code>y = 8;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">8</td>\n<td>只修改 <code>y</code>，<code>x</code> 不会自动跟着变化</td>\n</tr>\n</tbody>\n</table>\n<p>表中没有被当前语句修改的格子也要保留，避免误以为变量“重新变回初值”或“自动联动”。</p>\n<h3 id="画状态表的固定步骤">画状态表的固定步骤</h3>\n<ol>\n<li>按程序出现顺序列出需要追踪的变量。</li>\n<li>从 <code>main</code> 内第一条相关语句开始，不跳行。</li>\n<li>先用上一行状态计算当前语句右侧。</li>\n<li>当前语句写回哪些变量，就更新哪些列。</li>\n<li>其他变量照抄上一行的当前值。</li>\n<li>遇到输出语句，记录此刻真正输出的内容。</li>\n</ol>\n<p>如果某个变量还没有初始化或赋值，不要给它随意填 0，应标成“尚无可安全读取的值”，并检查程序是否会在读取它之前完成赋值。</p>\n<h2 id="03-输入计算输出">03 输入、计算、输出</h2>\n<h3 id="顺序程序常见的三段职责">顺序程序常见的三段职责</h3>\n<p>许多一级计算题可以整理成三段：</p>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>要回答的问题</th>\n<th>常见语句职责</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>输入</td>\n<td>题目给了哪些数据，顺序是什么？</td>\n<td>把外部数据读入变量</td>\n</tr>\n<tr>\n<td>计算</td>\n<td>需要哪些公式和中间量？</td>\n<td>读取当前值，计算并保存结果</td>\n</tr>\n<tr>\n<td>输出</td>\n<td>最终交出哪些量，格式怎样？</td>\n<td>按题目顺序输出结果</td>\n</tr>\n</tbody>\n</table>\n<p>“三段式”是职责划分，不要求每段只能写一行。一个问题可能需要先算多个中间量，再统一输出。</p>\n<h3 id="从题意写成语句">从题意写成语句</h3>\n<p>以“输入长方形的长和宽，输出面积与周长”为例：</p>\n<ol>\n<li>**输入量：**长 <code>length</code>、宽 <code>width</code>；</li>\n<li><strong>面积公式：</strong><code>area = length * width</code>；</li>\n<li><strong>周长公式：</strong><code>perimeter = (length + width) * 2</code>；</li>\n<li>**输出要求：**先面积，空一格，再周长。</li>\n</ol>\n<p>自测题问“哪条语句能计算并保存面积”，应选择：</p>\n<pre><code>area = length * width;</code></pre>\n<p><code>area = length + width;</code> 公式错误；<code>area == length * width;</code> 只是在比较；<code>length * width = area;</code> 把不能接收赋值的计算结果放到了左边。</p>\n<h3 id="先核对范围和中间结果">先核对范围和中间结果</h3>\n<p>假设题目约定 <code>length</code>、<code>width</code> 都是 1 到 30000 的整数：</p>\n<ul>\n<li>最大面积：<code>30000 * 30000 = 900000000</code>；</li>\n<li>最大周长：<code>(30000 + 30000) * 2 = 120000</code>。</li>\n</ul>\n<p>两个最大中间结果都没有超过常见 32 位 <code>int</code> 上限，因此本例使用 <code>int</code> 安全。若题目范围改变，要重新估算，不能因为样例很小就默认类型一定合适。</p>\n<h3 id="输出也有执行时刻">输出也有执行时刻</h3>\n<p>输出语句读取的是执行到那一刻变量中的当前值。如果在计算面积之前就输出 <code>area</code>，它可能尚未得到正确值；如果计算后又修改了 <code>area</code>，输出看到的会是修改后的新值。</p>\n<p>因此，输入、计算、输出的先后次序决定了数据是否已经准备好，不能只把它们当作页面上的三个区块。</p>\n<h2 id="04-临时变量交换">04 临时变量交换</h2>\n<h3 id="为什么直接互相赋值会丢数据">为什么直接互相赋值会丢数据</h3>\n<p>假设开始时 <code>x = 3</code>、<code>y = 5</code>，希望交换为 <code>x = 5</code>、<code>y = 3</code>。如果直接写：</p>\n<pre><code>x = y;y = x;</code></pre>\n<p>第一行已经用 5 覆盖了 <code>x</code> 原来的 3；第二行再读取 <code>x</code> 时，只能读到 5。最后两个变量都会是 5，旧的 3 已经丢失。</p>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th style="text-align: right"><code>x</code></th>\n<th style="text-align: right"><code>y</code></th>\n<th>问题</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>初始状态</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td>目标是交换</td>\n</tr>\n<tr>\n<td><code>x = y;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">5</td>\n<td>旧 <code>x = 3</code> 已被覆盖</td>\n</tr>\n<tr>\n<td><code>y = x;</code></td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">5</td>\n<td>已经找不回 3</td>\n</tr>\n</tbody>\n</table>\n<h3 id="用临时变量保住即将被覆盖的旧值">用临时变量保住即将被覆盖的旧值</h3>\n<p>正确顺序是：</p>\n<pre><code>int temp = x;x = y;y = temp;</code></pre>\n<p>逐步追踪：</p>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th style="text-align: right"><code>temp</code></th>\n<th style="text-align: right"><code>x</code></th>\n<th style="text-align: right"><code>y</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>初始状态</td>\n<td style="text-align: right">尚未建立</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>int temp = x;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>x = y;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">5</td>\n</tr>\n<tr>\n<td><code>y = temp;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">3</td>\n</tr>\n</tbody>\n</table>\n<p><code>temp</code> 的职责只是在覆盖 <code>x</code> 之前保存旧值。执行完三条语句后，<code>x</code>、<code>y</code> 已经完成交换。</p>\n<p>这个例子说明，顺序不是可以随意调换的外观。<code>int temp = x;</code> 必须发生在 <code>x = y;</code> 之前，否则要保护的数据已经丢失。</p>\n<h2 id="05-单位换算">05 单位换算</h2>\n<h3 id="除法取完整单位取余保留剩余量">除法取完整单位，取余保留剩余量</h3>\n<p>把总秒数拆成“小时、分钟、秒”时，每一步都使用上一步留下的状态：</p>\n<pre><code>int totalSeconds = 3723;int hour = totalSeconds / 3600;int rest = totalSeconds % 3600;int minute = rest / 60;int second = rest % 60;</code></pre>\n<p>按照顺序执行：</p>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th>计算</th>\n<th>新得到的状态</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>hour = totalSeconds / 3600</code></td>\n<td><code>3723 / 3600</code></td>\n<td><code>hour = 1</code></td>\n</tr>\n<tr>\n<td><code>rest = totalSeconds % 3600</code></td>\n<td><code>3723 % 3600</code></td>\n<td><code>rest = 123</code></td>\n</tr>\n<tr>\n<td><code>minute = rest / 60</code></td>\n<td><code>123 / 60</code></td>\n<td><code>minute = 2</code></td>\n</tr>\n<tr>\n<td><code>second = rest % 60</code></td>\n<td><code>123 % 60</code></td>\n<td><code>second = 3</code></td>\n</tr>\n</tbody>\n</table>\n<p>结果是 1 小时 2 分 3 秒。可以反向检查：<code>1 * 3600 + 2 * 60 + 3 = 3723</code>。</p>\n<h3 id="为什么分钟要从-rest-中计算">为什么分钟要从 <code>rest</code> 中计算</h3>\n<p><code>totalSeconds / 60</code> 得到的是总共包含多少个完整分钟，其中包括那 1 小时里的 60 分钟。若目标是分别输出“小时部分、分钟部分、秒部分”，分钟必须从不足一小时的 <code>rest</code> 中继续拆。</p>\n<p>顺序关系是：</p>\n<pre><code>总秒数  ├─ 除以 3600 → 完整小时  └─ 对 3600 取余 → 不足一小时的秒数 rest                         ├─ 除以 60 → 完整分钟                         └─ 对 60 取余 → 最后秒数</code></pre>\n<p>这条依赖链决定了 <code>rest</code> 必须先算，<code>minute</code> 和 <code>second</code> 才能使用它。</p>\n<h3 id="用自测数据快速验证">用自测数据快速验证</h3>\n<p>若总秒数为 125：</p>\n<ul>\n<li><code>minute = 125 / 60</code> 得 2；</li>\n<li><code>second = 125 % 60</code> 得 5；</li>\n<li>反向检查：<code>2 * 60 + 5 = 125</code>。</li>\n</ul>\n<p>所以结果是 2 分 5 秒。若总秒数正好为 120，余数为 0，结果是 2 分 0 秒；这里的 0 表示恰好整除，是正确状态。</p>\n<h2 id="写顺序程序的统一方法">写顺序程序的统一方法</h2>\n<p>面对一题输入—计算—输出问题，可以走下面六步：</p>\n<ol>\n<li>**列输入：**写清数据数量、顺序、含义和范围。</li>\n<li>**列目标：**明确最后输出哪些值，顺序和格式是什么。</li>\n<li>**写公式：**把题意拆成一个个能执行的计算步骤。</li>\n<li>**排顺序：**确保每个变量在第一次读取前已经有确定值；保护会被覆盖的旧值。</li>\n<li>**查范围：**估算输入、乘积、总和等中间结果，选择合适类型。</li>\n<li>**做验证：**记录一组输入，先手算期望结果，再画状态表，最后与实际输出逐字比较。</li>\n</ol>\n<p>这六步能同时检查“公式是否正确”和“语句是否放在正确时刻”。只盯着最后一行答案，很难发现问题到底从哪一步开始。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>关键动作</th>\n<th>最直接的验证方式</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>追踪语句</td>\n<td>从上到下，一行完成后再进入下一行</td>\n<td>每行执行后更新状态表</td>\n</tr>\n<tr>\n<td>计算问题</td>\n<td>输入 → 计算 → 输出</td>\n<td>手算与实际输出比较</td>\n</tr>\n<tr>\n<td>保存结果</td>\n<td>右侧先算，左侧后写</td>\n<td>标出赋值前后的变量值</td>\n</tr>\n<tr>\n<td>交换变量</td>\n<td>覆盖前先用 <code>temp</code> 保存旧值</td>\n<td>检查中间状态是否还保留两份数据</td>\n</tr>\n<tr>\n<td>拆分单位</td>\n<td>除法求完整单位，取余求剩余量</td>\n<td>用大单位还原成原总量</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>继续完成长方形任务：输入长和宽，输出面积和周长。输入范围均为 1 到 30000。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int length;    int width;    cin &gt;&gt; length &gt;&gt; width; // 按“长、宽”的顺序读取两个数据\n    int area = length * width; // 面积等于长乘宽    int perimeter = (length + width) * 2; // 周长先算长加宽，再乘 2\n    cout &lt;&lt; area &lt;&lt; " " &lt;&lt; perimeter &lt;&lt; endl; // 按“面积 空格 周长”输出    return 0;}</code></pre>\n<p>用输入 <code>3 5</code> 验证。</p>\n<h3 id="第一步先手算期望输出">第一步：先手算期望输出</h3>\n<ul>\n<li>面积：<code>3 * 5 = 15</code>；</li>\n<li>周长：<code>(3 + 5) * 2 = 16</code>；</li>\n<li>题目要求输出“面积 空格 周长”，所以期望输出为 <code>15 16</code>。</li>\n</ul>\n<h3 id="第二步画状态表">第二步：画状态表</h3>\n<table>\n<thead>\n<tr>\n<th>已完成的语句</th>\n<th style="text-align: right"><code>length</code></th>\n<th style="text-align: right"><code>width</code></th>\n<th style="text-align: right"><code>area</code></th>\n<th style="text-align: right"><code>perimeter</code></th>\n<th>当前输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>cin &gt;&gt; length &gt;&gt; width;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">尚未建立</td>\n<td style="text-align: right">尚未建立</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>int area = length * width;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">15</td>\n<td style="text-align: right">尚未建立</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>int perimeter = (length + width) * 2;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">15</td>\n<td style="text-align: right">16</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>cout &lt;&lt; area &lt;&lt; " " &lt;&lt; perimeter &lt;&lt; endl;</code></td>\n<td style="text-align: right">3</td>\n<td style="text-align: right">5</td>\n<td style="text-align: right">15</td>\n<td style="text-align: right">16</td>\n<td><code>15 16</code> 并换行</td>\n</tr>\n</tbody>\n</table>\n<h3 id="第三步运行并逐字比较">第三步：运行并逐字比较</h3>\n<p>实际输入：</p>\n<pre><code>3 5</code></pre>\n<p>实际输出应为：</p>\n<pre><code>15 16</code></pre>\n<p>手算结果、状态表和实际输出三者一致，普通样例通过。还要补充边界数据：</p>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th>期望输出</th>\n<th>检查目的</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>1 1</code></td>\n<td><code>1 4</code></td>\n<td>最小范围、长宽相等</td>\n</tr>\n<tr>\n<td><code>1 8</code></td>\n<td><code>8 18</code></td>\n<td>长宽不相等，检查周长括号</td>\n</tr>\n<tr>\n<td><code>30000 30000</code></td>\n<td><code>900000000 120000</code></td>\n<td>最大范围与中间结果</td>\n</tr>\n</tbody>\n</table>\n<p>这些数据都沿同一条顺序路线执行。变化的是输入和变量值，语句顺序没有改变。</p>\n<h2 id="顺序程序的验证清单">顺序程序的验证清单</h2>\n<p>发现输出不对时，不要同时改很多行。按证据逐项检查：</p>\n<table>\n<thead>\n<tr>\n<th>检查项</th>\n<th>要问的问题</th>\n<th>可用证据</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>输入</td>\n<td>数量、顺序和当前值是否正确？</td>\n<td>原始输入、输入后的状态表</td>\n</tr>\n<tr>\n<td>公式</td>\n<td>每个表达式是否符合题意？</td>\n<td>手算过程、单位和括号</td>\n</tr>\n<tr>\n<td>顺序</td>\n<td>使用变量前是否已经准备好？</td>\n<td>逐行状态表</td>\n</tr>\n<tr>\n<td>覆盖</td>\n<td>旧值是否在保存前就被赋值覆盖？</td>\n<td>交换过程中的前后状态</td>\n</tr>\n<tr>\n<td>范围</td>\n<td>输入和中间结果是否能放进类型？</td>\n<td>最大值估算</td>\n</tr>\n<tr>\n<td>输出</td>\n<td>内容、顺序、空格和换行是否准确？</td>\n<td>期望文本与实际文本逐字比较</td>\n</tr>\n</tbody>\n</table>\n<p>修正后用同一组失败数据重新运行，确认错误现象真的消失；再补一组边界数据，避免只修好了一个样例。</p>\n<h2 id="引导练习不记录进度">引导练习（不记录进度）</h2>\n<p>令 <code>a = 4</code>、<code>b = 7</code>，使用临时变量交换。先填写三个状态：保存旧 <code>a</code> 后 <code>temp = 4</code>；执行 <code>a = b</code> 后 <code>a = 7</code>；执行 <code>b = temp</code> 后 <code>b = 4</code>。</p>\n<p><strong>成功标准：</strong> 状态表最后必须是 <code>a = 7</code>、<code>b = 4</code>，且任何一步都能找到尚未丢失的旧值。完成后不看提示独立完成页末“整页综合测试”；提交后逐行重画错题的变量快照。</p>\n<h2 id="常见错误">常见错误</h2>\n<h3 id="把多条赋值当成同时成立的方程">把多条赋值当成同时成立的方程</h3>\n<p><strong>错误：</strong> 用初值 3 同时计算 <code>a = a + 2;</code> 和 <code>a = a * 4;</code>。</p>\n<p><strong>检查：</strong> 每执行一行就更新状态。第三行必须读取第二行留下的 5。</p>\n<h3 id="输入变量还没准备好就开始计算">输入变量还没准备好就开始计算</h3>\n<p><strong>错误：</strong> 把面积计算放在 <code>cin</code> 之前，或读取未初始化变量。</p>\n<p><strong>检查：</strong> 顺着程序找每个变量的第一次读取，确认此前已有输入、初始化或赋值。</p>\n<h3 id="面积公式或赋值方向写错">面积公式或赋值方向写错</h3>\n<p><strong>错误：</strong> 写成 <code>area = length + width;</code>、<code>area == length * width;</code> 或 <code>length * width = area;</code>。</p>\n<p><strong>检查：</strong> 先手算公式，再确认左侧是接收结果的变量，使用的是赋值号 <code>=</code>。</p>\n<h3 id="交换时先覆盖旧值">交换时先覆盖旧值</h3>\n<p><strong>错误：</strong> 直接执行 <code>x = y; y = x;</code>。</p>\n<p><strong>检查：</strong> 画三列表，确认第一步已用 <code>temp</code> 保存旧 <code>x</code>。</p>\n<h3 id="单位拆分重复计算大单位">单位拆分重复计算大单位</h3>\n<p><strong>错误：</strong> 已算出小时后，仍直接用总秒数除以 60 得分钟部分。</p>\n<p><strong>检查：</strong> 先对 3600 取余得到 <code>rest</code>，再从 <code>rest</code> 中拆分钟和秒。</p>\n<h3 id="公式正确输出顺序或格式错误">公式正确，输出顺序或格式错误</h3>\n<p><strong>错误：</strong> 题目要求“面积 周长”，却输出“周长 面积”，或擅自加提示文字。</p>\n<p><strong>检查：</strong> 把期望输出写成完整文本，与实际输出逐字符比较。</p>\n<h3 id="只测普通样例不估算最大中间结果">只测普通样例，不估算最大中间结果</h3>\n<p><strong>错误：</strong> 样例能算对就认为类型安全。</p>\n<p><strong>检查：</strong> 代入输入上限，分别计算乘积、总和等可能出现的最大值。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>前面的<a href="/gesp/level1/variables-expressions/">程序基本概念</a>解释了变量和赋值，<a href="/gesp/level1/data-types/">基本数据类型</a>与<a href="/gesp/level1/operations/">基本运算</a>提供了类型和表达式规则。下一页<a href="/gesp/level1/branch/">分支结构</a>会在顺序执行的基础上加入条件：程序先计算条件，再选择本次真正要走的路线。</p>\n<section aria-labelledby="related-knowledge-sequential"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-sequential" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/operations/" class="astro-mkwt4ymd">基本运算</a></li></ul></section></div></section>',
        '<h2 id="本章先记住">本章先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 分支结构先根据当前数据完成判断，再选择本次要走的路线；没有被选中的互斥路线不会执行。</li>\n<li><strong>常用结构：</strong> 范围、大小和复合条件用 <code>if</code>；一个整数或字符匹配多个固定值时可以用 <code>switch-case</code>。</li>\n<li><strong>最小示例：</strong> <code>score = 72 → score &gt;= 60 为 true → 进入“及格”路线</code>。</li>\n<li><strong>关键易错点：</strong> 写完条件还要检查边界、判断顺序和路线是否遗漏；只测一个普通数据，通常发现不了这些问题。</li>\n</ul>\n<p>前一章的顺序结构始终沿同一条路线向下执行。真实题目常常要求程序看数据办事：温度低于 0 才提示结冰，成绩落在哪个区间就输出哪个等级，菜单编号不同则完成不同操作。分支结构就是程序中的选择点。</p>\n<p>两个子课分别讲清 <code>if</code> 和 <code>switch-case</code> 的语法与真实执行。先按顺序学完它们，再用本章的选择与验证方法把知识连起来：看懂题目的判断方式，选对结构，用测试数据确认每条路线都正确。</p>\n<h2 id="从顺序执行到选择路线">从顺序执行到选择路线</h2>\n<p>分支不会取消顺序执行。程序到达选择点以前，普通语句仍按顺序完成；得到判断结果以后，只执行本次选中的路线；离开分支后，又从后续语句继续向下执行。</p>\n<pre><code>前面的顺序语句        ↓读取变量当前值，完成条件判断或固定值匹配        ↓   选择本次路线    ↙         ↘ 路线 A      路线 B    ↘         ↙   回到共同的后续语句</code></pre>\n<p>读一段分支时，可以按五步追踪：</p>\n<ol>\n<li>记录到达分支前各变量的当前值；</li>\n<li>把当前值代入条件，或取得 <code>switch</code> 表达式的值；</li>\n<li>找到本次真正进入的路线；</li>\n<li>标出没有执行的互斥路线；</li>\n<li>确认分支结束后从哪条语句继续。</li>\n</ol>\n<p>这里的“一次只走一条”指同一条 <code>if-else-if-else</code> 链或设计成独立结果的 <code>switch</code>。多个彼此独立的 <code>if</code> 会各自判断，一次可能进入多个代码块。</p>\n<h2 id="两个子课怎样衔接">两个子课怎样衔接</h2>\n<p>建议按下面的顺序学习，再回到本页做综合测试：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>子课</th>\n<th>学完后的可观察结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><a href="/gesp/level1/branch/if/">if 条件分支</a></td>\n<td>给出一组实际数据，能算出条件真假，指出单分支、双分支或多分支会走哪条路线，并检查范围边界</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><a href="/gesp/level1/branch/switch-case/">switch-case 分支</a></td>\n<td>给出一个整数或字符，能找到匹配入口，追踪 <code>break</code>、<code>default</code> 和贯穿带来的实际输出</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>回到本页</td>\n<td>能从题目规则选择结构，列出全部结果路线，并设计覆盖边界与非法输入的测试数据</td>\n</tr>\n</tbody>\n</table>\n<p>本章只处理一级常见的单层选择。<code>if</code> 子课会说明大括号范围和 <code>else</code> 的归属，但不把多层嵌套分支当作一级主任务；<code>switch</code> 的嵌套也留到后续等级。简单三目运算符已经在<a href="/gesp/level1/operations/#07-%E6%9D%A1%E4%BB%B6%E8%BF%90%E7%AE%97%E7%AC%A6">基本运算</a>中用于认识二选一，一级主程序仍优先写更容易追踪的完整 <code>if-else</code>。</p>\n<h2 id="判断方式决定写法">判断方式决定写法</h2>\n<p>先问“题目怎样区分情况”，再决定语法。不要因为刚学过某种结构，就把所有题都改成同一种写法。</p>\n<table>\n<thead>\n<tr>\n<th>题目的判断方式</th>\n<th>推荐结构</th>\n<th style="text-align: right">一次可能执行几条路线</th>\n<th>例子</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>条件成立时才额外做一件事，条件不成立时不用替代处理</td>\n<td>单分支 <code>if</code></td>\n<td style="text-align: right">0 或 1 条</td>\n<td>温度低于 0 时提示结冰</td>\n</tr>\n<tr>\n<td>条件真假分别对应一种结果</td>\n<td><code>if-else</code></td>\n<td style="text-align: right">恰好 1 条</td>\n<td>整数是偶数还是奇数</td>\n</tr>\n<tr>\n<td>三个及以上互斥范围，要按顺序选择第一条成立路线</td>\n<td><code>if-else if-else</code></td>\n<td style="text-align: right">最多 1 条；有末尾 <code>else</code> 时恰好 1 条</td>\n<td>按成绩区间输出等级</td>\n</tr>\n<tr>\n<td>多个条件可以同时成立，每个条件都要单独处理</td>\n<td>多个独立 <code>if</code></td>\n<td style="text-align: right">0、1 或多条</td>\n<td>同时标记“及格”和“优秀”</td>\n</tr>\n<tr>\n<td>一个整数或字符与多个离散固定值比较</td>\n<td><code>switch-case</code></td>\n<td style="text-align: right">从一个入口开始执行</td>\n<td>菜单编号、星期编号</td>\n</tr>\n</tbody>\n</table>\n<p><code>switch</code> 适合列出 <code>1</code>、<code>2</code>、<code>3</code> 或 <code>\'A\'</code>、<code>\'D\'</code> 这样的固定值，不直接处理“至少 60 分”或“0 到 100 之间”的范围。范围、取余和多个逻辑条件交给 <code>if</code> 更直接。</p>\n<h2 id="先把题目整理成决策表">先把题目整理成决策表</h2>\n<p>代码只是把规则写给计算机。规则本身如果有重叠、缺口或顺序错误，语法正确也会得到错误结果。</p>\n<p>以“90 分及以上输出 A，60 分及以上输出 B，否则输出 C”为例。假设题目保证成绩在 0 到 100 之间，先写出每条路线真正负责的范围：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">判断顺序</th>\n<th>写出的条件</th>\n<th>实际负责范围</th>\n<th>命中后的结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><code>score &gt;= 90</code></td>\n<td>90 到 100</td>\n<td>A</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>score &gt;= 60</code></td>\n<td>60 到 89，因为 90 以上已被第一条取走</td>\n<td>B</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><code>else</code></td>\n<td>0 到 59</td>\n<td>C</td>\n</tr>\n</tbody>\n</table>\n<p>第二条虽然只写了下界，实际不会接到 90 分，因为程序找到第一条成立路线后就停止检查本链后面的条件。若把 <code>score &gt;= 60</code> 放在前面，90 分会先进入 B 路线，A 路线就失去入口。</p>\n<p>这张表也暴露了一个前提：输入范围由题目保证。如果题目还要求处理小于 0 或大于 100 的成绩，就要先增加“成绩非法”的路线，不能让非法值落入普通等级。</p>\n<h2 id="综合应用闰年与月份天数">综合应用：闰年与月份天数</h2>\n<p>月份天数把复合条件、互斥路线和非法输入放在同一个问题中，适合用来检查整章是否真正学会。先判断月份是否合法，再处理 2 月、30 天月份和剩余合法月份。</p>\n<p>2 月需要使用闰年规则：年份能被 400 整除，或者能被 4 整除但不能被 100 整除。写成条件是 <code>year % 400 == 0 || (year % 4 == 0 &amp;&amp; year % 100 != 0)</code>。</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">年份</th>\n<th style="text-align: center">能被 400 整除</th>\n<th style="text-align: center">能被 4 整除且不能被 100 整除</th>\n<th>年份结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">2000</td>\n<td style="text-align: center">true</td>\n<td style="text-align: center">false</td>\n<td>闰年</td>\n</tr>\n<tr>\n<td style="text-align: right">1900</td>\n<td style="text-align: center">false</td>\n<td style="text-align: center">false</td>\n<td>平年</td>\n</tr>\n<tr>\n<td style="text-align: right">2024</td>\n<td style="text-align: center">false</td>\n<td style="text-align: center">true</td>\n<td>闰年</td>\n</tr>\n</tbody>\n</table>\n<p>完整月份决策可以整理成四条互斥路线：</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">判断顺序</th>\n<th>要检查的问题</th>\n<th>成立时的最终结果</th>\n<th>不成立时继续</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>`month &lt; 1</td>\n<td></td>\n<td>month &gt; 12`</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><code>month == 2</code></td>\n<td>闰年 29 天，平年 28 天</td>\n<td>检查 30 天月份</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td>`month == 4</td>\n<td></td>\n<td>month == 6</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td>已排除非法月份、2 月和 30 天月份</td>\n<td>31 天</td>\n<td>判断结束</td>\n</tr>\n</tbody>\n</table>\n<p>用实际输入走一遍，可以确认每条路线的含义：</p>\n<table>\n<thead>\n<tr>\n<th>输入 <code>year month</code></th>\n<th>选择过程</th>\n<th>最终结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>2024 2</code></td>\n<td>月份合法 → 2 月 → 2024 是闰年</td>\n<td>29 天</td>\n</tr>\n<tr>\n<td><code>1900 2</code></td>\n<td>月份合法 → 2 月 → 1900 是平年</td>\n<td>28 天</td>\n</tr>\n<tr>\n<td><code>2024 4</code></td>\n<td>月份合法 → 不是 2 月 → 30 天月份</td>\n<td>30 天</td>\n</tr>\n<tr>\n<td><code>2024 12</code></td>\n<td>月份合法 → 不是 2 月 → 不是 30 天月份</td>\n<td>31 天</td>\n</tr>\n<tr>\n<td><code>2024 13</code></td>\n<td>第一条就判定月份非法</td>\n<td>非法，不能继续归入 31 天</td>\n</tr>\n</tbody>\n</table>\n<p>这道题适合使用 <code>if</code>，因为它包含范围、取余和复合逻辑条件。即使月份编号看起来像固定整数，也不能只用一个简单 <code>switch</code> 解决闰年 2 月的判断。</p>\n<h2 id="怎样验证分支路径">怎样验证分支路径</h2>\n<p>分支程序不能只用一个“看起来正常”的数据。可靠的测试要同时检查三件事：</p>\n<ol>\n<li><strong>路线覆盖：</strong> 每一种最终结果至少实际运行一次；</li>\n<li><strong>边界覆盖：</strong> 对 <code>&gt;、&gt;=、&lt;、&lt;=</code> 使用边界值和相邻值；</li>\n<li><strong>互斥与完整：</strong> 需要唯一结果时，每组输入只能得到一个结果，同时不能留下没有结果的合法输入。</li>\n</ol>\n<p>成绩分级至少测试 59、60、89、90：59 和 60 检查及格边界两侧，89 和 90 检查优秀边界两侧。月份天数至少覆盖非法月份、平年 2 月、闰年 2 月、30 天月份和 31 天月份。<code>switch</code> 任务则要覆盖每组固定值、共享处理的入口以及 <code>default</code>。</p>\n<p>运行自己的程序时，可以用同一张记录表对照预期：</p>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th>条件或匹配的实际结果</th>\n<th>选中的路线</th>\n<th>明确跳过的路线</th>\n<th>预期结果</th>\n<th>实际结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>一组具体数据</td>\n<td>逐项写出 <code>true</code>、<code>false</code> 或匹配值</td>\n<td>写路线名称</td>\n<td>写未执行路线</td>\n<td>手算得到</td>\n<td>运行后填写</td>\n</tr>\n</tbody>\n</table>\n<p>若预期与实际不一致，先看第一个不同点：条件算错通常是运算或边界问题；选中路线不对通常是判断顺序问题；一次出现多个互斥结果，通常是把一条 <code>else-if</code> 链写成了多个独立 <code>if</code>，或 <code>switch</code> 路线漏写了应有的 <code>break</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>大主题</th>\n<th>先问自己的问题</th>\n<th>详细入口</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>条件与范围</td>\n<td>当前值代入后是真还是假，边界是否包含</td>\n<td><a href="/gesp/level1/branch/if/#%E6%9D%A1%E4%BB%B6%E6%80%8E%E6%A0%B7%E5%BE%97%E5%88%B0-true-%E6%88%96-false">if 条件分支</a></td>\n</tr>\n<tr>\n<td>单分支、双分支和多分支</td>\n<td>一次允许执行几条路线，是否需要兜底结果</td>\n<td><a href="/gesp/level1/branch/if/#%E4%B8%89%E7%A7%8D%E4%B8%BB%E8%A6%81%E5%BD%A2%E5%BC%8F">if 条件分支</a></td>\n</tr>\n<tr>\n<td>多个固定值选择</td>\n<td>是固定整数或字符吗，匹配后在哪里停止</td>\n<td><a href="/gesp/level1/branch/switch-case/#%E4%BB%80%E4%B9%88%E6%97%B6%E5%80%99%E4%BD%BF%E7%94%A8-switch">switch-case 分支</a></td>\n</tr>\n<tr>\n<td>常见综合问题</td>\n<td>规则怎样拆成互斥且完整的路线</td>\n<td>本页“闰年与月份天数”</td>\n</tr>\n<tr>\n<td>分支验证</td>\n<td>路线、边界、非法输入是否全部测试</td>\n<td>本页“怎样验证分支路径”</td>\n</tr>\n</tbody>\n</table>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li>还没有划分路线就直接写代码，结果条件互相重叠或漏掉一类输入。</li>\n<li>互斥分类使用多个独立 <code>if</code>，一次输出了两个本应互斥的结果。</li>\n<li>多分支先写宽范围，后写严格范围，后面的路线永远无法到达。</li>\n<li>用 <code>switch</code> 硬写范围或复杂条件，没有先看题目的判断方式。</li>\n<li>只测试区间中间的普通数据，没有检查边界值、相邻值和非法输入。</li>\n<li>只确认“选中了什么”，没有确认其他互斥路线确实被跳过、分支结束后从哪里继续。</li>\n</ol>\n<p>具体语法错误、条件形成过程和代码块范围在 <code>if</code> 子课中检查；<code>case</code>、<code>break</code>、<code>default</code> 与贯穿问题在 <code>switch-case</code> 子课中检查。父页不重复两套语法，只把选择与验证方法合在一起。</p>\n<h2 id="关联学习">关联学习</h2>\n<p><a href="/gesp/level1/sequential/">顺序结构</a>提供分支前后仍要遵守的逐句执行规则。学完本章后，<a href="/gesp/level1/loop/">循环结构</a>会让一段语句在条件控制下重复执行；循环的“是否继续”同样要把变量当前值代入条件，但本章不提前展开循环过程。</p>\n<section aria-labelledby="related-knowledge-branch"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-branch" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/sequential/" class="astro-mkwt4ymd">顺序结构</a></li></ul></section></div></section>',
        '<h2 id="本章先记住">本章先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 循环让程序重复处理同一类任务；初始状态决定从哪里开始，条件决定是否继续，更新让状态逐步走向结束。</li>\n<li><strong>常用选择：</strong> 起点和终点明确时先考虑 <code>for</code>；次数不定但继续条件清楚时考虑 <code>while</code>；必须先做一次再判断时才考虑 <code>do-while</code>。</li>\n<li><strong>最小对照：</strong> 初值为 1 时，三种循环都能输出 1 到 5；初值改为 6 后，<code>for</code> 和 <code>while</code> 执行 0 次，<code>do-while</code> 仍先执行 1 次。</li>\n<li><strong>关键易错点：</strong> 不能只看已经输出的结果，还要找到更新后的状态、条件第一次为假的判断，以及 <code>break</code> 或 <code>continue</code> 真正改变的去向。</li>\n</ul>\n<p>前一章的分支结构让程序从多条路线中选择一条。本章继续使用条件判断，但条件现在负责决定一段任务是否还要再做一轮。循环并不是把代码“自动重复几次”，每一轮能否开始、怎样前进、何时停止，都由程序中的具体状态决定。</p>\n<p>这是一张章节地图。四个子课负责语法、过程表和完整程序；本页负责把它们排成学习路线，并帮助你在做题时找到合适入口。</p>\n<h2 id="从顺序执行到重复处理">从顺序执行到重复处理</h2>\n<p>顺序结构中的语句通常只执行一次。循环到达判断位置后，会读取变量的当前值：条件成立就完成本轮任务，再准备下一轮状态；条件不成立就离开循环，继续执行后面的语句。</p>\n<p>无论选择哪种循环，都要能回答四个问题：</p>\n<ol>\n<li><strong>初始状态：</strong> 第一次判断或第一次执行使用什么值；</li>\n<li><strong>继续条件：</strong> 当前状态满足什么要求才继续；</li>\n<li><strong>本轮任务：</strong> 条件允许后，这一轮具体处理什么；</li>\n<li><strong>状态更新：</strong> 哪条语句让下一轮使用新状态，并最终使条件变成 <code>false</code>。</li>\n</ol>\n<p><code>for</code>、<code>while</code> 和 <code>do-while</code> 没有改变这四项职责，只是把判断与更新放在了不同位置。<code>break</code> 和 <code>continue</code> 则在正常路径上增加了提前离开或跳过本轮的去向。</p>\n<h2 id="同一个任务怎样比较三种循环">同一个任务怎样比较三种循环</h2>\n<p>先固定任务：“从 1 开始，每行输出一个整数，直到输出 5。”正常初值为 1 时，三种循环的可观察结果相同，都是 1、2、3、4、5；真正需要比较的是每一步先去哪里。</p>\n<table>\n<thead>\n<tr>\n<th>结构</th>\n<th>控制信息放在哪里</th>\n<th>第一轮怎样开始</th>\n<th>一轮任务结束后</th>\n<th>初值改为 6 时</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>for</code></td>\n<td>初始化、条件、更新集中在循环头</td>\n<td>初始化后先判断 <code>1 &lt;= 5</code></td>\n<td>先更新，再回到条件</td>\n<td><code>6 &lt;= 5</code> 为 false，执行 0 次</td>\n</tr>\n<tr>\n<td><code>while</code></td>\n<td>初始化在循环前，条件在循环头，更新在循环体内</td>\n<td>先判断 <code>1 &lt;= 5</code></td>\n<td>执行体内更新，再回到条件</td>\n<td><code>6 &lt;= 5</code> 为 false，执行 0 次</td>\n</tr>\n<tr>\n<td><code>do-while</code></td>\n<td>初始化在循环前，条件在循环体末尾</td>\n<td>不先判断，直接完成第一轮</td>\n<td>先更新，再判断末尾条件</td>\n<td>先处理 6，更新为 7，再由 <code>7 &lt;= 5</code> 为 false 离开</td>\n</tr>\n</tbody>\n</table>\n<p>初值为 1 时，三种写法都要在输出 5 后把状态更新为 6。<code>for</code> 和 <code>while</code> 接着判断 <code>6 &lt;= 5</code>；<code>do-while</code> 在本轮末尾判断同一个条件。正常结束一定能找到这次第一次为假的判断，不能在最后一次输出后直接说“循环结束”。</p>\n<h2 id="四个子课怎样衔接">四个子课怎样衔接</h2>\n<p>建议按表中顺序学习。每完成一课，都用“输出 1 到 5”重新走一遍，这样只改变控制位置，不会被新题目分散注意力。</p>\n<table>\n<thead>\n<tr>\n<th style="text-align: right">顺序</th>\n<th>子课与准确入口</th>\n<th>学完后的可观察结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td><a href="/gesp/level1/loop/for/#for-%E7%9A%84%E5%9B%9B%E4%B8%AA%E4%BD%8D%E7%BD%AE%E6%80%8E%E6%A0%B7%E9%85%8D%E5%90%88">for 循环：四个位置怎样配合</a></td>\n<td>能从初始化开始，按“判断、循环体、更新”追踪到第一次失败，并据此检查起点、终点和更新方向</td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td><a href="/gesp/level1/loop/while/#%E7%AC%AC%E4%B8%80%E6%AC%A1%E5%88%A4%E6%96%AD%E5%8F%AF%E8%83%BD%E7%9B%B4%E6%8E%A5%E5%A4%B1%E8%B4%A5">while 循环：第一次判断可能直接失败</a></td>\n<td>能说明为什么初始条件为假时执行 0 次，并确认循环体中的更新会让状态接近结束</td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td><a href="/gesp/level1/loop/do-while/#%E5%88%9D%E5%A7%8B%E6%9D%A1%E4%BB%B6%E4%B8%8D%E6%88%90%E7%AB%8B%E4%B8%BA%E4%BB%80%E4%B9%88%E4%BB%8D%E6%89%A7%E8%A1%8C%E4%B8%80%E6%AC%A1">do-while 循环：初始条件不成立为什么仍执行一次</a></td>\n<td>能指出第一轮发生在首次判断之前，并用真实状态区分 0 次与至少 1 次</td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td><a href="/gesp/level1/loop/break-continue/#%E5%90%8C%E4%B8%80%E4%B8%AA%E7%A8%8B%E5%BA%8F%E6%80%8E%E6%A0%B7%E5%BD%A2%E6%88%90%E4%B8%A4%E6%9D%A1%E7%89%B9%E6%AE%8A%E8%B7%AF%E5%BE%84">break 与 continue：同一程序的两条特殊路径</a></td>\n<td>能说清触发条件、被跳过或结束的语句、下一控制位置和最终输出</td>\n</tr>\n</tbody>\n</table>\n<p>学完四页后回到本页完成整章测试。子课可以直接访问，本页的推荐顺序只用于建立连续的理解路线，不是访问门槛。</p>\n<h2 id="先按题目特征选择入口">先按题目特征选择入口</h2>\n<p>不要因为三种循环能完成同一个输出，就认为它们在所有题目中都同样自然。先读题目怎样描述重复，再选择最容易核对的结构。</p>\n<table>\n<thead>\n<tr>\n<th>题目给出的线索</th>\n<th>优先考虑</th>\n<th>选择后立刻检查</th>\n<th>详细入口</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>起点、终点、次数或固定步长明确</td>\n<td><code>for</code></td>\n<td>两端是否包含，更新方向是否朝终点前进</td>\n<td><a href="/gesp/level1/loop/for/#%E6%80%8E%E6%A0%B7%E7%A1%AE%E5%AE%9A%E8%B5%B7%E7%82%B9%E7%BB%88%E7%82%B9%E5%92%8C%E6%9B%B4%E6%96%B0">怎样确定起点、终点和更新</a></td>\n</tr>\n<tr>\n<td>次数不容易预先算出，但“只要……就继续”的条件清楚</td>\n<td><code>while</code></td>\n<td>初始条件是否可能直接为假，所有能继续的路径是否更新状态</td>\n<td><a href="/gesp/level1/loop/while/#%E4%BB%80%E4%B9%88%E6%97%B6%E5%80%99%E9%80%89%E6%8B%A9-while">什么时候选择 while</a></td>\n</tr>\n<tr>\n<td>每轮完成后才能知道是否继续，而且第一次任务必须发生</td>\n<td><code>do-while</code></td>\n<td>初值越界时先做一轮是否符合题意，末尾分号是否保留</td>\n<td><a href="/gesp/level1/loop/do-while/#%E4%BB%80%E4%B9%88%E6%97%B6%E5%80%99%E9%80%82%E5%90%88-do-while">什么时候适合 do-while</a></td>\n</tr>\n<tr>\n<td>找到目标后不必继续，或某个特殊值本轮不处理</td>\n<td>在已有循环中加入 <code>break</code> 或 <code>continue</code></td>\n<td>是结束整个最近循环，还是只跳过本轮；下一步到底去哪</td>\n<td><a href="/gesp/level1/loop/break-continue/#continue-%E5%9C%A8%E4%B8%89%E7%A7%8D%E5%BE%AA%E7%8E%AF%E4%B8%AD%E5%88%86%E5%88%AB%E5%8E%BB%E5%93%AA%E9%87%8C">continue 在三种循环中的下一位置</a></td>\n</tr>\n</tbody>\n</table>\n<p><code>break</code> 和 <code>continue</code> 不是第四种循环。先选择 <code>for</code>、<code>while</code> 或 <code>do-while</code> 作为正常骨架，确实需要改变正常去向时再加入控制语句。</p>\n<h2 id="常见任务怎样找到子课">常见任务怎样找到子课</h2>\n<p>整章综合测试还会把循环用于累计、计数、求积、最值、枚举因数与质数、数位处理和简单过程模拟。本页不再给这些任务各写一套程序，只保留状态选择与学习入口。</p>\n<table>\n<thead>\n<tr>\n<th>任务类型</th>\n<th>需要保存的状态</th>\n<th>最合适的复习入口</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>累计、计数、求积或最值</td>\n<td>除循环变量外，还要保存“到目前为止的结果”</td>\n<td><a href="/gesp/level1/loop/for/#for-%E6%80%8E%E6%A0%B7%E6%9B%B4%E6%96%B0%E4%BB%BB%E5%8A%A1%E7%8A%B6%E6%80%81">for 怎样更新任务状态</a></td>\n</tr>\n<tr>\n<td>枚举因数与质数</td>\n<td>当前候选、判断结果以及是否找到额外因数</td>\n<td><a href="/gesp/level1/loop/break-continue/#%E6%9E%9A%E4%B8%BE%E5%9B%A0%E6%95%B0%E4%B8%8E%E8%B4%A8%E6%95%B0">break 与 continue：枚举因数与质数</a></td>\n</tr>\n<tr>\n<td>十进制数位处理</td>\n<td>用工作副本逐位变化，必要时保留原输入</td>\n<td><a href="/gesp/level1/loop/while/#%E6%AC%A1%E6%95%B0%E4%B8%8D%E7%A1%AE%E5%AE%9A%E7%9A%84%E6%95%B0%E4%BD%8D%E5%A4%84%E7%90%86">while 的数位处理</a></td>\n</tr>\n<tr>\n<td>简单过程模拟</td>\n<td>题目中的状态、执行顺序以及到达目标的时机</td>\n<td><a href="/gesp/level1/loop/break-continue/#%E7%AE%80%E5%8D%95%E8%BF%87%E7%A8%8B%E6%A8%A1%E6%8B%9F">break 与 continue：简单过程模拟</a></td>\n</tr>\n</tbody>\n</table>\n<p>状态的初值必须符合任务含义：累计和与计数通常从 0 开始，求积从 1 开始，最大值或最小值要从第一个真实数据开始。详细原因和过程放在 <code>for</code> 子课；做综合测试时，先确认自己保存的是循环位置还是任务结果，不要让一个变量同时承担两种职责。</p>\n<h2 id="怎样验证一个循环">怎样验证一个循环</h2>\n<p>循环的输出正确一次，还不能证明边界和停止路径都正确。至少准备下面四类检查：</p>\n<table>\n<thead>\n<tr>\n<th>检查</th>\n<th>要记录的事实</th>\n<th>能发现的问题</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>第一次执行</td>\n<td>初始值、第一次条件的实际值、是否进入循环体</td>\n<td>初值或判断位置错误</td>\n</tr>\n<tr>\n<td>终点附近</td>\n<td>终点值是否处理，更新后的下一个值是什么</td>\n<td><code>&lt;</code> 与 <code>&lt;=</code> 混淆，多一轮或少一轮</td>\n</tr>\n<tr>\n<td>第一次失败</td>\n<td>条件为什么第一次成为 false，离开后去哪</td>\n<td>只会输出，不会解释停止</td>\n</tr>\n<tr>\n<td>特殊路径</td>\n<td><code>break</code> 或 <code>continue</code> 是否触发，跳过了什么，下一步去哪</td>\n<td>把结束循环和跳过本轮混为一谈</td>\n</tr>\n</tbody>\n</table>\n<p>若循环可能不停止，不要故意运行。先在纸上写出连续两轮的状态：更新后没有变化，或变化方向让条件更难成为 false，就应先修正代码。实际运行时，再把预期输出与程序输出逐项对照；输出相同但停止原因不同，也要分别记录。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>大主题</th>\n<th>先问自己的问题</th>\n<th>详细入口</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>for</code> 的固定范围</td>\n<td>起点、终点和更新是否描述同一个范围</td>\n<td><a href="/gesp/level1/loop/for/#%E8%BE%93%E5%87%BA-1-%E5%88%B0-5">for 循环</a></td>\n</tr>\n<tr>\n<td><code>while</code> 的状态条件</td>\n<td>第一次能否进入，循环体内哪条语句改变条件所用状态</td>\n<td><a href="/gesp/level1/loop/while/#%E5%9B%9B%E9%A1%B9%E8%81%8C%E8%B4%A3%E5%88%86%E5%BC%80%E6%94%BE%E5%9C%A8%E5%93%AA%E9%87%8C">while 循环</a></td>\n</tr>\n<tr>\n<td><code>do-while</code> 的后判断</td>\n<td>第一轮为什么必定发生，末尾条件使用哪个更新后的值</td>\n<td><a href="/gesp/level1/loop/do-while/#%E4%B8%89%E7%A7%8D%E5%BE%AA%E7%8E%AF%E7%9A%84%E5%88%A4%E6%96%AD%E4%BD%8D%E7%BD%AE">do-while 循环</a></td>\n</tr>\n<tr>\n<td><code>break</code> 与 <code>continue</code></td>\n<td>当前语句结束了什么、跳过了什么、下一步到哪里</td>\n<td><a href="/gesp/level1/loop/break-continue/#break-%E4%B8%8E-continue-%E7%9A%84%E6%9C%80%E8%BF%91%E4%B8%80%E5%B1%82%E8%BE%B9%E7%95%8C">循环控制</a></td>\n</tr>\n<tr>\n<td>循环验证</td>\n<td>首次判断、边界、更新、失败条件与特殊路径是否都有证据</td>\n<td>本页“怎样验证一个循环”</td>\n</tr>\n</tbody>\n</table>\n<h2 id="常见错误">常见错误</h2>\n<ol>\n<li>看到重复就立刻写循环，没有先确定初始状态、继续条件、本轮任务和更新。</li>\n<li>只凭“次数多或少”选择结构，没有看题目给的是固定范围、状态条件还是至少一次要求。</li>\n<li>更新没有改变条件使用的变量，或更新方向让状态离停止边界越来越远。</li>\n<li>过程只写成功轮次，漏掉正常结束时条件第一次为假的判断。</li>\n<li>把 <code>do-while</code> 当成先判断，错误地认为初始条件为假时执行 0 次。</li>\n<li>把 <code>break</code> 当成跳过本轮，或把 <code>continue</code> 当成结束整个循环。</li>\n<li>只测试一个普通数据，没有覆盖零次、终点、首次失败和特殊控制路径。</li>\n</ol>\n<p>具体语法、完整程序和逐轮状态表都在四个子课中。本页只负责选择、连接和验证，不把同一份叶子课内容再讲一遍。</p>\n<h2 id="本章边界与关联学习">本章边界与关联学习</h2>\n<p><a href="/gesp/level1/branch/">分支结构</a>提供了循环条件和循环体内的选择路线。本章在它的基础上学习单层循环、常见状态更新以及 <code>break</code>、<code>continue</code> 的基本去向；复杂嵌套循环、数组配合和新的算法方法暂不展开。</p>\n<p>整章测试会检查三种循环、边界、状态更新、控制语句和一级常见应用。遇到某个语法或过程不确定时，先从上面的深链回到对应子课，再回来完成综合测试。</p>\n<section aria-labelledby="related-knowledge-loop"><div><p class="astro-mkwt4ymd">继续查字典</p><h2 id="related-knowledge-loop" class="astro-mkwt4ymd">关联知识与资源</h2><div><section><h3 class="astro-mkwt4ymd">前置知识</h3><ul class="astro-mkwt4ymd"><li class="astro-mkwt4ymd"><a href="/gesp/level1/branch/" class="astro-mkwt4ymd">分支结构</a></li></ul></section></div></section>',
    ],
    2: [
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> RAM 保存运行时临时数据，ROM 断电后通常仍保留内容，Cache 用小容量高速空间缓和 CPU 与内存的速度差；容量按 1024 逐级换算；LAN、MAN、WAN 按覆盖范围分类，协议各负其责，只有题目明确给出 <code>/24</code> 时才按 <code>/24</code> 判断网段。</li>\n<li><strong>常用语法：</strong> 先写单位或掩码规则，再代入数值：<code>1 B = 8 bit</code>、<code>1 KB = 1024 B</code>、<code>1 MB = 1024 KB</code>、<code>/24 = 255.255.255.0</code>。</li>\n<li><strong>最小示例：</strong> 先完成单位换算；只有题目给出掩码时，才按对应掩码判断网段：<code>8192 bit = 1024 B = 1 KB</code>；<code>10.0.3.7/24</code> 与 <code>10.0.3.80/24</code> 属于同一网段。</li>\n<li><strong>关键易错点：</strong> 不要把 RAM 当作长期存储；不要混淆 bit 与 Byte；未给出 <code>/24</code> 时不能机械比较前三个字节。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应 GESP 二级“计算机的存储与网络”。存储部分重点是认识 RAM、ROM、Cache、内存和外存，理解常见存储层次，并完成 bit、Byte、KB、MB、GB、TB 的基础换算与容量计算；网络部分重点是认识 LAN、MAN、WAN、网络分层与常见协议，理解域名和 IP 地址的关系，并按题目给出的子网掩码完成基础同网段判断。</p>\n<p>“计算机存储与网络”包含两个紧密相关的方向：存储关注数据放在哪里、容量怎样表示；网络关注设备怎样连接、数据怎样传输。本页把这两个方向放在同一条学习路径中，复习状态和综合测试继续沿用原来的 <code>storage</code> 记录。</p>\n<p>本页先用“用途、断电后是否保留、速度与容量”比较 RAM、ROM、Cache 和外存，再完成 bit、Byte、KB、MB、GB、TB 的逐级换算。随后按覆盖范围区分 LAN、MAN、WAN：先由 DNS 解析域名，发出网页请求时由 HTTP / HTTPS 形成请求内容、TCP 负责可靠传输、IP 负责寻址和路由、网络接口层实际发送数据。最后在题目明确给出 <code>/24</code> 时判断两个 IPv4 地址是否同网段。</p>\n<p>先把两个核心概念说准确：<strong>存储</strong>是把数据保存在某种介质中，供现在或以后读取；<strong>网络</strong>是把多台设备连接起来，并按共同规则交换数据。存储解决“数据放在哪里”，网络解决“数据怎样从一台设备到另一台设备”。两者经常连续出现：浏览器先通过网络取得网页数据，运行时把数据放在 RAM，下载后又可以把文件长期保存在外存。</p>\n<a id="01-memory"></a>\n<h2 id="01-ramrom-与-cache">01 RAM、ROM 与 Cache</h2>\n<p>**RAM（随机存取存储器）**通常就是日常所说的运行内存。程序运行时，正在使用的代码和数据会放入 RAM；断电后其中内容通常会丢失。电脑“内存不足”时，通常是 RAM 可用空间不足。</p>\n<p>**ROM（只读存储器）**常用于保存较固定的启动信息，断电后仍能保留。**Cache（高速缓存）**容量较小但速度很快，位于 CPU 与主存之间，用来保存近期常用的数据，减少 CPU 等待时间。</p>\n<p>RAM 名称中的“随机存取”不是“随机保存数据”，而是指访问不同位置时不必像磁带那样从头顺序寻找。ROM 的“只读”描述的是它主要承担的稳定存储用途；实际设备可以采用不同技术更新内容，二级题目仍按“较固定、断电保留”理解。Cache 也不是一块专门给用户保存文件的硬盘，它由硬件自动配合 CPU 工作。</p>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>主要作用</th>\n<th>断电后</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>RAM</td>\n<td>运行程序时临时保存代码和数据</td>\n<td>通常丢失</td>\n</tr>\n<tr>\n<td>ROM</td>\n<td>保存较固定的启动信息</td>\n<td>通常保留</td>\n</tr>\n<tr>\n<td>Cache</td>\n<td>暂存 CPU 近期常用数据，加快访问</td>\n<td>通常丢失</td>\n</tr>\n</tbody>\n</table>\n<p>易错点是把三者只按容量判断。更可靠的比较顺序是：先看用途，再看断电后是否保留，最后比较速度与容量。</p>\n<a id="02-storage"></a>\n<h2 id="02-内存外存与存储层次">02 内存、外存与存储层次</h2>\n<p>内存直接服务于正在运行的程序，速度快、容量相对小；硬盘、固态硬盘、U 盘、光盘等属于外存，适合长期保存文件，容量通常更大。光盘主要依靠光学方式读写，机械硬盘和磁带属于磁性存储介质。</p>\n<p>常见存储层次可以简化理解为：<strong>寄存器 → Cache → RAM → 外存</strong>。越靠左通常越快、容量越小、单位成本越高。</p>\n<p>例如打开安装在固态硬盘中的游戏时，可以把主要过程简化为：<strong>外存 → RAM → CPU</strong>。游戏文件长期保存在外存；运行时需要的代码和数据先载入 RAM，再由 CPU 处理。Cache 会在 CPU 与 RAM 之间加快近期常用数据的访问，但不能代替大容量外存。</p>\n<table>\n<thead>\n<tr>\n<th>层次</th>\n<th>速度</th>\n<th>容量</th>\n<th>断电后的数据</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>寄存器</td>\n<td>最快</td>\n<td>最小</td>\n<td>通常丢失</td>\n</tr>\n<tr>\n<td>Cache</td>\n<td>很快</td>\n<td>很小</td>\n<td>通常丢失</td>\n</tr>\n<tr>\n<td>RAM</td>\n<td>较快</td>\n<td>较大</td>\n<td>通常丢失</td>\n</tr>\n<tr>\n<td>外存（如固态硬盘、机械硬盘）</td>\n<td>较慢</td>\n<td>很大</td>\n<td>通常保留</td>\n</tr>\n</tbody>\n</table>\n<a id="03-units"></a>\n<h2 id="03-存储单位与容量计算">03 存储单位与容量计算</h2>\n<p>小写 <code>b</code> 常表示 bit（比特），大写 <code>B</code> 表示 Byte（字节），通常 <code>1 Byte = 8 bit</code>。二级题目若没有另外说明，通常按下列关系换算：</p>\n<table>\n<thead>\n<tr>\n<th>单位关系</th>\n<th>换算</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1 KB</td>\n<td>1024 B</td>\n</tr>\n<tr>\n<td>1 MB</td>\n<td>1024 KB</td>\n</tr>\n<tr>\n<td>1 GB</td>\n<td>1024 MB</td>\n</tr>\n<tr>\n<td>1 TB</td>\n<td>1024 GB</td>\n</tr>\n</tbody>\n</table>\n<p>例如 <code>6 GB = 6 × 1024 MB = 6144 MB</code>。若一张未压缩灰度图有 <code>800 × 600</code> 个像素，每像素占 1 B，总容量为 <code>480000 B</code>。计算时先求元素个数，再乘每个元素占用的字节数，最后按题目要求换单位。</p>\n<p>从较小单位换到相邻的较大单位要除以 1024。例如：<code>2048 KB ÷ 1024 = 2 MB</code>。这个过程与 <code>2 MB × 1024 = 2048 KB</code> 正好互为反向换算。</p>\n<p>以 <code>1 KB</code> 为例：先从 KB 换成 B，得到 <code>1024 B</code>；再乘 8，得到 <code>8192 bit</code>。反过来把 <code>8192 bit</code> 换成 KB，就依次除以 8、再除以 1024。</p>\n<p>若一张黑白图有 <code>128 × 64 = 8192</code> 个像素，每个像素占 1 bit，总量是 <code>8192 bit = 1024 B = 1 KB</code>。不能把像素个数直接当作字节数。</p>\n<p>容量题可以统一成一个公式：</p>\n<p><strong>总容量 = 元素个数 × 每个元素占用量</strong></p>\n<p>先让“元素个数”和“每个元素占用量”的单位对应，再换算最终单位。例如在常见 G++ / OJ 环境中，一个 <code>int</code> 通常占 4 B，100 个 <code>int</code> 保存的数据量可按 <code>100 × 4 B = 400 B</code> 估算；若有 20 行、30 列这样的整数数据，元素个数是 <code>20 × 30 = 600</code>，容量是 <code>600 × 4 B = 2400 B</code>。这里是在练习容量公式，不要求本课使用尚未讲解的数组语法；若题目明确给出其他字节数，应以题目为准。</p>\n<p>多级换算时不要把 8 和 1024 混在一起：bit 与 Byte 之间用 8，Byte、KB、MB、GB、TB 相邻两级之间用 1024。例如 <code>3 MB</code> 换成 bit 要依次计算 <code>3 × 1024 × 1024 × 8 = 25165824 bit</code>，不能只乘一次 1024。</p>\n<a id="01-network-types"></a><a id="04-network"></a>\n<h2 id="04-lanman-与-wan">04 LAN、MAN 与 WAN</h2>\n<p>按覆盖范围，常见网络分为：</p>\n<ul>\n<li><strong>LAN（局域网）</strong>：家庭、机房、同一教学楼等较小范围；</li>\n<li><strong>MAN（城域网）</strong>：覆盖一座城市或相近城区；</li>\n<li><strong>WAN（广域网）</strong>：跨城市、国家甚至大洲，Internet 具有广域网特征。</li>\n</ul>\n<p>判断时看实际覆盖范围，不只看联网设备数量。同一栋楼有很多电脑，仍更接近局域网。学校机房的 60 台电脑只覆盖同一栋楼，仍是 LAN；覆盖城区的教育网络更接近 MAN；跨城市连接的两个机房更接近 WAN。</p>\n<a id="02-protocols"></a>\n<h2 id="05-分层模型与常见协议">05 分层模型与常见协议</h2>\n<p>网络分层让不同任务由不同层负责。OSI 模型有七层；实际学习中常用 TCP/IP 模型理解网络通信。二级重点是认识分层思想和常见协议用途，不要求实现协议。</p>\n<p>可以把两个模型按职责对应起来。OSI 七层从上到下是应用层、表示层、会话层、传输层、网络层、数据链路层、物理层；TCP/IP 常简化为应用层、传输层、网际层、网络接口层。二级只需识别“分层是为了分工”，不需要背每一层的内部实现。</p>\n<table>\n<thead>\n<tr>\n<th>TCP/IP 四层</th>\n<th>大致对应的 OSI 层</th>\n<th>本页代表任务</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>应用层</td>\n<td>应用层、表示层、会话层</td>\n<td>DNS 解析名称，HTTP / HTTPS 交换网页数据</td>\n</tr>\n<tr>\n<td>传输层</td>\n<td>传输层</td>\n<td>TCP 组织可靠、有顺序的传输</td>\n</tr>\n<tr>\n<td>网际层</td>\n<td>网络层</td>\n<td>IP 按地址转发数据</td>\n</tr>\n<tr>\n<td>网络接口层</td>\n<td>数据链路层、物理层</td>\n<td>在具体网络介质上传送数据</td>\n</tr>\n</tbody>\n</table>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>主要用途</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>HTTP / HTTPS</td>\n<td>浏览网页、传输网页数据</td>\n</tr>\n<tr>\n<td>DNS</td>\n<td>把域名转换为 IP 地址</td>\n</tr>\n<tr>\n<td>TCP</td>\n<td>提供可靠、有顺序的数据传输</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>按地址在网络间转发数据</td>\n</tr>\n</tbody>\n</table>\n<p>HTTP 和 HTTPS 都用于浏览器与服务器之间的网页数据传输。HTTPS 在 HTTP 的基础上增加加密和身份验证，通常能更安全地保护传输内容；二级只需理解这个基本区别，不要求学习具体加密过程。</p>\n<p>域名从右向左逐级细分。例如 <code>gesp.ccf.org.cn</code> 最右侧的 <code>cn</code> 是顶级域名，<code>gesp</code> 不是顶级域名。</p>\n<p>在浏览器输入网址后，要把“先找到地址”和“分层发送数据”分开理解：</p>\n<ol>\n<li><strong>地址解析</strong>：浏览器把域名交给 DNS，得到目标服务器的 IP 地址；</li>\n<li><strong>形成请求</strong>：浏览器按 HTTP 或 HTTPS 规则组织网页请求；</li>\n<li><strong>分层传输</strong>：TCP 负责把应用层数据可靠、有顺序地传送，IP 负责寻址和路由，网络接口层把数据送入具体网络；</li>\n<li><strong>接收处理</strong>：接收方按相反方向逐层处理数据，服务器读到网页请求并准备响应；</li>\n<li><strong>响应返回</strong>：响应数据也经过各层传回，浏览器取得内容后再显示网页。</li>\n</ol>\n<p>这里的 HTTP、TCP、IP 不是轮流完成三次互不相关的发送，而是同一份数据在不同层获得不同职责。二级只需说清每层“负责什么”，不要求展开报文格式。</p>\n<p>例如输入 <code>https://xdzoi.com</code>，DNS 的任务是把 <code>xdzoi.com</code> 解析为可通信的 IP；它不负责直接显示网页。</p>\n<a id="03-ip"></a>\n<h2 id="06-ipv4-与基础子网">06 IPv4 与基础子网</h2>\n<p>IPv4 地址常写成四段点分十进制数，例如 <code>192.168.1.8</code>。每段对应 8 位二进制数，所以范围是 <strong>0 到 255</strong>，0 并没有被禁止。</p>\n<p>四段一共是 <code>4 × 8 = 32 bit</code>，也就是 4 B 的地址信息。点分十进制只是为了便于人阅读，网络设备处理的仍是 32 位模式。<code>/24</code> 表示前 24 位属于网络部分，剩下 <code>32-24=8</code> 位是主机部分；因此在这一基础情形下，前三段决定网络，最后一段区分该网络中的地址。</p>\n<p>子网掩码用来区分网络部分和主机部分。基础题常给出 <code>255.255.255.0</code>：这时前三段相同的地址通常属于同一子网。例如 <code>192.168.1.8</code> 与 <code>192.168.1.20</code> 在该掩码下同网段，而 <code>192.168.2.8</code> 不在同一网段。</p>\n<table>\n<thead>\n<tr>\n<th>地址 A</th>\n<th>地址 B</th>\n<th>前三段</th>\n<th>结论</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>10.0.3.7</code></td>\n<td><code>10.0.3.80</code></td>\n<td>都是 <code>10.0.3</code></td>\n<td>同一子网</td>\n</tr>\n<tr>\n<td><code>10.0.3.7</code></td>\n<td><code>10.0.4.7</code></td>\n<td>第三段不同</td>\n<td>不同子网</td>\n</tr>\n</tbody>\n</table>\n<p>在掩码明确为 <code>255.255.255.0</code> 时，可以先<strong>只比较前三段</strong>。这个简便规则只适用于当前掩码；掩码改变时，不能继续机械使用。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>查询词</th>\n<th>先检查什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>断电后是否保留</td>\n<td>RAM 通常丢失，ROM 与外存通常保留</td>\n</tr>\n<tr>\n<td>内存不足</td>\n<td>通常指 RAM</td>\n</tr>\n<tr>\n<td>bit 与 Byte</td>\n<td>先确认是否需要乘或除以 8</td>\n</tr>\n<tr>\n<td>GB 与 MB</td>\n<td>先确认题目是否按 1024 换算</td>\n</tr>\n<tr>\n<td>LAN / MAN / WAN</td>\n<td>看覆盖范围</td>\n</tr>\n<tr>\n<td>域名与 IP</td>\n<td>DNS 负责名称解析</td>\n</tr>\n<tr>\n<td>HTTP / HTTPS</td>\n<td>负责网页请求与传输</td>\n</tr>\n<tr>\n<td>是否同一子网</td>\n<td>按子网掩码比较网络部分</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<h3 id="例题一文件保存与容量换算">例题一：文件保存与容量换算</h3>\n<p>**题目：**某电脑有 8 GB RAM 和 512 GB 固态硬盘。一个 2 GB 的程序文件长期放在哪里？启动后主要加载到哪里？<code>2 GB</code> 等于多少 MB？</p>\n<p><strong>推演：</strong>“长期保存”对应外存，所以文件放在固态硬盘中；“正在运行”对应 RAM，所以所需代码和数据会加载到 RAM；<code>2 × 1024 = 2048 MB</code>。</p>\n<p>**结论：**外存负责长期保存，RAM 服务正在运行的程序；<code>2 GB = 2048 MB</code>。</p>\n<h3 id="例题二网络分类与同网段判断">例题二：网络分类与同网段判断</h3>\n<p>**题目：**同一教学楼的教室与机房组成一个网络。两台设备地址分别为 <code>10.0.3.7</code> 和 <code>10.0.3.80</code>，掩码是 <code>255.255.255.0</code>。这个网络属于哪一类？两台设备是否在同一子网？</p>\n<p>**推演：**覆盖范围只在同一教学楼，所以属于 LAN。掩码是 <code>255.255.255.0</code>，比较前三段，两台设备都是 <code>10.0.3</code>。</p>\n<p>**结论：**这个网络属于 LAN，两台设备在当前掩码下属于同一子网。</p>\n<p>**引导练习：**一台电脑从校园网下载了一个 <code>3072 KB</code> 的程序文件，下载完成后长期保存在固态硬盘中，启动时再把需要的代码和数据载入 RAM。请你先完成三步：把文件容量换成 MB；判断只覆盖同一教学楼的校园网属于 LAN、MAN 还是 WAN；说明“长期保存”和“正在运行”分别对应哪一类存储。提示：KB 换成 MB 要除以 1024，网络分类看覆盖范围，不看电脑数量。核对结果应为 <code>3072 ÷ 1024 = 3 MB</code>、LAN、长期保存对应外存、正在运行对应 RAM；四项都一致才算完成。</p>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>把 ROM 当作扩充运行内存的内存条；</li>\n<li>混淆 bit 与 Byte，漏乘或漏除 8；</li>\n<li>换算多个单位时少乘一个 1024；</li>\n<li>认为 IPv4 每段只能是 1 到 255；</li>\n<li>认为 DNS 直接负责显示网页；</li>\n<li>仅凭 IP 地址“看起来相近”就判断同网段。</li>\n</ul>\n<p><strong>错误判断一：</strong>“Cache 容量最大，所以最适合长期保存文件。”</p>\n<p>**错误原因：**Cache 的主要优势是速度快，容量通常比 RAM 小，而且断电后通常丢失。比较存储器时要分别核对用途、速度、容量和断电特性，不能用一个特点推出其他特点。</p>\n<p><strong>错误判断二：</strong>“两个 IP 地址前三段相同，所以在任何情况下都属于同一子网。”</p>\n<p>**错误原因：**这个判断忽略了子网掩码。只比较前三段只适用于题目明确给出 <code>255.255.255.0</code> 的基础情形；掩码改变时，必须重新确定网络部分。</p>\n<p><strong>错误换算：</strong>“<code>8192 bit = 8192 B = 8 KB</code>。”</p>\n<p>**错误原因：**bit 与 Byte 之间漏除了 8，把两种不同单位当成了同一种单位。</p>\n<p>**重新换算：**先算 <code>8192 bit ÷ 8 = 1024 B</code>，再算 <code>1024 B ÷ 1024 = 1 KB</code>。</p>\n<p>**单位核对：**每次向更大的单位换算，数值应变小；最终写成 <code>1 KB</code>，再反向计算 <code>1 × 1024 × 8 = 8192 bit</code>，能够回到原值才说明单位链正确。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>运行中的代码和数据主要放在 RAM，较固定的启动信息常保存在 ROM，Cache 用来缩短 CPU 等待近期常用数据的时间，外存负责长期保存。容量换算先看单位方向：向小单位乘 1024，向大单位除以 1024；bit 与 Byte 之间还要单独乘或除以 8。</p>\n<p>LAN、MAN、WAN 的关键是覆盖范围；DNS、IP、TCP、HTTP / HTTPS 各自承担不同任务。判断同网段必须先看掩码：只有题目明确给出 <code>/24</code> 或 <code>255.255.255.0</code> 时，才可以直接比较前三段。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 机器语言由 CPU 直接执行，汇编语言用助记符但仍需汇编，高级语言更接近人的表达；C++ 源代码必须经过翻译和链接才能运行。</li>\n<li><strong>常用语法：</strong> C++ 程序按 <code>源代码 → 编译 → 目标文件 → 链接 → 可执行程序 → 运行</code> 的固定阶段形成可执行结果。</li>\n<li><strong>最小示例：</strong> <code>int c = a + b;</code> 仍只是高级语言源代码，CPU 不能直接执行 <code>.cpp</code> 文件；它还要经过目标文件和可执行程序阶段。</li>\n<li><strong>关键易错点：</strong> 编译错误、链接错误、运行错误和逻辑错误发生在不同阶段；程序能运行不等于答案正确。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的程序设计语言分类与特点：认识机器语言、汇编语言和高级语言，知道常见高级语言，并理解 C++ 源程序通常需要经过编译和链接才能运行。</p>\n<p>程序设计语言是人向计算机表达处理步骤的工具。不同层次的语言写法不同，但最终都要让计算机得到能够执行的机器指令。</p>\n<p>本页用同一个“计算两个整数之和”任务比较机器语言、汇编语言和高级语言，再沿着“源代码、编译、链接、运行”追踪 C++ 程序从文字文件变成可执行程序的过程，并区分编译失败、链接失败、运行失败和答案错误。</p>\n<p><strong>程序设计语言</strong>是一套有明确词汇、语法和含义的表达规则，程序员用它写出处理步骤。这里的“语言”不只是若干英文单词：语法规定怎样写才合法，语义说明一条语句表示什么操作，翻译工具再把这些操作转换成计算机能够执行或处理的形式。</p>\n<a id="01-language-levels"></a>\n<h2 id="01-三类程序设计语言">01 三类程序设计语言</h2>\n<p><strong>机器语言</strong>由计算机能够直接执行的机器指令组成，常以二进制形式表示。它与具体硬件关系密切，难以阅读和编写。</p>\n<p><strong>汇编语言</strong>用助记符表示机器指令，比机器语言容易阅读，但仍与处理器密切相关，必须由汇编器翻译。</p>\n<p><strong>高级语言</strong>更接近人类表达和数学描述，通常更容易编写、阅读和维护。C++、Python、Java 都属于高级语言。高级语言源程序不能直接交给 CPU 执行，也需要翻译。</p>\n<p>可以用同一个“计算两个整数之和”任务比较三类语言：</p>\n<table>\n<thead>\n<tr>\n<th>语言层次</th>\n<th>人看到的表达</th>\n<th>计算机怎样处理</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>机器语言</td>\n<td>一串与处理器对应的 0 和 1</td>\n<td>CPU 可以直接执行对应指令</td>\n</tr>\n<tr>\n<td>汇编语言</td>\n<td>用助记符表示“取数、相加、保存”</td>\n<td>汇编器翻译成机器指令</td>\n</tr>\n<tr>\n<td>高级语言</td>\n<td>C++ 中可写成 <code>c = a + b;</code></td>\n<td>编译器等翻译工具处理</td>\n</tr>\n</tbody>\n</table>\n<p>三种写法表达的是同一件事，但抽象程度不同。高级语言更方便人阅读，不等于 CPU 能直接理解；机器语言能直接执行，也不等于适合人手工编写大型程序。</p>\n<p>“翻译工具”也要与语言层次对应：汇编器把汇编语言翻译成机器相关代码；编译器通常先把整份源程序翻译并检查，再生成后续可执行成果；解释器通常边读取边执行相应内容。实际语言可以有不同实现方式，二级只需掌握 C++ 通常先编译、Python 常见实现通常通过解释环境运行，不能据此断言一种高级语言永远只有一种执行方式。</p>\n<a id="02-high-level"></a>\n<h2 id="02-常见高级语言与-c">02 常见高级语言与 C++</h2>\n<p>C++ 是通用的高级程序设计语言，可以编写控制台程序、桌面软件、游戏和系统软件等。GESP C++ 认证以 C++ 为工具训练程序设计思维。</p>\n<p>C++ 编译后的程序通常运行效率高，因此常用于算法竞赛和对效率有要求的软件。它既支持按照步骤组织程序的<strong>过程式编程</strong>，也支持把数据和操作组织在一起的<strong>面向对象编程</strong>。二级只需要认识这些特点，不在本页展开类和对象的语法。</p>\n<p>常见名称可以这样辨认：</p>\n<table>\n<thead>\n<tr>\n<th>语言</th>\n<th>都属于什么层次</th>\n<th>常见运行准备方式</th>\n<th>本页只需记住</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>C</td>\n<td>高级语言</td>\n<td>通常先编译、链接</td>\n<td>接近系统开发，语法与 C++ 有联系但不是同一种语言</td>\n</tr>\n<tr>\n<td>C++</td>\n<td>高级语言</td>\n<td>通常先编译、链接</td>\n<td>效率高，支持过程式和面向对象等方式</td>\n</tr>\n<tr>\n<td>Python</td>\n<td>高级语言</td>\n<td>常见实现通过解释环境执行</td>\n<td>写法较接近人的表达，不等于 CPU 直接执行源文件</td>\n</tr>\n<tr>\n<td>Java</td>\n<td>高级语言</td>\n<td>常先编译成字节码，再由运行环境执行</td>\n<td>说明高级语言的执行准备方式并非只有一种</td>\n</tr>\n</tbody>\n</table>\n<p>“高级语言”是按表达抽象层次分类，不是按新旧、难易或运行速度排名。C++ 和 Python 都是高级语言，却不必采用完全相同的翻译和运行过程。</p>\n<p>下面的局部语句表达“把两个整数相加并保存结果”。这里只比较语言表达层次，不把完整程序作为本课的新核心：</p>\n<pre><code>int c = a + b;  // 读取 a、b 的当前整数值，相加后把结果保存到 c</code></pre>\n<p><code>.cpp</code> 文件保存的是源代码，不是 CPU 可以直接执行的机器指令。代码简单并不改变它需要翻译的事实。</p>\n<a id="03-translation"></a>\n<h2 id="03-源程序的翻译与运行">03 源程序的翻译与运行</h2>\n<p>使用编译型语言时，可以把过程简化为：</p>\n<p><strong>源代码 → 编译 → 目标代码 → 链接 → 可执行程序 → 运行</strong></p>\n<ol>\n<li><strong>编辑</strong>：编写并保存源代码；</li>\n<li><strong>编译</strong>：检查语法，把源代码翻译为目标代码；</li>\n<li><strong>链接</strong>：把目标代码和需要的库连接成可执行程序；</li>\n<li><strong>运行</strong>：操作系统加载程序，CPU 执行机器指令。</li>\n</ol>\n<p>若把 C++ 的常见过程再展开一步，可以看到每一阶段的输入和输出：</p>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>读入什么</th>\n<th>主要工作</th>\n<th>产生或交给下一步什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>编辑</td>\n<td>人的题意和设计</td>\n<td>写出 <code>.cpp</code> 源文件</td>\n<td>源代码</td>\n</tr>\n<tr>\n<td>预处理</td>\n<td><code>#include</code> 等指令和源代码</td>\n<td>展开头文件等预处理内容</td>\n<td>预处理后的源代码</td>\n</tr>\n<tr>\n<td>编译</td>\n<td>预处理后的代码</td>\n<td>检查语法、类型与名称并翻译</td>\n<td>目标代码</td>\n</tr>\n<tr>\n<td>链接</td>\n<td>目标代码和所需库</td>\n<td>找到各部分定义并组合</td>\n<td>可执行程序</td>\n</tr>\n<tr>\n<td>运行</td>\n<td>可执行程序和输入</td>\n<td>操作系统加载，CPU 执行</td>\n<td>输出或运行时现象</td>\n</tr>\n</tbody>\n</table>\n<p>预处理和编译在日常工具中常被一个“编译”按钮一起完成，但它们的职责不同。知道这个顺序能帮助定位错误：找不到头文件常发生在预处理附近，漏分号通常在编译阶段报告，缺少某个已声明功能的定义可能在链接阶段报告。</p>\n<p>编译报错通常说明语法、名称或类型等问题；程序能够运行但答案错误，属于逻辑错误；运行时出现除以 0 等问题，属于运行阶段错误。<strong>编译成功不等于答案一定正确。</strong></p>\n<p>把一份 C++ 文件想成沿着流水线前进：</p>\n<p><strong>源代码 → 编译器 → 目标代码 → 链接器 → 可执行程序 → 操作系统加载 → CPU 运行</strong></p>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>停在哪一步</th>\n<th>具体例子</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>漏写分号，提示语法错误</td>\n<td>编译失败</td>\n<td>还没有生成可执行程序</td>\n</tr>\n<tr>\n<td>使用了找不到定义的功能</td>\n<td>链接失败</td>\n<td>已经编译，但不能组成完整程序</td>\n</tr>\n<tr>\n<td>程序启动后做了不允许的运算</td>\n<td>运行失败</td>\n<td>例如整数除数实际变成 0</td>\n</tr>\n<tr>\n<td>程序输出错误答案</td>\n<td>已运行，逻辑错误</td>\n<td>把 <code>a + b</code> 误写为 <code>a - b</code></td>\n</tr>\n</tbody>\n</table>\n<p>“保存成功”只说明源文件写入磁盘；“编译成功”只说明能生成后续程序；“运行成功”也只说明程序走到了结束。是否得到正确答案，还要用具体输入检查输出。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>语言</th>\n<th>表达特点</th>\n<th>是否需翻译</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>机器语言</td>\n<td>机器指令，与硬件密切相关</td>\n<td>CPU 直接执行</td>\n</tr>\n<tr>\n<td>汇编语言</td>\n<td>助记符，与硬件关系较强</td>\n<td>需要汇编</td>\n</tr>\n<tr>\n<td>高级语言</td>\n<td>更接近人的表达</td>\n<td>需要编译或解释</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**判断“Python 与 C++ 都属于高级语言，所以 CPU 可以直接执行它们的源文件”是否正确。</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li>Python 和 C++ 的确都比机器语言更接近人的表达，因此前半句正确；</li>\n<li>CPU 直接执行的是机器指令，不直接认识 <code>.cpp</code> 等高级语言源文件；</li>\n<li>C++ 源代码通常还要经过编译和链接，形成可执行程序后再运行；</li>\n<li>因而“属于高级语言”不能推出“CPU 直接执行源文件”。</li>\n</ol>\n<p>**结论：**整句话错误。前半句正确，后半句错误；语言越方便人阅读，越不能省略必要的翻译过程。</p>\n<p>**引导练习：**下面四个现象分别最先对应哪个阶段：① <code>.cpp</code> 文件漏写分号；② 已声明的函数始终找不到定义；③ 程序启动后除数变成 0；④ 程序顺利结束却把加法写成减法。请先按“编译—链接—运行—逻辑检查”的顺序逐项配对。提示：先问可执行程序是否已经生成，再问程序是否已经开始运行。核对结果依次是编译失败、链接失败、运行失败、逻辑错误；还要能说明第④项不能由“运行结束”证明答案正确。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>认为汇编语言就是机器语言，因而不需要翻译；</li>\n<li>把编辑、保存和编译混为一谈；</li>\n<li>认为扩展名为 <code>.cpp</code> 的文件就是可执行程序；</li>\n<li>认为程序能运行就说明逻辑和输出一定正确。</li>\n</ul>\n<p><strong>错误判断：</strong>“C++ 是高级语言，所以 CPU 可以直接执行 <code>.cpp</code> 源文件。”</p>\n<p><strong>错误原因：</strong>“高级”表示更接近人的表达，不表示更接近 CPU 指令。C++ 源代码还要经过编译和链接。</p>\n<p>**正确结论：**C++ 的典型过程是“源代码 → 编译 → 目标代码 → 链接 → 可执行程序 → 运行”。</p>\n<p><strong>错误判断：</strong>“高级语言一定比机器语言运行得慢。”</p>\n<p>**错误原因：**源代码是否容易阅读和程序最终运行效率不是同一件事。高级语言经过翻译后也会变成机器指令，实际效率还与编译器和程序写法有关。</p>\n<p>**正确结论：**二级应掌握三类语言的表达特点和翻译方式，不用“高级”或“低级”两个词直接判断所有程序的运行速度。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>机器语言能由 CPU 直接执行，汇编语言需要汇编，高级语言需要编译或解释。对 C++ 来说，保存 <code>.cpp</code> 文件不等于得到可执行程序；编译、链接、运行分别可能失败，即使程序顺利结束，也还要核对输出是否符合题意。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>流程图用于在写代码前描述算法步骤；数据类型转换解释 C++ 表达式中不同类型怎样参与运算。两者都建立在“源程序表达处理步骤”这一基础上。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 起止框、输入输出框、处理框、判断框和箭头各有固定职责；顺序、分支、循环都要沿箭头执行，判断框必须算出真或假。</li>\n<li><strong>常用语法：</strong> 读图时把图形职责与控制方向一一对应：平行四边形表示输入或输出，矩形表示处理，菱形表示判断，箭头表示下一步方向。</li>\n<li><strong>最小示例：</strong> 循环追踪要记录状态变化，并补上最后一次失败判断：<code>i = 1</code> 且 <code>i &lt;= 3</code> 为真时输出并更新；到 <code>i = 4</code> 时条件为假，沿出口结束。</li>\n<li><strong>关键易错点：</strong> 不要凭感觉选分支，要代入条件；不要漏掉循环更新；不要漏写最后一次为假的判断。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的流程图：认识开始结束、输入输出、处理、判断和流程线等常用表示，能够读懂并描述顺序、分支、循环三种基本结构。</p>\n<p>流程图用统一图形和箭头描述程序步骤。读图时沿箭头逐步追踪，遇到判断框先计算条件，再选择对应路径。</p>\n<p>本页先认识起止、输入输出、处理、判断和流程线五类符号，再沿箭头追踪顺序、分支与循环。重点不是背图形名称，而是把每个框还原成一次真实操作，并记录判断条件的实际值、选择的出口、变量变化和最后一次失败判断。</p>\n<p><strong>算法</strong>是解决问题的一组有限、明确步骤；<strong>流程图</strong>是把这些步骤画出来的一种表示方法。流程图不是程序本身，也不会自动运行，但它能在写代码前暴露“漏了输入”“分支没有出口”“循环没有更新”等结构问题。</p>\n<h2 id="先认识符号与三种结构">先认识符号与三种结构</h2>\n<p>画图和读图前，先把图形的固定职责说清楚：</p>\n<table>\n<thead>\n<tr>\n<th>图形</th>\n<th>表示的步骤</th>\n<th>读到它时做什么</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>圆角矩形或椭圆</td>\n<td>开始、结束</td>\n<td>确认流程入口或出口</td>\n</tr>\n<tr>\n<td>平行四边形</td>\n<td>输入、输出</td>\n<td>读入数据或显示结果</td>\n</tr>\n<tr>\n<td>矩形</td>\n<td>处理</td>\n<td>执行计算、赋值等操作</td>\n</tr>\n<tr>\n<td>菱形</td>\n<td>判断</td>\n<td>算出条件真假，再选择一个出口</td>\n</tr>\n<tr>\n<td>箭头</td>\n<td>流程方向</td>\n<td>决定下一步到哪个图形</td>\n</tr>\n</tbody>\n</table>\n<p>三种基本结构也要先形成再看图：<strong>顺序结构</strong>按箭头依次执行，每一步通常只做一次；<strong>分支结构</strong>先判断，只选择符合条件的一条路径；<strong>循环结构</strong>在条件成立时沿返回箭头重复，条件不成立时从出口离开。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>下面十张标准图按“符号 → 顺序 → 分支 → 循环 → 嵌套 → 综合”的顺序，把前面的定义落到具体流程中。每张图都沿箭头阅读，判断框的“是/否”必须由具体条件值决定。</p>\n<h3 id="从符号顺序到分支">从符号、顺序到分支</h3>\n<pre><code>if (x &gt; 0) {  // 第一个判断框先检查正数路径    cout &lt;&lt; "正数";} else if (x &lt; 0) {  // 只有第一问为假才检查负数路径    cout &lt;&lt; "负数";} else {  // 前两问都为假时只剩 x 等于 0    cout &lt;&lt; "零";}</code></pre>\n<h3 id="从循环追踪到嵌套与综合">从循环追踪到嵌套与综合</h3>\n<pre><code>if (x &gt;= 0) {  // 判断框决定从哪一个出口离开    cout &lt;&lt; "non-negative" &lt;&lt; endl;} else {    cout &lt;&lt; "negative" &lt;&lt; endl;}</code></pre>\n<p>对应流程是“开始 → 输入 x → 判断 x&gt;=0 → 两条输出路径 → 结束”。代码中的条件、真假分支和汇合位置都应在图中找到对应关系。</p>\n<p>把具体输入代入后，路径可以这样追踪：</p>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th><code>x &gt;= 0</code></th>\n<th>选择出口</th>\n<th>执行结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>x = 5</code></td>\n<td>真</td>\n<td>是</td>\n<td>输出 <code>non-negative</code></td>\n</tr>\n<tr>\n<td><code>x = -2</code></td>\n<td>假</td>\n<td>否</td>\n<td>输出 <code>negative</code></td>\n</tr>\n</tbody>\n</table>\n<p>判断框只选择其中一条路径，不会把两个输出都执行。</p>\n<p>循环流程“<code>i=1</code> → 判断 <code>i&lt;=3</code> → 输出 <code>i</code> → <code>i=i+1</code> → 返回判断”可以写成：</p>\n<pre><code>int i = 1;while (i &lt;= 3) {  // 判断框先检查当前 i 是否仍在范围内    cout &lt;&lt; i &lt;&lt; endl;    i++;  // 更新后返回判断，避免停在同一个状态}</code></pre>\n<p>也可以把初始化、条件和更新集中写成 <code>for (int i = 1; i &lt;= 3; i++)</code>。互译时不是看到返回箭头就固定写某一种循环，而是确认初始化、判断、循环体和更新分别在哪里。</p>\n<h3 id="从代码反推流程图">从代码反推流程图</h3>\n<p>先看一段“计算 1 到 <code>n</code> 的和”的局部代码：</p>\n<pre><code>int i = 1;int sum = 0;while (i &lt;= n) {  // 判断为真才进入本轮    sum = sum + i;  // 保存加入当前 i 后的新结果    i++;}cout &lt;&lt; sum &lt;&lt; endl;</code></pre>\n<p>反推时不要一行代码随意画一个框，而要先判断每行承担什么职责：</p>\n<table>\n<thead>\n<tr>\n<th>代码部分</th>\n<th>对应图形或箭头</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>i=1、sum=0</code></td>\n<td>一个或两个处理框</td>\n</tr>\n<tr>\n<td><code>i&lt;=n</code></td>\n<td>有“是/否”两个出口的判断框</td>\n</tr>\n<tr>\n<td><code>sum=sum+i、i++</code></td>\n<td>“是”出口上的处理框</td>\n</tr>\n<tr>\n<td>循环体末尾</td>\n<td>返回 <code>i&lt;=n</code> 判断框的箭头</td>\n</tr>\n<tr>\n<td><code>cout&lt;&lt;sum</code></td>\n<td>“否”出口之后的输入输出框</td>\n</tr>\n</tbody>\n</table>\n<p>完整流程是“开始 → 初始化 → 判断 <code>i&lt;=n</code> → 是：累加、更新、返回判断；否：输出 <code>sum</code> → 结束”。最后一次失败判断也必须画出来，否则“什么时候离开循环”没有图形依据。</p>\n<h3 id="从题意设计流程图">从题意设计流程图</h3>\n<p>题目要求“输入成绩 <code>score</code>，达到 60 输出通过，否则输出继续练习”时，可以按五步设计：先圈出输入 <code>score</code> 和两种输出；再把 <code>score&gt;=60</code> 写进判断菱形；给真出口连接“输出通过”，给假出口连接“输出继续练习”；让两条路径都通向结束；最后代入 59、60 两个边界值沿箭头检查。也就是先从题意提取“输入—判断—输出”，再选择图形和连接顺序，不要先画形状再猜每个框里写什么。</p>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**流程为“输入 <code>x、y</code> → 判断 <code>x&gt;y</code> → 是：<code>a=x,b=y</code>；否：<code>a=y,b=x</code> → 输出 <code>a、b</code>”。输入 <code>x=7、y=5</code> 时输出什么？</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li>输入后变量表为 <code>x=7、y=5</code>；</li>\n<li>判断 <code>7&gt;5</code>，结果为真；</li>\n<li>沿“是”出口执行 <code>a=x</code>，得到 <code>a=7</code>；</li>\n<li>再执行 <code>b=y</code>，得到 <code>b=5</code>；</li>\n<li>两条分支汇合后输出 <code>7 5</code>，没有执行“否”路径。</li>\n</ol>\n<p>**结论：**输出 <code>7 5</code>。读分支流程图必须先代入具体值判断真假，再沿唯一一条路径更新变量。</p>\n<p>**引导练习：**请把“输入 <code>n</code>，计算 1 到 <code>n</code> 的和并输出”补成完整流程。纸上已经有“开始 → 输入 <code>n</code> → <code>i=1,sum=0</code>”以及判断框 <code>i&lt;=n</code>，你需要补出“是”出口上的两个处理步骤、返回箭头和“否”出口。提示：每轮既要累加也要更新 <code>i</code>。用 <code>n=3</code> 核对时，<code>sum</code> 应依次变为 1、3、6，随后在 <code>i=4</code> 时判断为假并输出 6；若缺少 <code>i=i+1</code> 或最后一次失败判断，就还没有完成。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>把平行四边形和处理矩形混淆；</li>\n<li>遇到判断框不计算条件，直接选择看起来更顺的路径；</li>\n<li>把“是”和“否”的出口标反，导致代码条件与流程图路径不一致；</li>\n<li>循环追踪时忘记变量更新；</li>\n<li>漏画离开循环的出口，使流程只能不断返回判断；</li>\n<li>箭头方向画反，阅读顺序与实际执行顺序相反；</li>\n<li>把只执行一次的分支误认成循环；</li>\n<li>互译时遗漏输入、输出或结束位置。</li>\n</ul>\n<p>**错误写法：**判断框写 <code>x &gt;= 0</code>，却把“是”箭头连到输出 <code>negative</code>。</p>\n<p>**错误原因：**条件成立时选择“是”出口。<code>x &gt;= 0</code> 为真表示非负数，出口含义与输出内容相反。</p>\n<p><strong>正确写法：</strong>“是”出口连接 <code>non-negative</code>，“否”出口连接 <code>negative</code>；再分别用 <code>x=5</code> 和 <code>x=-2</code> 追踪一次。</p>\n<p>**错误写法：**循环流程只有“判断 → 输出 → 返回判断”，没有 <code>i=i+1</code>，也没有条件为假时的出口。</p>\n<p>**错误原因：**变量不变化会让条件一直成立；没有“否”出口也无法表示离开循环。</p>\n<p>**正确写法：**返回判断之前先更新 <code>i</code>，并让条件为假的箭头指向循环后的步骤或结束框。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>读流程图始终遵守四步：确认当前框的作用，执行框内操作，记录变量或输出，再沿唯一箭头前进。分支要写出条件实际值和未执行路径；循环要记录初始化、每轮判断、循环体、更新以及最后一次失败判断。流程图与代码互译时，结构和执行顺序必须一一对应。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>多层分支和多层循环页面会把更复杂代码还原为执行路径；流程图是检查这些路径是否完整的直观工具。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<p>先观察一个现象：<code>cout &lt;&lt; (\'C\' + 2);</code> 会输出整数 <code>69</code>，把同一个计算结果保存到 <code>char</code> 后却会输出字符 <code>E</code>。两个结果为什么不同？这正是本页要解决的问题。</p>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> ASCII 中空格为 32、<code>\'0\'</code> 为 48、<code>\'A\'</code> 为 65、<code>\'a\'</code> 为 97，数字、大写字母和小写字母各自连续；字符参与运算时先按编码值计算。</li>\n<li><strong>常用语法：</strong> 转换前先检查字符范围，再明确结果要保存成整数还是字符：<code>int code = static_cast&lt;int&gt;(ch);</code>、<code>char ch = static_cast&lt;char&gt;(code);</code>；数字字符判断写成 <code>ch &gt;= \'0\' &amp;&amp; ch &lt;= \'9\'</code>。</li>\n<li><strong>最小示例：</strong> 数字字符和字母转换都建立在各自连续的编码范围上：<code>\'7\' - \'0\' == 7</code>，<code>char upper = lower - \'a\' + \'A\';</code>。</li>\n<li><strong>关键易错点：</strong> 不要把字符 <code>\'7\'</code> 当成整数 <code>7</code>；不要写 <code>\'0\' &lt;= ch &lt;= \'9\'</code>；字符相减的结果是整数，不会自动按字符显示。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页要认识 ASCII 编码的基本原理、常用字符编码和连续区间，并能在字符与整数编码之间转换。</p>\n<p>本页只建立三个新核心：</p>\n<ol>\n<li>记住空格、<code>\'0\'</code>、<code>\'A\'</code>、<code>\'a\'</code> 对应的编号，用连续区间推导其他编码；</li>\n<li>在字符与编码之间双向转换，并追踪当前值、类型和输出形式；</li>\n<li>先判断范围，再完成数字字符和 ASCII 英文字母大小写的基础转换。</li>\n</ol>\n<p>开始前，只需要已经认识 <code>char</code>、<code>int</code>、<code>cin/cout</code>、比较、<code>&amp;&amp;</code> 和 <code>if/else</code>。完整的类型转换规则会在下一页系统学习。</p>\n<p>计算机用整数编码保存字符。二级不要求背完整 ASCII 表，但要记住常用字符的编号、连续区间和字符参与运算时的结果。</p>\n<p>本页先记住空格、<code>\'0\'</code>、<code>\'A\'</code>、<code>\'a\'</code> 四个常用 ASCII 字符的编号，再利用数字、大写字母、小写字母各自连续排列的规律，完成字符与编码、数字字符与整数、大小写字母之间的基础转换。</p>\n<p>数字字符与整数的转换、ASCII 英文字母的大小写转换，是利用连续区间完成的两个小任务。完成它们，才算真正把编码规律用起来。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>分析字符运算时，依次记录四件事：<strong>当前值、当前类型、执行的操作、最终怎样输出</strong>。先看开头问题的最小过程：</p>\n<pre><code>cout &lt;&lt; (\'C\' + 2) &lt;&lt; endl;char letter = \'C\' + 2;cout &lt;&lt; letter &lt;&lt; endl;</code></pre>\n<table>\n<thead>\n<tr>\n<th>代码</th>\n<th>运算后的值与类型</th>\n<th>输出时的类型</th>\n<th>屏幕结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>\'C\' + 2</code></td>\n<td><code>69</code>，<code>int</code></td>\n<td><code>int</code></td>\n<td><code>69</code></td>\n</tr>\n<tr>\n<td><code>char letter = \'C\' + 2</code></td>\n<td>把整数 69 保存为 <code>char</code></td>\n<td><code>char</code></td>\n<td><code>E</code></td>\n</tr>\n</tbody>\n</table>\n<p>计算的中间值相同，显示不同，是因为输出时的类型不同。下面再解释 69 是怎样得到的。</p>\n<a id="01-ascii"></a>\n<h2 id="01-ascii-编码与常用字符">01 ASCII 编码与常用字符</h2>\n<p>先记住下面四个常用字符的编号：</p>\n<table>\n<thead>\n<tr>\n<th>字符</th>\n<th style="text-align: right">ASCII 编码</th>\n<th>用途</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>空格</td>\n<td style="text-align: right">32</td>\n<td>认识空白字符的编码</td>\n</tr>\n<tr>\n<td><code>\'0\'</code></td>\n<td style="text-align: right">48</td>\n<td>推导数字字符</td>\n</tr>\n<tr>\n<td><code>\'A\'</code></td>\n<td style="text-align: right">65</td>\n<td>推导大写英文字母</td>\n</tr>\n<tr>\n<td><code>\'a\'</code></td>\n<td style="text-align: right">97</td>\n<td>推导小写英文字母</td>\n</tr>\n</tbody>\n</table>\n<p>先区分三个概念：<strong>字符</strong>是 <code>\'A\'</code> 这样的符号；<strong>编码</strong>是计算机为字符规定的整数编号，例如 65；<strong>字形</strong>是屏幕或纸上画出的具体样子。ASCII 规定字符与整数怎样对应，不规定字体怎样画字母。</p>\n<details><summary>补充了解：为什么说 ASCII 是 7 位？</summary><p>标准 ASCII 使用 7 个二进制位，编码范围是 0 到 127。C++ 规定 <code>sizeof(char)</code> 等于 1 Byte；在本项目常用的 G++ / OJ 环境中，一个 Byte 是 8 bit，因此一个 <code>char</code> 能保存本页使用的 ASCII 编码。<strong>ASCII 的 7 位范围</strong>和<strong>本环境中 <code>char</code> 占 1 Byte</strong>是两件相关但不同的事。</p></details>\n<p>数字字符 <code>\'0\'</code> 到 <code>\'9\'</code> 连续排列，大写字母 <code>\'A\'</code> 到 <code>\'Z\'</code> 连续排列，小写字母 <code>\'a\'</code> 到 <code>\'z\'</code> 也连续排列。遇到同一段中的其他字符，可以从已经记住的字符开始往后数：</p>\n<ul>\n<li><code>\'3\'</code> 在 <code>\'0\'</code> 后面 3 格，所以编码是 <code>48 + 3 = 51</code>；</li>\n<li><code>\'C\'</code> 在 <code>\'A\'</code> 后面 2 格，所以编码是 <code>65 + 2 = 67</code>。</li>\n</ul>\n<p>不需要背完整 ASCII 表，但不能跨不同区间随意推算。</p>\n<p>字符常量用单引号，如 <code>\'A\'</code>；双引号 <code>"A"</code> 表示字符串，不是单个 <code>char</code>。</p>\n<p>字符 <code>\'1\'</code> 与整数 <code>1</code> 看起来相似，含义却不同：<code>\'1\'</code> 是编码为 49 的字符，<code>1</code> 是数值一。<code>cout &lt;&lt; \'1\'</code> 和 <code>cout &lt;&lt; 1</code> 都能在屏幕上显示 1，但 <code>\'1\' + 1</code> 得到整数 50，<code>1 + 1</code> 得到整数 2。判断类型不能只看屏幕上的符号。</p>\n<a id="02-operation"></a>\n<h2 id="02-字符编码与整数运算">02 字符编码与整数运算</h2>\n<p><code>char</code> 参与加减运算时，会先提升为整数再计算。<code>\'C\'</code> 的编码是 67，所以 <code>\'C\' + 2</code> 得到 <code>int</code> 类型的 69。只有把这个结果保存或转换成 <code>char</code>，才会按编码 69 显示字符 <code>E</code>。</p>\n<p>本页使用两个固定工具：</p>\n<pre><code>static_cast&lt;int&gt;(ch)    // 得到字符 ch 对应的整数编码static_cast&lt;char&gt;(code) // 得到整数 code 对应的字符</code></pre>\n<p><code>static_cast&lt;目标类型&gt;(值)</code> 会产生本次使用的新结果，不会永久改变原变量的声明类型。先会用这两个固定写法即可；隐式转换、显式转换和更多类型组合留到<a href="/gesp/level2/type-conversion/">数据类型转换</a>页面学习。</p>\n<p><code>cin &gt;&gt; ch</code> 默认跳过空格、换行和制表符等空白，所以不能用下面的完整程序把键盘输入的空格直接读入第一项 <code>ch</code>。如果只想验证空格编码，可以在代码中保存字符常量：</p>\n<pre><code>char space = \' \';cout &lt;&lt; static_cast&lt;int&gt;(space) &lt;&lt; endl; // 输出 32</code></pre>\n<p>ASCII 中 0 到 31 是控制代码，127 是 DEL；32 到 126 通常作为可打印范围，其中 32 是显示为空白的空格。</p>\n<p>下面唯一一份完整程序<strong>约定</strong>第一项是非空白可打印 ASCII 字符，第二项是 32 到 126 的编码。程序本身不检查输入范围；只有输入满足约定时，转换结果才是本课准备观察的字符。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    char ch;    int code;    cin &gt;&gt; ch &gt;&gt; code;    cout &lt;&lt; static_cast&lt;int&gt;(ch) &lt;&lt; endl;  // 字符转成整数编码    cout &lt;&lt; static_cast&lt;char&gt;(code) &lt;&lt; endl;  // 编码转成可显示字符    return 0;}</code></pre>\n<p><strong>输入样例：</strong></p>\n<pre><code>A 66</code></pre>\n<p><strong>输出样例：</strong></p>\n<pre><code>65B</code></pre>\n<p>执行过程如下：</p>\n<table>\n<thead>\n<tr>\n<th>输入值与类型</th>\n<th>操作</th>\n<th>新结果与类型</th>\n<th>屏幕输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>ch=\'A\'</code>，<code>char</code></td>\n<td><code>static_cast&lt;int&gt;(ch)</code></td>\n<td><code>65</code>，<code>int</code></td>\n<td><code>65</code></td>\n</tr>\n<tr>\n<td><code>code=66</code>，<code>int</code></td>\n<td><code>static_cast&lt;char&gt;(code)</code></td>\n<td><code>\'B\'</code>，<code>char</code></td>\n<td><code>B</code></td>\n</tr>\n</tbody>\n</table>\n<p>两次转换都只产生新的表达式结果：<code>ch</code> 仍是 <code>char</code>，<code>code</code> 仍是 <code>int</code>。如果第二项输入 32，第二行会输出一个空格，看起来像一行空白；本程序不把控制代码作为需要观察字形的输入。</p>\n<p>**引导练习：**先尝试自己写出输入 <code>0 57</code> 时的两行输出，再逐格核对“当前值—类型—操作—新类型—输出”。</p>\n<details><summary>完成后展开核对</summary><p>第一项 <code>ch=\'0\'</code> 是 <code>char</code>，转换成 <code>int</code> 后得到 48，第一行输出 <code>48</code>。第二项 <code>code=57</code> 是 <code>int</code>，转换成 <code>char</code> 后得到 <code>\'9\'</code>，第二行输出 <code>9</code>；两个原变量的声明类型都没有改变。</p></details>\n<a id="03-digits"></a>\n<h2 id="03-数字字符的判断与转换">03 数字字符的判断与转换</h2>\n<p>先观察：<code>\'8\' - \'0\'</code> 得到整数 8；<code>\':\' - \'0\'</code> 也能算出整数 10，但 <code>\':\'</code> 并不是数字字符。<strong>能算出差值，不等于输入合法</strong>，所以第一步必须判断字符是否落在连续的数字区间：</p>\n<pre><code>if (ch &gt;= \'0\' &amp;&amp; ch &lt;= \'9\') {  // 两个边界必须同时满足    cout &lt;&lt; "digit" &lt;&lt; endl;} else {    cout &lt;&lt; "not digit" &lt;&lt; endl;}</code></pre>\n<p>确认范围后，利用与 <code>\'0\'</code> 的距离完成双向转换：</p>\n<table>\n<thead>\n<tr>\n<th>目标</th>\n<th>具体计算</th>\n<th style="text-align: right">中间整数</th>\n<th>最终结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>字符 <code>\'7\'</code> 转数值</td>\n<td><code>\'7\' - \'0\'</code></td>\n<td style="text-align: right"><code>55 - 48 = 7</code></td>\n<td>整数 7</td>\n</tr>\n<tr>\n<td>整数 7 转字符</td>\n<td><code>\'0\' + 7</code></td>\n<td style="text-align: right"><code>48 + 7 = 55</code></td>\n<td>保存到 <code>char</code> 后为 <code>\'7\'</code></td>\n</tr>\n</tbody>\n</table>\n<p>整数转数字字符前，必须保证整数在 0 到 9 之间。若 <code>x=12</code>，<code>\'0\' + x</code> 得到编码 60，对应的不是字符 <code>\'12\'</code>；一个 <code>char</code> 也不能同时保存两个字符。</p>\n<p>不能写 <code>\'0\' &lt;= ch &lt;= \'9\'</code>。C++ 会先计算 <code>\'0\' &lt;= ch</code>，得到 0 或 1，再把这个结果与 <code>\'9\'</code> 比较，并不是数学中的连续不等式。正确写法是 <code>ch &gt;= \'0\' &amp;&amp; ch &lt;= \'9\'</code>。</p>\n<a id="04-case"></a>\n<h2 id="04-英文字母大小写转换">04 英文字母大小写转换</h2>\n<p>在 ASCII 中，对应的英文字母大小写编码相差 <code>\'a\' - \'A\'</code>，结果是 32。这个规律只用于 ASCII 英文字母：如果不判断范围就给 <code>\'?\'</code> 加 32，会得到字符 <code>\'_\'</code>，这不是大小写转换。因此，转换前必须先检查字符属于哪一段。</p>\n<pre><code>if (ch &gt;= \'A\' &amp;&amp; ch &lt;= \'Z\') {    ch = ch + (\'a\' - \'A\');} else if (ch &gt;= \'a\' &amp;&amp; ch &lt;= \'z\') {    ch = ch - (\'a\' - \'A\');}cout &lt;&lt; ch &lt;&lt; endl;</code></pre>\n<p>输入 <code>b</code> 时：</p>\n<ol>\n<li>大写区间判断为假，小写区间判断为真；</li>\n<li><code>\'b\'</code> 的编码 98 减去 32，得到整数 66；</li>\n<li>66 保存回 <code>char ch</code>，对应字符 <code>\'B\'</code>；</li>\n<li><code>cout &lt;&lt; ch</code> 输出 <code>B</code>。</li>\n</ol>\n<p>输入 <code>?</code> 时，两个范围判断都不成立，程序不会加减 32，仍输出 <code>?</code>。范围判断不是装饰，它防止数字和标点被错误移动到其他编码位置。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>目标</th>\n<th>前提</th>\n<th>写法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>查看字符编码</td>\n<td><code>ch</code> 是本页讨论的 ASCII 字符</td>\n<td><code>static_cast&lt;int&gt;(ch)</code></td>\n</tr>\n<tr>\n<td>编码转字符</td>\n<td><code>code</code> 已确认在目标 ASCII 范围</td>\n<td><code>static_cast&lt;char&gt;(code)</code></td>\n</tr>\n<tr>\n<td>判断数字字符</td>\n<td>无</td>\n<td><code>ch &gt;= \'0\' &amp;&amp; ch &lt;= \'9\'</code></td>\n</tr>\n<tr>\n<td>数字字符转整数</td>\n<td>已确认 <code>\'0\'</code> 到 <code>\'9\'</code></td>\n<td><code>ch - \'0\'</code></td>\n</tr>\n<tr>\n<td>0—9 转数字字符</td>\n<td><code>x</code> 在 0 到 9</td>\n<td><code>\'0\' + x</code>，再保存到 <code>char</code></td>\n</tr>\n<tr>\n<td>大写转小写</td>\n<td>已确认 <code>\'A\'</code> 到 <code>\'Z\'</code></td>\n<td><code>upper + (\'a\' - \'A\')</code></td>\n</tr>\n<tr>\n<td>小写转大写</td>\n<td>已确认 <code>\'a\'</code> 到 <code>\'z\'</code></td>\n<td><code>lower - (\'a\' - \'A\')</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**比较 <code>char ch=\'4\'; ch=ch+2; cout&lt;&lt;ch;</code> 与 <code>cout&lt;&lt;(\'4\'+2);</code> 的输出。</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li><code>\'4\'</code> 的编码是 52，加 2 后中间结果都是 <code>int</code> 类型的 54；</li>\n<li>第一段把 54 保存回 <code>char ch</code>，编码 54 对应字符 <code>\'6\'</code>，所以输出 <code>6</code>；</li>\n<li>第二段直接输出整数表达式，没有转回 <code>char</code>，所以输出数值 <code>54</code>。</li>\n</ol>\n<p>**结论：**两段计算的中间整数相同，最终显示不同，是因为保存或输出时的类型不同。</p>\n<p>**引导练习：**字符 <code>ch=\'8\'</code> 的编码是 56。请分别计算 <code>ch-\'0\'</code>、<code>ch+1</code> 直接交给 <code>cout</code>、以及 <code>char next=ch+1; cout&lt;&lt;next;</code> 的结果。提示：先写出中间整数，再看输出时是 <code>int</code> 还是 <code>char</code>。核对结果依次是整数 8、整数 57、字符 <code>9</code>；若把第二项也写成字符 9，说明漏看了表达式结果类型。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>**把字符 <code>\'7\'</code> 当成整数 <code>7</code>：**计算会相差 48。先确认范围，再用 <code>\'7\' - \'0\'</code> 得到数值 7；回看 <a href="#03-digits">03 数字字符</a>。</li>\n<li>**只算编码，不追踪类型：**整数 54 可能输出 <code>54</code>，保存为 <code>char</code> 后却会输出 <code>6</code>。每一步都写清结果类型；回看 <a href="#02-operation">02 字符编码与整数运算</a>。</li>\n<li>**写连续不等式 <code>\'0\' &lt;= c &lt;= \'9\'</code>：**左边比较会先变成 0 或 1。立即改成 <code>c &gt;= \'0\' &amp;&amp; c &lt;= \'9\'</code>；回看 <a href="#03-digits">03 数字字符</a>。</li>\n<li>**不检查范围就转 <code>char</code> 或直接加减 32：**可能把标点变成另一个无关字符。先检查目标编码、数字或字母区间；回看 <a href="#03-digits">03 数字字符</a> 和 <a href="#04-case">04 大小写转换</a>。</li>\n<li>**写 <code>char c = "A";</code>：**双引号表示字符串，不能保存到单个 <code>char</code>。立即改成单引号 <code>char c = \'A\';</code>；回看 <a href="#01-ascii">01 ASCII 编码与常用字符</a>。</li>\n</ul>\n<p>**修正后复演：**依次代入 <code>\'/\'</code>、<code>\'0\'</code>、<code>\'9\'</code>、<code>\':\'</code>。只有 <code>\'0\'</code> 和 <code>\'9\'</code> 同时满足上下边界，前后的标点都应进入 <code>else</code>；这四个结果正好检查两个等号和两个区间外边界。</p>\n<p>输出编码时也要明确类型：<code>cout &lt;&lt; c;</code> 输出字符本身，<code>cout &lt;&lt; static_cast&lt;int&gt;(c);</code> 才输出对应整数编码。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>ASCII 用整数给字符编号。记住空格、<code>\'0\'</code>、<code>\'A\'</code>、<code>\'a\'</code> 对应的编号，再利用数字、大写字母和小写字母各自连续的规律，就能推导常用编码。字符参与算术时先得到整数结果；之后是显示整数还是字符，要看结果最终以 <code>int</code> 还是 <code>char</code> 保存和输出。数字与大小写转换都必须先检查范围。</p>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li>如果还分不清当前值和当前类型，先回看<a href="/gesp/level1/data-types/">基本数据类型</a>；</li>\n<li>如果想系统理解 <code>static_cast</code> 和混合类型运算，继续学习<a href="/gesp/level2/type-conversion/">数据类型转换</a>；</li>\n<li>如果执行步骤容易跳步，可以回看<a href="/gesp/level2/flowcharts/">流程图基础</a>。</li>\n</ul>\n<h2 id="进入综合测试前">进入综合测试前</h2>\n<p>达到下面三项，才算真正学会：</p>\n<ol>\n<li>不查完整表，能从一个已知字符的编号推导另一个数字字符或英文字母的编码；算错时回看 <a href="#01-ascii">01 ASCII 编码与常用字符</a>；</li>\n<li>能解释 <code>A → 65</code>、<code>66 → B</code> 的每一步值和类型；混淆输出时回看 <a href="#02-operation">02 字符编码与整数运算</a>；</li>\n<li>能先判断范围，并分别完成一次数字字符双向转换和一次 ASCII 英文字母大小写转换；数字边界回看 <a href="#03-digits">03 数字字符</a>，字母边界回看 <a href="#04-case">04 大小写转换</a>。</li>\n</ol>\n<h3 id="三个核心自检">三个核心自检</h3>\n<ol>\n<li>不查表，从 <code>\'A\'=65</code> 推导 <code>\'G\'</code> 的编码；</li>\n<li>写出完整程序输入 <code>B 97</code> 时的两行输出，并说出两次转换后的类型；</li>\n<li>先写范围判断，再分别完成：字符 <code>\'8\'</code> 转整数、整数 3 转字符、字符 <code>\'d\'</code> 转大写；同时说明为什么标点 <code>\'?\'</code> 不应加减 32。</li>\n</ol>\n<details><summary>三题都完成后展开核对</summary><ol>\n<li><code>\'G\'</code> 在 <code>\'A\'</code> 后 6 格，编码是 <code>65 + 6 = 71</code>。</li>\n<li>第一行是 <code>66</code>，结果类型为 <code>int</code>；第二行是 <code>a</code>，结果类型为 <code>char</code>。</li>\n<li><code>\'8\' - \'0\'</code> 得整数 8；<code>\'0\' + 3</code> 的整数结果保存为 <code>char</code> 后得到 <code>\'3\'</code>；<code>\'d\'</code> 在 <code>\'a\'</code> 到 <code>\'z\'</code> 内，减去 32 后得到 <code>\'D\'</code>。<code>\'?\'</code> 不在两个英文字母区间内，直接加减 32 只会把它变成无关标点。</li>\n</ol></details>\n<p>下面 8 道题是本页唯一的综合巩固入口。做错后回看题目对应的知识部分，再重新解释原因，不只记答案。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 隐式转换由表达式规则触发，显式转换由程序员写出；转换发生在除法前才会改变除法类型，浮点数转整数按向零方向截断。</li>\n<li><strong>常用语法：</strong> 需要浮点除法时写 <code>double result = double(a) / b;</code>，浮点数转整数写 <code>int whole = int(value);</code>；还要能识别 <code>(double)(a)</code> 和 <code>(int)(value)</code>。</li>\n<li><strong>最小示例：</strong> <code>5 / 2</code> 得到 2，<code>double(5) / 2</code> 和 <code>(double)(5) / 2</code> 都得到 2.5，<code>int(-2.9)</code> 和 <code>(int)(-2.9)</code> 都得到 -2。</li>\n<li><strong>关键易错点：</strong> <code>double(a / b)</code> 或 <code>(double)(a / b)</code> 都转换得太晚，丢失的小数不能恢复；向零截断不是四舍五入。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的数据类型转换：理解系统自动进行的隐式转换，能够进行显式转换，并正确处理整数除法、浮点数转整数和混合类型表达式。</p>\n<p>先看一个会直接影响答案的现象：<code>int a=5, b=2; double result=a/b;</code> 中，<code>result</code> 保存的不是 2.5，而是 2.0。右侧的两个操作数都是 <code>int</code>，所以程序先完成整数除法得到 2，最后才把 2 保存为 <code>double</code>。要保留小数，必须在除法发生前先转换一个操作数。</p>\n<p>类型转换决定一个值用哪种形式参与运算。最常见的错误不是“不会转换”，而是转换发生得太晚，整数除法已经丢掉了小数部分。</p>\n<p>本页区分系统自动完成的隐式转换和程序员明确写出的显式转换；重点比较“除法前转换”与“除法后转换”，并用正数、负数和接近 0 的数确认浮点数转整数采用向 0 截断而不是四舍五入。</p>\n<p><strong>数据类型</strong>规定一段数据怎样解释、能参与哪些运算以及通常怎样存储；<strong>类型转换</strong>是把一个值按另一种类型产生新结果。转换可能改变表示范围、计算规则或输出形式，但不会自动把原变量的声明类型永久改掉。例如把 <code>int</code> 值 5 转成 <code>double</code> 会得到 5.0 参与本次运算，原来的整数变量仍是 <code>int</code>。</p>\n<a id="01-implicit"></a>\n<h2 id="01-隐式类型转换">01 隐式类型转换</h2>\n<p>不同类型共同运算时，C++ 会按规则自动转换。例如 <code>int</code> 与 <code>double</code> 相加时，整数先转成 <code>double</code>，结果通常也是 <code>double</code>。</p>\n<p>隐式转换常出现在三个位置：赋值时，右边值要适应左边变量；混合运算时，参与运算的值会按规则调整到可以共同计算的类型，例如 <code>int</code> 与 <code>double</code> 相加时把整数值转为 <code>double</code>；函数调用时，实参要适应形参类型。二级重点分析前两种。每次都问两句：“转换发生在运算前还是运算后？”“最终结果保存进什么类型？”</p>\n<p>字符参与算术时也会转换成整数编码，这与 ASCII 页的规律相连：<code>\'A\' + 1</code> 先把 <code>\'A\'</code> 按编码 65 参与计算，得到整数 66；若把 66 保存到 <code>char</code>，才会按字符显示为 <code>B</code>。所以“同一个中间数值”可能因为最终保存类型不同而产生不同输出形式。</p>\n<p>例如 <code>int a = 10; double b = a;</code> 中，不需要手动写转换。赋值过程可以追踪为：<code>10（int） → 10.0（double）</code>。<code>a</code> 本身仍是整数 10，只有赋给 <code>b</code> 的值按 <code>double</code> 保存。</p>\n<p>把浮点结果赋给整数变量时，小数部分会向 0 截去：</p>\n<pre><code>int value = 12.8;int other = -12.8;  // 负数也向 0 截断，因此保存 -12cout &lt;&lt; value &lt;&lt; " " &lt;&lt; other &lt;&lt; endl;</code></pre>\n<p>赋值过程分别是 <code>12.8 → 12</code> 和 <code>-12.8 → -12</code>，所以输出 <code>12 -12</code>。向 0 截断就是删去小数部分：正数向较小方向靠近 0，负数向较大方向靠近 0；它不是四舍五入。复合赋值 <code>a *= b</code> 也会把计算结果转换回左边变量 <code>a</code> 的类型。</p>\n<a id="02-explicit"></a>\n<h2 id="02-显式类型转换">02 显式类型转换</h2>\n<p>程序员明确要求改变类型叫显式转换。本页主教学使用 <code>目标类型(值)</code>，例如 <code>int(x)</code>、<code>double(a)</code>。这种写法看起来像函数调用，但这里的 <code>int</code>、<code>double</code> 是目标类型，圆括号里是要转换的值，整段表达式产生一个新值。</p>\n<p>还要能识别 C 风格的 <code>(目标类型)(值)</code>，例如 <code>(int)(x)</code>、<code>(double)(a)</code>。第一组括号写目标类型，第二组括号圈出要转换的值或表达式。两种写法都只产生本次计算使用的新值，不会修改原变量的声明类型。</p>\n<pre><code>double x = -98.76 / 8;int n1 = int(x);  // 主教学写法：把 x 转成 int 新值int n2 = (int)(x);  // 识读写法：两组括号分别表示目标类型和值int a = 5;int b = 2;double y1 = double(a) / b;  // 在除法前把 a 转成 doubledouble y2 = (double)(a) / b;  // 与上一行转换时机相同cout &lt;&lt; n1 &lt;&lt; \' \' &lt;&lt; n2 &lt;&lt; endl;cout &lt;&lt; y1 &lt;&lt; \' \' &lt;&lt; y2 &lt;&lt; endl;</code></pre>\n<p><code>x</code> 约为 <code>-12.345</code>，所以 <code>n1</code>、<code>n2</code> 都是 -12；<code>y1</code>、<code>y2</code> 都是 2.5。本站主示例优先使用 <code>int(x)</code> 和 <code>double(a)</code>，C 风格写法用于识读。显式转换让意图更清楚，但不能恢复之前已经丢失的信息。</p>\n<p>转换表达式不会改写源变量。例如 <code>double value=-2.9; int whole=int(value);</code> 执行后，<code>value</code> 仍是 <code>double</code> 类型的 -2.9，只有 <code>whole</code> 保存整数 -2。把两者分别输出，看到 <code>-2.9</code> 和 <code>-2</code>，就是“原变量未改变”的复核标准。</p>\n<p>转换目标还必须装得下结果。把普通小数转成 <code>int</code> 时可以讨论向 0 截断，但若原值已经超出目标整数类型能表示的范围，就不能再把结果当作可靠答案。竞赛题通常会给出输入范围；做题前先核对范围，比事后猜测溢出结果更重要。</p>\n<a id="03-division"></a>\n<h2 id="03-整数除法与转换时机">03 整数除法与转换时机</h2>\n<p>两个整数相除执行整数除法，<code>5 / 2</code> 得到 2。要得到 2.5，必须让除法发生前至少一个操作数变成浮点数：</p>\n<pre><code>int a = 5;int b = 2;double x1 = double(a) / b;  // 先转换，再做浮点除法double x2 = double(a / b);  // 先做整数除法，再转换结果cout &lt;&lt; x1 &lt;&lt; \' \' &lt;&lt; x2 &lt;&lt; endl;</code></pre>\n<p>把表达式拆开时，必须同时记录值和类型：</p>\n<table>\n<thead>\n<tr>\n<th>步骤</th>\n<th><code>double(a) / b</code> 或 <code>(double)(a) / b</code></th>\n<th><code>double(a / b)</code> 或 <code>(double)(a / b)</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>读变量</td>\n<td><code>a=5(int)</code>、<code>b=2(int)</code></td>\n<td><code>a=5(int)</code>、<code>b=2(int)</code></td>\n</tr>\n<tr>\n<td>第一个实际运算</td>\n<td><code>a → 5.0(double)</code></td>\n<td><code>5/2 → 2(int)</code></td>\n</tr>\n<tr>\n<td>第二个实际运算</td>\n<td><code>5.0/2 → 2.5(double)</code></td>\n<td><code>2 → 2.0(double)</code></td>\n</tr>\n<tr>\n<td>保存</td>\n<td><code>x1=2.5(double)</code></td>\n<td><code>x2=2.0(double)</code></td>\n</tr>\n</tbody>\n</table>\n<p><code>x1</code> 的转换发生在除法之前：先把 <code>a</code> 变成 <code>5.0</code>，再计算 <code>5.0 / 2</code>，得到 2.5；写成 <code>(double)(a) / b</code> 也是同一时机。<code>x2</code> 的转换发生在整数除法之后：先计算 <code>5 / 2</code> 得到 2，再把 2 转成 <code>double</code>，只能得到 2.0；写成 <code>(double)(a / b)</code> 也不能恢复小数。这就是“转换时机”。</p>\n<p>接收变量写成 <code>double</code> 并不会倒回右侧重算。转换发生的时机由右侧表达式的括号和操作数类型决定。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>现在已经形成了隐式转换、<code>目标类型(值)</code>、<code>(目标类型)(值)</code> 和整数除法的规则，再把三种结果放进唯一一份完整程序。程序依次输出整数除法、除法前显式转换和浮点数向 0 截断的结果，共用同一组输入，便于比较转换发生在什么时刻。</p>\n<p>下面的程序约定 <code>b</code> 不等于 0，并且 <code>value</code> 向 0 截断后的结果在 <code>int</code> 可表示范围内；不满足这两个前提时，不能把输出当作本页规则的有效验证。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int a, b;    double value;    cin &gt;&gt; a &gt;&gt; b &gt;&gt; value;    cout &lt;&lt; a / b &lt;&lt; endl;  // 两个操作数都是 int，执行整数除法    cout &lt;&lt; fixed &lt;&lt; setprecision(1) &lt;&lt; double(a) / b &lt;&lt; endl;  // 在除法前转换    cout &lt;&lt; int(value) &lt;&lt; endl;  // 向 0 截断    return 0;}</code></pre>\n<p>输入 <code>7 2 -3.8</code> 时：第一行先做整数除法得到 3；第二行在除法前把 <code>a</code> 转成 <code>double</code>，得到 3.5；第三行把 -3.8 向 0 截断，得到 -3。三行分别验证“运算类型”“转换时机”和“截断方向”。</p>\n<p>其中 <code>fixed &lt;&lt; setprecision(1)</code> 只控制浮点结果显示一位小数，显示时可能发生舍入；它既不改变变量的声明类型，也不是类型转换规则。真正让第二行在除法前使用浮点运算的是 <code>double(a)</code>。</p>\n<a id="04-mixed"></a>\n<h2 id="04-混合运算与计算顺序">04 混合运算与计算顺序</h2>\n<p>混合运算仍遵守括号、乘除、加减等顺序。处理面值或余量问题时，要先算已经使用的总金额，再计算剩余量：</p>\n<pre><code>int n = 28;int count10 = n / 10;int rest = n - count10 * 10;  // 余量要减去“数量 × 面值”int count3 = rest / 3;int count1 = rest - count3 * 3;cout &lt;&lt; count10 &lt;&lt; " " &lt;&lt; count3 &lt;&lt; " " &lt;&lt; count1 &lt;&lt; endl;</code></pre>\n<p>这里先得到 2 个 10 元，剩余 8 元，再得到 2 个 3 元和 2 个 1 元。每次减去的是“数量 × 面值”。</p>\n<p>逐项状态为：</p>\n<table>\n<thead>\n<tr>\n<th>步骤</th>\n<th>计算</th>\n<th>当前结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>取 10 元</td>\n<td><code>28 / 10</code></td>\n<td><code>count10=2</code></td>\n</tr>\n<tr>\n<td>算余量</td>\n<td><code>28-2×10</code></td>\n<td><code>rest=8</code></td>\n</tr>\n<tr>\n<td>取 3 元</td>\n<td><code>8 / 3</code></td>\n<td><code>count3=2</code></td>\n</tr>\n<tr>\n<td>再算余量</td>\n<td><code>8-2×3</code></td>\n<td><code>count1=2</code></td>\n</tr>\n</tbody>\n</table>\n<p>这里每一步都是整数运算。若误写成 <code>count1 = n - count10 - count3</code>，会得到 <code>28-2-2=24</code>，因为减掉的是代币个数，不是已经使用的金额。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>表达式</th>\n<th>结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>5 / 2</code></td>\n<td>整数 2</td>\n</tr>\n<tr>\n<td><code>double(5) / 2</code>、<code>(double)(5) / 2</code></td>\n<td>浮点数 2.5</td>\n</tr>\n<tr>\n<td><code>double(5 / 2)</code>、<code>(double)(5 / 2)</code></td>\n<td>浮点数 2.0</td>\n</tr>\n<tr>\n<td><code>int(3.9)</code></td>\n<td>3</td>\n</tr>\n<tr>\n<td><code>(int)(-3.9)</code></td>\n<td>-3</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**执行 <code>int a=5; double b=2.5; a*=b;</code> 后，<code>a</code> 的值是多少？</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li>右侧计算相当于先求 <code>a * b</code>；</li>\n<li>整数 5 与浮点数 2.5 运算时，5 隐式转成 5.0；</li>\n<li>得到浮点结果 <code>5.0 × 2.5 = 12.5</code>；</li>\n<li><code>a</code> 的类型仍是 <code>int</code>，复合赋值要把 12.5 转回整数；</li>\n<li>向 0 截去小数部分，得到 12。</li>\n</ol>\n<p><strong>结论：</strong><code>a</code> 最终为 12。判断复合赋值时既要看右侧怎样计算，也要看结果最后保存回什么类型。</p>\n<p>**引导练习：**设 <code>int sum=9, count=4; double value=-2.7;</code>，请分别计算 <code>double(sum) / count</code>、<code>(double)(sum) / count</code>、<code>double(sum / count)</code>、<code>int(value)</code> 和 <code>(int)(value)</code>。提示：每一步都写出“先发生的运算”和中间类型。核对结果应为 2.25、2.25、2.0、-2、-2；若第三项写成 2.25，说明把整数除法错误地推迟到了转换之后。</p>\n<p>**独立练习：**不看上面的过程，设 <code>int a=-7, b=2; double value=3.9;</code>，写出 <code>a / b</code>、<code>double(a) / b</code>、<code>(double)(a) / b</code>、<code>double(a / b)</code>、<code>(double)(a / b)</code>、<code>int(value)</code> 和 <code>(int)(value)</code> 的值与结果类型，并判断三个原变量是否被转换表达式改变。</p>\n<p>**成功标准：**七个结果依次为整数 -3、浮点数 -3.5、浮点数 -3.5、浮点数 -3.0、浮点数 -3.0、整数 3、整数 3；还应说明两个“晚转换”表达式都先算出整数 -3，并确认 <code>a</code>、<code>b</code>、<code>value</code> 的声明类型和值没有被转换表达式改写。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>把向 0 截断误认为四舍五入；</li>\n<li>在整数除法完成后才转换为 <code>double</code>；</li>\n<li>只看变量接收类型，不看右侧表达式先怎样计算；</li>\n<li>复合赋值后忘记结果要转回左值类型；</li>\n<li>计算余量时只减数量，没有乘面值。</li>\n</ul>\n<p><strong>错误写法：</strong><code>double average = double(sum / count);</code></p>\n<p><strong>错误原因：</strong><code>sum / count</code> 先执行整数除法，小数部分已经丢失，之后转成 <code>double</code> 也不能恢复。</p>\n<p><strong>正确写法：</strong><code>double average = double(sum) / count;</code>；也可以识读为 <code>double average = (double)(sum) / count;</code>。</p>\n<p>**修正后复演：**令 <code>sum=9、count=4</code>。错误写法先得到整数 2，再变成 2.0；两种正确写法都先把 <code>sum</code> 变成 9.0，再计算得到 2.25。能同时说明这两个最早不同步骤和最终结果，才算定位并修正了转换时机。</p>\n<p>**错误写法：**认为 <code>int value = 3.9;</code> 会得到 4。</p>\n<p>**错误原因：**浮点数转整数是向 0 截去小数部分，不是四舍五入。</p>\n<p>**正确写法：**明确按截断结果理解，<code>3.9</code> 转成 <code>int</code> 得到 3，<code>-3.9</code> 转成 <code>int</code> 得到 -3。</p>\n<p>**错误认识：**执行 <code>int whole = int(value);</code> 后，原来的 <code>double value</code> 也会变成整数。</p>\n<p>**错误原因：**转换表达式只产生一个新的整数值，再把它保存到 <code>whole</code>；它不会改写 <code>value</code> 的声明类型或原值。</p>\n<p>**正确处理与复演：**令 <code>value=-2.9</code>，同时输出 <code>value</code> 和 <code>whole</code>，应分别看到 -2.9 和 -2。若原变量仍能输出小数，而新变量保存截断结果，就说明转换对象和保存位置已经分清。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>隐式转换由 C++ 根据参与运算或赋值的类型自动完成，显式转换可以写成 <code>目标类型(值)</code>，也要能识别 <code>(目标类型)(值)</code>。整数除法一旦完成，小数部分就无法靠之后的转换恢复；浮点数转整数只向 0 截去小数部分。分析表达式时先按顺序算右侧，再检查结果保存到什么类型。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>ASCII 页面中的字符加减会发生字符到整数的提升；常用数学函数的返回值也可能是浮点类型，需要按本页规则处理。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 嵌套分支先走外层再决定是否进入内层；<code>else</code> 与最近的未配对 <code>if</code> 结合；<code>else if</code> 只执行第一条成立分支，<code>switch</code> 从匹配 <code>case</code> 开始并由 <code>break</code> 停止。</li>\n<li><strong>常用语法：</strong> 用大括号标清层次，并把更严格的 <code>else if</code> 条件放在前面：<code>if (...) { ... } else if (...) { ... } else { ... }</code>；固定值分支写成 <code>case value: ...; break;</code>。</li>\n<li><strong>最小示例：</strong> 分数判断先经过外层有效范围，再进入从严格到宽松的等级分支；<code>score = 90</code> 时，<code>score &gt;= 90</code> 为真，输出 A，后续条件不再检查。</li>\n<li><strong>关键易错点：</strong> 省略大括号容易误判悬空 <code>else</code>；宽条件放在严格条件前会挡住后者；<code>switch</code> 漏写 <code>break</code> 会继续落入后续 <code>case</code>。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的多层分支结构：掌握 <code>if</code>、<code>if-else</code> 和 <code>switch</code> 的嵌套或组合，能够追踪执行路径，并正确处理条件顺序、边界和 <code>break</code>。</p>\n<p>多层分支用于“先判断一层，再根据结果继续细分”的问题。读代码时先确定外层路径，再进入对应内层，不能把所有条件同时执行。</p>\n<p>本页从外层条件开始逐层追踪嵌套 <code>if</code>，理解 <code>else</code> 与最近未配对 <code>if</code> 的关系；再比较 <code>else if</code> 条件顺序和 <code>switch</code> 固定值分支，最后用 59、60、89、90 以及非法分数检查所有边界。</p>\n<p><strong>分支</strong>是根据条件只选择某条执行路径；<strong>多层分支</strong>是在已经选择的路径中继续判断。它与一串彼此独立的 <code>if</code> 不同：独立 <code>if</code> 可能有多项同时执行，<code>if ... else if ... else</code> 只进入第一个成立分支，嵌套 <code>if</code> 则要求先进入外层路径，内层判断才有机会执行。</p>\n<a id="01-nested-if"></a>\n<h2 id="01-嵌套-if-与配对规则">01 嵌套 if 与配对规则</h2>\n<p>当第二个判断只在第一个条件成立后才有意义，可以把 <code>if</code> 放进另一个 <code>if</code>：</p>\n<pre><code>if (score &gt;= 60) {  // 外层决定能否进入及格后的细分    if (score &gt;= 90) {        cout &lt;&lt; "pass and excellent" &lt;&lt; endl;    } else {  // 已及格但未达到 90        cout &lt;&lt; "pass" &lt;&lt; endl;    }} else {  // 外层为假时，内层完全不执行    cout &lt;&lt; "not pass" &lt;&lt; endl;}</code></pre>\n<p>这段语法可以拆成“外层条件、外层真分支、内层条件、内层两条路径、外层假分支”。缩进帮助阅读，但真正决定范围的是大括号。二级主代码始终保留大括号，避免仅靠缩进猜测 <code>else</code> 属于哪一层。</p>\n<p>大括号明确了层次。没有大括号时，<code>else</code> 与前面最近且尚未配对的 <code>if</code> 配对，容易看错，因此二级主代码应保留完整大括号。</p>\n<p>对输入 95、75、50 分别追踪：</p>\n<table>\n<thead>\n<tr>\n<th><code>score</code></th>\n<th>外层 <code>score&gt;=60</code></th>\n<th>内层 <code>score&gt;=90</code></th>\n<th>输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>95</td>\n<td>真，进入内层</td>\n<td>真</td>\n<td><code>pass and excellent</code></td>\n</tr>\n<tr>\n<td>75</td>\n<td>真，进入内层</td>\n<td>假</td>\n<td><code>pass</code></td>\n</tr>\n<tr>\n<td>50</td>\n<td>假，不进入内层</td>\n<td>不检查</td>\n<td><code>not pass</code></td>\n</tr>\n</tbody>\n</table>\n<p>输入 50 时，内层条件不是“假”，而是根本没有执行到。嵌套分支必须先取得外层路径，才能判断下一层。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>已经形成嵌套结构后，再看唯一完整程序。外层先把输入分成“非法”和“合法”两条路径；只有进入合法路径，内层才继续把成绩分成 A、B、C。追踪时要区分“条件为假”和“根本没有执行到”。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int score;    cin &gt;&gt; score;    if (score &lt; 0 || score &gt; 100) {  // 外层先排除非法范围        cout &lt;&lt; "invalid" &lt;&lt; endl;    } else {        if (score &gt;= 90) {  // 合法后从最严格条件开始            cout &lt;&lt; "A" &lt;&lt; endl;        } else if (score &gt;= 60) {            cout &lt;&lt; "B" &lt;&lt; endl;        } else {            cout &lt;&lt; "C" &lt;&lt; endl;        }    }    return 0;}</code></pre>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th>外层非法判断</th>\n<th>内层 <code>score&gt;=90</code></th>\n<th>内层 <code>score&gt;=60</code></th>\n<th>输出</th>\n<th>未执行分支</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>-1</td>\n<td>真</td>\n<td>不检查</td>\n<td>不检查</td>\n<td><code>invalid</code></td>\n<td>A、B、C</td>\n</tr>\n<tr>\n<td>59</td>\n<td>假，进入合法路径</td>\n<td>假</td>\n<td>假</td>\n<td><code>C</code></td>\n<td>无</td>\n</tr>\n<tr>\n<td>60</td>\n<td>假，进入合法路径</td>\n<td>假</td>\n<td>真</td>\n<td><code>B</code></td>\n<td>C</td>\n</tr>\n<tr>\n<td>89</td>\n<td>假，进入合法路径</td>\n<td>假</td>\n<td>真</td>\n<td><code>B</code></td>\n<td>C</td>\n</tr>\n<tr>\n<td>90</td>\n<td>假，进入合法路径</td>\n<td>真</td>\n<td>不检查</td>\n<td><code>A</code></td>\n<td>B、C</td>\n</tr>\n<tr>\n<td>101</td>\n<td>真</td>\n<td>不检查</td>\n<td>不检查</td>\n<td><code>invalid</code></td>\n<td>A、B、C</td>\n</tr>\n</tbody>\n</table>\n<p>例如输入 90，先得到外层非法判断为假，进入合法路径后，内层 <code>score&gt;=90</code> 才为真并输出 A。这正是“先选择外层，再执行内层”，不是一条平铺的四分支链。</p>\n<a id="02-conditions"></a>\n<h2 id="02-多条件与-else-if">02 多条件与 else if</h2>\n<p>一组互斥区间常用 <code>if ... else if ... else</code>。程序从上到下检查，遇到第一个为真的条件后执行对应分支，其余分支跳过。</p>\n<pre><code>if (score &gt;= 90) {  // 从最严格条件开始    cout &lt;&lt; "A";} else if (score &gt;= 80) {    cout &lt;&lt; "B";} else if (score &gt;= 60) {    cout &lt;&lt; "C";} else {  // 接住前面条件都不满足的剩余区间    cout &lt;&lt; "D";}</code></pre>\n<p>条件要从更严格到更宽松排列。若先写 <code>score &gt;= 60</code>，90 分也会提前进入该分支。</p>\n<p>输入 <code>score = 75</code> 时，程序按顺序追踪如下：</p>\n<table>\n<thead>\n<tr>\n<th>检查条件</th>\n<th>结果</th>\n<th>下一步</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>score &gt;= 90</code></td>\n<td>假</td>\n<td>继续检查下一项</td>\n</tr>\n<tr>\n<td><code>score &gt;= 80</code></td>\n<td>假</td>\n<td>继续检查下一项</td>\n</tr>\n<tr>\n<td><code>score &gt;= 60</code></td>\n<td>真</td>\n<td>输出 <code>C</code>，停止检查</td>\n</tr>\n</tbody>\n</table>\n<p>第三个条件成立后，后面的 <code>else</code> 不再执行。<code>else if</code> 链只会进入第一个满足条件的分支。</p>\n<a id="03-switch"></a>\n<h2 id="03-switchcase-与-break">03 switch、case 与 break</h2>\n<p>当一个整数或字符要与若干固定值比较时，可以使用 <code>switch</code>：</p>\n<pre><code>switch (month) {  // 从匹配的固定 case 位置开始执行    case 2:        cout &lt;&lt; "February";        break;  // 离开 switch，防止继续贯穿    case 4:        cout &lt;&lt; "April";        break;    default:  // 没有 case 匹配时进入        cout &lt;&lt; "other";}</code></pre>\n<p><code>switch (month)</code> 中圆括号里的内容叫<strong>选择表达式</strong>，它通常得到整数或字符值；<code>case 2</code>、<code>case 4</code> 是互不重复的固定常量入口；<code>default</code> 是没有匹配项时的可选入口。<code>case</code> 只决定“从哪里开始”，不会自动决定“在哪里停止”，所以通常需要 <code>break</code>。</p>\n<p>匹配某个 <code>case</code> 后从该位置继续执行，直到遇到 <code>break</code> 或离开 <code>switch</code>。遗漏 <code>break</code> 会继续执行后面的 <code>case</code>，叫作贯穿。题目可能专门考查贯穿，但日常代码若不需要贯穿，应写清 <code>break</code>。</p>\n<h3 id="if-与-switch-组合"><code>if</code> 与 <code>switch</code> 组合</h3>\n<p>当一个范围条件成立后，还要比较若干固定值，可以先用 <code>if</code>，再在它内部使用 <code>switch</code>：</p>\n<pre><code>if (type == 2) {  // 外层决定内层 switch 是否执行    switch (month) {        case 6:            cout &lt;&lt; "summer";            break;        case 12:            cout &lt;&lt; "winter";            break;        default:            cout &lt;&lt; "other month";    }} else {    cout &lt;&lt; "other type";}</code></pre>\n<p>输入 <code>2 6</code> 时，先检查 <code>type==2</code> 为真，才进入 <code>switch</code>；<code>month</code> 匹配 <code>case 6</code>，输出 <code>summer</code>，随后 <code>break</code> 只结束 <code>switch</code>。输入 <code>1 6</code> 时外层条件为假，整个 <code>switch</code> 都不执行，输出 <code>other type</code>。<code>if</code> 和 <code>switch</code> 的大括号都不能省略，否则层次和作用范围会难以辨认。</p>\n<p>反过来，也可以先由 <code>switch</code> 选中固定类别，再在某个 <code>case</code> 内用 <code>if</code> 判断范围：</p>\n<pre><code>switch (type) {  // 外层先选择固定类别    case 1:        if (score &gt;= 60) {  // case 内再判断范围            cout &lt;&lt; "pass";        } else {            cout &lt;&lt; "not pass";        }        break;  // 离开外层 switch    default:        cout &lt;&lt; "other type";}</code></pre>\n<p>两个 <code>switch</code> 也可以嵌套。下面先选组别，再在组别 1 内选择代码：</p>\n<pre><code>switch (group) {    case 1:        switch (code) {  // 只有外层选中组别 1 才进入            case 2:                cout &lt;&lt; "A";                break;  // 只结束最近的内层 switch            default:                cout &lt;&lt; "B";                break;        }        break;  // 再结束外层 switch    default:        cout &lt;&lt; "other";        break;}</code></pre>\n<p>输入 <code>group=1、code=2</code> 时，内层 <code>break</code> 只离开内层 <code>switch</code>，随后仍会执行外层 <code>case 1</code> 末尾的第二个 <code>break</code>。两层 <code>break</code> 各管最近的一层，不能用一个内层 <code>break</code> 同时结束两层选择。</p>\n<p>无论是哪一种组合，都先执行外层选择，再看是否能到达内层。<code>break</code> 在上例中结束的是它所在的 <code>switch</code>，不会像 <code>return</code> 那样直接结束整个程序。</p>\n<a id="04-boundary"></a>\n<h2 id="04-条件顺序与边界">04 条件顺序与边界</h2>\n<p>边界值要逐一代入。例如“0 到 100 分”通常包含 0 和 100；区间 <code>80—89</code> 可写 <code>score &gt;= 80 &amp;&amp; score &lt; 90</code>。多个条件组合时先确认每个比较，再看 <code>&amp;&amp;</code> 或 <code>||</code>。</p>\n<p>处理年份、票价、分段收费等问题时，先在纸上列出互不重叠的区间，再按顺序写分支。这样能避免一个值同时符合多段或没有任何分支接住。</p>\n<p>例如规则是“90 分及以上优秀，60—89 分及格，其余不及格”，把 59、60、89、90 逐个代入：</p>\n<table>\n<thead>\n<tr>\n<th>分数</th>\n<th><code>&gt;=90</code></th>\n<th><code>&gt;=60</code></th>\n<th>结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>59</td>\n<td>假</td>\n<td>假</td>\n<td>不及格</td>\n</tr>\n<tr>\n<td>60</td>\n<td>假</td>\n<td>真</td>\n<td>及格</td>\n</tr>\n<tr>\n<td>89</td>\n<td>假</td>\n<td>真</td>\n<td>及格</td>\n</tr>\n<tr>\n<td>90</td>\n<td>真</td>\n<td>不再检查</td>\n<td>优秀</td>\n</tr>\n</tbody>\n</table>\n<p>如果第二个条件误写成 <code>score&gt;60</code>，60 会掉进“不及格”；若优秀条件误写成 <code>score&gt;90</code>，90 会被分到“及格”。边界题必须实际代入等号两侧检查。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>场景</th>\n<th>常用结构</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>条件成立后继续细分</td>\n<td>嵌套 <code>if</code></td>\n</tr>\n<tr>\n<td>多个互斥区间</td>\n<td><code>if ... else if ... else</code></td>\n</tr>\n<tr>\n<td>与多个固定值比较</td>\n<td><code>switch</code></td>\n</tr>\n<tr>\n<td>防止 case 继续向下执行</td>\n<td><code>break</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**一个 <code>switch</code> 在 <code>case 1</code> 输出 <code>A</code> 后没有 <code>break</code>，紧接的 <code>case 2</code> 输出 <code>B</code> 后才 <code>break</code>。输入 1 时输出什么？</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li><code>switch</code> 的值为 1，从 <code>case 1</code> 开始执行；</li>\n<li>先输出 <code>A</code>；</li>\n<li>当前分支末尾没有 <code>break</code>，所以不会重新匹配，而是直接继续向下执行；</li>\n<li>到达 <code>case 2</code> 的语句并输出 <code>B</code>；</li>\n<li>随后遇到 <code>break</code>，离开 <code>switch</code>。</li>\n</ol>\n<p>**结论：**连续输出 <code>AB</code>。<code>case</code> 负责确定开始位置，<code>break</code> 才负责在需要的位置停止。</p>\n<p>**引导练习：**把合法成绩分成“90—100 为 A、60—89 为 B、0—59 为 C”，范围外输出 <code>invalid</code>。请先在纸上写外层合法性条件，再写内层两个条件，并用 <code>-1、59、60、89、90、101</code> 逐个走路径。提示：外层只负责合法/非法，内层从严格条件向宽条件检查。核对输出依次应为 <code>invalid、C、B、B、A、invalid</code>，而且 <code>-1</code> 和 <code>101</code> 都不应执行任何内层条件。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>忽略外层条件，直接进入内层分支；</li>\n<li>不写大括号，误判 <code>else</code> 的配对对象；</li>\n<li>把宽条件放在严格条件之前；</li>\n<li>忘记 <code>switch</code> 中的 <code>break</code>；</li>\n<li>用 <code>=</code> 代替 <code>==</code>，或遗漏边界等号。</li>\n</ul>\n<p>**错误写法：**先写 <code>if (score &gt;= 60)</code>，后写 <code>else if (score &gt;= 90)</code>。</p>\n<p>**错误原因：**90 分先满足较宽的 <code>score &gt;= 60</code>，后面的优秀分支永远没有机会执行。</p>\n<p>**正确写法：**先判断 <code>score &gt;= 90</code>，再判断 <code>score &gt;= 60</code>，最后用 <code>else</code> 接住不及格情况。</p>\n<p><strong>错误写法：</strong><code>if (score = 90)</code></p>\n<p>**错误原因：**单个 <code>=</code> 是赋值，不是判断相等；它还会修改 <code>score</code>。</p>\n<p>**正确写法：**相等比较使用 <code>if (score == 90)</code>。若判断分数段，使用 <code>score &gt;= 90</code>。</p>\n<p>**修正后复演：**假设输入原本是 50。错误写法 <code>score = 90</code> 会先把变量改成 90，条件又因非零而成立；改成 <code>score == 90</code> 后，50 与 90 比较为假，<code>score</code> 仍保持 50。检查“变量值没有被条件修改”就是修正成功的标准。</p>\n<p><code>switch</code> 中也要逐个检查 <code>case</code> 后是否需要 <code>break</code>；只有题目明确要求贯穿时才故意省略。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>多层分支先走外层，再决定是否进入内层；没有走到的条件属于未执行分支。<code>else if</code> 从上到下只进入第一个成立分支，因此严格条件应放在宽松条件之前。<code>switch</code> 从匹配的 <code>case</code> 开始，通常用 <code>break</code> 停止；所有结构都要用边界值逐一验证。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>流程图可以把多层条件画成清晰路径；多层循环中也会在循环体内使用分支控制计数、<code>break</code> 和 <code>continue</code>。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 外层控制组或行，内层完成每组的重复动作；每轮外层开始都要重置内层状态，总次数按各轮实际次数求和，并补上内外层最后一次失败判断；<code>break</code> 和 <code>continue</code> 只影响最近一层。</li>\n<li><strong>常用语法：</strong> 双层循环先认清每层变量、初值、条件和更新：外层 <code>for (int i = 1; i &lt;= rows; i++)</code> 控制行，内层 <code>for (int j = 1; j &lt;= columns; j++)</code> 完成每行。</li>\n<li><strong>最小示例：</strong> 固定两行、每行三次时共有六次输出；每行到 <code>j = 4</code> 时内层判断为假，最后到 <code>i = 3</code> 时外层判断为假。</li>\n<li><strong>关键易错点：</strong> 不要把内层初值放到外层之外；内层次数变化时不能直接相乘；不要把内层 <code>break</code> 当成结束所有循环；<code>while</code> 中的 <code>continue</code> 不能跳过必要更新。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的多层循环结构：掌握 <code>for</code>、<code>while</code>、<code>do-while</code> 的嵌套形式，能够计算执行次数、输出图形或数列，并理解内层 <code>break</code>、<code>continue</code> 的影响。</p>\n<p>循环嵌套就是在一个循环体中再放循环。外层通常控制“做几组”，内层控制“每组做几次”；总次数必须按每一轮内层实际次数求和。</p>\n<p>本页先用矩形输出理解“外层一轮，内层完整执行一遍”，再追踪内层变量重新初始化、最后一次失败判断，以及 <code>break</code>、<code>continue</code> 只影响最近一层循环的规则；最后把行列、字符周期和数位处理放回同一套过程表中检查。</p>\n<p><strong>循环嵌套</strong>是把一个完整循环写在另一个循环的循环体里。外层每进入一轮，内层都要从自己的初始化开始，经历判断、执行、更新，直到内层结束，控制权才回到外层。判断代码属于哪一层时看大括号范围，不能只看变量名或缩进。</p>\n<a id="01-forms"></a>\n<h2 id="01-嵌套循环的基本形式">01 嵌套循环的基本形式</h2>\n<p>最常见的是双重 <code>for</code>。下面外层控制 3 行，内层在第 <code>i</code> 行输出 <code>i</code> 个数字：</p>\n<pre><code>for (int i = 1; i &lt;= 3; i++) {  // 外层控制行号    for (int j = 1; j &lt;= i; j++) {  // 第 i 行输出 i 次        cout &lt;&lt; j;    }    cout &lt;&lt; endl;}</code></pre>\n<p>输出为 <code>1</code>、<code>12</code>、<code>123</code> 三行。内层每完成一次，外层才进入下一轮；进入新的外层轮次时，内层循环变量重新初始化。</p>\n<p>观察下面这个固定执行 2 行、每行 3 次的双层循环，最容易看清“外层一次，内层完整执行一次”：</p>\n<table>\n<thead>\n<tr>\n<th>i</th>\n<th>j</th>\n<th>当前是第几行、第几列</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td>1</td>\n<td>第 1 行第 1 列</td>\n</tr>\n<tr>\n<td>1</td>\n<td>2</td>\n<td>第 1 行第 2 列</td>\n</tr>\n<tr>\n<td>1</td>\n<td>3</td>\n<td>第 1 行第 3 列</td>\n</tr>\n<tr>\n<td>2</td>\n<td>1</td>\n<td>第 2 行第 1 列</td>\n</tr>\n<tr>\n<td>2</td>\n<td>2</td>\n<td>第 2 行第 2 列</td>\n</tr>\n<tr>\n<td>2</td>\n<td>3</td>\n<td>第 2 行第 3 列</td>\n</tr>\n</tbody>\n</table>\n<p>当 <code>i=1</code> 时，<code>j</code> 要从 1 完整走到 3；之后外层才令 <code>i=2</code>，内层的 <code>j</code> 又从 1 开始。</p>\n<p><code>for</code> 和 <code>while</code> 也可以混合嵌套。下面外层 <code>for</code> 控制两行，内层 <code>while</code> 每行输出三个星号：</p>\n<pre><code>for (int i = 1; i &lt;= 2; i++) {    int j = 1;  // 每行开始时重置内层变量    while (j &lt;= 3) {        cout &lt;&lt; \'*\';        j++;  // 内层循环的更新    }    cout &lt;&lt; endl;}</code></pre>\n<p><code>while</code> 嵌套时，每进入一轮外层循环，也要重新设置内层循环的初值。若把 <code>int j = 1;</code> 放到外层循环之前，第一行结束后 <code>j</code> 已经变成 4，第二行将不会进入内层循环。</p>\n<p>两层都使用 <code>while</code> 时，初始化和更新不会集中写在循环头里，更要逐项检查：</p>\n<pre><code>int row = 1;while (row &lt;= 2) {    int column = 1;  // 每行重新初始化列变量    while (column &lt;= 3) {        cout &lt;&lt; \'*\';        column++;  // 更新内层变量    }    cout &lt;&lt; endl;    row++;  // 更新外层变量}</code></pre>\n<p>这段代码中有两次初始化和两次更新：<code>row</code> 只在整个结构开始前初始化一次，<code>column</code> 却在每轮外层开始时重新初始化；漏掉任意一层的更新都可能造成该层无法结束。</p>\n<p><code>do-while</code> 嵌套同样遵守“外层一轮，内层完整执行”的规则，而且每层循环体都至少执行一次：</p>\n<pre><code>int i = 1;do {    int j = 1;  // 每轮外层开始时重置内层变量    do {        cout &lt;&lt; j;        j++;    } while (j &lt;= 3);  // 内层末尾判断    cout &lt;&lt; endl;    i++;} while (i &lt;= 2);  // 外层末尾判断</code></pre>\n<p>程序输出两行 <code>123</code>。读嵌套 <code>do-while</code> 时要分别找到内层和外层末尾的条件，不能把两个 <code>while</code> 误认为同一层。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>已经看过双重 <code>for</code>、混合嵌套、双重 <code>while</code> 和双重 <code>do-while</code> 的基本形式，再把最常用的双重 <code>for</code> 放进唯一完整程序。程序输入行数、列数和一个字符，输出对应矩形；外层每轮开始时，内层变量 <code>column</code> 都重新从 1 开始。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int rows, columns;    char mark;    cin &gt;&gt; rows &gt;&gt; columns &gt;&gt; mark;    for (int row = 1; row &lt;= rows; row++) {  // 外层控制行        for (int column = 1; column &lt;= columns; column++) {  // 每行都从第 1 列开始            cout &lt;&lt; mark;        }        cout &lt;&lt; endl;    }    return 0;}</code></pre>\n<p>输入 <code>2 3 #</code> 时，内层判断的完整轨迹是：第 1 行令 <code>column</code> 依次为 1、2、3 并输出三个 <code>#</code>，随后 <code>column=4</code> 的最后一次判断为假；第 2 行重新初始化 <code>column=1</code>，再次到 <code>column=4</code> 为假。外层完成第 2 行后，<code>row=3</code> 的最后一次判断为假，程序结束。</p>\n<p>**跟着做：先画轨迹表，再预测输出。**把输入改成 <code>2 3 @</code>，不要先运行程序。先写出第 1 行的三组 <code>(row, column)</code>，再写出第 2 行，并补上内层 <code>column=4</code> 与外层 <code>row=3</code> 的失败判断。提示：进入第 2 行时，<code>column</code> 会重新从 1 开始。你的表中应有 6 次输出，程序结果应是两行 <code>@@@</code>；实际运行后，若少了一行，就检查内层变量是否在每轮外层开始时重新建立。</p>\n<a id="02-count"></a>\n<h2 id="02-循环次数与行列角色">02 循环次数与行列角色</h2>\n<p>若外层每轮内层次数相同，总次数可以相乘；若内层次数依赖 <code>i</code>，要逐轮相加。例如第 1 到第 4 行分别执行 1、2、3、4 次，总次数是 <code>1+2+3+4=10</code>。</p>\n<p>更通用的计算写法是：<strong>总次数 = 每一轮外层对应的内层次数之和</strong>。当每轮内层都执行 <code>m</code> 次、外层执行 <code>n</code> 次时，这个和才可以简写成 <code>n × m</code>。例如 3 行每行 4 次是 <code>4+4+4=12</code>；三角形四行则是 <code>1+2+3+4=10</code>，不能误算成 <code>4×4</code>。</p>\n<p>网格题常用 <code>i</code> 表示行、<code>j</code> 表示列。本站主教学默认使用 1 下标：行列都从 1 到 <code>N</code> 时，主对角线满足 <code>i==j</code>，副对角线满足 <code>i+j==N+1</code>。例如 <code>N=3</code> 时，副对角线坐标依次是 <code>(1,3)、(2,2)、(3,1)</code>，三组坐标的和都是 4。</p>\n<p>官方题若明确从 0 开始，则行列范围改为 0 到 <code>N-1</code>，副对角线公式相应变为 <code>i+j==N-1</code>。这属于题目明确给定的坐标约定，不应反过来改变本站网格主教学的 1 下标写法。</p>\n<a id="03-jump"></a>\n<h2 id="03-break-与-continue">03 break 与 continue</h2>\n<p><code>break</code> 只结束它所在的最近一层循环。内层遇到 <code>break</code> 后，外层仍可进入下一轮。<code>continue</code> 跳过当前这一轮剩余语句，进入该层循环的下一轮。</p>\n<pre><code>for (int i = 1; i &lt;= 3; i++) {    for (int j = 1; j &lt;= 5; j++) {        if (j == 3) {            break;  // 只结束最近的内层循环        }        cout &lt;&lt; j;    }    cout &lt;&lt; endl;}</code></pre>\n<p>每行输出 <code>12</code>，共三行。<code>break</code> 没有直接结束外层循环。</p>\n<p><code>continue</code> 逐轮追踪可以看下面的内层循环：</p>\n<pre><code>for (int j = 1; j &lt;= 5; j++) {    if (j == 3) {        continue;  // 跳过本轮 cout，随后仍执行 for 的更新    }    cout &lt;&lt; j;}cout &lt;&lt; endl;</code></pre>\n<p><code>break</code> 和 <code>continue</code> 都作用于它们所在的<strong>最近一层循环</strong>：<code>break</code> 直接离开该层；<code>continue</code> 只跳过该层当前轮剩余语句。在 <code>for</code> 中执行 <code>continue</code> 后仍会先执行更新部分，再回到条件判断；在 <code>while</code> 中若把必要的更新写在 <code>continue</code> 后面，则更新可能被跳过，必须特别检查是否会形成死循环。</p>\n<table>\n<thead>\n<tr>\n<th><code>j</code></th>\n<th><code>j==3</code></th>\n<th>本轮动作</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td>假</td>\n<td>输出 1</td>\n</tr>\n<tr>\n<td>2</td>\n<td>假</td>\n<td>输出 2</td>\n</tr>\n<tr>\n<td>3</td>\n<td>真</td>\n<td>跳过 <code>cout</code>，进入下一轮</td>\n</tr>\n<tr>\n<td>4</td>\n<td>假</td>\n<td>输出 4</td>\n</tr>\n<tr>\n<td>5</td>\n<td>假</td>\n<td>输出 5</td>\n</tr>\n</tbody>\n</table>\n<p>最终输出 <code>1245</code>。<code>continue</code> 不会结束循环，也不会把 <code>j</code> 永远停在 3；在 <code>for</code> 中会先执行更新部分 <code>j++</code>，再开始下一次条件判断。</p>\n<p>官方真题少见写法 <code>if (i % 5) break;</code> 把非零整数直接当作真。它等价于更直白的 <code>if (i % 5 != 0) break;</code>。二级主教学优先写完整比较，读官方题时再把余数代入 0 或非 0 判断。</p>\n<a id="04-character"></a>\n<h2 id="04-嵌套循环中的字符与数位">04 嵌套循环中的字符与数位</h2>\n<p>取余可以生成周期。例如 <code>j % 3</code> 依次产生 0、1、2，可配合字符编码输出 <code>X、Y、Z</code> 的重复序列。写成直白形式更适合二级阅读：</p>\n<pre><code>for (int row = 1; row &lt;= 2; row++) {    for (int j = 0; j &lt; 6; j++) {        char letter = \'X\' + j % 3;  // 余数 0、1、2 映射为 X、Y、Z        cout &lt;&lt; letter;    }    cout &lt;&lt; endl;}</code></pre>\n<p>外层循环执行 2 行，内层每行按余数 <code>0、1、2、0、1、2</code> 输出 <code>XYZXYZ</code>。字符周期由内层产生，换行由外层每轮末尾产生。</p>\n<p>数位处理中，<code>n % 10</code> 取得个位，<code>n /= 10</code> 去掉个位。若外层处理多个整数，内层 <code>while</code> 可以逐位处理每个数；每个数开始前要重新设置计数或备份变量。例如依次处理 12 和 305 时，12 的数位轨迹是 <code>12→1→0</code>，305 的轨迹必须从 305 重新开始，不能沿用前一个数结束后的 0。</p>\n<a id="05-application"></a>\n<h2 id="05-综合循环应用">05 综合循环应用</h2>\n<h3 id="九九乘法表内层上界跟随行号">九九乘法表：内层上界跟随行号</h3>\n<pre><code>for (int i = 1; i &lt;= 9; i++) {    for (int j = 1; j &lt;= i; j++) {  // 第 i 行输出 i 项        cout &lt;&lt; j &lt;&lt; "*" &lt;&lt; i &lt;&lt; "=" &lt;&lt; i * j &lt;&lt; " ";    }    cout &lt;&lt; endl;}</code></pre>\n<p>第 1 到第 9 行分别输出 1、2、3、……、9 项，总项数是 <code>1+2+...+9=45</code>，不是 <code>9×9</code>。每次外层进入新行，内层 <code>j</code> 都重新从 1 开始。</p>\n<h3 id="多组数据每组重新统计">多组数据：每组重新统计</h3>\n<pre><code>for (int group = 1; group &lt;= t; group++) {    int n;    cin &gt;&gt; n;    int count = 0;  // 每组开始时重新统计    for (int i = 1; i &lt;= n; i++) {        int value;        cin &gt;&gt; value;        if (value % 2 == 0) {            count++;        }    }    cout &lt;&lt; count &lt;&lt; endl;}</code></pre>\n<p><code>count</code> 必须定义在外层循环体内，因此每组开始都会重新变成 0。若把它移到外层循环之前而又不手动清零，第二组输出会包含第一组已经统计的偶数，得到的是累计结果而不是“本组结果”。</p>\n<p>判断回文数时，逐位反转会不断修改 <code>n</code>，所以要先保存原值：</p>\n<p>下面的局部示例约定 <code>n</code> 是非负整数，并且反转后的值仍在 <code>int</code> 可表示范围内；真实题目必须先查看输入范围。</p>\n<pre><code>int original = n;  // 保存原数，n 作为工作副本int reversed = 0;while (n &gt; 0) {    reversed = reversed * 10 + n % 10;    n /= 10;}if (original == reversed) {    cout &lt;&lt; "yes";} else {    cout &lt;&lt; "no";}</code></pre>\n<p>枚举多个数并判断是否符合条件时，外层负责枚举候选，内层负责检查。内层一旦找到反例，可直接 <code>break</code>，不要继续做无用检查。</p>\n<p>例如要依次判断 121、123 是否为回文数，外层负责读入两个候选，内层分别反转。121 的中间反转值是 <code>1→12→121</code>，比较后为回文；123 的中间反转值是 <code>3→32→321</code>，比较后不是回文。每次进入内层前都要重新设置 <code>original</code> 和 <code>reversed=0</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>检查顺序</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>总执行次数</td>\n<td>写出每轮外层对应的内层次数再求和</td>\n</tr>\n<tr>\n<td>图形输出</td>\n<td>外层看行，内层看每行元素</td>\n</tr>\n<tr>\n<td><code>break</code> 影响</td>\n<td>只结束最近一层循环</td>\n</tr>\n<tr>\n<td><code>continue</code> 影响</td>\n<td>跳过当前层当前轮剩余语句</td>\n</tr>\n<tr>\n<td>数位处理</td>\n<td>先备份，再 <code>%10</code> 与 <code>/=10</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**输入 3 和 4，输出 3 行、每行 4 个星号。</p>\n<p>**推演：**外层循环控制 3 行；每一轮外层开始后，内层 <code>j</code> 都从 1 重新走到 4，共输出 4 个星号；内层结束后再换行。三轮内层次数是 <code>4+4+4=12</code>。</p>\n<pre><code>for (int i = 1; i &lt;= rows; i++) {  // 外层控制行    for (int j = 1; j &lt;= columns; j++) {  // 内层控制列        cout &lt;&lt; \'*\';    }    cout &lt;&lt; endl;}</code></pre>\n<p>输入：</p>\n<pre><code>3 4</code></pre>\n<p>输出：</p>\n<pre><code>************</code></pre>\n<p>**结论：**输出是 3×4 的星号矩形，共 12 个星号。固定内层次数时总次数可以相乘；若内层上界依赖 <code>i</code>，则应逐轮相加。</p>\n<p>另一个常见追踪是：外层 <code>i</code> 从 1 到 4，内层 <code>j</code> 从 1 到 <code>i</code>，连续输出 <code>j</code>。四轮分别输出 <code>1</code>、<code>12</code>、<code>123</code>、<code>1234</code>，连接后得到 <code>1121231234</code>。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>直接把外层次数和内层上界相乘，忽略内层次数会变化；</li>\n<li>忘记内层变量每轮重新初始化；</li>\n<li>认为内层 <code>break</code> 会结束所有循环；</li>\n<li><code>continue</code> 前后的累计顺序判断错误；</li>\n<li>反转数字后忘记原变量已变为 0。</li>\n</ul>\n<p>**错误写法：**把 <code>int j = 1;</code> 放在外层 <code>while</code> 之前，第一行结束后不再把 <code>j</code> 设回 1。</p>\n<p>**错误原因：**第一轮结束时 <code>j</code> 已经超过上界，后续外层轮次不会再次进入内层循环。</p>\n<p>**正确写法：**每次进入新的外层轮次时，在内层循环之前重新设置 <code>j = 1</code>。</p>\n<p>**错误写法：**内层循环条件误写成 <code>j &lt;= rows</code>，输出矩形时把行数当成列数。</p>\n<p>**错误原因：**外层变量和内层上界角色混乱。外层控制行，内层应按列数执行。</p>\n<p>**正确写法：**外层使用 <code>i &lt;= rows</code>，内层使用 <code>j &lt;= columns</code>；再用 1×1、单行和单列检查边界。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>外层决定有多少组或多少行，内层决定每组做多少次；每轮外层开始时都要重新建立内层初值。计算次数要逐轮记录，过程表必须包含内层和外层的最后一次失败判断。<code>break</code> 只结束最近一层循环，<code>continue</code> 只跳过当前层当前轮的剩余语句。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>多层分支决定循环体内哪些操作执行；ASCII 页面解释字符周期输出；类型转换页面帮助判断整数除法和复合赋值结果。</p>',
        '<div data-gesp-level2-ui data-gesp-level2-detail></div>\n<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> <code>abs</code>、<code>sqrt</code>、<code>min</code>、<code>max</code> 要核对参数范围与返回结果；<code>rand() % n</code> 的范围是 0 到 <code>n - 1</code>，<code>srand</code> 只设置序列起点，通常在一组取数开始前调用一次。</li>\n<li><strong>常用语法：</strong> 先选函数，再检查参数、返回类型和随机范围：<code>abs(x)</code>、<code>sqrt(x)</code>、<code>min(a, b)</code> / <code>max(a, b)</code>、<code>srand(seed)</code>、<code>rand() % n + start</code>。</li>\n<li><strong>最小示例：</strong> 确定随机范围时，先列出取余结果，再整体平移：<code>abs(-6) = 6</code>，<code>sqrt(49.0) = 7.0</code>，<code>rand() % 5 + 3</code> 的范围为 <code>[3, 7]</code>。</li>\n<li><strong>关键易错点：</strong> 不要对负实数直接求 <code>sqrt</code>；不要把 <code>sqrt</code> 结果一概当成 <code>int</code>；<code>% n</code> 从 0 开始；不要在每次 <code>rand</code> 前用同一种子重新 <code>srand</code>。</li>\n</ul>\n<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>本页对应二级大纲中的常用数学函数：掌握 <code>abs</code>、<code>sqrt</code>、<code>max</code>、<code>min</code> 的基本用途，并认识 <code>rand</code>、<code>srand</code> 以及简单随机整数范围的构造。</p>\n<p>数学函数把常见计算封装成标准写法。使用前先明确函数需要什么参数、返回什么类型，再把结果放入后续表达式。</p>\n<p>本页用 <code>abs</code> 求距离，用 <code>sqrt</code> 求非负数的算术平方根，用 <code>min</code>、<code>max</code> 比较两个值；再从取余范围推导 <code>rand()</code> 生成的整数区间，并理解 <code>srand</code> 只设置伪随机序列的起点。</p>\n<p><strong>函数</strong>是一段已经封装好的操作。调用表达式 <code>sqrt(value)</code> 可以拆成三部分：<code>sqrt</code> 是函数名，圆括号里的 <code>value</code> 是传入的实参，函数计算后产生一个返回值。返回值可以直接输出、参与更大的表达式，也可以先保存到类型合适的变量中；调用函数不会因为名字像数学公式就自动忽略参数范围和返回类型。</p>\n<a id="01-abs"></a>\n<h2 id="01-abs-绝对值函数">01 abs 绝对值函数</h2>\n<p><code>abs(x)</code> 返回 <code>x</code> 的绝对值，也就是它到 0 的距离。<code>abs(-15)</code> 为 15，<code>abs(8)</code> 为 8。</p>\n<p>在整数主教学中，参数和返回值都按整数理解：负数去掉负号，正数和 0 保持不变。绝对值不是“总把符号改成正号”，因为 <code>abs(0)</code> 仍是 0；做减法距离题时还要确保参与计算的整数没有超出题目给定范围。</p>\n<pre><code>int difference = 12 - 20;cout &lt;&lt; abs(difference) &lt;&lt; endl;  // 取到 0 的距离</code></pre>\n<p>输出 8。求两个整数的距离时，先相减再取绝对值即可。</p>\n<p>例如输入 <code>-9 25</code>，如果要算两点在数轴上的距离，先求 <code>-9-25=-34</code>，再计算 <code>abs(-34)=34</code>。<code>abs</code> 接收的是括号内表达式已经算出的结果，不会分别处理两个数。</p>\n<a id="02-sqrt"></a>\n<h2 id="02-sqrt-平方根函数">02 sqrt 平方根函数</h2>\n<p><code>sqrt(x)</code> 计算非负数 <code>x</code> 的算术平方根，返回浮点类型。<code>sqrt(49.0)</code> 的数值为 7，但结果类型不是 <code>int</code>。</p>\n<pre><code>double side = sqrt(81.0);  // sqrt 返回浮点结果cout &lt;&lt; side &lt;&lt; endl;</code></pre>\n<p><code>sqrt</code> 的实数参数必须满足 <code>x &gt;= 0</code>。它返回浮点值，即使 <code>sqrt(81.0)</code> 数值恰好是 9，也不应据此把所有平方根结果都当作整数；例如 <code>sqrt(2.0)</code> 是不能用有限小数精确写完的近似值。</p>\n<p>实数范围内不能对负数求平方根。若结果要保存到整数变量，还要考虑类型转换和可能的浮点误差，不能默认所有平方根都是整数。</p>\n<a id="03-minmax"></a>\n<h2 id="03-max-与-min">03 max 与 min</h2>\n<p><code>max(a, b)</code> 返回两者中的较大值，<code>min(a, b)</code> 返回较小值。两个参数应使用容易比较的相同类型。</p>\n<pre><code>int a = 8;int b = 3;int c = 6;cout &lt;&lt; min(max(a, b), c) &lt;&lt; endl;  // 先求 max(8,3)=8，再求 min(8,6)=6</code></pre>\n<p><code>max(a, b)</code> 和 <code>min(a, b)</code> 都接收两个可比较的同类值，并返回其中一个值；它们不会自动修改 <code>a</code> 或 <code>b</code>。需要比较三个数时，可以嵌套调用，但每次仍只比较两个，例如 <code>max(max(a, b), c)</code>，计算顺序从最内层开始。</p>\n<p>这里的中间结果可以写成：</p>\n<ol>\n<li>先进入最内层 <code>max(a,b)</code>，得到 <code>max(8,3)=8</code>；</li>\n<li>外层表达式变成 <code>min(8,c)</code>；</li>\n<li>代入 <code>c=6</code>，得到 <code>min(8,6)=6</code>。</li>\n</ol>\n<p>如果误从外层开始猜答案，就容易把三个数中的最大值 8 当作最终结果。嵌套调用和带括号的普通表达式一样，从最里面开始。</p>\n<h2 id="过程演示">过程演示</h2>\n<p>现在已经分别形成了 <code>abs</code>、<code>sqrt</code>、<code>min</code> 和 <code>max</code> 的参数、返回值与边界，再把它们放进唯一一份完整程序。程序读取两个整数和一个非负实数，依次输出第一个整数的绝对值、两整数的较小值与较大值、实数的算术平方根。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int a, b;    double value;    cin &gt;&gt; a &gt;&gt; b &gt;&gt; value;    cout &lt;&lt; abs(a) &lt;&lt; endl;    cout &lt;&lt; min(a, b) &lt;&lt; " " &lt;&lt; max(a, b) &lt;&lt; endl;    cout &lt;&lt; fixed &lt;&lt; setprecision(1) &lt;&lt; sqrt(value) &lt;&lt; endl;  // 平方根按一位小数显示    return 0;}</code></pre>\n<p>输入 <code>-7 3 81</code> 时，<code>abs(-7)=7</code>；<code>min(-7,3)=-7</code>、<code>max(-7,3)=3</code>；<code>sqrt(81)=9</code>，按一位小数输出为 <code>9.0</code>。每一行都先算函数参数，再使用返回值。<code>fixed &lt;&lt; setprecision(1)</code> 只负责把浮点结果显示为一位小数，不改变 <code>sqrt</code> 返回 <code>double</code> 的事实。</p>\n<p>**跟着做：先选函数，再核对结果。**设 <code>a=-6</code>、<code>b=4</code>、<code>value=49</code>，请分别写出“<code>a</code> 到 0 的距离”“<code>a</code>、<code>b</code> 中的较大值”和 <code>value</code> 的算术平方根所用表达式，并计算结果。然后不用运行代码，推导 <code>rand() % 5 + 3</code> 的最小值和最大值。提示：先列出 <code>% 5</code> 的余数范围。你的四项结果应依次为 6、4、7.0，以及闭区间 <code>[3, 7]</code>；若随机范围从 4 开始，回看余数是否漏掉了 0。</p>\n<a id="04-random"></a>\n<h2 id="04-rand-与-srand">04 rand 与 srand</h2>\n<p><code>rand()</code> 产生伪随机整数。<code>rand() % 6</code> 的余数范围是 0 到 5，所以 <code>rand() % 6 + 1</code> 的范围是 1 到 6。</p>\n<p><code>srand(seed)</code> 设置伪随机序列的种子。相同种子通常产生相同序列；种子不同，序列通常不同。<code>srand</code> 不负责直接产生随机数，也不改变取余表达式规定的范围。</p>\n<pre><code>srand(7);  // 一组取数前只设置一次种子int dice = rand() % 6 + 1;  // 把 0 到 5 平移到 1 到 6cout &lt;&lt; dice &lt;&lt; endl;</code></pre>\n<p>输出的具体数值依赖实现，但一定在 1 到 6 之间。二级题目重点是范围计算，不把伪随机数当作真正不可预测的随机数。</p>\n<p>范围推导不要跳步：<code>rand()</code> 返回的非负整数除以 6，余数只可能是 <code>0、1、2、3、4、5</code>，所以 <code>rand() % 6</code> 只能落在 0 到 5；每个可能值再加 1，就变为 <code>1、2、3、4、5、6</code>。加 1 是整体平移，不会多出 7。</p>\n<p>构造闭区间 <code>[left, right]</code> 时，可以先数清区间内有多少个整数：<code>count = right-left+1</code>，再写成 <code>rand() % count + left</code>。例如 5 到 9 有 <code>9-5+1=5</code> 个整数，所以使用 <code>rand()%5+5</code>。这个公式要求 <code>right &gt;= left</code>，且 <code>count</code> 必须大于 0；如果范围端点本身不合法，就不能直接套公式。</p>\n<p>若要连续模拟多次掷骰子，通常只在循环开始前设置一次种子，再在循环内重复调用 <code>rand()</code>。如果每轮都用同一个种子重新执行 <code>srand(7)</code>，伪随机序列会从同一位置重新开始，反而可能反复得到同样的第一个结果。</p>\n<p>可以把规则记成“<strong>只播种一次，多次取数</strong>”：程序开始产生这一组伪随机数前调用一次 <code>srand(seed)</code>，之后按需要多次调用 <code>rand()</code>。随机数的具体值不适合写成跨环境固定答案，应该检查范围性质，例如 <code>rand() % 6 + 1</code> 每次结果都满足 1 到 6。</p>\n<p>若使用完整的标准头文件，<code>rand</code>、<code>srand</code> 来自 <code>&lt;cstdlib&gt;</code>，时间值 <code>time(0)</code> 来自 <code>&lt;ctime&gt;</code>；本站完整程序使用的 <code>&lt;bits/stdc++.h&gt;</code> 会在常见 G++ 环境中统一引入这些内容。用当前时间作种子通常能让不同运行时的序列起点不同，但它仍是<strong>伪随机</strong>，不适合密码或安全用途。二级只讨论范围构造与播种位置。</p>\n<table>\n<thead>\n<tr>\n<th>函数</th>\n<th>主要实参</th>\n<th>常见返回结果</th>\n<th>必查边界</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>abs(x)</code></td>\n<td>一个数</td>\n<td>与 0 的非负距离</td>\n<td>0 仍返回 0，整数运算不越界</td>\n</tr>\n<tr>\n<td><code>sqrt(x)</code></td>\n<td>一个非负数</td>\n<td><code>double</code> 平方根</td>\n<td><code>x &gt;= 0</code></td>\n</tr>\n<tr>\n<td><code>max(a,b)</code></td>\n<td>两个同类型值</td>\n<td>较大者</td>\n<td>参数能用同一类型比较</td>\n</tr>\n<tr>\n<td><code>min(a,b)</code></td>\n<td>两个同类型值</td>\n<td>较小者</td>\n<td>参数能用同一类型比较</td>\n</tr>\n<tr>\n<td><code>rand()</code></td>\n<td>无实参</td>\n<td>非负伪随机整数</td>\n<td>具体序列依赖实现</td>\n</tr>\n<tr>\n<td><code>srand(seed)</code></td>\n<td>一个种子</td>\n<td>不直接产生随机数</td>\n<td>一组取数前通常只调用一次</td>\n</tr>\n</tbody>\n</table>\n<p>先按问题场景选函数，再检查参数和返回值：</p>\n<table>\n<thead>\n<tr>\n<th>使用场景</th>\n<th>常用写法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>两个位置相差多远</td>\n<td><code>abs(a - b)</code></td>\n</tr>\n<tr>\n<td>求正方形边长等平方根问题</td>\n<td><code>sqrt(x)</code></td>\n</tr>\n<tr>\n<td>从两个数中选择较大或较小值</td>\n<td><code>max(a, b)</code> / <code>min(a, b)</code></td>\n</tr>\n<tr>\n<td>模拟骰子等指定整数范围</td>\n<td><code>rand() % n + start</code></td>\n</tr>\n</tbody>\n</table>\n<p>其中随机范围写法还要根据题目计算 <code>n</code> 和 <code>start</code>，不能原样套用。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>函数</th>\n<th>用途</th>\n<th>关键提醒</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>abs(x)</code></td>\n<td>绝对值</td>\n<td>结果非负</td>\n</tr>\n<tr>\n<td><code>sqrt(x)</code></td>\n<td>算术平方根</td>\n<td>参数应非负，结果为浮点类型</td>\n</tr>\n<tr>\n<td><code>max(a,b)</code></td>\n<td>较大值</td>\n<td>从内向外算嵌套调用</td>\n</tr>\n<tr>\n<td><code>min(a,b)</code></td>\n<td>较小值</td>\n<td>参数类型应便于比较</td>\n</tr>\n<tr>\n<td><code>rand()</code></td>\n<td>伪随机整数</td>\n<td>取余后先从 0 开始</td>\n</tr>\n<tr>\n<td><code>srand(seed)</code></td>\n<td>设置种子</td>\n<td>不直接返回随机数</td>\n</tr>\n</tbody>\n</table>\n<h2 id="典型例题">典型例题</h2>\n<p>**题目：**怎样写出一个可能产生 5 到 9 的伪随机整数表达式，并说明范围？</p>\n<p><strong>推演：</strong></p>\n<ol>\n<li>5 到 9 一共有 <code>9-5+1=5</code> 个整数；</li>\n<li><code>rand() % 5</code> 的余数可能是 0、1、2、3、4；</li>\n<li>每个结果加 5 后，依次变成 5、6、7、8、9；</li>\n<li>因而表达式可写为 <code>rand() % 5 + 5</code>；</li>\n<li><code>srand(seed)</code> 在产生这一串数之前设置种子，不直接返回其中某个数。</li>\n</ol>\n<p><strong>结论：</strong><code>rand() % 5 + 5</code> 的范围是 5 到 9。构造 <code>[start, end]</code> 时，先计算整数个数，再把从 0 开始的余数整体平移到 <code>start</code>。</p>\n<a id="常见错误"></a>\n<h2 id="容易混淆的几个问题">容易混淆的几个问题</h2>\n<ul>\n<li>认为 <code>sqrt</code> 返回 <code>int</code>；</li>\n<li>对负数直接调用实数平方根；</li>\n<li>嵌套 <code>min</code>、<code>max</code> 时不从内向外计算；</li>\n<li>把 <code>% n</code> 的范围误写为 1 到 n；</li>\n<li>认为 <code>srand</code> 本身会产生随机数；</li>\n<li>把伪随机结果的具体值当作跨环境固定答案。</li>\n</ul>\n<p><strong>错误写法：</strong><code>double answer = sqrt(-4.0);</code></p>\n<p>**错误原因：**在实数范围内，负数没有算术平方根，不能把结果当作普通实数继续计算。</p>\n<p>**正确写法：**调用 <code>sqrt(x)</code> 前先确认 <code>x &gt;= 0</code>。</p>\n<p>**错误写法：**用 <code>rand() % 6</code> 表示骰子的 1 到 6。</p>\n<p>**错误原因：**余数范围是 0 到 5，既可能出现 0，也不会出现 6。</p>\n<p>**正确写法：**使用 <code>rand() % 6 + 1</code> 得到 1 到 6。<code>srand(seed)</code> 只负责设置种子，通常在产生随机数前设置一次，不需要每次调用 <code>rand()</code> 前都重新设置。</p>\n<h2 id="本节总结">本节总结</h2>\n<p>选择数学函数时先看任务，再检查参数范围和返回类型：<code>abs</code> 得到非负距离，<code>sqrt</code> 的实数参数应非负且结果为浮点类型，<code>min</code>、<code>max</code> 从内到外计算嵌套调用。随机整数先由 <code>% n</code> 得到 0 到 <code>n-1</code>，再整体平移；只播种一次，验证范围性质而不是固定某次结果。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>数据类型转换页面解释 <code>sqrt</code> 浮点结果怎样保存；多层循环可多次调用随机函数或用 <code>min</code>、<code>max</code> 维护当前结果，但应先明确每轮更新顺序。</p>',
    ],
    3: [
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 一串 0 和 1 必须结合固定位宽和编码规则才能确定真值；8 位补码能表示 -128 到 127。</li>\n<li><strong>常用语法：</strong> 本课不新增 C++ 语法；常用判断规则是先确认位宽和编码方式，负数补码再按“首位判负 → 逐位取反 → 加 1 → 恢复负号”读回真值。</li>\n<li><strong>最小示例：</strong> 8 位 <code>-5</code> 的原码、反码、补码依次是 <code>10000101</code>、<code>11111010</code>、<code>11111011</code>。</li>\n<li><strong>关键易错点：</strong> 不能丢掉前导 0 或符号位，也不能把无符号位串直接当成有符号补码；计算前先检查数值是否落在当前位宽的范围内。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>先看一个具体问题：同样是 8 个二进制位，<code>10000000</code> 按无符号数读取是 128，按原码读取是 -0，按反码读取是 -127，按补码读取却是 -128。位串本身没有变，改变答案的是<strong>位宽和编码规则</strong>。本课要解决的就是：怎样写出正负整数的固定 8 位编码，又怎样从位串读回正确真值。</p>\n<p>计算机只能保存 0 和 1。要把整数放进有限个二进制位里，必须先约定：一共有多少位、最高位代表什么、负数怎样编码。本课只讨论<strong>固定宽度</strong>的有符号整数，重点是原码、反码和补码；以 8 位为例学习判断表示范围和把补码读回真值。</p>\n<p>固定宽度不是装饰。<code>5</code> 可以写成 <code>101</code>，也可以写成 8 位的 <code>00000101</code>；两种写法数值相同，但后者才说明“这个数占 8 位”。位数一旦确定，最左边就是最高位，不能随意添删。下面所有二进制串都按 8 位阅读，缺少的高位用 0 补齐。</p>\n<p>这里的“位”就是 <strong>bit（比特）</strong>，一个 bit 只能保存 <code>0</code> 或 <code>1</code>。在常见 GCC/OJ 环境中，连续 8 个 bit 组成 1 <strong>byte（字节）</strong>，所以一个 8 位编码正好占 1 字节；<code>N</code> 位一共能够排出 <code>2^N</code> 种不同位串。位串怎样对应真值，还要看题目把它解释为无符号数、原码、反码还是补码，不能只凭“有多少种”直接判断正负范围。</p>\n<p>真实 C++ 类型的大小用 <code>sizeof(类型)</code> 查看，结果的单位是字节。在常见 GCC/OJ 环境中，<code>sizeof(char)</code> 是 1，<code>sizeof(int)</code> 通常是 4，也就是一个 <code>int</code> 通常占 32 位；但本课的 8 位例子是为了把编码过程完整写开，并不表示 C++ 的 <code>int</code> 只有 1 字节。遇到具体题目时，优先使用题目明确给出的位宽。</p>\n<p>本课的目标是：看见一个 8 位数，先判断它是正还是负，再按补码规则得到十进制真值；也能把一个在范围内的十进制整数写成 8 位补码。这里不要求把函数、指针、作用域或递归作为掌握内容。实际 C++ 中整数类型的位宽由实现决定，因此示例只展示确定的位串与普通算术，不依赖移位负数等容易产生歧义的写法。</p>\n<ul>\n<li><strong>已经会用：</strong> 整数的正负、加减法，以及 <code>5 = 4 + 1</code> 这类小整数算术。本页需要的二进制位权会随例子直接说明，不假定已经学完下一页的进制转换。</li>\n<li><strong>本课新学：</strong> 固定 8 位下的正负编码动作；原码、反码、补码的差异和补码用途；补码范围、真值读取与越界判断。</li>\n<li><strong>暂不展开：</strong> C++ 对象在内存中的真实字节顺序、有符号整数溢出的语言规则、位运算语法、浮点数编码，以及函数、指针、作用域和递归。</li>\n</ul>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>术语</th>\n<th>本课中的意思</th>\n<th>8 位示例</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>固定位宽</td>\n<td>每个数恰好占规定的位数</td>\n<td><code>00000101</code> 有 8 位</td>\n</tr>\n<tr>\n<td>符号位</td>\n<td>最左边一位，用来区分正负</td>\n<td><code>0</code> 表示非负，<code>1</code> 表示负</td>\n</tr>\n<tr>\n<td>真值</td>\n<td>人们通常说的十进制数值</td>\n<td><code>-5</code>、<code>0</code>、<code>127</code></td>\n</tr>\n<tr>\n<td>补码</td>\n<td>计算机常用的有符号整数编码</td>\n<td><code>11111011</code> 表示 <code>-5</code></td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><p><h2 id="kp-01">01 原码、反码与补码</h2>\n先从正数开始。正数的原码、反码和补码完全相同：符号位为 <code>0</code>，其余位写出数的二进制。例如十进制 <code>5</code> 的二进制为 <code>101</code>，写成 8 位就是 <code>00000101</code>。因此 <code>+5</code> 的原码、反码、补码都是 <code>00000101</code>。</p>\n<p>负数才会出现差别。对 <strong>-1 到 -127</strong>，先写出与其绝对值对应的 8 位正数表示；把最左位改为 <code>1</code> 得到原码；除符号位外其余每一位 0、1 互换得到反码；反码再加 1 得到补码。以 <code>-5</code> 为例，过程必须完整写出：</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>表示方法</th>\n<th>8 位结果</th>\n<th>怎样得到</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>+5</code> 的 8 位二进制</td>\n<td><code>00000101</code></td>\n<td>5 = 4 + 1</td>\n</tr>\n<tr>\n<td><code>-5</code> 的原码</td>\n<td><code>10000101</code></td>\n<td>符号位改为 1</td>\n</tr>\n<tr>\n<td><code>-5</code> 的反码</td>\n<td><code>11111010</code></td>\n<td>符号位不动，其余位逐位取反</td>\n</tr>\n<tr>\n<td><code>-5</code> 的补码</td>\n<td><code>11111011</code></td>\n<td>反码 <code>11111010</code> 加 1</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p>\n<p>所以必须记住这组三个关键结果：<code>10000101</code> 是 8 位 <code>-5</code> 的原码，<code>11111010</code> 是反码，<code>11111011</code> 才是补码。不要把“取反加一”直接用在原码上；它的起点是对应正数的二进制，或等价地先得到反码再加一。</p>\n<p><h2 id="kp-02">02 三种编码的差异</h2>\n理解三种编码时，可以把它们看成逐步改进的三种方案。先从最直观的想法开始：留出最高位表示正负，其余位保存绝对值，这就是原码。原码容易读写，但如果把所有位直接送入普通二进制加法器，结果不一定符合有符号整数运算。</p>\n<p>以 4 位表示为例，原码中的 <code>+1</code> 是 <code>0001</code>，<code>-1</code> 是 <code>1001</code>。直接相加得到 <code>0001 + 1001 = 1010</code>。若仍按 4 位原码解释，<code>1010</code> 表示 <code>-2</code>，而不是应有的 0。问题在于：原码的最高位只负责标记符号，不能和其余位一起按普通二进制权值参加加法。</p>\n<p>一种改进思路是把负数除符号位外的各位取反，得到反码。4 位反码中，<code>-1</code> 是 <code>1110</code>，于是 <code>0001 + 1110 = 1111</code>。这个结果在反码中表示“负零”；反码确实改善了正负数运算的对应关系，却保留了 <code>0000</code>（正零）和 <code>1111</code>（负零）两种零。相同的数值有两种编码，会让判断和运算规则不够统一。</p>\n<p>补码在负数反码的基础上再加 1。4 位补码中的 <code>-1</code> 是 <code>1111</code>，所以 <code>0001 + 1111 = 1 0000</code>。在固定 4 位中只能保留低 4 位，结果为 <code>0000</code>，正好是 0。此时最高位也成为数值权值的一部分，正数和负数能够使用同一套定宽加法规则。</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>编码</th>\n<th>4 位 <code>-1</code></th>\n<th><code>1 + (-1)</code> 的位运算</th>\n<th>暴露的问题或改进</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>原码</td>\n<td><code>1001</code></td>\n<td><code>0001 + 1001 = 1010</code></td>\n<td>按原码读成 -2，普通加法规则不能直接使用</td>\n</tr>\n<tr>\n<td>反码</td>\n<td><code>1110</code></td>\n<td><code>0001 + 1110 = 1111</code></td>\n<td>得到负零，仍有两种 0</td>\n</tr>\n<tr>\n<td>补码</td>\n<td><code>1111</code></td>\n<td><code>0001 + 1111 = 1 0000</code></td>\n<td>保留低 4 位得到唯一的 0</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><p><h2 id="kp-03">03 补码的运算与范围</h2>\n补码最重要的作用，是把减法转成加法：<code>a - b</code> 可以按 <code>a + (-b)</code> 处理。仍用 4 位补码验证 <code>5 - 3</code>：<code>+5</code> 是 <code>0101</code>，<code>-3</code> 是 <code>1101</code>，相加得到 <code>0101 + 1101 = 1 0010</code>；保留低 4 位 <code>0010</code>，结果就是 2。这样，加法和减法都可以复用同一套定宽加法电路，不需要为原码的符号位另外设计一套加减规则。</p>\n<p>不过，“保留低 4 位”有明确前提：题目正在讨论固定 4 位，并且真实结果 2 落在 4 位补码的范围 -8 到 7 内。补码并不会让溢出消失；如果真实结果超出当前位宽的范围，截去高位后可能得到另一个数，必须另行判断是否溢出。</p>\n<p>回到 8 位，<code>11111011</code>（-5）与 <code>00000101</code>（+5）相加得到 <code>1 00000000</code>；固定 8 位保留低 8 位就是 <code>00000000</code>。这与前面的 4 位示例遵循同一条规则，也说明补码为何能让正数和负数共用加法过程。</p>\n<p>三种编码的 8 位范围并不完全相同：</p>\n<p>把 8 位推广到 <code>N</code> 位后，补码只有一个 0，表示范围是 <code>-2^(N - 1)</code> 到 <code>2^(N - 1) - 1</code>。代入 <code>N = 8</code>，得到 <code>-128</code> 到 <code>127</code>；总数仍是 <code>256</code> 个，恰好对应全部 <code>2^8</code> 种位串。左端比右端多一个绝对值，是因为补码没有“负零”，把这个编码位置留给了 <code>-128</code>。</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>编码</th>\n<th>8 位表示范围</th>\n<th>关于 0 和 -128</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>原码</td>\n<td>-127 到 127</td>\n<td>有 <code>00000000</code>（+0）和 <code>10000000</code>（-0），不能表示 -128</td>\n</tr>\n<tr>\n<td>反码</td>\n<td>-127 到 127</td>\n<td>有 <code>00000000</code>（+0）和 <code>11111111</code>（-0），不能表示 -128</td>\n</tr>\n<tr>\n<td>补码</td>\n<td>-128 到 127</td>\n<td>只有一个 0；<code>10000000</code> 专门表示 -128</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p>\n<p>原码和反码都存在“正零”和“负零”两种编码，这会让同一个数值 0 有两种写法；补码把 0 统一为 <code>00000000</code>，并把空出来的一个编码用于 -128。</p>\n<p>因此，上面的“绝对值 → 原码 → 反码 → 补码”对 -1 到 -127 适用，不能机械套到 -128：<code>128</code> 的绝对值需要 8 个数值位，已经放不进“1 位符号 + 7 位数值”的 8 位原码或反码。8 位补码则把 <code>10000000</code> 用作 -128，从而获得 <strong>-128 到 127</strong> 的完整范围。</p>\n<p>从补码角度看，首位为 <code>0</code> 时，有 7 个数值位可变化，得到 <code>00000000</code> 到 <code>01111111</code>，即 0 到 127；首位为 <code>1</code> 时，得到 <code>10000000</code> 到 <code>11111111</code>，即 -128 到 -1。注意负数比正数多一个：<code>10000000</code> 表示 -128，但没有对应的 <code>+128</code>。</p>\n<p>读补码真值有两条稳定规则。首位是 <code>0</code>，它是非负数，直接按二进制权值展开；首位是 <code>1</code>，它是负数，可以“按位取反再加 1”，得到绝对值后加负号。还可以把最高位看成 <code>-128</code>，其余七位仍按 64、32、16、8、4、2、1 相加。两种方法应该给出同一个答案。</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>最高位</th>\n<th>真值范围</th>\n<th>稳定读法</th>\n<th>边界示例</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>0</code></td>\n<td>0 到 127</td>\n<td>直接按二进制权值展开</td>\n<td><code>01111111</code> 表示 127</td>\n</tr>\n<tr>\n<td><code>1</code></td>\n<td>-128 到 -1</td>\n<td>取反加一后加负号，或把最高位按 -128 计算</td>\n<td><code>10000000</code> 表示 -128</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><p><h2 id="kp-04">04 补码转换与真值</h2>\n下面把 <code>-5</code> 的编码过程逐行跟踪。每次都保留 8 位，才不会把符号位和数值位混在一起。</p><div tabindex="0" role="region"><table><caption>8 位补码：从真值 -5 到位串</caption><thead><tr><th scope="col">执行步骤</th><th scope="col">操作</th><th scope="col">8 位结果</th><th scope="col">当前含义</th></tr></thead><tbody><tr><th scope="row">1. 写出绝对值</th><td>5 = 4 + 1</td><td>00000101</td><td>+5 的二进制</td></tr><tr><th scope="row">2. 得到原码</th><td>最高位改为 1</td><td>10000101</td><td>-5 的原码</td></tr><tr><th scope="row">3. 得到反码</th><td>数值位 0、1 互换</td><td>11111010</td><td>-5 的反码</td></tr><tr><th scope="row">4. 得到补码</th><td>反码 + 1</td><td>11111011</td><td>-5 的补码</td></tr></tbody></table></div>\n<p>现在反向读 <code>11111011</code>。首位是 <code>1</code>，先确定它一定是负数；取反得到 <code>00000100</code>，再加 1 得到 <code>00000101</code>，其绝对值为 5，所以真值是 <code>-5</code>。不能把 <code>11111011</code> 直接当作无符号数 251，也不能只把首位删掉后读成 123。</p>\n<h3 id="从位串一步步读回--5">从位串一步步读回 -5</h3>\n<div tabindex="0" role="region"><table><caption>8 位补码：从位串 11111011 读回真值</caption><thead><tr><th scope="col">执行步骤</th><th scope="col">操作</th><th scope="col">本步结果</th><th scope="col">为什么这样做</th></tr></thead><tbody><tr><th scope="row">1. 检查首位</th><td>首位是 1</td><td>11111011</td><td>按 8 位补码解释时，它表示负数</td></tr><tr><th scope="row">2. 保持 8 位逐位取反</th><td>0、1 全部互换</td><td>00000100</td><td>得到加 1 之前的中间位串</td></tr><tr><th scope="row">3. 加 1</th><td>00000100 + 1</td><td>00000101</td><td>4 + 1 = 5，绝对值是 5</td></tr><tr><th scope="row">4. 恢复负号</th><td>给绝对值加负号</td><td>-5</td><td>原位串首位是 1，所以最终真值为 -5</td></tr></tbody></table></div>\n<p>另一种心算方法是权值法：<code>11111011 = -128 + 64 + 32 + 16 + 8 + 2 + 1 = -5</code>。最高位不是 <code>+128</code>，而是 <code>-128</code>；这是补码读真值最容易遗漏的一点。对于 <code>10000000</code>，权值法立刻得到 <code>-128</code>，也解释了 8 位补码范围的左端点。</p>\n<p><h2 id="typical-example">典型例题</h2>\n例题：某题给出 8 位补码 <code>10110110</code>，求它表示的十进制真值；再判断十进制 <code>130</code> 能否用 8 位补码表示。</p>\n<p>第一问，首位为 <code>1</code>，所以它是负数。按位取反：<code>01001001</code>；再加 1：<code>01001010</code>。<code>01001010 = 64 + 8 + 2 = 74</code>，故原补码的真值为 <code>-74</code>。用权值法核对：<code>-128 + 32 + 16 + 4 + 2 = -74</code>，结果一致。</p>\n<p>第二问，8 位补码范围为 -128 到 127，<code>130</code> 大于 127，不能表示。不要因为 130 的无符号二进制可以写成 <code>10000010</code> 就认为可行；若把这个 8 位串按补码读取，它的真值是 <code>-126</code>，已经不是 130。题目只要强调“有符号 8 位补码”，就必须先做范围判断。</p>\n<p>再看一个加法检查：<code>-5</code> 的补码为 <code>11111011</code>，<code>+5</code> 的补码为 <code>00000101</code>。逐位相加会产生第 9 位进位，保留 8 位结果 <code>00000000</code>，即 0。固定宽度意味着超出范围的高位不能保存在这个 8 位格子中；在真实程序里，是否溢出及其后果要看类型和语言规则，做题时先按题目给定的位宽分析。</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或任务</th>\n<th>正确结果</th>\n<th>检查目的</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础：正数 <code>+5</code></td>\n<td>写出 8 位原码、反码和补码</td>\n<td>三者都是 <code>00000101</code></td>\n<td>正数不需要取反加一，仍要补足 8 位</td>\n</tr>\n<tr>\n<td>基础：负数 <code>-5</code></td>\n<td>依次写出三种编码</td>\n<td>原码 <code>10000101</code>、反码 <code>11111010</code>、补码 <code>11111011</code></td>\n<td>检查符号位、逐位取反和最后加 1</td>\n</tr>\n<tr>\n<td>边界 <code>-128</code></td>\n<td>写出 8 位补码</td>\n<td><code>10000000</code></td>\n<td>不能机械套用只适合 -1 到 -127 的原码转换过程</td>\n</tr>\n<tr>\n<td>边界 <code>127</code></td>\n<td>写出 8 位补码</td>\n<td><code>01111111</code></td>\n<td>最高位仍为 0，这是 8 位有符号补码的最大值</td>\n</tr>\n<tr>\n<td>易错：超出范围</td>\n<td>判断 <code>130</code> 能否使用 8 位有符号补码表示</td>\n<td>不能表示</td>\n<td>先检查 -128 到 127 的范围，不能把无符号位串当成答案</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p>\n<h3 id="完成这类判断的成功标准">完成这类判断的成功标准</h3>\n<p>完成上面的典型例题后，可以按四点检查自己的过程：先写清“8 位补码”这一解释规则；编码前先判断十进制数是否在 -128 到 127；读负数补码时保留 8 位写完取反和加 1；最后用最高位 <code>-128</code> 的权值法复核，两种读法必须得到同一个真值。四点都能在纸面过程里找到，才算完成判断。</p>\n<section><h3>把补码当成普通二进制</h3><div><div><strong><span>×</span> 错误写法</strong><code>11111011 = 128 + 64 + 32 + 16 + 8 + 2 + 1 = 251，所以它表示 251</code></div><div><strong><span>✓</span> 正确写法</strong><code>11111011 = -128 + 64 + 32 + 16 + 8 + 2 + 1 = -5</code></div></div><p><strong>原因：</strong>251 是把该位串按无符号整数读取的结果。题设若说明是 8 位补码，最高位的权值应为 -128，位串表示的是负数。</p><p><strong>检查方法：</strong>先看题目给的是无符号数、原码还是补码；编码规则不同，同一串 0 和 1 的真值也不同。</p></section>\n<p>对这道错解做一次复验：按 8 位补码权值计算，<code>11111011 = -128 + 64 + 32 + 16 + 8 + 2 + 1 = -5</code>，与取反加一的结果一致；无符号读法得到的 251 不能作为本题答案。</p>\n<h2 id="common-errors">常见错误</h2><div><ol>\n<li><strong>忘记固定宽度。</strong> 现象：把 <code>5</code> 写成 <code>101</code> 后直接取反。原因：三位位串没有为 8 位题目保留符号位。修正：先写成 <code>00000101</code>，再保持 8 位完成后续步骤。复验：逐行数位数，每一行都必须正好有 8 位。</li>\n<li><strong>混淆三种编码。</strong> 现象：把 <code>11111010</code> 当成 -5 的补码。原因：只完成了取反，还没有执行最后的加 1。修正：按原码、反码、补码顺序写出 <code>10000101 → 11111010 → 11111011</code>。复验：用取反加一读回，最终绝对值应为 5。</li>\n<li><strong>负数补码少加了 1。</strong> 现象：由 <code>00000101</code> 取反得到 <code>11111010</code> 后立即停下。原因：把反码误认为补码。修正：在反码基础上再加 1。复验：<code>11111010 + 1 = 11111011</code>，再与 -5 的标准过程核对。</li>\n<li><strong>只看剩余七位。</strong> 现象：删掉 <code>11111011</code> 的首位后把后七位读成 123。原因：补码的最高位也参与数值计算，不能只当正负标签删掉。修正：使用 8 位取反加一，或把最高位按 -128 计算。复验：两种方法都应得到 -5。</li>\n<li><strong>把 -128 套入普通负数步骤。</strong> 现象：试图先写 <code>+128</code> 的 8 位原码再转换。原因：一位符号加七位数值只能保存绝对值 0 到 127。修正：记住 8 位补码把 <code>10000000</code> 专门用于 -128。复验：按权值计算 <code>-128 + 0 = -128</code>。</li>\n<li><strong>漏掉表示范围。</strong> 现象：把 130 写成 <code>10000010</code> 后说它是 8 位有符号补码。原因：没有先检查 -128 到 127 的范围。修正：编码前先做范围判断，超出范围就回答“不能表示”。复验：<code>10000010</code> 按补码读回是 -126，不是 130。</li>\n<li><strong>混用无符号与补码读法。</strong> 现象：同一道题中一会儿把最高位算作 128，一会儿算作 -128。原因：没有固定解释规则。修正：计算第一行先写清“无符号”或“8 位补码”。复验：<code>10000000</code> 按无符号、原码、反码、补码应分别得到 128、-0、-127、-128。</li>\n</ol></div>\n<h3 id="关联学习">关联学习</h3>\n<p>下一页“进制转换”会系统讲怎样在二、八、十、十六进制之间转换；本课只使用读懂固定 8 位所需的最小位权。后面的“位运算”会讲怎样逐位处理 0 和 1；本课先负责确定这些位应按哪一种有符号编码解释。两页都不能代替本课的位宽、符号和范围判断。</p>\n<h3 id="本课小结">本课小结</h3>\n<p>本课只收束三个核心：先固定 8 位再做正负编码；用原码、反码、补码的运算差异理解补码用途；读补码时先判符号和范围，再用取反加一或最高位负权值法复核真值。只要位宽、规则或范围没有写清，后面的计算就不能算完成。</p>\n<h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2>\n<p>下面五题是本课的独立自测。它们继续使用原有题目、答案与本地进度记录，不需要再增加一组重复练习。</p>\n</div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 同一个数量可以用不同进制表示；转换时先确认数位合法，再根据方向选择按权展开、除基取余或二进制分组。</li>\n<li><strong>常用语法：</strong> 本课不新增 C++ 语法。手算时记住：二进制逢 2 进 1，这个 2 叫基数；整数除基取余后逆序读余数。</li>\n<li><strong>最小示例：</strong> 同一个数字放在不同位置时代表的大小不同，这个大小叫位权；<code>(2D)₁₆=2×16¹+13×16⁰=45₁₀</code>。</li>\n<li><strong>关键易错点：</strong> 把每一位的数字乘以所在位置的位权再相加，这个过程叫按权展开；不要漏查非法数位，不要顺序抄余数，也不要从左端分组。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>先看一个具体问题：十进制的 45，可以写成二进制的 <code>(101101)₂</code>、八进制的 <code>(55)₈</code>，也可以写成十六进制的 <code>(2D)₁₆</code>。四种写法长得完全不同，却表示同一个数量。要确认它们确实相等，不能只靠背答案，而要知道每一位能写什么、每个位置值多少，以及转换时应该按什么方向记录。</p>\n<ul>\n<li><strong>已经会用：</strong> 整数乘法、除法、整除和取余；上一课已经见过固定位宽的二进制位串。</li>\n<li><strong>本课新学：</strong> 合法数位与按权读值；十进制整数的除基取余；二进制同八进制、十六进制的分组互转。</li>\n<li><strong>本站补充：</strong> 用乘基取整解释 <code>0.625₁₀→(0.101)₂</code>，只为承接本页已有第 1 题，不计入三个核心。</li>\n<li><strong>暂不展开：</strong> 用字符串保存很长的进制数、编写通用转换程序以及位运算。字符串将在后续课程单独学习，位运算由下一课承接。</li>\n</ul>\n<p>判断方法可以先压缩成一张路线图：目标是十进制，就按权展开；从十进制整数转换出去，就除以目标基数并逆序读余数；二进制与八、十六进制互转，就分别按 3 位或 4 位分组。无论选哪种方法，第一步都应先检查数位是否合法。</p>\n<h2 id="kp-01">01 合法数位与位权</h2>\n<p>一种进制规定“逢几进一”，其中的“几”就是这套记数法的基数。基数为 <code>b</code> 时，每个数位只能取 <code>0</code> 到 <code>b-1</code>。如果某个数位超出范围，整个写法就不合法，后面的计算也没有意义。</p>\n<pre><code>(101101)₂= 1×2⁵ + 0×2⁴ + 1×2³ + 1×2² + 0×2¹ + 1×2⁰= 32 + 8 + 4 + 1= 45₁₀\n(2D)₁₆= 2×16¹ + 13×16⁰= 32 + 13= 45₁₀</code></pre>\n<p>两个展开式都得到 <code>45₁₀</code>，所以 <code>(101101)₂=45₁₀=(2D)₁₆</code>。右下角的基数只说明采用哪套位权，不是要再乘一次 2 或 16。</p>\n<p>再用较短的数检查起始位置：</p>\n<pre><code>(1011)₂= 1×2³ + 0×2² + 1×2¹ + 1×2⁰= 8 + 2 + 1= 11₁₀</code></pre>\n<p>如果误从 <code>2⁴</code> 开始，所有位权都会整体放大一倍。可靠做法是先在最右位下方标出 <code>2⁰</code>，再逐位向左提高指数。</p>\n<h3 id="进制内加减只是同一规则的应用">进制内加减只是同一规则的应用</h3>\n<p>加减竖式仍然逐列进行，只是“逢 10 进 1”要改成“逢基数进 1”。例如 <code>(2D)₁₆+(0F)₁₆</code> 的个位是 <code>13+15=28</code>，而 <code>28=1×16+12</code>，所以本位写十六进制数位 <code>C</code>，向左进 1；左一列得到 <code>2+0+1=3</code>，结果为 <code>(3C)₁₆</code>。按权复验：<code>45+15=60</code>，而 <code>(3C)₁₆=3×16+12=60</code>。</p>\n<p>减法借 1 时，借来的量也等于当前基数。二进制某列借来的 1 代表 2，八进制代表 8，不能一律当成十进制的 10。这只是前三个核心后的应用，不另立新的学习核心。</p>\n<h3 id="官方真题少见写法0x1f-与-037">官方真题少见写法：<code>0x1F</code> 与 <code>037</code></h3>\n<p>数学中可把三十一写作 <code>(1F)₁₆</code> 或 <code>(37)₈</code>。在 C++ 整数字面量中，<code>0x1F</code> 表示十六进制，<code>037</code> 表示八进制：<code>1×16+15=31</code>，<code>3×8+7=31</code>，所以表达式 <code>0x1F == 037</code> 的结果是 <code>true</code>。</p>\n<p>这是官方真题中的少见写法。日常编程不要给普通十进制整数随意加前导 0，以免把进制看错；本课只要求识别，不借此新增 C++ 语法教学。</p>\n<h2 id="kp-03">03 除基取余</h2>\n<p>把十进制整数 <code>N</code> 转成 <code>b</code> 进制时，反复计算 <code>N÷b</code>。每轮余数都在 <code>0</code> 到 <code>b-1</code> 之间，正好是目标进制的一个合法数位；商交给下一轮继续处理。第一次余数对应最低位，后面的余数依次对应更高位，因此结束后必须从最后一枚余数向前读。</p>\n<h3 id="13-转二进制看清余数方向">13₁₀ 转二进制：看清余数方向</h3>\n<pre><code>45₁₀→(101101)₂=(55)₈=(2D)₁₆</code></pre>\n<p>去掉十进制起点后，分组关系仍可写成 <code>(101101)₂=(55)₈=(2D)₁₆</code>，三种表示的值完全相同。</p>\n<p>整数 0 是必须单独记住的边界。它没有可继续做的除法轮次，但答案不能是空白，必须直接写成 <code>0₁₀→0₂</code>；转成八进制或十六进制时也仍然写一个数位 0。</p>\n<h2 id="kp-04">04 二进制分组转换</h2>\n<p>因为 <code>8=2³</code>，一位八进制恰好对应三位二进制；因为 <code>16=2⁴</code>，一位十六进制恰好对应四位二进制。整数部分必须从最右端对齐分组，不足一组时只在左侧补 0，补出的前导 0 不改变数值。</p>\n<h3 id="二进制转八进制每-3-位压缩">二进制转八进制：每 3 位压缩</h3>\n<pre><code>(101101)₂→101 101→(55)₈</code></pre>\n<p>两个三位组 <code>101</code> 都表示 5，所以结果为 <code>(55)₈</code>。反向转换时，每个八进制数位固定展开为三位：<code>5→101</code>，于是 <code>(55)₈→101 101→(101101)₂</code>。</p>\n<h3 id="八进制转二进制每-1-位展开成-3-位">八进制转二进制：每 1 位展开成 3 位</h3>\n<pre><code>(55)₈→5 5→101 101→(101101)₂</code></pre>\n<p>展开过程中要先保留每组完整的三位，全部拼好后才可删掉整数最左侧多余的 0。</p>\n<h3 id="二进制转十六进制每-4-位压缩">二进制转十六进制：每 4 位压缩</h3>\n<pre><code>(101101)₂→0010 1101→(2D)₁₆</code></pre>\n<p>从右端分组后，左组不足四位，在左侧补成 <code>0010</code>。<code>0010</code> 对应 2，<code>1101</code> 对应 13，也就是数位 D。</p>\n<h3 id="十六进制转二进制每-1-位展开成-4-位">十六进制转二进制：每 1 位展开成 4 位</h3>\n<pre><code>(2D)₁₆→2 D→0010 1101→(101101)₂</code></pre>\n<p>每个十六进制数位先完整展开四位，最后删除整数最左侧多余的前导 0。用同样方法，<code>(111011)₂→111 011→(73)₈</code>。</p>\n<h2 id="kp-05">05 有限小数的乘基取整</h2>\n<p>这一节是为既有第 1 题保留的<strong>本站补充</strong>，不计入本课三个核心，也不写入 MasteryCheck。十进制小数部分转到 <code>b</code> 进制时，每轮把当前小数部分乘以 <code>b</code>，取乘积的整数部分作为从左到右的下一位，再把新的小数部分交给下一轮。它与整数除基取余的记录方向正好不同。</p>\n<pre><code>0.625×2=1.250 → 取整数位1，留下0.2500.250×2=0.500 → 取整数位0，留下0.5000.500×2=1.000 → 取整数位1，留下0</code></pre>\n<p>整数位按出现顺序是 <code>1、0、1</code>，所以 <code>0.625₁₀→(0.101)₂</code>。按权复验：<code>(0.101)₂=1×2⁻¹+0×2⁻²+1×2⁻³=0.5+0.125=0.625₁₀</code>。有些十进制有限小数换到二进制后不能有限写完，遇到这种情况应按题目要求保留位数，本课不展开无限循环小数。</p>\n<h2 id="typical-example">典型例题</h2>\n<p>题目：把 <code>45₁₀</code> 分别转换为二进制、八进制和十六进制，并说明怎样交叉核对。</p>\n<p>先用除基取余得到二进制。45 连续除以 2 的余数依次为 <code>1、0、1、1、0、1</code>，从最后一枚向前读出 <code>(101101)₂</code>。接着从右端分组：每三位得到 <code>101 101</code>，所以是 <code>(55)₈</code>；每四位先在左侧补 0，得到 <code>0010 1101</code>，所以是 <code>(2D)₁₆</code>。</p>\n<p>最后按权展开核对：<code>(101101)₂=32+8+4+1=45₁₀</code>，<code>(55)₈=5×8+5=45₁₀</code>，<code>(2D)₁₆=2×16+13=45₁₀</code>。三条都回到 45，说明读取方向和分组位置一致。</p>\n<h3 id="完成这类转换的成功标准">完成这类转换的成功标准</h3>\n<ul>\n<li>写答案前能指出每个数位是否合法。</li>\n<li>除基取余草稿同时保留商和余数，并明确沿余数列自下而上读。</li>\n<li>二进制分组从右端开始，缺位只在整数左侧补 0。</li>\n<li>至少用按权展开或反向分组复验一次，结果能回到原数。</li>\n</ul>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>检查类型</th>\n<th>输入或任务</th>\n<th>正确结果</th>\n<th>自检重点</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>零边界</td>\n<td><code>0₁₀</code> 转二进制</td>\n<td><code>0₂</code></td>\n<td>不能得到空答案</td>\n</tr>\n<tr>\n<td>按权展开</td>\n<td><code>(1011)₂</code> 转十进制</td>\n<td><code>11₁₀</code></td>\n<td>最右位从 <code>2⁰</code> 开始</td>\n</tr>\n<tr>\n<td>余数方向</td>\n<td><code>13₁₀</code> 转二进制</td>\n<td><code>(1101)₂</code></td>\n<td>余数逆序读取</td>\n</tr>\n<tr>\n<td>分组转换</td>\n<td><code>(111011)₂</code> 转八进制</td>\n<td><code>(73)₈</code></td>\n<td>从右端三位一组</td>\n</tr>\n<tr>\n<td>非法数位</td>\n<td>判断 <code>(1G)₁₆</code></td>\n<td>不合法</td>\n<td>十六进制只到 F</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><h2 id="common-errors">常见错误</h2>\n<ol>\n<li><strong>现象：</strong> 看见 <code>(102)₂</code> 就直接按权计算。<strong>原因：</strong> 忘了二进制数位只能是 0 和 1。<strong>修正：</strong> 先逐位检查合法范围，发现 <code>2∉{0,1}</code> 就停止。<strong>复验：</strong> <code>(101)₂</code> 合法，而 <code>(102)₂</code> 不合法。</li>\n<li><strong>现象：</strong> 把 13 的余数按 <code>1、0、1、1</code> 顺序写成 <code>(1011)₂</code>。<strong>原因：</strong> 第一次余数是最低位，却被放到了最左边。<strong>修正：</strong> 沿余数列自下而上读取，得到 <code>(1101)₂</code>。<strong>复验：</strong> <code>(1101)₂=13₁₀</code>，而 <code>(1011)₂=11₁₀</code>。</li>\n<li><strong>现象：</strong> 把 <code>(101101)₂</code> 从左端切成 <code>1011 01</code>。<strong>原因：</strong> 忽略了整数最低位必须与最右端对齐。<strong>修正：</strong> 从右端每四位分组，左侧补成 <code>0010 1101</code>。<strong>复验：</strong> 两组分别是 2 和 D，得到 <code>(2D)₁₆=45₁₀</code>。</li>\n<li><strong>现象：</strong> 把十六进制余数 13 写成两个数位 <code>13</code>。<strong>原因：</strong> 没把 10 到 15 映射为 A 到 F。<strong>修正：</strong> 13 写成单个数位 D。<strong>复验：</strong> <code>(2D)₁₆=2×16+13=45₁₀</code>。</li>\n<li><strong>现象：</strong> 小数转换也逆序记录 <code>1、0、1</code>。<strong>原因：</strong> 混淆了整数余数和小数乘积整数位的方向。<strong>修正：</strong> 乘基取整按出现顺序记录。<strong>复验：</strong> <code>0.625₁₀→(0.101)₂</code>，再按负次方位权可回到 <code>0.625₁₀</code>。</li>\n</ol>\n<section><h3>余数为什么必须逆序</h3><div><div><strong><span>×</span> 错误写法</strong><code>13₁₀的余数依次是1、0、1、1，所以直接写成(1011)₂</code></div><div><strong><span>✓</span> 正确写法</strong><code>从最后一枚余数向前读，13₁₀→(1101)₂</code></div></div><p><strong>原因：</strong>第一次余数对应最低位，后面的余数才逐步对应更高位。</p><p><strong>检查方法：</strong>结束后用按权展开复验，能立即发现(1011)₂只等于11₁₀。</p></section>\n<h3 id="关联学习">关联学习</h3>\n<ul>\n<li>前一课“数据编码”解释固定宽度下位串怎样表示正负数；本课只关心不同进制的记数规则。</li>\n<li>后一课“位运算”会直接操作整数的二进制位；先能读懂二进制表示，再学习掩码会更稳。</li>\n<li>后续“字符串”课会讲怎样保存和逐字符处理很长的数位串，本课不提前写相关代码。</li>\n</ul>\n<h3 id="本课小结">本课小结</h3>\n<p>本课只收束三个核心：先检查合法数位并按权读值；把十进制整数转换出去时除基取余并逆序；二进制同八、十六进制互转时从右端按 3 位或 4 位分组。<code>0.625₁₀→(0.101)₂</code> 是承接既有题目的本站补充，不扩大三个核心。</p>\n<h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2>\n<p>下面五题是本课的独立自测。提交后按最近一次完整结果检查错因；完成自测不会自动把本页标记为已复习。</p>\n</div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 位运算先把整数补成相同位宽，再逐位计算；读取或修改第 <code>k</code> 位时，先用 <code>1u &lt;&lt; k</code> 生成只选中目标位的掩码。</li>\n<li><strong>常用语法：</strong> <code>a &amp; b</code>、<code>a | b</code>、<code>a ^ b</code>、<code>~a</code>、<code>a &lt;&lt; k</code>、<code>a &gt;&gt; k</code>；读、设、清、翻分别使用 <code>((value &gt;&gt; k) &amp; 1u)</code>、<code>value | mask</code>、<code>value &amp; (~mask)</code>、<code>value ^ mask</code>。</li>\n<li><strong>最小示例：</strong> <code>6=0110₂</code>、<code>3=0011₂</code>，所以 <code>6 &amp; 3=0010₂=2</code>。</li>\n<li><strong>关键易错点：</strong> 不混淆 <code>&amp;</code> 与 <code>&amp;&amp;</code>；最右位编号为 0；<code>~</code> 必须结合位宽；移动量必须小于类型位宽；混合表达式主动加括号。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>想象一个整数保存着一排开关：每一位是 <code>0</code> 或 <code>1</code>。我们既可能只想查看某个开关，也可能只改变一个开关而不影响其他位置。位运算就是直接处理这些二进制位的工具。</p>\n<p>本课只收束三个核心：先在固定位宽下计算按位与 <code>&amp;</code>、按位或 <code>|</code>、异或 <code>^</code> 和按位非 <code>~</code>；再追踪非负无符号数的左移 <code>&lt;&lt;</code> 与右移 <code>&gt;&gt;</code>；最后用 <code>1u &lt;&lt; k</code> 读取、设置、清除或翻转第 <code>k</code> 位。后面的位段拆合与异或恢复只是这三个核心的直接应用，不进入 Mastery 三核心。</p>\n<p>这里的“位宽”是统一规定用多少位二进制观察一个数。例如只观察低 8 位时，十进制 45 写成 <code>00101101</code>。本课示例使用非负整数和 <code>unsigned int</code>；在常见 32 位 GCC/OJ 环境中，<code>unsigned int</code> 通常表示 0 到 2³² - 1，实际位宽仍由平台决定。无符号常量 <code>1u</code> 中的 <code>u</code> 表示无符号类型，不会改变数字 1 的大小。</p>\n<p>类型名 unsigned int 表示无符号整数类型，字面量 1u 表示无符号整数 1。</p>\n<p>位编号从最右侧开始，最右位是第 <code>0</code> 位，向左依次是第 <code>1</code>、<code>2</code>、<code>3</code> 位。生成 <code>1u &lt;&lt; k</code> 前必须保证 <code>k</code> 小于 <code>unsigned int</code> 的位宽。本课不讨论负数移位，也不把可能丢失高位的左移当作无条件乘法技巧。</p>\n<h2 id="kp-01">01 六种位运算</h2>\n<h3 id="核心一固定位宽下逐位计算">核心一：固定位宽下逐位计算</h3>\n<p><code>&amp;</code>、<code>|</code>、<code>^</code> 都有左右两个整数。先把两数补成相同位宽，再让同一列的两位配对。<code>~</code> 只有一个整数，会把约定位宽内的每一位翻转。</p>\n<pre><code>1101&amp; 1010------  1000</code></pre>\n<p>只有最左一列同时为 <code>1</code>，所以结果是 <code>1000₂=8</code>。</p>\n<h4 id="按位或竖式">按位或竖式</h4>\n<pre><code>1101| 1010------  1111</code></pre>\n<p>每一列至少出现一个 <code>1</code>，所以结果是 <code>1111₂=15</code>。</p>\n<h4 id="异或竖式">异或竖式</h4>\n<pre><code>1101^ 1010------  0111</code></pre>\n<p>后三列的两位不同，所以结果是 <code>0111₂=7</code>。</p>\n<h4 id="按位非竖式">按位非竖式</h4>\n<pre><code>~ 1101------  0010</code></pre>\n<p>纸面结果 <code>0010₂=2</code> 的前提是“只观察低 4 位”。在 C++ 中可以把这个前提写成 <code>((~13u) &amp; 15u)</code>：<code>15u</code> 的低 4 位全是 <code>1</code>，因此最后只保留低 4 位。不能脱离位宽直接把 <code>~13u</code> 写成 2。</p>\n<p>前三项双目运算可以压缩复算为：13&amp;10=1000₂=8、13|10=1111₂=15、13^10=0111₂=7。</p>\n<p>位运算和逻辑运算含义不同。<code>6 &amp; 3</code> 计算 <code>0110 &amp; 0011=0010</code>，结果为 2；<code>6 &amp;&amp; 3</code> 只判断两个数是否都非 0，结果为真。现象相似时仍要先看清符号。</p>\n<h3 id="核心二安全左移与右移">核心二：安全左移与右移</h3>\n<p>左移把所有位向左移动，右侧补 <code>0</code>；本课的非负无符号数右移时，左侧补 <code>0</code>，移出右端的低位被丢弃。</p>\n<pre><code>unsigned int value = 5u; // 本例只处理小范围非负无符号数unsigned int doubled = (value &lt;&lt; 1); // 先确认移动量小于位宽且高位不会丢失</code></pre>\n<p>完成移位判断的成功标准是：先画出固定位宽，再标出移动方向和补入的 <code>0</code>，最后检查是否有需要保留的位被移出。</p>\n<h2 id="kp-02">02 判断第 k 位</h2>\n<h3 id="核心三用掩码读取目标位">核心三：用掩码读取目标位</h3>\n<p>为了只选择第 <code>k</code> 位，可以准备一个“只有目标位置为 <code>1</code>、其他位置都是 <code>0</code>”的二进制数，这个数叫掩码。表达式 <code>1u &lt;&lt; k</code> 会把最低位的 <code>1</code> 移到第 <code>k</code> 位。</p>\n<h3 id="安全读取第-k-位">安全读取第 k 位</h3>\n<pre><code>unsigned int value = 45u; // 低8位是00101101unsigned int k = 4u; // 从右侧第0位开始编号，并保证k小于类型位宽unsigned int bit = ((value &gt;&gt; k) &amp; 1u); // 目标位移到最右后只保留最低位</code></pre>\n<p><code>45u &gt;&gt; 4</code> 的低 8 位是 <code>00000010</code>，再与 <code>00000001</code> 做按位与，得到 <code>0</code>，所以 45 的第 4 位为 0。括号说明先移位，再只保留最低位。</p>\n<p>判断奇偶是读取第 0 位的特例。<code>(value &amp; 1u) == 0u</code> 表示最低位为 0，也就是非负偶数；最低位为 1 时是奇数。</p>\n<h2 id="kp-03">03 设置、清除与翻转第 k 位</h2>\n<p>同一个掩码还可以设置、清除或翻转目标位：</p>\n<pre><code>unsigned int value = 45u; // 当前要读取或修改的非负整数unsigned int k = 4u; // 目标位编号，并保证k小于unsigned int位宽unsigned int mask = (1u &lt;&lt; k); // 只在第k位放置1unsigned int setValue = (value | mask); // 设置目标位unsigned int clearValue = (value &amp; (~mask)); // 清除目标位unsigned int flipValue = (value ^ mask); // 翻转目标位</code></pre>\n<p>不要用减法代替清除。如果目标位原本就是 0，减去 <code>1u &lt;&lt; k</code> 会改变其他位或得到错误数值；<code>value &amp; (~mask)</code> 无论目标位原来是 0 还是 1，都只会把该位清零。</p>\n<p>完成掩码操作的成功标准是：能先写出 <code>mask</code> 的位模式，再说明每个表达式只改变目标位，并用十进制结果反向核对。</p>\n<h2 id="kp-04">04 位段拆分与合并</h2>\n<p>这一节是三核心后的兼容应用，用于承接既有第 5 题，不进入 Mastery。上一课已经见过十六进制字面量：<code>0xABu</code> 的低 8 位是 <code>10101011</code>。<code>0x0Fu=00001111₂</code> 可以保留低四位，右移四位可以把高四位移到最右。</p>\n<pre><code>unsigned int value = 0xABu; // 低8位是10101011unsigned int low = (value &amp; 0x0Fu); // 保留低四位1011，得到11unsigned int high = ((value &gt;&gt; 4) &amp; 0x0Fu); // 高四位1010移到最右，得到10unsigned int combined = ((high &lt;&lt; 4) | low); // 放回高四位并接回低四位</code></pre>\n<p>因此 <code>0xABu</code> 拆分为高四位 10、低四位 11，再合并会回到原值。既有第 5 题使用 <code>10101100₂</code>，同理得到高四位 10、低四位 12。</p>\n<pre><code>bool odd = ((value &amp; 1u) != 0u); // 先读取最低位，再判断是否为1unsigned int bit = ((value &gt;&gt; k) &amp; 1u); // 先移动目标位，再保留最低位unsigned int cleared = (value &amp; (~mask)); // 先取反掩码，再清除目标位</code></pre>\n<p>不要写含义模糊的 <code>value &amp; mask == 0u</code>。括号既控制顺序，也把“先做哪一步”直接告诉读者。</p>\n<h2 id="typical-example">典型例题</h2>\n<p>前面的真值、移位和掩码追踪已经完成后，再把四个表达式放进一份完整程序。输入保证 <code>value</code> 是非负 <code>unsigned int</code>，<code>k</code> 小于 <code>unsigned int</code> 的位宽；本程序不处理非法移动量。</p>\n<h3 id="完成程序">完成程序</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    unsigned int value; // 保存要读取或修改的非负整数    unsigned int k; // 保存从右侧第0位开始的目标位编号    cin &gt;&gt; value &gt;&gt; k; // 题目保证k小于unsigned int位宽\n    unsigned int mask = (1u &lt;&lt; k); // 生成只选中第k位的掩码    unsigned int bit = ((value &gt;&gt; k) &amp; 1u); // 读取目标位    unsigned int setValue = (value | mask); // 把目标位设置为1    unsigned int clearValue = (value &amp; (~mask)); // 把目标位清除为0    unsigned int flipValue = (value ^ mask); // 翻转目标位\n    cout &lt;&lt; bit &lt;&lt; \' \' &lt;&lt; setValue &lt;&lt; \' \' &lt;&lt; clearValue &lt;&lt; \' \' &lt;&lt; flipValue &lt;&lt; \'\\n\'; // 按读、设、清、翻输出    return 0;}</code></pre>\n<p>三组运行结果如下：</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>输入</th>\n<th>读、设、清、翻输出</th>\n<th>检查目的</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>45 4</code></td>\n<td><code>0 61 45 61</code></td>\n<td>目标位原来为0</td>\n</tr>\n<tr>\n<td><code>45 3</code></td>\n<td><code>1 45 37 37</code></td>\n<td>目标位原来为1</td>\n</tr>\n<tr>\n<td><code>0 0</code></td>\n<td><code>0 1 0 1</code></td>\n<td>零值与最低位边界</td>\n</tr>\n<tr>\n<td><code>5u &lt;&lt; 1</code></td>\n<td><code>10</code>，即 <code>00000101 → 00001010</code></td>\n<td>左移方向与补0</td>\n</tr>\n<tr>\n<td><code>10u &gt;&gt; 1</code></td>\n<td><code>5</code></td>\n<td>非负无符号数右移</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p>\n<p>完成这道典型例题的成功标准是：能在运行程序前独立写出四个表达式和预期结果，运行后四项都与手算一致，并能解释为什么其他位置没有被修改。</p>\n<h2 id="common-errors">常见错误</h2>\n<ol>\n<li><strong>现象：</strong> 把 <code>6 &amp; 3</code> 算成真假值。<strong>原因：</strong> 混淆了 <code>&amp;</code> 与 <code>&amp;&amp;</code>。<strong>修正：</strong> 补齐为 <code>0110 &amp; 0011</code> 后逐列计算。<strong>复验：</strong> 结果是 <code>0010₂=2</code>。</li>\n<li><strong>现象：</strong> 把最右位称为第 1 位。<strong>原因：</strong> 忘记本课从 0 编号。<strong>修正：</strong> 在位串下先写 <code>7…2、1、0</code>。<strong>复验：</strong> <code>45=00101101₂</code> 的第 4 位是 0。</li>\n<li><strong>现象：</strong> 直接写 <code>~13u=2</code>。<strong>原因：</strong> 忽略了 C++ 会翻转类型中的所有位。<strong>修正：</strong> 明确只观察低4位并计算 <code>((~13u)&amp;15u)</code>。<strong>复验：</strong> 低4位为 <code>0010₂=2</code>。</li>\n<li><strong>现象：</strong> 把任意 <code>x&lt;&lt;k</code> 都当作乘 <code>2^k</code>。<strong>原因：</strong> 没检查移动量和高位丢失。<strong>修正：</strong> 先确认 <code>k</code> 小于位宽并画出移动后的固定位宽。<strong>复验：</strong> <code>5u&lt;&lt;1=10</code> 的低8位没有高位丢失。</li>\n<li><strong>现象：</strong> 用 <code>value-(1u&lt;&lt;k)</code> 清除目标位。<strong>原因：</strong> 假定目标位原本一定为1。<strong>修正：</strong> 使用 <code>value&amp;(~mask)</code>。<strong>复验：</strong> 45 的第4位原本为0，清除后仍是45。</li>\n<li><strong>现象：</strong> 写 <code>value &amp; mask == 0u</code>。<strong>原因：</strong> 混合表达式没有明确分组。<strong>修正：</strong> 写成 <code>((value &amp; mask)==0u)</code>。<strong>复验：</strong> 先得到掩码结果，再与0比较。</li>\n</ol>\n<section><h3>判断目标位时先完成按位运算</h3><div><div><strong><span>×</span> 错误写法</strong><code>value &amp; mask == 0u</code></div><div><strong><span>✓</span> 正确写法</strong><code>((value &amp; mask) == 0u)</code></div></div><p><strong>原因：</strong>混合表达式省略括号后难以确认先做按位与还是相等判断，容易读错或写错。</p><p><strong>检查方法：</strong>内层括号得到掩码结果，外层比较它是否为0。</p></section>\n<h3 id="本课小结">本课小结</h3>\n<p>本课只掌握三个核心：固定位宽下按真值逐位计算 <code>&amp; | ^ ~</code>；在非负 <code>unsigned</code> 范围内安全追踪 <code>&lt;&lt; &gt;&gt;</code>；用 <code>1u &lt;&lt; k</code> 掩码读取、设置、清除和翻转一位。位段拆合与异或恢复只是承接既有题目的兼容应用。</p>\n<h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2>\n<p>下面五题是本课的独立自测。提交后按最近一次完整结果检查错因；完成自测不会自动把本页标记为已复习。</p>\n</div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 解决一个问题时，按顺序完成的一步步操作，合起来叫作算法。合格的算法必须说清输入、输出和每一步，并且能够在有限步后结束。</li>\n<li><strong>常用语法：</strong> 本课不新增 C++ 语法，继续使用已经学过的 <code>cin</code>、赋值、<code>if</code> 和 <code>cout</code>。像代码一样把步骤写出来，但不要求完全符合 C++ 格式，这种写法叫作伪代码。</li>\n<li><strong>最小示例：</strong> 输入 <code>3 8 5</code>，先令 <code>maximum=3</code>，再比较后得到 <code>maximum=3→8→8</code>，最后输出 <code>8</code>。</li>\n<li><strong>关键易错点：</strong>\n<ul>\n<li>只写“求出最大值”没有给出可执行步骤。</li>\n<li>四种描述中的输入、顺序、条件方向和输出必须一致。</li>\n<li>一次样例追踪能帮助发现错误，但不能单独证明所有输入都正确。</li>\n</ul>\n</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>本课只解决一个问题：怎样把“要完成什么”整理成计算机能够照着执行的一组步骤。我们会用“输入三个整数，输出其中的最大值”贯穿自然语言、流程图、伪代码、执行追踪和 C++ 程序。变量、输入输出、赋值和 <code>if</code> 都已经学过；流程图的起止、输入输出、处理、判断图形也已经在二级学过，这里只复用，不重新讲一遍符号课。</p>\n<p>本课聚焦三个核心：用五个特征检查步骤；核对四种描述是否表达同一过程；用具体输入追踪条件、路径、动作和状态。后续课程会再学习更多解决问题的方法，本课不提前展开，也不把一次样例追踪说成对所有输入的完整证明。</p>\n<h2 id="kp-01">01 什么是算法</h2>\n<h3 id="核心一用五个特征检查步骤">核心一：用五个特征检查步骤</h3>\n<h3 id="算法的五个基本特征">算法的五个基本特征</h3>\n<p>题目说：“输入三个整数，输出其中的最大值。”如果只回答“求出最大值”，计算机仍然不知道先做什么、后做什么。可以真正执行的步骤应当写成：先把第一个数作为当前最大值，再依次比较第二个数和第三个数，遇到更大的数就更新，最后输出当前最大值。</p>\n<p>这两句话的差别不在长短，而在于后者能够逐步执行。检查一段描述时，可以使用下面五个基本特征。</p>\n<table>\n<thead>\n<tr>\n<th>特征</th>\n<th>准确含义</th>\n<th>对当前任务的检查问题</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>输入</td>\n<td>可以有零个或多个外部给定的数据</td>\n<td>是否明确读入 <code>a、b、c</code>？</td>\n</tr>\n<tr>\n<td>输出</td>\n<td>至少产生一个与问题有关的结果</td>\n<td>是否明确输出 <code>maximum</code>？</td>\n</tr>\n<tr>\n<td>有穷性</td>\n<td>对每组合法输入，都能在有限步后停止</td>\n<td>两次比较后是否一定到达输出和结束？</td>\n</tr>\n<tr>\n<td>确定性</td>\n<td>每一步含义清楚，相同状态下执行方式唯一</td>\n<td>“更大时更新”是否写明比较对象和更新值？</td>\n</tr>\n<tr>\n<td>可行性</td>\n<td>每一步都能用有限次基本操作完成</td>\n<td>是否给出读入、赋值、比较和输出，而不是直接写“得到答案”？</td>\n</tr>\n</tbody>\n</table>\n<p>“适当比较三个数，得到答案”不是合格描述。“适当”没有明确规则，“得到答案”也不是可以直接执行的基本操作。把它改成“令 <code>maximum=a</code>；若 <code>b&gt;maximum</code> 就令 <code>maximum=b</code>；若 <code>c&gt;maximum</code> 就令 <code>maximum=c</code>；输出 <code>maximum</code>”，五项检查才都有可以核对的内容。</p>\n<h2 id="kp-02">02 分析问题的输入、输出和处理步骤</h2>\n<h3 id="核心二四种描述保持同一执行含义">核心二：四种描述保持同一执行含义</h3>\n<p>先冻结这道题的单一事实：输入是三个整数 <code>a、b、c</code>，状态变量是 <code>maximum</code>，两个条件依次是 <code>b&gt;maximum</code> 和 <code>c&gt;maximum</code>，输出是最终的 <code>maximum</code>。表达方式可以改变，这四项不能悄悄改变。</p>\n<p>第一次判断检查 b 比当前最大值更大是否成立，第二次判断再用同样规则检查 c；两次判断读取的是按顺序更新后的同一个状态。</p>\n<h3 id="自然语言描述算法">自然语言描述算法</h3>\n<ol>\n<li>输入三个整数 <code>a、b、c</code>。</li>\n<li>把 <code>a</code> 保存为当前最大值 <code>maximum</code>。</li>\n<li>如果 <code>b</code> 比当前最大值更大，就把 <code>maximum</code> 更新为 <code>b</code>。</li>\n<li>如果 <code>c</code> 大于当前最大值，就把 <code>maximum</code> 更新为 <code>c</code>。</li>\n<li>输出 <code>maximum</code>，结束。</li>\n</ol>\n<h3 id="流程图描述算法">流程图描述算法</h3>\n<p>流程图把上面五步放到带箭头的执行路径中。每个判断都有“是”和“否”两条出口：为“是”时更新，为“否”时不更新；两条路径随后汇合到下一步。输入输出框在标准手绘流程图中使用平行四边形，处理步骤使用矩形，判断使用菱形。</p>\n<pre><code>输入 a, b, cmaximum ← a如果 b &gt; maximum，那么 maximum ← b如果 c &gt; maximum，那么 maximum ← c输出 maximum</code></pre>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int a, b, c;    cin &gt;&gt; a &gt;&gt; b &gt;&gt; c;\n    // 用第一个真实输入初始化，全为负数时也不会凭空得到 0    int maximum = a;\n    if (b &gt; maximum) {        maximum = b;    }    // 这里读取的是比较 b 之后的当前 maximum    if (c &gt; maximum) {        maximum = c;    }\n    cout &lt;&lt; maximum &lt;&lt; \'\\n\';    return 0;}</code></pre>\n<p>教学程序支持三个处于 C++ <code>int</code> 可表示范围内的整数。程序只做赋值和比较，不做可能溢出的加减乘除。下面五组结果分别覆盖两次更新、两次不更新、相等边界和全负数边界：</p>\n<table>\n<thead>\n<tr>\n<th>输入</th>\n<th><code>maximum</code> 变化</th>\n<th>精确输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>3 8 5</code></td>\n<td><code>maximum=3→8→8</code></td>\n<td><code>8</code></td>\n</tr>\n<tr>\n<td><code>9 3 7</code></td>\n<td><code>maximum=9→9→9</code></td>\n<td><code>9</code></td>\n</tr>\n<tr>\n<td><code>3 5 9</code></td>\n<td><code>maximum=3→5→9</code></td>\n<td><code>9</code></td>\n</tr>\n<tr>\n<td><code>4 4 4</code></td>\n<td><code>maximum=4→4→4</code></td>\n<td><code>4</code></td>\n</tr>\n<tr>\n<td><code>-4 -9 -2</code></td>\n<td><code>maximum=-4→-4→-2</code></td>\n<td><code>-2</code></td>\n</tr>\n</tbody>\n</table>\n<p>完成这道典型例题的成功标准是：四种描述都保留相同的三个输入、两次比较顺序、真假方向和最终输出；五组输入都得到表中的精确结果。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题三个整数的最大值">基础例题：三个整数的最大值</h3>\n<p>本页只用这一道主任务贯穿四种描述。独立检查时，先遮住程序，用自然语言或伪代码写出步骤，再沿 <code>3 8 5</code> 追踪到 <code>maximum=3→8→8</code>，最后对照完成程序。只要输入、顺序、条件方向或输出有一项不同，就还没有完成等价互译。</p>\n<h3 id="典型应用判断是否达到及格线">典型应用：判断是否达到及格线</h3>\n<p>下面只把“判断分数”作为描述完整性的反例，不把它发展成第二条编程主线。</p>\n<p>错误描述：“输入分数；如果分数达到 60，就输出 <code>Pass</code>；结束。”当输入 59 时，这段描述没有给出任何输出，因此没有覆盖题目中的全部合法输入。修正后应明确：条件成立输出 <code>Pass</code>，否则输出 <code>Fail</code>，两条路径最后都结束。</p>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或描述</th>\n<th>正确结果</th>\n<th>检查目的</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>第一次真、第二次假</td>\n<td><code>3、8、5</code> 求最大值</td>\n<td>8</td>\n<td>第二次比较使用更新后的 <code>maximum=8</code></td>\n</tr>\n<tr>\n<td>两次都假</td>\n<td><code>9、3、7</code> 求最大值</td>\n<td>9</td>\n<td>不更新时保留初始化值</td>\n</tr>\n<tr>\n<td>两次都真</td>\n<td><code>3、5、9</code> 求最大值</td>\n<td>9</td>\n<td>两次更新顺序不能交换</td>\n</tr>\n<tr>\n<td>相等边界</td>\n<td><code>4、4、4</code> 求最大值</td>\n<td>4</td>\n<td><code>&gt;</code> 为假时仍能输出正确值</td>\n</tr>\n<tr>\n<td>全部负数</td>\n<td><code>-4、-9、-2</code> 求最大值</td>\n<td>-2</td>\n<td>最大值必须从真实输入初始化</td>\n</tr>\n<tr>\n<td>步骤遗漏</td>\n<td>只有“达到 60 输出 Pass”</td>\n<td>描述不完整</td>\n<td>缺少条件为假时的输出</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><section><h3>初始化为 0 会漏掉全负数</h3><div><div><strong><span>×</span> 错误写法</strong><code>maximum = 0，然后依次与三个输入比较</code></div><div><strong><span>✓</span> 正确写法</strong><code>先输入数据，再令 maximum = a，然后比较 b 和 c</code></div></div><p><strong>原因：</strong>当三个输入都小于 0 时，没有任何数能更新 maximum，程序会输出并不存在于输入中的 0。</p><p><strong>检查方法：</strong>维护最值时，优先用第一个真实候选值初始化。</p></section><h2 id="common-errors">常见错误</h2>\n<ol>\n<li><strong>把任务目标当作可执行步骤。</strong> 现象：描述只有“求出最大值”。原因：没有初始化、比较和更新动作。修正：写出 <code>maximum=a</code>，再依次比较 <code>b</code> 和 <code>c</code>。复验：逐步指出每句话由哪条语句执行。</li>\n<li><strong>四种描述的条件方向不一致。</strong> 现象：流程图在“是”出口更新，伪代码却在条件为假时更新。原因：互译时只看关键词，没有核对真假路径。修正：统一为“新数大于当前最大值时更新”。复验：代入 <code>3 8 5</code>，确认第一次走“是”、第二次走“否”。</li>\n<li><strong>追踪时读取旧状态。</strong> 现象：比较 <code>c</code> 时仍使用初始值 3。原因：漏记比较 <code>b</code> 后的更新。修正：每一步先记录更新后状态，再进入下一步。复验：表中必须出现 <code>maximum=3→8→8</code>。</li>\n<li><strong>最大值凭空初始化为 0。</strong> 现象：输入 <code>-4 -9 -2</code> 却输出 0。原因：0 不是三个候选值之一。修正：使用 <code>maximum=a</code>。复验：全负数样例应精确输出 <code>-2</code>。</li>\n<li><strong>把一个样例当成全部证明。</strong> 现象：只测 <code>3 8 5</code> 正确就停止检查。原因：其他路径和边界还没有执行。修正：补测两次都假、两次都真、相等和全负数。复验：五组代表输入全部得到登记输出。</li>\n</ol>\n<h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2>\n<p>下面五题是本课的独立自测，分别检查算法特征、四种描述互译和具体状态追踪。完整提交后记录会保存在当前设备；重新测试时以最近一次完整结果覆盖旧结果。</p>\n</div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 数组最多能放多少个元素，叫作容量；这次实际用了多少个，叫作元素个数。元素在数组中的位置编号，叫作下标。数组的开头、末尾以及允许访问的最小和最大下标，都是需要检查的边界。</li>\n<li><strong>常用语法：</strong> 用 <code>int a[100] = {};</code> 声明定长数组；0-based 的前 <code>n</code> 项是 <code>a[0]</code> 到 <code>a[n - 1]</code>，遍历写成 <code>for (int i = 0; i &lt; n; i++)</code>。</li>\n<li><strong>最小示例：</strong> <code>int score[5] = {72, 80};</code> 会把前两项设为 72、80，其余三项自动设为 0；本次若只使用两项，循环仍只走 <code>i &lt; 2</code>。</li>\n<li><strong>关键易错点：</strong> <code>a[n]</code> 已越过本次有效元素；只有 <code>n</code> 等于容量时，<code>a[n]</code> 才越过数组本身。写程序时既要检查 <code>n</code> 不超过容量，也要按本次实际元素个数控制访问。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>先看一个具体问题：要保存 30 名同学的成绩，若分别声明 30 个变量，输入、统计和查找都会写成大量重复语句。一维数组把一组<strong>同类型、有顺序</strong>的数据放在连续的位置中，再用编号访问其中一项，循环就能按同一规则处理全部数据。</p>\n<p>本课只解决三个核心问题：怎样声明数组并安全访问有效位置；怎样在遍历中维护计数、查找和最值状态；怎样根据访问式和值域反推相邻、移动、标记和计数的合法范围。七个稳定知识区继续完整保留，但都服务于这三个核心。</p>\n<p>本课使用编译时已经确定长度的原生一维数组，输入数据最多 100 项。输入输出、变量、分支和循环沿用已经学过的写法，不重新展开。</p>\n<h2 id="核心概念">核心概念</h2>\n<p><strong>核心一：安全存取。</strong> 先分清固定容量、实际元素个数 <code>n</code> 和合法位置，再读写数组。</p>\n<p><strong>核心二：遍历维护状态。</strong> <code>i</code> 决定本轮访问哪一项，计数器、查找位置或最值变量保存已经处理部分的结果。</p>\n<p><strong>核心三：从访问式和值域反推限制。</strong> 看见 <code>a[i - 1]</code>、<code>a[i + 1]</code> 或 <code>cnt[value]</code>，要先证明这些位置不会超出可用范围。</p>\n<h2 id="kp-01">01 数组声明、容量与下标</h2>\n<h3 id="数组声明与容量">数组声明与容量</h3>\n<p>声明 5 个整数位置的语法如下：</p>\n<pre><code>int score[5]; // 声明数组 score，预留 5 个同类型元素位置</code></pre>\n<p><code>int</code> 是每个元素的类型，<code>score</code> 是数组名，<code>5</code> 是容量。有效下标从 0 开始，因此五个位置依次是 <code>score[0]</code>、<code>score[1]</code>、<code>score[2]</code>、<code>score[3]</code>、<code>score[4]</code>。</p>\n<p>如果希望所有位置一开始都是 0，可以写：</p>\n<pre><code>int score[5] = {}; // 声明数组 score，预留 5 个元素位置并全部初始化为 0</code></pre>\n<p>也可以在声明时直接给出元素：</p>\n<pre><code>int score[] = {72, 80, 91, 65}; // 声明数组 score，并按初始化列表依次写入各元素</code></pre>\n<p>这里初始化列表中有 4 个数，所以编译器确定数组容量为 4，有效下标是 0 到 3。</p>\n<p>初始化时也可以只给前几项赋值：</p>\n<pre><code>int a[5] = {1, 2}; // 声明数组 a，并按初始化列表依次写入各元素</code></pre>\n<p>这时 <code>a[0]</code> 是 1，<code>a[1]</code> 是 2，没有写出的后三项会自动初始化为 0。它和只写 <code>int a[5];</code> 不同：后者没有为这五项指定初值，不能直接把其中的值当作 0 使用。</p>\n<p>题目通常还会给出实际元素个数 <code>n</code>。容量表示“最多能放多少项”，<code>n</code> 表示“本次真正用了多少项”，两者不能混为一谈。</p>\n<pre><code>const int CAPACITY = 100; // 定义只读常量 CAPACITY，值为 100，统一控制容量或规则边界int a[CAPACITY] = {}; // 声明数组 a，预留 CAPACITY 个元素位置并全部初始化为 0</code></pre>\n<p><code>CAPACITY</code> 是不会改变的容量。程序必须保证 <code>n &lt;= CAPACITY</code>，并且只能处理本次输入的前 <code>n</code> 项。</p>\n<pre><code>下标       0        1        2        3        4元素     a[0]     a[1]     a[2]     a[3]     a[4]存储     [ int ][ int ][ int ][ int ][ int ]</code></pre>\n<p>数组元素类型一旦确定，每个位置都按同一种类型解释；数组长度也必须在编译时确定。本课使用的原生数组是固定长度，不能在运行中把 <code>int a[5]</code> 自动扩展成 <code>int a[6]</code>，所以要先根据题目数据范围预留足够容量。</p>\n<p>数组占用的总字节数按“<strong>容量 × 每个元素的字节数</strong>”计算。在常见 GCC/OJ 环境中，一个 <code>int</code> 通常占 4 字节，因此 <code>int a[5]</code> 通常占 <code>5 × 4 = 20</code> 字节，<code>int a[100]</code> 通常占 <code>100 × 4 = 400</code> 字节。<code>int</code> 的准确大小由编译器和平台决定，所以程序应使用 <code>sizeof</code> 核对，不能把 4 当作所有环境永远不变的语言规则。</p>\n<p>实际元素个数 <code>n</code> 不会改变数组已经分配的容量。即使 <code>int a[100]</code> 本次只读入 <code>n = 7</code> 个数，这个数组仍为 100 个 <code>int</code> 位置预留空间；循环只应访问 <code>a[0]</code> 到 <code>a[6]</code>。因此“分配多少空间”看容量，“本次处理多少数据”看 <code>n</code>，两项要分别检查。</p>\n<p>对在当前作用域中声明的原生数组，可以用 <code>sizeof</code> 计算容量：</p>\n<pre><code>int a[5] = {3, 1, 4, 1, 5}; // 声明数组 a，并按初始化列表依次写入各元素int length = sizeof(a) / sizeof(a[0]); // 用数组总字节数除以单个元素字节数，计算数组容量cout &lt;&lt; length;  // 输出 5</code></pre>\n<p><code>sizeof(a)</code> 得到整个数组占用的字节数，<code>sizeof(a[0])</code> 得到一个元素占用的字节数，二者相除就是元素个数。这个公式只适用于当前作用域中完整的原生数组，不要把它当成适用于任何变量的通用求长度公式。</p>\n<p>代入常见的 4 字节 <code>int</code> 可以完整算一遍：<code>sizeof(a)</code> 通常是 20，<code>sizeof(a[0])</code> 通常是 4，所以 <code>20 / 4 = 5</code>。这里相除后得到的是<strong>元素容量 5</strong>，不是最后一个下标；最后一个有效下标仍是 <code>5 - 1 = 4</code>。</p>\n<p><strong>原生数组不能整体赋值。</strong></p>\n<p>两个普通变量可以写 <code>a = b;</code>，但两个原生数组不能直接写成 <code>destination = source;</code>。要复制数组，必须逐项赋值：</p>\n<pre><code>int source[5] = {2, 4, 6, 8, 10}; // 声明数组 source，并按初始化列表依次写入各元素int destination[5] = {}; // 声明数组 destination，预留 5 个元素位置并全部初始化为 0\nfor (int i = 0; i &lt; 5; i++) { // 从本循环起点开始，只在 i &lt; 5 成立时访问元素，每轮再执行 i++    destination[i] = source[i]; // 把源数组下标 i 的值复制到目标数组同一位置} // 下标 i 的元素复制完成，i++ 后继续复制下一个位置</code></pre>\n<p>循环第 <code>i</code> 轮只复制下标为 <code>i</code> 的一个元素。这样既能看清复制过程，也能根据实际元素个数 <code>n</code> 控制需要复制的范围。</p>\n<h2 id="kp-02">02 数组输入与遍历</h2>\n<h3 id="数组输入与遍历">数组输入与遍历</h3>\n<p>数组声明后，需要通过下标访问具体元素。下面的片段读入前 <code>n</code> 项，再按原顺序输出：</p>\n<pre><code>// 从下标 0 到 n - 1 依次读入本次真正使用的每个位置。for (int i = 0; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    cin &gt;&gt; a[i]; // 把第 i + 1 个输入值存入数组下标 i} // 当前元素已写入 a[i]，i++ 后读取下一个位置\n// 按同样的有效下标范围依次输出数组元素。for (int i = 0; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    cout &lt;&lt; a[i] &lt;&lt; " "; // 输出当前数组元素，并为下一项保留一个分隔空格} // a[i] 已输出，i++ 后继续输出下一个有效元素</code></pre>\n<p>循环变量 <code>i</code> 表示当前位置，<code>a[i]</code> 表示这个位置保存的数值。每轮结束后 <code>i++</code>，也就是让下标增加 1。</p>\n<p>第一次循环访问 <code>a[0]</code>。当 <code>i = n - 1</code> 时访问最后一个有效元素；下一次 <code>i</code> 变成 <code>n</code>，条件 <code>i &lt; n</code> 不成立，循环结束，因此不会访问 <code>a[n]</code>。</p>\n<h2 id="kp-03">03 数组统计与计数</h2>\n<h3 id="数组计数">数组计数</h3>\n<p>遍历数组时，可以让一个变量保存已经处理过的结果。求和时，<code>sum</code> 从 0 开始，每轮把当前元素加入总和：</p>\n<p>数组求和过程：5、3、8。<code>sum</code> 从 0 开始，依次变成 5、8、16，也就是 <code>0 → 5 → 8 → 16</code>；每一步都能用“原来的总和 + 当前元素”复算。</p>\n<pre><code>int sum = 0; // 用 sum 记录已经遍历过的元素总和；从 0 开始表示尚未累计任何结果\n// 从左到右处理全部 n 个元素，每轮把当前值加入总和。for (int i = 0; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    sum = sum + a[i]; // 把当前数组元素加入总和，保留到下一轮继续累计} // a[i] 已加入 sum，i++ 后继续累计下一个元素</code></pre>\n<p>计数器也从 0 开始。每当当前元素满足条件，就让计数器增加 1：</p>\n<pre><code>int positiveCount = 0; // 用 positiveCount 记录已经发现的正数个数；从 0 开始表示尚未累计任何结果\n// 依次检查每个数组元素是否为正数。for (int i = 0; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    if (a[i] &gt; 0) {  // 当前元素大于 0，说明找到一个正数，正数个数加 1        positiveCount++; // 当前元素是正数，已发现的正数个数增加 1    } // 当前元素是否贡献一次正数计数已经确定} // 当前元素是否为正数已经判断完，i++ 后检查下一项</code></pre>\n<p>这里判断的是元素 <code>a[i]</code> 是否为正数，不是判断下标 <code>i</code>。</p>\n<p>求平均值要先得到总和，再除以实际元素个数 <code>n</code>。平均值只在 <code>n &gt; 0</code> 时有意义；若先做两个整数的除法，小数部分会被舍去，所以要让其中一个运算数成为浮点数：</p>\n<pre><code>long long sum = 0; // 用 long long 保存总和，给多个 int 相加留下更大范围for (int i = 0; i &lt; n; i++) { // 只累计本次输入的前 n 个有效元素    sum = sum + a[i]; // 把当前元素加入已处理部分的总和} // 结束求和循环，此时 sum 已包含全部 n 个元素double average = 1.0 * sum / n; // n 大于 0 时计算小数平均值，1.0 避免整数除法截断</code></pre>\n<p>例如数组 <code>2、3、5</code> 的总和是 10，平均值是 <code>10.0 / 3 ≈ 3.333</code>；若写成 <code>10 / 3</code>，两个运算数都是整数，只会先得到 3。题目若允许 <code>n = 0</code>，必须先单独处理空数组，不能除以 0。</p>\n<h2 id="kp-04">04 顺序查找</h2>\n<h3 id="数组查找">数组查找</h3>\n<p>查找目标值时，可以用 <code>bool found = false;</code> 表示开始时还没有找到，用 <code>position = -1</code> 表示没有合法下标。</p>\n<pre><code>bool found = false; // 开始查找前还没有答案，用 false 记录这一状态int position = -1; // -1 不是合法下标，用它表示目前尚未找到\n// 从第一个元素开始寻找目标值，最晚检查到下标 n - 1。for (int i = 0; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    if (a[i] == target) {  // 当前元素等于目标值，记录第一次出现的位置并停止查找        found = true; // 当前元素等于 target，记录已经找到目标        position = i; // 保存目标第一次出现的下标 i，供循环外输出        break; // 条件“a[i] == target”已经成立，立即停止当前循环，保留第一个答案    } // 一旦进入此分支，前一行 break 会直接跳出外层 for 循环} // 未找到时才执行 i++ 检查下一项；找到时从这里跳到循环后</code></pre>\n<p>题目要求第一次出现的位置，因此找到后立刻 <code>break</code>，不再检查后面的元素。<code>position</code> 保存 C++ 下标；如果题目问“第几个元素”，输出时再使用 <code>position + 1</code>。</p>\n<h2 id="kp-05">05 最大值与最小值</h2>\n<h3 id="数组最大值和最小值">数组最大值和最小值</h3>\n<p>只要题目保证 <code>n &gt;= 1</code>，就可以用第一个真实元素初始化最大值：</p>\n<pre><code>int maximum = a[0]; // 用第一个真实元素初始化最大值，兼容全为负数的输入int minimum = a[0]; // 用第一个真实元素初始化最小值，避免使用范围外的假值\n// a[0] 已作为初值，因此从下标 1 开始检查剩余元素。for (int i = 1; i &lt; n; i++) { // 从本循环起点开始，只在 i &lt; n 成立时访问元素，每轮再执行 i++    if (a[i] &gt; maximum) {  // 当前元素比已记录的最大值更大，需要更新最大值        maximum = a[i]; // 用更大的当前元素更新已处理部分的最大值    } // maximum 已保存前 i + 1 个元素中的最大值，下面继续检查最小值\n    if (a[i] &lt; minimum) {  // 当前元素比已记录的最小值更小，需要更新最小值        minimum = a[i]; // 用更小的当前元素更新已处理部分的最小值    } // minimum 已保存前 i + 1 个元素中的最小值，本轮比较完成} // a[i] 与当前最大值、最小值均已比较，i++ 后处理下一项</code></pre>\n<p>循环从下标 1 开始，因为 <code>a[0]</code> 已经作为初值。不能习惯性把最大值设为 0；当数组全部是负数时，0 可能根本不在输入中。</p>\n<h2 id="kp-06">06 数组处理与状态变化</h2>\n<h3 id="数组遍历与状态变化">数组遍历与状态变化</h3>\n<p>数组处理不只读取数据，也会在合法位置内改变数据。交换两个元素时，先用临时变量保存左边旧值，再覆盖左边，最后把旧值写到右边。反向排列就是让 <code>left</code> 从 0 向右、<code>right</code> 从 <code>n - 1</code> 向左，反复交换，直到两者相遇。输入 <code>1、2、3、4、5</code> 时，两次交换后得到 <code>5、4、3、2、1</code>；中间的 3 不需要和自己交换。</p>\n<p>在固定容量数组中插入或删除时，核心是<strong>先移动，再改变实际元素个数</strong>：</p>\n<pre><code>for (int i = n; i &gt; position; i--) { // 从末尾向前腾出 position 位置，保证旧元素不被覆盖    a[i] = a[i - 1]; // 把前一个元素向右移动一格} // 右侧元素全部移动后，position 已经空出a[position] = value; // 把新值写入指定插入位置n++; // 插入成功后，实际元素个数增加 1</code></pre>\n<pre><code>for (int i = position; i + 1 &lt; n; i++) { // 从删除位置向后处理到原最后一项之前    a[i] = a[i + 1]; // 用右侧元素覆盖当前空位，相当于整体左移} // 原最后一项已经不再属于有效数据n--; // 删除成功后，实际元素个数减少 1</code></pre>\n<p>去除重复值时，也可以用两层循环做直白检查：<code>uniqueCount</code> 表示数组前部已经保留了多少个不同值；每读到一项，就只和前 <code>uniqueCount</code> 项比较，没有出现过才写到保留区末尾。输入 <code>3、1、3、2、1</code> 时，保留区依次变为 <code>3</code>、<code>3 1</code>、<code>3 1</code>、<code>3 1 2</code>、<code>3 1 2</code>，最终 <code>uniqueCount = 3</code>。这一段只帮助理解“外层选当前项、内层查已保留项”的状态，不作为本页第二条完整程序主线。</p>\n<h2 id="数组遍历与状态变化-1">数组遍历与状态变化</h2>\n<p>用数组 <code>4、-2、7、7、1</code> 同时观察正数计数、最大值和最大值第一次出现的位置。<code>a[0]</code> 已经作为最值初值，所以比较循环从 <code>i = 1</code> 开始；严格使用 <code>&gt;</code>，遇到第二个 7 时不会覆盖第一次出现的位置 2。</p>\n<pre><code>bool appeared[101] = {}; // 下标 0 到 100 分别代表对应数值，false 表示尚未出现int cnt[101] = {}; // 下标 0 到 100 分别保存对应数值的出现次数，初始均为 0\nfor (int i = 0; i &lt; n; i++) { // 循环 n 次，每轮读取并统计一个输入值    int value; // 暂存本轮输入，随后直接把它当作 appeared 和 cnt 的下标    cin &gt;&gt; value; // 读入第 i + 1 个待统计数值    appeared[value] = true; // 把 value 对应的标记设为 true，表示这个值至少出现过一次    cnt[value]++; // 当前又读到一个 value，把它的出现次数增加 1} // 本轮输入值的标记和次数均已更新，i++ 后读取下一个数</code></pre>\n<p><code>appeared[value]</code> 是标记数组：只回答这个数是否出现。<code>cnt[value]</code> 是计数数组：记录这个数出现了多少次。数组长度写成 101，是因为要使用下标 <code>0</code> 到 <code>100</code>。只有题目保证数值范围时才能这样做；若 <code>value</code> 为负数或大于 100，就会越界。</p>\n<pre><code>if (!appeared[x]) { // 目标值从未出现时，输出对应的缺失提示    cout &lt;&lt; x &lt;&lt; " 没有出现"; // 标记为 false，说明 x 在输入中一次也没有出现} // appeared[x] 的缺失判断结束，下面再用次数区分缺失和重复\nif (cnt[x] == 0) { // 出现次数为 0 时，说明目标值在输入中缺失    cout &lt;&lt; x &lt;&lt; " 缺失"; // 出现次数为 0，输出 x 缺失} else if (cnt[x] &gt;= 2) { // 上一情况不成立时，继续检查另一种边界情况    cout &lt;&lt; x &lt;&lt; " 重复出现"; // 出现次数超过 1，输出 x 重复} // cnt[x] 为 0 或至少为 2 的两种情况已经判断完毕</code></pre>\n<p><code>!appeared[x]</code> 和 <code>cnt[x] == 0</code> 都能判断缺失；<code>cnt[x] &gt;= 2</code> 表示至少出现两次，可以判断重复。访问这些位置前仍要保证 <code>x</code> 在 0 到 100 内。</p>\n<p>输入 <code>2、5、2</code> 时，<code>cnt[2]</code> 依次变为 1、1、2，<code>cnt[5]</code> 依次变为 0、1、1。完整统计程序仍然只是“读一个值、先检查 0 到 100、再执行 <code>cnt[value]++</code>”的重复；本页保留这个可复算结果，不再复制第二份带 <code>main</code> 的程序。</p>\n<h2 id="typical-example">典型例题</h2>\n<p>反向输出不改变数组内容，只把访问顺序改为 <code>n - 1、n - 2、…、0</code>。例如 <code>{4, 7, 2}</code> 依次访问下标 2、1、0，输出 <code>2 7 4</code>；这是遍历方向的变化，不再另设完整程序。</p>\n<h3 id="相邻元素">相邻元素</h3>\n<p>相邻元素题每轮要访问一对元素。使用 <code>a[i - 1]</code> 和 <code>a[i]</code> 时，<code>i</code> 必须从 1 开始：</p>\n<pre><code>int largestDifference = 0;// i 从 1 开始，保证 a[i - 1] 是合法下标。for (int i = 1; i &lt; n; i++) {    int difference = a[i] - a[i - 1];    if (difference &lt; 0) {        difference = -difference;    }    if (difference &gt; largestDifference) {        largestDifference = difference;    }}</code></pre>\n<p>第 1 轮访问 <code>a[0]</code> 和 <code>a[1]</code>，最后一轮访问 <code>a[n - 2]</code> 和 <code>a[n - 1]</code>。这段代码要求 <code>n &gt;= 2</code>。</p>\n<p>为避免两个很大的 <code>int</code> 相减溢出，本课完整示例约定每个输入整数在 <code>-10000</code> 到 <code>10000</code> 之间，因此差值一定能放进 <code>int</code>。</p>\n<p>下面的唯一完整程序只完成一项可迁移任务：读入 <code>n</code> 个整数，输出最大值和它第一次出现的 0-based 位置。输入约定为 <code>1 &lt;= n &lt;= 100</code>，每个数都在 C++ <code>int</code> 可表示范围内；程序只比较整数，不做可能溢出的算术。</p>\n<h3 id="完整程序数组综合练习">完整程序：数组综合练习</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    const int CAPACITY = 100;    int a[CAPACITY] = {};\n    int n;    cin &gt;&gt; n;\n    // 元素个数必须为正，并且不能超过固定容量，否则后续访问会越界。    if (n &lt; 1 || n &gt; CAPACITY) {        cout &lt;&lt; "Invalid" &lt;&lt; \'\\n\';        return 0;    }\n    for (int i = 0; i &lt; n; i++) {        cin &gt;&gt; a[i];    }\n    // 用第一个真实元素初始化，全部为负数时也不会凭空得到 0。    int maximum = a[0];    int position = 0;\n    // a[0] 已经作为初值，所以从下标 1 开始比较。    for (int i = 1; i &lt; n; i++) {        // 只在严格更大时更新，相等时保留最大值第一次出现的位置。        if (a[i] &gt; maximum) {            maximum = a[i];            position = i;        }    }\n    cout &lt;&lt; maximum &lt;&lt; \' \' &lt;&lt; position &lt;&lt; \'\\n\';\n    return 0;}</code></pre>\n<p>完成这道典型例题的成功标准是：合法输入只输出“最大值 空格 第一次出现位置”；非法元素个数输出 <code>Invalid</code>；重复最大值不覆盖第一次位置；输出末尾换行。</p>\n<p><strong>数组综合程序测试案例</strong>必须覆盖下面八类真值，并且全部成立：</p>\n<ol>\n<li><code>5\\n4 -2 7 7 1\\n</code> 输出 <code>7 2</code>。</li>\n<li><code>1\\n-5\\n</code> 输出 <code>-5 0</code>。</li>\n<li><code>4\\n3 3 3 3\\n</code> 输出 <code>3 0</code>。</li>\n<li><code>3\\n-8 -3 -5\\n</code> 输出 <code>-3 1</code>。</li>\n<li><code>5\\n1 2 3 4 9\\n</code> 输出 <code>9 4</code>。</li>\n<li><code>5\\n9 1 8 2 7\\n</code> 输出 <code>9 0</code>。</li>\n<li><code>0\\n</code> 输出 <code>Invalid</code>。</li>\n<li><code>n = 100</code> 且依次输入 1 到 100 时，输出 <code>100 99</code>。</li>\n</ol>\n<section><h3>把循环写到 i &lt;= n 会多访问一项</h3><div><div><strong><span>×</span> 错误写法</strong><code>有 n 个元素时使用 for (int i = 0; i &lt;= n; i++) 访问 a[i]</code></div><div><strong><span>✓</span> 正确写法</strong><code>0-based 遍历写为 i = 0 到 i &lt; n；需要前一项的相邻扫描写为 i = 1 到 i &lt; n。</code></div></div><p><strong>原因：</strong>最后一次 i 等于 n，而有效的最后一项是 a[n - 1]；a[n] 已越过有效数据。</p><p><strong>检查方法：</strong>先写出最后一次循环要访问的下标，再反推循环条件。</p></section><h2 id="common-errors">常见错误</h2><div><ol>\n<li><strong>把容量当成元素个数。</strong> 现象：声明 <code>int a[100]</code> 后总是处理 100 项。原因：100 只是最多可放的位置数，本次有效数据由 <code>n</code> 决定。修正：输入前检查 <code>n</code>，遍历只走 <code>i &lt; n</code>。复验：<code>n = 3</code> 时只访问 <code>a[0]</code> 到 <code>a[2]</code>。</li>\n<li><strong>混用两套编号。</strong> 现象：输入写进 <code>a[0]</code> 到 <code>a[n - 1]</code>，输出却从 <code>a[1]</code> 开始。原因：0-based 与 1-based 混在同一段代码里。修正：一段程序只采用一套约定。复验：逐项写出第一个和最后一个实际访问的位置。</li>\n<li><strong>循环写成 <code>i &lt;= n</code>。</strong> 现象：最后一轮访问 <code>a[n]</code>。原因：最后一个有效位置是 <code>n - 1</code>。修正：0-based 遍历使用 <code>i &lt; n</code>。复验：代入 <code>n = 5</code>，最后一次循环应为 <code>i = 4</code>。</li>\n<li><strong>输入前不检查 <code>n</code>。</strong> 现象：<code>n = 101</code> 时仍向容量 100 的数组写入。原因：循环开始前没有验证实际元素个数。修正：先判断 <code>n &lt; 1 || n &gt; CAPACITY</code>。复验：<code>n = 0</code> 和 <code>n = 101</code> 都输出 <code>Invalid</code>。</li>\n<li><strong>空数据仍读取首项。</strong> 现象：<code>n = 0</code> 时执行 <code>maximum = a[0]</code>。原因：首项并不是本次有效数据。修正：在初始化最值前排除空数据。复验：只有 <code>1 &lt;= n &lt;= CAPACITY</code> 才进入读数和最值循环。</li>\n<li><strong>相邻扫描从 0 开始。</strong> 现象：第一轮尝试访问 <code>a[-1]</code>。原因：表达式含 <code>a[i - 1]</code>，却没有反推 <code>i</code> 的最小值。修正：从 <code>i = 1</code> 开始。复验：第一对是 <code>a[0]</code> 与 <code>a[1]</code>，最后一对是 <code>a[n - 2]</code> 与 <code>a[n - 1]</code>。</li>\n<li><strong>最大值初始化为 0。</strong> 现象：全负数输入得到不存在的最大值 0。原因：初值不是任何真实候选。修正：使用 <code>a[0]</code> 初始化。复验：<code>-8 -3 -5</code> 必须得到 <code>-3</code>。</li>\n<li><strong>相等时覆盖第一次位置。</strong> 现象：<code>4 -2 7 7 1</code> 得到位置 3。原因：更新条件写成了 <code>&gt;=</code>。修正：只在 <code>a[i] &gt; maximum</code> 时更新。复验：两个 7 中保留下标 2。</li>\n<li><strong>直接整体赋值。</strong> 现象：写出 <code>destination = source;</code>。原因：原生数组不能用普通赋值语句整体复制。修正：按相同合法位置逐项赋值。复验：逐轮核对 <code>destination[i] = source[i]</code>。</li>\n<li><strong>把任意数值当位置。</strong> 现象：负数或大于 100 的值直接用于 <code>appeared[value]</code>。原因：没有证明值域落在 0 到 100。修正：只在题目保证或程序检查值域后访问。复验：分别检查最小值 0、最大值 100 和两个范围外反例。</li>\n</ol></div>\n<p>本课收束为三个动作：先用容量、<code>n</code> 和合法位置保证安全存取；再在遍历中维护真实状态；最后根据访问式和值域反推限制。计数、查找、最值、相邻、移动和按值记录都只是这三个动作的不同应用。</p>\n<h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2>\n<p>下面五题是本课的独立自测。题目、答案和当前设备上的记录继续保持原有含义；完成自测不会自动把本课标记为已复习或已掌握。</p>\n</div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<ul>\n<li><strong>核心结论：</strong> 一个字符也没有、长度为 0 的字符串，叫作空串。原字符串中连续的一段字符，叫作子串。字符数组中有效文本最后一个字符后面的 <code>\\0</code> 用来告诉程序字符串在哪里结束，因此它叫作结束标记。</li>\n<li><strong>常用语法：</strong> 用 <code>getline(cin, s)</code> 读取包含空格的一整行；用 <code>s.length()</code> 得到字符个数；合法下标是 <code>0</code> 到 <code>s.length() - 1</code>；用 <code>find</code>、<code>substr</code> 和 <code>replace</code> 处理连续文本。</li>\n<li><strong>最小示例：</strong> <code>string s = "aB2";</code> 中，<code>s[0]</code> 是 <code>\'a\'</code>。把小写字母逐个改成大写后得到 <code>"AB2"</code>，原来的大写字母和数字保持不变。</li>\n<li><strong>关键易错点：</strong> 空串不能访问 <code>s[0]</code>；<code>find</code> 返回 0 也表示在开头找到，失败才返回 <code>string::npos</code>；字符数组容量为 <code>N</code> 时最多保存 <code>N - 1</code> 个普通字符，最后要留给 <code>\\0</code>。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>字符串用来保存和处理文本。本课先区分一个字符、一串字符和固定容量字符数组，再学习输入、长度、下标、拼接、查找和替换。</p>\n<h2 id="核心概念">核心概念</h2>\n<p>本课只解决三个核心问题：<strong>核心一：文本表示与安全输入</strong>，先判断数据是一个字符、一段文本还是固定容量字符数组，再选择不会丢失空格的输入方式；<strong>核心二：按下标遍历、修改与比较</strong>，只访问合法位置并解释每轮怎样改变文本；<strong>核心三：连接、截取、查找、替换与分割</strong>，根据位置、数量和修改后的新长度检查边界。八个知识章节完整保留，但都服务于这三个核心。</p>\n<p>学完本课，你需要会：</p>\n<ul>\n<li>使用 <code>char</code> 保存一个字符，使用 <code>string</code> 保存一段文本；</li>\n<li>根据文本是否包含空格选择 <code>cin &gt;&gt;</code> 或 <code>getline</code>；</li>\n<li>使用长度和下标遍历字符串，并在访问前检查边界；</li>\n<li>使用 <code>+</code>、<code>find</code>、<code>substr</code> 和 <code>replace</code> 处理文本；</li>\n<li>为字符数组保留 <code>\\0</code> 结束标记，避免写出缓冲区。</li>\n</ul>\n<p>本课不使用指针、函数、递归、文件、异常处理或更高等级的字符串算法。</p>\n<pre><code>char letter = \'A\'; // 用 letter 保存单个英文字母字符；当前值由 \'A\' 得到char digit = \'7\'; // 用 digit 保存字符形式的数字 7，而不是整数 7；当前值由 \'7\' 得到</code></pre>\n<p><code>string</code> 保存一串字符，字符串使用双引号：</p>\n<pre><code>string word = "Apple"; // 用 word 保存不含空格的单词文本；当前值由 "Apple" 得到string sentence = "I like C++"; // 用 sentence 保存可能包含空格的一整行文本；当前值由 "I like C++" 得到</code></pre>\n<p><code>\'A\'</code> 是一个字符，<code>"A"</code> 是长度为 1 的字符串。比较当前位置的单个字符时写 <code>text[i] == \'A\'</code>；查找一段文本时写 <code>text.find("cat")</code>。</p>\n<p>字符数组也能保存文本，但它的容量在声明时已经固定：</p>\n<pre><code>char word[10] = "go"; // 字符串字面量依次写入 \'g\'、\'o\'、\'\\0\'，其余位置补 0</code></pre>\n<p>数组中依次保存 <code>\'g\'</code>、<code>\'o\'</code> 和 <code>\\0</code>。<code>\\0</code> 是数值为 0 的结束标记，不是可显示的字符 <code>\'0\'</code>。</p>\n<p>在 C++ 中，<code>sizeof(char)</code> 规定为 1 字节，所以 <code>char word[10]</code> 一共占 10 字节。用 <code>"go"</code> 初始化后，前 3 个位置依次是 <code>\'g\'</code>、<code>\'o\'</code>、<code>\'\\0\'</code>，其余位置也会被初始化为 0。这里要分清三个量：数组容量是 10，文本长度是 2，为文本和结束标记实际用到的位置数是 3。</p>\n<p>对这个完整字符数组，<code>sizeof(word)</code> 得到固定容量 10；<code>strlen(word)</code> 需要 <code>&lt;cstring&gt;</code>，它从开头数到第一个 <code>\\0</code>，结果是文本长度 2。若使用 <code>string text = "go";</code>，应写 <code>text.size()</code> 或 <code>text.length()</code> 得到 2，不能用 <code>sizeof(text)</code> 当作字符个数，因为 <code>string</code> 对象还要保存自己的管理信息。</p>\n<p>容量为 10 的字符数组若要保存以 <code>\\0</code> 结尾的普通文本，最多只能放 9 个可显示字符。<code>word[9]</code> 是最后一个存储位置，不能在已经放满 10 个可显示字符后再把 <code>\\0</code> 写到 <code>word[10]</code>，因为下标 10 已经越界。</p>\n<table>\n<thead>\n<tr>\n<th>表示方式</th>\n<th>保存内容</th>\n<th>示例</th>\n<th>主要边界</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>char</code></td>\n<td>一个字符</td>\n<td><code>char c = \'x\';</code></td>\n<td>使用单引号</td>\n</tr>\n<tr>\n<td><code>string</code></td>\n<td>可变长度文本</td>\n<td><code>string s = "cat";</code></td>\n<td>空串不能访问首尾字符</td>\n</tr>\n<tr>\n<td><code>char s[105]</code></td>\n<td>固定容量字符序列</td>\n<td><code>char s[105] = {};</code></td>\n<td>最后要为 <code>\\0</code> 留一个位置</td>\n</tr>\n</tbody>\n</table>\n<h2 id="kp-02">02 字符串输入</h2>\n<h3 id="字符串输入">字符串输入</h3>\n<p><code>cin &gt;&gt; text</code> 读到空格、制表符或换行时停止，适合读取一个不含空格的单词：</p>\n<pre><code>string word; // 用 word 保存不含空格的单词文本；值将在紧接着的输入或判断分支中确定cin &gt;&gt; word; // 依次读入 word（不含空格的单词文本）</code></pre>\n<p><code>getline(cin, text)</code> 会读取一整行，空格也是文本的一部分：</p>\n<pre><code>string sentence; // 用 sentence 保存可能包含空格的一整行文本；值将在紧接着的输入或判断分支中确定getline(cin, sentence); // 读取一整行文本（包括中间的空格）并保存到 sentence</code></pre>\n<p>如果前面刚使用 <code>cin &gt;&gt; n</code> 读过数据，输入流中通常还留着一个换行符。在题目保证数字后立即换行时，可以先忽略这个换行，再读取下一整行：</p>\n<pre><code>int n; // 用 n 保存数字输入，用来演示 getline 前的换行处理；值将在紧接着的输入或判断分支中确定cin &gt;&gt; n; // 依次读入 n（数字输入，用来演示 getline 前的换行处理）cin.ignore(); // 忽略数字后留下的一个换行符，避免 getline 立即读到空行\nstring sentence; // 用 sentence 保存可能包含空格的一整行文本；值将在紧接着的输入或判断分支中确定getline(cin, sentence); // 读取一整行文本（包括中间的空格）并保存到 sentence</code></pre>\n<p>这里 <code>ignore()</code> 只处理紧跟在数字后的一个字符，因此这段写法依赖“数字后立即换行”的输入格式。若题目输入格式不同，要按题意处理分隔字符。</p>\n<h2 id="kp-03">03 长度、空串与下标</h2>\n<h3 id="字符串长度空串和下标">字符串长度、空串和下标</h3>\n<p><code>size()</code> 和 <code>length()</code> 都返回字符串中的字符个数。本站示例统一使用 <code>length()</code>；官方题中看到 <code>size()</code> 时，要知道它们在这里作用相同。</p>\n<p><code>empty()</code> 用来判断字符串是否为空：</p>\n<pre><code>if (text.empty()) { // empty() 为 true 表示长度为 0，此时没有任何字符可访问    cout &lt;&lt; "Empty"; // 输出提示 Empty，表示当前判断分支的结果} else { // 条件“text.empty()”不成立时，改走这一条处理路径    cout &lt;&lt; text[0]; // 字符串非空，输出下标 0 的首字符} // 条件“text.empty()”不成立时的替代动作到此完成</code></pre>\n<p>如果要读取首个字符，也要先保护空串：</p>\n<pre><code>string s; // 用 s 保存本例正在遍历或修改的字符串；值将在紧接着的输入或判断分支中确定getline(cin, s); // 读取一整行文本（包括中间的空格）并保存到 s\nif (!s.empty()) { // 只有字符串非空时才能安全访问下标 0    char first = s[0]; // 用 first 保存字符串的第一个字符；当前值由 s[0] 得到    cout &lt;&lt; first; // 输出非空字符串的首字符} // 空串不会访问 s[0]；非空时 first 已保存首字符</code></pre>\n<p>字符串位置和长度使用 <code>string::size_type</code> 保存。这是 <code>string</code> 专门提供的非负整数类型，<code>length()</code> 的返回值也是这个类型。先了解它，再用它遍历字符串，就不会把普通 <code>int</code> 和字符串长度混在一起比较。</p>\n<p>长度为 5 的字符串，有效下标是 0、1、2、3、4。不存在下标 5。访问首项 <code>text[0]</code> 或末项 <code>text[text.length() - 1]</code> 前，必须先确认字符串不是空串。</p>\n<p><strong>核心二：按下标遍历、修改与比较</strong></p>\n<h2 id="kp-04">04 字符串遍历</h2>\n<h3 id="字符串遍历">字符串遍历</h3>\n<p>下面的片段读取一整行，并统计字符 <code>\'a\'</code> 出现的次数。它复用本课的合法下标循环，但不是另一份完整程序：</p>\n<pre><code>string text;getline(cin, text);int count = 0;for (string::size_type i = 0; i &lt; text.length(); i++) {    if (text[i] == \'a\') {        count++;    }}</code></pre>\n<p>空格和标点也各占一个位置。空串的长度为 0，循环第一次判断就不成立，因此不会访问 <code>text[0]</code>。</p>\n<pre><code>char lower = tolower(ch); // 用 lower 保存把当前字符转换成小写后的结果；当前值由 tolower(ch) 得到char upper = toupper(ch); // 用 upper 保存把当前字符转换成大写后的结果；当前值由 toupper(ch) 得到</code></pre>\n<p>例如，字符串已经保存在 <code>s</code> 中时，可以用下面的局部片段统一改成小写：</p>\n<pre><code>for (string::size_type i = 0; i &lt; s.size(); i++) { // 从下标 0 开始逐个处理有效字符    s[i] = tolower(s[i]); // 接住转换后的字符，再写回原来的位置}</code></pre>\n<p>如果只想判断而不转换，可以写 <code>if (islower(ch))</code> 或 <code>if (isupper(ch))</code>。它们回答的是“这个字符是不是英文小写/大写字母”，不是比较两个字符串。</p>\n<p><h2 id="kp-06">06 通过下标修改字符</h2>\n字符串下标不仅可以读取字符，也可以直接修改合法位置上的字符。下面的完整程序把一行文本中的小写英文字母改成大写：</p>\n<h3 id="完整程序逐字符转成大写">完整程序：逐字符转成大写</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt; // 引入输入输出和 string，供本例按下标修改字符using namespace std; // 后续直接使用 cin、cout 和 string，避免重复书写 std 前缀\nint main() { // 程序从这里开始，把字符串中的小写字母改成大写    string s; // 用 s 保存本例正在遍历或修改的字符串；值将在紧接着的输入或判断分支中确定    getline(cin, s); // 读取一整行文本（包括中间的空格）并保存到 s\n    // 从下标 0 到最后一个有效位置，逐个检查并修改小写英文字母。    for (string::size_type i = 0; i &lt; s.size(); i++) { // 让下标覆盖每个有效字符：继续条件是 i &lt; s.size()，每轮结束执行 i++        if (s[i] &gt;= \'a\' &amp;&amp; s[i] &lt;= \'z\') {  // 当前字符是小写字母，把它改成对应大写字母            s[i] = s[i] - \'a\' + \'A\'; // 保持字母序号不变，把当前小写字母映射到对应大写字母        } // 小写字母已经转成对应大写，其他字符保持原样    } // s[i] 是否需要转成大写已处理，i++ 后继续下一字符\n    cout &lt;&lt; s &lt;&lt; \'\\n\'; // 输出修改后的完整字符串并换行    return 0; // 修改后的大写文本已输出，向运行环境返回成功状态 0} // 通过下标原地修改字符串的完整过程结束</code></pre>\n<p>表达式 <code>s[i] - \'a\'</code> 得到当前小写字母与 <code>\'a\'</code> 的编码距离，再加上 <code>\'A\'</code> 就得到对应的大写字母。这个转换依据英文字母编码连续排列的特点；字符编码基础可回看<a href="/gesp/level2/ascii/">二级 ASCII 编码课程</a>。</p>\n<p>长度为 <code>s.size()</code> 的字符串，最后一个合法下标是 <code>s.size() - 1</code>。<code>s[s.size()]</code> 已经越过最后一个字符，不能读取也不能修改。空串没有任何合法下标，所以访问前仍要先检查 <code>s.empty()</code>。</p>\n<h3 id="两端交换完成字符串反转">两端交换完成字符串反转</h3>\n<p>字符串反转和数组反转使用同一过程：左下标从 0 开始，右下标从最后一个字符开始，每轮交换一对字符后同时向中间移动。</p>\n<pre><code>int left = 0; // 左下标从字符串首字符开始int right = (int)s.length() - 1; // 先把长度转成 int 再减 1，空串时得到 -1 而不会访问字符while (left &lt; right) { // 左右下标尚未相遇时，继续交换一对字符    char temporary = s[left]; // 暂存左侧字符，避免下一行覆盖后丢失    s[left] = s[right]; // 把右侧字符写到当前左侧位置    s[right] = temporary; // 把原左侧字符写到当前右侧位置    left++; // 左下标向中间移动一格    right--; // 右下标向中间移动一格} // 左右下标相遇或交错时，整个字符串已经反转</code></pre>\n<p>对 <code>"abcd"</code>，第一次交换下标 0 和 3 得到 <code>"dbca"</code>，第二次交换下标 1 和 2 得到 <code>"dcba"</code>，然后 <code>left = 2</code>、<code>right = 1</code>，条件不成立并停止。空串或长度为 1 的字符串一开始就不满足 <code>left &lt; right</code>，不会访问不存在的位置。</p>\n<h3 id="简单密码转换每个小写字母后移一位">简单密码转换：每个小写字母后移一位</h3>\n<p>简单密码题通常会明确给出字符映射规则。下面只处理常见 GCC/OJ 的 ASCII 小写英文字母：<code>a</code> 变成 <code>b</code>，……，<code>y</code> 变成 <code>z</code>，<code>z</code> 回到 <code>a</code>。输入若含空格、数字或大写字母，必须另按题意决定是否保留，不能擅自套用这段规则。</p>\n<pre><code>for (string::size_type i = 0; i &lt; s.size(); i++) { // 从左到右处理每个待加密字符    if (s[i] == \'z\') { // z 已在小写字母末尾，后移一位要回到 a        s[i] = \'a\'; // 按首尾相接规则把 z 替换成 a    } else if (s[i] &gt;= \'a\' &amp;&amp; s[i] &lt;= \'y\') { // a 到 y 都有紧邻的下一个小写字母        s[i] = s[i] + 1; // 利用 ASCII 连续编码，把当前字符后移一位    } // 其他字符不满足两个分支，因此保持原样} // 所有字符处理完后，s 保存完整密码转换结果</code></pre>\n<p>例如 <code>"az by"</code> 依次变为 <code>"ba cz"</code>：<code>a → b</code>、<code>z → a</code>、空格保持、<code>b → c</code>、<code>y → z</code>。这类题的关键不是背代码，而是先把“哪些字符会变、怎样变、边界字符怎样回绕”写成完整规则。</p>\n<p>片段 <code>s[1] = \'d\';</code> 会让 abc 修改为 adc：下标 1 是第二个字符，赋值只改变这个位置，原来的 <code>a</code> 和 <code>c</code> 保持不变。</p>\n<h2 id="kp-07">07 字符串比较与字典序</h2>\n<h3 id="字符串比较与字典序">字符串比较与字典序</h3>\n<p>两个字符串可以使用 <code>==</code>、<code>!=</code>、<code>&lt;</code>、<code>&gt;</code>、<code>&lt;=</code>、<code>&gt;=</code> 比较。<code>==</code> 和 <code>!=</code> 判断整段文本是否相同；大小关系按字典序比较，也就是从左到右找到第一对不同字符，再比较它们的字符编码。若前面的字符都相同，较短的字符串排在前面。</p>\n<pre><code>string firstName = "Xiao"; // 用 firstName 保存名字的前半部分；当前值由 "Xiao" 得到string lastName = "Dengzi"; // 用 lastName 保存名字的后半部分；当前值由 "Dengzi" 得到string fullName = firstName + " " + lastName; // 用 fullName 保存加入空格后拼接成的完整名字；当前值由 firstName + " " + lastName 得到</code></pre>\n<p>结果是 <code>"Xiao Dengzi"</code>。中间的空格也要明确写入，不能假定程序会自动添加。</p>\n<h3 id="字符串截取-substr">字符串截取 substr()</h3>\n<p><code>substr(position, count)</code> 从下标 <code>position</code> 开始，截取至多 <code>count</code> 个字符。</p>\n<h2 id="substr-截取位置示意">substr() 截取位置示意</h2>\n<pre><code>string text = "ABCDE123"; // 原字符串共 8 个字符，下面将从下标 2 开始截取string part = text.substr(2, 3); // 从下标 2 开始连续截取 3 个字符，part 得到 "CDE"</code></pre>\n<p>下标 2 对应字符 <code>\'C\'</code>，向后取 3 个字符，<code>part</code> 得到 <code>"CDE"</code>。本课只在已经确认起点不超过字符串长度时使用 <code>substr</code>，不讨论非法起点引发的异常。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="字符串查找-find">字符串查找 find()</h3>\n<p><code>find(target)</code> 查找目标字符串第一次出现的位置。找到时返回起始下标，找不到时返回特殊标记 <code>string::npos</code>。</p>\n<p>前面已经用 <code>string::size_type</code> 保存长度和下标。<code>find</code> 还会在失败时返回同一类型的特殊标记 <code>string::npos</code>，所以查找位置也继续使用这个类型。</p>\n<pre><code>string text = "red blue red"; // 原文本中 red 出现两次，find 默认只返回第一次的位置string::size_type position = text.find("red"); // 查找 red 第一次出现的位置；找不到时保存 string::npos</code></pre>\n<p>这里第一次出现的位置是 0。位置 0 仍然表示查找成功，所以不能把 <code>find</code> 的结果直接当成真假值。</p>\n<h3 id="字符串替换-replace">字符串替换 replace()</h3>\n<p><code>replace(position, count, newText)</code> 从 <code>position</code> 开始，替换 <code>count</code> 个字符。使用前必须先确认已经找到目标：</p>\n<pre><code>if (position != string::npos) { // position 不是 npos，说明 find 确实找到了目标词    text.replace(position, 3, "green"); // 从找到的位置开始删去 3 个字符，再写入 green} // 找到时已完成首次替换；未找到时不进入分支，原文本保持不变</code></pre>\n<p>替换后 <code>text</code> 变成 <code>"green blue red"</code>。新文本可能比旧文本更长或更短，后续访问必须根据替换后的新长度判断。</p>\n<p>下面的局部片段把查找、截取、替换和拼接连在一起，并且只替换第一次出现的目标词：</p>\n<h3 id="查找并替换片段">查找并替换片段</h3>\n<pre><code>string oldWord = "cat"; // oldWord 是待查找的旧词string newWord = "dog"; // newWord 是替换后写入的新词string::size_type position = text.find(oldWord); // 保存第一次出现的位置if (position != string::npos) { // 只有确实找到时才能使用这个位置    string piece = text.substr(position, oldWord.length()); // 截取被替换的旧片段    text.replace(position, oldWord.length(), newWord); // 在原位置写入新词    string result = "Result: " + text; // 用修改后的文本完成拼接}</code></pre>\n<h3 id="字符串分割">字符串分割</h3>\n<p>C++14 的 <code>string</code> 没有 split() 成员函数。字符串分割是按一个分隔字符把原字符串拆成若干片段。</p>\n<p>例如把 club.code.org 按 <code>.</code> 分割。开始时 <code>part</code> 为空；依次读到 <code>c</code>、<code>l</code>、<code>u</code>、<code>b</code> 后，<code>part</code> 变成 <code>club</code>。读到第一个 <code>.</code> 时输出 <code>club</code> 并清空 <code>part</code>；随后同样得到并输出 <code>code</code>；循环结束时 <code>part</code> 里还剩 <code>org</code>，最后再输出一次。</p>\n<table>\n<thead>\n<tr>\n<th>当前读到的字符</th>\n<th><code>part</code> 的变化</th>\n<th>输出</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>c</code>、<code>l</code>、<code>u</code>、<code>b</code></td>\n<td>逐个累积为 <code>club</code></td>\n<td>暂不输出</td>\n</tr>\n<tr>\n<td><code>.</code></td>\n<td>输出后清空</td>\n<td><code>club</code></td>\n</tr>\n<tr>\n<td><code>c</code>、<code>o</code>、<code>d</code>、<code>e</code></td>\n<td>逐个累积为 <code>code</code></td>\n<td>暂不输出</td>\n</tr>\n<tr>\n<td><code>.</code></td>\n<td>输出后清空</td>\n<td><code>code</code></td>\n</tr>\n<tr>\n<td><code>o</code>、<code>r</code>、<code>g</code></td>\n<td>逐个累积为 <code>org</code></td>\n<td>循环后输出 <code>org</code></td>\n</tr>\n</tbody>\n</table>\n<p>下面的局部片段会保留空片段：</p>\n<pre><code>string part = ""; // part 保存当前正在累积的片段for (string::size_type i = 0; i &lt; s.size(); i++) { // 从左到右检查每个字符    if (s[i] == \'.\') { // 遇到分隔符时，一个片段结束        cout &lt;&lt; part &lt;&lt; \'\\n\'; // 空片段也会输出一个空行        part = ""; // 清空后开始收集下一段    } else {        part = part + s[i]; // 普通字符接到当前片段末尾    }}cout &lt;&lt; part &lt;&lt; \'\\n\'; // 循环后别漏掉最后一个片段</code></pre>\n<p>因此开头分隔符、末尾分隔符或连续分隔符都会输出空行。例如 <code>.a</code> 的第一段为空，<code>a.</code> 的最后一段为空，<code>a..b</code> 的中间一段为空。空片段应不应该保留，必须按照题目要求决定，不能自己悄悄丢掉或保留。</p>\n<h3 id="字符数组与结束标记">字符数组与结束标记</h3>\n<p>字符数组容量固定，而且文本末尾必须保留 <code>\\0</code>。<code>char text[105]</code> 的下标是 0 到 104，因此最多安全保存 104 个普通字符。</p>\n<p><code>cin.getline(text, CAPACITY)</code> 会读取一整行，最多写入 <code>CAPACITY - 1</code> 个普通字符，并在后面补上 <code>\\0</code>。下面的局部片段统计数字字符：</p>\n<pre><code>const int CAPACITY = 105; // 最后一个位置留给结束标记char text[CAPACITY] = {}; // 先把字符数组全部初始化为 0cin.getline(text, CAPACITY); // 最多读入 CAPACITY - 1 个普通字符int digitCount = 0; // 从还没有统计到数字开始for (int i = 0; text[i] != \'\\0\'; i++) { // 遇到结束标记时停止    if (text[i] &gt;= \'0\' &amp;&amp; text[i] &lt;= \'9\') {        digitCount++; // 当前字符是数字，计数增加 1    }}</code></pre>\n<p>循环遇到 <code>\\0</code> 时停止，因此不会把结束标记当成普通文本。若输入可能超过 104 个字符，题目必须另外说明如何处理过长输入；程序不能把字符写到数组外面。</p>\n<h3 id="官方真题少见写法append-的三个参数">官方真题少见写法：append 的三个参数</h3>\n<p>官方题中可能出现：</p>\n<pre><code>s.append(other, 0, 2); // 取 other 从下标 0 开始的 2 个字符，追加到 s 末尾</code></pre>\n<p>先用本课已经学过的操作写成直白等价形式：</p>\n<pre><code>string piece = other.substr(0, 2); // 用 piece 保存从原字符串截取出的局部文本；当前值由 other.substr(0, 2) 得到s = s + piece; // 把截取出的 piece 接到 s 末尾，形成新的字符串内容</code></pre>\n<p>它的作用是取出 <code>other</code> 从下标 0 开始的两个字符，再追加到 <code>s</code> 末尾。原写法把“从哪个字符串取、从哪里开始、取几个字符”压进一次 <code>append</code> 调用中，是官方真题少见写法。</p>\n<p>容易误读的地方是把第二个参数 0 当成“追加 0 个字符”，或把第三个参数 2 当成结束下标。实际上 0 是起始下标，2 是字符个数。日常编程和本站主示例优先使用上面的两句直白写法。</p>\n<h3 id="字符串常用函数速查表">字符串常用函数速查表</h3>\n<table>\n<thead>\n<tr>\n<th>函数</th>\n<th>作用</th>\n<th>关键参数、返回值或常见错误</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>size()</code> / <code>length()</code></td>\n<td>得到字符个数</td>\n<td>首尾访问前先确认非空；两者这里作用相同</td>\n</tr>\n<tr>\n<td><code>empty()</code></td>\n<td>判断是否为空串</td>\n<td>空串时不要访问 <code>s[0]</code></td>\n</tr>\n<tr>\n<td><code>substr(pos, count)</code></td>\n<td>从 <code>pos</code> 开始截取</td>\n<td>先确认起点不超过长度</td>\n</tr>\n<tr>\n<td><code>find(target)</code></td>\n<td>查找第一次出现的位置</td>\n<td>与 <code>string::npos</code> 比较，位置 0 也表示找到</td>\n</tr>\n<tr>\n<td><code>replace(pos, count, text)</code></td>\n<td>替换指定范围</td>\n<td>先确认已经找到合法位置</td>\n</tr>\n<tr>\n<td><code>islower(ch)</code> / <code>isupper(ch)</code></td>\n<td>判断英文字符是否为小写/大写</td>\n<td>返回判断结果，不会转换字符</td>\n</tr>\n<tr>\n<td><code>tolower(ch)</code> / <code>toupper(ch)</code></td>\n<td>转换一个英文字符的大小写</td>\n<td>要接住返回值，例如 <code>ch = tolower(ch)</code></td>\n</tr>\n</tbody>\n</table>\n<div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>示例输入</th>\n<th>应重点核对</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础：普通字符串</td>\n<td><code>cat and cat</code></td>\n<td><code>find("cat")</code> 返回 0，只替换第一次出现的位置</td>\n</tr>\n<tr>\n<td>边界：空字符串</td>\n<td>空行</td>\n<td>长度为 0，遍历不执行，也不能访问 <code>s[0]</code></td>\n</tr>\n<tr>\n<td>特殊：大小写混合与特殊字符</td>\n<td><code>HeLLo 2026!</code></td>\n<td>转成大写后为 <code>HELLO 2026!</code>，数字、空格和 <code>!</code> 等标点保持不变</td>\n</tr>\n<tr>\n<td>易错：查找失败</td>\n<td><code>blue sky</code> 中查找 <code>cat</code></td>\n<td>结果等于 <code>string::npos</code>，不能继续对该位置调用 <code>replace</code></td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><section><h3>find 返回 0 也表示找到了</h3><div><div><strong><span>×</span> 错误写法</strong><code>if (text.find(&quot;cat&quot;)) 认为条件为真才找到 cat</code></div><div><strong><span>✓</span> 正确写法</strong><code>先保存位置，再判断 position != string::npos；成功后才对这个位置使用 substr 或 replace。</code></div></div><p><strong>原因：</strong>目标位于开头时 find 返回 0，而 0 在条件中会被当作假；找不到时返回的才是 string::npos。</p><p><strong>检查方法：</strong>把 find 的结果当作下标处理，不要直接当成真假答案。</p></section><h2 id="common-errors">常见错误</h2><div><ol>\n<li>混用字符和字符串。单个字符写 <code>\'a\'</code>，一段文本写 <code>"a"</code> 或 <code>"cat"</code>。</li>\n<li>用 <code>cin &gt;&gt; text</code> 读取带空格句子。它在第一个空白处停止，整行文本应使用 <code>getline</code>。</li>\n<li>前一次 <code>cin &gt;&gt;</code> 后直接 <code>getline</code>。输入流中的换行可能被立即读走，要按输入格式先处理分隔字符。</li>\n<li>空串仍访问首尾字符。访问 <code>text[0]</code> 或 <code>text[text.length() - 1]</code> 前先检查 <code>empty()</code>。</li>\n<li>把 <code>find</code> 返回的 0 当成失败。0 是第一个合法下标，失败标记是 <code>string::npos</code>。</li>\n<li>未确认查找成功就调用 <code>replace</code>。必须先判断位置不等于 <code>string::npos</code>。</li>\n<li>替换后仍使用旧长度。替换可能改变字符个数，后续访问要按新长度判断。</li>\n<li>访问 <code>s[s.size()]</code>。字符串最后一个合法下标是 <code>s.size() - 1</code>，空串则没有合法下标。</li>\n<li>把字典序误认为长度比较。字符串从左到右比较第一对不同字符，只有一方是另一方前缀时才由长度决定先后。</li>\n<li>把 <code>char text[105]</code> 当作能保存 105 个普通字符。必须为末尾的 <code>\\0</code> 留一个位置。</li>\n<li>以为 <code>string</code> 自带 <code>split()</code>。C++14 的 <code>string</code> 没有这个成员函数；本课按分隔字符遍历、累积并在合适的位置输出片段。</li>\n<li>调用 <code>tolower()</code> 或 <code>toupper()</code> 后不保存返回值。这两个函数返回转换后的字符，要写成 <code>ch = tolower(ch)</code> 或 <code>s[i] = toupper(s[i])</code>。</li>\n<li>没有判断空串就访问 <code>s[0]</code>。先写 <code>if (!s.empty())</code>，确认存在首个字符后再访问。</li>\n<li>分割时没有阅读题意就丢弃或保留空片段。开头、末尾和连续分隔字符都可能产生空片段，输出规则由题目决定。</li>\n</ol></div><h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<h3 id="核心结论">核心结论</h3>\n<p>枚举法适合候选数量有限、每个候选都能逐个判断的问题。先确定要枚举的对象、完整范围和尝试顺序，再由题目条件判断当前候选是否合法。最后按题意统计、输出或维护答案，做到不重复、不遗漏，并保证循环一定结束。</p>\n<h3 id="常用语法">常用语法</h3>\n<p>常用写法是用 <code>for</code> 循环依次生成候选，在 <code>if</code> 中判断合法性；只找第一个答案时，可以在命中后用 <code>break</code> 停止。若一个量能由另一个量直接推出，例如 <code>rabbit = heads - chicken</code>，就不必再为它增加一重循环。</p>\n<h3 id="最小示例">最小示例</h3>\n<p>寻找 <code>0</code> 到 <code>5</code> 中的偶数时，让 <code>x</code> 从 <code>0</code> 逐个增加到 <code>5</code>，每轮检查 <code>x % 2 == 0</code>；通过判断的候选依次是 <code>0、2、4</code>。</p>\n<h3 id="关键易错点">关键易错点</h3>\n<ul>\n<li>把 <code>&lt;</code> 和 <code>&lt;=</code> 写混，漏掉上界或下界。</li>\n<li>能直接推导第二个量时仍写双重循环，造成重复候选。</li>\n<li>候选还没通过全部条件就计数，或没有依据就提前 <code>break</code>。</li>\n<li>只写循环而不先估算候选总数，可能让枚举次数过大。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>枚举法把<strong>有限范围</strong>内的候选按确定顺序逐个生成，再用同一组条件判断是否合法，并按题意处理答案。本课只处理范围能够证明、候选数量可以接受的问题；若没有可靠上界，不能写无限尝试的循环。</p>\n<p>基本路线是：<strong>确定范围 → 逐个尝试 → 判断条件 → 处理答案</strong>。写循环前必须说清五件事：枚举对象是什么、范围两端在哪里、尝试顺序怎样、合法条件是什么、命中后如何处理。</p>\n<p>本课使用三级已经学过的变量、分支、循环、字符串与一维数组，不引入函数封装、指针、递归、二维数组、排序、搜索、动态规划或新的容器。</p>\n<h2 id="kp-01">01 枚举思想与适用条件</h2>\n<p>枚举不是“随便试几个数”，而是让每一轮循环都对应一个明确候选。只要候选范围有限、判断规则明确，并且逐个检查的次数能够接受，就可以考虑枚举。</p>\n<p>第一个课程核心是：<strong>先确定对象、有限范围与尝试顺序</strong>。对象决定“每轮在试什么”，范围保证“不漏端点且一定结束”，顺序则决定候选怎样依次出现。</p>\n<pre><code>for (int x = 0; x &lt;= 1; x++) {    for (int y = 0; y &lt;= 1; y++) {        for (int z = 0; z &lt;= 1; z++) {            if (x + y + z == 2) {                count++;            }        }    }}</code></pre>\n<p>最内层的 z 变化最快；固定一组 (x, y) 后，z = 0、1 都会被检查。三层范围都有限，所以恰好生成 2 × 2 × 2 = 8 个候选并正常结束。</p>\n<span id="kp-02"></span>\n<h2 id="枚举对象与范围">枚举对象与范围</h2>\n<p>枚举对象常见有两种：<strong>数值</strong>和<strong>下标</strong>。枚举数值时，循环变量本身就是候选；枚举下标时，循环变量表示位置，要通过 a[i] 或 s[i] 取得该位置的内容。</p>\n<pre><code>for (int i = 0; i &lt; n; i++) {    for (int j = i + 1; j &lt; n; j++) {        // 当前候选是位置对 (i, j)    }}</code></pre>\n<p>这样既不会选择同一位置，也不会在 (i, j) 之后再次生成反向的 (j, i)。如果题目区分先后顺序，范围必须重新设计，不能照搬这一写法。</p>\n<p>尝试顺序也要与题意一致。寻找最小合法值时，通常从小到大枚举；只要范围完整，顺序不会改变“有哪些合法候选”，却会影响“第一个找到的是谁”。</p>\n<h2 id="kp-03">03 候选、范围、判断与处理</h2>\n<p>第二个课程核心是：<strong>由条件推导候选，再判断它是否合法</strong>。先把题目中的量分成“需要尝试的量”和“可以计算出来的量”，能直接计算的就不再枚举。</p>\n<p>以鸡兔同笼为例，已知总头数 heads 和总腿数 legs。自然想法是同时尝试鸡数和兔数，再检查头数、腿数两个条件；但只要鸡数确定，兔数就由总头数唯一推出：</p>\n<p>rabbit = heads - chicken</p>\n<p>因此只需让 chicken 从 0 到 heads，每轮计算 rabbit，再检查 chicken * 2 + rabbit * 4 == legs。这种形成过程把双变量尝试收束成单重枚举：没有漏掉任何可能的鸡数，也不会为同一个鸡数重复生成兔数。</p>\n<p>同样地，寻找满足 x + y = target 的非负整数对时，可以枚举 x，再计算 y = target - x。如果交换顺序算不同方案，就让 x 从 0 到 target；如果交换顺序算同一种方案，可以再证明并限制 <code>x &lt;= y</code>，但不能凭感觉把范围砍半。</p>\n<p>写任何枚举前，都用下面五问完成规则：</p>\n<ol>\n<li><strong>对象：</strong> 每轮候选是什么，例如一个数、一个下标或一对位置。</li>\n<li><strong>范围：</strong> 每个需要尝试的量从哪里到哪里，端点是否包含。</li>\n<li><strong>顺序：</strong> 候选按什么次序出现，顺序是否影响首解或去重。</li>\n<li><strong>判断：</strong> 当前候选必须通过哪些题目条件。</li>\n<li><strong>处理：</strong> 合法时是计数、输出、记录首解，还是更新最优答案。</li>\n</ol>\n<p>这里“推导”只负责生成当前候选的一部分，“判断”才决定整个候选是否合法。即使 rabbit = heads - chicken 能保证头数正确，仍要检查腿数；不能把“算得出来”误当成“已经合法”。</p>\n<h2 id="kp-04">04 合法性判断</h2>\n<p>生成候选之后，要把题目条件逐条翻译成判断。只有通过<strong>全部条件</strong>的候选，才能计数、输出或用于更新答案；少检查一个条件，就可能把不合法候选当成答案。</p>\n<p>例如“寻找第一个能同时被 2 和 3 整除的整数”，当前候选 x 必须同时满足 x % 2 == 0 和 x % 3 == 0。若只检查其中一个条件，答案就不再符合原题。</p>\n<p>判断与处理要分开理解：</p>\n<ol>\n<li>先计算由候选得到的中间量。</li>\n<li>再检查所有合法条件。</li>\n<li>只有判断为真时才处理答案。</li>\n<li>判断为假时跳过本轮，继续尝试下一个候选。</li>\n</ol>\n<p>条件剪枝是“提前排除已经能证明不合法的候选”。例如要求 x + y = 8 且 x、y 都是非负整数时，若已经算出 <code>y &lt; 0</code>，就能直接跳过；但不能因为“看起来不像答案”而停止整个循环。</p>\n<h3 id="计数最优答案与首解停止">计数、最优答案与首解停止</h3>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int heads, legs;    cin &gt;&gt; heads &gt;&gt; legs;\n    int solutionCount = 0;    for (int chicken = 0; chicken &lt;= heads; chicken++) {        int rabbit = heads - chicken;        int actualLegs = chicken * 2 + rabbit * 4;\n        if (actualLegs == legs) {            cout &lt;&lt; chicken &lt;&lt; \' \' &lt;&lt; rabbit &lt;&lt; \'\\n\';            solutionCount++;        }    }\n    if (solutionCount == 0) {        cout &lt;&lt; "No" &lt;&lt; \'\\n\';    }\n    return 0;}</code></pre>\n<p>程序只有 chicken 是独立枚举量。rabbit 和 actualLegs 都由当前候选计算得到；只有腿数完全相等时才输出和计数。若所有候选都失败，solutionCount 保持 0，最后输出 No。</p>\n<pre><code>bool found = false;for (int x = 0; x &lt;= limit; x++) {    if (x % 2 == 0 &amp;&amp; x % 3 == 0) {        answer = x;        found = true;        break;    }}</code></pre>\n<p>不能在条件尚未全部检查时 break；求全部方案、计数或最优答案时也不能照搬首解停止。</p>\n<h3 id="官方真题少见写法">官方真题少见写法</h3>\n<p>官方题中可能写出 out[cnt++] = x;。它的直白等价写法是：</p>\n<pre><code>out[cnt] = x;cnt++;</code></pre>\n<p>也可能写出 count += height[i] &gt; 135 ? 1 : 0;。它的直白等价写法是：</p>\n<pre><code>if (height[i] &gt; 135) {    count++;}</code></pre>\n<p>这两种压缩写法只用于识别。三级日常编程优先采用分步写法，先看清写入、判断与累计各自发生在什么时候。</p>\n<h2 id="common-errors">常见错误</h2><div><ol>\n<li>\n<p><strong>范围漏掉端点。</strong> 现象：上界恰好是答案时程序找不到。→ 原因：没有先判断端点是否属于合法范围，把 <code>&lt;=</code> 误写成 <code>&lt;</code>，或忘记从 0 开始。→ 修正：从题意写出闭区间或下标区间，再翻译成循环条件。→ 复验：分别用下界合法、上界合法和空范围附近的数据检查。</p>\n</li>\n<li>\n<p><strong>能推导第二个量时仍用双重枚举。</strong> 现象：同一个鸡数配出很多不满足头数的兔数，候选数量明显增多。→ 原因：没有利用 rabbit = heads - chicken。→ 修正：只枚举 chicken，由头数等式唯一推出 rabbit，再检查腿数。→ 复验：确认每个 chicken 只生成一个 rabbit，候选最多为 heads + 1 个。</p>\n</li>\n<li>\n<p><strong>计数位置错误。</strong> 现象：solutionCount 等于循环次数，或不合法候选也被算入。→ 原因：在合法性判断之前执行了 solutionCount++。→ 修正：只有候选通过全部条件时才输出并计数。→ 复验：使用一个无解输入，结果必须保持 0 并输出 No。</p>\n</li>\n<li>\n<p><strong>没有依据就 break。</strong> 现象：找到第一个候选后立即停止，漏掉后面的合法方案或更优答案。→ 原因：没有区分首解、全部方案、计数和最优值任务。→ 修正：只有题目明确要首个答案时，才在完整判断和记录之后 break。→ 复验：准备一个有多个合法候选的输入，确认全部方案题仍会检查到范围末端。</p>\n</li>\n<li>\n<p><strong>同一方案正反各记一次。</strong> 现象：不区分顺序的 (2, 6) 和 (6, 2) 同时出现。→ 原因：候选没有统一表示，仍完整枚举两个顺序。→ 修正：证明交换顺序等价后，限制 <code>x &lt;= y</code>，或让位置对使用 j = i + 1。→ 复验：逐项检查输出，任意两项都不能只交换顺序就完全相同。</p>\n</li>\n<li>\n<p><strong>最优值错误地初始化为 0。</strong> 现象：所有合法候选都为负数时，程序却输出不存在的 0。→ 原因：初值不是来自合法候选，也没有记录是否已经找到答案。→ 修正：用首个合法候选初始化，或用 found 区分“还没有答案”和“已有最优值”。→ 复验：使用全负候选和完全无解两类数据，二者必须得到不同且正确的处理。</p>\n</li>\n<li>\n<p><strong>忽略多层循环次数相乘。</strong> 现象：每一层范围看似不大，程序整体却运行很久。→ 原因：只看单层次数，没有计算 A × B 或 A × B × C。→ 修正：编码前先算候选总数，再寻找直接推导、可靠缩界或条件剪枝。→ 复验：代入最大输入估算最坏候选数，确认在题目限制内可以完成。</p>\n</li>\n</ol></div><section><h3>去重范围必须来自题意</h3><div><div><strong><span>×</span> 错误写法</strong><code>只因为两个数能交换，就随手把循环范围减半</code></div><div><strong><span>✓</span> 正确写法</strong><code>先确认交换顺序是否仍是同一方案，再用 x &lt;= y 或 j = i + 1 保留唯一表示</code></div></div><p><strong>原因：</strong>题目可能区分顺序；即使不区分，也要说明保留哪一种统一表示。</p><p><strong>检查方法：</strong>用 target = 8 列出全部候选，检查缩小范围后是否既无重复又无遗漏。</p></section><h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="本课先记住">本课先记住</h2>\n<h3 id="核心结论">核心结论</h3>\n<p>模拟法把题目给出的过程按原顺序执行，并把下一步仍要使用的信息保存在状态变量中。只保存完成任务所需的最小状态，并给出明确初值；严格按照 <code>day++ → 上爬 → 立即判断 → 未到才下滑</code> 的顺序更新；先处理边界和停止条件，再从最终状态输出答案。</p>\n<h3 id="常用语法">常用语法</h3>\n<p>本课只使用三级已经学过的变量、分支、循环、<code>break</code>、一维数组、字符串、取模和位运算。典型的一轮写法是：先修改状态，再用 <code>if</code> 判断是否停止；只有未停止时，才执行本轮剩余操作。</p>\n<h3 id="最小示例">最小示例</h3>\n<p>青蛙清晨高度是 0，白天上爬 5 米后到达 5 米；因为 <code>5 &lt; 15</code>，夜晚才下滑 2 米，下一天从 3 米开始。这个小过程已经包含“读旧状态、更新、判断、决定下一步”。</p>\n<h3 id="关键易错点">关键易错点</h3>\n<ul>\n<li>最后一天到达井口后不再下滑，不能把每一天都简单写成 <code>up - down</code>。</li>\n<li>第一天已经到达时，即使 <code>up &lt;= down</code>，答案仍是 1，不能误判为 <code>Impossible</code>。</li>\n<li>候选状态必须先检查再写回；计分板累加、光标移动和字符串分割也遵守同一原则。</li>\n</ul>\n\n<h2 id="知识范围与使用边界">知识范围与使用边界</h2>\n<p>模拟法不是背模板，而是把题目的每条规则变成可核对的一次动作。一次完整模拟可以概括为：<strong>读取当前状态 → 执行一次操作 → 更新状态 → 判断是否结束</strong>。本课只处理规则明确、状态数量少、能够用三级已学语法逐步执行的问题，不引入自定义函数、指针、二维数组、容器、排序、递归、搜索或状态机术语。</p>\n<p>贯穿全课的代表问题是<strong>青蛙爬井</strong>：一口井深 <code>target</code> 米，青蛙白天上爬 <code>up</code> 米；若已到达井口就立即结束，否则夜晚下滑 <code>down</code> 米。程序输出首次到达所需天数；永远无法到达时输出 <code>Impossible</code>。</p>\n<p>教学程序公开支持：<code>1 &lt;= target, up, down &lt;= 1000000</code>。最多只输出一个不超过 <code>target</code> 的正整数或单词 <code>Impossible</code>；循环中的高度小于 <code>target + up</code>，最大不超过 1999999，所以 <code>int</code> 安全。</p>\n<h2 id="kp-01">01 模拟思想</h2>\n<p>模拟时先问：下一步还需要知道什么？青蛙问题只需要井深 <code>target</code>、白天上爬量 <code>up</code>、夜晚下滑量 <code>down</code>、当前高度 <code>height</code> 和已经开始的天数 <code>day</code>。其中前三个量来自输入，后两个量会在执行中变化。</p>\n<p>主样例 S1 的输入是 <code>15 5 2</code>，输出是 <code>5</code>。第一天从高度 0 开始，上爬到 5，没有到达 15，才下滑到 3；后一天必须从 3 接着算，不能回到初值 0。</p>\n<p>这说明模拟法的关键不是循环写几层，而是后一轮必须读取前一轮完成后的真实状态。</p>\n<span id="kp-02"></span>\n<h2 id="枚举与模拟的区别">枚举与模拟的区别</h2>\n<p>枚举法和模拟法都可能使用循环，但每一轮含义不同。枚举法每轮检查一个候选，重点是不重不漏；模拟法每轮执行一条操作或一个时刻，重点是状态变量与更新顺序。青蛙问题的一轮表示“一天”，不是在猜一个答案。</p>\n<p>如果一道题先枚举候选、再对候选执行若干规则，要分别说清两部分职责，不能因为都使用循环就把它们混为一种方法。</p>\n<span id="kp-03"></span>\n<h2 id="状态变量与更新顺序">状态变量与更新顺序</h2>\n<p>只保存下一步确实需要的信息，叫作最小状态。<code>height</code> 只表示当前高度，初值为 0；<code>day</code> 只表示已经开始了几天，初值为 0。进入新一天时才执行 <code>day++</code>，白天上爬时才改变 <code>height</code>。</p>\n<p>当前分数、当前位置、当前字符，都是下一步还要继续使用的信息；在别的模拟题里，它们同样应由职责清楚的状态变量保存。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int target, up, down;    cin &gt;&gt; target &gt;&gt; up &gt;&gt; down;\n    // 第一天未到达且完整昼夜没有正增长，才会永远无法到达。    if (up &lt; target &amp;&amp; up &lt;= down) {        cout &lt;&lt; "Impossible" &lt;&lt; \'\\n\';        return 0;    }\n    int height = 0;    int day = 0;\n    while (height &lt; target) {        day++;        height = height + up;\n        // 白天到达后立即停止，不能继续执行本日夜晚下滑。        if (height &gt;= target) {            break;        }\n        height = height - down;    }\n    cout &lt;&lt; day &lt;&lt; \'\\n\';    return 0;}</code></pre>\n<p>程序与八步方法逐项对应。首日到达不会进入无解分支；进入循环后，每轮要么到达并停止，要么在完整昼夜后取得正增长。因此合法输入不会无限循环，输出就是首次到达时的天数。</p>\n<h2 id="common-errors">常见错误</h2><div><ol>\n<li><strong>把每天都写成净增长。</strong> 现象：样例天数偏大或最后一天高度被再次减小。→原因：到达后仍计算 <code>up - down</code>。→修正：先上爬再判断，未到才下滑。→复验：<code>5 5 3</code> 必须输出 1。</li>\n<li><strong>把判断放在下滑之后。</strong> 现象：<code>15 5 2</code> 第 5 天上爬到 17 后又变成 15。→原因：没有在白天到达时立即停止。→修正：把 <code>if (height &gt;= target)</code> 放在下滑之前。→复验：追踪第 5 天的“未到才下滑”必须是不执行。</li>\n<li><strong>首日到达仍判无解。</strong> 现象：<code>5 5 5</code> 输出 <code>Impossible</code>。→原因：只检查 <code>up &lt;= down</code>。→修正：无解条件写成 <code>up &lt; target &amp;&amp; up &lt;= down</code>。→复验：<code>5 5 5</code> 输出 1。</li>\n<li><strong>无解不等号写错。</strong> 现象：<code>10 3 3</code> 进入死循环。→原因：把 <code>up &lt;= down</code> 错写成 <code>up &lt; down</code>。→修正：净增长为 0 也无法到达。→复验：<code>10 3 3</code> 输出 <code>Impossible</code>。</li>\n<li><strong>把累加写成覆盖。</strong> 现象：同一玩家后一次得分抹掉旧分数。→原因：写成 <code>score[player] = value</code>。→修正：读取当前分数后使用 <code>score[player] += value</code>。→复验：初值 4 再加 4 应得到 8。</li>\n<li><strong>候选状态未检查就写回。</strong> 现象：光标可能保存越界位置。→原因：先修改 <code>position</code>，再检查是否合法。→修正：先计算 <code>nextPosition</code>，检查通过后再执行 <code>position = nextPosition</code>。→复验：位置 4 再向后 1、有效范围 0 到 4 时仍保持 4。</li>\n<li><strong>后一步读取旧值。</strong> 现象：Q5 得到的分数不是 8。→原因：没有先完成 <code>code ^= 2</code>。→修正：严格按输入顺序写回每一步状态。→复验：<code>code</code> 为 6、分数为 4 时，先得到 4，再累计为 8。</li>\n</ol></div><section><h3>到达后仍下滑会破坏真实停止时机</h3><div><div><strong><span>×</span> 错误写法</strong><code>每一天都先执行 height = height + up - down</code></div><div><strong><span>✓</span> 正确写法</strong><code>day++，上爬，立即判断；只有未到达时才下滑</code></div></div><p><strong>原因：</strong>最后一天白天已经到达井口，题目规定立即结束；合并更新会多执行一次本不该发生的下滑。</p><p><strong>检查方法：</strong>把题目中的时间顺序写在代码旁，再逐项核对每个判断位于哪个动作之前。</p></section><h2 id="core-check">核心内容检查</h2><h2 id="page-test-title">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
    ],
    4: [
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">函数把一项有明确输入、处理和输出的任务命名。它既能避免重复代码，也能把长程序拆成容易检查的小部分。真正掌握函数，不是只会背格式，而是能说清声明、定义、调用和返回各在什么时候发生。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">声明让编译器先认识接口，定义给出任务做法，调用才开始一次真实执行；return 交回结果并立即结束本次调用。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int larger(int left, int right); // 声明：说明返回类型、函数名和参数类型\nint larger(int left, int right) { // 定义：给出相同接口和函数体\n    return left; // 返回一个 int 结果并结束本次调用\n} // 结束 larger 函数定义</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int square(int value) { // 接收一个整数副本并计算平方\n    return value * value; // 把平方结果返回调用处\n} // 结束 square 函数定义</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">把“写出函数定义”误认为函数已经执行</li><li class="astro-e22r5uwj">声明与定义的返回类型、函数名或参数列表不一致</li><li class="astro-e22r5uwj">有返回值的函数存在走到末尾却没有 return 的路径</li><li class="astro-e22r5uwj">把返回值当成自动输出，忘记在调用处接收或输出</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：看懂一个函数接口的每个组成部分；追踪“调用—进入—返回—继续”的执行路线；把一项重复任务拆成职责清楚的函数。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">班级系统要多次比较两名同学的分数。若每次都重新写一段 if，代码会重复；若把“接收两个整数并返回较大值”命名为 larger，main 只需把数据交给它并接回结果。全页都用这台“比较机器”理解函数。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要掌握函数的作用、声明、定义、调用、参数列表、返回值、void 函数和嵌套调用。函数重载只作识别性补充：能根据参数列表判断调用哪个版本即可，不把复杂重载规则扩大为本级核心。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 声明与定义</h2><p class="astro-e22r5uwj">先拆开 `int larger(int left, int right)`：最前面的 `int` 是返回类型，说明调用完成后交回一个整数；`larger` 是函数名；圆括号里的两个 `int` 是形参类型，left、right 是函数体内使用的形参名。函数接口回答“接收什么、返回什么”，函数体回答“具体怎么做”。</p><p class="astro-e22r5uwj">函数声明只写接口并以分号结束，例如 `int larger(int, int);`。形参名在声明中可以省略，因为编译器此时只需要知道类型和数量；初学阶段保留 left、right 更容易阅读。声明没有函数体，也不会进行比较。</p><p class="astro-e22r5uwj">函数定义写出相同接口和一对大括号。若定义放在 main 前面，main 调用时编译器已经见过它，可以不另写声明；若定义放在 main 后面，就要先声明。C++ 编译器按源码顺序处理，调用前至少要看见声明或完整定义。</p><p class="astro-e22r5uwj">声明和定义描述的是同一个函数时，返回类型、函数名、参数个数、参数顺序和参数类型必须对应。只改一个位置，会让编译器把它们当成不同接口或报告错误。</p><p class="level4-dictionary-article__code-label">先声明接口，再把定义放到 main 后面</p><pre class="astro-e22r5uwj"><code class="language-cpp">int larger(int left, int right); // 先声明稍后会出现的两整数比较函数\nint main() { // 主函数可以在只见到声明时调用 larger\n    cout &lt;&lt; larger(3, 5) &lt;&lt; &#39;\\n&#39;; // 传入两个实参并输出返回结果 5\n    return 0; // 返回 0 表示主程序正常结束\n} // 结束 main 函数\nint larger(int left, int right) { // 按声明给出完全一致的函数定义\n    if (left &gt; right) { // 左边分数更大时选择左边结果\n        return left; // 返回 left 并立即结束本次 larger 调用\n    } // 结束左边更大的返回分支\n    return right; // 前一条件不成立时返回 right\n} // 结束 larger 函数定义</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">参数负责把数据送进函数，作用域决定一个名字在程序的哪些位置有效。调用函数、阅读同名变量或排查“变量未定义”时，要先找参数，再看局部变量和外层变量。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">实参在调用处提供数据，形参在函数内接收数据；查找同名变量时，从使用位置向外找最近的声明。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int add(int left, int right) { // left、right 是本次调用的形参\n    int sum = left + right; // sum 是只在函数体内有效的局部变量\n    return sum; // 返回结果并结束本次调用\n} // 离开函数体后，本次形参和局部变量的生命周期结束</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int value = 10; // 定义外层 value，值为 10\n{ // 进入一个新的内层作用域\n    int value = 3; // 定义内层同名变量并暂时遮蔽外层 value\n    cout &lt;&lt; value; // 最近声明是内层 value，因此输出 3\n} // 内层 value 的作用域和生命周期在此结束</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">把值参数当成调用处的原变量</li><li class="astro-e22r5uwj">离开大括号后仍使用其中的局部变量</li><li class="astro-e22r5uwj">看到同名变量就默认它是全局变量</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：分清实参与形参；同时判断名字的作用域和对象的生命周期；遇到同名变量时从使用位置向外找到真正对象。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">学校里可以同时有“全校积分”“班级积分”和“小组积分”三个同名概念。程序里的名字也有自己的有效范围：站在不同的大括号里，看到的可能不是同一个变量。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要掌握形参与实参、局部变量、全局变量、作用域和生命周期，并能判断同名变量的遮蔽关系。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 形参与实参</h2><p class="astro-e22r5uwj">函数定义中用于接收数据的变量叫形参，调用表达式中真正提供数据的值、变量或表达式叫实参。`add(3, 5)` 里的 3、5 是实参，函数体里的 left、right 是形参。</p><p class="astro-e22r5uwj">一次调用开始时，程序先计算实参，再按位置把结果交给形参：第一个实参给第一个形参，第二个给第二个；并不是按名字对应。</p><p class="astro-e22r5uwj">每次调用都会重新创建一组形参。第一次 add(3,5) 的 left 与第二次 add(8,2) 的 left 名字相同，却是两次调用中的不同对象。</p><pre class="astro-e22r5uwj"><code class="language-cpp">int add(int left, int right) { // 定义接收两个整数形参的加法函数\n    return left + right; // 返回本次调用中两个形参之和\n} // 本次调用结束时，left 和 right 随之销毁\n\nint result = add(3, 5); // 3、5 是实参，按位置创建 left=3、right=5</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">指针变量保存的是地址。需要间接访问对象、遍历数组或让函数通过地址修改实参时，可以先保存地址，再用解引用读取或修改对象。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">指针保存地址，解引用访问地址对应的对象；只有指向仍然存在的合法对象时才能解引用。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int value = 7; // 创建一个保存整数 7 的对象\nint* pointer = &amp;value; // 取得 value 的地址并保存到 int 指针中\n*pointer = 10; // 沿保存的地址找到 value，并把它改为 10</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int score = 80; // 定义分数对象，值为 80\nint* pointer = &amp;score; // 保存 score 的地址并建立指向关系\ncout &lt;&lt; *pointer; // 解引用 pointer，读取并输出 score 的值 80</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">未初始化或为 nullptr 时解引用</li><li class="astro-e22r5uwj">混淆 pointer、&amp;value 与 *pointer</li><li class="astro-e22r5uwj">指针偏移后越过数组合法元素范围</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：区分对象、地址和指针变量；理解解引用怎样读写同一对象；在数组和空指针场景中守住对象生命周期与合法边界。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">变量像一间放着数据的房间，地址像门牌号，指针则是专门保存门牌号的纸条。`&amp;` 用来抄下门牌号，`*` 用来顺着门牌号进入房间。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要理解变量地址、指针变量和解引用，能够判断指针指向的对象并避免空指针、失效指针等基本风险。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 地址与指针变量</h2><p class="astro-e22r5uwj">对象是实际保存数据的存储区域，地址用来标识这块区域的位置，指针变量则专门保存某种类型对象的地址。`&amp;value` 取得 value 的地址，`int* pointer` 声明一个能保存 int 对象地址的指针。</p><p class="astro-e22r5uwj">执行 `pointer = &amp;value` 后，pointer 的内容是地址，value 的内容仍是整数 7。假设讲解时把 value 的地址记作 1000，那么三栏应写成“value：7；&amp;value：1000；pointer：1000”。真实地址由运行环境决定，不能假定固定数值。</p><p class="astro-e22r5uwj">声明 `int* first, second;` 时只有 first 是指针，second 仍是 int。初学阶段一行只声明一个指针，或把星号贴近变量名，能减少误读。</p><pre class="astro-e22r5uwj"><code class="language-cpp">int value = 7; // 创建整数对象 value，内容为 7\nint* pointer = &amp;value; // 创建指针对象 pointer，内容为 value 的地址</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">参数传递方式决定函数得到的是数据副本，还是能够直接访问调用处的对象。设计修改、交换或只读函数时，要先确定是否需要改变原变量，再选择值、引用或指针。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">值传递改副本，引用传递改原对象，指针传递通过地址改对象；数组传参还必须单独给出长度。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">void byValue(int value); // 值参数接收一个新副本\nvoid byReference(int&amp; value); // 引用参数成为实参的别名\nvoid byPointer(int* pointer); // 指针参数接收实参地址</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">void addOne(int&amp; value) { // value 直接代表调用处的整数对象\n    value = value + 1; // 修改引用也就修改了原对象\n} // 函数结束后，修改结果保留在实参中</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">想修改实参却使用值传递</li><li class="astro-e22r5uwj">指针参数未判空就解引用</li><li class="astro-e22r5uwj">在函数内用 sizeof 推算数组参数长度</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：用同一组数据对照值、引用和指针传递；追踪调用前、函数内、返回后的对象变化；为数组和只读大对象选择边界清楚的接口。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">值传递像把资料复印件交给别人，引用像允许对方直接在原件上修改，指针像把原件的存放地址交出去。三种方式都能传信息，但修改结果完全不同。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要比较值传递、引用传递和指针传递，理解数组作为参数时的特点，并根据是否需要修改实参选择方式。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 值传递</h2><p class="astro-e22r5uwj">值参数得到实参当前值的副本。调用 addOne(number) 时，先创建新的形参 value，再把 number 的值复制进去；两者数值可能相同，但属于不同对象。</p><p class="astro-e22r5uwj">函数内修改 value 只改变副本。返回后副本销毁，number 保持原值。它适合 int 等较小数据的只读计算，或本来就只想在副本上处理的任务。</p><pre class="astro-e22r5uwj"><code class="language-cpp">void addOne(int value) { // 创建形参副本并接收实参当前值\n    value = value + 1; // 只修改函数内部的副本\n} // 副本销毁，调用处原变量不变</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-02"><h2 id="topic-02" class="astro-e22r5uwj">02 引用传递</h2><p class="astro-e22r5uwj">引用参数不是新副本，而是调用处对象的另一个名字。`int&amp; value` 中的 &amp; 表示引用类型；调用时直接写 addOne(number)，不写 &amp;number。</p><p class="astro-e22r5uwj">函数内给 value 赋值就是给 number 赋值，所以接口应让读者一眼看出它可能修改实参。引用必须绑定有效对象，不能用 nullptr 表示“没有对象”。</p><pre class="astro-e22r5uwj"><code class="language-cpp">void addOne(int&amp; value) { // 让 value 成为调用处整数对象的别名\n    value = value + 1; // 通过别名直接修改原对象\n} // 引用结束，但原对象的新值仍然保留</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-03"><h2 id="topic-03" class="astro-e22r5uwj">03 指针传递</h2><p class="astro-e22r5uwj">指针参数自身仍是值传递：函数得到一份地址副本。虽然修改这个指针副本的指向不会自动改调用处指针，但解引用它可以访问地址对应的原对象。</p><p class="astro-e22r5uwj">调用时用 &amp;number 取得地址，函数内先检查 pointer 是否为 nullptr，再用 *pointer 读写。指针适合接口确实需要表示“可能没有对象”的情况。</p><pre class="astro-e22r5uwj"><code class="language-cpp">void addOne(int* pointer) { // 接收一个 int 对象的地址副本\n    if (pointer != nullptr) { // 解引用前确认地址不是空指针\n        *pointer = *pointer + 1; // 沿地址读取、加一并写回原对象\n    } // 结束非空指针处理分支\n} // 结束指针参数函数</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-04"><h2 id="topic-04" class="astro-e22r5uwj">04 数组作为参数</h2><p class="astro-e22r5uwj">一维数组作为实参时，函数参数接收首元素地址，原数组不会被整体复制；因此修改 values[i] 会修改调用处对应元素。</p><p class="astro-e22r5uwj">首地址本身不带元素个数。`const int values[]` 与 `const int* values` 在函数参数位置表达同类接口，`sizeof(values)` 只反映参数本身，不是原数组总字节数。</p><p class="astro-e22r5uwj">长度必须单独传入，并让循环严格满足 0≤i&lt;length。const 表示本函数只读取元素，既保护数据，也清楚表达职责。</p><pre class="astro-e22r5uwj"><code class="language-cpp">int sumArray(const int values[], int length) { // 接收只读首地址和明确长度\n    int sum = 0; // 初始化累计和\n    for (int i = 0; i &lt; length; i++) { // 只遍历下标 0 到 length-1\n        sum = sum + values[i]; // 把当前合法元素加入累计和\n    } // 结束数组遍历\n    return sum; // 返回全部元素之和\n} // 结束数组求和函数</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">结构体把同一个对象的不同类型数据组合在一起。例如管理学生、图书或比赛记录时，可以把姓名、编号和成绩等相关数据放进一个整体。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">结构体先定义“一个对象有哪些字段”，再创建对象；对象用点访问成员，结构体指针用箭头访问成员。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">struct Student { // 开始定义学生这种新类型\n    string name; // 声明姓名成员\n    int score; // 声明分数成员\n}; // 结束类型定义，分号不可省略</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">Student student = {&quot;Alice&quot;, 90}; // 按成员声明顺序创建并初始化对象\ncout &lt;&lt; student.score; // 用点运算符读取该对象的 score 成员</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">定义结构体后忘记创建对象</li><li class="astro-e22r5uwj">对象使用 -&gt; 或指针使用 .</li><li class="astro-e22r5uwj">大括号初始化顺序与成员声明顺序不一致</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：把同一对象的相关字段组织成类型；沿“对象/下标—成员”准确访问数据；理解结构体对象、数组、指针和函数参数在内存中的关系。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">一份学生档案不能只放一个整数：它还要记录姓名、年龄和成绩。结构体像一个贴好字段标签的档案袋，把属于同一对象的不同数据装在一起。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要掌握结构体类型和对象的定义、成员访问、结构体数组、结构体指针、嵌套结构以及结构体参数的基本使用。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 定义类型与对象</h2><p class="astro-e22r5uwj">`struct Student { ... };` 定义的是一种新类型，说明每名学生对象包含哪些成员；它像统一的档案格式，本身还不是某名学生的数据。右大括号后的分号属于类型定义，不能省略。</p><p class="astro-e22r5uwj">写 `Student student;` 才创建具体对象。两个 Student 对象会分别拥有自己的 name 和 score，修改一个对象的成员不会自动改变另一个对象。</p><p class="astro-e22r5uwj">一个结构体对象要为所有成员保存数据，但 `sizeof(Student)` 不保证等于各成员 sizeof 的简单相加：编译器可能为对齐加入填充，不同平台、编译器和标准库实现也可能不同。需要真实字节数时在目标环境使用 sizeof 实测，不背固定结论。</p><pre class="astro-e22r5uwj"><code class="language-cpp">struct Student { // 定义一种包含姓名与分数的新类型\n    string name; // 每个 Student 对象各自拥有一个姓名成员\n    int score; // 每个 Student 对象各自拥有一个分数成员\n}; // 完成类型定义，末尾必须写分号\n\nStudent student; // 根据 Student 类型创建一个具体对象</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">二维数组把同一类型的数据排成“行×列”的表格，矩阵、棋盘、座位表和字符地图都可以这样保存。学会它的关键不是多写一层中括号，而是始终说清：两个下标各表示什么、循环现在走到哪个格子、这个格子在内存中的真实位置怎样计算。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">二维数组先看行、再看列；元素总数等于各维长度相乘，字节数还要乘 `sizeof(元素类型)`；访问任何格子前，两个下标都必须合法。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int grid[105][105] = {}; // 声明固定容量并把所有元素清零\nfor (int row = 1; row &lt;= rows; row++) { // 从第 1 行遍历到实际最后一行\n    for (int column = 1; column &lt;= columns; column++) { // 遍历当前行的所有实际列\n        cin &gt;&gt; grid[row][column]; // 把数据存入“当前行、当前列”\n    } // 当前行的列全部读完\n} // 所有实际行全部读完</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int score[2][3] = {{1, 7, 3}, {4, 2, 9}}; // 创建 2 行 3 列的成绩表\ncout &lt;&lt; score[0][1] &lt;&lt; &#39;\\n&#39;; // 0 下标下输出第 0 行第 1 列的 7</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">把声明容量当成实际使用量</li><li class="astro-e22r5uwj">把 0 下标与 1 下标写法混在一起</li><li class="astro-e22r5uwj">用实际列数 m 代替声明时第二维长度计算内存位置</li><li class="astro-e22r5uwj">先访问邻格，再检查它是否越界</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课只建立三个核心：用两个下标表示和定位表格；用双重循环按坐标安全遍历；用声明的各维长度解释连续存储、内存大小、函数传参和更多维数组。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">先看同一张 2×3 成绩表：第一行是 1、7、3，第二行是 4、2、9。问“数字 7 在哪里”，必须回答第 1 行第 2 列；问“所有数之和”，必须让程序按顺序访问 6 个格子。后面的定义、遍历、内存和转置都继续使用这张表，不在每一步更换问题。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要掌握二维及多维数组的定义、初始化、存储和遍历，能够把二维数组传入函数，完成行列、对角线、转置、字符地图、坐标边界、四方向和简单三维数组等基础处理。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 二维数组定义与初始化</h2><p class="astro-e22r5uwj">一维数组像一排抽屉，只用一个下标；二维数组像一张表格，要用“行下标”和“列下标”共同确定一个格子。`int score[2][3]` 中，`int` 是每个元素的类型，`score` 是数组名，第一个长度 2 表示行数，第二个长度 3 表示每行有 3 列，因此一共有 2×3=6 个 `int` 元素。</p><p class="astro-e22r5uwj">按照 C++ 原生的 0 下标，`score[0][0]` 是左上角，合法行下标是 0..1，合法列下标是 0..2；`score[0][1]` 对应第一行中间的 7。两个下标必须分别合法，只要一个越界，访问就不合法。</p><p class="astro-e22r5uwj">初始化有三种常见形式：完整初始化给出每个值；部分初始化只写前面若干值，没写到的元素自动补 0；`={}` 或 `={0}` 会把全部元素置 0。花括号按“每一行一组”书写，最容易看出表格结构。</p><p class="astro-e22r5uwj">要特别区分“声明后未初始化”和“部分初始化”：函数内部的普通局部数组 `int a[3][4];` 不会自动清零，读取尚未赋值的元素没有可靠结果；写成 `int a[3][4] = {};` 才明确全部为 0。全局数组和静态数组会自动初始化为 0，但初学阶段仍建议把意图写清楚。</p><p class="astro-e22r5uwj">本站处理行列编号时常使用 1 下标：例如实际要用 1..4 行、1..5 列，就声明 `int table[5][6] = {};`，把第 0 行和第 0 列留空。这样更接近题目坐标，但数组实际仍分配 5×6=30 个元素，不是 4×5=20 个。</p><p class="level4-dictionary-article__code-label">完整、部分、全零和 1 下标容量的写法</p><pre class="astro-e22r5uwj"><code class="language-cpp">int score[2][3] = { // 开始完整初始化 2 行 3 列的成绩表\n    {1, 7, 3}, // 给第 0 行的 3 个元素依次赋值\n    {4, 2, 9} // 给第 1 行的 3 个元素依次赋值\n}; // 完成 score 的全部初始化\nint partial[3][4] = { // 开始部分初始化 3 行 4 列的数组\n    {1, 2}, // 第 0 行成为 1、2、0、0\n    {3} // 第 1 行成为 3、0、0、0，第 2 行全部补 0\n}; // 完成 partial 的部分初始化\nint zeroed[3][4] = {}; // 把 3 行 4 列的全部元素初始化为 0\nint table[5][6] = {}; // 分配 30 个元素，逻辑上使用第 1..4 行和第 1..5 列</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">递推先给出初始状态，再用已经算出的状态推出后面的状态。遇到序列、台阶方案数或按天增长等“当前结果依赖较早结果”的问题时，要先确定“先算谁、依赖谁”。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">递推必须先定义状态，再找初值和从小状态到大状态的关系；公式中的依赖项必须先算好。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">ways[0] = 1; // 到第 0 级视为有一种“什么也不做”的方案\nways[1] = 1; // 到第 1 级只有走一步这一种方案\nfor (int i = 2; i &lt;= n; i++) { // 从第一个能由前两项推出的状态开始\n    ways[i] = ways[i - 1] + ways[i - 2]; // 按最后一步的两种来源合并方案\n} // 完成下标 2 到 n 的递推</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">sequence[1] = 2; // 给出不能继续向前推的首项\nfor (int i = 2; i &lt;= n; i++) { // 按从小到大的依赖顺序计算\n    sequence[i] = sequence[i - 1] + 3; // 用已知前一项推出当前项\n} // 完成第 2 项到第 n 项</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">状态含义没有写清就直接套公式</li><li class="astro-e22r5uwj">初始值与递推式不一致</li><li class="astro-e22r5uwj">计算当前状态时依赖的前项还没有得到</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">学完后，你能用“状态含义—初始状态—最后一步—递推式—计算顺序”五步独立写出简单递推。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">爬到第 i 级台阶的最后一步不可能凭空出现：要么从 i-1 走 1 级，要么从 i-2 走 2 级。把所有方案按最后一步分类，就能从已经算出的较小答案推出当前答案。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要根据问题确定递推状态、初始条件和递推关系，按依赖顺序计算，能够处理斐波那契型、台阶型等基础递推。</p><section aria-labelledby="algorithm-formation" class="astro-e22r5uwj"><h2 id="algorithm-formation" class="astro-e22r5uwj">算法是怎样形成的</h2><ol class="level4-dictionary-article__formation"><li class="astro-e22r5uwj">定义状态：ways[i] 表示“恰好到达第 i 级台阶”的方案数，不能只写成含义模糊的“第 i 项”。</li><li class="astro-e22r5uwj">确定初始状态：到第 1 级只有 1 种走法，到第 2 级有 1+1 和 2 两种走法，所以 ways[1]=1、ways[2]=2。</li><li class="astro-e22r5uwj">分析最后一步：到 i 的方案分成“从 i-1 走 1 级”和“从 i-2 走 2 级”两类，两类不重不漏。</li><li class="astro-e22r5uwj">写出关系并确定顺序：ways[i]=ways[i-1]+ways[i-2]，因此必须从小到大计算。</li><li class="astro-e22r5uwj">最后核对边界与数值：n=1 时应直接使用初值，n=2 才第一次使用两来源公式；方案数增长很快，还要让数组容量和整数类型覆盖题目范围。</li></ol></section><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 状态、初始值与递推关系</h2><p class="astro-e22r5uwj">状态要说明下标代表什么、数值代表什么。初始值是不能再由更早状态推出的起点。</p><p class="astro-e22r5uwj">递推关系写清当前状态依赖哪些已知状态；三者缺一，递推就无法正确开始。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">先定义 f[i] 的含义</li><li class="astro-e22r5uwj">写出所有必要初始值</li><li class="astro-e22r5uwj">确认右侧状态在计算当前项前已经得到</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-02"><h2 id="topic-02" class="astro-e22r5uwj">02 斐波那契型递推</h2><p class="astro-e22r5uwj">若每一项等于前两项之和，就要准备两个初始值，再从第三项开始。这里的“像斐波那契”只描述依赖形状，初值仍必须由题意确定。</p><p class="astro-e22r5uwj">数组最大合法下标要覆盖 n，数值类型也要覆盖结果。增长很快的序列即使下标不大也可能超过 int；本页完整程序把 n 限制为 90 并使用 long long。</p><pre class="astro-e22r5uwj"><code class="language-cpp">f[1] = 1; // 设置第一项初值\nf[2] = 1; // 设置第二项初值\nfor (int i = 3; i &lt;= n; i++) { // 从第三项开始按依赖顺序计算\n    f[i] = f[i - 1] + f[i - 2]; // 当前项等于前两项之和\n} // 完成第 3 项到第 n 项</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-03"><h2 id="topic-03" class="astro-e22r5uwj">03 台阶型递推</h2><p class="astro-e22r5uwj">每次走 1 级或 2 级时，到达第 i 级的最后一步只能来自 i-1 或 i-2；两类最后一步不同，互不重复，且覆盖所有方案，所以方案数相加。</p><p class="astro-e22r5uwj">把 ways[0] 定义为 1 种空方案后，ways[1]=1，统一关系可从 i=2 开始。若换一种状态定义，初值也会跟着变，不能脱离含义只背公式。</p><pre class="astro-e22r5uwj"><code class="language-cpp">ways[0] = 1; // 空台阶只有一种不移动方案\nways[1] = 1; // 到第 1 级只有走 1 级的方案\nfor (int i = 2; i &lt;= n; i++) { // 从第 2 级开始合并两个来源\n    ways[i] = ways[i - 1] + ways[i - 2]; // 最后走 1 级或 2 级\n} // 完成到第 n 级的全部状态</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-04"><h2 id="topic-04" class="astro-e22r5uwj">04 存储与计算顺序</h2><p class="astro-e22r5uwj">数组可以保存每个状态，便于输出中间值或让后续多次使用。</p><p class="astro-e22r5uwj">循环方向必须满足依赖关系。若 f[i] 依赖 f[i-1]，通常按 i 递增计算；倒序会读取尚未得到的值。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">已知 f[1]=2，关系为 f[i]=f[i-1]+i</li><li class="astro-e22r5uwj">必须先算 f[2]=4，才能算 f[3]=7，再算 f[4]=11</li><li class="astro-e22r5uwj">第一次填表若从 i=4 倒推，会读取尚未计算的 f[3]，结果没有依据</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-05"><h2 id="topic-05" class="astro-e22r5uwj">05 常见递推序列</h2><p class="astro-e22r5uwj">等差型可写成前一项加固定差；前缀和可写成前一段和加当前数；增长过程也可根据上一时刻状态推下一时刻。它们共同点是当前状态依赖已经算出的较早状态。</p><p class="astro-e22r5uwj">简单路径也能递推：只能向右或向下走时，到达 `(row, column)` 的最后一步只能来自上方或左方，所以两个来源相加。</p><p class="astro-e22r5uwj">杨辉三角的两个边界数先设为 1，内部数字由上一行左上方与右上方相加。它与网格路径都是“两来源合并”，但状态含义和边界初值不同，不能只看公式相似就混用。</p><p class="astro-e22r5uwj">下面只演示固定 2×3 小网格，结果都在 long long 范围内。更大网格的方案数增长很快，不能因为数组开得下就默认数值也不会溢出。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">2×3 网格第一行和第一列都只有 1 种走法</li><li class="astro-e22r5uwj">ways[2][2]=1+1=2，ways[2][3]=1+2=3</li><li class="astro-e22r5uwj">因此从左上角到右下角共有 3 条简单路径；每个右侧状态都来自已经算出的上方和左方</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">long long ways[3][4] = {}; // 为 1 下标的 2 行 3 列小网格分配空间\nfor (int row = 1; row &lt;= 2; row++) { // 遍历第一列的两个位置\n    ways[row][1] = 1; // 第一列只能一直向下到达\n} // 完成第一列边界初始化\nfor (int column = 1; column &lt;= 3; column++) { // 遍历第一行的三个位置\n    ways[1][column] = 1; // 第一行只能一直向右到达\n} // 完成第一行边界初始化\nfor (int row = 2; row &lt;= 2; row++) { // 从第 2 行开始填内部状态\n    for (int column = 2; column &lt;= 3; column++) { // 从第 2 列填到第 3 列\n        ways[row][column] = ways[row - 1][column] + ways[row][column - 1]; // 合并上方和左方来源\n    } // 结束当前行内部状态计算\n} // 完成整个 2×3 网格递推</code></pre></section><section aria-labelledby="quick-table" class="astro-e22r5uwj"><h2 id="quick-table" class="astro-e22r5uwj">知识点速查表</h2><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">项目</th><th class="astro-e22r5uwj">含义</th><th class="astro-e22r5uwj">检查重点</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">状态</td><td class="astro-e22r5uwj">f[i] 表示什么</td><td class="astro-e22r5uwj">下标和数值含义明确</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">初始值</td><td class="astro-e22r5uwj">递推起点</td><td class="astro-e22r5uwj">覆盖所有无法继续向前推的情况</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">关系式</td><td class="astro-e22r5uwj">当前项怎样由旧项得到</td><td class="astro-e22r5uwj">右侧状态已计算</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">顺序</td><td class="astro-e22r5uwj">填表方向</td><td class="astro-e22r5uwj">满足依赖方向</td></tr></tbody></table></div></section><section aria-labelledby="algorithm-trace" class="astro-e22r5uwj"><h2 id="algorithm-trace" class="astro-e22r5uwj">算法实际怎样执行</h2><h3 class="astro-e22r5uwj">n = 5 时的真实计算过程</h3><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">i</th><th class="astro-e22r5uwj">来自 i-1</th><th class="astro-e22r5uwj">来自 i-2</th><th class="astro-e22r5uwj">ways[i]</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">0</td><td class="astro-e22r5uwj">初始状态</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">1</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">初始状态</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">1</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">ways[1] = 1</td><td class="astro-e22r5uwj">ways[0] = 1</td><td class="astro-e22r5uwj">2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">ways[2] = 2</td><td class="astro-e22r5uwj">ways[1] = 1</td><td class="astro-e22r5uwj">3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">4</td><td class="astro-e22r5uwj">ways[3] = 3</td><td class="astro-e22r5uwj">ways[2] = 2</td><td class="astro-e22r5uwj">5</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">5</td><td class="astro-e22r5uwj">ways[4] = 5</td><td class="astro-e22r5uwj">ways[3] = 3</td><td class="astro-e22r5uwj">8</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">6</td><td class="astro-e22r5uwj">检查 6≤5 为假</td><td class="astro-e22r5uwj">不再读取</td><td class="astro-e22r5uwj">循环结束</td></tr></tbody></table></div></section><section aria-labelledby="examples" class="astro-e22r5uwj"><h2 id="examples" class="astro-e22r5uwj">典型例题</h2><h3 class="astro-e22r5uwj">计算爬到第 n 级台阶的方案数</h3><p class="astro-e22r5uwj">教学程序约定 1≤n≤90，每次走 1 级或 2 级，求恰好到达第 n 级台阶的方案数。</p><h3 class="astro-e22r5uwj">完成程序</h3><pre class="astro-e22r5uwj"><code class="language-cpp">#include &lt;bits/stdc++.h&gt; // 引入本程序需要的标准库\nusing namespace std; // 允许直接使用 cin、cout、string 等标准库名称\n\nint main() { // 主函数是程序的入口\n    int n; // 定义目标台阶编号\n    cin &gt;&gt; n; // 读入需要到达的台阶数\n    long long ways[105] = {}; // 创建递推数组并初始化为 0\n    ways[0] = 1; // 设置第 0 级为一种什么也不做的空方案\n    ways[1] = 1; // 到第 1 级只有直接走 1 级这一种方案\n    for (int i = 2; i &lt;= n; i++) { // 从第一个能由前两项推出的位置开始\n        ways[i] = ways[i - 1] + ways[i - 2]; // 按最后一步分类并合并两类方案\n    } // 结束递推填表循环\n    cout &lt;&lt; ways[n] &lt;&lt; &#39;\\n&#39;; // 输出恰好到达第 n 级的方案数\n    return 0; // 返回 0，表示程序正常结束\n} // 结束 main 函数</code></pre><div class="level4-dictionary-article__io"><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输入</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">5</code></pre><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输出</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">8</code></pre></div><h3 class="astro-e22r5uwj">解题与执行过程</h3><ol class="astro-e22r5uwj"><li class="astro-e22r5uwj">先定义状态：ways[i] 表示恰好到达第 i 级的方案数；空台阶有一种不移动方案，所以 ways[0]=1，ways[1]=1。</li><li class="astro-e22r5uwj">i=2 时按最后一步分类，ways[2]=ways[1]+ways[0]=2；这也对应 1+1 和 2 两种走法。</li><li class="astro-e22r5uwj">随后依次得到 ways[3]=3、ways[4]=5、ways[5]=8，每一步只读取已经算好的两个较小下标。</li><li class="astro-e22r5uwj">i 增加到 6 时条件 6≤5 首次失败，循环停止并输出 ways[5]；n=1 时循环一开始就失败，仍能正确输出初值 1。</li></ol></section><section aria-labelledby="practice" class="astro-e22r5uwj"><h2 id="practice" class="astro-e22r5uwj">分层练习</h2><p class="astro-e22r5uwj">先独立完成，再展开提示核对思路。三题分别练基本识别、完整应用和换情境迁移。</p><div class="level4-dictionary-article__practice-grid"><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">基础</p><h3 class="astro-e22r5uwj">补全等差递推</h3><p class="astro-e22r5uwj">首项为 4，每项比前一项多 3，写出初值、递推式和前 5 项。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">state[i] 只依赖 state[i-1]。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">巩固</p><h3 class="astro-e22r5uwj">改成走 1 或 3 级</h3><p class="astro-e22r5uwj">每次只能走 1 级或 3 级，定义状态并写出合法的初值与递推关系。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">先处理 i 小于 3 的边界，再按最后一步分类。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">迁移</p><h3 class="astro-e22r5uwj">网格路径递推</h3><p class="astro-e22r5uwj">只能向右或向下时，解释 path[row][column] 的两个来源。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">最后一步来自上方或左方，边界行列需要单独初始化。</p></details></article></div></section><section aria-labelledby="mistakes" class="astro-e22r5uwj"><h2 id="mistakes" class="astro-e22r5uwj">常见错误</h2><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">初始值不足</h3><p class="astro-e22r5uwj">错误写法：f[i] 依赖前两项，却只设置 f[1]。原因：计算 f[3] 时缺少 f[2]。正确写法：写全所有不能由关系式继续向前推出的初始状态。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">循环起点错误</h3><p class="astro-e22r5uwj">错误写法：已经给出 f[1]、f[2]，循环仍从 i = 2 开始套用前两项关系。原因：会访问 f[0] 或覆盖初值。正确写法：从第一个能由关系式推出的位置开始。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">下标越界</h3><p class="astro-e22r5uwj">错误写法：数组只开到 f[100]，却计算并访问 f[100]。原因：0 下标数组最后位置是 f[99]。正确写法：按最大下标多开足够空间。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">计算方向违背依赖</h3><p class="astro-e22r5uwj">错误写法：f[i] 依赖 f[i-1] 却从大下标向小下标首次计算。原因：右侧状态尚未得到。正确写法：先画依赖关系，再按小下标到大下标填表。</p></div></section><section aria-labelledby="related-learning" class="astro-e22r5uwj"><h2 id="related-learning" class="astro-e22r5uwj">关联学习</h2><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj"><a href="/gesp/level4/complexity/" class="astro-e22r5uwj">程序复杂度分析</a>：可以估算递推填表所需的时间和空间。</li></ul></section><section aria-labelledby="core-check" class="astro-e22r5uwj"><h2 id="core-check" class="astro-e22r5uwj">核心内容检查</h2><gesp-dictionary-review class="dictionary-review-check" data-level="4" data-lesson-id="recurrence" data-progress-event="xdz:gesp-level4-progress"><section class="astro-l37rdmqc"></section></gesp-dictionary-review></section><section aria-labelledby="test" class="astro-e22r5uwj"><h2 id="test" class="astro-e22r5uwj">整页综合测试</h2><gesp-dictionary-test class="dictionary-test" data-level="4" data-lesson-id="recurrence" data-progress-event="xdz:gesp-level4-progress"><p class="dictionary-test__intro">请完成全部 8 道单选题后统一提交。测试记录与“已复习”状态相互独立。</p><div class="dictionary-test__questions"></div><div class="dictionary-test__result" data-test-result hidden aria-live="polite"></div><div class="dictionary-test__actions"></div></gesp-dictionary-test></section><nav class="dictionary-lesson-pager"><a href="/gesp/level4/multidimensional-arrays/" rel="prev" class="astro-hl664apc"><span class="astro-hl664apc">上一知识点</span><strong class="astro-hl664apc">二维与多维数组</strong></a><a href="/gesp/level4/sorting/" rel="next" class="astro-hl664apc"><span class="astro-hl664apc">下一知识点</span><strong class="astro-hl664apc">排序算法</strong></a></nav></article></div><footer',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">排序按指定关键字重新排列数据。需要按成绩、编号或数值大小组织结果时，可以使用排序；四级重点是亲手追踪冒泡、插入和选择排序，而不是直接调用库函数。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">冒泡让极值逐趟到边界，插入把当前元素放进已排序区，选择每趟挑出未排序区的最小元素；三者最坏通常为 O(n²)。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">if (values[i] &gt; values[i + 1]) { // 相邻两项逆序时才需要交换\n    int temporary = values[i]; // 暂存左侧值，防止被覆盖\n    values[i] = values[i + 1]; // 把较小值放到左边\n    values[i + 1] = temporary; // 把原左值放到右边\n} // 完成一对相邻元素的升序调整</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int values[4] = {0, 3, 1, 2}; // 采用 1 下标，0 号位置占位\n// 从左向右比较一趟后为 1、2、3，最大值 3 到达末尾</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">冒泡访问 values[i+1] 时循环到末尾导致越界</li><li class="astro-e22r5uwj">插入排序右移前没有保存当前值</li><li class="astro-e22r5uwj">选择排序只记最小值却没有记录最小值下标</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">学完后，你能在同一组数据上逐趟执行冒泡、插入和选择排序，写出正确边界，并判断稳定性。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">整理一手数字卡片时，可以反复交换相邻逆序卡片，也可以把新卡插入已排好部分，还可以每次挑出最小卡放到前面。三种动作分别形成三种基础排序。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要掌握冒泡排序、插入排序和选择排序，理解比较、交换或移动过程，能够估算复杂度并识别稳定性。</p><section aria-labelledby="algorithm-formation" class="astro-e22r5uwj"><h2 id="algorithm-formation" class="astro-e22r5uwj">算法是怎样形成的</h2><ol class="level4-dictionary-article__formation"><li class="astro-e22r5uwj">任务是把 4 2 5 1 3 排成升序。先尝试只看相邻卡片：4&gt;2，所以交换；4&lt;5，不交换；5&gt;1、5&gt;3，继续交换。走完一遍后，5 一定到了最右边。</li><li class="astro-e22r5uwj">右端的 5 已经就位，不必再碰。对前四个数重复相邻比较，每趟都把未排序区最大值送到右端，这就形成冒泡排序。</li><li class="astro-e22r5uwj">还可以把左边当成已经排好的区域。开始只有 [4]；拿到 2 时，把更大的 4 右移，再把 2 放进空位，左侧变成 [2 4]。</li><li class="astro-e22r5uwj">每次先保存当前值，再右移所有比它大的元素，最后插入空位；已排序区每轮扩大一个位置，这就形成插入排序。</li><li class="astro-e22r5uwj">第三种想法是先找“接下来应该放谁”。在 4 2 5 1 3 中找到最小值 1，把它与第一个位置交换，左端的 1 立即就位。</li><li class="astro-e22r5uwj">之后只在剩余区域继续找最小值并放到区域开头，每趟确定左端一个位置，这就形成选择排序。</li><li class="astro-e22r5uwj">三种方法都只交换或移动原有元素，不会凭空增加或漏掉数据；当未排序区为空时停止。它们最坏通常为 O(n²)，稳定性要看相等元素是否被跨越。</li></ol></section><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 排序目标与稳定性</h2><p class="astro-e22r5uwj">排序先确定关键字和方向。按整数升序要求前项不大于后项；结构体可以按 score 等某个成员作为关键字，移动时仍要保持整个对象完整。</p><p class="astro-e22r5uwj">待排序数据都放在内存中处理称为内排序；数据量太大、过程需要外部文件参与称为外排序。四级编程核心是数组上的三种内排序。</p><p class="astro-e22r5uwj">稳定性只讨论“关键字相等的不同记录”原相对顺序是否保留。冒泡使用严格大于才交换、插入使用严格大于才右移时可以稳定；直接交换远处最小值的选择排序通常不稳定。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">原顺序为 (2,A)、(2,B)、(1,C)，A 在 B 前</li><li class="astro-e22r5uwj">选择排序第一趟把 (1,C) 与首项交换，得到 (1,C)、(2,B)、(2,A)</li><li class="astro-e22r5uwj">两个关键字同为 2 的记录从 A-B 变成 B-A，所以这份选择排序不稳定</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-02"><h2 id="topic-02" class="astro-e22r5uwj">02 冒泡排序</h2><p class="astro-e22r5uwj">从左到右比较相邻元素，左大右小时交换。一个较大元素可能连续向右移动；一趟结束后，未排序区最大值一定到达右端。</p><p class="astro-e22r5uwj">end 表示本趟未排序区的最后位置，内层必须满足 i&lt;end 才能安全访问 i+1。每完成一趟，end 左移；若整趟没有交换，说明当前数组已经有序，可以 break。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">初始：4 2 5 1 3</li><li class="astro-e22r5uwj">第 1 趟：2 4 1 3 5，最大值 5 到末尾</li><li class="astro-e22r5uwj">第 2 趟：2 1 3 4 5</li><li class="astro-e22r5uwj">继续处理未排序区，最终得到 1 2 3 4 5</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">for (int end = n; end &gt;= 2; end--) { // end 是当前未排序区右边界\n    bool changed = false; // 记录本趟是否发生过交换\n    for (int i = 1; i &lt; end; i++) { // 比较到 end-1，保证 i+1 不越界\n        if (values[i] &gt; values[i + 1]) { // 相邻元素逆序时交换\n            int temporary = values[i]; // 暂存左值\n            values[i] = values[i + 1]; // 较小的右值移到左边\n            values[i + 1] = temporary; // 原左值移到右边\n            changed = true; // 标记本趟确实改变了序列\n        } // 结束当前相邻对判断\n    } // 完成本趟全部相邻比较\n    if (!changed) { // 本趟零交换表示数组已经升序\n        break; // 立即停止不必要的后续趟数\n    } // 结束提前终止判断\n} // 冒泡排序结束</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-03"><h2 id="topic-03" class="astro-e22r5uwj">03 插入排序</h2><p class="astro-e22r5uwj">始终维持 values[1..i-1] 已经有序。先把 values[i] 保存为 current，再从有序区右端向左寻找位置；比 current 大的元素依次右移，最后把 current 放进留下的空位。</p><p class="astro-e22r5uwj">while 条件要先检查 position&gt;=1，再访问 values[position]。只在严格大于时右移，相等记录不会跨过 current，因此可以保持稳定。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">初始有序区：[4]</li><li class="astro-e22r5uwj">插入 2 后：[2 4] 5 1 3</li><li class="astro-e22r5uwj">插入 5 后：[2 4 5] 1 3</li><li class="astro-e22r5uwj">继续插入 1、3，最终得到 [1 2 3 4 5]</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">for (int i = 2; i &lt;= n; i++) { // 依次把第 i 个元素插入左侧有序区\n    int current = values[i]; // 右移之前保存待插入值\n    int position = i - 1; // 从有序区最右端开始检查\n    while (position &gt;= 1 &amp;&amp; values[position] &gt; current) { // 边界合法且前项更大时右移\n        values[position + 1] = values[position]; // 给 current 腾出一个位置\n        position--; // 继续向左寻找插入点\n    } // 条件首次失败时，position+1 就是插入位置\n    values[position + 1] = current; // 把保存的元素放入空位\n} // 全部元素插入后数组升序</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-04"><h2 id="topic-04" class="astro-e22r5uwj">04 选择排序</h2><p class="astro-e22r5uwj">每一趟先假设 start 位置最小，再扫描 start+1 到 n；发现更小元素时只更新 minimum 下标。扫描结束后才交换一次，把真正最小值放到 start。</p><p class="astro-e22r5uwj">即使一趟只交换一次，寻找最小值仍要查看整个未排序区，所以最坏比较次数仍为二次数量级。远距离交换可能跨过相等记录，因此通常不稳定。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">初始：4 2 5 1 3</li><li class="astro-e22r5uwj">第 1 趟选到最小值 1：1 2 5 4 3</li><li class="astro-e22r5uwj">第 2 趟最小值已经是 2：1 2 5 4 3</li><li class="astro-e22r5uwj">继续选择未排序区最小值，最终得到 1 2 3 4 5</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">for (int start = 1; start &lt; n; start++) { // 每趟确定 start 位置的元素\n    int minimum = start; // 先假设未排序区首项最小\n    for (int i = start + 1; i &lt;= n; i++) { // 扫描未排序区其余位置\n        if (values[i] &lt; values[minimum]) { // 找到更小元素时更新候选\n            minimum = i; // 保存最小元素下标而不只保存数值\n        } // 结束当前元素比较\n    } // 扫描结束后 minimum 指向本区最小值\n    int temporary = values[start]; // 暂存未排序区首项\n    values[start] = values[minimum]; // 把最小值放到已确定位置\n    values[minimum] = temporary; // 把原首项放到最小值原位置\n} // 完成所有选择排序趟数</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-05"><h2 id="topic-05" class="astro-e22r5uwj">05 三种排序的比较</h2><p class="astro-e22r5uwj">三种算法最坏时间复杂度通常都是 O(n²)，额外空间都是 O(1)。这说明增长数量级相同，不表示每组数据做的操作完全一样。</p><p class="astro-e22r5uwj">冒泡交换相邻元素，插入移动有序区，选择扫描并记录最小下标。稳定性必须结合严格比较与实际移动方式判断，不能只背算法名字。</p><p class="astro-e22r5uwj">结构体数组排序时，比较的是某个成员，交换或移动的必须是整个结构体对象。下面用已经讲清的选择排序按 score 升序排列，姓名和分数才不会错位。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">Alice 88、Bob 95、Carol 91 按 score 升序后是 Alice 88、Carol 91、Bob 95</li><li class="astro-e22r5uwj">只交换 score 会让姓名与成绩错配；交换整个 Student 才保持同一对象的成员关系</li><li class="astro-e22r5uwj">n=5 时，最坏情况下三种排序都要进行数量级约为 5² 的比较或移动</li><li class="astro-e22r5uwj">数据接近有序时，带提前结束的冒泡和插入排序可能少做很多工作</li><li class="astro-e22r5uwj">选择排序每趟仍要扫描未排序区寻找最小值，但交换次数通常较少</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">for (int start = 1; start &lt; n; start++) { // 逐趟确定结构体数组左端位置\n    int minimum = start; // 保存当前最低分学生的下标\n    for (int i = start + 1; i &lt;= n; i++) { // 扫描剩余学生对象\n        if (students[i].score &lt; students[minimum].score) { // 只比较 score 关键字\n            minimum = i; // 更新最低分对象的完整下标\n        } // 结束当前学生比较\n    } // 完成本趟最低分查找\n    Student temporary = students[start]; // 暂存整个学生对象\n    students[start] = students[minimum]; // 把最低分学生对象放到 start\n    students[minimum] = temporary; // 把原 start 对象整体放回空位\n} // 完成结构体数组选择排序</code></pre></section><section aria-labelledby="quick-table" class="astro-e22r5uwj"><h2 id="quick-table" class="astro-e22r5uwj">知识点速查表</h2><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">项目</th><th class="astro-e22r5uwj">含义</th><th class="astro-e22r5uwj">检查重点</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">冒泡</td><td class="astro-e22r5uwj">相邻比较交换</td><td class="astro-e22r5uwj">一趟确定末尾</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">插入</td><td class="astro-e22r5uwj">有序区右移后插入</td><td class="astro-e22r5uwj">保存 current</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">选择</td><td class="astro-e22r5uwj">找未排序区最小值</td><td class="astro-e22r5uwj">保存下标再交换</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">稳定性</td><td class="astro-e22r5uwj">相等元素相对顺序是否保留</td><td class="astro-e22r5uwj">分析实际移动方式</td></tr></tbody></table></div></section><section aria-labelledby="algorithm-trace" class="astro-e22r5uwj"><h2 id="algorithm-trace" class="astro-e22r5uwj">算法实际怎样执行</h2><h3 class="astro-e22r5uwj">同一组 4 2 5 1 3 的三种排序完整过程</h3><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">步骤</th><th class="astro-e22r5uwj">本步判断与动作</th><th class="astro-e22r5uwj">本步后序列</th><th class="astro-e22r5uwj">已确定区域或下一步</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">冒泡第 1 趟</td><td class="astro-e22r5uwj">4&gt;2 换；4&lt;5 不换；5&gt;1 换；5&gt;3 换</td><td class="astro-e22r5uwj">2 4 1 3 5</td><td class="astro-e22r5uwj">位置 5 的 5 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">冒泡第 2 趟</td><td class="astro-e22r5uwj">2&lt;4 不换；4&gt;1 换；4&gt;3 换</td><td class="astro-e22r5uwj">2 1 3 4 5</td><td class="astro-e22r5uwj">位置 4～5 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">冒泡第 3 趟</td><td class="astro-e22r5uwj">2&gt;1 换；2&lt;3 不换</td><td class="astro-e22r5uwj">1 2 3 4 5</td><td class="astro-e22r5uwj">位置 3～5 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">冒泡第 4 趟</td><td class="astro-e22r5uwj">1&lt;2，不交换</td><td class="astro-e22r5uwj">1 2 3 4 5</td><td class="astro-e22r5uwj">位置 2～5 已确定，结束</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">插入 i=2</td><td class="astro-e22r5uwj">保存 2；4&gt;2，把 4 右移；插入 2</td><td class="astro-e22r5uwj">2 4 5 1 3</td><td class="astro-e22r5uwj">左侧 2 个元素有序</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">插入 i=3</td><td class="astro-e22r5uwj">保存 5；4&gt;5 不成立，原位插入</td><td class="astro-e22r5uwj">2 4 5 1 3</td><td class="astro-e22r5uwj">左侧 3 个元素有序</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">插入 i=4</td><td class="astro-e22r5uwj">保存 1；5、4、2 依次右移；插入 1</td><td class="astro-e22r5uwj">1 2 4 5 3</td><td class="astro-e22r5uwj">左侧 4 个元素有序</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">插入 i=5</td><td class="astro-e22r5uwj">保存 3；5、4 右移；在 2 后插入 3</td><td class="astro-e22r5uwj">1 2 3 4 5</td><td class="astro-e22r5uwj">左侧 5 个元素有序，结束</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">选择 start=1</td><td class="astro-e22r5uwj">扫描全区，最小值 1 在位置 4；与 4 交换</td><td class="astro-e22r5uwj">1 2 5 4 3</td><td class="astro-e22r5uwj">位置 1 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">选择 start=2</td><td class="astro-e22r5uwj">剩余区最小值就是位置 2 的 2，不必改变</td><td class="astro-e22r5uwj">1 2 5 4 3</td><td class="astro-e22r5uwj">位置 1～2 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">选择 start=3</td><td class="astro-e22r5uwj">剩余区最小值 3 在位置 5；与 5 交换</td><td class="astro-e22r5uwj">1 2 3 4 5</td><td class="astro-e22r5uwj">位置 1～3 已确定</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">选择 start=4</td><td class="astro-e22r5uwj">剩余区最小值就是位置 4 的 4，不必改变</td><td class="astro-e22r5uwj">1 2 3 4 5</td><td class="astro-e22r5uwj">只剩最后一个位置，结束</td></tr></tbody></table></div></section><section aria-labelledby="examples" class="astro-e22r5uwj"><h2 id="examples" class="astro-e22r5uwj">典型例题</h2><h3 class="astro-e22r5uwj">用冒泡排序完成升序排列</h3><p class="astro-e22r5uwj">教学程序约定 1≤n≤100、每个元素在 int 范围内，使用 1 下标冒泡排序后输出。</p><h3 class="astro-e22r5uwj">完成程序</h3><pre class="astro-e22r5uwj"><code class="language-cpp">#include &lt;bits/stdc++.h&gt; // 引入本程序需要的标准库\nusing namespace std; // 允许直接使用 cin、cout、string 等标准库名称\n\nint main() { // 主函数是程序的入口\n    int n; // 定义待排序元素数量\n    cin &gt;&gt; n; // 读入元素数量\n    int values[105] = {}; // 创建 1 下标数组并初始化为 0\n    for (int i = 1; i &lt;= n; i++) { // 遍历所有待排序位置\n        cin &gt;&gt; values[i]; // 读入第 i 个元素\n    } // 结束数组输入循环\n\n    for (int end = n; end &gt;= 2; end--) { // 每趟把未排序区的最大值放到 end\n        for (int i = 1; i &lt; end; i++) { // 比较未排序区中的每一对相邻元素\n            if (values[i] &gt; values[i + 1]) { // 发现相邻元素不满足升序\n                int temporary = values[i]; // 暂存左侧元素，防止交换时丢失\n                values[i] = values[i + 1]; // 把较小的右侧元素移到左边\n                values[i + 1] = temporary; // 把原左侧元素放到右边\n            } // 结束本次是否交换的判断\n        } // 结束当前一趟的相邻比较\n    } // 结束所有冒泡排序趟数\n\n    for (int i = 1; i &lt;= n; i++) { // 按顺序遍历排序后的数组\n        if (i == n) { // 判断当前元素是否为最后一个输出项\n            cout &lt;&lt; values[i] &lt;&lt; &#39;\\n&#39;; // 输出末元素并结束本行\n        } else { // 尚未到末元素时使用空格分隔\n            cout &lt;&lt; values[i] &lt;&lt; &#39; &#39;; // 输出当前元素和一个空格\n        } // 结束输出格式判断\n    } // 结束结果输出循环\n    return 0; // 返回 0，表示程序正常结束\n} // 结束 main 函数</code></pre><div class="level4-dictionary-article__io"><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输入</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">5\n4 2 5 1 3</code></pre><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输出</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">1 2 3 4 5</code></pre></div><h3 class="astro-e22r5uwj">解题与执行过程</h3><ol class="astro-e22r5uwj"><li class="astro-e22r5uwj">初始数组是 4 2 5 1 3，比较条件 values[i] &gt; values[i + 1] 表示目标为升序。</li><li class="astro-e22r5uwj">第一趟相邻比较后得到 2 4 1 3 5，最大值 5 到达末尾。</li><li class="astro-e22r5uwj">第二趟只检查前 4 个位置，得到 2 1 3 4 5；后续每趟继续缩小未排序区。</li><li class="astro-e22r5uwj">所有趟完成后数组为 1 2 3 4 5，输出时按下标 1 到 n 访问。</li></ol></section><section aria-labelledby="practice" class="astro-e22r5uwj"><h2 id="practice" class="astro-e22r5uwj">分层练习</h2><p class="astro-e22r5uwj">先独立完成，再展开提示核对思路。三题分别练基本识别、完整应用和换情境迁移。</p><div class="level4-dictionary-article__practice-grid"><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">基础</p><h3 class="astro-e22r5uwj">手算一趟冒泡</h3><p class="astro-e22r5uwj">对 6 2 4 1 执行一趟从左到右的冒泡，写出每次比较后的序列。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">只在左数大于右数时交换。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">巩固</p><h3 class="astro-e22r5uwj">补全插入排序</h3><p class="astro-e22r5uwj">补全“保存 current—右移—插入”三步代码，并说明每一步防止了什么错误。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">移动前一定要保存 values[i]。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">迁移</p><h3 class="astro-e22r5uwj">按结构体成员排序</h3><p class="astro-e22r5uwj">把 Student 数组按 score 升序排列，并说明为什么交换时不能只交换 score。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">比较 students[i].score，位置调整时交换整个 Student 对象，保持姓名与分数对应。</p></details></article></div></section><section aria-labelledby="mistakes" class="astro-e22r5uwj"><h2 id="mistakes" class="astro-e22r5uwj">常见错误</h2><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">内层循环访问 i+1 越界</h3><p class="astro-e22r5uwj">错误写法：冒泡内层写 i &lt;= end 后仍访问 values[i + 1]。原因：i = end 时访问越过当前范围。正确写法：保证 i &lt; end。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">排序趟数和未排序区不缩小</h3><p class="astro-e22r5uwj">错误写法：每一趟都重复检查整个数组末尾。原因：已就位元素被无谓重复处理。正确写法：让 end 或 start 随趟数缩小未排序区。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">插入前没有保存 current</h3><p class="astro-e22r5uwj">错误写法：先右移 values[i - 1] 覆盖 values[i]，之后才读取待插入值。原因：原值已经丢失。正确写法：右移前先保存 current。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">选择排序只保存最小值、不保存下标</h3><p class="astro-e22r5uwj">错误写法：只记 minimumValue，最后不知道和哪个位置交换。原因：交换需要位置。正确写法：保存 minimum 下标，再交换 values[start] 与 values[minimum]。</p></div></section><section aria-labelledby="core-check" class="astro-e22r5uwj"><h2 id="core-check" class="astro-e22r5uwj">核心内容检查</h2><gesp-dictionary-review class="dictionary-review-check" data-level="4" data-lesson-id="sorting" data-progress-event="xdz:gesp-level4-progress"><section class="astro-l37rdmqc"></section></gesp-dictionary-review></section><section aria-labelledby="test" class="astro-e22r5uwj"><h2 id="test" class="astro-e22r5uwj">整页综合测试</h2><gesp-dictionary-test class="dictionary-test" data-level="4" data-lesson-id="sorting" data-progress-event="xdz:gesp-level4-progress"><p class="dictionary-test__intro">请完成全部 6 道单选题后统一提交。测试记录与“已复习”状态相互独立。</p><div class="dictionary-test__questions"></div><div class="dictionary-test__result" data-test-result hidden aria-live="polite"></div><div class="dictionary-test__actions"></div></gesp-dictionary-test></section></article></div><footer',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">复杂度关注输入规模增大时，程序所需时间和额外空间怎样增长。写程序前估算复杂度，可以判断它在题目数据范围和时间限制下是否可能运行完，而不是猜某台电脑上的具体秒数。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">先数主要操作，再保留增长最快的数量级；并列步骤相加，嵌套步骤按真实次数相乘。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">一层遍历：O(n) // 主要操作次数与 n 同数量级\n两层各遍历 n 次：O(n²) // 内层总次数为 n 乘 n\n三个固定变量：O(1) 空间 // 变量数量不随 n 增长</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">for (int i = 1; i &lt;= n; i++) { // 循环体恰好成功执行 n 次\n    operations++; // 每轮记录一次主要操作\n} // i=n+1 时条件首次失败并结束</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">看到两个循环就一律判断 O(n²)</li><li class="astro-e22r5uwj">把代码行数当成实际执行次数</li><li class="astro-e22r5uwj">分析空间时漏掉随 n 增长的辅助数组</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">本课建立三个核心：从真实执行记录数出主要操作；比较 O(1)、O(n)、O(n²)、O(n³)、O(2ⁿ) 的增长；把时间工作量与额外存储量分开估算。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">两段程序在 n=10 时都很快，不代表 n=100000 时仍一样。复杂度不猜某台电脑要几秒，而是观察 n 变大后，主要操作次数按什么速度增长。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要根据主要操作次数估算时间复杂度，识别 O(1)、O(n)、O(n²)、O(n³)、O(2ⁿ) 等增长，并估算辅助空间。</p><section aria-labelledby="algorithm-formation" class="astro-e22r5uwj"><h2 id="algorithm-formation" class="astro-e22r5uwj">算法是怎样形成的</h2><ol class="level4-dictionary-article__formation"><li class="astro-e22r5uwj">先观察本页完成程序，输入规模只有 n。单层循环每成功进入一次就把 singleCount 加 1；n=3 时，i=1、2、3 共进入 3 次。</li><li class="astro-e22r5uwj">把 n 改成任意正整数，单层循环仍恰好进入 n 次，所以主要操作次数是 n；n 扩大 10 倍，次数也扩大 10 倍，记为 O(n)。</li><li class="astro-e22r5uwj">双层循环中，每个固定的外层 i 都让 j 从 1 走到 n，因此一次外层带来 n 次计数；外层本身又有 n 轮，总次数是 n×n。</li><li class="astro-e22r5uwj">n=3 时可以数到 9 次；n 扩大 10 倍时，n² 扩大 100 倍，因此双层部分记为 O(n²)。这一步来自真实次数，不是因为代码看起来有两层大括号。</li><li class="astro-e22r5uwj">若分析整个程序，两个部分先后执行，次数相加为 n+n²；n 越大，n² 增长更快，所以整体时间复杂度由 n² 主导。</li><li class="astro-e22r5uwj">最后检查存储：程序只有 n、i、j 和两个计数器，变量个数不随 n 增加，额外空间是 O(1)。换成长为 n 的辅助数组才会变成 O(n)。</li><li class="astro-e22r5uwj">若每个阶段都产生两个继续展开的分支，阶段 0、1、2、3 的分支数依次可能是 1、2、4、8；到第 n 层达到 2ⁿ 量级，这就是指数增长的形成来源。</li><li class="astro-e22r5uwj">一般分析时沿用同一方法：确定规模，选择主要操作，数真实次数，再保留增长最快的数量级；并列步骤相加，嵌套次数按边界相乘。</li></ol></section><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 操作次数与时间复杂度</h2><p class="astro-e22r5uwj">先选择最能代表工作量的主要操作，再写出它随输入规模 n 的次数。</p><p class="astro-e22r5uwj">复杂度保留增长最快的项，并省略常数倍。例如 3n+5 记为 O(n)，2n²+n 记为 O(n²)。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">n=10 时，3n+5=35；n=100 时变成 305，增长主要由 n 决定</li><li class="astro-e22r5uwj">2n²+n 在 n=10 时是 210，在 n=100 时是 20100，平方项越来越占主导</li><li class="astro-e22r5uwj">因此分别记为 O(n) 与 O(n²)，不是在计算精确秒数</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-02"><h2 id="topic-02" class="astro-e22r5uwj">02 常数与单层循环</h2><p class="astro-e22r5uwj">固定次数的赋值、比较或计算通常是 O(1)。</p><p class="astro-e22r5uwj">从 1 到 n 的单层循环通常执行 n 次，记为 O(n)。两个先后执行的 O(n) 循环相加仍是 O(n)，不是 O(n²)。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">无论 n 是 10 还是 100000，只取 values[1] 都是固定一次访问，属于 O(1)</li><li class="astro-e22r5uwj">循环 i=1..n 执行 n 次，属于 O(n)</li><li class="astro-e22r5uwj">两个先后循环各 n 次，总数 2n，省略常数后仍是 O(n)</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-03"><h2 id="topic-03" class="astro-e22r5uwj">03 多层循环与增长数量级</h2><p class="astro-e22r5uwj">两层都各执行 n 次时，总次数为 n×n，属于 O(n²)；三层都为 n 次时是 n³。若内层边界是 j≤i，总次数则是 1+2+…+n，同样属于 O(n²)，必须看边界而非只看层数。</p><p class="astro-e22r5uwj">若每个阶段都产生两个继续展开的分支，分支数可从 1 变成 2、4、8，直到 2ⁿ，属于指数增长。这里学习识别增长形状，不提前展开递归算法。</p><pre class="astro-e22r5uwj"><code class="language-cpp">for (int i = 1; i &lt;= n; i++) { // 第一层执行 n 轮\n    for (int j = 1; j &lt;= n; j++) { // 每个 i 都让第二层执行 n 轮\n        for (int k = 1; k &lt;= n; k++) { // 每组 i、j 都让第三层执行 n 轮\n            operations++; // 主要操作总计执行 n 乘 n 乘 n 次\n        } // 结束当前 k 循环\n    } // 结束当前 j 循环\n} // 完成三层嵌套，时间复杂度为 O(n³)</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-04"><h2 id="topic-04" class="astro-e22r5uwj">04 空间复杂度</h2><p class="astro-e22r5uwj">空间复杂度关注算法额外使用的存储。固定几个变量是 O(1)，长度为 n 的辅助数组是 O(n)，n×n 辅助矩阵是 O(n²)。</p><p class="astro-e22r5uwj">输入本身是否计入要看题目口径；比较算法时通常重点看额外空间。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">只增加 sum、index 两个整数，n 增大时变量数不变，辅助空间 O(1)</li><li class="astro-e22r5uwj">新建 helper[1..n]，元素数量随 n 成正比，辅助空间 O(n)</li><li class="astro-e22r5uwj">新建 helper[1..n][1..n]，元素数量为 n²，辅助空间 O(n²)</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-05"><h2 id="topic-05" class="astro-e22r5uwj">05 补充识别：对数增长</h2><p class="astro-e22r5uwj">如果循环变量每次乘 2，达到 n 只需约 log₂n 次，常记为 O(log n)。它与“每层分成两个分支”的 O(2ⁿ) 方向相反：前者数翻倍多少次到 n，后者每增加一层让工作量翻倍。</p><p class="astro-e22r5uwj">这是识别性补充；本页核心仍是官方列出的常数、线性、平方、立方和指数增长。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">每轮把 value 扩大 2 倍，不是只增加 1</li><li class="astro-e22r5uwj">n=16 时 2⁴=16，所以循环 4 次</li><li class="astro-e22r5uwj">n 扩大一倍通常只多执行一次，增长慢于 O(n)</li></ul><pre class="astro-e22r5uwj"><code class="language-cpp">int value = 1; // 从 1 开始记录倍增值\nint count = 0; // 记录实际循环次数\nwhile (value &lt; n) { // 尚未达到 n 时继续倍增\n    value = value * 2; // 每轮把 value 扩大为原来的两倍\n    count++; // 记录完成一次倍增\n} // value 第一次达到或超过 n 时结束\n// n=16 时 value 依次变为 2、4、8、16，共执行 4 次</code></pre></section><section aria-labelledby="quick-table" class="astro-e22r5uwj"><h2 id="quick-table" class="astro-e22r5uwj">知识点速查表</h2><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">项目</th><th class="astro-e22r5uwj">含义</th><th class="astro-e22r5uwj">检查重点</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">O(1)</td><td class="astro-e22r5uwj">次数不随 n 增长</td><td class="astro-e22r5uwj">固定工作量</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">O(n)</td><td class="astro-e22r5uwj">与 n 成正比</td><td class="astro-e22r5uwj">常见单层遍历</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">O(n²) / O(n³)</td><td class="astro-e22r5uwj">平方或立方增长</td><td class="astro-e22r5uwj">核对嵌套边界</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">O(2ⁿ)</td><td class="astro-e22r5uwj">指数增长</td><td class="astro-e22r5uwj">每层产生多个继续展开的分支</td></tr></tbody></table></div></section><section aria-labelledby="algorithm-trace" class="astro-e22r5uwj"><h2 id="algorithm-trace" class="astro-e22r5uwj">算法实际怎样执行</h2><h3 class="astro-e22r5uwj">完成程序输入 n=3 时的逐轮计数</h3><div class="level4-dictionary-article__table-wrap"><table class="astro-e22r5uwj"><thead class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><th class="astro-e22r5uwj">阶段</th><th class="astro-e22r5uwj">当前 i</th><th class="astro-e22r5uwj">当前 j</th><th class="astro-e22r5uwj">计数变化</th><th class="astro-e22r5uwj">下一步或停止</th></tr></thead><tbody class="astro-e22r5uwj"><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">单层循环</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">singleCount: 0→1</td><td class="astro-e22r5uwj">i 变为 2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">单层循环</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">singleCount: 1→2</td><td class="astro-e22r5uwj">i 变为 3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">单层循环</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">singleCount: 2→3</td><td class="astro-e22r5uwj">i 变为 4</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">单层检查</td><td class="astro-e22r5uwj">4</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">4≤3 不成立，不再计数</td><td class="astro-e22r5uwj">单层结束，进入双层</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">doubleCount: 0→1</td><td class="astro-e22r5uwj">j 变为 2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">doubleCount: 1→2</td><td class="astro-e22r5uwj">j 变为 3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">doubleCount: 2→3</td><td class="astro-e22r5uwj">本轮内层结束，i 变为 2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">doubleCount: 3→4</td><td class="astro-e22r5uwj">j 变为 2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">doubleCount: 4→5</td><td class="astro-e22r5uwj">j 变为 3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">doubleCount: 5→6</td><td class="astro-e22r5uwj">本轮内层结束，i 变为 3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">1</td><td class="astro-e22r5uwj">doubleCount: 6→7</td><td class="astro-e22r5uwj">j 变为 2</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">2</td><td class="astro-e22r5uwj">doubleCount: 7→8</td><td class="astro-e22r5uwj">j 变为 3</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层循环</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">3</td><td class="astro-e22r5uwj">doubleCount: 8→9</td><td class="astro-e22r5uwj">本轮内层结束，i 变为 4</td></tr><tr class="astro-e22r5uwj"><td class="astro-e22r5uwj">双层检查</td><td class="astro-e22r5uwj">4</td><td class="astro-e22r5uwj">—</td><td class="astro-e22r5uwj">4≤3 不成立，不再计数</td><td class="astro-e22r5uwj">输出 3 9，程序结束</td></tr></tbody></table></div></section><section aria-labelledby="examples" class="astro-e22r5uwj"><h2 id="examples" class="astro-e22r5uwj">典型例题</h2><h3 class="astro-e22r5uwj">比较单层循环和双层循环</h3><p class="astro-e22r5uwj">教学程序约定 1≤n≤1000，统计一个单层循环和一个 n×n 双层循环各执行多少次。</p><h3 class="astro-e22r5uwj">完成程序</h3><pre class="astro-e22r5uwj"><code class="language-cpp">#include &lt;bits/stdc++.h&gt; // 引入本程序需要的标准库\nusing namespace std; // 允许直接使用 cin、cout、string 等标准库名称\n\nint main() { // 主函数是程序的入口\n    int n; // 定义输入规模\n    cin &gt;&gt; n; // 读入用于统计循环次数的 n\n\n    int singleCount = 0; // 记录单层循环体执行次数\n    for (int i = 1; i &lt;= n; i++) { // 单层循环一共执行 n 轮\n        singleCount = singleCount + 1; // 每轮把计数增加 1\n    } // 结束 O(n) 的单层循环\n\n    int doubleCount = 0; // 记录双层循环最内层执行次数\n    for (int i = 1; i &lt;= n; i++) { // 外层循环执行 n 轮\n        for (int j = 1; j &lt;= n; j++) { // 每轮外层都执行 n 轮内层循环\n            doubleCount = doubleCount + 1; // 统计一次最内层操作\n        } // 结束当前外层轮次对应的内层循环\n    } // 结束 O(n²) 的双层循环\n\n    cout &lt;&lt; singleCount &lt;&lt; &#39; &#39; &lt;&lt; doubleCount &lt;&lt; &#39;\\n&#39;; // 输出 n 与 n×n 两种实际次数\n    return 0; // 返回 0，表示程序正常结束\n} // 结束 main 函数</code></pre><div class="level4-dictionary-article__io"><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输入</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">3</code></pre><div class="astro-e22r5uwj"><strong class="astro-e22r5uwj">示例输出</strong><pre class="astro-e22r5uwj"><code class="astro-e22r5uwj">3 9</code></pre></div><h3 class="astro-e22r5uwj">解题与执行过程</h3><ol class="astro-e22r5uwj"><li class="astro-e22r5uwj">单层循环执行 n 次；n = 3 时 singleCount 从 0 变到 3，属于 O(n)。</li><li class="astro-e22r5uwj">双层循环的外层和内层都执行 n 次，总次数是 n×n；n = 3 时 doubleCount 为 9，属于 O(n²)。</li><li class="astro-e22r5uwj">如果 n 扩大 10 倍，线性次数约扩大 10 倍，平方次数约扩大 100 倍。</li><li class="astro-e22r5uwj">程序只使用固定数量的计数变量，没有随 n 增长的辅助数组，额外空间是 O(1)。</li></ol></section><section aria-labelledby="practice" class="astro-e22r5uwj"><h2 id="practice" class="astro-e22r5uwj">分层练习</h2><p class="astro-e22r5uwj">先独立完成，再展开提示核对思路。三题分别练基本识别、完整应用和换情境迁移。</p><div class="level4-dictionary-article__practice-grid"><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">基础</p><h3 class="astro-e22r5uwj">判断并列循环</h3><p class="astro-e22r5uwj">两个先后执行、各遍历 n 次的循环，写出总次数和复杂度。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">先后执行要相加：n+n。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">巩固</p><h3 class="astro-e22r5uwj">分析三角形循环</h3><p class="astro-e22r5uwj">外层 i=1..n，内层 j=1..i，估算总执行次数与数量级。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">次数是 1+2+...+n，增长最快的项是 n²。</p></details></article><article class="level4-dictionary-article__practice-card"><p class="level4-dictionary-article__practice-level">迁移</p><h3 class="astro-e22r5uwj">结合数据范围选程序</h3><p class="astro-e22r5uwj">当 n=100000 时，比较 O(n) 与 O(n²) 方案的主要操作数量级。</p><details class="astro-e22r5uwj"><summary class="astro-e22r5uwj">查看提示</summary><p class="astro-e22r5uwj">分别代入 n 和 n²，先看数量级差距，不猜具体秒数。</p></details></article></div></section><section aria-labelledby="mistakes" class="astro-e22r5uwj"><h2 id="mistakes" class="astro-e22r5uwj">常见错误</h2><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">只看代码长度</h3><p class="astro-e22r5uwj">错误写法：代码行数少就判断运行一定快。原因：短代码也可能执行大量循环。正确写法：选择主要操作并统计它随输入规模的次数。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">看到两个循环就判 O(n²)</h3><p class="astro-e22r5uwj">错误写法：两个先后执行的 n 次循环直接相乘。原因：先后执行应相加。正确写法：先判断循环是嵌套还是并列，再核对各自边界。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">忽略嵌套循环的乘法关系</h3><p class="astro-e22r5uwj">错误写法：两层都执行 n 次却记为 O(n)。原因：内层会被外层重复完整执行。正确写法：把相关层次数相乘得到 n×n。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">保留所有低阶项和常数</h3><p class="astro-e22r5uwj">错误写法：把 3n + 5 记成 O(3n + 5)。原因：复杂度描述增长数量级。正确写法：省略常数倍和低阶项，记为 O(n)。</p></div><div class="level4-dictionary-article__mistake"><h3 class="astro-e22r5uwj">把数组元素值当成空间规模</h3><p class="astro-e22r5uwj">错误写法：数组中数字很大就判断空间复杂度更高。原因：空间取决于元素数量和类型。正确写法：按额外变量、数组或矩阵的数量级估算。</p></div></section><section aria-labelledby="related-learning" class="astro-e22r5uwj"><h2 id="related-learning" class="astro-e22r5uwj">关联学习</h2><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj"><a href="/gesp/level4/sorting/" class="astro-e22r5uwj">排序算法</a>：基础排序是分析 O(n²) 的典型例子。</li></ul></section><section aria-labelledby="core-check" class="astro-e22r5uwj"><h2 id="core-check" class="astro-e22r5uwj">核心内容检查</h2><gesp-dictionary-review class="dictionary-review-check" data-level="4" data-lesson-id="complexity" data-progress-event="xdz:gesp-level4-progress"><section class="astro-l37rdmqc"></section></gesp-dictionary-review></section><section aria-labelledby="test" class="astro-e22r5uwj"><h2 id="test" class="astro-e22r5uwj">整页综合测试</h2><gesp-dictionary-test class="dictionary-test" data-level="4" data-lesson-id="complexity" data-progress-event="xdz:gesp-level4-progress"><p class="dictionary-test__intro">请完成全部 6 道单选题后统一提交。测试记录与“已复习”状态相互独立。</p><div class="dictionary-test__questions"></div><div class="dictionary-test__result" data-test-result hidden aria-live="polite"></div><div class="dictionary-test__actions"></div></gesp-dictionary-test></section><nav class="dictionary-lesson-pager"><a href="/gesp/level4/sorting/" rel="prev" class="astro-hl664apc"><span class="astro-hl664apc">上一知识点</span><strong class="astro-hl664apc">排序算法</strong></a><a href="/gesp/level4/file-operations/" rel="next" class="astro-hl664apc"><span class="astro-hl664apc">下一知识点</span><strong class="astro-hl664apc">文件操作</strong></a></nav></article></div><footer',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">文件让数据在程序结束后仍能保存。题目明确要求从指定文件读入、把结果长期保存或追加记录时，可以使用文件流；读写前要成功打开，操作后要检查状态并正常关闭。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">文件操作不能只写读写语句：打开是否成功、每次读取是否成功、写入后的流状态都要检查。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">ifstream input(&quot;data.txt&quot;); // 尝试建立连接 data.txt 的输入流\nif (!input.is_open()) { // 立即检查文件是否成功打开\n    return 0; // 打开失败就停止，不能继续读取\n} // 结束打开状态检查\n// 只有输入打开且内容读取成功后，才考虑打开输出文件</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">int value; // 保存本次成功读取的整数\nwhile (input &gt;&gt; value) { // 先尝试读取，成功后才进入循环体\n    cout &lt;&lt; value; // 只处理这一次新读到的有效数据\n} // 读取失败或到达文件末尾时结束</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">打开失败后仍继续读写</li><li class="astro-e22r5uwj">使用 while (!input.eof()) 控制读取</li><li class="astro-e22r5uwj">需要追加旧内容却使用覆盖写模式</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">学完后，你能按“打开—检查—读写—再检查—关闭”完成文本文件操作，并区分重定向、文件流、追加和二进制模式。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">控制台里的数据像写在临时草稿纸上，程序结束后就不再保留。文件更像一本账本：程序先找到并打开它，再读入旧记录或写入新记录，最后确认操作成功。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要理解标准输入输出重定向、文本与二进制文件、文件流及常见打开模式，能够完成基本读写并处理失败状态。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 freopen 重定向</h2><p class="astro-e22r5uwj">`freopen` 可以把标准输入或输出重新连接到文件，成功后仍使用 cin、cout；&quot;r&quot; 表示读，&quot;w&quot; 表示从头写。</p><p class="astro-e22r5uwj">重定向调用也可能失败，正式使用时要检查返回结果。竞赛程序只在题目明确要求文件输入输出时使用；普通在线评测通常直接使用标准输入输出。</p><pre class="astro-e22r5uwj"><code class="language-cpp">FILE* inputResult = freopen(&quot;input.txt&quot;, &quot;r&quot;, stdin); // 尝试把标准输入重定向到文件\nFILE* outputResult = freopen(&quot;output.txt&quot;, &quot;w&quot;, stdout); // 尝试把标准输出重定向到文件\nif (inputResult == nullptr || outputResult == nullptr) { // 检查任一重定向是否失败\n    return 0; // 失败时停止后续标准输入输出操作\n} // 结束重定向结果检查</code></pre><aside',
        '<article class="level4-dictionary-article"><div id="page-top" class="astro-e22r5uwj"></div><p class="level4-dictionary-article__intro">异常处理用于把“无法在当前步骤正常完成”的情况交给合适的位置处理。阅读错误时先区分编译错误、运行错误和逻辑错误；能在操作前检查的问题，仍应优先直接校验。</p><section class="level4-quick-review" aria-labelledby="quick-review-title"><h2 id="quick-review-title" class="astro-e22r5uwj">本课先记住</h2><div class="level4-quick-review__grid"><article class="level4-quick-review__card level4-quick-review__card--conclusion"><h3 class="astro-e22r5uwj">核心结论</h3><p class="astro-e22r5uwj">throw 会立刻结束当前执行路径并寻找类型匹配的 catch；能提前判断的边界和输入条件仍优先使用 if。</p></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">常用语法</h3><pre class="astro-e22r5uwj"><code class="language-cpp">try { // 监控可能无法正常完成的操作\n    throw errorValue; // 抛出 int 异常并立即离开当前路径\n} catch (int value) { // 由类型匹配的处理器接收异常值\n    cout &lt;&lt; value; // 在处理器中报告或处理真实失败\n} // 处理完成后从整个 try-catch 之后继续</code></pre></article><article class="level4-quick-review__card"><h3 class="astro-e22r5uwj">最小示例</h3><pre class="astro-e22r5uwj"><code class="language-cpp">if (denominator == 0) { // 在真正执行除法前检查危险条件\n    throw denominator; // 无法完成任务时向调用者报告 int 异常\n} // 分母非零时才会继续到除法语句</code></pre></article><article class="level4-quick-review__card level4-quick-review__card--mistakes"><h3 class="astro-e22r5uwj">关键易错点</h3><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">认为普通数组越界或整数除零一定能被 catch</li><li class="astro-e22r5uwj">throw 的类型与 catch 参数类型不匹配</li><li class="astro-e22r5uwj">空 catch 吞掉失败后仍报告成功</li></ul></article></div></section><section class="level4-lesson-opening" aria-labelledby="learning-goal"><h2 id="learning-goal" class="astro-e22r5uwj">这节课要会什么</h2><p class="astro-e22r5uwj">学完后，你能判断何时先用 if 校验、何时用 throw 报告失败，并完整追踪 try 到匹配 catch 的跳转路径。</p><h3 class="astro-e22r5uwj">从这个场景开始</h3><p class="astro-e22r5uwj">普通条件像进门前检查门票；异常则像工作进行到一半发现无法继续，立即把问题交给能处理它的人。两者职责不同，不能把所有错误都塞进 try-catch。</p></section><h2 id="knowledge-range" class="astro-e22r5uwj">知识范围与官方定位</h2><p class="astro-e22r5uwj">四级需要理解异常的基本概念和 try、throw、catch 的执行路径，能够按类型捕获并避免掩盖错误。异常不是替代普通条件判断的工具。</p><section class="level4-dictionary-topic" aria-labelledby="topic-01"><h2 id="topic-01" class="astro-e22r5uwj">01 先校验可预防的问题</h2><p class="astro-e22r5uwj">编译错误是代码不符合语法或类型规则，程序无法生成；运行错误发生在程序执行期间；逻辑错误能运行却得到错误结果。三类错误不能全部依靠 try-catch 解决。</p><p class="astro-e22r5uwj">数组下标、除数是否为 0、输入范围等条件在操作前就能判断，应先做输入校验。若当前函数自己就能给出正常处理，直接用 if 最清楚；若函数无法完成约定任务，需要交给调用者决定，才可在检查后 throw。</p><p class="astro-e22r5uwj">普通原生数组越界、整数除零等未定义或错误行为不保证产生 C++ 异常，不能先做危险操作再期待 catch 补救。</p><pre class="astro-e22r5uwj"><code class="language-cpp">if (divisor == 0) { // 在执行除法前检查分母\n    cout &lt;&lt; &quot;divisor cannot be zero\\n&quot;; // 当前层直接说明可预见错误\n    return 0; // 停止本次任务，绝不执行整数除零\n} // 分母合法时才继续计算</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-02"><h2 id="topic-02" class="astro-e22r5uwj">02 try、throw 与 catch</h2><p class="astro-e22r5uwj">try 包围调用者准备监控的操作。执行 throw 时先产生异常对象，随后当前语句之后的正常路径被放弃，程序沿调用关系寻找类型匹配的 catch。</p><p class="astro-e22r5uwj">catch 负责处理失败；处理完成后，程序从整个 try-catch 结构后继续，而不是回到 throw 下一行。若根本没有 throw，所有 catch 都会被跳过。</p><pre class="astro-e22r5uwj"><code class="language-cpp">try { // 开始监控负数检查与正常输出\n    if (value &lt; 0) { // 判断当前值是否违反要求\n        throw value; // 抛出 int 后立即跳离 try 的剩余语句\n    } // 结束负数检查\n    cout &lt;&lt; value &lt;&lt; &#39;\\n&#39;; // 只有非负路径才会执行正常输出\n} catch (int errorValue) { // 捕获并接收 int 类型的负数值\n    cout &lt;&lt; &quot;negative: &quot; &lt;&lt; errorValue &lt;&lt; &#39;\\n&#39;; // 明确报告异常数据\n} // 两条路径最终都从这里之后继续</code></pre></section><section class="level4-dictionary-topic" aria-labelledby="topic-03"><h2 id="topic-03" class="astro-e22r5uwj">03 异常类型匹配</h2><p class="astro-e22r5uwj">catch 的参数类型决定它能捕获哪类异常。抛出 int 时应有匹配的 int 处理器，不能把普通数值隐式转换规则当作异常匹配规则。</p><p class="astro-e22r5uwj">若同时写具体类型处理器和 `catch (...)`，具体类型必须放在前面，`catch (...)` 只能作为最后防线，并且不能悄悄报告成功。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">执行 throw 7 时，抛出对象类型是 int</li><li class="astro-e22r5uwj">catch (string) 类型不匹配，会被跳过</li><li class="astro-e22r5uwj">后面的 catch (int value) 匹配，value 得到 7，并且只执行这一条处理器</li></ul></section><section class="level4-dictionary-topic" aria-labelledby="topic-04"><h2 id="topic-04" class="astro-e22r5uwj">04 处理边界与清理</h2><p class="astro-e22r5uwj">当前层只处理自己真正能解释或修复的失败。不能处理时，应让上层知道，而不是用空 catch 吞掉。</p><p class="astro-e22r5uwj">文件等资源应保证在正常和失败路径上都能安全结束。使用局部文件流对象可以借助离开作用域自动关闭。</p><ul class="astro-e22r5uwj"><li class="astro-e22r5uwj">局部 ifstream 在进入函数后成功创建</li><li class="astro-e22r5uwj">后续 throw 让程序离开该函数作用域时，局部文件流对象会被销毁并关闭文件</li><li class="astro-e22r5uwj">catch 应报告真实失败；空 catch 后继续输出“成功”会掩盖无效数据</li></ul><aside',
    ],
    5: [
        '<article id="top"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>判断整数关系时，先分清题目是在问整除与约数、单个数的素合属性与两个数的互质关系，还是模意义下的位置；每一类都要先检查自己的前提。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>b 不为 0 时，用 a % b == 0 判断 b 是否整除 a；正模数 m 下，负余数可通过加一次 m 化为规范余数。</p><pre tabindex="0"><code>int r = x % m;\nif (r &lt; 0) {\n    r = r + m;\n}</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>36 % 4 == 0，所以 4 整除 36；-1 % 5 在 C++ 中是 -1，加 5 后得到规范余数 4。</p></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>整除方向不能颠倒</li><li>0 不能作除数</li><li>1 既不是素数也不是合数</li><li>平方数中间约数只记一次</li><li>负余数不能直接当规范余数</li></ul></dd></div></dl></section><h2 id="问题引入三类整数关系">问题引入：三类整数关系</h2><p>同样是面对整数，题目真正问的事情可能完全不同：</p><ul>\n<li>36 个零件能不能每 4 个装一盒？如果能，4 和 36 是什么关系？</li>\n<li>13、15 各属于哪一类数？8 和 15 都不是素数，它们之间又有什么关系？</li>\n<li>钟面走过 12 小时会回到原位置，那么 23 点和 11 点能不能看作同一个钟面位置？负方向走一步又该怎样记录？</li>\n</ul><p>这三类问题分别需要三种工具。本课只学习概念和可复演的判断过程，不学习求最大公约数、判素与筛素数、质因数分解等算法。</p><p>学完后，你应当能独立完成三件事：</p><ol>\n<li>方向正确地说明整除、约数和倍数，并用乘积配对整理正约数。</li>\n<li>按定义判断素数、合数和互质，正确处理整数 1。</li>\n<li>在正模数下求规范余数，并用它判断同余。</li>\n</ol><h2 id="k1-整除约数倍数与约数配对">K1 整除、约数、倍数与约数配对</h2><h3 id="先锁定整除的方向">先锁定整除的方向</h3><p>对整数 <code>a</code>、<code>b</code>，必须先确认 <code>b != 0</code>。如果 <code>a % b == 0</code>，就说：</p><ul>\n<li><code>b</code> 整除 <code>a</code>；</li>\n<li><code>b</code> 是 <code>a</code> 的约数；</li>\n<li><code>a</code> 是 <code>b</code> 的倍数。</li>\n</ul><p>三句话说的是同一个事实，方向不能交换。判断“4 是否整除 36”时，要算的是：</p><pre><code>36 % 4 == 0</code></pre><p>余数是 0，所以 4 整除 36，4 是 36 的约数，36 是 4 的倍数。相反，<code>4 % 36</code> 的余数是 4，它回答的是“36 是否整除 4”，不是原来的问题。</p><h3 id="正约数为什么成对出现">正约数为什么成对出现</h3><p>本页讨论正约数时，先规定 <code>n &gt; 0</code>。如果正整数 <code>d</code> 能整除 <code>n</code>，那么 <code>n / d</code> 也是 <code>n</code> 的正约数，因为二者相乘正好得到 <code>n</code>。</p><p>以 <code>n=36</code> 为例，按较小的一项从 1 开始检查。每次先保证 <code>d</code> 不为 0，再计算 <code>36 % d</code>；只有余数为 0 时，才写出一对约数。</p><table>\n<thead>\n<tr>\n<th style="text-align: right"><code>d</code></th>\n<th style="text-align: right"><code>36 % d</code></th>\n<th>是否产生约数对</th>\n<th>记录的乘积</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td style="text-align: right">0</td>\n<td>是</td>\n<td><code>1 * 36</code></td>\n</tr>\n<tr>\n<td style="text-align: right">2</td>\n<td style="text-align: right">0</td>\n<td>是</td>\n<td><code>2 * 18</code></td>\n</tr>\n<tr>\n<td style="text-align: right">3</td>\n<td style="text-align: right">0</td>\n<td>是</td>\n<td><code>3 * 12</code></td>\n</tr>\n<tr>\n<td style="text-align: right">4</td>\n<td style="text-align: right">0</td>\n<td>是</td>\n<td><code>4 * 9</code></td>\n</tr>\n<tr>\n<td style="text-align: right">5</td>\n<td style="text-align: right">1</td>\n<td>否</td>\n<td>没有约数对</td>\n</tr>\n<tr>\n<td style="text-align: right">6</td>\n<td style="text-align: right">0</td>\n<td>是</td>\n<td><code>6 * 6</code></td>\n</tr>\n</tbody>\n</table><p>到 <code>6 * 6</code> 后就可以停止：如果一对因数的两项都大于 6，乘积一定大于 36；如果只有一项大于 6，另一项就小于 6，前面已经检查过。这样不会漏掉新约数。</p><p>前四对各贡献两个不同约数，最后一对的两个 6 是同一个数，只能记一次。因此 36 的不同正约数有：</p><pre><code>1, 2, 3, 4, 6, 9, 12, 18, 36</code></pre><p>约数个数是 <code>2+2+2+2+1=9</code>，约数和是 <code>(1+36)+(2+18)+(3+12)+(4+9)+6=91</code>。这里直接根据配对计数、求和，不需要其他公式。</p><h3 id="k1-当场检查与纠错">K1 当场检查与纠错</h3><p>错误做法一：算 <code>4 % 36</code> 后说“4 不整除 36”。原因是把被除数和除数放反了。修正时先把问题翻译成“用 36 除以 4”，再算 <code>36 % 4</code>。重新检查的结果必须同时支持“4 整除 36、4 是 36 的约数、36 是 4 的倍数”。</p><p>错误做法二：尝试计算 <code>36 % 0</code>。0 不能作除数，前提检查已经失败，不能继续执行 <code>%</code>。</p><p>错误做法三：把 <code>6 * 6</code> 记成两个不同的 6。修正后只记录一个 6，重新得到 9 个不同正约数，约数和为 91。</p><h2 id="k2-素数合数互质与-1-的边界">K2 素数、合数、互质与 1 的边界</h2><h3 id="单个数的分类先看两个条件">单个数的分类先看两个条件</h3><p>讨论素数和合数时，先看这个整数是否大于 1：</p><ul>\n<li><strong>素数</strong>：大于 1，并且恰好有两个正约数，即 1 和它本身。</li>\n<li><strong>合数</strong>：大于 1，并且除了 1 和它本身，还有其他正约数。</li>\n<li><strong>整数 1</strong>：只有一个正约数 1，不满足上面任何一个定义，所以既不是素数，也不是合数。</li>\n</ul><p>按“先检查是否大于 1，再列出正约数”的顺序判断：</p><table>\n<thead>\n<tr>\n<th style="text-align: right">数</th>\n<th>是否大于 1</th>\n<th>正约数</th>\n<th>结论</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td style="text-align: right">1</td>\n<td>否</td>\n<td>1</td>\n<td>既不是素数，也不是合数</td>\n</tr>\n<tr>\n<td style="text-align: right">13</td>\n<td>是</td>\n<td>1、13</td>\n<td>恰有两个正约数，是素数</td>\n</tr>\n<tr>\n<td style="text-align: right">15</td>\n<td>是</td>\n<td>1、3、5、15</td>\n<td>有其他正约数，是合数</td>\n</tr>\n</tbody>\n</table><p>这里不能因为“1 的约数很少”就把 1 归入素数。定义要求的是“<strong>大于 1</strong>且恰有两个正约数”，两个条件缺一不可。</p><h3 id="互质判断的是两个数之间的关系">互质判断的是两个数之间的关系</h3><p>两个正整数的公共正约数只有 1，就说它们<strong>互质</strong>。互质不要求两个数本身是素数。</p><p>例如，8 的正约数是 1、2、4、8；15 的正约数是 1、3、5、15。把两列共同出现的数挑出来，只剩 1，所以 8 和 15 互质。8 与 15 都是合数，这正好说明“合数之间也可能互质”。</p><p>再看反例：8 与 14 都有公共正约数 2，所以它们不互质。判断链必须完整：</p><ol>\n<li>确认讨论的是两个正整数。</li>\n<li>分别列出两数的正约数。</li>\n<li>取公共部分。</li>\n<li>公共部分只有 1 才能得出互质结论。</li>\n</ol><p>以后看到“两数的最大公约数为 1”，可以把它识读为“两数互质”；怎样快速求最大公约数留到下一页学习，本页只按定义判断。</p><h3 id="k2-当场检查与纠错">K2 当场检查与纠错</h3><p>错误做法一：“1 只有一个正约数，所以它是素数。”原因是漏掉“恰有两个正约数”，也漏掉“大于 1”。修正后重新数：1 的正约数只有 1，因此它既不是素数，也不是合数。</p><p>错误做法二：“8 和 15 都是合数，所以不互质。”原因是把单个数的分类和两个数的关系混在一起。修正后重新比较约数清单，公共部分只有 1，因此 8 与 15 互质。</p><p>自检时要能说清：素数、合数在判断一个数；互质在比较两个数。这两种问题的判断对象不同。</p><h2 id="k3-同余模运算与规范余数">K3 同余、模运算与规范余数</h2><h3 id="同余表示落在同一个周期位置">同余表示落在同一个周期位置</h3><p>先规定模数 <code>m &gt; 0</code>。整数 <code>a</code>、<code>b</code> 除以 <code>m</code> 后得到相同的<strong>规范余数</strong>，就说 <code>a</code> 与 <code>b</code> 模 <code>m</code> 同余。规范余数必须落在 <code>0</code> 到 <code>m-1</code> 之间，也就是满足 <code>0 &lt;= r &lt; m</code>。</p><p>例如模 12 时，23 的规范余数是 11，11 的规范余数也是 11，所以 23 与 11 模 12 同余。还可以检查差：<code>23-11=12</code>，12 能被模数 12 整除，得到同样的结论。</p><h3 id="c-负余数要再规范化">C++ 负余数要再规范化</h3><p>C++14 使用正模数时，负整数的 <code>%</code> 结果可能是负数。例如：</p><pre><code>-1 % 5 == -1</code></pre><p><code>-1</code> 不是模 5 的规范余数，因为它不在 0 到 4 之间。先算语言实际余数，再在余数为负时加一次模数：</p><pre><code>int x = -1;int m = 5;int r = x % m;if (r &lt; 0) {    r = r + m;}</code></pre><p>这段局部计算结束后，<code>r=4</code>。在 <code>m &gt; 0</code> 时，C++ 的余数绝对值小于 <code>m</code>，所以负余数加一次 <code>m</code> 就会进入 <code>0..m-1</code>。规范化后的 4 才能用来表示环形位置；不能把原来的 <code>-1</code> 直接当数组下标。</p><p>用同一套五步流程比较 <code>-1</code> 与 4：</p><table>\n<thead>\n<tr>\n<th>步骤</th>\n<th><code>-1</code> 一侧</th>\n<th><code>4</code> 一侧</th>\n<th>检查结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1. 检查模数</td>\n<td><code>m=5</code></td>\n<td><code>m=5</code></td>\n<td>5 大于 0，可以继续</td>\n</tr>\n<tr>\n<td>2. 算 C++ 实际余数</td>\n<td><code>-1 % 5 = -1</code></td>\n<td><code>4 % 5 = 4</code></td>\n<td>左侧还不是规范余数</td>\n</tr>\n<tr>\n<td>3. 规范化</td>\n<td><code>-1+5=4</code></td>\n<td>已在 0 到 4 之间</td>\n<td>两边都是 4</td>\n</tr>\n<tr>\n<td>4. 比较或检查差</td>\n<td>比较 4 与 4</td>\n<td><code>-1-4=-5</code> 可被 5 整除</td>\n<td>两种复核一致</td>\n</tr>\n<tr>\n<td>5. 写结论</td>\n<td><code>-1</code> 与 4 模 5 同余</td>\n<td></td>\n<td>规范余数都为 4</td>\n</tr>\n</tbody>\n</table><p>如果 <code>m=0</code> 或 <code>m&lt;0</code>，本页采用的前提不成立，应停止这套计算并改用正模数。</p><h3 id="加减乘可以先取模普通除法不能照搬">加、减、乘可以先取模，普通除法不能照搬</h3><p>同余的加法、减法、乘法可以先把参与运算的数换成它们的规范余数，再进行组合，最后仍要把结果规范到 <code>0..m-1</code>。</p><p>以模 12 的加法为例：23 的规范余数是 11，14 的规范余数是 2；<code>11+2=13</code>，再取模得到 1。直接计算 <code>23+14=37</code>，37 的规范余数也是 1，两条路结果相同。</p><p>普通整数除法不能无条件照搬。虽然 <code>2</code> 与 <code>8</code> 模 6 同余，但两边同时除以 2 后得到 1 和 4；1 与 4 模 6 的规范余数不同，所以不能直接约分。本页只用这个反例记住边界，不展开更高阶的除法条件。</p><h3 id="k3-当场检查与纠错">K3 当场检查与纠错</h3><p>错误做法一：模数取 0 或负数后继续计算。原因是本页约定的 <code>m &gt; 0</code> 已经不成立；修正方法是停止并换成正模数，重新从第 1 步开始。</p><p>错误做法二：把 <code>-1 % 5</code> 得到的 <code>-1</code> 直接当成规范余数或数组下标。修正后执行“实际余数 -1 → 加 5 → 规范余数 4”，再检查 <code>0 &lt;= 4 &lt; 5</code>。越界写法只做纸面纠错，不要运行。</p><p>错误做法三：认为同余式两边可以随意同时除以同一个整数。修正后重算上面的反例：<code>2</code> 与 <code>8</code> 模 6 同余，但 1 与 4 模 6 不同余，说明普通整数除法不能直接照搬加、减、乘的规则。</p><h2 id="把三种工具合成判断账本">把三种工具合成判断账本</h2><p>遇到整数关系题时，先问“题目真正要判断什么”，再选择对应流程：</p><table>\n<thead>\n<tr>\n<th>题目真正问什么</th>\n<th>先检查</th>\n<th>核心操作</th>\n<th>结果怎样复核</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>b</code> 能否整除 <code>a</code></td>\n<td><code>b != 0</code></td>\n<td>计算 <code>a % b</code></td>\n<td>余数是否为 0，三种说法的方向是否一致</td>\n</tr>\n<tr>\n<td>正整数 <code>n</code> 有哪些正约数</td>\n<td><code>n &gt; 0</code></td>\n<td>观察乘积配对</td>\n<td>配对乘积是否为 <code>n</code>，平方处是否重复</td>\n</tr>\n<tr>\n<td>单个数是素数还是合数</td>\n<td>是否大于 1</td>\n<td>列正约数并数清数量</td>\n<td>是否正确排除整数 1</td>\n</tr>\n<tr>\n<td>两个数是否互质</td>\n<td>两个数都是正整数</td>\n<td>比较公共正约数</td>\n<td>公共部分是否只有 1</td>\n</tr>\n<tr>\n<td>两数是否模 <code>m</code> 同余</td>\n<td><code>m &gt; 0</code></td>\n<td>求规范余数或检查两数之差</td>\n<td>余数是否相同且都在 <code>0..m-1</code></td>\n</tr>\n</tbody>\n</table><p>这张账本就是本课的完整产物。它把前提、操作和复核放在一起，可以防止只记术语却不会判断。</p><h2 id="常见错误怎样改正">常见错误怎样改正</h2><table>\n<thead>\n<tr>\n<th>错误现象或认识</th>\n<th>原因</th>\n<th>修正动作</th>\n<th>重新推演的通过标准</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>用 <code>4 % 36</code> 判断 4 是否整除 36</td>\n<td>被除数与除数方向颠倒</td>\n<td>改算 <code>36 % 4</code>，并先确认 4 不为 0</td>\n<td>余数为 0，三种关系说法方向一致</td>\n</tr>\n<tr>\n<td>把平方数中间约数记两次</td>\n<td>没发现配对的两项相同</td>\n<td><code>d * d == n</code> 时只记一个 <code>d</code></td>\n<td>36 有 9 个不同正约数，和为 91</td>\n</tr>\n<tr>\n<td>把 1 判断为素数</td>\n<td>漏掉“大于 1”和“恰有两个正约数”</td>\n<td>回到定义逐项检查</td>\n<td>1 被判断为既非素也非合</td>\n</tr>\n<tr>\n<td>认为两个合数一定不互质</td>\n<td>混淆单个数分类和两个数关系</td>\n<td>列两数的正约数并取公共部分</td>\n<td>8 与 15 的公共正约数只有 1</td>\n</tr>\n<tr>\n<td>把 C++ 负余数直接当规范余数</td>\n<td>漏掉规范余数区间</td>\n<td>负余数加一次正模数</td>\n<td><code>-1</code> 模 5 的规范余数是 4</td>\n</tr>\n<tr>\n<td>直接约分同余式</td>\n<td>把普通除法错当成无条件保持同余</td>\n<td>用具体反例重新计算</td>\n<td>1 与 4 模 6 不同余</td>\n</tr>\n</tbody>\n</table><h2 id="练习与自检">练习与自检</h2><h3 id="引导练习按步骤补全">引导练习：按步骤补全</h3><ol>\n<li>对 24 与 6，先补全 <code>24 % 6 = __</code>，再写出整除、约数、倍数三种说法。接着补全约数对 <code>1 * 24、2 * 12、3 * __、4 * __</code>，求不同正约数的个数与和。</li>\n<li>对 1、2、21，依次填写“是否大于 1 → 正约数 → 素合属性”；再列出 9 和 20 的公共正约数，判断是否互质。</li>\n<li>对 <code>x=-8、m=5</code>，填写“C++ 实际余数 → 是否需要加 5 → 规范余数”，再与 17 的规范余数比较。</li>\n</ol><p><strong>引导练习答案：</strong></p><ol>\n<li><code>24 % 6=0</code>；6 整除 24，6 是 24 的约数，24 是 6 的倍数。约数对缺项是 <code>3 * 8、4 * 6</code>，共有 8 个不同正约数，和为 60。</li>\n<li>1 既不是素数也不是合数；2 是素数；21 的正约数有 1、3、7、21，是合数。9 与 20 的公共正约数只有 1，所以互质。</li>\n<li><code>-8 % 5=-3</code>，需要加 5，规范余数为 2；<code>17 % 5=2</code>，所以 -8 与 17 模 5 同余。</li>\n</ol><h3 id="独立练习不看步骤完成判断">独立练习：不看步骤完成判断</h3><ol>\n<li>列出 49 的全部正约数，求个数与和，并说明哪个约数不能重复记录。</li>\n<li>判断 29、21 的素合属性，再判断 14 与 25 是否互质；只用定义和小规模约数清单说明。</li>\n<li>求 -14 模 6 的规范余数，并判断它与 10 是否模 6 同余。</li>\n</ol><p><strong>独立练习答案：</strong></p><ol>\n<li>49 的正约数是 1、7、49，共 3 个，和为 57；<code>7 * 7</code> 中的 7 只能记录一次。</li>\n<li>29 的正约数只有 1、29，所以是素数；21 还有正约数 3、7，所以是合数。14 与 25 的公共正约数只有 1，所以互质。</li>\n<li>C++ 中 <code>-14 % 6=-2</code>，加 6 得规范余数 4；<code>10 % 6=4</code>，所以 -14 与 10 模 6 同余。</li>\n</ol><p>独立通过标准：不看示范也能写出每类问题的必要前提和关键中间结果；整除方向正确，整数 1 与平方数边界正确，规范余数落在 <code>0..m-1</code>，并能指出下一步算法应去哪个现有页面学习。</p>',
        '<article id="top"><p>两个数很小时，我们还能从较小的数开始逐个试除；可输入接近 10^9 时，这样检查会很慢。欧几里得算法抓住“用余数替换后，公共约数不会改变”这一点，让第二个数不断变小，最后直接得到最大公约数。本课会把这个理由、循环过程和三个常见应用连成一条完整路线。</p><h2 id="知识范围与官方定位" class="astro-2i3ys3wc">知识范围与官方定位</h2><p class="astro-2i3ys3wc">对应 GESP 五级“欧几里得算法”：会用辗转相除法求最大公约数，并能在明确的整数范围与零边界下计算最小公倍数、完成分数约分和多个数的最大公约数。</p><p>本页只使用一级至五级已经学习的语法和能力；小知识点按页内编号连续查询，不设置前置完成门槛。</p><dl><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">是什么</dt><dd class="astro-2i3ys3wc">这是一节独立算法课：先证明余数替换为什么不改变答案，再把替换写成能处理负数和 0 的循环。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">解决什么问题</dt><dd class="astro-2i3ys3wc">学完后，你能求两个整数的非负 gcd 与 lcm，并把同一个 gcd 用到规范约分和多个数的公共因数计算。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">什么时候使用</dt><dd class="astro-2i3ys3wc">看到最大等长切分、分数约分、同时分组或多个整数的共同尺度时，可以先判断问题是否需要 gcd；遇到周期同时发生时还常会继续用 lcm。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">核心思想</dt><dd class="astro-2i3ys3wc">若 a=q×b+r，那么 (a,b) 与 (b,r) 的公共正约数集合双向相同；余数又严格小于正的 b，所以可以反复缩小直到第二个数为 0。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">C++ 实现方法</dt><dd class="astro-2i3ys3wc">先把输入归一为非负数；循环中先保存 remainder=a%b，再更新 a、b；求 lcm 时先保护零，再按 a/greatest*b 先除后乘。本课数据保证结果可放入 long long。</dd></div></dl><section aria-labelledby="01-最大公约数与欧几里得算法"><h2 id="01-最大公约数与欧几里得算法" class="astro-2i3ys3wc">01 最大公约数与欧几里得算法</h2><p class="astro-2i3ys3wc">先看固定例子 48 和 18。除法关系是 48=18×2+12，也就是余数 12 来自“48 减去 18 的 2 倍”。</p><p class="astro-2i3ys3wc">若正整数 d 同时整除 48 和 18，那么 d 也整除 48-18×2=12；所以原来每一个公共约数，也都是 18 和 12 的公共约数。反过来，若 d 同时整除 18 和 12，那么 d 也整除 18×2+12=48；所以 18 和 12 的每一个公共约数，也都是 48 和 18 的公共约数。两个方向都成立，公共正约数集合完全相同，最大者当然也相同。一般地，当 a=q×b+r 时，gcd(a,b)=gcd(b,r)，其中 r=a%b。</p><p class="astro-2i3ys3wc">这条关系可以重复使用：(48,18) 变成 (18,12)，再变成 (12,6)，最后变成 (6,0)。当 b&gt;0 时，余数满足 0&lt;=r&lt;b，因此新的第二个数严格变小，不会无限循环；当 b==0 时停止，当前 a 就是最大公约数。</p><p class="astro-2i3ys3wc">循环每轮必须按固定顺序做三件事：先计算并保存 remainder=a%b，再令 a=b，最后令 b=remainder。临时量 remainder 保住了旧 a 和旧 b 算出的余数，避免前一次赋值把旧值覆盖。while (b != 0) 还保证只有除数非零时才执行 %。</p><p class="astro-2i3ys3wc">本课输入允许负数和 0，所以 main 先把 a、b 归一为非负数。约定 gcd(0,b)=|b|、gcd(a,0)=|a|，教学程序把 gcd(0,0) 定义为 0；题目若另有定义，应以题面为准。数据范围排除了 LLONG_MIN，因此取相反数安全。</p><p class="astro-2i3ys3wc">当两数都为正时，欧几里得算法的时间复杂度是 O(log(min(|a|,|b|)))；含零边界直接结束，是 O(1)。若要统一写法，可记为 O(log(min(|a|,|b|)+1))。程序只使用几个 long long 变量，额外空间复杂度是 O(1)。</p><div><h3 class="astro-2i3ys3wc">具体样例与推演</h3><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">场景：</strong>从“枚举公共约数”出发，用 48 和 18 亲手形成余数替换规则，再按程序顺序走到停止。</p><ol class="astro-2i3ys3wc"><li class="astro-2i3ys3wc">自然想法是从 18 开始向下试除 48 和 18；数字接近 10^9 时，逐个尝试可能很慢，我们需要一次可靠的缩小。</li><li class="astro-2i3ys3wc">写出 48=18×2+12。若 d 同时整除 48 和 18，则 d 也整除 48-18×2=12，所以原来的公共约数不会丢。</li><li class="astro-2i3ys3wc">反过来，若 d 同时整除 18 和 12，则 d 也整除 18×2+12=48，所以替换后没有增加假的公共约数。</li><li class="astro-2i3ys3wc">两个方向合起来，gcd(48,18)=gcd(18,12)。一般地，a=q×b+r 时都有 gcd(a,b)=gcd(b,r)。</li><li class="astro-2i3ys3wc">程序从 (48,18) 开始：b!=0，先保存 remainder=48%18=12，再更新为 (18,12)。</li><li class="astro-2i3ys3wc">第二轮保存 18%12=6，更新为 (12,6)；第三轮保存 12%6=0，更新为 (6,0)。</li><li class="astro-2i3ys3wc">此时 b==0，循环停止，返回 a=6。只要 b&gt;0，新余数就在 0 到 b-1 之间，第二个数严格变小，因此过程一定结束。</li><li class="astro-2i3ys3wc">输入先取绝对值，零边界按 gcd(0,b)=|b|、gcd(a,0)=|a|、教学约定 gcd(0,0)=0 处理。两数为正时是 O(log(min(|a|,|b|)))，含零直接 O(1)，额外空间 O(1)。</li></ol><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">适用结论：</strong>先证明替换前后公共正约数双向相同，再使用“判断 b、保存余数、更新 a、更新 b”的循环。固定样例必须走出 12、6、0 三个真实余数，最终 gcd=6。</p><aside',
        '<article id="top" data-level5-topic-hub="prime-sieves"><p>当题目要反复判断一个范围内的数是否为素数时，筛法会把逐个试除改成一次预处理、随后快速查询。筛表时先证明哪些数是合数，再把结果保存在标记数组中。以后每次查询只读一个数组位置，不必重新寻找因数。</p><div id="本课先记住"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>查询很多次时，先用筛法建立 0 到 n 的素数表，再用 isPrime[x] 直接回答；埃氏筛和线性筛得到同一类结果，只是证明合数的方式不同。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>预处理函数只调用一次，后面的每次查询只读取对应下标。</p><pre tabindex="0"><code>buildPrimeTable(n);\nif (isPrime[x]) {\n    cout &lt;&lt; &quot;YES&quot;;\n}</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>筛到 16 后，2 和 7 的标记为真，0、1、9、16 的标记为假。</p><pre tabindex="0"><code>isPrime[2] = true\nisPrime[9] = false</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>0 和 1 必须设为非素数</li><li>数组要覆盖下标 n</li><li>先判断 p &lt;= n / p 再计算 p * p</li><li>不要混淆 isPrime 与 isComposite 的真假含义。</li></ul></dd></div></dl></section></div><h2 id="知识范围与官方定位" class="astro-c5eeuzv2">知识范围与官方定位</h2><p class="astro-c5eeuzv2">五级需要理解并会使用埃拉托斯特尼筛法与线性筛：前者批量标记素数的倍数，后者借助素数表和最小质因数让每个合数只被筛一次。</p><p>本页先建立共同基础并比较各方法；完整过程、C++ 写法与复杂度请进入对应子知识点学习。</p><section aria-labelledby="问题和范围"><h2 id="问题和范围" class="astro-c5eeuzv2">问题和范围</h2><div><section aria-labelledby="prime-sieves-problem" class="astro-c5eeuzv2"><h3 id="prime-sieves-problem" class="astro-c5eeuzv2">要解决的问题</h3><p class="astro-c5eeuzv2">如果只有一个数 x，可以尝试从 2 开始寻找它的因数。但当 q 个问题都在问 0 到 n 中的数时，每次重新试除会反复做相同工作：例如判断 9 和 15 时，都会再次检查它们是否能被 3 整除。</p><p class="astro-c5eeuzv2">更合适的目标是先回答一个共同问题：0 到 n 中每个数是不是素数？我们把全部结果保存为 isPrime[0..n]，以后查询 x 时只读 isPrime[x]，一次预处理就能服务后面的所有问题。</p><p class="astro-c5eeuzv2">本页用埃氏筛完成一份最小程序，让你看清“建表一次，查询多次”的共同流程，并比较何时继续学习埃氏筛或线性筛。两种方法各自的完整推导、证明和实现仍在对应子知识点中。</p></section><section aria-labelledby="prime-sieves-scope" class="astro-c5eeuzv2"><h3 id="prime-sieves-scope" class="astro-c5eeuzv2">本课范围</h3><ul class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">本教学程序先输入 n、q，再输入 q 个查询值 x；支持 0 ≤ n ≤ 1000000、0 ≤ q ≤ 100000，并保证每个查询满足 0 ≤ x ≤ n。</li><li class="astro-c5eeuzv2">使用全局 bool isPrime[1000001]，合法下标是 0 到 1000000。n = 0 时只访问 isPrime[0]；只有 n ≥ 1 时才访问 isPrime[1]。</li><li class="astro-c5eeuzv2">常见 GCC/OJ 环境中 bool 通常占 1 字节，这张表约占 0.95 MiB，实际大小仍以编译器和平台为准。程序最多输出 q 行 YES 或 NO，连同换行约不超过 400000 个字符。</li><li class="astro-c5eeuzv2">在 D1 范围内 p、p * p 和 multiple + p 都能放入 int。程序仍先判断 p &lt;= n / p，再计算 p * p，避免把不安全的乘法条件带到更大范围。</li><li class="astro-c5eeuzv2">父页只建立共同结果表、查询闭环和方法选择；埃氏筛子页负责 p * p 的完整形成与标记证明，线性筛子页负责素数表、最小质因数唯一标记和 break 的完整推导。</li></ul></section></div></section><section aria-labelledby="共同基础"><h2 id="共同基础" class="astro-c5eeuzv2">共同基础</h2><ul><li class="astro-c5eeuzv2">素数是大于 1 且只有 1 和自身两个正因数的整数，因此 0 和 1 都不是素数。</li><li class="astro-c5eeuzv2">筛法先建立 isPrime 等标记数组，再按从小到大的顺序排除合数。</li><li class="astro-c5eeuzv2">前缀计数 cnt[i] 表示 1 到 i 中的素数个数；令 cnt[0] = 0，并递推 cnt[i] = cnt[i - 1] + (isPrime[i] ? 1 : 0)。</li><li class="astro-c5eeuzv2">区间 [l, r] 中的素数个数是 cnt[r] - cnt[l - 1]；这里要求 1 &lt;= l &lt;= r，l = 1 时会使用 cnt[0]。</li><li class="astro-c5eeuzv2">数组下标必须覆盖 0 到 n。常见 GCC/OJ 环境中 bool 通常占 1 字节，bool isPrime[1000001] 约占 0.95 MiB；若再用两个 int 数组保存最小质因数和素数表，约再占 7.63 MiB。实际大小仍以编译器和平台为准。</li><li class="astro-c5eeuzv2">标记 p 的倍数时要防止乘法越界：可先保证 p &lt;= n / p，再计算 p * p；线性筛也应先判断 p &gt; n / i。</li></ul></section><section aria-labelledby="方法对比"><h2 id="方法对比" class="astro-c5eeuzv2">方法对比</h2><div><section aria-labelledby="prime-sieves-comparison-0" class="astro-c5eeuzv2"><h3 id="prime-sieves-comparison-0" class="astro-c5eeuzv2">埃拉托斯特尼筛法</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>用每个已确认素数批量标记它的倍数。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>写法直观，时间复杂度为 O(n log log n)。</p></section><section aria-labelledby="prime-sieves-comparison-1" class="astro-c5eeuzv2"><h3 id="prime-sieves-comparison-1" class="astro-c5eeuzv2">线性筛</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>维护素数表，并用最小质因数唯一地筛掉每个合数。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>必须理解整除后 break 的原因，时间复杂度为 O(n)。</p></section></div></section><section aria-labelledby="算法怎样形成"><h2 id="算法怎样形成" class="astro-c5eeuzv2">算法怎样形成</h2><p class="astro-c5eeuzv2">以 n = 16、连续查询 0、1、2、7、9、16 为例。逐个试除能回答每一问，却会一遍遍寻找因数；六次查询真正共享的是同一份“0 到 16 哪些数是素数”的结果。</p><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">先把重复工作移到查询前：只要预先确定 0 到 n 中每个数的真假，后面无论查询多少次，都不需要再次寻找因数。这就是“一次预处理，多次查询”。</li><li class="astro-c5eeuzv2">用 isPrime[x] 保存共同结果最直接：true 表示 x 筛完后仍可能是素数，false 表示 x 已确定不是素数。数组必须覆盖 0 到 n，因为查询值可能正好是 0 或 n。</li><li class="astro-c5eeuzv2">初始化不能只依赖全局数组原来的 false：建表开始时先把 0 到 n 全设为 true，再明确把 0 设为 false；只有 n ≥ 1 时再把 1 设为 false。这样 0 和 1 的数学边界与数组访问边界同时正确。</li><li class="astro-c5eeuzv2">接下来需要一种方法证明合数。埃氏筛从一个已确认素数出发，批量标记它的倍数；线性筛维护素数表，让每个合数只由最小质因数筛掉一次。两者最终都建立能直接查询的素数标记表。</li><li class="astro-c5eeuzv2">本页的最小程序选择埃氏筛演示共同流程：p = 2 时标记 4、6、8、10、12、14、16；p = 3 时检查 9、12、15。为什么从 p * p 开始以及更小倍数为何已处理，留到埃氏筛子页完整推导。</li><li class="astro-c5eeuzv2">安全停止也属于共同边界意识：先检查 p &lt;= n / p，条件成立后才计算 p * p。主样例到 p = 5 时，5 &lt;= 16 / 5 不成立，筛表停止；不会先计算一个未经保护的平方条件。</li><li class="astro-c5eeuzv2">查询阶段不再改变表：依次读取 x，isPrime[x] 为 true 输出 YES，否则输出 NO。主样例最终为真的下标是 2、3、5、7、11、13，所以六次查询得到 NO、NO、YES、YES、NO、NO。</li><li class="astro-c5eeuzv2">选择方法时看任务需要：先求稳、写法直观，进入埃氏筛；要求线性预处理或后续要使用素数表，进入线性筛。父页只帮助选择，不把两个子页的循环和证明复制一遍。</li><li class="astro-c5eeuzv2">筛完还可以做一次简短应用：令 cnt[0] = 0，cnt[i] = cnt[i - 1] + (isPrime[i] ? 1 : 0)。当 1 ≤ l ≤ r 时，[l, r] 的素数个数是 cnt[r] - cnt[l - 1]；l = 1 时正好读取 cnt[0]。</li></ol></section><section aria-labelledby="自然语言步骤"><h2 id="自然语言步骤" class="astro-c5eeuzv2">自然语言步骤</h2><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">读入范围上界 n 和查询数量 q，确认每个查询值 x 都在 0 到 n 内。</li><li class="astro-c5eeuzv2">调用 buildPrimeTable(n) 一次。先把 isPrime[0..n] 全部设为 true，再令 isPrime[0] = false；若 n ≥ 1，再令 isPrime[1] = false。</li><li class="astro-c5eeuzv2">令 p = 2。每轮先判断 p &lt;= n / p；若不成立，说明不再需要用新的 p 标记倍数，筛表结束。</li><li class="astro-c5eeuzv2">若 isPrime[p] 为 true，就从 p * p 开始，每次增加 p，把不超过 n 的倍数设为 false；若 isPrime[p] 已为 false，本轮跳过标记。</li><li class="astro-c5eeuzv2">让 p 增加 1，回到安全条件继续检查。筛表结束后，isPrime[x] 就是查询 x 的固定答案。</li><li class="astro-c5eeuzv2">回到 main()，按输入顺序处理 q 次查询。每次读入 x，只检查 isPrime[x]：为 true 输出 YES，否则输出 NO。</li><li class="astro-c5eeuzv2">若题目改问区间素数个数，可以在筛表后另建 cnt：从 1 到 n 递推一次，再用 cnt[r] - cnt[l - 1] 回答；这不是本页 C1 的第二个目标。</li><li class="astro-c5eeuzv2">需要完整理解批量标记及 p * p 时先学埃氏筛；需要理解素数表、最小质因数和 break 时再学线性筛。</li></ol></section><section aria-labelledby="执行账本"><h2 id="执行账本" class="astro-c5eeuzv2">执行账本</h2><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">共享预处理与查询流程</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">阶段</th><th scope="col" class="astro-c5eeuzv2">当前对象</th><th scope="col" class="astro-c5eeuzv2">处理前状态</th><th scope="col" class="astro-c5eeuzv2">动作或判断依据</th><th scope="col" class="astro-c5eeuzv2">本次检查</th><th scope="col" class="astro-c5eeuzv2">状态变化或输出</th><th scope="col" class="astro-c5eeuzv2">下一步</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">初始化</td><td class="astro-c5eeuzv2">n = 16</td><td class="astro-c5eeuzv2">0 到 16 尚未建表</td><td class="astro-c5eeuzv2">先全部设为 true，再单独处理 0 和 1</td><td class="astro-c5eeuzv2">isPrime[0..16]</td><td class="astro-c5eeuzv2">0、1 为 false；2 到 16 为 true</td><td class="astro-c5eeuzv2">从 p = 2 开始筛表</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">筛表</td><td class="astro-c5eeuzv2">p = 2</td><td class="astro-c5eeuzv2">isPrime[2] = true</td><td class="astro-c5eeuzv2">2 是当前未被标记的数，从 2 × 2 开始按步长 2 标记</td><td class="astro-c5eeuzv2">4、6、8、10、12、14、16</td><td class="astro-c5eeuzv2">这些数第一次变为 false</td><td class="astro-c5eeuzv2">p 增加到 3</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">筛表</td><td class="astro-c5eeuzv2">p = 3</td><td class="astro-c5eeuzv2">isPrime[3] = true</td><td class="astro-c5eeuzv2">3 是当前未被标记的数，从 3 × 3 开始按步长 3 标记</td><td class="astro-c5eeuzv2">9、12、15</td><td class="astro-c5eeuzv2">9、15 第一次变为 false；12 已是 false</td><td class="astro-c5eeuzv2">p 增加到 4</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">筛表</td><td class="astro-c5eeuzv2">p = 4</td><td class="astro-c5eeuzv2">isPrime[4] = false</td><td class="astro-c5eeuzv2">4 已确定是合数，不用它再标记倍数</td><td class="astro-c5eeuzv2">不检查倍数</td><td class="astro-c5eeuzv2">表不变</td><td class="astro-c5eeuzv2">p 增加到 5</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">筛表停止</td><td class="astro-c5eeuzv2">p = 5</td><td class="astro-c5eeuzv2">16 / 5 = 3</td><td class="astro-c5eeuzv2">5 &lt;= 3 不成立，安全循环条件失败</td><td class="astro-c5eeuzv2">不计算新的 p × p 起点</td><td class="astro-c5eeuzv2">素数下标为 2、3、5、7、11、13</td><td class="astro-c5eeuzv2">回到 main() 处理查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 1</td><td class="astro-c5eeuzv2">x = 0</td><td class="astro-c5eeuzv2">isPrime[0] = false</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 1 个查询</td><td class="astro-c5eeuzv2">输出 NO</td><td class="astro-c5eeuzv2">还剩 5 个查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 2</td><td class="astro-c5eeuzv2">x = 1</td><td class="astro-c5eeuzv2">isPrime[1] = false</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 2 个查询</td><td class="astro-c5eeuzv2">输出 NO</td><td class="astro-c5eeuzv2">还剩 4 个查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 3</td><td class="astro-c5eeuzv2">x = 2</td><td class="astro-c5eeuzv2">isPrime[2] = true</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 3 个查询</td><td class="astro-c5eeuzv2">输出 YES</td><td class="astro-c5eeuzv2">还剩 3 个查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 4</td><td class="astro-c5eeuzv2">x = 7</td><td class="astro-c5eeuzv2">isPrime[7] = true</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 4 个查询</td><td class="astro-c5eeuzv2">输出 YES</td><td class="astro-c5eeuzv2">还剩 2 个查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 5</td><td class="astro-c5eeuzv2">x = 9</td><td class="astro-c5eeuzv2">isPrime[9] = false</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 5 个查询</td><td class="astro-c5eeuzv2">输出 NO</td><td class="astro-c5eeuzv2">还剩 1 个查询</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">查询 6</td><td class="astro-c5eeuzv2">x = 16</td><td class="astro-c5eeuzv2">isPrime[16] = false</td><td class="astro-c5eeuzv2">直接读取表</td><td class="astro-c5eeuzv2">第 6 个查询</td><td class="astro-c5eeuzv2">输出 NO</td><td class="astro-c5eeuzv2">q 次查询全部完成，程序结束</td></tr></tbody></table></div><p><strong class="astro-c5eeuzv2">追踪结论：</strong>这张表只追踪父页共有的“预处理一次，再按顺序查询”闭环。筛表结束后六次查询都不修改 isPrime；它不是埃氏筛和线性筛两种算法的完整执行推导。</p></section><section aria-labelledby="完整程序"><h2 id="完整程序" class="astro-c5eeuzv2">完整程序</h2><h3 class="astro-c5eeuzv2">共同模型的最小完整示例：建表一次，查询多次</h3><p class="astro-c5eeuzv2">下面是本页唯一一份完整 C++14 程序。它用最小埃氏筛实现 buildPrimeTable(n)，只为闭合共享预处理与查询流程；线性筛的素数表、最小质因数和 break 不在这里实现。</p><div><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输入</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">16 6\n0 1 2 7 9 16</code></pre><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输出</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">NO\nNO\nYES\nYES\nNO\nNO</code></pre></div><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">样例说明：</strong>筛表完成后，0、1、9、16 对应 false，2、7 对应 true；0 到 16 的素数是 2、3、5、7、11、13，因此按查询顺序输出六行结果。</p><pre tabindex="0"><code class="language-cpp">#include &lt;bits/stdc++.h&gt;\nusing namespace std;\n\nconst int MAX_N = 1000000;\nbool isPrime[MAX_N + 1];\n\nvoid buildPrimeTable(int n) {\n  for (int value = 0; value &lt;= n; value++) {\n      isPrime[value] = true;\n  }\n  isPrime[0] = false;\n  if (n &gt;= 1) {\n      isPrime[1] = false;\n  }\n\n  int p = 2;\n  while (p &lt;= n / p) {\n      if (isPrime[p]) {\n          for (int multiple = p * p; multiple &lt;= n; multiple += p) {\n              isPrime[multiple] = false;\n          }\n      }\n      p++;\n  }\n}\n\nint main() {\n  int n, q;\n  cin &gt;&gt; n &gt;&gt; q;\n\n  buildPrimeTable(n);\n\n  for (int queryIndex = 1; queryIndex &lt;= q; queryIndex++) {\n      int x;\n      cin &gt;&gt; x;\n      if (isPrime[x]) {\n          cout &lt;&lt; &quot;YES\\n&quot;;\n      } else {\n          cout &lt;&lt; &quot;NO\\n&quot;;\n      }\n  }\n\n  return 0;\n}\n</code></pre></section><section aria-labelledby="复杂度"><h2 id="复杂度" class="astro-c5eeuzv2">复杂度</h2><p class="astro-c5eeuzv2">要把“预处理”和“查询”分开计算。C1 先建立整张表，再回答 q 次查询；查询再多，也不会重新执行筛法。</p><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">时间与空间复杂度</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">项目</th><th scope="col" class="astro-c5eeuzv2">复杂度</th><th scope="col" class="astro-c5eeuzv2">说明</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">初始化标记数组</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">把 isPrime[0..n] 写入一次；这部分包含在整个预处理量级中。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">C1 埃氏筛预处理</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n log log n)</code></td><td class="astro-c5eeuzv2">按素数批量标记倍数；这是本页最小程序采用的方法。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">单次点查询</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(1)</code></td><td class="astro-c5eeuzv2">只读取 isPrime[x] 并输出一行。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">q 次点查询</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(q)</code></td><td class="astro-c5eeuzv2">每个查询只做常数次操作。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">C1 总时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n log log n + q)</code></td><td class="astro-c5eeuzv2">一次埃氏筛预处理加 q 次查询。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">线性筛方案总时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n + q)</code></td><td class="astro-c5eeuzv2">线性筛预处理为 O(n)，之后同样每次 O(1) 查询；线性筛维护素数表，但不要求必须另存 minPrime 数组。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">标记表空间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">isPrime 有 n + 1 个有效位置；固定上限数组约 0.95 MiB，实际以平台为准。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">区间计数扩展</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">预处理 O(n)，查询 O(1)</code></td><td class="astro-c5eeuzv2">筛表后再建 cnt；每个区间用 cnt[r] - cnt[l - 1]。</td></tr></tbody></table></div></section><section aria-labelledby="常见错误"><h2 id="常见错误" class="astro-c5eeuzv2">常见错误</h2><div><section aria-labelledby="prime-sieves-error-0" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-0" class="astro-c5eeuzv2">每次查询都重新试除</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是 q 增大后程序重复检查相同因数，查询时间随每个 x 的大小继续增加；根因是没有利用所有查询共享同一范围。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>先调用 buildPrimeTable(n) 一次，再让每次查询只读 isPrime[x]。修正后用同一个 x 连续查询多次，确认只有一次筛表而输出每次都相同。</p></section><section aria-labelledby="prime-sieves-error-1" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-1" class="astro-c5eeuzv2">把 0 或 1 留成 true</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">isPrime[0] = true;\nisPrime[1] = true;</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是查询 0 或 1 时输出 YES；根因是“没有被倍数循环标记”不等于“它是素数”，素数定义要求大于 1。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>始终令 isPrime[0] = false；只有 n ≥ 1 时再令 isPrime[1] = false。修正后分别运行 n = 0 和 n = 1 的端点查询，都应输出 NO。</p></section><section aria-labelledby="prime-sieves-error-2" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-2" class="astro-c5eeuzv2">数组没有给下标 n 留位置</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">bool isPrime[1000000];</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是 n = 1000000 或查询 x = n 时越界；根因是 0 到 n 一共有 n + 1 个下标。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>按 D1 使用 bool isPrime[1000001]，最大合法下标正好是 1000000。修正后重测 n = 1000000 且查询两个端点 0 和 n。</p></section><section aria-labelledby="prime-sieves-error-3" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-3" class="astro-c5eeuzv2">先计算平方再判断范围</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">while (p * p &lt;= n) {</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>在本课 D1 内通常看不出错误，但放大范围后 p * p 可能先发生 int 溢出，条件结果就不可靠；根因是保护判断发生得太晚。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>先写 p &lt;= n / p，只有条件成立后才在循环体中计算 p * p。修正后检查主样例在 p = 5 时因 5 &lt;= 16 / 5 为假而停止。</p></section><section aria-labelledby="prime-sieves-error-4" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-4" class="astro-c5eeuzv2">混淆 isPrime 与 isComposite</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">if (isPrime[x]) {\n  cout &lt;&lt; &quot;NO&quot;;\n}</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是素数 2 输出 NO、合数 9 反而输出 YES；根因是把“为真表示素数”和“为真表示合数”两套命名混在一起。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>本页统一规定 isPrime[x] 为 true 才输出 YES；若改用 isComposite，就要同步反转初始化、标记和查询判断。修正后用 2 与 9 成对复测。</p></section><section aria-labelledby="prime-sieves-error-5" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-5" class="astro-c5eeuzv2">没有把 0 到 n 初始化为 true</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">bool isPrime[MAX_N + 1];\nisPrime[0] = false;</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是几乎所有查询都输出 NO；根因是全局 bool 数组初始为 false，而筛法需要先把 2 到 n 看作候选素数。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>buildPrimeTable 开始先逐个把 isPrime[0..n] 设为 true，再覆盖 0 和 1。修正后运行 n = 2，查询 0、1、2，应依次得到 NO、NO、YES。</p></section><section aria-labelledby="prime-sieves-error-6" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-6" class="astro-c5eeuzv2">把一次查询也说成筛法复杂度</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是把每次 isPrime[x] 查询误写成 O(n log log n)，或只写 O(1) 却漏掉建表；根因是没有区分预处理、单次查询和总流程。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>分别记录 C1 预处理 O(n log log n)、单次查询 O(1)、q 次查询 O(q)、总时间 O(n log log n + q)。修正后用 q = 0 与 q = 100000 对照哪些步骤仍会执行。</p></section><section aria-labelledby="prime-sieves-error-7" class="astro-c5eeuzv2"><h3 id="prime-sieves-error-7" class="astro-c5eeuzv2">在父页同时背两份完整筛法代码</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是还没有看清共同结果表，就要同时处理两套循环、两套证明和两个停止条件；根因是把父页的模型选择职责与子页的完整实现职责混在一起。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>父页只保留一份最小埃氏筛 C1 和准确比较；先到埃氏筛子页学完整批量标记，再到线性筛子页学最小质因数与 break。复核时确认父页只有一个 main 且没有线性筛内层循环。</p></section></div></section><section aria-labelledby="练习与自检"><h2 id="练习与自检" class="astro-c5eeuzv2">练习与自检</h2><div><section aria-labelledby="prime-sieves-practice-0" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-0" class="astro-c5eeuzv2">练习一：读懂筛后表</h3><p class="astro-c5eeuzv2">n = 16 的筛表完成后，isPrime[1]、isPrime[3]、isPrime[12] 分别是什么？各查询应输出什么？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>先用“大于 1 且只有两个正因数”判断，再对应 true/false 与 YES/NO。</p></section><section aria-labelledby="prime-sieves-practice-1" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-1" class="astro-c5eeuzv2">练习二：守住安全边界</h3><p class="astro-c5eeuzv2">为什么 n = 0 时不能无条件写 isPrime[1] = false？为什么计算 p * p 前要先检查 p &lt;= n / p？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>分别检查数组合法下标和乘法发生的先后顺序。</p></section><section aria-labelledby="prime-sieves-practice-2" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-2" class="astro-c5eeuzv2">练习三：选择筛法</h3><p class="astro-c5eeuzv2">只需直观地筛出一段范围内的素数，和“要求 O(n) 预处理并继续使用素数表”这两种任务，分别优先进入哪个子知识点？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>比较两张方法卡的核心做法，不需要写第二份程序。</p></section><section aria-labelledby="prime-sieves-practice-3" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-3" class="astro-c5eeuzv2">练习四：只用表回答</h3><p class="astro-c5eeuzv2">表已建好，依次查询 0、2、16、2。写出四行输出，并说明第二次查询 2 时是否要重新筛表。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>同一个下标的标记不会在查询阶段改变。</p></section><section aria-labelledby="prime-sieves-practice-4" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-4" class="astro-c5eeuzv2">练习五：区间计数</h3><p class="astro-c5eeuzv2">已经建立 cnt，求 [1, 7] 中的素数个数。应使用哪个式子，答案是多少？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>l = 1 时，l - 1 等于 0；1 到 7 的素数是 2、3、5、7。</p></section><section aria-labelledby="prime-sieves-practice-5" class="astro-c5eeuzv2"><h3 id="prime-sieves-practice-5" class="astro-c5eeuzv2">自检：复述完整闭环</h3><p class="astro-c5eeuzv2">不看程序，按顺序说出初始化、筛表、停止、查询和方法选择，并说明父页没有展开哪两部分。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>检查自己是否说到了 0/1、0..n、一次预处理、O(1) 查询、埃氏筛与线性筛子页。</p></section></div><div><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习一答案</summary><p class="astro-c5eeuzv2">1 不是素数，所以 isPrime[1] = false，输出 NO；3 是素数，所以 isPrime[3] = true，输出 YES；12 是合数，所以 isPrime[12] = false，输出 NO。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习二答案</summary><p class="astro-c5eeuzv2">n = 0 时合法下标只有 0，访问 1 会越界；因此必须先判断 n ≥ 1。p * p 若先计算就可能先溢出，除法条件把保护放在乘法之前。主样例 p = 5 时条件失败并停止。</p><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">isPrime[0] = false;\nif (n &gt;= 1) {\n  isPrime[1] = false;\n}</code></pre></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习三答案</summary><p class="astro-c5eeuzv2">要直观批量标记倍数，先进入“埃拉托斯特尼筛法”；要求 O(n) 预处理并维护素数表，再进入“线性筛”。父页只负责选路，不代替两页的完整推导。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习四答案</summary><p class="astro-c5eeuzv2">四行依次是 NO、YES、NO、YES。第二次查询 2 仍直接读取 isPrime[2]，不重新筛表；查询阶段不改变标记。</p><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">NO\nYES\nNO\nYES</code></pre></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习五答案</summary><p class="astro-c5eeuzv2">使用 cnt[7] - cnt[0]。1 到 7 中有 2、3、5、7 四个素数，所以答案是 4；cnt[0] = 0 保证 l = 1 时公式仍成立。</p><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">cnt[7] - cnt[0]</code></pre></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看自检答案</summary><p class="astro-c5eeuzv2">先初始化 isPrime[0..n] 并把 0、1 设为非素数；再选择筛法建立一次结果表；满足安全停止条件前完成标记；随后 q 次查询各用 O(1) 读取；按任务需要先学埃氏筛，再学线性筛。父页不展开 p * p 的完整证明，也不展开最小质因数与 break 的完整证明。</p></details></div></section><section aria-labelledby="下一步"><h2 id="下一步" class="astro-c5eeuzv2">下一步</h2><p class="astro-c5eeuzv2">共同模型已经建立。请先进入“埃拉托斯特尼筛法”，完整理解为什么从 p × p 开始以及倍数怎样被批量标记；再进入“线性筛”，理解素数表、最小质因数唯一标记与整除后的 break。两个子页都可以直接访问，不设置完成门槛。</p></section><section aria-labelledby="选择子知识点"><h2 id="选择子知识点" class="astro-c5eeuzv2">选择子知识点</h2><p class="astro-c5eeuzv2">根据当前要解决的问题进入对应页面；每个子知识点都可以直接访问，不设置完成门槛。</p><ul><li class="astro-c5eeuzv2"><a href="/gesp/level5/prime-sieves/eratosthenes-sieve/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">埃拉托斯特尼筛法</strong><small class="astro-c5eeuzv2">从 p × p 开始标记倍数，掌握直观的区间素数预处理。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/prime-sieves/linear-sieve/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">线性筛</strong><small class="astro-c5eeuzv2">用素数表和最小质因数保证每个合数只被筛一次。</small></span><span class="astro-c5eeuzv2">→</span></a></li></ul></section><h2 id="核心内容检查" class="astro-c5eeuzv2">核心内容检查</h2><h2 id="整页综合测试" class="astro-c5eeuzv2">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></article></div><footer',
        '<article id="top"><p>一个整数可以有许多乘法拆法，但继续拆到质数并合并相同质因数后，会得到唯一的标准形式。本课从 360 的具体拆分出发，推导升序试除、重复相除和最后剩余因子的处理规则。得到各质因数的指数后，还能直接计算约数个数，并理解约数和与完全平方数等基础应用。</p><section class="uf-quick-card" aria-labelledby="本课先记住"><h2 id="本课先记住">本课先记住</h2><div class="uf-quick-card__grid"><div class="uf-quick-card__core"><h3>核心结论</h3><p>每个大于 1 的正整数，都能唯一分解为素数幂的乘积（不计排列顺序）。</p><p class="uf-quick-card__formula">360 = 2<sup>3</sup> × 3<sup>2</sup> × 5</p></div><div class="uf-quick-card__syntax"><h3>常用语法</h3><p>用 <code>while</code> 除尽同一质因数。</p><pre><code>while (n % p == 0) {\n    n = n / p;\n    count++;\n}</code></pre><p class="uf-quick-card__caption">p 是当前试除因子；count 提前设为 0，用来累计它的指数。</p></div><div class="uf-quick-card__example"><h3>最小示例</h3><p class="uf-quick-card__small-formula">12 = 2<sup>2</sup> × 3</p><p>指数是 2 和 1。</p><p>约数个数：<br>(2 + 1) × (1 + 1) = <strong>6</strong></p></div><div class="uf-quick-card__warning"><h3>关键易错点</h3><ul><li>同一质因数要连续除尽。</li><li>循环后别漏掉剩余的大质因数。</li><li>约数个数是“指数加一”后相乘。</li></ul></div></div></section><details class="uf-mobile-toc"><summary>本页目录 <span>定位知识、例题与自测</span></summary></details><section aria-labelledby="01-唯一分解定理"><h2 id="01-唯一分解定理" class="astro-2i3ys3wc">01 唯一分解定理</h2><p class="astro-2i3ys3wc">先看 360：它可以写成 36×10，也可以写成 8×45。把这些合数继续拆开并合并相同质因数，都会整理成 360=2³×3²×5。</p><p class="astro-2i3ys3wc">唯一分解定理准确地说：每个大于 1 的整数都能写成 n=p₁^a₁×p₂^a₂×…×pₖ^aₖ，其中 p₁&lt;p₂&lt;…&lt;pₖ 是互不相同的质数，a₁、a₂、…、aₖ 是正整数；这样的质数和指数唯一。“唯一”忽略乘法因子的书写顺序，但不允许把 4、9 等合数留作底数。</p><p class="astro-2i3ys3wc">1 不在定理“大于 1”的范围内。1 没有质因数，后面会把它作为单独边界处理。</p><div><h3 class="astro-2i3ys3wc">具体样例与推演</h3><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">场景：</strong>有人沿着 360=36×10 分解，另一个人沿着 360=8×45 分解，他们最后会不会得到不同答案？</p><ol class="astro-2i3ys3wc"><li class="astro-2i3ys3wc">沿 36×10 拆：36=2²×3²，10=2×5，合并后是 2³×3²×5。</li><li class="astro-2i3ys3wc">沿 8×45 拆：8=2³，45=3²×5，合并后仍是 2³×3²×5。</li><li class="astro-2i3ys3wc">两条路径的先后顺序不同，但只要继续拆到质数、合并相同质数并按升序整理，质数底数和指数就完全相同。</li></ol><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">适用结论：</strong>算术基本定理保证：每个大于 1 的整数都能写成质数幂的乘积，而且除因子排列顺序外，这种写法唯一。</p><aside',
        '<article id="top" data-level5-topic-hub="big-integer-add-subtract"><p>同一份仓库记录可能提出四种问题：两批货物合计多少、发出一部分后还剩多少、相同数量装了很多箱共多少、按固定容量能装几箱并余多少。数很大时，先别急着套某段代码；本页先带你看懂共同表示，再根据题意关系、要维护的状态和扫描方向选择正确子页。</p><div id="本课先记住"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>高精度运算先把超大非负整数保存为十进制数位，再按题意关系、维护状态和扫描方向选择加、减、乘或除。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>字符串负责完整读入，普通数组负责逐位保存；下面只展示表示方法，不是完整运算程序。</p><pre tabindex="0"><code>string number;\nint digit[1005];\ndigit[0] = number[number.size() - 1] - &#39;0&#39;;</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>507 逆序保存后，数组从下标 0 开始依次是 7、0、5；这样加、减、乘可以先处理个位。</p><pre tabindex="0"><code>507 -&gt; digit[0..2] = {7, 0, 5}</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>不能只看数字位数选运算</li><li>加、减、乘通常从低位开始，除法从高位开始</li><li>输出时要删除无效前导零，但零必须保留一个 0。</li></ul></dd></div></dl></section></div><h2 id="知识范围与官方定位" class="astro-c5eeuzv2">知识范围与官方定位</h2><p class="astro-c5eeuzv2">五级高精度运算包括大整数表示、加法、可出现负结果的减法、高精度乘普通整数、高精度乘高精度整数、高精度除普通正整数，以及阶乘中的高精度应用。本页只建立共同模型和选择路线；各运算的完整过程、程序与复杂度由四个子页分别讲解。</p><p>本页先建立共同基础并比较各方法；完整过程、C++ 写法与复杂度请进入对应子知识点学习。</p><section aria-labelledby="共同基础"><h2 id="共同基础" class="astro-c5eeuzv2">共同基础</h2><ul><li class="astro-c5eeuzv2">常见 long long 也有固定范围，位数继续增加就不能把整个数一次放进去。高精度运算改为先用 string 完整读入，再把每个十进制数字转换成普通 int 数组中的一个元素。</li><li class="astro-c5eeuzv2">十进制数位序列保留了个位、十位、百位等位值。把 507 逆序保存为 [7, 0, 5] 后，下标 0 就是个位；加法、减法和乘法通常可以按下标从小到大处理。</li><li class="astro-c5eeuzv2">除法的商位由已经读过的高位前缀决定，所以要从最高位向最低位扫描。方向不是固定模板，要由当前运算中信息传递的方向决定。</li><li class="astro-c5eeuzv2">不同运算要维护不同状态：加法跟踪进位，减法跟踪借位，乘法累加数位贡献并处理进位，除法跟踪余数。这些状态在父页只用来帮助选择，具体更新规则留给子页。</li><li class="astro-c5eeuzv2">无论选择哪种运算，最后都要得到规范十进制结果：删除无效前导零，但结果为零时仍保留一个 0；数组容量还要覆盖可能出现的全部结果数位。</li><li class="astro-c5eeuzv2">选择演示 1：仓库原有 123456789012345678901 件零件，发出 987654321 件，题目要求剩余量。第一个数已经超出常见 long long 的十进制范围，所以先确定要按十进制数位保存。</li><li class="astro-c5eeuzv2">选择演示 2：题目动作是“原有量去掉发出量”，关系是求差，不是求总和、若干倍或平均分配，因此应选择减法。</li><li class="astro-c5eeuzv2">选择演示 3：计算前要比较两个数的大小，确定结果符号；逐位计算时要从低位向高位跟踪借位。这些正是高精度减法子页要解决的状态和方向。</li><li class="astro-c5eeuzv2">选择演示 4：因此下一步进入“高精度减法”子页，而不是在总览页展开逐位借位；本页也不计算这个大数减法的最终结果。</li><li class="astro-c5eeuzv2">选择演示 5：学完后可用“非负差 + 987654321 = 123456789012345678901”复核。若还原失败，先检查运算选择、大小关系和借位。</li><li class="astro-c5eeuzv2">本页到“会选择并会检查”为止。逐位进位、借位与符号、乘法数位贡献与阶乘、逐位商和余数的完整实现，都在对应子页中展开。</li></ul></section><section aria-labelledby="方法对比"><h2 id="方法对比" class="astro-c5eeuzv2">方法对比</h2><div><section aria-labelledby="big-integer-add-subtract-comparison-0" class="astro-c5eeuzv2"><h3 id="big-integer-add-subtract-comparison-0" class="astro-c5eeuzv2">合计：高精度加法</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>题目把两个非负大数量合在一起，关系是求和；从低位向高位处理并跟踪进位，应进入“高精度加法”子页。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>交换两个加数后总和应保持不变；不同长度也仍是加法，不能只看位数改选别的结构。</p></section><section aria-labelledby="big-integer-add-subtract-comparison-1" class="astro-c5eeuzv2"><h3 id="big-integer-add-subtract-comparison-1" class="astro-c5eeuzv2">剩余：高精度减法</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>题目从原有大数量中去掉一部分，关系是求差；先比较大小，再从低位向高位跟踪借位，应进入“高精度减法”子页。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>被减数不小于减数时，用“差 + 减数 = 被减数”复核；若不成立，检查大小关系、符号和借位。</p></section><section aria-labelledby="big-integer-add-subtract-comparison-2" class="astro-c5eeuzv2"><h3 id="big-integer-add-subtract-comparison-2" class="astro-c5eeuzv2">相同数量重复：高精度乘法</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>每箱数量相同，求很多箱的总量，关系是相同数量重复；累加数位贡献并从低位处理进位，应进入“高精度乘法”子页。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>错误现象是只把每箱数量加一次，结果只代表两箱；原因是把重复次数漏掉了。改选乘法后，可先用小箱数重复相加复核，例如每箱 7 件、3 箱应与 7 + 7 + 7 的 21 件一致。</p></section><section aria-labelledby="big-integer-add-subtract-comparison-3" class="astro-c5eeuzv2"><h3 id="big-integer-add-subtract-comparison-3" class="astro-c5eeuzv2">平均分配：高精度除普通整数</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>把超大总量按一个正的普通容量平均分配，要求份数和余数，关系是除法；从高位向低位扫描并跟踪余数，应进入“高精度除普通整数”子页。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>错误现象是看到“有剩余”就只做一次减法，无法回答能装几箱；原因是忽略了平均分配。改选除法后，用“商 × 除数 + 余数 = 被除数”且“余数小于正除数”复核。</p></section><section aria-labelledby="big-integer-add-subtract-comparison-4" class="astro-c5eeuzv2"><h3 id="big-integer-add-subtract-comparison-4" class="astro-c5eeuzv2">方向纠错：除法不能照搬低位扫描</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>若除法从个位开始，当前商位缺少更高位前缀，无法正确确定；原因是把加、减、乘的信息方向机械套给了除法。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>修正为从最高位向最低位扫描并维护余数；每处理一位，都检查当前余数小于正除数，最后再用商和余数还原被除数。</p></section></div></section><section aria-labelledby="选择子知识点"><h2 id="选择子知识点" class="astro-c5eeuzv2">选择子知识点</h2><p class="astro-c5eeuzv2">根据当前要解决的问题进入对应页面；每个子知识点都可以直接访问，不设置完成门槛。</p><ul><li class="astro-c5eeuzv2"><a href="/gesp/level5/big-integer-add-subtract/addition/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">高精度加法</strong><small class="astro-c5eeuzv2">已完成：能处理不同长度和连续进位，并输出规范的非负十进制总和。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/big-integer-add-subtract/subtraction/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">高精度减法</strong><small class="astro-c5eeuzv2">下一步：能比较大小、判断符号、完成逐位借位，并用加法还原被减数。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li id="04-高精度乘法" class="astro-c5eeuzv2"><a href="/gesp/level5/big-integer-add-subtract/multiply-by-integer/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">高精度乘法</strong><small class="astro-c5eeuzv2">之后：能区分高乘低与高乘高，说明数位贡献位置，并把阶乘识别为连续的高乘低。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/big-integer-add-subtract/divide-by-integer/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">高精度除普通整数</strong><small class="astro-c5eeuzv2">最后：能从高位确定商位，得到商和余数，并用商、除数和余数还原被除数。</small></span><span class="astro-c5eeuzv2">→</span></a></li></ul></section><h2 id="核心内容检查" class="astro-c5eeuzv2">核心内容检查</h2><h2 id="整页综合测试" class="astro-c5eeuzv2">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></article></div><footer',
        '<article id="top" data-level5-topic-hub="linked-list-structures"><p>位置 1、3、2 并不连续，却可以按 10、30、20 的顺序访问：链表用链接指出下一项。本页先用同一个小链表看懂共同模型、三种形态和实现选择，再进入子页学习完整操作。</p><div id="本课先记住"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>链表的先后顺序由链接决定，不由结点所在的地址或数组下标决定；改链接前要先保存仍需访问的位置。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>从 head 出发，沿当前结点的 next 前进；普通链遇到各自的空标记停止，循环链再次回到 head 停止。</p><pre tabindex="0"><code>current = current-&gt;next;\ncurrent = nextIndex[current];</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>head=1，位置链 1→3→2→0，对应数据顺序 10→30→20；这里的 0 只是数组模拟约定的空下标。</p><pre tabindex="0"><code>head = 1;\nnextIndex[1] = 3;\nnextIndex[3] = 2;\nnextIndex[2] = 0;</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>把下标加一当遍历、循环链仍等待空标记、双向只改一侧，或先覆盖旧后继，都会导致漏结点、无法停止或断链。</li></ul></dd></div></dl></section></div><h2 id="知识范围与官方定位" class="astro-c5eeuzv2">知识范围与官方定位</h2><p class="astro-c5eeuzv2">五级需要认识结点的数据域、链接域、head 与 tail，理解单向、双向、循环链表以及数组下标模拟，并能在子页完成创建、遍历、插入和删除。父页只建立共同模型、识别方法和选择路线；完整 C++ 写法、逐轮操作和容量管理留在两个子页。</p><p>本页先建立共同基础并比较各方法；完整过程、C++ 写法与复杂度请进入对应子知识点学习。</p><section aria-labelledby="共同基础"><h2 id="共同基础" class="astro-c5eeuzv2">共同基础</h2><ul><li class="astro-c5eeuzv2">K1｜一个结点由数据域和链接域组成：数据域保存内容，链接域保存下一结点的位置。head 是进入链表的入口，不是额外的数据结点；tail 表示尾部位置。</li><li class="astro-c5eeuzv2">K1｜统一状态 S1：head=1；位置 1 保存 10、next=3，位置 3 保存 30、next=2，位置 2 保存 20、next=0。位置虽然不连续，链接给出的逻辑顺序仍是 10→30→20。</li><li class="astro-c5eeuzv2">K1｜普通数组模拟链的观察过程：current=1，非空，访问 10 后到 3；current=3，访问 30 后到 2；current=2，访问 20 后到 0；current=0 是条件第一次不成立的停止行，不访问 value[0]。</li><li class="astro-c5eeuzv2">K1｜空标记随实现而变：指针链表常用 nullptr，数组模拟可约定 0。0 不是所有链表通用的空值；空链必须先判断 head，不能先访问结点。</li><li class="astro-c5eeuzv2">K2｜单向链只有 next，适合从头向后走，普通链遇空标记停止；双向链再有 prev，适合前后移动；循环链让尾部 next 回到 head，适合轮转。三者都从入口沿链接访问。</li><li class="astro-c5eeuzv2">K2｜在 S1 上识读双向链，只需补上 prev[1]=0、prev[3]=1、prev[2]=3，反向应得到 2→3→1→0；识读循环链，只把 next[2] 从 0 改为 head(1)，访问 1、3、2 后再次到 1 时停止。</li><li class="astro-c5eeuzv2">K2｜只需从头向后访问时选单向；需要从当前位置回到前一个结点时选双向；需要尾部之后自动回到开头时选循环。循环链应先处理空表，再以“再次到 head”为一整轮的结束。</li><li class="astro-c5eeuzv2">K3｜修改链接前先保存仍要使用的位置。例如从 1→2→0 形成 S1：先保存 oldNext=2，再令位置 3 的 next 指向 oldNext，最后才让位置 1 的 next 指向 3；每一步都不能让位置 2 从 head 不可达。</li><li class="astro-c5eeuzv2">K3｜双向链把 x 放在 a、b 之间时，必须让 x 的 prev/next 记住两侧，也让 a 的 next 与 b 的 prev 接回 x。中间状态还不是完成状态，最后要从前后两个方向检查相邻关系。</li><li class="astro-c5eeuzv2">K3｜边界检查：空链的 head、tail 都为空；单结点的 head=tail；头部没有前驱，尾部没有后继；删掉唯一结点后两者都要为空。普通链尾部指向空标记，循环链尾部指回 head。</li><li class="astro-c5eeuzv2">选择演示｜题目给出“最多 100 个结点、结点用整数编号、nextIndex 保存后继、0 是空下标”，因此应选数组模拟。对 S1 从 head=1 沿链接复核得到 1→3→2→0，而不是用 current++；只有题目要求动态结点、地址链接和释放时，才转到指针实现。</li><li class="astro-c5eeuzv2">空间口径｜两种实现都使用 O(n) 总存储。若 value、nextIndex、previousIndex 都是 int，槽位为 0..N，且常见环境中 int=4 字节，三个数组约为 12(N+1) 字节；平台不同时以 sizeof(int) 为准。指针结点大小受指针宽度和对齐影响，只能以 sizeof(Node) 为准。</li><li class="astro-c5eeuzv2">引导练习｜从 S1 写出 current 的变化并指出何时不再访问数据。答案：1→3→2→0，在 current=0 的首次失败判断停止。成功标准：只访问 1、3、2，且没有漏掉停止行。</li><li class="astro-c5eeuzv2">独立练习｜在 S1 的位置 3 后插入位置 4。答案：先保存 oldNext=next[3]=2，再令 next[4]=2，最后令 next[3]=4，结果为 1→3→4→2→0。成功标准：从 head 到达四个位置各一次，尾部仍到 0。</li><li class="astro-c5eeuzv2">迁移练习｜值班名单要从最后一人回到第一人。答案：选循环链，把 next[2] 改为 head(1)；空表先停，非空表访问 1、3、2 后在下一位置再次等于 head 时停。成功标准：一轮中每个结点恰访问一次。</li></ul></section><section aria-labelledby="方法对比"><h2 id="方法对比" class="astro-c5eeuzv2">方法对比</h2><div><section aria-labelledby="linked-list-structures-comparison-0" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-0" class="astro-c5eeuzv2">指针实现链表</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>题目需要动态创建结点，并用真实地址表示链接。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>进入指针子页学习 Node、nullptr、创建、遍历、插删、反转与释放；结点大小以 sizeof(Node) 为准。</p></section><section aria-labelledby="linked-list-structures-comparison-1" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-1" class="astro-c5eeuzv2">数组模拟链表</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>题目给出固定上限和整数编号，适合用 value、nextIndex、previousIndex 表示数据与链接。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>进入数组子页学习容量和下标管理；本章约定 0 为空下标，有效结点从 1 开始。</p></section><section aria-labelledby="linked-list-structures-comparison-2" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-2" class="astro-c5eeuzv2">纠错：把链表当连续数组</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>现象：在 S1 中写 current++ 会访问 1、2，漏掉位置 3 且顺序错误；原因：编号不表示后继关系。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>修正为读取当前结点的 next 或 nextIndex；复验应恰好得到 1→3→2，并在 0 的首次失败判断停止。</p></section><section aria-labelledby="linked-list-structures-comparison-3" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-3" class="astro-c5eeuzv2">纠错：循环链等待空标记</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>现象：访问 1、3、2 后又回到 1，一直重复；原因：尾链接回 head，链中不会出现 nullptr 或 0。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>先判断空表；非空表完整走一圈后，在 current 再次等于 head 时停止。复验一轮只访问 1、3、2 各一次。</p></section><section aria-labelledby="linked-list-structures-comparison-4" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-4" class="astro-c5eeuzv2">纠错：双向链只改一侧</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>现象：正向 1→3→2 看似正确，反向却不能从 2 回到 3；原因：next 与 prev 没有同步。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>让新结点记住前后两侧，并让两侧接回新结点；复验时分别正向、反向检查相邻结点是否互相指回。</p></section><section aria-labelledby="linked-list-structures-comparison-5" class="astro-c5eeuzv2"><h3 id="linked-list-structures-comparison-5" class="astro-c5eeuzv2">纠错：覆盖旧后继</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>现象：接入位置 3 后，位置 2 从 head 再也到不了；原因：把 next[1] 改成 3 前没有保存原来的 2。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>先保存 oldNext，再连接新结点，最后改前驱链接；复验从 head 应到达 1、3、2 各一次，并满足当前链表的终止规则。</p></section></div></section><section aria-labelledby="选择子知识点"><h2 id="选择子知识点" class="astro-c5eeuzv2">选择子知识点</h2><p class="astro-c5eeuzv2">根据当前要解决的问题进入对应页面；每个子知识点都可以直接访问，不设置完成门槛。</p><ul><li id="05-链表的插入删除与反转" class="astro-c5eeuzv2"><a href="/gesp/level5/linked-list-structures/pointer-implementation/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">指针实现链表</strong><small class="astro-c5eeuzv2">先学：用真实结点和指针安全完成创建、遍历、插删、反转与释放，并检查空指针和头尾边界。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/linked-list-structures/array-simulation/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">数组模拟链表</strong><small class="astro-c5eeuzv2">再学：用 value、nextIndex、previousIndex 和 0 空下标维护链接，并检查固定容量与有效下标。</small></span><span class="astro-c5eeuzv2">→</span></a></li></ul></section><h2 id="核心内容检查" class="astro-c5eeuzv2">核心内容检查</h2><h2 id="整页综合测试" class="astro-c5eeuzv2">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></article></div><footer',
        '<article id="top" data-level5-topic-hub="binary-search"><p>二分查找利用有序性每次排除约一半候选范围，适合在有序序列中快速找值或找边界。</p><div id="本课先记住"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>二分查找依靠有序性安全排除整段候选；先看清题目要任意命中还是左右边界，再选择对应模型。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>共同模型先固定区间含义，再用安全中点比较；下面是闭区间存在性查找的循环骨架。</p><pre tabindex="0"><code>while (left &lt;= right) {\n    int middle = left + (right - left) / 2;\n}</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>在 1、3、5、7、9、11 中查找 7，先比较中点值 5，就能排除下标 0 到 2。</p><pre tabindex="0"><code>[0, 5] -&gt; middle = 2, value = 5\n5 &lt; 7 -&gt; left = 3</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>无序数组不能直接二分</li><li>闭区间与半开区间不能混用</li><li>更新时不排除 middle 可能死循环</li><li>任意命中不等于第一个或最后一个命中。</li></ul></dd></div></dl></section></div><h2 id="知识范围与官方定位" class="astro-c5eeuzv2">知识范围与官方定位</h2><p class="astro-c5eeuzv2">五级需要掌握二分查找的适用前提、区间含义、循环条件和边界更新，并能分别处理精确查找与四种常见边界：第一个 ≥、第一个 &gt;、最后一个 ≤、最后一个 &lt;。</p><p>本页先建立共同基础并比较各方法；完整过程、C++ 写法与复杂度请进入对应子知识点学习。</p><section aria-labelledby="问题和范围"><h2 id="问题和范围" class="astro-c5eeuzv2">问题和范围</h2><div><section aria-labelledby="binary-search-problem" class="astro-c5eeuzv2"><h3 id="binary-search-problem" class="astro-c5eeuzv2">要解决的问题</h3><p class="astro-c5eeuzv2">二分查找是在有序序列中反复缩小候选范围的方法。它不是一上来就背模板，而是利用一次比较判断哪一整段已经不可能含有答案。</p><p class="astro-c5eeuzv2">看同一个非递减数组 1、3、5、7、9、11：问题可以问“7 是否存在”，也可以问“第一个不小于 6 的位置”，还可以问“最后一个不大于 8 的位置”。三个问题都利用有序性，却需要选择不同的查找目标。</p><p class="astro-c5eeuzv2">本页先建立这些任务共用的思考方式，并用“7 是否存在”完成一次最小实现。精确位置和四种边界的完整写法分别留在两个子知识点中。</p></section><section aria-labelledby="binary-search-scope" class="astro-c5eeuzv2"><h3 id="binary-search-scope" class="astro-c5eeuzv2">本课范围</h3><ul class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">本教学程序支持 0 ≤ n ≤ 100000；数组按非递减顺序排列，数组值与目标值都在 -1000000000 到 1000000000 之间。程序不会自动排序输入。</li><li class="astro-c5eeuzv2">数组使用 0 下标和 numbers[100000]。n = 100000 时最大合法下标是 99999；n = 0 时 right = n - 1 等于 -1，循环不会访问数组。</li><li class="astro-c5eeuzv2">right - left 在循环中最大为 99999，left、right、middle 和数组值使用 int 都安全。程序只输出 YES 或 NO 加换行，最多 4 个字符。</li><li class="astro-c5eeuzv2">父页只讲有序性、候选区间、严格缩小和模型选择；精确查找子页负责任意命中或 -1 的闭区间细节，边界查找子页负责半开区间、四种边界与出现次数。</li></ul></section></div></section><section aria-labelledby="共同基础"><h2 id="共同基础" class="astro-c5eeuzv2">共同基础</h2><ul><li class="astro-c5eeuzv2">序列必须有序，才能根据中点大小安全排除一侧。</li><li class="astro-c5eeuzv2">开始前先确定搜索区间是闭区间还是半开区间，循环条件必须与它一致。</li><li class="astro-c5eeuzv2">中点写成 left + (right - left) / 2，可以避免 left + right 的加法溢出。</li><li class="astro-c5eeuzv2">每轮都必须严格缩小区间，否则循环可能卡住。</li></ul></section><section aria-labelledby="方法对比"><h2 id="方法对比" class="astro-c5eeuzv2">方法对比</h2><div><section aria-labelledby="binary-search-comparison-0" class="astro-c5eeuzv2"><h3 id="binary-search-comparison-0" class="astro-c5eeuzv2">精确查找</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>寻找任意一个等于目标值的位置。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>找到就返回；找不到返回 -1。</p></section><section aria-labelledby="binary-search-comparison-1" class="astro-c5eeuzv2"><h3 id="binary-search-comparison-1" class="astro-c5eeuzv2">左边界</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>寻找第一个 ≥x 或第一个 &gt;x 的位置。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>遇到满足条件的位置仍向左收缩。</p></section><section aria-labelledby="binary-search-comparison-2" class="astro-c5eeuzv2"><h3 id="binary-search-comparison-2" class="astro-c5eeuzv2">右边界</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>寻找最后一个 ≤x 或最后一个 &lt;x 的位置。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>遇到满足条件的位置仍向右收缩。</p></section></div></section><section aria-labelledby="算法怎样形成"><h2 id="算法怎样形成" class="astro-c5eeuzv2">算法怎样形成</h2><p class="astro-c5eeuzv2">先在数组 1、3、5、7、9、11 中判断 7 是否存在。顺序查找从左到右比较，一次只能排除一个位置；现在要利用“越往右数值越大”这个条件，一次排除一整段。</p><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">有序性带来整段信息：第一次查看下标 2，值是 5。因为 5 &lt; 7，而且它左边的数都不大于 5，所以目标不可能在下标 0 到 2，整个左段都可以排除。</li><li class="astro-c5eeuzv2">为什么查看中间：如果总看最左端，一次仍可能只排除一个位置。查看当前区间的中点，会让中点左右两侧尽量接近；无论排除哪一边，留下的候选通常不超过原来的一半。</li><li class="astro-c5eeuzv2">共同不变量：每轮开始时，若目标存在，它一定在当前候选区间内。比较中点后，只能排除已经由有序性证明不可能的部分，不能随意丢掉仍可能有答案的位置。</li><li class="astro-c5eeuzv2">两种大小更新：中点值小于目标时，中点及其左侧都偏小，令 left = middle + 1；中点值大于目标时，中点及其右侧都偏大，令 right = middle - 1。两次更新都把已经检查的 middle 排除。</li><li class="astro-c5eeuzv2">命中与停止：中点值等于目标时，存在性问题立即得到 true；若更新后 left &gt; right，候选区间为空，得到 false。每轮至少排除 middle，所以区间一定严格缩小并最终停止。</li><li class="astro-c5eeuzv2">无序反例：[7, 1, 3] 中查找 7，若只因中点值 1 &lt; 7 就排除中点左侧，会把真正的 7 一起丢掉。没有有序前提，整段排除就没有依据。</li><li class="astro-c5eeuzv2">先选模型：题目只问是否存在或任意一个命中位置，进入精确查找；问第一个满足条件的位置，选择左边界；问最后一个满足条件的位置，选择右边界。重复值中“任意一个”不能冒充“第一个或最后一个”。</li></ol></section><section aria-labelledby="自然语言步骤"><h2 id="自然语言步骤" class="astro-c5eeuzv2">自然语言步骤</h2><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">先确认数组已经按非递减顺序排列；若没有这个前提，不能根据一次比较排除整段。</li><li class="astro-c5eeuzv2">读清题目要什么：只判断存在或找任意命中，选择精确查找；找第一个满足条件，选择左边界；找最后一个满足条件，选择右边界。</li><li class="astro-c5eeuzv2">选定模型后固定区间含义。本页最小程序使用闭区间 [left, right]，初始 left = 0、right = n - 1；两个端点都可能是答案。</li><li class="astro-c5eeuzv2">候选区间非空时，用 middle = left + (right - left) / 2 计算中点，避免先把左右端点相加。</li><li class="astro-c5eeuzv2">比较 numbers[middle] 与 target。相等就返回 true；中点值偏小就更新 left = middle + 1；偏大就更新 right = middle - 1。</li><li class="astro-c5eeuzv2">每次更新都排除已经检查过的 middle。继续处理新的候选区间，直到命中，或 left &gt; right 表示区间为空。</li><li class="astro-c5eeuzv2">main() 根据返回值输出 YES 或 NO。若题目要求具体位置，再进入对应子知识点学习它自己的返回值和边界规则。</li></ol></section><section aria-labelledby="执行账本"><h2 id="执行账本" class="astro-c5eeuzv2">执行账本</h2><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">主样例的完整三轮区间变化与输出</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">步骤</th><th scope="col" class="astro-c5eeuzv2">当前闭区间</th><th scope="col" class="astro-c5eeuzv2">middle</th><th scope="col" class="astro-c5eeuzv2">中点值</th><th scope="col" class="astro-c5eeuzv2">判断依据</th><th scope="col" class="astro-c5eeuzv2">更新后状态</th><th scope="col" class="astro-c5eeuzv2">下一步</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">第 1 轮</td><td class="astro-c5eeuzv2">[0, 5]</td><td class="astro-c5eeuzv2">2</td><td class="astro-c5eeuzv2">5</td><td class="astro-c5eeuzv2">5 &lt; 7；有序数组中下标 0 到 2 的值都不可能等于 7</td><td class="astro-c5eeuzv2">left = 3，新区间 [3, 5]</td><td class="astro-c5eeuzv2">继续比较新区间的中点</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">第 2 轮</td><td class="astro-c5eeuzv2">[3, 5]</td><td class="astro-c5eeuzv2">4</td><td class="astro-c5eeuzv2">9</td><td class="astro-c5eeuzv2">9 &gt; 7；有序数组中下标 4 到 5 的值都不可能等于 7</td><td class="astro-c5eeuzv2">right = 3，新区间 [3, 3]</td><td class="astro-c5eeuzv2">继续检查唯一候选</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">第 3 轮</td><td class="astro-c5eeuzv2">[3, 3]</td><td class="astro-c5eeuzv2">3</td><td class="astro-c5eeuzv2">7</td><td class="astro-c5eeuzv2">7 == 7，目标存在</td><td class="astro-c5eeuzv2">立即返回 true；不再产生新区间</td><td class="astro-c5eeuzv2">回到 main()</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">输出</td><td class="astro-c5eeuzv2">containsTarget 已返回</td><td class="astro-c5eeuzv2">—</td><td class="astro-c5eeuzv2">—</td><td class="astro-c5eeuzv2">返回值是 true</td><td class="astro-c5eeuzv2">main() 输出 YES</td><td class="astro-c5eeuzv2">程序结束</td></tr></tbody></table></div><p><strong class="astro-c5eeuzv2">追踪结论：</strong>三轮都与同一闭区间不变量一致：只排除已经证明不可能的一段。第三轮在相等分支立即返回，程序不会虚构第四轮搜索。</p></section><section aria-labelledby="完整程序"><h2 id="完整程序" class="astro-c5eeuzv2">完整程序</h2><h3 class="astro-c5eeuzv2">共同模型的最小完整示例：判断目标是否存在</h3><p class="astro-c5eeuzv2">下面是本页唯一一份完整 C++14 程序，只演示二分家族共同的有序、取中、排除和停止机制，输出 YES 或 NO；它不覆盖四种边界的完整实现。</p><div><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输入</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">6\n1 3 5 7 9 11\n7</code></pre><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输出</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">YES</code></pre></div><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">样例说明：</strong>先由中点值 5 排除左段，再由中点值 9 排除右段，最后在下标 3 找到 7，因此输出 YES。</p><pre tabindex="0"><code class="language-cpp">#include &lt;bits/stdc++.h&gt;\nusing namespace std;\n\nconst int MAX_N = 100000;\nint numbers[MAX_N];\n\nbool containsTarget(int n, int target) {\n  int left = 0;\n  int right = n - 1;\n\n  while (left &lt;= right) {\n      int middle = left + (right - left) / 2;\n\n      if (numbers[middle] == target) {\n          return true;\n      }\n      if (numbers[middle] &lt; target) {\n          left = middle + 1;\n      } else {\n          right = middle - 1;\n      }\n  }\n\n  return false;\n}\n\nint main() {\n  int n;\n  cin &gt;&gt; n;\n\n  for (int i = 0; i &lt; n; i++) {\n      cin &gt;&gt; numbers[i];\n  }\n\n  int target;\n  cin &gt;&gt; target;\n\n  if (containsTarget(n, target)) {\n      cout &lt;&lt; &quot;YES\\n&quot;;\n  } else {\n      cout &lt;&lt; &quot;NO\\n&quot;;\n  }\n\n  return 0;\n}\n</code></pre></section><section aria-labelledby="复杂度"><h2 id="复杂度" class="astro-c5eeuzv2">复杂度</h2><p class="astro-c5eeuzv2">二分查找阶段每轮把候选范围缩小约一半，所以查找时间是 O(log n)。完整程序还要先读入 n 个数，因此包含输入时的总时间是 O(n)；这不改变查找阶段的复杂度。</p><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">时间与空间复杂度</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">项目</th><th scope="col" class="astro-c5eeuzv2">复杂度</th><th scope="col" class="astro-c5eeuzv2">说明</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">查找阶段时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(log n)</code></td><td class="astro-c5eeuzv2">每轮至少排除中点，并通常只留下原候选的一半左右。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">完整程序总时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">读入 n 个数组元素需要 O(n)，之后再进行 O(log n) 查找。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">迭代额外空间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(1)</code></td><td class="astro-c5eeuzv2">查找过程只增加 left、right、middle 等常数个变量。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">输入数组存储</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">numbers 保存 n 个输入；常见 int 为 4 字节时，100000 个元素约占 400000 字节，实际以平台为准。</td></tr></tbody></table></div></section><section aria-labelledby="常见错误"><h2 id="常见错误" class="astro-c5eeuzv2">常见错误</h2><div><section aria-labelledby="binary-search-error-0" class="astro-c5eeuzv2"><h3 id="binary-search-error-0" class="astro-c5eeuzv2">没有有序前提就直接排除一段</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是目标明明存在，程序却输出 NO。例如无序数组 [7, 1, 3] 查找 7 时，中点值 1 小于 7，却不能说明左侧都更小。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>先核对题目保证输入非递减，再使用二分；本程序不自动排序。修正后用一个有序命中样例和一个有序未命中样例重新运行。</p></section><section aria-labelledby="binary-search-error-1" class="astro-c5eeuzv2"><h3 id="binary-search-error-1" class="astro-c5eeuzv2">混用闭区间与半开区间</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">int right = n;\nwhile (left &lt;= right) {</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>right = n 属于半开区间边界，却配上闭区间循环，可能访问 numbers[n] 越界；另一种混用还可能漏掉最后一个候选。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>本程序固定闭区间：right = n - 1，循环写 left &lt;= right。修正后重测 n = 0，以及单元素命中和未命中。</p></section><section aria-labelledby="binary-search-error-2" class="astro-c5eeuzv2"><h3 id="binary-search-error-2" class="astro-c5eeuzv2">更新后没有排除 middle</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">left = middle;</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>可观察的症状是程序在某个单元素区间反复计算同一个 middle。例如 [3, 3] 中若仍令 left = middle，区间完全没有缩小。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>中点偏小时写 left = middle + 1，偏大时写 right = middle - 1。修正后用目标 8 查找主数组，确认程序能以 NO 停止。</p></section><section aria-labelledby="binary-search-error-3" class="astro-c5eeuzv2"><h3 id="binary-search-error-3" class="astro-c5eeuzv2">把任意命中当成第一个或最后一个</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>在 [1, 2, 2, 2, 5] 中，存在性查找只保证命中某个 2；若题目要第一个 2，直接返回任意中点就可能答错位置。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>先重读题目问法：任意命中进入精确查找；第一个或最后一个满足条件进入边界查找。用重复值样例复核所选模型。</p></section><section aria-labelledby="binary-search-error-4" class="astro-c5eeuzv2"><h3 id="binary-search-error-4" class="astro-c5eeuzv2">把不存在位置直接当数组下标</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">cout &lt;&lt; numbers[index] &lt;&lt; &#39;\\n&#39;;</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>精确位置可能用 -1 表示不存在，边界位置也可能等于 n；直接访问会越界。本页 YES/NO 程序没有这个访问动作，但进入子页后必须检查。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>访问数组前先验证 0 &lt;= index &amp;&amp; index &lt; n。分别用目标小于全部和大于全部的样例，确认不存在位置不会被访问。</p></section><section aria-labelledby="binary-search-error-5" class="astro-c5eeuzv2"><h3 id="binary-search-error-5" class="astro-c5eeuzv2">把额外空间 O(1) 说成整个程序只占 O(1)</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>left、right、middle 确实只有常数个，但 numbers 要保存 n 个输入；n 增大时数组占用也会增大。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>分别描述“迭代查找额外空间 O(1)”和“输入数组存储 O(n)”。令 n = 100000，重新核对数组有 100000 个 int，而不是常数个元素。</p></section></div></section><section aria-labelledby="练习与自检"><h2 id="练习与自检" class="astro-c5eeuzv2">练习与自检</h2><div><section aria-labelledby="binary-search-practice-0" class="astro-c5eeuzv2"><h3 id="binary-search-practice-0" class="astro-c5eeuzv2">练习一：先选对模型</h3><p class="astro-c5eeuzv2">数组中可能有重复值。以下三问分别应进入哪类模型：① x 是否存在；② 第一个大于等于 x 的位置；③ 最后一个小于 x 的位置？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>先判断题目要任意命中、第一个满足，还是最后一个满足。</p></section><section aria-labelledby="binary-search-practice-1" class="astro-c5eeuzv2"><h3 id="binary-search-practice-1" class="astro-c5eeuzv2">练习二：只更新一轮</h3><p class="astro-c5eeuzv2">在主数组中查找 7。初始闭区间是 [0, 5]，middle = 2，numbers[2] = 5。写出本轮被排除的下标和新区间。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>5 小于 7；利用非递减顺序判断中点左侧是否还可能等于 7。</p></section><section aria-labelledby="binary-search-practice-2" class="astro-c5eeuzv2"><h3 id="binary-search-practice-2" class="astro-c5eeuzv2">练习三：找出不停止的原因</h3><p class="astro-c5eeuzv2">闭区间已经缩到 [3, 3]，中点值仍小于目标。如果错误写成 left = middle，下一轮区间是什么？为什么程序会卡住？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>比较更新前后的 left、right 和 middle 是否发生变化。</p></section><section aria-labelledby="binary-search-practice-3" class="astro-c5eeuzv2"><h3 id="binary-search-practice-3" class="astro-c5eeuzv2">练习四：出现次数去哪里学</h3><p class="astro-c5eeuzv2">题目要求统计 x 在有序数组中出现几次。仅判断 YES/NO 是否够用？接下来应进入哪个子知识点？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>出现次数需要找到相等区间的左右分界。</p></section></div><div><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习一答案</summary><p class="astro-c5eeuzv2">① 只问是否存在，选择精确查找；② 问第一个满足条件，选择左边界；③ 问最后一个满足条件，选择右边界。后两类都进入边界查找子页。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习二答案</summary><p class="astro-c5eeuzv2">下标 0 到 2 的值都不大于 5，因此都不可能等于 7；令 left = middle + 1 = 3，新区间是 [3, 5]。</p><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">left = middle + 1;</code></pre></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习三答案</summary><p class="astro-c5eeuzv2">middle 仍是 3，错误更新后 left 也仍是 3，所以区间还是 [3, 3]。下一轮会重复同样判断，无法停止；正确更新要排除中点。</p><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">left = middle + 1;</code></pre></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习四答案</summary><p class="astro-c5eeuzv2">YES/NO 只能说明是否存在，不能给出相等元素覆盖的范围。应进入“边界查找”子知识点，学习左右边界后再计算出现次数。</p></details></div></section><section aria-labelledby="下一步"><h2 id="下一步" class="astro-c5eeuzv2">下一步</h2><p class="astro-c5eeuzv2">共同模型已经建立。请先进入“精确查找”，把闭区间、任意命中和不存在位置完整学会；再进入“边界查找”，学习第一个、最后一个满足条件的位置与出现次数。两个子页都可直接访问，不设置完成门槛。</p></section><section aria-labelledby="选择子知识点"><h2 id="选择子知识点" class="astro-c5eeuzv2">选择子知识点</h2><p class="astro-c5eeuzv2">根据当前要解决的问题进入对应页面；每个子知识点都可以直接访问，不设置完成门槛。</p><ul><li class="astro-c5eeuzv2"><a href="/gesp/level5/binary-search/exact-search/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">精确查找</strong><small class="astro-c5eeuzv2">用闭区间手写查找，正确处理 ±1 和不存在。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/binary-search/boundary-search/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">边界查找</strong><small class="astro-c5eeuzv2">统一推导四种边界，处理重复值、不存在与插入位置。</small></span><span class="astro-c5eeuzv2">→</span></a></li></ul></section><h2 id="核心内容检查" class="astro-c5eeuzv2">核心内容检查</h2><h2 id="整页综合测试" class="astro-c5eeuzv2">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></article></div><footer',
        '<article id="top"><p>二分答案不是在数组里找某个元素，而是在一段候选答案中寻找可行与不可行的分界。它先用 check 判断一个候选值，再利用真假结果的单调变化排除一半范围。本课用木材切段完整学习“最大可行答案”，同时用一个短对照辨清“最小可行答案”的相反方向。</p><h2 id="知识范围与官方定位" class="astro-2i3ys3wc">知识范围与官方定位</h2><p class="astro-2i3ys3wc">对应五级大纲“二分答案”：理解答案域的单调性与判定函数，能区分最小可行值和最大可行值，并在正整数答案范围内正确处理边界、类型与复杂度。</p><p>本页只使用一级至五级已经学习的语法和能力；小知识点按页内编号连续查询，不设置前置完成门槛。</p><dl><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">是什么</dt><dd class="astro-2i3ys3wc">二分答案把每个可能答案看成一个候选，用 check(x) 标记它可行还是不可行，再寻找真假分界。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">解决什么问题</dt><dd class="astro-2i3ys3wc">当最优数值难以直接计算、逐个尝试又太慢，但一个候选容易判断时，它能快速找到最小可行值或最大可行值。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">什么时候使用</dt><dd class="astro-2i3ys3wc">必须同时具备明确的整数上下界和单调真假序列；“候选是整数”本身不够。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">核心思想</dt><dd class="astro-2i3ys3wc">先从具体数据证明单调性，再建立只负责判定的 check，最后按目标选择真时向左或向右。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">C++ 实现方法</dt><dd class="astro-2i3ys3wc">本课沿用闭区间 left&lt;=right；木材长度候选从 1 到 maxLength，最大可行时 true 记录并向右，false 向左。</dd></div></dl><section aria-labelledby="01-从查找位置到查找答案"><h2 id="01-从查找位置到查找答案" class="astro-2i3ys3wc">01 从查找位置到查找答案</h2><p class="astro-2i3ys3wc">上一页的二分查找在有序数组里找位置，本页继续沿用闭区间、取中点和用 mid±1 排除已检查候选的写法；新问题是怎样在“答案范围”里找到最优数值。</p><p class="astro-2i3ys3wc">主问题：有 4 根长度分别为 8、7、6、5 的木材，要切出至少 7 段等长木材，每段长度必须是正整数，求最大的段长。依次尝试 1、2、3、4 时，最多可切 26、12、7、5 段，所以真假依次为真、真、真、假；最大可行长度是 3。</p><p class="astro-2i3ys3wc">本教学程序支持 1≤n≤100000、1≤length[i]≤10^9、1≤k≤所有木材长度之和。若从 1 一直试到 10^9，每个候选又扫描 n 根木材，会做太多重复检查；我们要先证明真假只有一个分界，才有理由二分。</p><div><h3 class="astro-2i3ys3wc">具体样例与推演</h3><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">场景：</strong>四根木材长度为 8、7、6、5，需要至少 7 段，先逐个检查正整数段长。</p><ol class="astro-2i3ys3wc"><li class="astro-2i3ys3wc">段长 1 时可切 26 段，段长 2 时可切 12 段，段长 3 时可切 7 段，这三个候选都可行。</li><li class="astro-2i3ys3wc">段长 4 时只能切 5 段，不可行；段长继续增大时，每根木材能切出的段数都不会增加。</li><li class="astro-2i3ys3wc">因此候选 1～8 的真假是 true、true、true、false、false、false、false、false，目标是最右侧的 true，也就是 3。</li><li class="astro-2i3ys3wc">若逐个检查最多 10^9 个候选，每次再扫描最多 100000 根木材，重复工作太多；真假只有一个分界才允许每轮排除一半。</li></ol><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">适用结论：</strong>二分答案不是因为“答案是整数”就能使用，而是因为 check 真假具有单调分界；先证明分界，再二分。</p><aside',
        '<article id="top"><p>贪心算法会在每一步先作出一个当前选择，再继续处理剩余部分。关键不在“挑最大的或最小的”，而在于找到能够证明安全的选择标准。本课只围绕活动选择，学习怎样从冲突区间中选出尽量多的活动。</p><h2 id="知识范围与官方定位" class="astro-2i3ys3wc">知识范围与官方定位</h2><p class="astro-2i3ys3wc">对应五级大纲“贪心算法”：理解局部最优与全局最优的区别，能设计并检验贪心策略，并能说明一个局部选择为什么不会破坏整体最优。</p><p>本页只使用一级至五级已经学习的语法和能力；小知识点按页内编号连续查询，不设置前置完成门槛。</p><dl><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">是什么</dt><dd class="astro-2i3ys3wc">贪心算法每一步确定一个不再撤销的选择，再在剩余候选中重复同样动作；每一步都必须有安全依据。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">解决什么问题</dt><dd class="astro-2i3ys3wc">本课用它解决活动选择：从若干半开区间中挑出尽量多的互不重叠活动，只输出最大数量。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">什么时候使用</dt><dd class="astro-2i3ys3wc">只有当选择标准明确，并且能证明当前选择可以换进某个最优方案而不使答案变差时，才使用贪心。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">核心思想</dt><dd class="astro-2i3ys3wc">先按 finish 升序、同 finish 时 start 升序得到完整候选顺序，再用交换论证证明这一步安全，最后在剩余活动中重复选择。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">C++ 实现方法</dt><dd class="astro-2i3ys3wc">用 Activity 数组保存开始、结束时间；比较函数先比较 finish，同 finish 时比较 start，随后顺序扫描，start&gt;=lastEnd 时选择并更新状态。</dd></div></dl><section aria-labelledby="01-贪心思想与适用条件"><h2 id="01-贪心思想与适用条件" class="astro-2i3ys3wc">01 贪心思想与适用条件</h2><p class="astro-2i3ys3wc">有 n 个活动，每个活动占用半开区间 [start, finish)。半开区间不包含右端点；本题把“互不重叠”定义为所选活动按执行先后排列后，每个后续活动都满足 start≥前一个活动的 finish，所以 [1,3) 与 [3,5) 可以首尾相接。目标是选择尽量多的活动。</p><p class="astro-2i3ys3wc">本教学程序支持 0≤n≤1000、-10^9≤start≤finish≤10^9，只输出最多活动数，答案在 0 到 n 之间。输入允许 start=finish，这种活动仍按同一先后关系参加判断，不另设特殊规则。数组容量是 1005，n=1000 时实际使用下标 0 到 999，不会越界。</p><p class="astro-2i3ys3wc">主样例按 [6,8)、[2,5)、[1,3)、[4,6) 的乱序输入。若直接沿用输入顺序，先选 [6,8) 会失去前面三个可用活动；若先选“开始最早”的 [1,10)，又会挡住 [2,3)、[3,4)、[4,5)。输入顺序和开始得早都不是可靠依据。真正要保留的是后续可用时间：当前活动结束得越早，留给后面的时间越多。</p><div><h3 class="astro-2i3ys3wc">具体样例与推演</h3><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">场景：</strong>主样例输入顺序为 [6,8)、[2,5)、[1,3)、[4,6)，目标是选择最多个互不重叠活动。</p><ol class="astro-2i3ys3wc"><li class="astro-2i3ys3wc">如果只按输入顺序先选 [6,8)，前面的三个活动都会被浪费，所以输入顺序没有提供可靠信息。</li><li class="astro-2i3ys3wc">再看短反例 [1,10)、[2,3)、[3,4)、[4,5)：若先选开始最早的 [1,10)，只能得到 1 个活动；这条规则也不可靠。</li><li class="astro-2i3ys3wc">把候选按结束时间观察，最早结束的是 [1,3)；选它以后，从时刻 3 起仍有 [4,6)、[6,8) 可以继续选择。</li><li class="astro-2i3ys3wc">较早结束不会占用更多后续时间，由此先得到 finish 从小到大的主顺序；输入允许零长度活动，所以 finish 相同时还必须按 start 从小到大排列，完整顺序缺一不可。</li></ol><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">适用结论：</strong>贪心规则必须来自题目目标与样例观察。本题要最大化活动数，所以每一步优先保留后续时间，而不是优先开始早、持续短或沿用输入顺序。</p><aside',
        '<article id="top" data-level5-topic-hub="divide-conquer-sorting"><p>分治把一个大问题拆成结构相同的小问题，分别解决后再组合结果；归并排序和快速排序展示了两种不同的组合方式。</p><div id="本课先记住"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>分治先把一个大问题分成定义相同、规模更小的子问题，分别解决后再组合子结果；每个子问题都必须严格变小。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>闭区间只剩一个元素时直接返回；右半段从 middle + 1 开始，避免与左半段重叠。</p><pre tabindex="0"><code>if (left == right) {\n    return numbers[left];\n}\nint middle = left + (right - left) / 2;</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>把 [7, -2, 5, 4] 分成左右两段：左段和为 5，右段和为 9，组合后得到 14。</p><pre tabindex="0"><code>[7, -2] -&gt; 5\n[5, 4] -&gt; 9\n5 + 9 -&gt; 14</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>不能漏掉递归出口</li><li>两个子区间不能重叠或漏元素</li><li>区间和要用 long long</li><li>用了分治不表示程序一定更快。</li></ul></dd></div></dl></section></div><h2 id="知识范围与官方定位" class="astro-c5eeuzv2">知识范围与官方定位</h2><p class="astro-c5eeuzv2">五级需要理解分治的分解、解决、合并过程，并掌握归并排序与快速排序的递归终止条件、递归树、划分或合并过程、复杂度及稳定性特点；还要理解归并统计逆序对的基本入口。</p><p>本页先建立共同基础并比较各方法；完整过程、C++ 写法与复杂度请进入对应子知识点学习。</p><section aria-labelledby="问题和范围"><h2 id="问题和范围" class="astro-c5eeuzv2">问题和范围</h2><div><section aria-labelledby="divide-conquer-sorting-problem" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-problem" class="astro-c5eeuzv2">要解决的问题</h3><p class="astro-c5eeuzv2">现在要计算一组整数的总和。用循环从左到右累加当然能做对，但它看不出一个大问题怎样交给同一个函数去解决更小的同类问题。</p><p class="astro-c5eeuzv2">本课先用区间求和看清分治的共同骨架：分解、解决、组合。这个例子故意很简单，重点不是用递归打败循环，而是让每次调用的范围、返回值和组合位置都能看见。</p><p class="astro-c5eeuzv2">归并排序和快速排序的具体合并、划分与完整代码仍放在两个子知识点中；父页只建立它们共同需要的分治思考方式。</p></section><section aria-labelledby="divide-conquer-sorting-scope" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-scope" class="astro-c5eeuzv2">本课范围</h3><ul class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">本教学程序支持 1 ≤ n ≤ 100000，每个整数满足 -1000000000 ≤ numbers[i] ≤ 1000000000。</li><li class="astro-c5eeuzv2">数组使用 0 下标，整个数组对应闭区间 [0, n - 1]；容量为 100005，最大合法下标 99999 不会越界。</li><li class="astro-c5eeuzv2">总和的最大绝对值是 100000 × 1000000000 = 100000000000000，也就是 10^14，超过常见 32 位 int，因此数组元素、子结果和返回值都使用 long long。</li><li class="astro-c5eeuzv2">本课只处理非空区间，不展开空数组；也不在父页重讲归并排序、快速排序和逆序对的具体实现。</li></ul></section></div></section><section aria-labelledby="共同基础"><h2 id="共同基础" class="astro-c5eeuzv2">共同基础</h2><ul><li class="astro-c5eeuzv2">递归函数必须先写清最小问题的终止条件。</li><li class="astro-c5eeuzv2">每次递归调用都要处理严格更小的区间。</li><li class="astro-c5eeuzv2">排序正确性来自子区间处理正确，以及合并或划分规则保持正确。</li><li class="astro-c5eeuzv2">递归树中，归并排序每层处理元素总数都是 n，约有 log₂n 层；快速排序的树高取决于每次划分是否均衡。</li><li class="astro-c5eeuzv2">合并两个有序段时，若左段当前值大于右段当前值，那么左段当前值及其后面的数都与这个右段值构成逆序对，可一次统计。</li><li class="astro-c5eeuzv2">除了比较时间，还要检查临时数组和递归栈占用。</li></ul></section><section aria-labelledby="方法对比"><h2 id="方法对比" class="astro-c5eeuzv2">方法对比</h2><div><section aria-labelledby="divide-conquer-sorting-comparison-0" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-comparison-0" class="astro-c5eeuzv2">归并排序</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>先分别排好左右两段，再合并两个有序区间。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>稳定，时间 O(n log n)，需要 O(n) 临时数组。</p></section><section aria-labelledby="divide-conquer-sorting-comparison-1" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-comparison-1" class="astro-c5eeuzv2">快速排序</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">主要用途：</strong>先按基准划分，再递归处理左右两侧。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">使用提醒：</strong>通常很快但不稳定，最坏时间可能达到 O(n²)。</p></section></div></section><section aria-labelledby="算法怎样形成"><h2 id="算法怎样形成" class="astro-c5eeuzv2">算法怎样形成</h2><p class="astro-c5eeuzv2">先观察主样例 7、-2、5、4。我们给每个调用同一个任务：rangeSum(left, right) 返回闭区间 [left, right] 中所有元素的和。只要子调用仍在完成这件事，得到的答案就能按同一种方式组合。</p><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">分解：最外层区间是 [0, 3]。中点 middle = 1，把它分为 [0, 1] 和 [2, 3]。两段相邻、不重叠，合起来又恰好覆盖 [0, 3]。</li><li class="astro-c5eeuzv2">解决：两个子区间仍然在求“闭区间内所有元素的和”，所以继续调用 rangeSum。遇到 [0, 0] 这样的单元素区间时，不必再分，直接返回这个元素。</li><li class="astro-c5eeuzv2">组合：[0, 1] 返回 7 + (-2) = 5，[2, 3] 返回 5 + 4 = 9。原区间的每个元素恰好属于其中一段，所以 5 + 9 = 14 就是 [0, 3] 的答案。</li><li class="astro-c5eeuzv2">严格缩小：当 left &lt; right 时，middle 一定满足 left ≤ middle &lt; right。因此 [left, middle] 和 [middle + 1, right] 都比原区间短，递归最终一定到达 left == right。</li><li class="astro-c5eeuzv2">适用判断：只有当大问题能拆成定义相同的小问题，而且子结果能可靠地组合成原答案时，分治才提供了清楚的结构。若拆分后仍要重复完成同样的大量工作，就不能只因“用了递归”断言它更快。</li></ol></section><section aria-labelledby="自然语言步骤"><h2 id="自然语言步骤" class="astro-c5eeuzv2">自然语言步骤</h2><ol class="astro-c5eeuzv2"><li class="astro-c5eeuzv2">读入 n 个整数，保存在 numbers[0] 到 numbers[n - 1]，然后调用 rangeSum(0, n - 1)。</li><li class="astro-c5eeuzv2">进入 rangeSum(left, right) 后，先检查 left == right。若成立，当前区间只有一个元素，直接返回 numbers[left]。</li><li class="astro-c5eeuzv2">若区间中至少有两个元素，计算 middle = left + (right - left) / 2。</li><li class="astro-c5eeuzv2">先调用 rangeSum(left, middle)，把返回值保存为 leftSum。</li><li class="astro-c5eeuzv2">再调用 rangeSum(middle + 1, right)，把返回值保存为 rightSum。</li><li class="astro-c5eeuzv2">返回 leftSum + rightSum。这个返回值就是当前闭区间的元素和。</li><li class="astro-c5eeuzv2">最外层调用返回后，main() 输出得到的总和，程序结束。</li></ol></section><section aria-labelledby="执行账本"><h2 id="执行账本" class="astro-c5eeuzv2">执行账本</h2><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">主样例的完整 14 个调用与返回事件</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">事件</th><th scope="col" class="astro-c5eeuzv2">当前调用</th><th scope="col" class="astro-c5eeuzv2">判断与真实动作</th><th scope="col" class="astro-c5eeuzv2">返回值与下一步</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">1 CALL</td><td class="astro-c5eeuzv2">rangeSum(0, 3)</td><td class="astro-c5eeuzv2">不是单元素；middle = 1，先调用左段 [0, 1]</td><td class="astro-c5eeuzv2">等待左段返回</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">2 CALL</td><td class="astro-c5eeuzv2">rangeSum(0, 1)</td><td class="astro-c5eeuzv2">不是单元素；middle = 0，先调用左段 [0, 0]</td><td class="astro-c5eeuzv2">等待左段返回</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">3 CALL</td><td class="astro-c5eeuzv2">rangeSum(0, 0)</td><td class="astro-c5eeuzv2">left == right，读 numbers[0] = 7</td><td class="astro-c5eeuzv2">准备返回 7</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">4 RETURN</td><td class="astro-c5eeuzv2">rangeSum(0, 0)</td><td class="astro-c5eeuzv2">返回 7</td><td class="astro-c5eeuzv2">回到 [0, 1]，leftSum = 7，接着调用 [1, 1]</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">5 CALL</td><td class="astro-c5eeuzv2">rangeSum(1, 1)</td><td class="astro-c5eeuzv2">left == right，读 numbers[1] = -2</td><td class="astro-c5eeuzv2">准备返回 -2</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">6 RETURN</td><td class="astro-c5eeuzv2">rangeSum(1, 1)</td><td class="astro-c5eeuzv2">返回 -2</td><td class="astro-c5eeuzv2">回到 [0, 1]，rightSum = -2</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">7 RETURN</td><td class="astro-c5eeuzv2">rangeSum(0, 1)</td><td class="astro-c5eeuzv2">组合 7 + (-2) = 5，返回 5</td><td class="astro-c5eeuzv2">回到 [0, 3]，leftSum = 5，接着调用 [2, 3]</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">8 CALL</td><td class="astro-c5eeuzv2">rangeSum(2, 3)</td><td class="astro-c5eeuzv2">不是单元素；middle = 2，先调用左段 [2, 2]</td><td class="astro-c5eeuzv2">等待左段返回</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">9 CALL</td><td class="astro-c5eeuzv2">rangeSum(2, 2)</td><td class="astro-c5eeuzv2">left == right，读 numbers[2] = 5</td><td class="astro-c5eeuzv2">准备返回 5</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">10 RETURN</td><td class="astro-c5eeuzv2">rangeSum(2, 2)</td><td class="astro-c5eeuzv2">返回 5</td><td class="astro-c5eeuzv2">回到 [2, 3]，leftSum = 5，接着调用 [3, 3]</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">11 CALL</td><td class="astro-c5eeuzv2">rangeSum(3, 3)</td><td class="astro-c5eeuzv2">left == right，读 numbers[3] = 4</td><td class="astro-c5eeuzv2">准备返回 4</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">12 RETURN</td><td class="astro-c5eeuzv2">rangeSum(3, 3)</td><td class="astro-c5eeuzv2">返回 4</td><td class="astro-c5eeuzv2">回到 [2, 3]，rightSum = 4</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">13 RETURN</td><td class="astro-c5eeuzv2">rangeSum(2, 3)</td><td class="astro-c5eeuzv2">组合 5 + 4 = 9，返回 9</td><td class="astro-c5eeuzv2">回到 [0, 3]，rightSum = 9</td></tr><tr class="astro-c5eeuzv2"><td class="astro-c5eeuzv2">14 RETURN</td><td class="astro-c5eeuzv2">rangeSum(0, 3)</td><td class="astro-c5eeuzv2">组合 5 + 9 = 14，返回 14</td><td class="astro-c5eeuzv2">回到 main()，输出 14 后结束</td></tr></tbody></table></div><p><strong class="astro-c5eeuzv2">追踪结论：</strong>程序真实发生了 7 次 CALL 和 7 次 RETURN。叶子先给出单个元素，内部区间必须等两个子结果都返回后才能组合；最外层返回 14 时，答案才形成。</p></section><section aria-labelledby="完整程序"><h2 id="完整程序" class="astro-c5eeuzv2">完整程序</h2><h3 class="astro-c5eeuzv2">用分治计算整个数组的和</h3><p class="astro-c5eeuzv2">下面是本页唯一一份完整程序。它只使用普通全局数组和已经解释过的闭区间递归；D1 保证 n 至少为 1，因此初始调用 [0, n - 1] 一定非空。</p><div><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输入</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">4\n7 -2 5 4</code></pre><div class="astro-c5eeuzv2"><h4 class="astro-c5eeuzv2">示例输出</h4><pre tabindex="0" class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">14</code></pre></div><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">样例说明：</strong>rangeSum(0, 1) 得到 5，rangeSum(2, 3) 得到 9；最外层把两个子结果相加，输出 14。</p><pre tabindex="0"><code class="language-cpp">#include &lt;bits/stdc++.h&gt;\nusing namespace std;\n\nconst int MAX_N = 100005;\n\nlong long numbers[MAX_N];\n\nlong long rangeSum(int left, int right) {\n  if (left == right) {\n      return numbers[left];\n  }\n\n  int middle = left + (right - left) / 2;\n  long long leftSum = rangeSum(left, middle);\n  long long rightSum = rangeSum(middle + 1, right);\n  return leftSum + rightSum;\n}\n\nint main() {\n  int n;\n  cin &gt;&gt; n;\n\n  for (int i = 0; i &lt; n; i++) {\n      cin &gt;&gt; numbers[i];\n  }\n\n  cout &lt;&lt; rangeSum(0, n - 1) &lt;&lt; &#39;\\n&#39;;\n  return 0;\n}\n</code></pre></section><section aria-labelledby="复杂度"><h2 id="复杂度" class="astro-c5eeuzv2">复杂度</h2><p class="astro-c5eeuzv2">这份区间求和程序会到达每个元素一次。分治在这里帮助我们组织问题，却没有减少必须读取的元素，因此它和普通循环求和的时间复杂度都是 O(n)；递归调用还会带来额外开销，分治不自动更快。</p><div tabindex="0"><table class="astro-c5eeuzv2"><caption class="astro-c5eeuzv2">时间与空间复杂度</caption><thead class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="col" class="astro-c5eeuzv2">项目</th><th scope="col" class="astro-c5eeuzv2">复杂度</th><th scope="col" class="astro-c5eeuzv2">说明</th></tr></thead><tbody class="astro-c5eeuzv2"><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">分治求和时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">n 个单元素区间都要被访问；整棵调用树共有 2n - 1 个调用，仍是线性数量。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">普通循环求和时间</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">循环也必须读取 n 个元素，所以本例不能靠分治降低时间复杂度。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">数组存储</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(n)</code></td><td class="astro-c5eeuzv2">numbers 保存全部输入；最大合法下标是 99999。</td></tr><tr class="astro-c5eeuzv2"><th scope="row" class="astro-c5eeuzv2">额外递归栈</th><td class="astro-c5eeuzv2"><code class="astro-c5eeuzv2">O(log n)</code></td><td class="astro-c5eeuzv2">每次把区间大约分成两半，从 100000 个元素缩到 1 个元素只需对数层调用。</td></tr></tbody></table></div></section><section aria-labelledby="常见错误"><h2 id="常见错误" class="astro-c5eeuzv2">常见错误</h2><div><section aria-labelledby="divide-conquer-sorting-error-0" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-error-0" class="astro-c5eeuzv2">漏掉单元素出口</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">long long rangeSum(int left, int right) {\n  int middle = left + (right - left) / 2;\n  return rangeSum(left, middle) + rangeSum(middle + 1, right);\n}</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>区间缩到 [0, 0] 后还会继续计算 middle 并调用自己，递归无法在最小问题处停止。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>把 left == right 的判断放在分解之前，并直接返回 numbers[left]。</p></section><section aria-labelledby="divide-conquer-sorting-error-1" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-error-1" class="astro-c5eeuzv2">右半段仍从 middle 开始</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">long long rightSum = rangeSum(middle, right);</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>在 [0, 1] 中 middle = 0，错误右段仍是 [0, 1]，与原区间一样大；中点还与左段重叠。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>右半段固定写成 [middle + 1, right]，这样两段不重叠且都严格缩小。</p></section><section aria-labelledby="divide-conquer-sorting-error-2" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-error-2" class="astro-c5eeuzv2">调用了两边，却没有组合返回值</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">rangeSum(left, middle);\nrangeSum(middle + 1, right);\nreturn 0;</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>两个子调用确实算出了答案，但返回值没有保存和相加，当前区间最后只能错误地返回 0。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>分别保存 leftSum、rightSum，再返回 leftSum + rightSum。</p></section><section aria-labelledby="divide-conquer-sorting-error-3" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-error-3" class="astro-c5eeuzv2">用 int 保存区间和</h3><pre tabindex="0" class="astro-c5eeuzv2"><code class="language-cpp">int leftSum = rangeSum(left, middle);\nint rightSum = rangeSum(middle + 1, right);</code></pre><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>D1 下最大绝对和是 10^14，远超常见 32 位 int 的可表示范围，转换后可能溢出。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>数组元素、函数返回类型、leftSum 和 rightSum 全部使用 long long。</p></section><section aria-labelledby="divide-conquer-sorting-error-4" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-error-4" class="astro-c5eeuzv2">看到递归就认为一定更快</h3><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">为什么错：</strong>这份程序仍会访问全部 n 个元素，时间是 O(n)；普通循环求和也是 O(n)，递归还有调用开销。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">怎样改：</strong>分别计算每层做了多少工作、总共有多少状态，再判断复杂度；不要把“用了分治”当作速度证明。</p></section></div></section><section aria-labelledby="练习与自检"><h2 id="练习与自检" class="astro-c5eeuzv2">练习与自检</h2><div><section aria-labelledby="divide-conquer-sorting-practice-0" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-practice-0" class="astro-c5eeuzv2">练习一：手算分解与返回</h3><p class="astro-c5eeuzv2">对数组 [3, 1, 4, 2] 调用 rangeSum(0, 3)。写出两个长度为 2 的子区间各自返回什么，以及根区间最后返回什么。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>先写四个单元素返回值，再从较小区间向上组合。</p></section><section aria-labelledby="divide-conquer-sorting-practice-1" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-practice-1" class="astro-c5eeuzv2">练习二：检查会卡住的边界</h3><p class="astro-c5eeuzv2">有人把右子区间写成 [middle, right]。请代入 [0, 1]，说明哪次调用没有缩小，并写出正确右边界。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>先算 middle，再比较错误子区间与原区间。</p></section><section aria-labelledby="divide-conquer-sorting-practice-2" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-practice-2" class="astro-c5eeuzv2">练习三：迁移到区间最大值</h3><p class="astro-c5eeuzv2">若任务改为求一个闭区间中的最大值，三个分治阶段分别做什么？它会不会因此比一次线性扫描更低阶？</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>单元素答案就是该元素；组合两个子结果时只需保留较大者。</p></section><section aria-labelledby="divide-conquer-sorting-practice-3" class="astro-c5eeuzv2"><h3 id="divide-conquer-sorting-practice-3" class="astro-c5eeuzv2">练习四：判断整数类型</h3><p class="astro-c5eeuzv2">当 n = 100000 且每个数都是 1000000000 时，总和是多少？说明为什么 int 不够。</p><p class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">提示：</strong>先计算 n 与单个数的乘积，再与常见 32 位 int 上限约 2.1 × 10^9 比较。</p></section></div><div><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习一答案</summary><p class="astro-c5eeuzv2">[0, 1] 把 3 和 1 组合成 4；[2, 3] 把 4 和 2 组合成 6；[0, 3] 再返回 4 + 6 = 10。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习二答案</summary><p class="astro-c5eeuzv2">[0, 1] 的 middle 是 0。错误右段 [middle, right] 仍是 [0, 1]，与原区间完全相同，所以可能无限递归；正确右段是 [middle + 1, right]，也就是 [1, 1]。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习三答案</summary><p class="astro-c5eeuzv2">分解时仍把 [left, right] 分为两个相邻子区间；解决时递归求两个子区间最大值；组合时取两个子结果中的较大值。它仍要看过每个元素，时间是 O(n)，与线性扫描同阶，分治没有自动加速。</p></details><details class="astro-c5eeuzv2"><summary class="astro-c5eeuzv2">查看练习四答案</summary><p class="astro-c5eeuzv2">总和是 100000 × 1000000000 = 100000000000000，也就是 10^14。这个数远大于常见 32 位 int 上限，必须使用 long long。</p></details></div></section><section aria-labelledby="下一步"><h2 id="下一步" class="astro-c5eeuzv2">下一步</h2><p class="astro-c5eeuzv2">共同模型已经建立。请按下方“选择子知识点”卡片先进入归并排序，观察怎样显式合并两个已经有序的子区间；再进入快速排序，观察怎样先划分、再分别处理两侧。两个子页都能直接访问，不设置完成门槛。</p></section><section aria-labelledby="选择子知识点"><h2 id="选择子知识点" class="astro-c5eeuzv2">选择子知识点</h2><p class="astro-c5eeuzv2">根据当前要解决的问题进入对应页面；每个子知识点都可以直接访问，不设置完成门槛。</p><ul><li class="astro-c5eeuzv2"><a href="/gesp/level5/divide-conquer-sorting/merge-sort/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">归并排序</strong><small class="astro-c5eeuzv2">跟踪分解与合并，理解稳定性和临时数组。</small></span><span class="astro-c5eeuzv2">→</span></a></li><li class="astro-c5eeuzv2"><a href="/gesp/level5/divide-conquer-sorting/quick-sort/" data-level5-child-link class="astro-c5eeuzv2"><span class="astro-c5eeuzv2"><strong class="astro-c5eeuzv2">快速排序</strong><small class="astro-c5eeuzv2">安全划分区间，分析平均与最坏情况。</small></span><span class="astro-c5eeuzv2">→</span></a></li></ul></section><h2 id="核心内容检查" class="astro-c5eeuzv2">核心内容检查</h2><h2 id="整页综合测试" class="astro-c5eeuzv2">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></article></div><footer',
        '<article id="top"><p>递归把一个问题交给规模更小的同类问题处理；每条调用路径都要有直接回答的出口，并且严格走向出口。</p><h2 id="知识范围与官方定位" class="astro-2i3ys3wc">知识范围与官方定位</h2><p class="astro-2i3ys3wc">对应五级大纲“递归”：理解递归函数含义、出口、严格推进、栈帧调用与返回，能识别阶乘、斐波那契、汉诺塔的递归形状，并用递归枚举和递归树分析时间、空间。</p><p>本页只使用一级至五级已经学习的语法和能力；小知识点按页内编号连续查询，不设置前置完成门槛。</p><dl><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">是什么</dt><dd class="astro-2i3ys3wc">递归函数不是“无限调用自己”，而是在当前合同下把更小的同类部分交给下一帧，出口直接完成最小状态。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">解决什么问题</dt><dd class="astro-2i3ys3wc">它能把“逐层决定、完成后回到原处继续”的问题写得清楚，例如阶乘的返回、汉诺塔的分步和本课的二进制串枚举。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">什么时候使用</dt><dd class="astro-2i3ys3wc">只有能写出精确函数含义、可到达出口和严格变小的下一状态时才使用；三者任缺一个都不能开始写递归。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">核心思想</dt><dd class="astro-2i3ys3wc">每个栈帧独立保存自己的参数和继续位置；共享 answer 数组保存当前路径，但只有 answer[0..pos-1] 是本帧承诺有效的前缀。</dd></div><div class="astro-2i3ys3wc"><dt class="astro-2i3ys3wc">C++ 实现方法</dt><dd class="astro-2i3ys3wc">先公开 0≤n≤20，再写 search(pos) 的前缀合同、pos==n 的出口和 pos+1 的推进；每个非叶帧固定先 0、返回本帧后再 1。</dd></div></dl><section aria-labelledby="01-递归的三个必要部分"><h2 id="01-递归的三个必要部分" class="astro-2i3ys3wc">01 递归的三个必要部分</h2><p class="astro-2i3ys3wc">一段递归先要写清函数合同：它当前解决什么同类问题。例如 search(pos) 的合同是：在 answer[0..pos-1] 已固定后，补全所有合法的后缀。合同清楚，调用者才知道这一层完成后会得到什么。</p><p class="astro-2i3ys3wc">第二部分是出口：遇到可以直接回答的最小状态就停止调用。第三部分是严格推进：每次递归后，离出口必须更近。若 factorial(n) 的出口是 n==1，却错误调用 factorial(n+1)，参数看似在变化却永远到不了出口。</p><div><h3 class="astro-2i3ys3wc">具体样例与推演</h3><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">场景：</strong>先不写代码：为 search(pos) 补全函数合同、出口和严格推进；再用错误的 factorial(n+1) 检查“变化”不等于“靠近出口”。</p><ol class="astro-2i3ys3wc"><li class="astro-2i3ys3wc">合同：answer[0..pos-1] 已固定，search(pos) 要枚举每一种合法后缀；因此 pos 不是任意数字，而是下一位的位置。</li><li class="astro-2i3ys3wc">出口：pos==n 时 n 位都已经确定，直接输出一行并 return；n=0 也在 search(0) 直接输出一行空行。</li><li class="astro-2i3ys3wc">推进：非出口只能调用 search(pos+1)，因为已确定的位置从 pos 个增加到 pos+1 个，必会到 n。</li></ol><p class="astro-2i3ys3wc"><strong class="astro-2i3ys3wc">适用结论：</strong>自检题：把“函数完成什么、最小状态是什么、下一层为何更近”各说一句；三句缺一句就不要写递归。</p><aside',
        '<article id="top"><section data-lesson-quick-review><h2><span></span>本课先记住</h2><dl><div data-review-kind="core"><dt>核心结论</dt><dd><p>先确定输入规模 n 和关键操作，数出操作次数怎样随 n 增长，再用 O 表示增长数量级；最后还要检查额外空间和数据范围。</p></dd></div><div data-review-kind="syntax"><dt>常用语法</dt><dd><p>顺序段次数相加，独立嵌套次数相乘；规模每轮减半时，轮数通常是对数级。</p><pre tabindex="0"><code>while (remaining &gt; 1) {\n    remaining /= 2;\n}</code></pre></dd></div><div data-review-kind="example"><dt>最小示例</dt><dd><p>n=8 时，每轮除以 2 的轨迹是 8→4→2→1，共执行 3 轮，因此是 O(log n)。</p><pre tabindex="0"><code>8 -&gt; 4 -&gt; 2 -&gt; 1\n3 rounds</code></pre></dd></div><div data-review-kind="mistakes"><dt>关键易错点</dt><dd><ul><li>不能按代码行数猜复杂度</li><li>前后相邻的循环次数相加，不是相乘</li><li>O 表示增长趋势，不是精确秒数</li><li>只看时间而漏掉数组和递归栈仍可能失败</li></ul></dd></div></dl></section><h2 id="从同样很快到差距巨大">从同样很快到差距巨大</h2><p>输入规模只有 <code>n=8</code> 时，扫描一次、两层循环甚至三层循环都能很快结束。可是当 <code>n</code> 增长到 <code>100000</code>，不同写法的工作量会拉开巨大差距：一次扫描约十万次，两层各跑 <code>n</code> 次的循环却会达到一百亿次。</p><p>因此，判断程序能不能完成，不能只看代码短不短，也不能只在自己的电脑上试一个小数据。本课只新增三项能力：</p><ol>\n<li>**K1：**确定输入规模 <code>n</code> 和关键操作，先数实际次数，再写复杂度。</li>\n<li>**K2：**根据顺序、嵌套、减半和二叉分支，判断常见增长数量级。</li>\n<li>**K3：**把时间、额外空间和数据范围放在一起检查。</li>\n</ol><p>循环、数组、函数、递归、二分和分治都已经学过。本课只借这些已学结构观察工作量，不重新讲算法本身，也不展开大 O 的严格数学证明、主定理、摊还分析、NP 问题或六级以上内容。</p><p>现行官方材料有两个呈现层级：总表列出“多项式、指数、对数复杂度”，另一处详述与知识块表重点展开多项式和对数。本站按两处内容的并集讲清三类增长，不把指数内容扩大成新的算法教学；其中 <code>O(1)</code> 用作比较的起点，<code>O(n log n)</code> 用来连接已经学过的分治过程。</p><h2 id="01-输入规模与基本操作">01 输入规模与基本操作</h2><h3 id="k1先确定-n再选择要数的操作">K1：先确定 <code>n</code>，再选择要数的操作</h3><p><strong>输入规模</strong>是会随输入变大的数量，通常记作 <code>n</code>。处理长度为 <code>n</code> 的数组时，<code>n</code> 是元素个数，不是数组中某个元素的数值。</p><p>接着选择最能代表工作量、并且会重复执行的<strong>关键操作</strong>，例如一次比较、一次赋值或一次递归分支。代码行数不能代替执行次数：循环体里只有一行，这一行仍可能执行 <code>n</code> 次、<code>n^2</code> 次，甚至更多。</p><p>下面的局部代码把 <code>count++</code> 当作关键操作：</p><pre><code>long long count = 0;for (int i = 0; i &lt; n; i++) {    count++;}</code></pre><p>循环变量依次是 <code>0, 1, ..., n - 1</code>，所以关键操作执行 <code>n</code> 次。我们先得到“实际次数是 <code>n</code>”，再说它的时间复杂度是 <code>O(n)</code>。</p><h3 id="从次数表达式得到-o">从次数表达式得到 O</h3><p>大 O 记录的是：<code>n</code> 继续增长时，工作量属于哪一种增长数量级。化简时保留增长最快的部分，并忽略固定倍数。</p><table>\n<thead>\n<tr>\n<th>实际次数表达式</th>\n<th>观察</th>\n<th>数量级</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>5</code></td>\n<td>不随 <code>n</code> 改变</td>\n<td><code>O(1)</code></td>\n</tr>\n<tr>\n<td><code>3n + 5</code></td>\n<td>主导增长的是 <code>n</code></td>\n<td><code>O(n)</code></td>\n</tr>\n<tr>\n<td><code>n^2 + 2n</code></td>\n<td><code>n</code> 很大时，<code>n^2</code> 增长更快</td>\n<td><code>O(n^2)</code></td>\n</tr>\n</tbody>\n</table><p>这不表示常数永远不重要，也不表示 <code>O(n)</code> 一定在任意小数据上比 <code>O(n^2)</code> 快。它只让我们比较输入继续变大时的增长趋势，不是某台机器上的精确秒数，更不是一定通过的保证。</p><h2 id="02-从程序结构判断增长">02 从程序结构判断增长</h2><h3 id="k2顺序相加独立嵌套相乘">K2：顺序相加，独立嵌套相乘</h3><p>两个循环前后执行时，先做 <code>n</code> 次，再做 <code>n</code> 次，总次数是 <code>n + n = 2n</code>，化简为 <code>O(n)</code>：</p><pre><code>for (int i = 0; i &lt; n; i++) {    count++;}for (int j = 0; j &lt; n; j++) {    count++;}</code></pre><p>如果第二个循环在第一个循环里面，并且每一层都独立跑满 <code>n</code> 次，才是 <code>n * n = n^2</code>。三层独立循环同理是 <code>n^3</code>。所以不能看见两个或三个 <code>for</code> 就直接相乘，必须先看它们是前后关系还是包含关系，以及每层实际循环多少次。</p><h3 id="每轮减半olog-n">每轮减半：<code>O(log n)</code></h3><p>当剩余规模每轮除以 <code>2</code>，只需很少的轮数就会缩到 <code>1</code>。正式样例取 <code>n=8</code>，最终程序真实运行的减半过程如下。</p><table>\n<thead>\n<tr>\n<th>判断</th>\n<th style="text-align: right">判断前 <code>remaining</code></th>\n<th><code>remaining &gt; 1</code></th>\n<th>本轮动作</th>\n<th style="text-align: right">累计 <code>logarithmic</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>第 1 次</td>\n<td style="text-align: right">8</td>\n<td>成立</td>\n<td><code>remaining = 8 / 2 = 4</code></td>\n<td style="text-align: right">1</td>\n</tr>\n<tr>\n<td>第 2 次</td>\n<td style="text-align: right">4</td>\n<td>成立</td>\n<td><code>remaining = 4 / 2 = 2</code></td>\n<td style="text-align: right">2</td>\n</tr>\n<tr>\n<td>第 3 次</td>\n<td style="text-align: right">2</td>\n<td>成立</td>\n<td><code>remaining = 2 / 2 = 1</code></td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td>停止判断</td>\n<td style="text-align: right">1</td>\n<td>第一次不成立</td>\n<td>不再执行循环体</td>\n<td style="text-align: right">3</td>\n</tr>\n</tbody>\n</table><p>轨迹是 <code>8 -&gt; 4 -&gt; 2 -&gt; 1</code>，共执行 <code>3</code> 轮。因为 <code>2^3 = 8</code>，这种每轮按固定比例缩小的轮数随 <code>n</code> 呈对数增长，记作 <code>O(log n)</code>。在复杂度中，若底数是固定常数，通常不必专门写底数。</p><p>如果每个减半层还完整扫描 <code>n</code> 项，总次数就是“约 <code>log n</code> 层 × 每层约 <code>n</code> 次”，得到 <code>O(n log n)</code>。不能因为外层会减半，就漏掉每层的线性工作。</p><h3 id="二叉分支o2n">二叉分支：<code>O(2^n)</code></h3><p>当 <code>n</code> 个位置都各有两种选择时，完整方案数为 <code>2^n</code>。最终程序中的递归在深度达到 <code>n</code> 时返回 <code>1</code>，表示找到一个叶子方案；否则进入两个 <code>depth + 1</code> 分支，并把两边的叶子数相加。</p><p><code>n=8</code> 时叶子数是 <code>2^8 = 256</code>。这里的 <code>256</code> 只表示叶子数，不是整棵递归树的总调用数；总调用数虽然不是恰好 <code>2^n</code>，增长数量级仍是 <code>O(2^n)</code>。如果还要在每个叶子输出最多 <code>n</code> 项内容，输出量可能达到 <code>O(n * 2^n)</code>。</p><h2 id="03-七种次数放到同一个输入中">03 七种次数放到同一个输入中</h2><p>下面七段结构彼此独立，每一段只数自己的关键操作。它们是一个“操作次数实验室”，不能把七项相加后说成某个真实解法同时具有七种复杂度。</p><table>\n<thead>\n<tr>\n<th>增长数量级</th>\n<th><code>n=8</code> 时的真实结构</th>\n<th style="text-align: right">精确次数</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>O(1)</code></td>\n<td>固定执行一次</td>\n<td style="text-align: right">1</td>\n</tr>\n<tr>\n<td><code>O(log n)</code></td>\n<td><code>8 -&gt; 4 -&gt; 2 -&gt; 1</code></td>\n<td style="text-align: right">3</td>\n</tr>\n<tr>\n<td><code>O(n)</code></td>\n<td>扫描 8 项</td>\n<td style="text-align: right">8</td>\n</tr>\n<tr>\n<td><code>O(n log n)</code></td>\n<td>3 层，每层扫描 8 项</td>\n<td style="text-align: right">24</td>\n</tr>\n<tr>\n<td><code>O(n^2)</code></td>\n<td>两层各执行 8 次</td>\n<td style="text-align: right">64</td>\n</tr>\n<tr>\n<td><code>O(n^3)</code></td>\n<td>三层各执行 8 次</td>\n<td style="text-align: right">512</td>\n</tr>\n<tr>\n<td><code>O(2^n)</code></td>\n<td>8 个位置各有两种选择，统计叶子</td>\n<td style="text-align: right">256</td>\n</tr>\n</tbody>\n</table><p>这个表说明三种不同的判断方法：前后执行先相加，独立嵌套才相乘，每轮减半先数层数。七项按常见增长速度排列，但这仍是“大数据增长趋势”的顺序，不保证任意小数据的真实耗时严格照此排序。</p><h2 id="04-用程序真实计数">04 用程序真实计数</h2><p>先建立每一段自己的计数变量。减半段用 <code>remaining</code> 保存当前剩余规模；每完成一轮，计数加 <code>1</code>，再把规模除以 <code>2</code>。指数段只统计递归叶子，避免把叶子数和总调用数混在一起。</p><p>下面的教学程序只接受 <code>1 &lt;= n &lt;= 12</code>。上界设得小，是为了让三层循环和二叉递归都能在实验中安全、快速地真实执行；这不表示实际题目的 <code>n</code> 只能到 <code>12</code>。计数统一使用 <code>long long</code>。</p><h3 id="完成程序">完成程序</h3><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nlong long countExponentialLeaves(int depth, int n) {    if (depth == n) {        return 1;    }    return countExponentialLeaves(depth + 1, n) + countExponentialLeaves(depth + 1, n);}\nint main() {    int n;    cin &gt;&gt; n;    if (n &lt; 1 || n &gt; 12) {        cout &lt;&lt; "invalid\\n";        return 0;    }\n    long long constantCount = 0;    constantCount++;\n    long long logarithmicCount = 0;    int remaining = n;    while (remaining &gt; 1) {        remaining /= 2;        logarithmicCount++;    }\n    long long linearCount = 0;    for (int i = 0; i &lt; n; i++) {        linearCount++;    }\n    long long linearLogCount = 0;    remaining = n;    while (remaining &gt; 1) {        for (int i = 0; i &lt; n; i++) {            linearLogCount++;        }        remaining /= 2;    }\n    long long quadraticCount = 0;    for (int i = 0; i &lt; n; i++) {        for (int j = 0; j &lt; n; j++) {            quadraticCount++;        }    }\n    long long cubicCount = 0;    for (int i = 0; i &lt; n; i++) {        for (int j = 0; j &lt; n; j++) {            for (int k = 0; k &lt; n; k++) {                cubicCount++;            }        }    }\n    long long exponentialCount = countExponentialLeaves(0, n);\n    cout &lt;&lt; "constant " &lt;&lt; constantCount &lt;&lt; \'\\n\';    cout &lt;&lt; "logarithmic " &lt;&lt; logarithmicCount &lt;&lt; \'\\n\';    cout &lt;&lt; "linear " &lt;&lt; linearCount &lt;&lt; \'\\n\';    cout &lt;&lt; "linear_log " &lt;&lt; linearLogCount &lt;&lt; \'\\n\';    cout &lt;&lt; "quadratic " &lt;&lt; quadraticCount &lt;&lt; \'\\n\';    cout &lt;&lt; "cubic " &lt;&lt; cubicCount &lt;&lt; \'\\n\';    cout &lt;&lt; "exponential " &lt;&lt; exponentialCount &lt;&lt; \'\\n\';    return 0;}</code></pre><h3 id="样例输入">样例输入</h3><pre><code>8</code></pre><h3 id="样例输出">样例输出</h3><pre><code>constant 1logarithmic 3linear 8linear_log 24quadratic 64cubic 512exponential 256</code></pre><p>样例输入、减半过程表、七项对比表和程序输出使用同一个 <code>n=8</code>。程序整体顺序执行七段，所以若把它当成一个程序分析，运行量由增长最快的指数计数段主导；本课观察的是每一段各自的增长方式。</p><h2 id="05-时间空间与数据范围">05 时间、空间与数据范围</h2><h3 id="k3时间和额外空间分开检查">K3：时间和额外空间分开检查</h3><p><strong>时间复杂度</strong>看累计执行量怎样增长。<strong>额外空间复杂度</strong>看程序为了处理问题又使用了多少存储，例如辅助数组和同时存在的递归调用层。输入数组本身是否计入空间，要看题目采用的口径；无论采用哪种口径，都应把输入存储和新增辅助空间分开说清。</p><p>递归不是“调用过多少次，就同时占多少层栈”。栈空间看的是某一时刻最多有多少层调用没有返回。每层还会保存参数、局部变量和返回位置，所以真实字节数受编译器、平台与函数内容影响；如果最大同时调用深度随 <code>n</code> 线性增长，栈空间数量级通常是 <code>O(n)</code>。</p><h3 id="用-n100000-做粗略判断">用 <code>n=100000</code> 做粗略判断</h3><table>\n<thead>\n<tr>\n<th>方案</th>\n<th style="text-align: right">关键操作的粗略量级</th>\n<th>判断</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>O(n)</code></td>\n<td style="text-align: right">约 <code>100000</code> 次</td>\n<td>十万级</td>\n</tr>\n<tr>\n<td><code>O(n log n)</code></td>\n<td style="text-align: right"><code>log2(100000)</code> 约为 17，因此不到 <code>2000000</code> 次</td>\n<td>约两百万级以内</td>\n</tr>\n<tr>\n<td><code>O(n^2)</code></td>\n<td style="text-align: right"><code>100000 * 100000 = 10000000000</code> 次</td>\n<td>一百亿级，通常明显不可行</td>\n</tr>\n</tbody>\n</table><p>这些数只用于比较数量级，不能据此承诺固定运行秒数。还要看测试组数、每次关键操作的常数、时间限制以及实际实现。</p><h3 id="把数组换算成近似字节数">把数组换算成近似字节数</h3><p>在常见 GCC/OJ 环境中，<code>int</code> 通常占 <code>4</code> 字节：</p><ul>\n<li><code>int a[100000]</code> 约占 <code>100000 * 4 = 400000</code> 字节，约 <code>390.6 KiB</code>；</li>\n<li><code>int grid[1000][1000]</code> 约占 <code>1000 * 1000 * 4 = 4000000</code> 字节，约 <code>3.81 MiB</code>。</li>\n</ul><p>这只是常见环境的估算，实际大小以编译器、平台和评测环境为准。还要另算其他数组、递归栈和对象开销，并检查计数或乘法结果是否会超出整数范围。</p><p>统一的判断顺序是：<strong>先看最大输入规模和测试组数，再估关键操作次数；随后估主要数组、递归栈和整数范围。三者都符合题目限制，方案才有通过的可能。</strong></p><h2 id="常见错误怎样改正">常见错误怎样改正</h2><table>\n<thead>\n<tr>\n<th>错误判断</th>\n<th>最早原因与实际后果</th>\n<th>修正方法</th>\n<th>重新检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>“代码只有几行，所以是 <code>O(1)</code>”</td>\n<td>把源代码行数当成运行次数，漏掉循环和递归的重复执行</td>\n<td>先确定 <code>n</code>，再挑关键操作并写出实际次数</td>\n<td>单循环的 <code>count++</code> 应数出 <code>n</code> 次</td>\n</tr>\n<tr>\n<td>“两个 <code>for</code> 一定是 <code>O(n^2)</code>”</td>\n<td>没看循环的包含关系，前后两个循环被错误相乘</td>\n<td>前后执行次数相加；内层每轮独立跑满时才相乘</td>\n<td>两个相邻 <code>n</code> 次循环应得到 <code>2n -&gt; O(n)</code></td>\n</tr>\n<tr>\n<td>“循环体只有一次比较，所以二分是 <code>O(1)</code>”</td>\n<td>只数一轮，漏掉规模反复减半的轮数</td>\n<td>列出完整减半轨迹，并保留第一次条件不成立</td>\n<td><code>8 -&gt; 4 -&gt; 2 -&gt; 1</code> 应记录 3 轮和停止判断</td>\n</tr>\n<tr>\n<td>“<code>2n</code> 和 <code>2^n</code> 都有 2，增长差不多”</td>\n<td>没分清固定倍数与指数位置</td>\n<td>代入相邻的 <code>n</code> 比较：<code>2n</code> 每次加 2，<code>2^n</code> 每次翻倍</td>\n<td><code>n=8</code> 时分别是 16 和 256</td>\n</tr>\n<tr>\n<td>“时间能过，内存一定能过”</td>\n<td>忽略辅助数组和最大递归深度，可能超内存或爆栈</td>\n<td>分开估算时间、数组字节数和递归栈深度</td>\n<td>写出主要数组元素数 × 单个元素字节数，并说明最大同时递归层数</td>\n</tr>\n<tr>\n<td>“写出 <code>O(n log n)</code> 就能保证通过”</td>\n<td>把增长数量级当成精确秒数，漏掉测试组数、常数和限制</td>\n<td>用最大数据粗算次数，再核对时间、空间和整数范围</td>\n<td>结论中必须带数据范围和评测限制，而不是只写一个 O 符号</td>\n</tr>\n</tbody>\n</table><h2 id="练习与答案">练习与答案</h2><h3 id="引导练习顺序还是相乘">引导练习：顺序还是相乘</h3><p>有两个前后相邻的循环，每个都执行 <code>n</code> 次。写出总次数和复杂度。</p><p>**答案：**总次数是 <code>n + n = 2n</code>，忽略固定倍数后为 <code>O(n)</code>。它们没有相互包含，所以不能写成 <code>n * n</code>。</p><h3 id="独立练习改变内层结构">独立练习：改变内层结构</h3><p>先判断外层、内层都独立执行 <code>n</code> 次的两层循环；再把内层改成每轮把 <code>remaining</code> 除以 <code>2</code>。</p><p>**答案：**第一种每个外层轮次都完成 <code>n</code> 次内层操作，总量为 <code>n * n = n^2</code>，即 <code>O(n^2)</code>。第二种外层有 <code>n</code> 轮，每轮内层约执行 <code>log n</code> 次，总量为 <code>n * log n</code>，即 <code>O(n log n)</code>。</p><h3 id="迁移练习为十万规模选方案">迁移练习：为十万规模选方案</h3><p>当 <code>n=100000</code> 时，在 <code>O(n log n)</code> 和 <code>O(n^2)</code> 两个方案中选择更可行的一项，并给出数量比较。</p><p>**答案：**优先选择 <code>O(n log n)</code>：按 <code>log2(100000)</code> 约为 17 粗算，不到两百万次；<code>O(n^2)</code> 达到 <code>10000000000</code> 次，即一百亿级。最终仍需结合实际限制测试。</p><h3 id="空间练习数量级和-mib-都要会说">空间练习：数量级和 MiB 都要会说</h3><p>常见 GCC/OJ 环境中按一个 <code>int</code> 为 <code>4</code> 字节估算，<code>int a[200000]</code> 约占多少字节、多少 MiB？数组空间的数量级是什么？</p><p>**答案：**约 <code>200000 * 4 = 800000</code> 字节，约 <code>0.76 MiB</code>；元素个数随 <code>n</code> 线性增长时，数组空间是 <code>O(n)</code>。<code>O(n)</code> 回答增长趋势，<code>0.76 MiB</code> 回答这个具体规模下的近似占用，两者不能互相替代。</p>',
    ],
    6: [
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级官方大纲“树的基础知识”：树的定义、基本属性和树的存储。重点是先建立准确术语，再为后续建树、遍历和搜索准备数据表示；本页不提前展开遍历算法。</p><h2 id="kp-01">01 树的定义与关系</h2><p>线性结构适合表达“前一个、后一个”的顺序关系，例如排队名单；当一个对象下面可能分出多个对象时，就需要树来表达层次关系，例如学校—年级—班级、文件夹—子文件夹。树由结点和连接结点的边组成。非空树有且只有一个根结点；根没有父结点；根以外每个结点恰好有一个父结点；沿父子关系不能绕一圈回到原结点。没有结点的树叫空树。</p><ul>\n<li>父结点与孩子结点：一条向下的边连接的直接关系。</li>\n<li>兄弟结点：拥有同一个父结点。</li>\n<li>叶结点：没有孩子的结点；单结点树的根也同时是叶结点。</li>\n<li>祖先与后代：从某结点沿父子关系向上或向下能够到达的结点。</li>\n<li>子树：某个结点以及它的全部后代组成的树。</li>\n</ul><h2 id="kp-02">02 深度、高度与度</h2><p>本字典统一采用：根深度为 <code>0</code>，孩子深度等于父结点深度加 <code>1</code>；叶结点高度为 <code>0</code>，非叶结点高度等于孩子高度最大值加 <code>1</code>；树高等于根结点高度。若题目把根深度记为 <code>1</code>，必须按题目口径重新计算。</p><p>结点的度是孩子数量，树的度是所有结点度的最大值。叶结点度为 <code>0</code>。二叉树只表示每个结点至多有两个孩子，不代表每个结点一定有两个孩子。</p><div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>术语</th>\n<th>判断方法</th>\n<th>常见混淆</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>深度</td>\n<td>从根到该结点经过的边数</td>\n<td>先确认根记 0 还是 1</td>\n</tr>\n<tr>\n<td>高度</td>\n<td>从该结点向下到最深叶子的边数</td>\n<td>与深度方向相反</td>\n</tr>\n<tr>\n<td>结点的度</td>\n<td>该结点的孩子数</td>\n<td>不是相邻边总数</td>\n</tr>\n<tr>\n<td>树的度</td>\n<td>全树最大结点度</td>\n<td>不是树高</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><h2 id="kp-03">03 树的常见存储</h2><p>父亲数组用 <code>father[child] = parent</code> 保存每个非根结点的父亲，适合向上查询；孩子列表为每个父结点保存全部直接孩子，适合向下遍历。孩子列表也叫树的邻接表：例如边为 <code>1→2、1→3、2→4</code> 时，可写成 <code>children[1] = {2, 3}</code>、<code>children[2] = {4}</code>，3 和 4 的列表为空。读入一对 <code>parent child</code>，就把 <code>child</code> 加到 <code>children[parent]</code> 末尾；这里存的是直接孩子，不能把孙结点 4 直接放进 1 的列表。</p><p>普通树还可以用“左孩子右兄弟”表示：<code>leftChild[u]</code> 指向第一个孩子，<code>rightSibling[u]</code> 指向下一个兄弟。这样每个结点只保留两个位置，却仍能依次找到全部孩子。这里要求能读懂关系，不要求把它当作二叉排序树。</p><h2 id="typical-example">典型例题</h2><h3 id="基础示例">基础示例</h3><p>已知 <code>father[2]=1、father[3]=1、father[4]=2、father[5]=2</code>。根是 1；2 和 3 是兄弟；4 和 5 是兄弟；1 的度为 2，2 的度也为 2，树的度为 2。按根深度为 0，4 的深度为 2；4 是叶结点，高度为 0；2 的高度为 1；整棵树高度为 2。</p><div tabindex="0" role="region"><table><caption>从根向下统计结点和叶结点</caption><thead><tr><th scope="col">执行步骤</th><th scope="col">本步观察</th><th scope="col">累计结点数</th><th scope="col">累计叶结点数</th></tr></thead><tbody><tr><th scope="row">访问 1</th><td>有孩子 2、3，不是叶结点</td><td>1</td><td>0</td></tr><tr><th scope="row">访问 2</th><td>有孩子 4、5，不是叶结点</td><td>2</td><td>0</td></tr><tr><th scope="row">访问 4、5</th><td>都没有孩子，各计一个叶结点</td><td>4</td><td>2</td></tr><tr><th scope="row">访问 3</th><td>没有孩子，计一个叶结点</td><td>5</td><td>3</td></tr></tbody></table></div><h3 id="典型应用">典型应用</h3><p>文件目录就是树：总文件夹是根，直接包含的文件夹是孩子，同一文件夹下的项目互为兄弟。若要找到某个文件的完整路径，就从该结点沿父亲数组不断向上，直到根；若要列出一个文件夹内的全部内容，则从它向下访问各棵子树。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>树形</th>\n<th>应得到的关键结论</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>根 1 有孩子 2、3</td>\n<td>结点数 3，叶结点 2、3 共 2 个，树高 1</td>\n</tr>\n<tr>\n<td>边界</td>\n<td>单结点树只有根 1</td>\n<td>根同时是叶结点，深度和高度都为 0，树的度为 0</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>链式树 1→2→3→4</td>\n<td>只有结点 4 是叶结点，树高 3，不能误判成四层高度 4</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>把深度和高度当成同一个方向</h3><div><div><strong><span>×</span> 错误写法</strong><code>结点越靠近根，高度和深度就同时越小</code></div><div><strong><span>✓</span> 正确写法</strong><code>先画出根到结点、结点到最深叶子的两条路径，再分别数边</code></div></div><p><strong>原因：</strong>深度从根向下数，高度从当前结点向最深叶子数，两者方向相反。</p><p><strong>检查方法：</strong>做题第一行写清根深度和叶高度的口径。</p></section><section><h3>把相邻结点都当成孩子</h3><div><div><strong><span>×</span> 错误写法</strong><code>结点 2 与父结点 1 相连，因此计算 2 的度时把 1 也算进去</code></div><div><strong><span>✓</span> 正确写法</strong><code>先确定边的父子方向，再只数从当前结点向下连接的孩子</code></div></div><p><strong>原因：</strong>树中结点的度只统计直接孩子数量，不统计父结点。</p><p><strong>检查方法：</strong>叶结点虽然与父结点相连，但它的度仍然是 0。</p></section><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页覆盖六级大纲“树的构造与遍历”：根据父子或边关系建树、四种二叉树遍历、递归与队列实现，以及遍历序列还原入门和基础统计。树的术语放在上一页，特殊二叉树性质放在下一页。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-tree-traversal-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-tree-traversal-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">3 个子页</span></header><div><article><div><span class="astro-aidf4k4y">02</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/tree-traversal/">树的构造与遍历</a></h3><p class="astro-aidf4k4y">根据父子关系构造小树，推演前序、中序、后序和层序遍历，并完成基础统计。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/tree-traversal/construction-recursive-traversal/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">树的构造与递归遍历</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/tree-traversal/level-order-traversal/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">层序遍历</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/tree-traversal/traversal-reconstruction/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">03</span><strong class="astro-aidf4k4y">根据遍历序列还原二叉树</strong></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 根据关系构造树</h2><p>根据父子关系建树时，先确定唯一根，再把每个孩子连接到唯一父结点。根据边关系建树时还要结合题意判断边是否有方向；若是无向输入，遍历时需要记住父结点或访问状态，避免沿原边返回。</p><p>下面的二叉树将用于比较四种遍历：</p><pre><code>1      /   \\     2     3    / \\   / \\   4   5 6   7</code></pre><h2 id="kp-02">02 三种递归遍历</h2><p>“前、中、后”描述根相对左右子树的访问位置：前序是根、左、右；中序是左、根、右；后序是左、右、根。递归函数必须先判断空指针，再访问结点。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nstruct Node {    int value;    Node* left;    Node* right;};\nvoid preorder(Node* root) {    if (root == nullptr) {  // 当前子树为空，没有结点需要访问        return;    }    // 前序遍历先访问根，再依次处理左、右子树    cout &lt;&lt; root-&gt;value &lt;&lt; \' \';    preorder(root-&gt;left);    preorder(root-&gt;right);}\nint countNodes(Node* root) {    if (root == nullptr) {  // 空树的结点数为 0        return 0;    }    // 当前根计 1，再加上左右子树的结点数    return 1 + countNodes(root-&gt;left) + countNodes(root-&gt;right);}\nint main() {    Node n4{4, nullptr, nullptr};    Node n5{5, nullptr, nullptr};    Node n2{2, &amp;n4, &amp;n5};    Node n3{3, nullptr, nullptr};    Node n1{1, &amp;n2, &amp;n3};    preorder(&amp;n1);    cout &lt;&lt; \'\\n\' &lt;&lt; countNodes(&amp;n1) &lt;&lt; \'\\n\';    return 0;}</code></pre><p>输出第一行为 <code>1 2 4 5 3</code>，第二行为 <code>5</code>。每个结点只处理一次，时间复杂度 <code>O(n)</code>；递归额外空间与树高有关。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>输入树</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>根 1，左孩子 2，右孩子 3</td>\n<td>前序 <code>1 2 3</code>，中序 <code>2 1 3</code>，后序 <code>2 3 1</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>空树</td>\n<td>四种遍历都不输出，结点数和叶结点数都是 0</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>单结点树 8</td>\n<td>四种遍历都只输出 <code>8</code>；它既是根也是叶结点</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>7 个结点的完全二叉树</td>\n<td>层序为 <code>1 2 3 4 5 6 7</code>，可检查同层从左到右</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>先访问结点再判断空指针</h3><div><div><strong><span>×</span> 错误写法</strong><code>cout &lt;&lt; root-&gt;value; if (root == nullptr) return;</code></div><div><strong><span>✓</span> 正确写法</strong><code>函数开头先判断 root == nullptr，再读取成员</code></div></div><p><strong>原因：</strong>root 为空时已经先发生非法访问。</p><p><strong>检查方法：</strong>把空树出口写成每个树递归函数的第一段。</p></section><section><h3>只背前中后名称，不看根的位置</h3><div><div><strong><span>×</span> 错误写法</strong><code>把中序也写成先输出根，再遍历左右子树</code></div><div><strong><span>✓</span> 正确写法</strong><code>先写出规则，再把访问根的语句放到相应位置</code></div></div><p><strong>原因：</strong>前、中、后表示根分别在左右子树之前、中间或之后访问。</p><p><strong>检查方法：</strong>用只有三个结点的小树手算一次最容易发现顺序错误。</p></section><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“特殊二叉树”，把三类结构放在一个大知识点中比较。这里只讲结构性质；二叉排序树的查找与插入算法集中在“二叉树搜索”，哈夫曼编码放在“编码知识”。</p><h2 id="kp-01">01 完全二叉树</h2><p>完全二叉树除最后一层外每层都满，最后一层结点从左到右连续。满二叉树一定是完全二叉树，完全二叉树不一定满。按 1 下标连续存入数组时，结点 <code>i</code> 的左孩子候选为 <code>2*i</code>，右孩子为 <code>2*i+1</code>，父结点为 <code>i/2</code>；访问孩子前必须检查候选下标不超过 <code>n</code>。</p><h2 id="kp-02">02 二叉排序树</h2><p>本页采用严格规则：每个结点左子树所有值小于当前值，右子树所有值大于当前值，重复值不插入。中序遍历会得到严格递增序列。这个结构性质是下一页查找与插入方向的依据。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n = 0;    cin &gt;&gt; n;    long long weight[2005] = {};    bool used[2005] = {};    for (int i = 1; i &lt;= n; i++) {  // 读入每个叶结点的权值        cin &gt;&gt; weight[i];    }\n    long long wpl = 0;    int total = n;    for (int round = 1; round &lt; n; round++) {  // 每轮合并当前最小的两个权值        int first = 0;        int second = 0;        for (int i = 1; i &lt;= total; i++) {            if (used[i]) continue;  // 已经被合并的结点不再参与选择            if (first == 0 || weight[i] &lt; weight[first]) {  // 当前权值成为新的最小值                second = first;                first = i;            } else if (second == 0 || weight[i] &lt; weight[second]) {  // 更新第二小值                second = i;            }        }        used[first] = true;        used[second] = true;        total++;        weight[total] = weight[first] + weight[second];        wpl = wpl + weight[total];    }    cout &lt;&lt; wpl &lt;&lt; \'\\n\';    return 0;}</code></pre><p>每轮扫描当前全部权值，时间复杂度 <code>O(n²)</code>，数组空间 <code>O(n)</code>。若只有一个叶结点，不发生合并，WPL 为 0。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>数据或结构</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>二叉排序树依次插入 5、3、8</td>\n<td>根为 5，左孩子 3，右孩子 8，中序为 <code>3 5 8</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>哈夫曼树只有一个权值 7</td>\n<td>不发生合并，WPL 为 0</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>完全二叉树最后一层只有最左两个位置</td>\n<td>仍是完全二叉树；若中间空缺则不是</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>哈夫曼权值 2、3、7、9</td>\n<td>合并值为 5、12、21，WPL 为 38</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>把三类树的条件混在一起</h3><div><div><strong><span>×</span> 错误写法</strong><code>完全二叉树一定满足左小右大</code></div><div><strong><span>✓</span> 正确写法</strong><code>先辨认题目问位置、大小关系还是权值合并</code></div></div><p><strong>原因：</strong>完全性约束结点位置，排序性约束结点值，两者是不同维度。</p><p><strong>检查方法：</strong>为每类树只抓住一条核心条件，再判断应用。</p></section><section><h3>哈夫曼合并后丢掉新权值</h3><div><div><strong><span>×</span> 错误写法</strong><code>合并 2 和 3 后，下一轮仍只在原来的 7、9 中选择</code></div><div><strong><span>✓</span> 正确写法</strong><code>每轮执行“取出两个最小值 → 求和 → 把和放回”</code></div></div><p><strong>原因：</strong>新结点权值 5 代表刚形成的子树，必须放回集合继续比较。</p><p><strong>检查方法：</strong>把每轮的新集合完整写出来，再进入下一轮。</p></section><h2 id="related-learning">关联学习</h2><p>二叉排序树的查找与插入见“07 二叉树搜索”；从哈夫曼树生成字符编码见“04 编码知识”。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“编码知识”，合并哈夫曼编码和格雷编码。哈夫曼树的权值合并与 WPL 已在上一页讲解；本页关注怎样由树得到前缀编码，以及怎样构造和识别格雷编码。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-coding-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-coding-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">2 个子页</span></header><div><article><div><span class="astro-aidf4k4y">04</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/coding/">编码知识</a></h3><p class="astro-aidf4k4y">理解哈夫曼前缀编码与格雷编码，能完成编码、译码和相邻一位变化判断。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/coding/huffman-coding/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">哈夫曼编码</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/coding/gray-code/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">格雷码</strong></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 哈夫曼编码</h2><p>字符放在哈夫曼树叶结点，约定向左写 <code>0</code>、向右写 <code>1</code>，根到叶的完整路径就是字符编码。因为字符只在叶结点，任何完整字符编码都不会成为另一个字符编码的前缀，可以从左到右无分隔译码：走到叶结点就输出字符并回到根。</p><p>字符出现频率越高，权值越大，通常越靠近根，因此编码越短；频率较低的字符可以使用更长编码。这样能减少整段消息的总编码长度，但不能只按频率直接指定编码，仍要先构造哈夫曼树并保证前缀条件。</p><p>若只有一种字符，根到自身路径为空，实际编码格式通常把唯一字符约定为 <code>0</code>，才能保留重复次数。交换左右孩子或改变相同权值的平局顺序，可能得到不同但仍有效的编码。</p><pre><code>000, 001, 011, 010, 110, 111, 101, 100</code></pre><p>二进制整数转格雷码可计算 <code>gray = binary ^ (binary &gt;&gt; 1)</code>。例如 <code>110 ^ 011 = 101</code>。还原时不能直接重复同一公式：最高二进制位等于最高格雷位，后续每个二进制位等于上一位已还原二进制位与当前格雷位异或。</p><p>格雷码还原示例 <code>101 → 110</code>：最高位直接得到 <code>b1=1</code>；下一位 <code>b2=b1^g2=1^0=1</code>；最后一位 <code>b3=b2^g3=1^1=0</code>，所以二进制为 <code>110</code>。每一步都要使用“上一位已经还原的二进制位”，不能使用上一位格雷码。</p><h2 id="quick-table">编码速查表</h2><div tabindex="0" role="region"><table>\n<thead>\n<tr>\n<th>内容</th>\n<th>核心规则</th>\n<th>检查方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>哈夫曼编码</td>\n<td>根到叶路径，左 0 右 1</td>\n<td>是否存在某编码是另一编码前缀</td>\n</tr>\n<tr>\n<td>格雷编码</td>\n<td>相邻编码只变一位</td>\n<td>相邻两项异或结果是否只有一个 1</td>\n</tr>\n</tbody>\n</table></div><p>横向滑动查看完整表格</p><h2 id="typical-example">典型例题</h2><h3 id="基础示例">基础示例</h3><p>编码表 <code>A=0、B=10、C=11</code>，译码 <code>01011</code>：先读 <code>0</code> 得 A，回根；读 <code>10</code> 得 B，回根；读 <code>11</code> 得 C，结果为 <code>ABC</code>。若最后只剩 <code>1</code>，它停在内部结点，说明编码串不完整。</p><h3 id="典型应用">典型应用</h3><p>二进制 <code>110</code> 转格雷码：右移得到 <code>011</code>，逐位异或得到 <code>101</code>。检查镜像序列连接处 <code>010 → 110</code>，异或为 <code>100</code>，只有一位为 1，因此连接合法。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>数据</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>A=0、B=10、C=11</code>，译码 <code>01011</code></td>\n<td>结果为 <code>ABC</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>只有一个字符 A</td>\n<td>实际编码约定为 <code>0</code>，两个 A 编成 <code>00</code></td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>只有两个字符 A、B</td>\n<td>两者都使用 1 位编码，例如 <code>A=0、B=1</code></td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>3 位格雷序列</td>\n<td>任意相邻两项都只有一位不同，包括 <code>010→110</code></td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>多个字符出现频率相同</td>\n<td>可能得到不同编码表，但都必须满足前缀条件</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>在哈夫曼树内部结点提前结束</h3><div><div><strong><span>×</span> 错误写法</strong><code>走到路径 1 就直接输出字符</code></div><div><strong><span>✓</span> 正确写法</strong><code>只有到达叶结点才完成一个字符编码</code></div></div><p><strong>原因：</strong>内部结点下面仍有多个叶结点，路径还不能唯一确定字符。</p><p><strong>检查方法：</strong>译码后若停在内部结点，说明编码串不完整。</p></section><section><h3>只看格雷码中 0 和 1 的数量</h3><div><div><strong><span>×</span> 错误写法</strong><code>两项都含两个 1，所以它们一定相邻合法</code></div><div><strong><span>✓</span> 正确写法</strong><code>把相邻两项逐位比较，确认恰好一位不同</code></div></div><p><strong>原因：</strong>格雷码检查的是相同位置上有几位发生改变，与 1 的总数是否相同无关。</p><p><strong>检查方法：</strong>也可以异或两项，结果应只有一个二进制位为 1。</p></section><h2 id="related-learning">关联学习</h2><p>哈夫曼树的构造与 WPL 见“03 特殊二叉树”；格雷码转换会用到三级学习的异或和右移。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲 DFS 的思想、基本框架、递归、回溯与迷宫路径类基础应用。网格使用 1 下标，方向数组使用 <code>fx</code>、<code>fy</code> 且下标 0 占位。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-dfs-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-dfs-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">5 个子页</span></header><div><article><div><span class="astro-aidf4k4y">05</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/dfs/">深度优先搜索 DFS</a></h3><p class="astro-aidf4k4y">理解一条路走到底再回退的搜索过程，掌握递归出口、标记与回溯。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/dfs/maze-search/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">DFS 初学：走迷宫</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/dfs/flood-fill/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">Flood Fill：连通块</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/dfs/maze-backtracking/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">03</span><strong class="astro-aidf4k4y">迷宫路径枚举与回溯</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/dfs/permutation-enumeration/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">04</span><strong class="astro-aidf4k4y">全排列</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/dfs/subset-enumeration/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">05</span><strong class="astro-aidf4k4y">选与不选：子集枚举</strong></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 DFS 状态与出口</h2><p>DFS 不只是“使用递归”，而是一种先沿一个选择继续深入、遇到目标或无路可走时再返回的搜索顺序。写代码前先说明当前状态，例如 <code>dfs(x, y)</code> 表示“当前已经走到 <code>(x,y)</code>，继续搜索”。函数开头处理越界、障碍、已访问或到达终点等出口，之后才枚举下一步。</p><h2 id="kp-02">02 标记与回溯</h2><p>标记含义决定是否恢复：路径枚举中的 <code>visited</code> 表示“当前路径已经使用”，离开本层时要恢复现场，让其他分支还能选择这个位置；连通区域标记表示“已经确认处理”，不恢复，否则会重复访问甚至在环中不停递归。一个完整回溯步骤是“记录当前选择 → 继续搜索 → 撤销当前选择”。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint n, m;char grid[8][8];bool visited[8][8];long long answer = 0;int fx[5] = {0, -1, 1, 0, 0};int fy[5] = {0, 0, 0, -1, 1};\nvoid dfs(int x, int y) {    if (x == n &amp;&amp; y == m) {  // 已到达终点，当前路径构成一种有效方案        answer++;        return;    }    visited[x][y] = true;    for (int d = 1; d &lt;= 4; d++) {  // 依次尝试上、下、左、右四个方向        int nx = x + fx[d], ny = y + fy[d];        if (nx &lt; 1 || nx &gt; n || ny &lt; 1 || ny &gt; m) continue;  // 越界位置不能进入        if (grid[nx][ny] == \'#\' || visited[nx][ny]) continue;  // 障碍和当前路径中的格子不能重复走        dfs(nx, ny);    }    visited[x][y] = false;  // 返回上一层前恢复现场，供其他路径使用}\nint main() {    cin &gt;&gt; n &gt;&gt; m;    for (int x = 1; x &lt;= n; x++) {  // 读入第 1 到第 n 行网格        for (int y = 1; y &lt;= m; y++) {            cin &gt;&gt; grid[x][y];        }    }    if (grid[1][1] == \'.\' &amp;&amp; grid[n][m] == \'.\') {  // 起点和终点都可走时才开始搜索        dfs(1, 1);    }    cout &lt;&lt; answer &lt;&lt; \'\\n\';    return 0;}</code></pre><p>路径枚举最坏会尝试大量分支，只适合题目给出的较小范围；递归深度不超过可走格子数。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>网格或任务</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>2×2</code> 全部可走</td>\n<td>不重复经过格子时有 2 条路径</td>\n</tr>\n<tr>\n<td>边界</td>\n<td>起点四周都是障碍</td>\n<td>无路可走，答案为 0</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td><code>1×1</code> 且起点就是终点</td>\n<td>进入 DFS 后立即得到唯一一条路径</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>有环的连通区域</td>\n<td>永久访问标记不能恢复，否则会重复进入</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>所有 DFS 都恢复标记</h3><div><div><strong><span>×</span> 错误写法</strong><code>连通标记结束也写 visited[x][y] = false</code></div><div><strong><span>✓</span> 正确写法</strong><code>路径枚举恢复；连通标记不恢复</code></div></div><p><strong>原因：</strong>别的方向会再次进入已处理格子，环形区域可能不停递归。</p><p><strong>检查方法：</strong>先写清标记表示当前路径还是已经处理。</p></section><section><h3>找到终点后仍继续扩展</h3><div><div><strong><span>×</span> 错误写法</strong><code>到达终点只让 answer 加一，但不 return</code></div><div><strong><span>✓</span> 正确写法</strong><code>到达终点后记录答案并立即结束当前分支</code></div></div><p><strong>原因：</strong>当前分支会从终点继续绕行，可能把同一条到达方案延长成不符合题意的路径。</p><p><strong>检查方法：</strong>递归出口不仅决定何时计数，也决定哪里必须停止。</p></section><h2 id="related-learning">关联学习</h2><p>需要每步代价相同的最少步数时，应使用下一页的 BFS；需要枚举完整路径或递归处理树时，DFS 更自然。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲 BFS 的思想、队列、访问标记、扩展方向、步数记录和迷宫最短路径。这里只保证每一步代价相同问题的最少步数，不扩展七级图论。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-bfs-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-bfs-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">2 个子页</span></header><div><article><div><span class="astro-aidf4k4y">06</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/bfs/">宽度优先搜索 BFS</a></h3><p class="astro-aidf4k4y">使用队列按层扩展状态，记录首次到达距离，求每步代价相同问题的最短步数。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/bfs/bfs-basics/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">BFS 初学：队列按层搜索</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/bfs/bfs-shortest-path/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">BFS 求最短路径</strong></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 队列分层扩展</h2><p>BFS 从起点开始处理距离 <code>0、1、2……</code> 的状态。先把起点入队；循环取出队首，依次检查相邻状态，并把新状态加入队尾。先进先出让距离较小的状态先处理。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint n, m, dist[105][105];char grid[105][105];int fx[5] = {0, -1, 1, 0, 0};int fy[5] = {0, 0, 0, -1, 1};\nint main() {    cin &gt;&gt; n &gt;&gt; m;    for (int x = 1; x &lt;= n; x++) {  // 读入网格并把所有格子设为未访问        for (int y = 1; y &lt;= m; y++) {            cin &gt;&gt; grid[x][y];            dist[x][y] = -1;        }    }    queue&lt;pair&lt;int, int&gt;&gt; q;    if (grid[1][1] == \'.\') {  // 起点可走时，把它作为第 0 层加入队列        dist[1][1] = 0;        q.push({1, 1});    }    while (!q.empty()) {  // 按发现顺序逐个扩展队首格子        int x = q.front().first, y = q.front().second;        q.pop();        for (int d = 1; d &lt;= 4; d++) {  // 检查当前格子的四个相邻位置            int nx = x + fx[d], ny = y + fy[d];            if (nx &lt; 1 || nx &gt; n || ny &lt; 1 || ny &gt; m) continue;  // 越界位置不能进入            if (grid[nx][ny] == \'#\' || dist[nx][ny] != -1) continue;  // 跳过障碍和已发现格子            // 第一次发现邻点时就确定最短距离并立即标记            dist[nx][ny] = dist[x][y] + 1;            q.push({nx, ny});        }    }    cout &lt;&lt; dist[n][m] &lt;&lt; \'\\n\';    return 0;}</code></pre><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>网格情况</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>2×2</code> 全部可走</td>\n<td>从左上到右下最短为 2 步</td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>1×1</code>，起点就是终点</td>\n<td>距离为 0，不需要移动</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>终点被障碍完全围住</td>\n<td>搜索结束仍为 <code>-1</code>，表示无路可达</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>存在多条可行路径</td>\n<td>返回最少步数，不统计路径总数</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>等到出队才标记</h3><div><div><strong><span>×</span> 错误写法</strong><code>先 q.push(next)，以后出队才修改 dist</code></div><div><strong><span>✓</span> 正确写法</strong><code>判断未访问 → 写距离 → 入队</code></div></div><p><strong>原因：</strong>等待出队期间仍显示未访问，可能被重复加入。</p><p><strong>检查方法：</strong>把这三步写成固定顺序。</p></section><section><h3>把 BFS 当成任意代价下的最短路</h3><div><div><strong><span>×</span> 错误写法</strong><code>不论每一步代价是多少，都直接按经过边数做 BFS</code></div><div><strong><span>✓</span> 正确写法</strong><code>先确认每次移动代价相同，再用本页 BFS 求最少步数</code></div></div><p><strong>原因：</strong>普通 BFS 的分层只表示步数，只有每一步代价相同时，先到达才保证总代价最小。</p><p><strong>检查方法：</strong>题目若给不同费用，本页结论不能直接套用。</p></section><h2 id="related-learning">关联学习</h2><p>需要枚举所有路径或沿一个分支深入时使用 DFS；BFS 的队列基础可在“12 队列”中查询。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“二叉树搜索算法”，集中讲二叉排序树的查找、插入与遍历。结构定义已在“03 特殊二叉树”出现，这里固定采用左小右大、重复值不插入。</p><h2 id="kp-01">01 按大小关系查找</h2><p>从根开始：目标等于当前值时成功；目标更小就只进入左子树；目标更大就只进入右子树；走到空指针仍未相等则不存在。</p><p>直观记忆是“左边更小，右边更大”。这个方向来自二叉排序树的结构性质，不是随意选择；每比较一次，就能排除另一整棵子树。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;struct Node { int value; Node* left; Node* right; };\nNode* insert(Node* root, int value) {    if (root == nullptr) {  // 找到空位置，在这里连接新结点        return new Node{value, nullptr, nullptr};    }    if (value &lt; root-&gt;value) {  // 新值更小，应插入左子树        root-&gt;left = insert(root-&gt;left, value);    } else if (value &gt; root-&gt;value) {  // 新值更大，应插入右子树        root-&gt;right = insert(root-&gt;right, value);    }    return root;}\nbool search(Node* root, int value) {    if (root == nullptr) {  // 已走到空位置，说明查找不存在        return false;    }    if (value == root-&gt;value) {  // 当前结点就是目标，查找成功        return true;    }    if (value &lt; root-&gt;value) {  // 目标更小，只需继续查找左子树        return search(root-&gt;left, value);    }    // 目标更大，只需继续查找右子树    return search(root-&gt;right, value);}\nint main() {    int a[8] = {0, 8, 4, 12, 2, 6, 10, 14};  // 下标 0 占位，数据从 1 开始    Node* root = nullptr;    for (int i = 1; i &lt;= 7; i++) {  // 按给定顺序逐个建立二叉排序树        root = insert(root, a[i]);    }    cout &lt;&lt; (search(root, 10) ? "found" : "not found") &lt;&lt; \'\\n\';    return 0;}</code></pre><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>操作</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>插入 <code>5、3、8、2、6</code> 后查找 6</td>\n<td>路径 <code>5→8→6</code>，查找成功</td>\n</tr>\n<tr>\n<td>边界</td>\n<td>空树中查找 5</td>\n<td>立即返回不存在</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>单结点树 5 中分别查找 5、3</td>\n<td>查找 5 成功；查找 3 走到左侧空位置后失败</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>上述树中查找不存在的 7</td>\n<td>路径 <code>5→8→6→空</code>，返回不存在</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>插入结果没有接回孩子</h3><div><div><strong><span>×</span> 错误写法</strong><code>insert(root-&gt;left, value);</code></div><div><strong><span>✓</span> 正确写法</strong><code>root-&gt;left = insert(root-&gt;left, value);</code></div></div><p><strong>原因：</strong>空位置创建的新结点返回后没有保存，会丢失。</p><p><strong>检查方法：</strong>递归插入返回更新后的子树根。</p></section><section><h3>把任意二叉树都按大小方向查找</h3><div><div><strong><span>×</span> 错误写法</strong><code>普通二叉树也根据目标小就向左、大就向右</code></div><div><strong><span>✓</span> 正确写法</strong><code>先确认整棵树满足二叉排序树规则，再使用比较搜索</code></div></div><p><strong>原因：</strong>只有二叉排序树保证左小右大，普通二叉树没有这个性质。</p><p><strong>检查方法：</strong>中序严格递增是本页约定下的一种检查方法。</p></section><h2 id="related-learning">关联学习</h2><p>二叉排序树的结构性质、中序有序结论见“03 特殊二叉树”；普通二叉树遍历见“02 树的构造与遍历”。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“简单动态规划”：记录子问题答案、状态定义、转移、初始化、计算顺序和答案位置，覆盖数列 DP、爬楼梯、简单路径计数和最大连续和入门。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-one-dimensional-dp-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-one-dimensional-dp-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">2 个子页</span></header><div><article><div><span class="astro-aidf4k4y">08</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/one-dimensional-dp/">简单动态规划</a></h3><p class="astro-aidf4k4y">按状态定义、初值、转移、顺序和答案五步建立一维动态规划。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/one-dimensional-dp/dp-basics/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">动态规划基础</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/one-dimensional-dp/linear-dp/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">线性动态规划</strong></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 状态定义</h2><p>动态规划保存已经求出的子问题答案，避免重复计算。第一步必须写清 <code>dp[i]</code> 的唯一含义，例如“走到第 i 级台阶的方案数”。状态含义决定后面的所有公式。</p><h2 id="kp-02">02 初值与转移</h2><p>爬楼梯每次走 1 或 2 级时，<code>dp[1]=1、dp[2]=2</code>。到第 <code>i</code> 级的最后一步来自 <code>i-1</code> 或 <code>i-2</code>，两类方案互不重复，所以 <code>dp[i]=dp[i-1]+dp[i-2]</code>。</p><h2 id="kp-03">03 顺序与答案</h2><p>因为当前状态依赖更小下标，必须从小到大计算。题目问第 <code>n</code> 级，答案就是 <code>dp[n]</code>，不能凭数组习惯改成 <code>dp[n-1]</code>。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;int main() {    int n;    cin &gt;&gt; n;    int dp[46] = {};    // 初始化最小的两个状态，避免转移时访问不存在的下标    dp[1] = 1;    if (n &gt;= 2) {  // n 至少为 2 时，第二个初值才会成为有效状态        dp[2] = 2;    }    for (int i = 3; i &lt;= n; i++) {  // 从小到大计算每一级的方案数        dp[i] = dp[i - 1] + dp[i - 2];  // 最后一步分别来自前一级或前两级    }    cout &lt;&lt; dp[n] &lt;&lt; \'\\n\';    return 0;}</code></pre><p>限定 <code>1 &lt;= n &lt;= 45</code> 时答案在 <code>int</code> 范围内。时间与空间复杂度均为 <code>O(n)</code>。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>输入</th>\n<th>应得到的结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>边界</td>\n<td><code>n=1</code></td>\n<td>只有一步走 1 级，共 1 种</td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>n=2</code></td>\n<td>走 <code>1+1</code> 或直接走 2，共 2 种</td>\n</tr>\n<tr>\n<td>基础</td>\n<td><code>n=5</code></td>\n<td>状态依次为 <code>1、2、3、5、8</code>，答案 8</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>较大的 <code>n=45</code></td>\n<td>应在 <code>int</code> 范围内得到正确结果，并检查数组空间和溢出边界</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>状态定义与答案下标不一致</h3><div><div><strong><span>×</span> 错误写法</strong><code>定义到第 i 级的方案数，最后输出 dp[n-1]</code></div><div><strong><span>✓</span> 正确写法</strong><code>按定义输出 dp[n]，并处理最小输入</code></div></div><p><strong>原因：</strong>题目目标是第 n 级，答案位置应与定义一致。</p><p><strong>检查方法：</strong>公式写完后重新读一遍 dp[i] 表示什么。</p></section><section><h3>先套转移再补初值</h3><div><div><strong><span>×</span> 错误写法</strong><code>从 i=1 开始统一计算 dp[i]=dp[i-1]+dp[i-2]</code></div><div><strong><span>✓</span> 正确写法</strong><code>先确定不能由转移推出的最小状态，再从第一个可转移位置开始循环</code></div></div><p><strong>原因：</strong>最小状态没有更小的合法来源，会访问负下标或未定义状态。</p><p><strong>检查方法：</strong>把初始化、转移起点和数组下标放在一起检查。</p></section><h2 id="related-learning">关联学习</h2><p>下一页 01 背包把状态增加“物品选择”含义，并进一步讲解一维数组的更新方向。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<div data-level6-topic-hub="complex-dp"><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-complex-dp-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-complex-dp-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">4 个子页</span></header><div><article><div><span class="astro-aidf4k4y">09</span><span>本站拓展</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/complex-dp/">复杂动态规划</a></h3><p class="astro-aidf4k4y">在二维状态基础上理解滚动数组、最长上升子序列和最长公共子序列。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/complex-dp/two-dimensional-dp/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">二维动态规划</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/complex-dp/rolling-array/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">动态规划空间优化（滚动数组）</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/complex-dp/lis/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">03</span><strong class="astro-aidf4k4y">最长上升子序列 LIS</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/complex-dp/lcs/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">04</span><strong class="astro-aidf4k4y">最长公共子序列 LCS</strong><small class="astro-aidf4k4y">本站拓展</small></a></li></ul></article></div></section></gesp-course-grid><blockquote>\n<p>本站补充｜非 GESP 六级官方核心</p>\n</blockquote><p>本专题是本站六级教学体系的拓展阅读，不计入 GESP 六级官方核心进度，也不会创建复习状态或持久自测。</p><h2 id="学习前先确认">学习前先确认</h2><p>先能独立完成“状态含义、初值、转移、计算顺序、答案位置”五步，并理解二维数组下标。若这五步还不稳定，请先回到“动态规划基础”和“线性动态规划”。</p><h2 id="推荐顺序">推荐顺序</h2><ol>\n<li><strong>二维动态规划</strong>：先学会用两个下标描述子问题。</li>\n<li><strong>动态规划空间优化（滚动数组）</strong>：明确二维依赖后，再判断哪些旧状态可以丢弃。</li>\n<li><strong>最长上升子序列 LIS</strong>：练习“必须以某位置结尾”的一维状态。</li>\n<li><strong>最长公共子序列 LCS</strong>：回到两个前缀的二维状态，比较相等与不等两种转移。</li>\n</ol><p>滚动数组必须放在 LIS 前面：它不是一条孤立技巧，而是从二维依赖关系中推导出的空间优化方法。</p><h2 id="使用边界">使用边界</h2><p>这些页面帮助学生建立后续算法学习的完整框架，但不把本站补充描述成 GESP 六级官方核心。阅读任意子页不修改原 13 个父知识点的 <code>v1</code> 进度，也不会让原有 <code>13/13</code> 倒退。</p></div></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“简单背包问题”，包含背包容量、物品体积与价值、选择或不选择、二维写法、一维数组优化，以及最大价值、恰好装满、可行性和方案数的入门辨认。</p><gesp-course-grid data-status-idle="未复习" data-status-active="未复习" data-status-complete="已复习" class="astro-aidf4k4y gesp-level6-knowledge-grid"><section aria-labelledby="gesp-level6-knapsack-children-title" class="astro-aidf4k4y"><header><div class="astro-aidf4k4y"><h2 id="gesp-level6-knapsack-children-title" class="astro-aidf4k4y">本专题子知识点</h2><p class="astro-aidf4k4y">按建议顺序进入独立子页阅读。子页不创建复习状态或持久自测。</p></div><span class="astro-aidf4k4y">5 个子页</span></header><div><article><div><span class="astro-aidf4k4y">10</span><span data-course-status class="astro-aidf4k4y">未复习</span></div><h3 class="astro-aidf4k4y"><a href="/gesp/level6/knapsack/">背包动态规划</a></h3><p class="astro-aidf4k4y">理解 01 背包的容量、价值与选或不选，掌握二维推导和一维倒序更新。</p><ul class="astro-aidf4k4y"><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/knapsack/zero-one-knapsack/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">01</span><strong class="astro-aidf4k4y">01 背包</strong></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/knapsack/complete-knapsack/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">02</span><strong class="astro-aidf4k4y">完全背包</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/knapsack/multiple-knapsack/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">03</span><strong class="astro-aidf4k4y">多重背包</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/knapsack/binary-optimized-multiple-knapsack/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">04</span><strong class="astro-aidf4k4y">多重背包二进制优化</strong><small class="astro-aidf4k4y">本站拓展</small></a></li><li class="astro-aidf4k4y"><a data-level6-child-link href="/gesp/level6/knapsack/mixed-knapsack/" class="astro-aidf4k4y"><span class="astro-aidf4k4y">05</span><strong class="astro-aidf4k4y">混合背包</strong><small class="astro-aidf4k4y">本站拓展</small></a></li></ul></article></div></section></gesp-course-grid><h2 id="kp-01">01 背包模型</h2><p>01 背包有 <code>n</code> 件物品，每件最多选一次。物品有重量 <code>weight[item]</code> 和价值 <code>value[item]</code>，总重量不能超过容量 <code>limit</code>，常见目标是最大总价值。“01”表示当前物品只做不选或选一次两个决定。</p><h2 id="kp-02">02 二维状态推导</h2><p>定义 <code>dp[item][capacity]</code> 为只考虑前 <code>item</code> 件、容量上限为 <code>capacity</code> 时的最大价值。不选时读取上一行同容量；能选时再比较上一行剩余容量加当前价值：</p><pre><code>dp[item][capacity] = dp[item - 1][capacity];if (capacity &gt;= weight[item]) {    dp[item][capacity] = max(dp[item][capacity],        dp[item - 1][capacity - weight[item]] + value[item]);}</code></pre><h2 id="kp-03">03 一维倒序更新</h2><p>压缩后 <code>dp[capacity]</code> 保存当前已经处理物品的最优值。每件物品内部必须从大容量向小容量更新，确保读取的 <code>dp[capacity-weight]</code> 仍是加入当前物品前的旧状态。正序会让同一件物品在一轮中被重复选取。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;int main() {    int n, limit;    cin &gt;&gt; n &gt;&gt; limit;    int weight[105];    long long value[105], dp[10005] = {};    for (int i = 1; i &lt;= n; i++) {  // 读入每件物品的重量和价值        cin &gt;&gt; weight[i] &gt;&gt; value[i];    }    for (int item = 1; item &lt;= n; item++) {  // 依次决定每件物品选或不选        for (int capacity = limit; capacity &gt;= weight[item]; capacity--) {  // 倒序保证当前物品只选一次            dp[capacity] = max(dp[capacity],                dp[capacity - weight[item]] + value[item]);        }    }    cout &lt;&lt; dp[limit] &lt;&lt; \'\\n\';    return 0;}</code></pre><p>限定 <code>n&lt;=100、limit&lt;=10000</code>，时间复杂度 <code>O(n×limit)</code>，空间复杂度 <code>O(limit)</code>。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>物品与容量</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>边界</td>\n<td>容量不足，小于所有物品重量</td>\n<td>一个物品也不能选，最大价值为 0</td>\n</tr>\n<tr>\n<td>基础</td>\n<td>容量 5，物品 <code>(2,3)、(3,4)</code></td>\n<td>两件都选，最大价值为 7</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>容量足够，全部可选</td>\n<td>最大价值为所有物品价值之和</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>两件物品重量相同、价值不同</td>\n<td>受容量限制只能选一件时，应选价值更大的</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>01 背包容量正序更新</h3><div><div><strong><span>×</span> 错误写法</strong><code>capacity 从 weight[item] 增加到 limit</code></div><div><strong><span>✓</span> 正确写法</strong><code>每件物品内部从 limit 倒序到 weight[item]</code></div></div><p><strong>原因：</strong>本轮刚更新的小容量会被大容量再次读取，等于重复使用当前物品。</p><p><strong>检查方法：</strong>看到每件至多一次就检查更新方向。</p></section><section><h3>把最大价值状态当成恰好装满</h3><div><div><strong><span>×</span> 错误写法</strong><code>dp[capacity]=0 就说明这个容量一定能被恰好组成</code></div><div><strong><span>✓</span> 正确写法</strong><code>先辨认题目问不超过容量还是必须恰好装满；恰好装满需要单独表示不可达</code></div></div><p><strong>原因：</strong>最大价值模型中初值 0 只表示不选物品时价值为 0，不能证明恰好使用了该容量。</p><p><strong>检查方法：</strong>状态含义变化时，初始化和最终判断也必须一起变化。</p></section><h2 id="related-learning">关联学习</h2><p>如果还不能稳定写出状态含义、初值和答案位置，先回看“08 简单动态规划”。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页严格对应六级大纲“面向对象思想与类”：类、对象、属性、方法、封装、成员变量、成员函数、访问控制，以及学生类、点类、计数器类等简单应用。不把继承、虚函数和多态列为六级目标。</p><h2 id="kp-01">01 类、对象与封装</h2><p>类描述一类事物共同的数据和操作，对象是按类创建出的具体实例。类中的数据叫成员变量（属性），类中的操作叫成员函数（方法）。把数据与相关操作放在一起，并限制外部直接修改内部数据，就是封装。</p><h2 id="kp-02">02 类的创建与访问</h2><p>用 <code>class</code> 定义类，右花括号后必须有分号。<code>public</code> 成员可从类外通过对象访问；<code>private</code> 成员只能由本类成员函数直接访问。类外需要读取私有数据时，应由类提供公开成员函数。</p><h2 id="kp-03">03 构造函数与简单应用</h2><p>构造函数与类同名，没有返回类型，创建对象时自动调用，常用来设置成员初值。简单类应围绕一个明确职责，例如计数器保存当前数值并提供增加、读取操作。</p><p>本页程序中的 <code>Student(string studentName, int studentScore)</code> 逐项含义是：<code>Student</code> 与类同名，所以它是构造函数；圆括号内两个形参接收创建对象时给出的姓名和成绩；冒号后的初始化列表 <code>name(studentName), score(studentScore)</code> 在进入函数体前分别初始化两个成员，不能省略逗号。也可以在构造函数体内写赋值，但初始化列表更直接。</p><p><code>void show() const</code> 中，前面的 <code>void</code> 表示函数不返回结果，<code>show</code> 是成员函数名，空括号表示调用时不传参数，函数末尾的 const 表示这个成员函数承诺不修改当前对象。若函数需要修改 <code>score</code>，就不能保留这个末尾 <code>const</code>。这些位置的作用不同，不能把构造函数误写成带返回类型的普通函数。</p><h2 id="quick-table">类成员速查</h2><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nclass Student {private:    string name;    int score;\npublic:    Student(string studentName, int studentScore)        : name(studentName), score(studentScore) {  // 创建对象时保存初始姓名和成绩    }\n    void setScore(int newScore) {        score = newScore;  // 通过公开成员函数安全地修改私有成绩    }\n    void show() const {        cout &lt;&lt; name &lt;&lt; \' \' &lt;&lt; score &lt;&lt; \'\\n\';    }};\nint main() {    Student student("Xiao Ming", 90);    student.setScore(95);    student.show();    return 0;}</code></pre><p>程序输出 <code>Xiao Ming 95</code>。类把姓名、成绩以及修改和显示操作组织在一起，类外不直接接触私有数据。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>操作</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>创建 <code>Student("Xiao Ming", 90)</code></td>\n<td>对象保存姓名 <code>Xiao Ming</code> 和成绩 90</td>\n</tr>\n<tr>\n<td>边界</td>\n<td>创建一个学生后不修改</td>\n<td>显示结果保持构造时的初值</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>创建两个 Student 对象</td>\n<td>两个对象各自保存数据，修改其中一个不影响另一个</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>对对象调用 <code>setScore(95)</code></td>\n<td>通过公开函数完成修改成绩，不能从类外直接写 private 成员</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>从类外直接访问 private 成员</h3><div><div><strong><span>×</span> 错误写法</strong><code>cout &lt;&lt; student.score;</code></div><div><strong><span>✓</span> 正确写法</strong><code>由类提供 public 的读取或显示成员函数</code></div></div><p><strong>原因：</strong>score 位于 private 区域，类外不能直接读取。</p><p><strong>检查方法：</strong>看到 private，先寻找类提供的公开操作。</p></section><section><h3>把类定义当成已经创建对象</h3><div><div><strong><span>×</span> 错误写法</strong><code>写完 class Student 后，直接使用 student.show()，却没有声明 student</code></div><div><strong><span>✓</span> 正确写法</strong><code>先声明并初始化 Student 对象，再通过对象调用公开成员函数</code></div></div><p><strong>原因：</strong>类只是共同结构的说明，只有创建对象后才有一份具体数据。</p><p><strong>检查方法：</strong>区分“定义一种类型”和“创建一个具体实例”。</p></section><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“栈”：后进先出、入栈、出栈、栈顶、STL <code>stack</code> 的基本操作，以及括号匹配和撤销过程等简单应用。</p><h2 id="kp-01">01 后进先出与基本操作</h2><p>栈只在同一端加入和删除，遵守后进先出。<code>push(value)</code> 入栈，<code>top()</code> 读取栈顶但不删除，<code>pop()</code> 删除栈顶但不返回值，<code>size()</code> 返回元素数量。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;int main() {    string text;    cin &gt;&gt; text;    stack&lt;char&gt; brackets;    bool matched = true;    for (int i = 0; i &lt; (int)text.size(); i++) {  // 从左到右检查每个括号        if (text[i] == \'(\') {  // 左括号暂时没有配对，需要压入栈中等待            brackets.push(text[i]);        } else {  // 当前是右括号，需要匹配最近的左括号            if (brackets.empty()) {  // 没有可配对的左括号，立即判为不匹配                matched = false;                break;            }            brackets.pop();        }    }    if (!brackets.empty()) {  // 检查结束仍有左括号，说明它们没有配对        matched = false;    }    if (matched) {  // 所有括号都完成配对，输出匹配成功        cout &lt;&lt; "YES\\n";    } else {  // 出现多余括号或最终仍有左括号，输出不匹配        cout &lt;&lt; "NO\\n";    }    return 0;}</code></pre><p>输入只包含字符 <code>(</code> 和 <code>)</code>。输入 <code>(())()</code> 输出 <code>YES</code>；输入 <code>(()</code> 或 <code>)(</code> 输出 <code>NO</code>。程序只处理圆括号，时间复杂度 <code>O(n)</code>。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>输入或操作</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>边界</td>\n<td>空栈直接读取 <code>top()</code></td>\n<td>操作不安全，必须先判断 <code>empty()</code></td>\n</tr>\n<tr>\n<td>基础</td>\n<td>单个元素入栈后读取并出栈</td>\n<td>读到该元素，出栈后为空</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>多个元素依次入栈</td>\n<td>出栈顺序与入栈顺序相反</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>括号串 <code>(())()</code></td>\n<td>括号匹配，最终栈为空</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>括号串 <code>)(</code></td>\n<td>第一个右括号出现时栈为空，立即判错</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>把 pop 当作返回值函数</h3><div><div><strong><span>×</span> 错误写法</strong><code>int x = values.pop();</code></div><div><strong><span>✓</span> 正确写法</strong><code>int x = values.top(); values.pop();</code></div></div><p><strong>原因：</strong>标准栈 pop 只删除，不返回被删除元素。</p><p><strong>检查方法：</strong>读取和删除分成两步，并先判空。</p></section><section><h3>只检查过程中没有多余右括号</h3><div><div><strong><span>×</span> 错误写法</strong><code>扫描结束就判定 (() 匹配</code></div><div><strong><span>✓</span> 正确写法</strong><code>过程中防止空栈弹出，结束后还要确认栈为空</code></div></div><p><strong>原因：</strong>虽然过程中每个右括号都有对应左括号，但结束时栈里仍剩一个未配对左括号。</p><p><strong>检查方法：</strong>括号匹配有两个边界：右括号不能过多，左括号也不能剩余。</p></section><h2 id="related-learning">关联学习</h2><p>DFS 的递归调用过程也具有后进先出的返回顺序，但六级 DFS 主代码可以直接使用递归。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“队列”：先进先出、入队、出队、队首、队尾、STL <code>queue</code> 的基本操作和简单任务排队。固定数组的回绕复用放在下一页“循环队列”。</p><h2 id="kp-01">01 先进先出与基本操作</h2><p>队列从队尾加入、从队首删除，遵守先进先出。<code>push(value)</code> 入队，<code>front()</code> 读取队首，<code>back()</code> 读取队尾，<code>pop()</code> 删除队首，<code>size()</code> 返回元素数量。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;int main() {    int n;    cin &gt;&gt; n;    queue&lt;int&gt; waiting;    for (int i = 1; i &lt;= n; i++) {  // 按到达顺序把每个人加入队尾        int person;        cin &gt;&gt; person;        waiting.push(person);    }    while (!waiting.empty()) {  // 每轮处理并删除当前队首的一人        cout &lt;&lt; waiting.front() &lt;&lt; \'\\n\';        waiting.pop();    }    return 0;}</code></pre><p>输入 <code>3 2 5 3</code>，输出依次为 <code>2、5、3</code>。每次入队和出队可看作 <code>O(1)</code>。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>输入或操作</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>边界</td>\n<td>空队列读取 <code>front()</code> 或执行 <code>pop()</code></td>\n<td>操作不安全，必须先判断 <code>empty()</code></td>\n</tr>\n<tr>\n<td>基础</td>\n<td>单个元素 7 入队后出队</td>\n<td>队首和队尾都为 7，出队后为空</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>多个元素 <code>2、5、3</code> 依次入队</td>\n<td>出队顺序仍为 <code>2、5、3</code></td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>处理中途又有人到达</td>\n<td>新元素加入队尾，不影响当前队首</td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>混淆 front 与 back</h3><div><div><strong><span>×</span> 错误写法</strong><code>用 back() 读取下一位应处理的任务</code></div><div><strong><span>✓</span> 正确写法</strong><code>处理时读取 front()，观察最后一项时才用 back()</code></div></div><p><strong>原因：</strong>下一位应处理的是最先进入的队首，而 back 是最后进入的队尾。</p><p><strong>检查方法：</strong>画出队首和队尾方向再读代码。</p></section><section><h3>读取队首后忘记出队</h3><div><div><strong><span>×</span> 错误写法</strong><code>循环中反复输出 front()，但没有调用 pop()</code></div><div><strong><span>✓</span> 正确写法</strong><code>需要完成一次处理时，先读取 front()，再调用 pop() 删除它</code></div></div><p><strong>原因：</strong>队首元素一直留在队列中，下一轮仍会处理同一个人，循环无法结束。</p><p><strong>检查方法：</strong>front 只读取，pop 才会改变队列。</p></section><h2 id="related-learning">关联学习</h2><p>“06 宽度优先搜索 BFS”把队列用于分层扩展；下一页讲固定数组上的循环队列。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="official-position" class="astro-shsevxoi">知识范围与官方定位</h2><p>本页对应六级大纲“循环队列”：数组模拟、头尾指针、环形移动、判空判满、固定容量队列与环形报数入门。统一采用空一格合同，不混用计数法。</p><h2 id="kp-01">01 环形数组与下标</h2><p>普通数组队列若只让 <code>head</code>、<code>tail</code> 一直向右移动，前面的元素出队后，即使数组左侧已经空出位置，<code>tail</code> 到达末尾仍无法继续写入，造成空间浪费。循环队列把数组首尾看成相连：<code>head</code> 指向当前队首元素，<code>tail</code> 指向下一个可写位置。下标前进一步统一写成 <code>(index + 1) % capacity</code>，到达数组末尾后会回到 0，从而复用已经出队的位置。</p><h2 id="kp-02">02 空一格判空判满</h2><p>空一格法规定：<code>head == tail</code> 表示空；<code>(tail + 1) % capacity == head</code> 表示满。数组长度为 <code>capacity</code> 时只能保存 <code>capacity-1</code> 个元素。预留的一格用于区分空与满。</p><h2 id="kp-03">03 入队与出队</h2><p>入队先判满，把值写到 <code>data[tail]</code>，再移动 <code>tail</code>；出队先判空，读取 <code>data[head]</code>，再移动 <code>head</code>。每次操作都是 <code>O(1)</code>。</p><pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;int main() {    const int capacity = 5;    int data[capacity] = {};    int head = 0, tail = 0;    int input[4] = {10, 20, 30, 40};    for (int i = 0; i &lt; 4; i++) {  // 按顺序尝试把四个数加入循环队列        if ((tail + 1) % capacity != head) {  // 下一尾位置没有追上 head，说明队列未满            data[tail] = input[i];            tail = (tail + 1) % capacity;  // 写入后移动 tail，必要时回绕到 0        }    }    while (head != tail) {  // 队列非空时依次读取并删除队首        cout &lt;&lt; data[head] &lt;&lt; \' \';        head = (head + 1) % capacity;  // 删除后移动 head，必要时回绕到 0    }    cout &lt;&lt; \'\\n\';    return 0;}</code></pre><p>固定输出 <code>10 20 30 40</code>，数组空间为 <code>O(capacity)</code>。</p><h3 id="测试案例">测试案例</h3><table>\n<thead>\n<tr>\n<th>类型</th>\n<th>初始状态或操作</th>\n<th>应得到的关键结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>边界</td>\n<td><code>head==tail</code></td>\n<td>队列为空，不能读取或出队</td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>(tail+1)%capacity==head</code></td>\n<td>队列已满，不能继续入队</td>\n</tr>\n<tr>\n<td>基础</td>\n<td>容量 5，依次入队 10、20</td>\n<td><code>head=0、tail=2</code>，队首为 10</td>\n</tr>\n<tr>\n<td>特殊</td>\n<td>入队 10、20、30、40，出队两次，再入队 50、60</td>\n<td>tail 发生回绕，逻辑顺序为 <code>30、40、50、60</code></td>\n</tr>\n</tbody>\n</table><h2 id="common-errors">常见错误</h2><section><h3>混用空一格法与计数法</h3><div><div><strong><span>×</span> 错误写法</strong><code>一边用 head==tail 判空，一边用 count==capacity 判满</code></div><div><strong><span>✓</span> 正确写法</strong><code>本页只用 head==tail 判空、下一 tail 追上 head 判满</code></div></div><p><strong>原因：</strong>两套合同的状态含义不同，混用会产生矛盾边界。</p><p><strong>检查方法：</strong>先写合同，再写入队出队。</p></section><section><h3>移动下标时忘记回绕</h3><div><div><strong><span>×</span> 错误写法</strong><code>tail++，到达数组末尾后仍继续增加</code></div><div><strong><span>✓</span> 正确写法</strong><code>头尾每次前进都写成 (index + 1) % capacity</code></div></div><p><strong>原因：</strong>下标会离开数组范围，也无法复用前面已经出队的位置。</p><p><strong>检查方法：</strong>入队检查 tail 的移动，出队检查 head 的移动。</p></section><h2 id="related-learning">关联学习</h2><p>标准队列接口和先进先出规则见“12 队列”。循环报数可用队列模拟：把 <code>1、2、3、4、5</code> 依次入队，每次取出队首；若本次报数不是 2 的倍数，就把它重新放回队尾，否则让它出列。状态依次为 <code>2,3,4,5,1</code>、出列 2、<code>4,5,1,3</code>、出列 4……最终出列顺序为 <code>2、4、1、5、3</code>。重新入队只改变逻辑顺序，底层数组仍按本页的回绕规则复用空间，不改变判空判满合同。</p><h2 id="core-check" class="astro-shsevxoi">核心内容检查</h2><h2 id="page-test-title" class="astro-shsevxoi">整页综合测试</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
    ],
    7: [
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握数学库中的三角函数、对数函数、指数函数、平方根、绝对值和取整函数。本页只讨论常见 OJ 输入范围内的安全调用、定义域和返回值，不研究函数内部实现。完整程序使用 C++ 标准数学函数，页面不要求记忆某个平台的近似常数。</p>\n<p>当题目出现角度、距离、增长倍数、数量级估算或误差要求时，应先判断所需函数，再检查单位、定义域和输出精度。数学库函数解决的是“已有数学模型怎样可靠计算”，不能代替建模本身。</p>\n<h2 id="kp-01">01 三角函数与弧度</h2>\n<p><code>sin(x)</code>、<code>cos(x)</code>、<code>tan(x)</code> 的参数是<strong>弧度</strong>。角度 <code>degree</code> 要先换算：</p>\n<pre><code>double radian = degree * acos(-1.0) / 180.0;double answer = sin(radian);</code></pre>\n<p><code>acos(-1.0)</code> 得到圆周率 π。以 <code>90°</code> 为例，换算后是 <code>π/2</code>，正弦约为 <code>1</code>。<code>asin</code>、<code>acos</code> 的输入必须在 <code>[-1, 1]</code> 内，返回的也是弧度。</p>\n<p><strong>具体推演：</strong> 输入 <code>degree = 30</code> 时，先算 <code>radian = 30 × π / 180 = π/6</code>，再调用 <code>sin(π/6)</code>，得到约 <code>0.5</code>。如果误写成 <code>sin(30)</code>，函数会把 <code>30</code> 当成弧度，结果约为 <code>-0.988</code>，并不是 <code>30°</code> 的正弦值。结论是：只要题目给的是角度，就必须先换成弧度。</p>\n<h2 id="kp-02">02 对数函数与指数函数</h2>\n<p><code>log(x)</code> 表示以 <code>e</code> 为底的自然对数，<code>log10(x)</code> 表示以 <code>10</code> 为底的常用对数，<code>log2(x)</code> 表示以 <code>2</code> 为底的对数；三者都要求 <code>x &gt; 0</code>。<code>exp(x)</code> 表示 <code>e</code> 的 <code>x</code> 次幂，<code>pow(a, b)</code> 表示 <code>a</code> 的 <code>b</code> 次幂。调用对数函数前必须先判断定义域，较大的 <code>x</code> 传给 <code>exp</code> 还可能溢出。</p>\n<p>例如 <code>log(1.0)</code> 为 <code>0</code>，<code>log10(1000.0)</code> 为 <code>3</code>，<code>log2(8.0)</code> 为 <code>3</code>；而 <code>log(0.0)</code> 在实数范围内没有定义。不能先调用函数再检查结果。</p>\n<p><strong>具体推演：</strong> 若一个数量每轮翻倍，从 <code>1</code> 增长到 <code>32</code>，需要的轮数可以用 <code>log2(32) = 5</code> 判断，因为 <code>2⁵ = 32</code>。调用顺序是先检查 <code>32 &gt; 0</code>，再调用 <code>log2</code>，最后按题意取用结果；若输入改为 <code>0</code>，定义域检查失败，应直接报告无定义，而不是继续计算。</p>\n<h2 id="kp-03">03 平方根、绝对值、幂与取整</h2>\n<table>\n<thead>\n<tr>\n<th>函数</th>\n<th>含义</th>\n<th>关键条件</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>sqrt(x)</code></td>\n<td>非负平方根</td>\n<td><code>x &gt;= 0</code></td>\n</tr>\n<tr>\n<td><code>fabs(x)</code></td>\n<td>浮点数绝对值</td>\n<td>返回非负值</td>\n</tr>\n<tr>\n<td><code>pow(a, b)</code></td>\n<td><code>a</code> 的 <code>b</code> 次幂</td>\n<td>注意范围与浮点结果</td>\n</tr>\n<tr>\n<td><code>ceil(x)</code></td>\n<td>不小于 <code>x</code> 的最小整数值</td>\n<td>向正无穷方向</td>\n</tr>\n<tr>\n<td><code>floor(x)</code></td>\n<td>不大于 <code>x</code> 的最大整数值</td>\n<td>向负无穷方向</td>\n</tr>\n</tbody>\n</table>\n<p>因此 <code>ceil(-2.3) == -2</code>，<code>floor(-2.3) == -3</code>。两者都不是“简单删除小数部分”。</p>\n<p><strong>具体推演：</strong> 对 <code>x = -2.3</code>，数轴上不小于它的整数有 <code>-2、-1、0……</code>，其中最小的是 <code>-2</code>，所以 <code>ceil(x) = -2</code>；不大于它的整数有 <code>……-4、-3</code>，其中最大的是 <code>-3</code>，所以 <code>floor(x) = -3</code>。同一数据同时推演两种方向，就不会把它们误解成“去掉小数”。</p>\n<h2 id="kp-04">04 浮点结果与允许误差</h2>\n<p><code>double</code> 保存近似值，数学上的 <code>0</code> 可能算成极小数。判断两个浮点结果是否足够接近时，可写：</p>\n<pre><code>const double EPS = 1e-9;if (fabs(calculated - expected) &lt; EPS) {    cout &lt;&lt; "equal" &lt;&lt; endl;}</code></pre>\n<p><code>fixed &lt;&lt; setprecision(6)</code> 只控制显示为小数点后 6 位，不会提高计算精度。</p>\n<p><strong>具体推演：</strong> 假设计算得到 <code>calculated = 0.30000000000000004</code>，期望值是 <code>0.3</code>，两者直接用 <code>==</code> 可能判为不等；差值绝对值约为 <code>4 × 10⁻¹⁷</code>，小于 <code>1e-9</code>，因此允许误差判断得到“足够接近”。随后用 <code>setprecision(2)</code> 输出为 <code>0.30</code>，只是改变显示结果，内存中的近似值并未变成精确的 <code>0.3</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<p>先检查单位和定义域，再调用函数；最后按题目误差要求比较或输出。<code>sqrt</code> 允许零，<code>log</code> 不允许零；三角函数使用弧度；<code>ceil</code> 与 <code>floor</code> 在负数处最容易混淆。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>计算 <code>sin(30°)</code>、<code>log10(1000)</code> 和 <code>ceil(-2.3)</code>：</p>\n<ol>\n<li><code>30°</code> 先换成 <code>π/6</code>，所以 <code>sin(π/6)</code> 约为 <code>0.5</code>；</li>\n<li><code>10³ = 1000</code>，所以 <code>log10(1000) = 3</code>；</li>\n<li>不小于 <code>-2.3</code> 的最小整数是 <code>-2</code>，所以 <code>ceil(-2.3) = -2</code>。</li>\n</ol>\n<p>这类题先判断单位和定义域，再代入函数，不需要编写复杂程序。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>读入角度与实数，输出正弦、平方根或未定义：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    double degree = 0.0;    double x = 0.0;    cin &gt;&gt; degree &gt;&gt; x;    // 标准三角函数接收弧度，先把输入的角度转换为弧度。    double radian = degree * acos(-1.0) / 180.0;    // fixed 与 setprecision 只控制输出格式，不改变 double 的计算精度。    cout &lt;&lt; fixed &lt;&lt; setprecision(6) &lt;&lt; sin(radian) &lt;&lt; endl;    // sqrt 的实数定义域要求 x &gt;= 0，必须在调用前完成判断。    if (x &gt;= 0.0) {        cout &lt;&lt; sqrt(x) &lt;&lt; endl;    } else {        cout &lt;&lt; "undefined" &lt;&lt; endl;    }    return 0;}</code></pre>\n<p>输入 <code>90 4</code> 时输出约为 <code>1.000000</code> 和 <code>2.000000</code>。程序只做常数次计算，时间和额外空间复杂度均为 <code>O(1)</code>。</p>\n<h3 id="调用过程">调用过程</h3>\n<p>输入 <code>90 4</code> 时，程序依次完成：</p>\n<table>\n<thead>\n<tr>\n<th>步骤</th>\n<th>当前数据</th>\n<th>处理结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>读取输入</td>\n<td><code>degree=90</code>，<code>x=4</code></td>\n<td>等待检查与换算</td>\n</tr>\n<tr>\n<td>角度转弧度</td>\n<td><code>90 × π / 180</code></td>\n<td><code>radian=π/2</code></td>\n</tr>\n<tr>\n<td>调用三角函数</td>\n<td><code>sin(π/2)</code></td>\n<td>约为 <code>1</code></td>\n</tr>\n<tr>\n<td>检查平方根定义域</td>\n<td><code>4 &gt;= 0</code></td>\n<td>可以调用 <code>sqrt(4)</code></td>\n</tr>\n<tr>\n<td>格式化输出</td>\n<td>保留 6 位小数</td>\n<td><code>1.000000</code>、<code>2.000000</code></td>\n</tr>\n</tbody>\n</table>\n<p>如果输入为 <code>0 -1</code>，正弦仍可计算，但 <code>-1</code> 不满足平方根的实数定义域，第二行应输出 <code>undefined</code>。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>90 4</code></td>\n<td>正弦为 <code>1.000000</code>，平方根为 <code>2.000000</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>0 0</code></td>\n<td>正弦与平方根都为 <code>0.000000</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td><code>180 -1</code></td>\n<td>角度先转弧度；平方根输出 <code>undefined</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>sin(90)</code> 直接把角度传入</td>\n<td>标准三角函数把 <code>90</code> 当作弧度</td>\n<td>先计算 <code>90 × π / 180</code></td>\n</tr>\n<tr>\n<td>认为 <code>x &gt;= 0</code> 就能调用 <code>log(x)</code></td>\n<td>对数要求参数严格大于 <code>0</code></td>\n<td>调用前检查 <code>x &gt; 0</code></td>\n</tr>\n<tr>\n<td>用 <code>calculated == expected</code> 比较计算结果</td>\n<td>浮点运算可能产生微小舍入误差</td>\n<td>比较 <code>fabs(calculated-expected) &lt; EPS</code></td>\n</tr>\n<tr>\n<td>认为 <code>ceil(-2.3)=-3</code></td>\n<td><code>ceil</code> 朝正无穷方向取整</td>\n<td>区分 <code>ceil(-2.3)=-2</code> 与 <code>floor(-2.3)=-3</code></td>\n</tr>\n<tr>\n<td>认为 <code>setprecision</code> 提升计算精度</td>\n<td>它只改变显示格式</td>\n<td>用 <code>double</code> 计算，再按题意格式化输出</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>把 <code>90°</code> 直接写成 <code>sin(90)</code> 时，程序计算的是 <code>90</code> 弧度，输出约 <code>0.894</code>，而正确过程是先算 <code>90 × π / 180 = π/2</code>，再得到 <code>sin(π/2) = 1</code>。同一个输入产生明显不同结果，说明“角度未转弧度”不是格式问题，而是传给函数的数据含义已经错了。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>数学函数常用于几何计算和数值处理；动态规划与图论页面只有在题目确实需要时才调用它们，不能为了使用函数而强行套入算法。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲把复杂动态规划列为独立知识点。这里的“复杂”不是追求难写，而是指一个状态需要记录更多信息，或转移要按阶段、区间长度等特定顺序进行。本页在基础 DP 上继续扩展，每次都要说明状态含义、初始状态、状态转移、计算顺序和答案位置。</p>\n<p><strong>什么时候使用复杂 DP？</strong> 当一个下标不能完整描述问题、当前阶段只能依赖上一阶段，或者答案由区间内部的分割方式决定时，应考虑增加状态维度、分离阶段或使用区间 DP。先确认确实缺少必要信息，再增加维度，不能看到二维数组就直接套模板。</p>\n<h2 id="kp-01">01 复杂状态设计</h2>\n<p>当 <code>dp[i]</code> 无法区分后续会用到的情况时，需要增加一维状态。例如 <code>dp[i][0]</code> 与 <code>dp[i][1]</code> 可以分别表示处理到第 <code>i</code> 个位置且“未选择/已选择”某种状态。每一维都必须能用一句完整的话解释，不能先写数组再猜含义。</p>\n<p>状态设计的判断顺序是：答案需要什么信息；这些信息能否由更小问题得到；转移时是否还缺少条件。缺少的必要信息才进入状态，和未来无关的信息不应保留。</p>\n<p><strong>具体推演：</strong> 某条路线有一次“免付通行费”机会，到达位置 <code>i</code> 时即使总代价同为 <code>8</code>，仍要区分机会是否已经使用。可以令 <code>dp[i][0] = 8</code> 表示机会未用、<code>dp[i][1] = 8</code> 表示机会已用；下一段费用为 <code>5</code> 时，前者还能选择免费通过，后者只能变成 <code>13</code>。只记录一个 <code>dp[i] = 8</code> 会丢失影响后续选择的信息。</p>\n<h2 id="kp-02">02 分阶段转移</h2>\n<p>有些问题天然分成若干阶段。第 <code>i</code> 阶段的状态只能从已经完成的阶段转移，不能在同一轮错误读取刚更新、原本属于下一阶段的数据。</p>\n<p>常见写法使用两个数组：<code>previous</code> 保存上一阶段，<code>current</code> 计算当前阶段；一轮结束后再交换或复制。这样牺牲少量代码长度，却能清楚保证依赖关系。</p>\n<p><strong>具体推演：</strong> 假设上一阶段只有 <code>previous[0] = 1</code>、<code>previous[1] = 4</code>，当前阶段规定 <code>current[j] = previous[j] + previous[j - 1]</code>。先得到 <code>current[0] = 1</code>，再得到 <code>current[1] = 4 + 1 = 5</code>；整个计算始终读取旧数组。若直接原地把 <code>previous[0]</code> 改成别的当前值，后面的状态就可能读到“半新半旧”的数据。</p>\n<h2 id="kp-03">03 区间动态规划</h2>\n<p>区间 DP 用 <code>dp[left][right]</code> 表示闭区间 <code>[left, right]</code> 的答案。短区间是长区间的子问题，所以计算顺序必须按区间长度从小到大：</p>\n<pre><code>for (int length = 2; length &lt;= n; length++) {    for (int left = 1; left + length - 1 &lt;= n; left++) {        int right = left + length - 1;        for (int split = left; split &lt; right; split++) {            // 用 [left, split] 和 [split + 1, right] 更新当前区间        }    }}</code></pre>\n<p>分割点必须满足 <code>left &lt;= split &lt; right</code>，否则会产生空区间或重复当前区间。</p>\n<p><strong>具体推演：</strong> 区间 <code>[1,3]</code> 的长度为 <code>3</code>，合法分割点只有 <code>1</code> 和 <code>2</code>：分别拆成 <code>[1,1] + [2,3]</code>、<code>[1,2] + [3,3]</code>。必须先算长度为 <code>1、2</code> 的状态，才能比较这两个候选；若把 <code>split = 3</code> 也算进去，右段会变成空区间 <code>[4,3]</code>，已经不符合原问题。</p>\n<h2 id="kp-04">04 计算顺序、初值与不可达状态</h2>\n<p>动态规划不是“循环越多越复杂”，关键是每次读取的子状态已经算好。求最小值时，可把暂不可达状态初始化为足够大的 <code>INF</code>，但做加法前要确认它不是 <code>INF</code>，避免溢出。区间长度为 <code>1</code> 的边界通常单独初始化。</p>\n<p><strong>具体推演：</strong> 设 <code>INF = 1000000000</code>，状态 <code>dp[2]</code> 暂不可达，转移费用为 <code>7</code>。此时不能把 <code>INF + 7</code> 当作候选值，因为它不代表一条真实路线；程序应先判断 <code>dp[2] != INF</code>。若另一条可达路线代价为 <code>12</code>，当前最小值就应更新为 <code>12</code>，而不是让“不可达哨兵”参与比较。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>优先检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>一个下标无法区分情况</td>\n<td>是否缺少必要状态维度</td>\n</tr>\n<tr>\n<td>同一轮更新互相污染</td>\n<td>是否应分开上一阶段和当前阶段</td>\n</tr>\n<tr>\n<td>左右端点共同决定答案</td>\n<td>是否适合区间状态</td>\n</tr>\n<tr>\n<td>正确转移却得到旧值</td>\n<td>计算顺序和初值是否正确</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>区间中只有两堆石子 <code>4 6</code> 时，只能把它们合并一次，代价就是 <code>4 + 6 = 10</code>。因此 <code>dp[1][1] = dp[2][2] = 0</code>，<code>dp[1][2] = 10</code>。这个最小例子能直接核对“长度为 1 不需要合并”和“长度为 2 只有一个分割点”两个边界。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>石子合并的基础模型：每次合并相邻两堆，代价为区间石子总数，求最小总代价。设 <code>sum[i]</code> 为前缀和，<code>dp[left][right]</code> 为合并区间的最小代价：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    int stone[205] = {};    int sum[205] = {};    int dp[205][205] = {};    cin &gt;&gt; n;    for (int i = 1; i &lt;= n; i++) {        cin &gt;&gt; stone[i];        // sum[i] 保存前 i 堆石子的总重量，用来 O(1) 求区间重量。        sum[i] = sum[i - 1] + stone[i];    }    // 长区间依赖更短区间，因此必须按区间长度从小到大计算。    for (int length = 2; length &lt;= n; length++) {        for (int left = 1; left + length - 1 &lt;= n; left++) {            int right = left + length - 1;            dp[left][right] = 1000000000;            // 枚举最后一次合并的分割点，左右两段都必须非空。            for (int split = left; split &lt; right; split++) {                int cost = dp[left][split] + dp[split + 1][right]                    + sum[right] - sum[left - 1];                dp[left][right] = min(dp[left][right], cost);            }        }    }    // 完整区间 [1,n] 的状态就是最终最小合并代价。    cout &lt;&lt; dp[1][n] &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入 <code>3</code> 和 <code>1 2 3</code> 时输出 <code>9</code>。外层按长度递增，保证两个子区间已经求出。三层循环的时间复杂度为 <code>O(n^3)</code>，空间复杂度为 <code>O(n^2)</code>。</p>\n<h3 id="状态追踪">状态追踪</h3>\n<p>对石子 <code>1 2 3</code>，先算短区间，再算长区间：</p>\n<table>\n<thead>\n<tr>\n<th>计算阶段</th>\n<th>候选计算</th>\n<th>当前状态</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>长度 1</td>\n<td>单堆不需要合并</td>\n<td><code>dp[1][1] = dp[2][2] = dp[3][3] = 0</code></td>\n</tr>\n<tr>\n<td>长度 2</td>\n<td><code>1 + 2 = 3</code></td>\n<td><code>dp[1][2] = 3</code></td>\n</tr>\n<tr>\n<td>长度 2</td>\n<td><code>2 + 3 = 5</code></td>\n<td><code>dp[2][3] = 5</code></td>\n</tr>\n<tr>\n<td>长度 3，分割点 1</td>\n<td><code>0 + 5 + 6 = 11</code></td>\n<td>候选值 <code>11</code></td>\n</tr>\n<tr>\n<td>长度 3，分割点 2</td>\n<td><code>3 + 0 + 6 = 9</code></td>\n<td><code>dp[1][3] = 9</code></td>\n</tr>\n</tbody>\n</table>\n<p>最后一次合并的区间重量总是 <code>1 + 2 + 3 = 6</code>，区别在于左右子区间此前已经付出的代价。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>n = 2</code>，石子为 <code>4 6</code></td>\n<td>输出 <code>10</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>n = 1</code>，石子为 <code>7</code></td>\n<td>输出 <code>0</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td><code>n = 3</code>，石子为 <code>1 2 3</code></td>\n<td>输出 <code>9</code>，不能按固定从左到右得到 <code>11</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>状态只写“前 <code>i</code> 个的答案”</td>\n<td>没有说明其余维度，转移无法核对</td>\n<td>用完整句子说明每一维和状态值的含义</td>\n</tr>\n<tr>\n<td>区间 DP 直接按左端点递增</td>\n<td>可能读取尚未计算的长区间</td>\n<td>按区间长度从小到大计算</td>\n</tr>\n<tr>\n<td>分割点写成 <code>split &lt;= right</code></td>\n<td>会产生空的右区间</td>\n<td>保证 <code>left &lt;= split &lt; right</code></td>\n</tr>\n<tr>\n<td>求最小值时把初值设为 <code>0</code></td>\n<td><code>0</code> 会被错误选成最小值</td>\n<td>初始化为足够大的合法上界</td>\n</tr>\n<tr>\n<td>对不可达的极大值继续相加</td>\n<td>可能溢出并产生伪答案</td>\n<td>转移前先判断来源状态可达</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>石子 <code>1 2 3</code> 若错误地按左端点递增、在短区间尚未完成时就计算 <code>dp[1][3]</code>，读取到的 <code>dp[2][3]</code> 仍可能是初始 <code>0</code>，候选会被算成 <code>0 + 0 + 6 = 6</code>；真实的 <code>dp[2][3]</code> 是 <code>5</code>，正确两个候选为 <code>11</code> 和 <code>9</code>，答案应为 <code>9</code>。错误结果 <code>6</code> 比一次最终合并的重量还小，正好暴露了计算顺序问题。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>二维动态规划也使用二维数组，但两个下标不一定是区间端点；“二维”描述状态形状，“区间 DP”描述子问题结构。下一页会专门比较两者。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握二维动态规划。二维状态常用两个位置共同描述子问题：可以是网格的行列，也可以是两个序列各自的前缀长度。重点仍是状态含义、边界、转移和计算顺序。</p>\n<p><strong>什么时候使用二维 DP？</strong> 当答案同时由两个位置决定，例如网格中的行与列、两个字符串各自的前缀长度，单个下标无法区分所有子问题时，可以设计 <code>dp[i][j]</code>。两维各表示什么必须先写清楚，再判断上、左、左上等依赖是否已经算好。</p>\n<h2 id="kp-01">01 二维状态与依赖方向</h2>\n<p><code>dp[i][j]</code> 只是存储形式，必须补全含义。例如“从 <code>(1,1)</code> 到 <code>(i,j)</code> 的路径数”或“两个序列前 <code>i</code>、<code>j</code> 项的最长公共子序列长度”。填表顺序应保证转移所依赖的位置已经求出。</p>\n<p><strong>具体推演：</strong> 在 <code>2 × 3</code> 网格中，<code>dp[2][3]</code> 若定义为“到达第 2 行第 3 列的路径数”，两个下标分别固定了行和列；它依赖已经算好的 <code>dp[1][3]</code> 与 <code>dp[2][2]</code>。若只写成一维 <code>dp[3]</code> 却没有说明当前保存的是哪一行，就无法判断读取的是上一行旧值还是当前行新值。</p>\n<h2 id="kp-02">02 网格路径计数</h2>\n<p>若只能向右或向下移动，到达 <code>(i,j)</code> 的最后一步来自 <code>(i-1,j)</code> 或 <code>(i,j-1)</code>：</p>\n<p><code>dp[i][j] = dp[i - 1][j] + dp[i][j - 1]</code></p>\n<p>使用 1 下标时，可以令 <code>dp[1][1] = 1</code>，再按行、列递增计算。边界外状态不能被读取。</p>\n<p><strong>具体推演：</strong> 无障碍 <code>2 × 3</code> 网格先得到第一行 <code>1,1,1</code>、第一列 <code>1,1</code>；随后 <code>dp[2][2] = 1 + 1 = 2</code>，<code>dp[2][3] = dp[1][3] + dp[2][2] = 1 + 2 = 3</code>。中间值说明每个状态恰好汇总“从上来”和“从左来”两类互不重复的路径。</p>\n<h2 id="kp-03">03 障碍与网格最优值</h2>\n<p>遇到障碍格时，路径数应直接设为 <code>0</code>，表示不可达。求最大路径和时从合法来源取 <code>max</code>，求最小路径时从合法来源取 <code>min</code>；若权值可能为负，不能用 <code>0</code> 代表不可达。路径数量使用相加，最大路径和或最小路径使用比较，不能混用转移与初值。</p>\n<p><strong>具体推演：</strong> 在 <code>2 × 3</code> 网格中若 <code>(1,2)</code> 是障碍，第一行路径数变为 <code>1,0,0</code>；第二行依次为 <code>1</code>、<code>1</code>、<code>1</code>。到 <code>(2,3)</code> 的唯一合法路线是先向下再一直向右。若仍让障碍格沿用普通转移，<code>dp[1][2]</code> 会错误变成 <code>1</code>，后面就会凭空多出一条穿过障碍的路线。</p>\n<h2 id="kp-04">04 最长公共子序列 LCS</h2>\n<p>设 <code>dp[i][j]</code> 表示字符串 <code>a</code> 的前 <code>i</code> 个字符与 <code>b</code> 的前 <code>j</code> 个字符的 LCS 长度：</p>\n<ul>\n<li><code>a[i - 1] == b[j - 1]</code>：<code>dp[i][j] = dp[i - 1][j - 1] + 1</code>；</li>\n<li>不相等：<code>dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])</code>。</li>\n</ul>\n<p>字符串使用 <code>0</code> 下标，所以长度为 <code>i</code>、<code>j</code> 的前缀末尾分别是 <code>a[i - 1]</code>、<code>b[j - 1]</code>。子序列可以不连续，但相对顺序不能改变。空前缀边界 <code>dp[0][j]</code> 与 <code>dp[i][0]</code> 都为 <code>0</code>。</p>\n<p><strong>具体推演：</strong> 对 <code>a = "abc"</code>、<code>b = "ac"</code>，先匹配 <code>a</code> 得到长度 <code>1</code>；字符 <code>b</code> 在第二个字符串当前前缀中没有匹配，长度仍为 <code>1</code>；最后两个 <code>c</code> 相等，从左上状态加一得到 <code>2</code>。最终公共子序列是 <code>"ac"</code>，它在 <code>a</code> 中不连续，却保持了先 <code>a</code> 后 <code>c</code> 的相对顺序。</p>\n<h2 id="kp-05">05 编辑距离入门</h2>\n<p>编辑距离表示把字符串 <code>a</code> 的前 <code>i</code> 个字符变成字符串 <code>b</code> 的前 <code>j</code> 个字符，最少需要多少次单字符操作。允许的三种操作是插入、删除、替换。</p>\n<p>设 <code>dp[i][j]</code> 为两个前缀间的最少编辑次数。空串变成长度为 <code>j</code> 的前缀需要插入 <code>j</code> 次，所以 <code>dp[0][j] = j</code>；长度为 <code>i</code> 的前缀变成空串需要删除 <code>i</code> 次，所以 <code>dp[i][0] = i</code>。若 <code>a[i - 1] == b[j - 1]</code>，末尾不用操作，从 <code>dp[i - 1][j - 1]</code> 转移；否则在插入、删除、替换三个来源的最小值上加 <code>1</code>。</p>\n<p><strong>具体推演：</strong> 把 <code>"cat"</code> 变成 <code>"cut"</code> 时，首字符 <code>c</code> 与末字符 <code>t</code> 都可保留，只需把中间的 <code>a</code> 替换成 <code>u</code>，总代价为 <code>1</code>。若只允许插入和删除，就要先删 <code>a</code> 再插 <code>u</code>，代价为 <code>2</code>；同时比较三种操作后取最小值，才能得到正确编辑距离 <code>1</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>状态</th>\n<th>典型前驱</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>网格路径数</td>\n<td>到达 <code>(i,j)</code> 的方案数</td>\n<td>上方、左方</td>\n</tr>\n<tr>\n<td>网格最大/最小路径</td>\n<td>到达 <code>(i,j)</code> 的最优值</td>\n<td>可达的上方、左方</td>\n</tr>\n<tr>\n<td>LCS</td>\n<td>两个前缀的公共子序列长度</td>\n<td>左上、上方、左方</td>\n</tr>\n<tr>\n<td>编辑距离</td>\n<td>两个前缀间的最少编辑次数</td>\n<td>插入、删除、替换</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>在没有障碍的 <code>2 × 2</code> 网格中，从左上角出发，只能向右或向下走到右下角。令 <code>dp[i][j]</code> 表示到达 <code>(i,j)</code> 的路径数，则状态表为：</p>\n<table>\n<thead>\n<tr>\n<th>位置</th>\n<th>路径数</th>\n<th>来源</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>(1,1)</code></td>\n<td><code>1</code></td>\n<td>起点</td>\n</tr>\n<tr>\n<td><code>(1,2)</code></td>\n<td><code>1</code></td>\n<td>只能从左边来</td>\n</tr>\n<tr>\n<td><code>(2,1)</code></td>\n<td><code>1</code></td>\n<td>只能从上边来</td>\n</tr>\n<tr>\n<td><code>(2,2)</code></td>\n<td><code>2</code></td>\n<td><code>1 + 1</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>计算两个字符串的 LCS 长度：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    string a, b;    cin &gt;&gt; a &gt;&gt; b;    int n = a.size();    int m = b.size();    int dp[205][205] = {};    // dp[i][j] 表示 a 的前 i 个字符与 b 的前 j 个字符的 LCS 长度。    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= m; j++) {            // 两个前缀的末尾字符下标分别是 i-1 和 j-1。            if (a[i - 1] == b[j - 1]) {                dp[i][j] = dp[i - 1][j - 1] + 1;            } else {                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);            }        }    }    // 两个完整字符串对应状态 dp[n][m]。    cout &lt;&lt; dp[n][m] &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入 <code>abcde ace</code>，输出 <code>3</code>。时间复杂度为 <code>O(nm)</code>，空间复杂度为 <code>O(nm)</code>。</p>\n<h3 id="状态追踪">状态追踪</h3>\n<p>以 <code>a = "ab"</code>、<code>b = "ac"</code> 为例，空前缀所在的第 0 行和第 0 列都是 <code>0</code>：</p>\n<table>\n<thead>\n<tr>\n<th><code>dp[i][j]</code></th>\n<th>比较的字符</th>\n<th>转移</th>\n<th>结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>dp[1][1]</code></td>\n<td><code>a</code> 与 <code>a</code></td>\n<td><code>dp[0][0] + 1</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>dp[1][2]</code></td>\n<td><code>a</code> 与 <code>c</code></td>\n<td><code>max(dp[0][2], dp[1][1])</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>dp[2][1]</code></td>\n<td><code>b</code> 与 <code>a</code></td>\n<td><code>max(dp[1][1], dp[2][0])</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>dp[2][2]</code></td>\n<td><code>b</code> 与 <code>c</code></td>\n<td><code>max(dp[1][2], dp[2][1])</code></td>\n<td><code>1</code></td>\n</tr>\n</tbody>\n</table>\n<p>两个完整字符串的 LCS 长度是 <code>dp[2][2] = 1</code>，可以选择公共子序列 <code>"a"</code>。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>LCS 输入 <code>abcde ace</code></td>\n<td>输出 <code>3</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>单行网格，从 <code>(1,1)</code> 走到 <code>(1,5)</code></td>\n<td>路径数为 <code>1</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td>起点就是障碍格</td>\n<td>路径数为 <code>0</code>，不能先固定为 <code>1</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>不解释两个下标分别代表什么</td>\n<td>状态与转移无法核对</td>\n<td>先写出 <code>dp[i][j]</code> 的完整含义</td>\n</tr>\n<tr>\n<td>访问 <code>i-1</code>、<code>j-1</code> 却不设空前缀边界</td>\n<td>第一个字符处会越界或使用错误初值</td>\n<td>明确初始化第 0 行和第 0 列</td>\n</tr>\n<tr>\n<td>把子序列当成必须连续的子串</td>\n<td>LCS 允许跳过字符</td>\n<td>只保持相对顺序，不要求连续</td>\n</tr>\n<tr>\n<td>障碍格仍沿用普通转移</td>\n<td>不存在的路径会被继续传递</td>\n<td>障碍状态直接设为 <code>0</code></td>\n</tr>\n<tr>\n<td>字符串写成 <code>a[i]</code>、<code>b[j]</code></td>\n<td>前缀长度与字符串下标错一位</td>\n<td>比较 <code>a[i - 1]</code> 与 <code>b[j - 1]</code></td>\n</tr>\n<tr>\n<td>编辑距离只考虑替换</td>\n<td>漏掉插入和删除可能更优的情况</td>\n<td>比较插入、删除、替换三个来源</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>在 <code>2 × 3</code> 网格中把 <code>(1,2)</code> 设为障碍。错误写法仍执行普通转移，会得到第一行 <code>1,1,1</code>、最终 <code>dp[2][3] = 3</code>；正确写法先把障碍状态设为 <code>0</code>，状态表变为第一行 <code>1,0,0</code>、第二行 <code>1,1,1</code>，答案只有 <code>1</code>。多出的两条路线都穿过了不可走格，说明障碍必须在转移前截断。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>复杂动态规划强调状态结构和计算阶段；本页聚焦二维状态。空间压缩属于优化问题，只有明确每一行依赖哪些旧状态后才能进行。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握动态规划最优解优化。本页从“为什么可以从子问题最优解得到当前最优解”出发，整理最长上升子序列、候选最值比较和空间压缩。优化必须保持原状态含义与依赖关系，不能只追求数组更小。</p>\n<p>当题目要求连续区间或子序列的最优值时，先写出能说明答案的基础状态；当基础写法已经正确、但重复枚举候选或保存了不再需要的旧阶段时，再考虑时间或空间优化。优化的判断依据是依赖关系，不是代码看起来更短。</p>\n<h2 id="kp-01">01 最优状态与最大连续和</h2>\n<p>若一个问题的最优解能由若干更小问题的最优解组合得到，就具有最优子结构。状态转移要枚举所有合法前驱，再取 <code>min</code> 或 <code>max</code>。求最小值时初值应足够大，求最大值时则应结合题意设为合法下界。</p>\n<p>最大连续和是典型的一维最值状态。设 <code>dp[i]</code> 表示<strong>必须以第 <code>i</code> 个数结尾</strong>的最大连续子段和，则 <code>dp[i] = max(a[i], dp[i - 1] + a[i])</code>：要么从当前数重新开始，要么接到前一段后面。最终答案是所有 <code>dp[i]</code> 的最大值；全负序列不能强行选择不存在的空子段 <code>0</code>。</p>\n<p><strong>具体推演：</strong> 对序列 <code>-2, 3, -1</code>，<code>dp[1] = -2</code>；到 <code>3</code> 时比较“单独从 <code>3</code> 开始”和“接在 <code>-2</code> 后得到 <code>1</code>”，选择 <code>3</code>；到 <code>-1</code> 时比较 <code>-1</code> 与 <code>3 + (-1) = 2</code>，选择 <code>2</code>。三个结尾状态为 <code>-2,3,2</code>，最终答案取最大值 <code>3</code>。</p>\n<h2 id="kp-02">02 最长上升子序列 LIS</h2>\n<p>设 <code>dp[i]</code> 表示<strong>以第 <code>i</code> 个数结尾</strong>的最长严格上升子序列长度。初值为 <code>1</code>；枚举 <code>j &lt; i</code>，若 <code>a[j] &lt; a[i]</code>，可以转移：</p>\n<p><code>dp[i] = max(dp[i], dp[j] + 1)</code></p>\n<p>最终答案是所有 <code>dp[i]</code> 的最大值，而不一定是 <code>dp[n]</code>。严格上升使用 <code>&lt;</code>，若题目说“不下降”才使用 <code>&lt;=</code>。</p>\n<p><strong>具体推演：</strong> 对序列 <code>3,1,2</code>，初值都是 <code>1</code>。<code>3</code> 不能接到任何更早元素后；<code>1</code> 也不能接在 <code>3</code> 后；处理 <code>2</code> 时，<code>1 &lt; 2</code>，所以由 <code>dp[2] + 1</code> 得到 <code>dp[3] = 2</code>。最终状态为 <code>1,1,2</code>，最长严格上升子序列是 <code>1,2</code>。</p>\n<h2 id="kp-03">03 01 背包与候选最值优化</h2>\n<p><code>01 背包</code> 的二维状态压成一维后，每件物品最多选一次，因此容量必须从大到小倒序更新。正序会读到本轮刚写入的新状态，造成同一物品被重复选择。</p>\n<p>当多个状态反复枚举同一批候选值时，还可以思考是否能维护前缀最值或其他汇总量。但必须先写出正确的基础转移，证明汇总量正好覆盖相同候选集合，不能因为变量更少就认为更优。网格最值也遵循同一原则：从可达的上方、左方等合法来源中取 <code>max</code> 或 <code>min</code>。</p>\n<p><strong>具体推演：</strong> 背包容量为 <code>4</code>，当前只有一件重量 <code>2</code>、价值 <code>3</code> 的物品。倒序先算容量 <code>4</code>，读取旧的 <code>dp[2] = 0</code>，得到 <code>3</code>；再算容量 <code>2</code>，得到 <code>3</code>，最终同一物品只用一次。若正序先把 <code>dp[2]</code> 改成 <code>3</code>，再算 <code>dp[4]</code> 就会读到这个本轮新值并得到 <code>6</code>，等于把唯一物品选了两次。</p>\n<h2 id="kp-04">04 空间优化与更新方向</h2>\n<p>如果当前阶段只依赖上一阶段，可以使用滚动数组；若压成一维数组，更新方向必须避免覆盖仍要读取的旧状态。依赖“上一行左侧旧值”的问题往往需要倒序，依赖“当前行左侧新值”的问题往往需要正序，最终以原转移为准。</p>\n<p><strong>具体推演：</strong> 网格路径压成一维后，处理第二行时 <code>dp[j]</code> 原本保存上方路径数，<code>dp[j - 1]</code> 已更新为当前行左方路径数，因此要从左到右。若上一行是 <code>1,1,1</code>，第二行依次更新为 <code>1,2,3</code>；反向更新会在计算右侧时读不到当前行已经得到的左方状态，结果不再对应原二维转移。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<p>先确认状态含义不变，再比较时间和空间。LIS 的状态以位置结尾；候选最值必须全部合法；滚动数组需要区分上一阶段与当前阶段；一维更新方向由依赖关系决定。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>序列 <code>-2 3 -1 4 -5</code> 的最大连续子段和可以逐项计算：</p>\n<table>\n<thead>\n<tr>\n<th><code>i</code></th>\n<th><code>a[i]</code></th>\n<th><code>dp[i] = max(a[i], dp[i-1] + a[i])</code></th>\n<th>当前答案</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td><code>-2</code></td>\n<td><code>-2</code></td>\n<td><code>-2</code></td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>3</code></td>\n<td><code>3</code></td>\n<td><code>3</code></td>\n</tr>\n<tr>\n<td>3</td>\n<td><code>-1</code></td>\n<td><code>2</code></td>\n<td><code>3</code></td>\n</tr>\n<tr>\n<td>4</td>\n<td><code>4</code></td>\n<td><code>6</code></td>\n<td><code>6</code></td>\n</tr>\n<tr>\n<td>5</td>\n<td><code>-5</code></td>\n<td><code>1</code></td>\n<td><code>6</code></td>\n</tr>\n</tbody>\n</table>\n<p>最优连续子段是 <code>3 -1 4</code>，答案为 <code>6</code>。状态强调“必须以当前位置结尾”，最终答案再取所有状态的最大值。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>求长度为 <code>n</code> 的序列的 LIS 长度：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    int a[1005] = {};    int dp[1005] = {};    cin &gt;&gt; n;    for (int i = 1; i &lt;= n; i++) {        cin &gt;&gt; a[i];        // dp[i] 初始为 1：只选择 a[i] 自身也是合法上升子序列。        dp[i] = 1;    }    int answer = 0;    // 按结尾位置从左到右计算，当前状态只读取更早的 j。    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt; i; j++) {            if (a[j] &lt; a[i]) {                // 把 a[i] 接到以 j 结尾的严格上升子序列后面。                dp[i] = max(dp[i], dp[j] + 1);            }        }        // 最优结尾不一定是 n，所以每个 dp[i] 都要参与答案比较。        answer = max(answer, dp[i]);    }    cout &lt;&lt; answer &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入 <code>6</code> 和 <code>3 1 2 5 4 6</code>，输出 <code>4</code>。时间复杂度为 <code>O(n^2)</code>，空间复杂度为 <code>O(n)</code>。</p>\n<h3 id="状态追踪">状态追踪</h3>\n<p>以容量为 <code>4</code>、只有一件重量 <code>2</code>、价值 <code>3</code> 的 01 背包为例：</p>\n<table>\n<thead>\n<tr>\n<th>写法</th>\n<th>更新顺序</th>\n<th>容量 2 更新后</th>\n<th>容量 4 更新后</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>优化前：二维 <code>dp[item][capacity]</code></td>\n<td>从上一行读取</td>\n<td>选一次得到 <code>3</code></td>\n<td>仍只能选一次，得到 <code>3</code></td>\n</tr>\n<tr>\n<td>优化后：一维 <code>dp[capacity]</code> 倒序</td>\n<td><code>4 → 2</code></td>\n<td>得到 <code>3</code></td>\n<td>先更新容量 4，读取的仍是上一轮 <code>dp[2] = 0</code>，得到 <code>3</code></td>\n</tr>\n<tr>\n<td>错误的一维正序</td>\n<td><code>2 → 4</code></td>\n<td>先得到 <code>3</code></td>\n<td>又读取本轮的新 <code>dp[2] = 3</code>，错误得到 <code>6</code></td>\n</tr>\n</tbody>\n</table>\n<p>空间优化后状态含义不变；倒序的作用是阻止同一件物品在同一轮被重复使用。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>LIS：<code>3 1 2 5 4 6</code></td>\n<td>长度为 <code>4</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>LIS 只有一个数 <code>7</code></td>\n<td>长度为 <code>1</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td>最大连续和：<code>-5 -2 -8</code></td>\n<td>输出 <code>-2</code>，不能输出空子段的 <code>0</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>把 <code>dp[i]</code> 写成前 <code>i</code> 个数的统一答案，却使用“以 <code>i</code> 结尾”的转移</td>\n<td>状态含义和转移不一致</td>\n<td>明确 <code>dp[i]</code> 是否必须以第 <code>i</code> 个数结尾</td>\n</tr>\n<tr>\n<td>LIS 只输出 <code>dp[n]</code></td>\n<td>最优子序列不一定在最后一个位置结束</td>\n<td>取所有 <code>dp[i]</code> 的最大值</td>\n</tr>\n<tr>\n<td>严格上升使用 <code>&lt;=</code></td>\n<td>相等元素会被错误接入</td>\n<td>严格上升用 <code>&lt;</code>，不下降才用 <code>&lt;=</code></td>\n</tr>\n<tr>\n<td>最大连续和把答案初始化为 <code>0</code></td>\n<td>全负序列会错误选择空子段</td>\n<td>用第一个合法状态初始化答案</td>\n</tr>\n<tr>\n<td>01 背包一维状态正序更新</td>\n<td>同一物品会在一轮中被重复使用</td>\n<td>容量从大到小倒序更新</td>\n</tr>\n<tr>\n<td>没有分析依赖就改变更新方向</td>\n<td>可能覆盖后面仍要读取的旧状态</td>\n<td>先画清依赖，再决定正序或倒序</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>全负序列 <code>-5,-2,-8</code> 若把最大连续和答案初始化为 <code>0</code>，每个合法非空子段都小于 <code>0</code>，程序最终会保留 <code>0</code>；但题目要求非空子段，真实候选中最大的是只选 <code>-2</code>。把答案改为第一个合法状态 <code>-5</code>，再依次比较，才会更新到正确结果 <code>-2</code>。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>二维动态规划与复杂动态规划提供基础状态；本页关注保持答案正确的优化。若无法证明覆盖相同候选集合，应保留清晰的基础写法。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>图的定义是七级图论内容的起点。官方大纲要求理解图的基本概念，为后续存储、遍历和泛洪算法建立统一语言。本页主要是概念判断，不强行加入程序。</p>\n<p>地图中的城市与道路、计算机网络中的设备与连接、社交关系中的用户与关注，都可以抽象成图。看到“对象之间存在关系、能否到达、经过哪些连接”等描述时，先判断顶点代表什么、边代表什么，再决定方向和权值。</p>\n<h2 id="kp-01">01 图、顶点与边</h2>\n<p>图由顶点集合和边集合组成，常写作 <code>G = (V, E)</code>。顶点表示对象，边表示两个对象之间的关系。例如城市是顶点，道路是边。边数为零的图仍然是图，只是顶点之间没有连接。</p>\n<p><strong>具体推演：</strong> 三座城市 <code>A、B、C</code> 中只有 <code>A</code> 与 <code>B</code> 之间有道路，可以写成 <code>V={A,B,C}</code>、<code>E={(A,B)}</code>。顶点数是 <code>3</code>，边数是 <code>1</code>；<code>C</code> 虽然没有相连道路，仍是图中的顶点。这个例子说明“没有边”不等于“对象不存在”。</p>\n<h2 id="kp-02">02 图的类型与权值</h2>\n<p>无向边 <code>{u,v}</code> 没有方向，<code>u</code> 与 <code>v</code> 相互连接；有向边 <code>&lt;u,v&gt;</code> 表示从 <code>u</code> 指向 <code>v</code>，反方向不一定存在。边上带有距离、费用等数值时称为带权图，否则是无权图。方向与权值是两个不同属性，所以既可以有带权有向图，也可以有带权无向图。</p>\n<p>边数远少于顶点对数量的图通常称为稀疏图，边很多、接近能容纳的顶点对数量时称为稠密图。这种分类会影响存储选择，但不会改变路径、环、连通等定义。</p>\n<p><strong>具体推演：</strong> 道路 <code>A—B</code> 长 <code>5</code> 千米表示一条带权无向边，从两端都能通行；关注关系 <code>A→B</code> 表示无权有向边，只能说明 <code>A</code> 关注 <code>B</code>。若 4 个顶点只有 2 条边，它较稀疏；若几乎每对顶点都有边，它较稠密。判断顺序应是先看方向，再看权值，最后结合边数判断稀疏或稠密。</p>\n<h2 id="kp-03">03 度、路径与环</h2>\n<p>无向图中，顶点的度是与它相连的边数，每条边为两个端点的度数和贡献 <code>2</code>。有向图要区分入度与出度。路径是首尾相接的一串边；起点与终点相同且形成闭合的路径可构成环。</p>\n<p><strong>具体推演：</strong> 对有向边 <code>1→2</code>、<code>3→2</code>、<code>2→3</code>，顶点 <code>2</code> 的入度是 <code>2</code>，出度是 <code>1</code>；路线 <code>1→2→3</code> 是从 <code>1</code> 到 <code>3</code> 的路径，而 <code>2→3→2</code> 回到起点，构成环。逐条数“箭头进入”和“箭头离开”，就不会把入度、出度混成一个数。</p>\n<h2 id="kp-04">04 连通性与连通分量</h2>\n<p>无向图中两个顶点之间存在路径，则它们连通。任意两个顶点都连通的图称为连通图。非连通图可分成若干个极大的连通部分，每一部分称为一个连通分量。</p>\n<p><strong>具体推演：</strong> 无向边只有 <code>(1,2)</code>、<code>(2,3)</code>、<code>(4,5)</code> 时，<code>1</code> 可以经过 <code>2</code> 到达 <code>3</code>，所以 <code>{1,2,3}</code> 是一个连通分量；<code>4</code> 与 <code>5</code> 构成另一个分量。两组之间没有路径，整张图不连通，连通分量数为 <code>2</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>名称</th>\n<th>判断重点</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>无向边</td>\n<td>两端关系对称</td>\n</tr>\n<tr>\n<td>有向边</td>\n<td>起点与终点有顺序</td>\n</tr>\n<tr>\n<td>度</td>\n<td>无向边数；有向图分入度、出度</td>\n</tr>\n<tr>\n<td>路径</td>\n<td>相邻边首尾衔接</td>\n</tr>\n<tr>\n<td>连通分量</td>\n<td>内部互相可达的极大部分</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>无向图有边 <code>(1,2)</code>、<code>(2,3)</code>、<code>(3,1)</code>、<code>(3,4)</code>。顶点 <code>3</code> 的度为 <code>3</code>；前三个顶点构成一个环；四个顶点都在同一个连通分量中。所有顶点度数和为 <code>8</code>，等于边数 <code>4</code> 的两倍。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>把四座城市记为顶点 <code>1</code> 到 <code>4</code>。道路可以双向通行，长度分别为 <code>(1,2,5)</code>、<code>(2,3,4)</code>、<code>(1,3,8)</code>，城市 <code>4</code> 暂无道路。</p>\n<ul>\n<li>道路双向通行，所以这是无向图；</li>\n<li>道路带长度，所以还是带权图；</li>\n<li>顶点 <code>1</code>、<code>2</code>、<code>3</code> 连通，顶点 <code>4</code> 单独构成一个连通分量；</li>\n<li>整张图有两个连通分量，因此不是连通图。</li>\n</ul>\n<h3 id="关系推演">关系推演</h3>\n<p>对无向边 <code>(1,2)</code>、<code>(2,3)</code>、<code>(3,1)</code>、<code>(3,4)</code> 逐条加入：</p>\n<table>\n<thead>\n<tr>\n<th>加入的边</th>\n<th>度数变化</th>\n<th>当前连通关系</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>(1,2)</code></td>\n<td><code>degree[1]=1</code>，<code>degree[2]=1</code></td>\n<td><code>1</code> 与 <code>2</code> 连通</td>\n</tr>\n<tr>\n<td><code>(2,3)</code></td>\n<td><code>degree[2]=2</code>，<code>degree[3]=1</code></td>\n<td><code>1、2、3</code> 连通</td>\n</tr>\n<tr>\n<td><code>(3,1)</code></td>\n<td><code>degree[3]=2</code>，<code>degree[1]=2</code></td>\n<td>出现环 <code>1-2-3-1</code></td>\n</tr>\n<tr>\n<td><code>(3,4)</code></td>\n<td><code>degree[3]=3</code>，<code>degree[4]=1</code></td>\n<td>四个顶点全部连通</td>\n</tr>\n</tbody>\n</table>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>3 个顶点，边 <code>(1,2)</code>、<code>(2,3)</code></td>\n<td>度数为 <code>1,2,1</code>，图连通</td>\n</tr>\n<tr>\n<td>边界</td>\n<td>1 个顶点、0 条边</td>\n<td>仍是一个图；该顶点度数为 0</td>\n</tr>\n<tr>\n<td>易错</td>\n<td>只有有向边 <code>1→2</code></td>\n<td>不能自动认为存在 <code>2→1</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>把 <code>u→v</code> 当作双向通行</td>\n<td>有向边只保证从 <code>u</code> 到 <code>v</code></td>\n<td>只有真实存在 <code>v→u</code> 时才能反向走</td>\n</tr>\n<tr>\n<td>把权值理解成方向</td>\n<td>权值与方向是两个独立属性</td>\n<td>分别判断“能否反向”和“边上数值”</td>\n</tr>\n<tr>\n<td>有向图只计算一种“度”</td>\n<td>有向边对起点和终点作用不同</td>\n<td>分别统计出度与入度</td>\n</tr>\n<tr>\n<td>有路径就认为有直接边</td>\n<td>路径可以由多条边首尾连接</td>\n<td>区分“直接相邻”和“经过若干边可达”</td>\n</tr>\n<tr>\n<td>用边的多少判断是否连通</td>\n<td>稀疏/稠密只描述边数规模</td>\n<td>根据任意两点间是否存在路径判断连通</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>有向图只有 <code>1→2</code>、<code>2→3</code>。若把有向边误当成双向边，会错误断言 <code>3</code> 能沿 <code>3→2→1</code> 到达 <code>1</code>；实际上图中没有 <code>3→2</code> 或 <code>2→1</code>，从 <code>3</code> 一步也走不出去。画出箭头并按箭头方向逐步检查，就能发现这条“反向路径”根本不存在。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>图的存储把这些抽象关系放进数组或容器；图的遍历用于实际判断可达性和连通分量。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握图的存储。存储方式不会改变图本身，但会改变判断一条边、枚举相邻点和占用空间的代价。本页覆盖邻接矩阵、邻接表和链式前向星入门。</p>\n<h2 id="kp-01">01 邻接矩阵</h2>\n<p>用 <code>edge[u][v]</code> 表示 <code>u</code> 到 <code>v</code> 是否有边。无向图要同时设置 <code>edge[u][v]</code> 与 <code>edge[v][u]</code>。邻接矩阵判断两点是否直接相连很方便，空间复杂度为 <code>O(n^2)</code>，适合顶点较少或较稠密的图。</p>\n<p><strong>具体推演：</strong> 无向图有边 <code>1—3</code> 时，把 <code>edge[1][3]</code> 和 <code>edge[3][1]</code> 都设为 <code>1</code>。随后查询 <code>1</code> 与 <code>3</code> 是否直接相连，只需读取一个数组位置；查询 <code>1</code> 与 <code>2</code> 得到 <code>0</code>，表示没有直接边，但不能据此断言两点之间一定不存在经过其他顶点的路径。</p>\n<h2 id="kp-02">02 邻接表</h2>\n<p><code>vector&lt;int&gt; graph[N]</code> 中，<code>graph[u]</code> 保存 <code>u</code> 的所有相邻点。存储无向边也要加入两个方向。邻接表空间通常为 <code>O(n+m)</code>，枚举某个顶点的邻边只访问实际存在的边，适合稀疏图。</p>\n<p><strong>具体推演：</strong> 依次加入无向边 <code>1—2</code>、<code>1—3</code> 后，<code>graph[1]=[2,3]</code>、<code>graph[2]=[1]</code>、<code>graph[3]=[1]</code>。枚举顶点 <code>1</code> 只访问两个真实邻点，不必扫描所有顶点；若漏存反向边，枚举顶点 <code>2</code> 时就会错误地看不到顶点 <code>1</code>。</p>\n<h2 id="kp-03">03 链式前向星入门</h2>\n<p>链式前向星使用数组模拟邻接表，四个常见数组各有明确职责：</p>\n<ul>\n<li><code>head[u]</code>：顶点 <code>u</code> 当前第一条出边的编号，没有出边时为 <code>-1</code>；</li>\n<li><code>to[index]</code>：编号为 <code>index</code> 的边到达哪个顶点；</li>\n<li><code>next[index]</code>：同一起点的下一条边编号；</li>\n<li><code>w[index]</code>：这条边的权值；无权图可以不使用。</li>\n</ul>\n<p>加入有向边 <code>u -&gt; v</code> 时，先令 <code>to[index] = v</code>、<code>next[index] = head[u]</code>，再令 <code>head[u] = index</code>。枚举 <code>u</code> 的出边时，从 <code>head[u]</code> 开始不断跳到 <code>next[index]</code>，直到 <code>-1</code>。无向边仍要调用两次加边过程。</p>\n<p><strong>具体推演：</strong> 初始 <code>head[1]=-1</code>。加入编号 <code>0</code> 的边 <code>1→2</code> 后，<code>to[0]=2</code>、<code>next[0]=-1</code>、<code>head[1]=0</code>；再加入编号 <code>1</code> 的边 <code>1→3</code> 后，<code>to[1]=3</code>、<code>next[1]=0</code>、<code>head[1]=1</code>。从边号 <code>1</code> 开始沿 <code>next</code> 走到 <code>0</code>，枚举顺序就是 <code>3、2</code>。</p>\n<h2 id="kp-04">04 存储选择与建图规则</h2>\n<p>频繁判断任意两点是否有边可考虑邻接矩阵；稀疏图中枚举邻点可用邻接表或链式前向星；只按输入顺序枚举全部边时也可直接保存边集。建图前还要确认顶点编号范围、图是否有向、是否带权、是否可能有重边或自环。带权邻接矩阵的“没有边”不能总用 <code>0</code> 表示，因为合法边权也可能为 <code>0</code>。</p>\n<p><strong>具体推演：</strong> <code>n=1000、m=1200</code> 时，邻接矩阵需要约一百万个位置，而邻接表只保存表头和约 <code>2400</code> 条无向邻接记录，后者更合适。若输入是一条权值为 <code>0</code> 的边 <code>1—2</code>，再用矩阵值 <code>0</code> 表示“无边”，就无法区分这条合法零权边，因此必须另设存在标记或哨兵。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>结构</th>\n<th>空间</th>\n<th>擅长</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>邻接矩阵</td>\n<td><code>O(n^2)</code></td>\n<td>快速判断两点是否直接相连</td>\n</tr>\n<tr>\n<td>邻接表</td>\n<td><code>O(n+m)</code></td>\n<td>枚举某个顶点的实际邻边</td>\n</tr>\n<tr>\n<td>链式前向星</td>\n<td><code>O(n+m)</code></td>\n<td>用固定数组枚举顶点出边</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>无向图只有一条边 <code>1—2</code>。使用邻接矩阵时设置 <code>edge[1][2] = edge[2][1] = 1</code>；使用邻接表时把 <code>2</code> 加入 <code>graph[1]</code>，同时把 <code>1</code> 加入 <code>graph[2]</code>。两种结构表示的是同一张图，只是查询方式和空间不同。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>读入无向图并输出每个顶点的相邻点：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n, m;    cin &gt;&gt; n &gt;&gt; m;    vector&lt;int&gt; graph[1005];    // 无向边必须在两个端点的邻接表中各保存一次。    for (int i = 1; i &lt;= m; i++) {        int u, v;        cin &gt;&gt; u &gt;&gt; v;        graph[u].push_back(v);        graph[v].push_back(u);    }    // 逐个顶点枚举邻接表，只访问真实存在的邻接记录。    for (int u = 1; u &lt;= n; u++) {        cout &lt;&lt; u &lt;&lt; ":";        for (int i = 0; i &lt; graph[u].size(); i++) {            cout &lt;&lt; " " &lt;&lt; graph[u][i];        }        cout &lt;&lt; endl;    }    return 0;}</code></pre>\n<p>建图与枚举总时间复杂度为 <code>O(n+m)</code>，空间复杂度为 <code>O(n+m)</code>。</p>\n<h3 id="加边追踪">加边追踪</h3>\n<p>依次加入无向边 <code>(1,2)</code>、<code>(1,3)</code>，邻接表和链式前向星的变化如下：</p>\n<table>\n<thead>\n<tr>\n<th>操作</th>\n<th>邻接表变化</th>\n<th>链式前向星中顶点 1 的边链</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>初始</td>\n<td><code>graph[1]</code> 为空</td>\n<td><code>head[1] = -1</code></td>\n</tr>\n<tr>\n<td>加入 <code>(1,2)</code> 的 <code>1 → 2</code></td>\n<td><code>graph[1] = [2]</code></td>\n<td><code>to[0] = 2</code>，<code>next[0] = -1</code>，<code>head[1] = 0</code></td>\n</tr>\n<tr>\n<td>加入 <code>(1,3)</code> 的 <code>1 → 3</code></td>\n<td><code>graph[1] = [2,3]</code></td>\n<td><code>to[2] = 3</code>，<code>next[2] = 0</code>，<code>head[1] = 2</code></td>\n</tr>\n</tbody>\n</table>\n<p>链式前向星采用头插法，所以从 <code>head[1]</code> 枚举时会先看到新加入的 <code>3</code>，再沿 <code>next</code> 看到 <code>2</code>。无向边的反向记录也要分别加入对应端点。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>n = 3</code>，边为 <code>1 2</code>、<code>1 3</code></td>\n<td>顶点 1 的邻点包含 2 和 3</td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>n = 1, m = 0</code></td>\n<td>唯一顶点的邻接表为空</td>\n</tr>\n<tr>\n<td>易错</td>\n<td>有向边 <code>1 → 2</code></td>\n<td>只存一个方向，不能自动加入 <code>2 → 1</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>无向边只存一个方向</td>\n<td>从另一端遍历时会漏边</td>\n<td>分别保存 <code>u → v</code> 和 <code>v → u</code></td>\n</tr>\n<tr>\n<td>顶点使用 1 下标，数组只开到 <code>n</code></td>\n<td>下标 <code>n</code> 会越界</td>\n<td>数组至少开到 <code>n + 1</code></td>\n</tr>\n<tr>\n<td>稀疏大图仍使用邻接矩阵</td>\n<td><code>O(n^2)</code> 空间可能无法容纳</td>\n<td>改用邻接表或链式前向星</td>\n</tr>\n<tr>\n<td>忘记把 <code>head</code> 初始化为 <code>-1</code></td>\n<td>无法正确识别边链结束</td>\n<td>建图前统一初始化 <code>head</code></td>\n</tr>\n<tr>\n<td>先覆盖 <code>head[u]</code> 再保存旧表头</td>\n<td>原有出边会丢失</td>\n<td>先令 <code>next[index] = head[u]</code>，最后更新 <code>head[u]</code></td>\n</tr>\n<tr>\n<td>用合法边权表示“没有边”</td>\n<td>零权边等情况会被误判</td>\n<td>使用单独标记或不可能混淆的哨兵值</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>初始 <code>head[1]=0</code>，表示顶点 <code>1</code> 已有一条边。新增边号 <code>1</code> 时若先写 <code>head[1]=1</code>，再写 <code>next[1]=head[1]</code>，就会得到 <code>next[1]=1</code>，边链永远指向自己，旧边号 <code>0</code> 也丢失。正确顺序先保存 <code>next[1]=0</code>，再更新 <code>head[1]=1</code>，枚举才能得到新边和旧边。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>图的遍历通常使用邻接表枚举相邻点；选择存储结构时要同时考虑后续算法的访问方式。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握图的遍历。深度优先遍历（DFS）沿一条分支尽量深入，广度优先遍历（BFS）按与起点的边数分层扩展。两者都必须使用访问标记避免在环中重复处理。</p>\n<h2 id="kp-01">01 遍历目标与访问标记</h2>\n<p>遍历是从起点出发，系统访问所有可达顶点。顶点第一次被发现时就应标记，后续遇到它直接跳过。无向图的一条边存了两个方向，如果没有标记，程序会在两个端点之间往返。</p>\n<p><strong>具体推演：</strong> 无向边只有 <code>1—2</code> 时，从 <code>1</code> 出发先标记 <code>1</code>，再发现并标记 <code>2</code>；处理 <code>2</code> 时又看到邻点 <code>1</code>，因为 <code>visited[1]=true</code>，所以直接跳过。若没有访问标记，程序会沿 <code>1→2→1→2……</code> 不断往返。</p>\n<h2 id="kp-02">02 深度优先遍历 DFS</h2>\n<p>DFS 访问顶点后，依次递归访问尚未访问的相邻点：</p>\n<p>进入 <code>dfs(u)</code> 后先执行 <code>visited[u] = true</code>，再枚举 <code>graph[u]</code> 中的相邻点；只有 <code>visited[v]</code> 为假时才递归。DFS 适合连通性、连通分量和需要沿路径深入的路径搜索。若只需判断能否到达目标，发现目标后可以结束当前搜索；若要记录一条具体路径，还需保存每个顶点从哪个前驱到达。</p>\n<p><strong>具体推演：</strong> 图有边 <code>1—2</code>、<code>1—3</code>、<code>2—4</code>，邻点按编号递增。DFS 从 <code>1</code> 先进入 <code>2</code>，再进入 <code>4</code>；<code>4</code> 没有新邻点后回溯到 <code>2</code>、再回到 <code>1</code>，最后访问 <code>3</code>。首次访问顺序是 <code>1,2,4,3</code>，递归路径的“进入—回溯”解释了为什么不是按层访问。</p>\n<h2 id="kp-03">03 广度优先遍历 BFS</h2>\n<p>BFS 使用队列。起点入队时距离为 <code>0</code>；每次取出队首，把未访问邻点的距离设为当前距离加 <code>1</code>，并立即标记、入队。无权图中第一次到达顶点时的距离，就是起点到它的最少边数。</p>\n<p>执行顺序是：起点入队并标记；反复取出队首；把每个未访问邻点标记，令它的距离等于当前距离加 <code>1</code>，再入队。标记必须发生在入队时。</p>\n<p><strong>具体推演：</strong> 对同一张图，从 <code>1</code> 开始时队列 <code>[1]</code>、<code>dist[1]=0</code>；取出 <code>1</code> 后把 <code>2、3</code> 入队，队列变为 <code>[2,3]</code>，距离都为 <code>1</code>；取出 <code>2</code> 后发现 <code>4</code>，队列变为 <code>[3,4]</code>，<code>dist[4]=2</code>。第一次到达 <code>4</code> 经过两条边，因此最少边数为 <code>2</code>。</p>\n<h2 id="kp-04">04 非连通图与连通分量</h2>\n<p>从一个起点只能访问同一连通分量。要遍历整张非连通图，应从 <code>1</code> 到 <code>n</code> 检查每个未访问顶点，每启动一次 DFS 或 BFS，连通分量数加 <code>1</code>。</p>\n<p><strong>具体推演：</strong> <code>n=5</code>，边为 <code>1—2</code>、<code>2—3</code>、<code>4—5</code>。外层遇到顶点 <code>1</code> 时计数变为 <code>1</code>，一次遍历标记 <code>1、2、3</code>；继续检查到 <code>4</code> 时仍未访问，计数变为 <code>2</code>，第二次遍历标记 <code>4、5</code>。最终所有顶点都被处理，连通分量数为 <code>2</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>方法</th>\n<th>核心结构</th>\n<th>常见用途</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>DFS</td>\n<td>递归或栈</td>\n<td>深入搜索、连通性、连通分量</td>\n</tr>\n<tr>\n<td>BFS</td>\n<td>队列</td>\n<td>分层遍历、无权图最少边数</td>\n</tr>\n</tbody>\n</table>\n<p>两种方法在邻接表上完整遍历时，时间复杂度均为 <code>O(n+m)</code>，访问标记和队列/递归空间为 <code>O(n)</code>。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>无向图有边 <code>1—2</code>、<code>1—3</code>。若邻接点按 <code>2,3</code> 的顺序保存，从 1 开始 DFS 会访问 <code>1,2,3</code>；BFS 也会先访问 1，再发现同一层的 2 和 3。这个例子中顺序可能相同，但原因不同：DFS 沿分支深入，BFS 按距离分层。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>统计无向图连通分量：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nvector&lt;int&gt; graph[1005];bool visited[1005];\nvoid dfs(int u) {    // 顶点第一次进入 DFS 时立即标记，避免在无向边或环上重复递归。    visited[u] = true;    for (int i = 0; i &lt; graph[u].size(); i++) {        int v = graph[u][i];        // 只沿尚未访问的邻点继续深入。        if (!visited[v]) {            dfs(v);        }    }}\nint main() {    int n, m;    cin &gt;&gt; n &gt;&gt; m;    // 无向图的每条边要存入两个方向。    for (int i = 1; i &lt;= m; i++) {        int u, v;        cin &gt;&gt; u &gt;&gt; v;        graph[u].push_back(v);        graph[v].push_back(u);    }    int answer = 0;    // 每遇到一个未访问顶点，就发现了一个新的连通分量。    for (int u = 1; u &lt;= n; u++) {        if (!visited[u]) {            answer++;            dfs(u);        }    }    cout &lt;&lt; answer &lt;&lt; endl;    return 0;}</code></pre>\n<p>输入 <code>5 3</code>，边为 <code>1 2</code>、<code>2 3</code>、<code>4 5</code> 时输出 <code>2</code>。一次 DFS 会标记一个完整分量，因此不会重复计数。时间复杂度为 <code>O(n+m)</code>，空间复杂度为 <code>O(n+m)</code>。</p>\n<h3 id="dfs-与-bfs-过程追踪">DFS 与 BFS 过程追踪</h3>\n<p>对边 <code>1—2</code>、<code>1—3</code>、<code>2—4</code>，从顶点 1 出发：</p>\n<table>\n<thead>\n<tr>\n<th>方法</th>\n<th>当前操作</th>\n<th>待处理状态</th>\n<th>已标记顶点</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>DFS</td>\n<td>进入 1，再递归到 2</td>\n<td>递归路径 <code>1 → 2</code></td>\n<td><code>1,2</code></td>\n</tr>\n<tr>\n<td>DFS</td>\n<td>从 2 递归到 4</td>\n<td>递归路径 <code>1 → 2 → 4</code></td>\n<td><code>1,2,4</code></td>\n</tr>\n<tr>\n<td>DFS</td>\n<td>回到 1，再访问 3</td>\n<td>递归路径 <code>1 → 3</code></td>\n<td><code>1,2,3,4</code></td>\n</tr>\n<tr>\n<td>BFS</td>\n<td>1 入队并出队，发现 2、3</td>\n<td>队列变化：<code>[1] → [2,3]</code></td>\n<td><code>1,2,3</code></td>\n</tr>\n<tr>\n<td>BFS</td>\n<td>2 出队，发现 4</td>\n<td>队列变化：<code>[2,3] → [3,4]</code></td>\n<td><code>1,2,3,4</code></td>\n</tr>\n<tr>\n<td>BFS</td>\n<td>3、4 依次出队</td>\n<td><code>[3,4] → [4] → []</code></td>\n<td><code>1,2,3,4</code></td>\n</tr>\n</tbody>\n</table>\n<p>BFS 在顶点入队时立即标记，因此即使另一个顶点也连接到 4，顶点 4 也不会重复入队。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>5</code> 个顶点，边为 <code>1 2</code>、<code>2 3</code>、<code>4 5</code></td>\n<td>连通分量数为 <code>2</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>n = 1, m = 0</code></td>\n<td>连通分量数为 <code>1</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td>三角形 <code>1—2—3—1</code></td>\n<td>每个顶点只访问一次，不发生无限递归</td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>BFS 出队时才标记</td>\n<td>同一顶点可能被多个前驱重复入队</td>\n<td>顶点入队时立即标记</td>\n</tr>\n<tr>\n<td>DFS 不检查访问标记就递归</td>\n<td>在无向边或环上会反复递归</td>\n<td>只递归访问尚未标记的邻点</td>\n</tr>\n<tr>\n<td>只从顶点 1 出发就结束</td>\n<td>非连通图中会漏掉其他分量</td>\n<td>枚举所有未访问顶点并启动新遍历</td>\n</tr>\n<tr>\n<td>在带权图上直接用 BFS 求最小权值和</td>\n<td>BFS 只保证无权图的最少边数</td>\n<td>使用与边权条件匹配的算法</td>\n</tr>\n<tr>\n<td>把 DFS 首次找到的路径称为最短路</td>\n<td>DFS 不按边数分层</td>\n<td>无权图最少边数使用 BFS</td>\n</tr>\n<tr>\n<td>认为 DFS 访问顺序固定</td>\n<td>顺序受邻接表排列影响</td>\n<td>只依赖“全部可达点会被访问”等稳定性质</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>在菱形图 <code>1—2、1—3、2—4、3—4</code> 中，若 BFS 等到出队才标记顶点，处理 <code>2</code> 时会把 <code>4</code> 入队，处理 <code>3</code> 时又会把尚未出队、仍未标记的 <code>4</code> 再入队，队列出现两个 <code>4</code>。改为发现 <code>4</code> 时立即标记，第二次遇到它就会跳过，既避免重复处理，也保留第一次到达的最少边数。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>泛洪算法本质上是在网格图上执行 DFS 或 BFS；区别只是相邻点由方向数组现场计算，而不是预先存入邻接表。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>泛洪算法（Flood Fill）是七级大纲中的独立知识点。它把满足条件的网格格子看作顶点，把相邻关系看作边，再用 DFS 或 BFS 找出一个完整连通区域。</p>\n<h2 id="kp-01">01 把网格看作图</h2>\n<p>每个可通行格子是一个顶点，四方向相邻或八方向相邻由题目决定。区域计数、区域大小、染色和“岛屿”问题都可转化为连通分量问题。不同字符是否连通也必须按题意判断。</p>\n<p><strong>具体推演：</strong> 在网格 <code>10 / 01</code> 中，两个 <code>1</code> 只在对角线上接触。若题目规定四方向，它们之间没有边，是两个区域；若规定八方向，就存在一条对角相邻边，成为一个区域。建图时改变的只是“哪些格子相邻”，搜索框架本身不变。</p>\n<h2 id="kp-02">02 方向数组与边界</h2>\n<p>本项目二维数组示例使用 1 下标，方向数组命名为 <code>fx</code>、<code>fy</code>，下标 <code>0</code> 占位：</p>\n<pre><code>int fx[5] = {0, -1, 1, 0, 0};int fy[5] = {0, 0, 0, -1, 1};</code></pre>\n<p>新位置 <code>(nx, ny)</code> 必须先检查 <code>1 &lt;= nx &lt;= n</code>、<code>1 &lt;= ny &lt;= m</code>，再访问数组；条件顺序不能颠倒。</p>\n<p><strong>具体推演：</strong> 在 <code>3 × 4</code> 网格的 <code>(1,1)</code> 向上尝试时，新位置是 <code>(0,1)</code>。先检查得到 <code>0 &lt; 1</code>，立即放弃这个方向；若先读取 <code>grid[0][1]</code>，就已经访问了约定范围外的格子。向右得到 <code>(1,2)</code>，四个边界条件都成立，才继续检查格子类型。</p>\n<h2 id="kp-03">03 标记、染色与区域统计</h2>\n<p>进入合法格子后立即标记，再扩展相邻方向。可以使用独立 <code>visited</code> 数组，也可以把原网格字符改成新的颜色作为已访问标记。统计区域数量时，主循环每发现一个未访问目标格就启动一次搜索并加一；统计区域大小时，在一次搜索内每访问一个合法格就把计数加一。地图搜索还可能要求找最大区域、替换整片颜色或判断两个位置是否同属一个区域。</p>\n<p><strong>具体推演：</strong> 一行网格 <code>11101</code> 中，外层扫描到第一个 <code>1</code>，区域数加一；搜索依次标记前三个格子，区域大小得到 <code>3</code>。扫描越过 <code>0</code> 后又遇到未标记的最后一个 <code>1</code>，区域数再加一，第二个区域大小是 <code>1</code>。最终“区域数量 <code>2</code>”与“最大区域大小 <code>3</code>”来自不同层次的计数。</p>\n<h2 id="kp-04">04 DFS 与 BFS 两种实现</h2>\n<p>DFS 写法短，适合规模允许递归的网格；BFS 使用队列，不依赖递归深度。两者只要相邻规则、标记时机相同，会找到同一连通区域。网格有 <code>n*m</code> 个格子时，时间复杂度为 <code>O(nm)</code>，标记空间为 <code>O(nm)</code>。</p>\n<p><strong>具体推演：</strong> 对 L 形区域 <code>(1,1)、(1,2)、(2,1)</code>，DFS 可能沿 <code>(1,1)→(1,2)</code> 深入，回溯后再访问 <code>(2,1)</code>；BFS 从 <code>(1,1)</code> 出队时会同时把后两个格子入队。两种顺序不同，但最终访问集合都是这三个格子，区域大小也都为 <code>3</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<p>搜索一个区域：检查边界 → 检查格子类型 → 检查是否访问 → 立即标记 → 扩展相邻格。统计多个区域时，还需在外层枚举所有格子。</p>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>网格只有一行 <code>111</code>，按四方向相邻规则，三个 <code>1</code> 首尾连成一个区域。搜索从第一个 <code>1</code> 出发会依次覆盖其余两个格子，因此区域数量是 <code>1</code>，区域大小是 <code>3</code>。这两个统计量不能混淆。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>统计由字符 <code>1</code> 组成的四连通区域：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint n, m;char grid[105][105];bool visited[105][105];int fx[5] = {0, -1, 1, 0, 0};int fy[5] = {0, 0, 0, -1, 1};\nvoid dfs(int x, int y) {    // 合法目标格第一次进入 DFS 时立即标记，保证每个格子只处理一次。    visited[x][y] = true;    // 下标 1 到 4 依次表示上、下、左、右四个方向。    for (int i = 1; i &lt;= 4; i++) {        int nx = x + fx[i];        int ny = y + fy[i];        // 必须先确认新坐标在网格内，再读取 grid 与 visited。        if (nx &gt;= 1 &amp;&amp; nx &lt;= n &amp;&amp; ny &gt;= 1 &amp;&amp; ny &lt;= m            &amp;&amp; grid[nx][ny] == \'1\' &amp;&amp; !visited[nx][ny]) {            dfs(nx, ny);        }    }}\nint main() {    cin &gt;&gt; n &gt;&gt; m;    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= m; j++) cin &gt;&gt; grid[i][j];    }    int answer = 0;    // 外层扫描负责发现每个尚未访问的新区域。    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= m; j++) {            if (grid[i][j] == \'1\' &amp;&amp; !visited[i][j]) {                answer++;                dfs(i, j);            }        }    }    cout &lt;&lt; answer &lt;&lt; endl;    return 0;}</code></pre>\n<h3 id="扩散追踪">扩散追踪</h3>\n<p>对下面的 <code>3 × 3</code> 网格使用四连通：</p>\n<pre><code>110010001</code></pre>\n<table>\n<thead>\n<tr>\n<th>扫描或搜索步骤</th>\n<th>新访问的格子</th>\n<th>区域数量</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>扫描到 <code>(1,1)</code>，启动第一次搜索</td>\n<td><code>(1,1)</code>、<code>(1,2)</code>、<code>(2,2)</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td>继续扫描，前三个格子已标记</td>\n<td>无</td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td>扫描到 <code>(3,3)</code>，启动第二次搜索</td>\n<td><code>(3,3)</code></td>\n<td><code>2</code></td>\n</tr>\n</tbody>\n</table>\n<p><code>(2,2)</code> 与 <code>(3,3)</code> 只在对角线上接触，四连通时不属于同一区域；如果题目明确使用八连通，结果才会改变。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td>一行 <code>111</code>，四连通</td>\n<td>区域数 <code>1</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td>网格全部为 <code>0</code></td>\n<td>区域数 <code>0</code></td>\n</tr>\n<tr>\n<td>易错</td>\n<td><code>10 / 01</code>，四连通</td>\n<td>区域数 <code>2</code>，对角线不连通</td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>访问数组后才检查边界</td>\n<td>可能先发生越界访问</td>\n<td>先检查坐标范围，再读网格和标记</td>\n</tr>\n<tr>\n<td>四连通问题加入对角线</td>\n<td>会把只在角上接触的区域错误合并</td>\n<td>严格按题意设置四方向或八方向</td>\n</tr>\n<tr>\n<td>搜索结束后才标记</td>\n<td>同一格子可能重复递归或入队</td>\n<td>第一次发现合法格子时立即标记</td>\n</tr>\n<tr>\n<td>只搜索第一个区域</td>\n<td>会漏掉与起点不连通的区域</td>\n<td>外层枚举全部格子</td>\n</tr>\n<tr>\n<td>在外层循环统计区域大小</td>\n<td>混淆区域数量与单个区域格子数</td>\n<td>启动搜索时计区域数，搜索内部计大小</td>\n</tr>\n<tr>\n<td>行列与 <code>fx</code>、<code>fy</code> 混用</td>\n<td>新坐标方向错误</td>\n<td>统一用 <code>nx = x + fx[i]</code>、<code>ny = y + fy[i]</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>网格 <code>10 / 01</code> 按四方向应有两个区域。若错误地加入 <code>(1,1)</code> 到 <code>(2,2)</code> 的对角方向，第一次搜索会把两个 <code>1</code> 都标记，程序输出区域数 <code>1</code>。正确的四方向数组只产生上、下、左、右四个相邻位置，因此右下角的 <code>1</code> 要由外层扫描启动第二次搜索，答案为 <code>2</code>。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>图的遍历提供 DFS/BFS 框架；本页把邻接表换成方向数组，并增加网格边界和格子类型条件。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方七级大纲要求掌握哈希表。哈希表通过哈希函数把键映射到存储位置，适合快速查找和计数。C++ 关联容器是常见实现工具，但必须理解有序与无序、键是否存在以及平均复杂度和最坏情况的区别。</p>\n<h2 id="kp-01">01 键、值与映射</h2>\n<p>键用于查找，值保存与键关联的信息。例如统计单词次数时，单词是键，出现次数是值。相同键对应同一个统计位置，不能把“键”和“出现次序”混为一谈。计数、去重、快速查找和建立映射关系，是关联容器最常见的四类应用。</p>\n<p><strong>具体推演：</strong> 依次读入 <code>apple、banana、apple</code>。第一次读取 <code>apple</code> 时建立 <code>apple→1</code>，读取 <code>banana</code> 后得到 <code>banana→1</code>，再次读取 <code>apple</code> 时找到原来的同一个键并更新为 <code>apple→2</code>。最终只有两个键，而不是三个“输入位置”。</p>\n<h2 id="kp-02">02 哈希函数、冲突与复杂度</h2>\n<p>不同键可能映射到相同位置，这称为哈希冲突，实际实现必须处理冲突。哈希表的查找、插入平均可视为 <code>O(1)</code>，但不能宣称任何情况下都绝对是 <code>O(1)</code>；数据分布和实现会影响最坏情况。</p>\n<p><strong>具体推演：</strong> 假设教学用哈希位置是 <code>key % 5</code>，键 <code>2</code> 和 <code>7</code> 都落到位置 <code>2</code>，这就是冲突。容器必须继续区分两个真实键；查找 <code>7</code> 时先定位这个位置，再核对键本身。多数位置分布均匀时检查很少，若许多键都冲突到同一位置，最坏就可能逐个检查。</p>\n<h2 id="kp-03">03 map 与 unordered_map</h2>\n<p><code>map</code> 按键有序，常见操作复杂度为 <code>O(log n)</code>；<code>unordered_map</code> 不保证遍历顺序，平均查找和插入为 <code>O(1)</code>。若题目要求按键从小到大输出，应使用 <code>map</code> 或另行排序，不能依赖 <code>unordered_map</code> 的显示顺序。</p>\n<p>访问 <code>count[key]</code> 时，如果键不存在，会插入默认值 <code>0</code>，很适合计数；若只想判断而不想插入，可使用 <code>find</code>。</p>\n<p><strong>具体推演：</strong> 插入键值 <code>3→30、1→10、2→20</code> 后，遍历 <code>map</code> 会按键输出 <code>1、2、3</code>；遍历 <code>unordered_map</code> 只能保证三组键值仍存在，不能预测顺序。若只执行 <code>count[5]</code> 查询，不存在的键 <code>5</code> 还会被创建并取默认值 <code>0</code>；使用 <code>find(5)</code> 则不会改变容器。</p>\n<h2 id="kp-04">04 set、unordered_set 与去重</h2>\n<p>集合只保存键，不保存对应值。<code>set</code> 中元素有序，<code>unordered_set</code> 不保证顺序。插入重复元素不会增加集合大小，因此可用于去重和存在性判断。</p>\n<p><strong>具体推演：</strong> 把 <code>5,2,5,3,2</code> 依次插入 <code>set</code>，集合大小变化为 <code>1,2,2,3,3</code>，最终元素按序为 <code>2,3,5</code>。换成 <code>unordered_set</code> 后大小仍是 <code>3</code>，存在性判断结果也相同，但遍历顺序不能作为答案的一部分。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>容器</th>\n<th>内容</th>\n<th>顺序</th>\n<th>常见操作</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>map</code></td>\n<td>键值对</td>\n<td>按键有序</td>\n<td>有序统计</td>\n</tr>\n<tr>\n<td><code>unordered_map</code></td>\n<td>键值对</td>\n<td>不保证</td>\n<td>快速计数</td>\n</tr>\n<tr>\n<td><code>set</code></td>\n<td>唯一键</td>\n<td>有序</td>\n<td>有序去重</td>\n</tr>\n<tr>\n<td><code>unordered_set</code></td>\n<td>唯一键</td>\n<td>不保证</td>\n<td>快速存在性判断</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<h3 id="基础例题">基础例题</h3>\n<p>依次读入 <code>1 2 2 3</code>。第一次看到一个键时，它的旧计数是 <code>0</code>；再次看到键 <code>2</code> 时，只更新同一个键对应的值。最后得到映射 <code>{1:1, 2:2, 3:1}</code>。哈希或映射结构解决的是“按键找到对应信息”，不是保存每次出现的独立副本。</p>\n<h3 id="典型应用例题">典型应用例题</h3>\n<p>统计输入整数的出现次数并按数值从小到大输出：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    cin &gt;&gt; n;    map&lt;int, int&gt; count;    // 下标访问不存在的键会插入默认值 0，因此可以直接累加次数。    for (int i = 1; i &lt;= n; i++) {        int value;        cin &gt;&gt; value;        count[value]++;    }    // map 按键有序遍历，所以输出自然按数值从小到大排列。    for (auto item : count) {        cout &lt;&lt; item.first &lt;&lt; " " &lt;&lt; item.second &lt;&lt; endl;    }    return 0;}</code></pre>\n<p>使用 <code>map</code> 是因为输出要求有序。若只查询次数而不要求顺序，可按题目范围考虑 <code>unordered_map</code>。</p>\n<h3 id="映射追踪">映射追踪</h3>\n<p>对输入 <code>1 2 2 3</code>，每次读取后的键与当前计数如下：</p>\n<table>\n<thead>\n<tr>\n<th>输入的键</th>\n<th>操作前计数</th>\n<th>操作后当前计数</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>1</code></td>\n<td><code>0</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>2</code></td>\n<td><code>0</code></td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>2</code></td>\n<td><code>1</code></td>\n<td><code>2</code></td>\n</tr>\n<tr>\n<td><code>3</code></td>\n<td><code>0</code></td>\n<td><code>1</code></td>\n</tr>\n</tbody>\n</table>\n<p>若使用 <code>map</code>，最终遍历顺序按键从小到大；若换成 <code>unordered_map</code>，计数结果相同，但不能据此预测输出顺序。</p>\n<h3 id="建议测试案例">建议测试案例</h3>\n<table>\n<thead>\n<tr>\n<th>测试类型</th>\n<th>输入或条件</th>\n<th>预期结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基础</td>\n<td><code>4</code> 个数：<code>1 2 2 3</code></td>\n<td>输出 <code>1 1</code>、<code>2 2</code>、<code>3 1</code></td>\n</tr>\n<tr>\n<td>边界</td>\n<td><code>n = 0</code></td>\n<td>不输出任何键值对</td>\n</tr>\n<tr>\n<td>易错</td>\n<td>使用 <code>unordered_map</code> 统计后直接遍历</td>\n<td>计数正确，但不能要求按键有序</td>\n</tr>\n</tbody>\n</table>\n<h2 id="common-errors">常见错误</h2>\n<table>\n<thead>\n<tr>\n<th>错误做法</th>\n<th>为什么错</th>\n<th>正确方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>依赖 <code>unordered_map</code> 或 <code>unordered_set</code> 的遍历顺序</td>\n<td>无序容器不承诺按键或插入顺序输出</td>\n<td>需要有序输出时使用 <code>map</code>、<code>set</code> 或另行排序</td>\n</tr>\n<tr>\n<td>只想查询却使用下标访问</td>\n<td>不存在的键会被插入并赋默认值</td>\n<td>使用 <code>find</code> 判断是否存在</td>\n</tr>\n<tr>\n<td>把平均 <code>O(1)</code> 写成最坏也一定 <code>O(1)</code></td>\n<td>冲突和实现会影响最坏情况</td>\n<td>准确表述为平均查找、插入 <code>O(1)</code></td>\n</tr>\n<tr>\n<td>需要保存“键对应次数”却使用 <code>set</code></td>\n<td>集合只保存唯一键，没有对应值</td>\n<td>使用 <code>map</code> 或 <code>unordered_map</code></td>\n</tr>\n<tr>\n<td>小范围整数也强行使用哈希</td>\n<td>增加实现复杂度和常数开销</td>\n<td>数据范围小时优先考虑直接计数数组</td>\n</tr>\n</tbody>\n</table>\n<h3 id="错误反例推演">错误反例推演</h3>\n<p>容器初始为空，只想判断键 <code>"cat"</code> 是否存在。错误写法读取 <code>count["cat"]</code>，得到数值 <code>0</code> 的同时把 <code>"cat"</code> 插入容器，随后 <code>count.size()</code> 从 <code>0</code> 变成 <code>1</code>；程序会把“查询”变成“修改”。正确写法使用 <code>count.find("cat") == count.end()</code>，判断结果仍是不存在，容器大小保持 <code>0</code>。</p>\n<h2 id="关联学习">关联学习</h2>\n<p>图顶点标签不是连续小整数时，哈希映射可帮助编号；但七级图遍历主示例仍优先使用连续编号和邻接表，保持流程清晰。</p>\n<div id="core-check"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
    ],
    8: [
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>计数原理是八级组合数学的起点。官方要求掌握加法原理、乘法原理、分类计数、分步计数和基础混合计数。本页不引入容斥原理；遇到分类重叠时，先重新划分互不重叠的类别。</p>\n<h2 id="kp-01">01 完整方案与分类、分步</h2>\n<p>计数前先说明“一种完整方案”包含什么。若只需从若干互斥办法中选一种完成任务，这是分类；若必须依次完成若干环节，每种完整方案都要包含每一步，这是分步。</p>\n<p>例如“乘公交或地铁到校”是分类，“先选上衣再选裤子”是分步。判断的关键不是题目里有没有“或”“再”，而是完整方案到底选择一类，还是包含多个步骤。</p>\n<h2 id="kp-02">02 加法原理</h2>\n<p>完成同一件事有若干类互不重叠的办法，只需选择其中一类，总方案数是各类方案数之和。4 条公交线路和 3 条地铁线路都能直达学校，且没有重复线路，因此共有 <code>4 + 3 = 7</code> 种选择。</p>\n<p>使用加法前要检查两件事：分类覆盖所有合法方案；任意方案只属于一个类别。漏掉类别会少算，类别相交会重复计数。</p>\n<h2 id="kp-03">03 乘法原理</h2>\n<p>一件事必须分若干步完成，并且每一步的选择都能与后一步的每种选择组合，总方案数是各步方案数之积。3 种主食各配 2 种饮料，共有 <code>3 × 2 = 6</code> 种套餐。</p>\n<p>若后一步方案数随前一步改变，应分别计算各分支，不能机械地乘一个固定数字。例如选了不同入口后可走的出口数量不同，需要先按入口分类。</p>\n<h2 id="kp-04">04 混合计数与查重</h2>\n<p>复杂计数常同时出现分类和分步。从 A 到 B 可走 2 条陆路或 1 条水路，从 B 到 C 有 3 条路。A 到 B 先分类得到 <code>2 + 1 = 3</code> 种，全程再分步得到 <code>(2 + 1) × 3 = 9</code> 种。</p>\n<p>计算后可用小规模枚举核对：逐个写出完整方案，确认每个方案恰好出现一次。这个检查比只看算式更容易发现漏算和重复。</p>\n<h2 id="kp-05">05 程序中的方案计数</h2>\n<p>当类别很多时，可以用循环累加各类方案数。下面输入互不重叠的 <code>n</code> 类方案数并输出总数。约定 <code>1 &lt;= n &lt;= 100000</code>，结果不超过 <code>long long</code>。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    cin &gt;&gt; n;\n    long long total = 0;    // 每次加入一个互斥类别的方案数，执行的是加法原理。    for (int category = 1; category &lt;= n; category++) {        long long ways;        cin &gt;&gt; ways;        total = total + ways;    }\n    cout &lt;&lt; total &lt;&lt; endl;    return 0;}</code></pre>\n<p>程序的时间复杂度为 <code>O(n)</code>，额外空间复杂度为 <code>O(1)</code>。代码只能完成“类别互不重叠”的求和，不能自动修正错误分类。</p>\n<p><strong>方案树追踪。</strong> 设密码依次填写“字母、数字、数字”，可选数分别为 <code>2、3、2</code>。第一层有 2 个分支；每个分支在第二层再分出 3 个分支；每个第二层分支又有 2 个叶子，所以叶子总数为 <code>2×3×2=12</code>。每个叶子对应一个完整密码，这正是乘法原理。</p>\n<p>再看路线计数：从家到中转站可乘 2 条公交线或 1 条地铁线，从中转站到学校有 4 条步行路线。前半程先按互斥方式相加，得到 <code>2+1=3</code>；全程必须连续完成两段，再相乘，得到 <code>(2+1)×4=12</code>。</p>\n<p>边界检查不能省略：若某一步可选数为 0，那么任何完整方案都无法完成，总数就是 0；若某一类本身有 0 种方案，它只在加法中贡献 0，不会让其他互斥类别消失。规模很大时还要先估算乘积是否超过 <code>long long</code>。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 判断分类还是分步。</strong> 食堂提供米饭套餐 2 种和面条套餐 3 种，学生只买其中一类，完整选择是 <code>2+3=5</code> 种；如果一份套餐要先选 2 种主食之一，再选 3 种饮料之一，每份套餐必须完成两步，方案数才是 <code>2×3=6</code>。</p>\n<p><strong>02 加法原理。</strong> 从 A 到 B 可以坐 2 班公交或 3 班地铁，逐类列出是 <code>{公交1,公交2}</code> 与 <code>{地铁1,地铁2,地铁3}</code>，合并后正好 5 个不同方案。前提是一班车不会同时属于两类。</p>\n<p><strong>03 乘法原理。</strong> 百位可选 <code>{1,2}</code>，十位可选 <code>{0,3}</code>，个位可选 <code>{5,7}</code>。以百位 2 为例，会得到 <code>205、207、235、237</code>；百位 1 也有 4 个，共 <code>2×2×2=8</code> 个。密码 <code>205</code> 的形成顺序是先选 2，再选 0，最后选 5。</p>\n<p><strong>04 混合计数。</strong> A 到 B 有 2 条陆路和 1 条水路，B 到 C 有 3 条路。第一段先分类得到 <code>2+1=3</code>，再与第二段分步相乘，最终 <code>3×3=9</code>。中间结果 3 表示到达 B 的方式数，不是最终答案。</p>\n<p><strong>05 程序计数。</strong> 三层循环分别遍历 <code>2、2、2</code> 个候选，最内层执行 8 次，应输出 8 个数。如果增加“不能出现数字 0”的限制，十位只剩 3，结果变成 <code>2×1×2=4</code>；循环也必须同步跳过 0。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>任务结构</th>\n<th>运算</th>\n<th>使用前检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>从互斥类别中选一种</td>\n<td>加法</td>\n<td>分类完整且不重叠</td>\n</tr>\n<tr>\n<td>依次完成所有步骤</td>\n<td>乘法</td>\n<td>每步选择能正确衔接</td>\n</tr>\n<tr>\n<td>既分类又分步</td>\n<td>分层写算式</td>\n<td>每一层分别检查</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>密码由 1 个大写字母和 2 个互不相同的数字组成。大写字母有 26 种；第一个数字有 10 种；第二个数字不能重复，只有 9 种。因此共有 <code>26 × 10 × 9 = 2340</code> 个密码。这里三个位置都必须填写，所以用乘法；“数字互不相同”改变了第二个数字的方案数。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>重复计数反例。</strong> “到校方式”分成“乘公交”和“乘 1 号线”，但 1 号线本身就是公交线路。若公交共 4 条，其中包含 1 号线，再算 <code>4+1=5</code> 就把同一方案算了两次，真实答案仍是 4。正确做法是让分类互不重叠。</p>\n<ul>\n<li>把“二选一”的互斥类别相乘；正确做法是确认只选一类后相加。</li>\n<li>把“每一步都必须完成”的步骤相加；正确做法是逐步相乘。</li>\n<li>分类有交集却直接相加；应重新划分互斥类别或逐项查重。</li>\n<li>忽略前一步对后一步选择数的影响；应沿每个分支分别计数。</li>\n<li>使用 <code>int</code> 保存很大的方案数；应先估算上界并选择合适整数类型。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/permutations-combinations/">排列与组合</a>：顺序限制会改变每一步的可选数量。</li>\n<li><a href="/gesp/level8/pascal-triangle/">杨辉三角</a>：用递推计算组合数。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求掌握排列、组合、阶乘、排列数与组合数，并能用 DFS 完成基础排列、组合枚举。本页只处理互不相同元素；含重复元素的去重排列不作为本页核心。</p>\n<h2 id="kp-01">01 排列与组合的区别</h2>\n<p>从若干元素中选出一些元素，若交换顺序会产生新方案，就是排列；若只关心选了哪些元素，交换顺序仍是同一方案，就是组合。</p>\n<p>从 5 人中选班长和副班长，职位有顺序，是排列；从 5 人中选 2 人参赛，不分角色，是组合。先问“交换两个人后是否算新方案”，通常就能判断。</p>\n<h2 id="kp-02">02 阶乘与全排列</h2>\n<p><code>n! = n × (n-1) × ... × 2 × 1</code>，并规定 <code>0! = 1</code>。<code>n</code> 个互不相同元素的全排列有 <code>n!</code> 种。例如 1、2、3 的全排列共有 <code>3! = 6</code> 种。</p>\n<p>阶乘增长非常快：<code>10! = 3628800</code>。枚举全排列时不仅要考虑计算时间，还要考虑输出本身已经有 <code>n!</code> 组。</p>\n<h2 id="kp-03">03 部分排列</h2>\n<p>从 <code>n</code> 个不同元素中选 <code>m</code> 个并安排顺序，排列数为：</p>\n<p><code>A(n,m) = n × (n-1) × ... × (n-m+1) = n! / (n-m)!</code></p>\n<p>从 5 人中选班长和副班长，班长有 5 种选择，副班长剩 4 种，共 <code>5 × 4 = 20</code> 种。</p>\n<h2 id="kp-04">04 DFS 生成排列</h2>\n<p>DFS 逐个填写位置，<code>used[value]</code> 表示数字是否已在当前排列中。递归返回后必须撤销标记，才能尝试其他方案。下面输出 <code>1..n</code> 的全排列，约定 <code>1 &lt;= n &lt;= 9</code>。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint n;int path[10];bool used[10];\nvoid generatePermutation(int position) {    if (position &gt; n) {        for (int i = 1; i &lt;= n; i++) {            cout &lt;&lt; path[i] &lt;&lt; (i == n ? \'\\n\' : \' \');        }        return;    }\n    // 当前层选择还没有出现在排列中的数字。    for (int value = 1; value &lt;= n; value++) {        if (used[value]) {            continue;        }        path[position] = value;        used[value] = true;        generatePermutation(position + 1);        // 回到当前层后恢复现场，继续尝试其他数字。        used[value] = false;    }}\nint main() {    cin &gt;&gt; n;    generatePermutation(1);    return 0;}</code></pre>\n<p>时间复杂度为 <code>O(n × n!)</code>，数组和递归栈使用 <code>O(n)</code> 额外空间。</p>\n<h2 id="kp-05">05 组合数与基本性质</h2>\n<p><code>C(n,m)</code> 表示从 <code>n</code> 个不同元素中选 <code>m</code> 个且不考虑顺序的方案数：</p>\n<p><code>C(n,m) = n! / (m!(n-m)!)</code></p>\n<p>边界为 <code>C(n,0)=C(n,n)=1</code>，对称性为 <code>C(n,m)=C(n,n-m)</code>。直接使用阶乘公式时，中间乘积可能先溢出，较大范围应使用下一页的递推预处理。</p>\n<h2 id="kp-06">06 DFS 生成组合</h2>\n<p>组合枚举让所选数字严格递增。参数 <code>start</code> 表示本层允许选择的最小数字，因此会生成 <code>1,3</code>，不会再生成顺序不同但集合相同的 <code>3,1</code>。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint n, m;int selected[25];\nvoid choose(int position, int start) {    if (position &gt; m) {        for (int i = 1; i &lt;= m; i++) {            cout &lt;&lt; selected[i] &lt;&lt; (i == m ? \'\\n\' : \' \');        }        return;    }\n    int remaining = m - position + 1;    int largestStart = n - remaining + 1;    // 只从 start 往后选，保证组合递增且不重复。    for (int value = start; value &lt;= largestStart; value++) {        selected[position] = value;        choose(position + 1, value + 1);    }}\nint main() {    cin &gt;&gt; n &gt;&gt; m;    choose(1, 1);    return 0;}</code></pre>\n<p>程序约定 <code>1 &lt;= m &lt;= n &lt;= 20</code> 且输出规模可接受。时间复杂度为 <code>O(C(n,m) × m)</code>，额外空间为 <code>O(m)</code>。</p>\n<p><strong>位置填写追踪。</strong> 从 <code>A、B、C</code> 中选 2 个按顺序站到第 1、2 个位置：第 1 个位置有 3 种选择；选定后，第 2 个位置只剩 2 种，共 <code>3×2=6</code> 种。<code>AB</code> 与 <code>BA</code> 是不同方案，所以这是排列。</p>\n<p><strong>选与不选追踪。</strong> 从 <code>A、B、C</code> 中选 2 人组队，可按是否选择 <code>A</code> 分类：选择 <code>A</code> 时，还需从 <code>B、C</code> 中选 1 人，共 2 种；不选 <code>A</code> 时，只能选择 <code>B、C</code>，共 1 种，因此 <code>C(3,2)=2+1=3</code>。这正对应组合数递推中的两部分。</p>\n<p>边界要单独核对：<code>n=1</code> 时全排列只有 1 种；<code>m=0</code> 时“一个也不选”仍是一种合法组合；<code>m=n</code> 时只能全部选中。若 <code>m&gt;n</code>，题目输入本身不合法，不能继续套公式或进入 DFS。</p>\n<p><strong>逐段具体推演（对应 01—06）。</strong></p>\n<p><strong>01 判断模型。</strong> 从甲、乙、丙中选两人：若担任班长和副班长，<code>甲乙</code> 与 <code>乙甲</code> 职位不同，所以是排列；若只是组成两人小组，交换后成员没变，所以是组合。</p>\n<p><strong>02 全排列。</strong> 3 人排队时，第 1 位有 3 种选择；假设先放甲，第 2 位只剩 2 种，第 3 位被唯一确定，因此共 <code>3×2×1=6</code>。<code>n=1</code> 时只有排列 <code>1</code>，所以 <code>1!=1</code>，递归应输出一次。</p>\n<p><strong>03 部分排列。</strong> 5 人选班长和副班长：班长 5 种，副班长不能重复，只剩 4 种，中间结果是 <code>5×4=20</code>。只填两个职位，不再乘剩余的 3、2、1。</p>\n<p><strong>04 排列 DFS。</strong> <code>n=3</code> 时先选 1，状态变为 <code>path=[1]、used[1]=true</code>；再选 2、3 后输出 <code>123</code>。返回上一层时先弹出 3 并令 <code>used[3]=false</code>，才可能继续得到 <code>132</code>。</p>\n<p><strong>05 组合数。</strong> 从 5 人选 2 人，先按排列得到 <code>5×4=20</code>；每组如 <code>{甲,乙}</code> 被 <code>甲乙、乙甲</code> 算两次，所以 <code>20÷2=10</code>。<code>C(5,0)=1</code> 表示一人也不选这一种方案。</p>\n<p><strong>06 组合 DFS。</strong> 从 <code>{1,2,3,4}</code> 选 2 个：先选 1 后只尝试 2、3、4，得到 <code>12、13、14</code>；先选 2 后只尝试 3、4，得到 <code>23、24</code>；最后是 <code>34</code>，共 6 个，不会再生成 <code>21</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>是否考虑顺序</th>\n<th>数量</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>全排列</td>\n<td>是</td>\n<td><code>n!</code></td>\n</tr>\n<tr>\n<td>从 n 个选 m 个并安排</td>\n<td>是</td>\n<td><code>A(n,m)</code></td>\n</tr>\n<tr>\n<td>从 n 个选 m 个组成集合</td>\n<td>否</td>\n<td><code>C(n,m)</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>从 6 人中选 3 人，分别担任队长、记录员和联络员。三个职位不同，交换人员会产生新方案，因此是部分排列：<code>A(6,3)=6×5×4=120</code>。若只是组成 3 人小组，则是组合：<code>C(6,3)=20</code>。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>回溯恢复反例。</strong> 第一次输出 <code>123</code> 后若只弹出 3，却忘记把 <code>used[3]</code> 改回 <code>false</code>，后面的分支会一直认为 3 已被占用，<code>132、213</code> 等答案都会丢失。正确顺序是“递归返回 → 弹出本次选择 → 恢复访问标记”。</p>\n<ul>\n<li>只看到“选人”就使用组合；应先检查角色和顺序。</li>\n<li>忘记 <code>0! = 1</code> 或 <code>C(n,0)=1</code>；这些是公式和递推边界。</li>\n<li>排列 DFS 返回后没有恢复 <code>used</code>；会遗漏后续方案。</li>\n<li>组合 DFS 每层都从 1 开始；会产生重复选择和重复组合。</li>\n<li>忽略 <code>n!</code>、<code>C(n,m)</code> 的输出规模与整数溢出。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/pascal-triangle/">杨辉三角</a>：不用阶乘也能递推组合数。</li>\n<li><a href="/template/search/dfs-permutation/">DFS 程序模板</a>：查看排列搜索的标准框架。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求理解杨辉三角、组合数递推、组合数预处理和基础路径计数。本页使用 <code>long long</code> 讲解有限范围的精确整数结果；组合数取模只说明“应按题意在递推时取模”，不扩展逆元等内容。</p>\n<h2 id="kp-01">01 杨辉三角定义与边界</h2>\n<p>杨辉三角每行两侧都是 1，内部每个数等于上一行左上方与右上方两个数之和。按组合数下标书写时，两侧边界是 <code>C(n,0)=1</code> 和 <code>C(n,n)=1</code>。</p>\n<p>第 0 行只有 <code>1</code>；第 1 行是 <code>1 1</code>；第 2 行是 <code>1 2 1</code>。计算任何内部位置前，必须先准备好上一行。</p>\n<h2 id="kp-02">02 组合数递推</h2>\n<p>计算 <code>C(n,k)</code> 时，按第 <code>n</code> 个元素是否被选择分类：</p>\n<ul>\n<li>选择它，还需从前 <code>n-1</code> 个中选 <code>k-1</code> 个；</li>\n<li>不选择它，还需从前 <code>n-1</code> 个中选 <code>k</code> 个。</li>\n</ul>\n<p>两类互不重叠，所以：</p>\n<p><code>C(n,k)=C(n-1,k-1)+C(n-1,k)</code></p>\n<h2 id="kp-03">03 杨辉三角与组合数的关系</h2>\n<p>杨辉三角第 <code>n</code> 行从左到右就是 <code>C(n,0), C(n,1), ..., C(n,n)</code>。例如第 5 行为 <code>1 5 10 10 5 1</code>，因此 <code>C(5,2)=10</code>。</p>\n<p>这种对应关系使多个组合数查询可以先统一预处理，再直接查表，避免每次重复计算阶乘。</p>\n<h2 id="kp-04">04 二维数组预处理</h2>\n<p>下面输入 <code>n,k</code> 并输出 <code>C(n,k)</code>。约定 <code>0 &lt;= k &lt;= n &lt;= 60</code>，结果可由 <code>long long</code> 保存。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n, k;    cin &gt;&gt; n &gt;&gt; k;\n    long long combination[65][65] = {};    // 按行计算，确保使用第 i-1 行时它已经完成。    for (int i = 0; i &lt;= n; i++) {        combination[i][0] = 1;        combination[i][i] = 1;        for (int j = 1; j &lt; i; j++) {            combination[i][j] =                combination[i - 1][j - 1] + combination[i - 1][j];        }    }\n    cout &lt;&lt; combination[n][k] &lt;&lt; endl;    return 0;}</code></pre>\n<p>时间复杂度和空间复杂度都是 <code>O(n²)</code>。组合数增长很快，扩大 <code>n</code> 前必须重新核对整数范围；若题目要求取模，应在每次加法后按指定模数处理。</p>\n<h2 id="kp-05">05 路径计数</h2>\n<p>网格中只能向右或向下走，从起点到终点若必须走 <code>r</code> 步向右、<code>d</code> 步向下，总共 <code>r+d</code> 步。只要从这些位置中选出 <code>d</code> 个放“向下”，其余自动向右，因此路径数是 <code>C(r+d,d)</code>。</p>\n<p>例如走 3 步向右、2 步向下共有 <code>C(5,2)=10</code> 条路径。若出现障碍，直接使用这个公式通常不再成立，需要根据可走位置重新计数。</p>\n<p><strong>逐行填表追踪。</strong> 从 <code>C(0,0)=1</code> 开始：</p>\n<table>\n<thead>\n<tr>\n<th>当前行</th>\n<th>先填边界</th>\n<th>再算内部位置</th>\n<th>完整结果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>n=1</code></td>\n<td><code>C(1,0)=C(1,1)=1</code></td>\n<td>无</td>\n<td><code>1 1</code></td>\n</tr>\n<tr>\n<td><code>n=2</code></td>\n<td>两端为 1</td>\n<td><code>C(2,1)=1+1=2</code></td>\n<td><code>1 2 1</code></td>\n</tr>\n<tr>\n<td><code>n=3</code></td>\n<td>两端为 1</td>\n<td><code>C(3,1)=1+2=3</code>，<code>C(3,2)=2+1=3</code></td>\n<td><code>1 3 3 1</code></td>\n</tr>\n</tbody>\n</table>\n<p>这张表说明必须先有上一行，才能填下一行。基础案例可查 <code>C(3,1)=3</code>；边界案例查 <code>C(n,0)</code> 和 <code>C(n,n)</code>；规模案例则要先判断结果是否超过整数范围。即使数组下标没有越界，组合数本身也可能让 <code>long long</code> 溢出。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 边界。</strong> 第 0 行只有 <code>C(0,0)=1</code>；第 1 行是 <code>1 1</code>；第 2 行先放两侧的 1，中间才由上一行相加得到 <code>1+1=2</code>，所以是 <code>1 2 1</code>。边界必须单独初始化。</p>\n<p><strong>02 递推。</strong> 求 <code>C(5,2)</code> 时，按第 5 个元素是否入选分成两类：选它有 <code>C(4,1)=4</code> 种，不选它有 <code>C(4,2)=6</code> 种，中间结果相加为 10。</p>\n<p><strong>03 对应关系。</strong> 第 5 行是 <code>1 5 10 10 5 1</code>，从左到右分别是 <code>C(5,0)</code> 到 <code>C(5,5)</code>。第 2 列（从 0 开始）是 10，正好表示从 5 个元素选 2 个。</p>\n<p><strong>04 二维表与取模。</strong> 每行先写 <code>c[i][0]=c[i][i]=1</code>，再填内部位置。若题目要求对 7 取模，仍按递推加法后取模：<code>C(5,2)=10</code>，所以 <code>C(5,2) mod 7=3</code>。这个 3 是余数，不是真实组合数。</p>\n<p><strong>05 路径计数。</strong> 必须走 3 次向右、2 次向下，总共 5 步。只要从 5 个位置选出 2 个放“向下”，其余自动向右，所以路径数是 <code>C(5,2)=10</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>项目</th>\n<th>规则</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>两侧边界</td>\n<td><code>C(n,0)=C(n,n)=1</code></td>\n</tr>\n<tr>\n<td>内部递推</td>\n<td><code>C(n,k)=C(n-1,k-1)+C(n-1,k)</code></td>\n</tr>\n<tr>\n<td>对称性</td>\n<td><code>C(n,k)=C(n,n-k)</code></td>\n</tr>\n<tr>\n<td>无障碍网格路径</td>\n<td><code>C(r+d,d)</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>连续查询 <code>C(6,2)</code>、<code>C(6,3)</code> 和 <code>C(6,4)</code>。先预处理到第 6 行，可直接读出 <code>15、20、15</code>。首尾两个结果相等，来自对称性 <code>C(6,2)=C(6,4)</code>。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>边界初始化反例。</strong> 若整个数组初始为 0，却直接执行内部递推，那么第 1 行两侧也会保持 0，后面整张表继续全为 0。正确做法是每行先把 <code>c[i][0]</code> 和 <code>c[i][i]</code> 设为 1，再计算内部位置。</p>\n<ul>\n<li>忘记初始化第 0 行或两侧边界，导致后续整表错误。</li>\n<li>从大行向小行计算，读取了尚未得到的状态。</li>\n<li>把 <code>C(n,k)</code> 与 <code>C(n-1,k-1)</code> 的下标位置写错。</li>\n<li>未估算组合数大小，<code>long long</code> 发生溢出。</li>\n<li>有障碍的网格仍直接套无障碍路径公式。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/permutations-combinations/">排列与组合</a>：理解 <code>C(n,k)</code> 的选择含义。</li>\n<li><a href="/template/math/pascal-triangle/">杨辉三角程序模板</a>：查看可复用的预处理框架。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求掌握倍增思想、二进制拆分、倍增预处理和倍增查询。本页使用“一维后继快速跳转”讲清状态，不把 LCA 作为核心专题；树上祖先只是同一状态含义的应用。</p>\n<h2 id="kp-01">01 倍增思想</h2>\n<p>若每次只向后走一步，多次询问“走 <code>k</code> 步到哪里”会重复大量相同跳转。倍增预先保存走 <code>1、2、4、8……</code> 步的结果，再用少量长跳转组合答案。</p>\n<p>当 <code>k=13</code> 时，不必走 13 次一步跳转，因为 <code>13=8+4+1</code>。只需使用预处理好的三段跳转。</p>\n<h2 id="kp-02">02 状态含义与转移</h2>\n<p>定义 <code>jump[i][j]</code> 为从位置 <code>i</code> 出发走 <code>2^j</code> 步后的到达位置。第 0 层就是题目给出的下一位置。</p>\n<p>走 <code>2^j</code> 步可拆成两段 <code>2^(j-1)</code> 步，因此：</p>\n<p><code>jump[i][j] = jump[jump[i][j-1]][j-1]</code></p>\n<p>若前半段已经到达哨兵 <code>0</code>，完整跳转也记为 <code>0</code>。写转移前必须先用一句话固定状态含义，否则很容易交换“起点”和“层数”下标。</p>\n<h2 id="kp-03">03 二进制拆分查询步数</h2>\n<p>任何非负整数都能写成若干不同的 2 的幂之和。查询时检查 <code>k</code> 的每个二进制位：第 <code>j</code> 位为 1，就执行一次 <code>2^j</code> 步跳转。</p>\n<p>例如 <code>13</code> 的二进制为 <code>1101</code>，对应第 <code>3、2、0</code> 位。各段跳转前后衔接，步数之和恰好为 13。</p>\n<h2 id="kp-04">04 倍增预处理与查询</h2>\n<p>下面处理一维后继关系。<code>next[i]=0</code> 表示离开有效范围，<code>0 &lt;= k &lt;= 10^9</code>，因此保存第 0 到 30 层。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nconst int MAX_N = 100000 + 5;const int LOG = 31;int jumpTable[MAX_N][LOG];\nint main() {    int n, q;    cin &gt;&gt; n &gt;&gt; q;\n    for (int position = 1; position &lt;= n; position++) {        cin &gt;&gt; jumpTable[position][0];    }\n    // 两个连续的 2^(bit-1) 步合成一次 2^bit 步。    for (int bit = 1; bit &lt; LOG; bit++) {        for (int position = 1; position &lt;= n; position++) {            int middle = jumpTable[position][bit - 1];            if (middle == 0) {                jumpTable[position][bit] = 0;            } else {                jumpTable[position][bit] = jumpTable[middle][bit - 1];            }        }    }\n    for (int query = 1; query &lt;= q; query++) {        int position;        long long steps;        cin &gt;&gt; position &gt;&gt; steps;\n        // 第 bit 位为 1 时，使用对应的长跳转。        for (int bit = 0; bit &lt; LOG &amp;&amp; position != 0; bit++) {            if ((steps &amp; (1LL &lt;&lt; bit)) != 0) {                position = jumpTable[position][bit];            }        }        cout &lt;&lt; position &lt;&lt; \'\\n\';    }    return 0;}</code></pre>\n<p>预处理时间和空间都是 <code>O(n log K)</code>，单次查询时间为 <code>O(log K)</code>。<code>LOG</code> 必须覆盖最大步数的最高二进制位。</p>\n<h2 id="kp-05">05 与快速幂的联系</h2>\n<p>快速幂同样把指数拆成二进制位。若当前 <code>base</code> 表示 <code>a^(2^j)</code>，执行 <code>base=base×base</code> 后得到 <code>a^(2^(j+1))</code>。这里是“所代表的指数翻倍”，不是数值简单乘 2。</p>\n<p>倍增跳转保存位置，快速幂保存乘法贡献；两者共同思想都是预先准备二的幂次结果，再按二进制位组合。</p>\n<p><strong>13 步查询追踪。</strong> 设从起点 <code>x</code> 出发，预处理表已经保存走 <code>1、2、4、8</code> 步的位置。因为 <code>13=(1101)₂=8+4+1</code>，查询依次使用第 3、2、0 层：</p>\n<table>\n<thead>\n<tr>\n<th>当前累计步数</th>\n<th>使用层</th>\n<th>本次跳跃</th>\n<th>跳后位置</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>0</td>\n<td>3</td>\n<td>8 步</td>\n<td><code>jump[x][3]</code></td>\n</tr>\n<tr>\n<td>8</td>\n<td>2</td>\n<td>4 步</td>\n<td>从上一位置再查第 2 层</td>\n</tr>\n<tr>\n<td>12</td>\n<td>0</td>\n<td>1 步</td>\n<td>从上一位置再查第 0 层</td>\n</tr>\n</tbody>\n</table>\n<p>基础边界中，<code>k=0</code> 不使用任何层，答案仍是起点；<code>k=1</code> 只查第 0 层。面对最大 <code>k</code>，先确认 <code>LOG</code> 能覆盖它的最高二进制位，并检查跳转中途到达哨兵 <code>0</code> 后是否立即停止。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 二的幂跳跃。</strong> 走 13 步若逐步更新要 13 次；准备 1、2、4、8 步跳转后，只需组合 <code>8+4+1</code> 三段。任何非负整数都能这样拆分。</p>\n<p><strong>02 状态转移。</strong> 链为 <code>1→2→3→4→5→0</code> 时，<code>jump[1][0]=2</code>；走 2 步先到 2 再到 3，所以 <code>jump[1][1]=3</code>；走 4 步是两个 2 步，得到 <code>jump[1][2]=5</code>。</p>\n<p><strong>03 查询。</strong> 从 1 出发走 3 步，<code>3=2+1</code>。先用 2 步表从 1 到 3，再用 1 步表从 3 到 4，最终是 4；走 0 步时不执行跳转，答案仍是 1。</p>\n<p><strong>04 层数。</strong> 最大 <code>k=13</code> 时必须有 8 步层，因为 13 的最高位是 8。每层先检查中间位置是否为 0，0 表示已经不能继续。</p>\n<p><strong>05 快速幂。</strong> 求 <code>3¹³</code> 时依次准备 <code>3¹、3²、3⁴、3⁸</code>，只把指数位为 1 的 <code>3¹、3⁴、3⁸</code> 乘入答案，得到 <code>3¹³</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>项目</th>\n<th>含义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>jump[i][0]</code></td>\n<td>从 i 走 1 步</td>\n</tr>\n<tr>\n<td><code>jump[i][j]</code></td>\n<td>从 i 走 <code>2^j</code> 步</td>\n</tr>\n<tr>\n<td>层数</td>\n<td>覆盖最大查询步数的最高二进制位</td>\n</tr>\n<tr>\n<td>查询</td>\n<td>只使用 k 的二进制位为 1 的层</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>后继关系为 <code>1→2→3→4→0</code>。从 1 走 3 步到 4；走 4 步到 0；走 0 步仍在 1。最后一种边界提醒我们：查询初始答案必须是起点本身，不能先无条件走一步。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>层数不足反例。</strong> 最大查询为 13 步，却只预处理 1、2、4 步三层。执行到二进制的 8 位时没有表项，程序会漏掉 8 步并错误地只走 5 步。正确做法是让预处理覆盖最大 <code>k</code> 的最高位。</p>\n<ul>\n<li>没有先说明 <code>jump[i][j]</code> 的含义，导致转移下标颠倒。</li>\n<li><code>LOG</code> 太小，最大查询的最高位没有被处理。</li>\n<li>到达 <code>0</code> 后仍把它当作普通位置继续查表。</li>\n<li>查询 <code>k=0</code> 时仍执行一步跳转。</li>\n<li>把快速幂的平方写成数值乘 2。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/template/data-structure/doubling/">倍增程序模板</a>：查看倍增表的可复用结构。</li>\n<li><a href="/template/math/quick-pow/">快速幂程序模板</a>：比较相同的二进制拆分思想。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级代数基础包括一元一次方程、二元一次方程的基础求解、表达式化简、公式转程序、等差数列和等比数列入门。本页侧重把已知数学关系准确翻译成程序，不引入高斯消元。</p>\n<h2 id="kp-01">01 方程与退化情况</h2>\n<p>对 <code>ax+b=0</code>，先判断 <code>a</code>：</p>\n<ul>\n<li><code>a≠0</code>：唯一解 <code>x=-b/a</code>；</li>\n<li><code>a=0,b≠0</code>：无解；</li>\n<li><code>a=0,b=0</code>：无数解。</li>\n</ul>\n<p>使用浮点数时，可按题目精度设置 <code>EPS</code>，用 <code>fabs(value)&lt;EPS</code> 判断结果是否接近 0。二元一次方程也要先检查分母对应的行列式是否为 0，再决定唯一解或退化情况。</p>\n<h2 id="kp-02">02 表达式与公式转程序</h2>\n<p>翻译公式时要保留括号、运算顺序和数据类型。例如 <code>s=(a+b)h/2</code> 应写成 <code>(a+b)*h/2.0</code> 或让参与运算的数据使用 <code>double</code>，避免整数除法提前截断。</p>\n<p>平方可写为 <code>x*x</code>；不要用 <code>^</code> 表示乘方，因为 C++ 中 <code>^</code> 是按位异或。长公式可以先计算有明确含义的中间量，使检查更容易。</p>\n<h2 id="kp-03">03 等差数列</h2>\n<p>首项为 <code>a1</code>、公差为 <code>d</code>：</p>\n<ul>\n<li>第 <code>n</code> 项：<code>an=a1+(n-1)d</code>；</li>\n<li>前 <code>n</code> 项和：<code>Sn=n(a1+an)/2</code>。</li>\n</ul>\n<p>例如首项 3、公差 2，第 5 项是 <code>3+4×2=11</code>。公差只增加 <code>n-1</code> 次，这是最常见的下标错误。</p>\n<h2 id="kp-04">04 等比数列入门</h2>\n<p>首项为 <code>a1</code>、公比为 <code>q</code>，第 <code>n</code> 项是 <code>an=a1×q^(n-1)</code>。例如首项 2、公比 3，第 4 项为 <code>2×3^3=54</code>。</p>\n<p>当指数较大时，数值增长很快，要先估算整数范围；需要取模时可关联倍增思想下的快速幂，但本页不把复杂数列求和公式作为核心。</p>\n<p><strong>公式代入追踪。</strong> 梯形上底 3、下底 7、高 4，按“公式 → 代入 → 计算”完成：</p>\n<ol>\n<li>写公式 <code>S=(a+b)h/2</code>；</li>\n<li>代入为 <code>S=(3+7)×4÷2</code>；</li>\n<li>先算括号，再乘高，最后除以 2，得到 <code>20</code>。</li>\n</ol>\n<p>若长度中同时出现米和厘米，单位必须统一后才能代入。方程类案例还要先做退化判断：分母为 0 时不能继续除法；输入系数很大时，要检查乘法和中间结果是否越界；需要小数时，要明确精度与输出格式。</p>\n<p><strong>逐段具体推演（对应 01—04）。</strong></p>\n<p><strong>01 方程。</strong> 一元方程 <code>2x-6=0</code> 得 <code>x=3</code>，代回后 <code>2×3-6=0</code>。二元一次方程组 <code>x+y=3、x-y=1</code> 两式相加得 <code>2x=4</code>，所以 <code>x=2，y=1</code>；代回两式分别得到 3 和 1。两式相同或矛盾时不能硬算唯一解。</p>\n<p><strong>02 公式转程序。</strong> 梯形上底 3、下底 5、高 4，先算括号 <code>3+5=8</code>，再乘高得 32，最后除以 2 得 16。写成 <code>a+b*h/2</code> 会先算乘除，已经改变原公式。</p>\n<p><strong>03 等差数列。</strong> 首项 3、公差 2，第 5 项经历 4 次增加：<code>3→5→7→9→11</code>，所以是 <code>3+(5-1)×2=11</code>；前 5 项和是 <code>(3+11)×5÷2=35</code>。</p>\n<p><strong>04 等比数列。</strong> 首项 2、公比 3，第 4 项过程是 <code>2→6→18→54</code>，共乘 3 次，所以指数是 <code>4-1</code>。<code>q=1</code> 时每项都等于首项。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>内容</th>\n<th>公式或检查</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>一元一次方程</td>\n<td>先判断 a 是否为 0</td>\n</tr>\n<tr>\n<td>公式转程序</td>\n<td>保留括号，检查整数除法</td>\n</tr>\n<tr>\n<td>等差第 n 项</td>\n<td><code>a1+(n-1)d</code></td>\n</tr>\n<tr>\n<td>等差前 n 项和</td>\n<td><code>n(a1+an)/2</code></td>\n</tr>\n<tr>\n<td>等比第 n 项</td>\n<td><code>a1×q^(n-1)</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>输入一元一次方程 <code>ax+b=0</code> 的系数，输出解的类型和唯一解。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nconst double EPS = 1e-9;\nint main() {    double a, b;    cin &gt;&gt; a &gt;&gt; b;\n    // 先处理 a 为 0 的退化情况，避免直接除以 0。    if (fabs(a) &lt; EPS) {        if (fabs(b) &lt; EPS) {            cout &lt;&lt; "many" &lt;&lt; endl;        } else {            cout &lt;&lt; "no" &lt;&lt; endl;        }    } else {        // 只有 a 不为 0 时，方程才有唯一解 -b/a。        double x = -b / a;        cout &lt;&lt; fixed &lt;&lt; setprecision(6) &lt;&lt; x &lt;&lt; endl;    }    return 0;}</code></pre>\n<p>程序只做固定次数运算，时间和额外空间复杂度均为 <code>O(1)</code>。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>整数除法反例。</strong> 用 <code>int</code> 计算 <code>1/2*3</code>，程序先得到 <code>1/2=0</code>，再算出 0，但数学结果应为 1.5。正确做法是使用 <code>double</code>，例如 <code>1.0/2*3</code>，并让结果变量也能保存小数。</p>\n<ul>\n<li>没有判断 <code>a</code> 就计算 <code>-b/a</code>，产生除零。</li>\n<li>把 <code>^</code> 当乘方运算符；应使用乘法或逐次计算幂。</li>\n<li>忽略括号，改变原公式运算顺序。</li>\n<li>在需要小数时仍使用整数除法。</li>\n<li>等差、等比第 <code>n</code> 项误写成增加或相乘 <code>n</code> 次。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/binary-lifting/">倍增法</a>：理解快速幂中的二进制拆分。</li>\n<li><a href="/gesp/level8/algorithm-optimization/">算法优化</a>：用公式替代重复循环。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级平面几何包括长方形、正方形、三角形、圆的周长与面积，以及点坐标、欧几里得距离、曼哈顿距离和基础坐标移动。本页只处理平面上的基础计算，不扩展计算几何高级算法。</p>\n<h2 id="kp-01">01 常见图形的周长与面积</h2>\n<ul>\n<li>长方形：周长 <code>2(a+b)</code>，面积 <code>ab</code>；</li>\n<li>正方形：周长 <code>4a</code>，面积 <code>a²</code>；</li>\n<li>圆：周长 <code>2πr</code>，面积 <code>πr²</code>。</li>\n</ul>\n<p>题目给直径时先换成半径。使用 <code>double</code> 计算圆，并按题目要求设置输出精度；不要把圆周长与面积公式混淆。</p>\n<h2 id="kp-02">02 三角形面积</h2>\n<p>已知底和高时，面积为 <code>base×height/2</code>。已知三点坐标时，可计算两倍有向面积：</p>\n<p><code>cross=(x2-x1)(y3-y1)-(y2-y1)(x3-x1)</code></p>\n<p>普通面积是 <code>|cross|/2</code>。点的顺序可能改变符号，所以必须取绝对值。</p>\n<h2 id="kp-03">03 欧几里得距离与曼哈顿距离</h2>\n<p>两点 <code>(x1,y1)</code>、<code>(x2,y2)</code> 的欧几里得距离是：</p>\n<p><code>sqrt((x2-x1)²+(y2-y1)²)</code></p>\n<p>曼哈顿距离是：</p>\n<p><code>|x2-x1|+|y2-y1|</code></p>\n<p>前者表示直线距离，后者表示只能水平、竖直移动的最少路程。必须根据题目允许的移动方式选择。</p>\n<h2 id="kp-04">04 共线与退化边界</h2>\n<p>三点的叉积为 0，说明三点共线；点重合也是共线的特殊情况。此时“三角形”面积为 0，不是公式失效。</p>\n<p>浮点坐标经过运算后可用 <code>fabs(cross)&lt;EPS</code> 判断接近 0；整数坐标且范围安全时，可用整数叉积避免浮点误差。</p>\n<p><strong>几何计算追踪。</strong> 半径为 <code>2.5</code> 的圆，按“公式 → 代入 → 计算”处理：周长 <code>C=2πr=2×π×2.5=5π</code>，面积 <code>S=πr²=π×2.5×2.5=6.25π</code>。不要在中途把 <code>π</code> 或小数截成整数。</p>\n<p>几何题先做单位换算，再选择公式。若直径为 50 厘米而答案要求米，应先得到半径 <code>0.25</code> 米；坐标范围较大时，平方前要使用足够宽的类型；输出有精度要求时统一在最后设置位数，避免每一步舍入累积误差。</p>\n<p><strong>逐段具体推演（对应 01—04）。</strong></p>\n<p><strong>01 周长与面积。</strong> 长方形长 6、宽 4，周长是 <code>(6+4)×2=20</code>，面积是 <code>6×4=24</code>。两者一个是长度、一个是面积，不能因使用同样的边长就混用公式。</p>\n<p><strong>02 三角形面积。</strong> 底 6、高 4，先算 <code>6×4=24</code>，再除以 2，得到 <code>6×4÷2=12</code>。若题目没给高，不能把另一条边直接当高。</p>\n<p><strong>03 坐标距离。</strong> 从 <code>(1,2)</code> 到 <code>(4,6)</code>，横差 3、纵差 4。允许直线移动时距离是 <code>sqrt(3²+4²)=5</code>；只能横竖移动时是 <code>3+4=7</code>。</p>\n<p><strong>04 退化图形。</strong> 三点 <code>(0,0)、(1,1)、(2,2)</code> 的两倍面积为 <code>|1×2-1×2|=0</code>，说明三点共线。程序应按题意输出 0，而不是把它当作计算失败。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>公式</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>圆周长</td>\n<td><code>2πr</code></td>\n</tr>\n<tr>\n<td>圆面积</td>\n<td><code>πr²</code></td>\n</tr>\n<tr>\n<td>两点直线距离</td>\n<td><code>sqrt(dx²+dy²)</code></td>\n</tr>\n<tr>\n<td>曼哈顿距离</td>\n<td>`</td>\n</tr>\n<tr>\n<td>坐标三角形面积</td>\n<td>`</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>输入三个点，输出前两点的欧几里得距离和三点组成的三角形面积。坐标绝对值不超过 <code>10^4</code>。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    double x1, y1, x2, y2, x3, y3;    cin &gt;&gt; x1 &gt;&gt; y1 &gt;&gt; x2 &gt;&gt; y2 &gt;&gt; x3 &gt;&gt; y3;\n    double dx = x2 - x1;    double dy = y2 - y1;    double distance = sqrt(dx * dx + dy * dy);\n    // 叉积绝对值是对应平行四边形面积。    double cross = (x2 - x1) * (y3 - y1)                 - (y2 - y1) * (x3 - x1);    double area = fabs(cross) / 2.0;\n    cout &lt;&lt; fixed &lt;&lt; setprecision(6);    cout &lt;&lt; distance &lt;&lt; \'\\n\' &lt;&lt; area &lt;&lt; \'\\n\';    return 0;}</code></pre>\n<p>程序时间与额外空间复杂度均为 <code>O(1)</code>。样例点 <code>(1,2)、(4,6)、(5,2)</code> 的前两点距离为 5，三角形面积为 8。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>单位混用反例。</strong> 长方形长 2 米、宽 50 厘米，直接算 <code>2×50=100</code> 并写成平方米是错的。先把 50 厘米换成 0.5 米，面积才是 <code>2×0.5=1</code> 平方米。</p>\n<ul>\n<li>把直径直接代入半径公式。</li>\n<li>三角形底高公式忘记除以 2。</li>\n<li>坐标面积不取绝对值，点顺序改变后得到负面积。</li>\n<li>把曼哈顿距离与欧几里得距离混用。</li>\n<li>坐标差先用范围不足的整数平方，发生溢出。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/algebra-basics/">代数基础</a>：检查公式括号、数据类型和浮点边界。</li>\n<li><a href="/gesp/level8/graph-modeling/">图论综合应用</a>：几何距离可以作为图的边权。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求能够根据实际问题建立图模型，理解图的基本概念和性质，并综合运用已经学习的搜索、并查集、最小生成树和最短路算法。本页重点不是背算法名称，而是把题意中的对象、关系、费用和目标准确地翻译成图。</p>\n<h2 id="kp-01">01 图的基本元素</h2>\n<p>图由顶点和边组成。通常把“对象”看作顶点，把对象之间的“关系”看作边。例如城市是顶点，道路是边；学生是顶点，朋友关系是边。</p>\n<p>建模时先逐项确认：</p>\n<ol>\n<li>顶点代表什么，编号范围是什么；</li>\n<li>哪两个顶点之间有边；</li>\n<li>边是否有方向；</li>\n<li>边上是否带有距离、费用或时间等权值。</li>\n</ol>\n<p>同一段题意可能有不同模型。例如“课程先修关系”有方向，而“双向道路”没有方向。模型一旦建错，后面的算法即使写对也得不到正确答案。</p>\n<h2 id="kp-02">02 方向、权值与存储</h2>\n<p>无向边表示关系可以双向使用；有向边 <code>u → v</code> 只表示从 <code>u</code> 到 <code>v</code>。带权图的边还保存一个数值，如路程、费用或耗时；无权图只关心是否相连。</p>\n<ul>\n<li>邻接矩阵 <code>g[u][v]</code> 查询一条边方便，空间通常为 <code>O(n²)</code>，适合顶点较少或边很密的图。</li>\n<li>邻接表只保存实际存在的边，空间通常为 <code>O(n+m)</code>，适合边较少的图。</li>\n<li>边集直接保存每条边的两个端点和权值，适合 Kruskal 这类按边处理的算法。</li>\n</ul>\n<p>无向边写入邻接矩阵或邻接表时，通常要同时保存 <code>u → v</code> 和 <code>v → u</code>。</p>\n<h2 id="kp-03">03 从任务目标选择算法</h2>\n<p>算法由任务目标决定，而不是由“题目里出现了图”决定：</p>\n<table>\n<thead>\n<tr>\n<th>任务目标</th>\n<th>常见选择</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>遍历可达顶点、统计连通块</td>\n<td>DFS 或 BFS</td>\n</tr>\n<tr>\n<td>动态判断两个顶点是否连通</td>\n<td>并查集</td>\n</tr>\n<tr>\n<td>让所有顶点连通且总边权最小</td>\n<td>Kruskal 或 Prim</td>\n</tr>\n<tr>\n<td>一个起点到其他顶点的最短距离，边权非负</td>\n<td>Dijkstra</td>\n</tr>\n<tr>\n<td>多个起点、终点之间的最短距离，顶点较少</td>\n<td>Floyd</td>\n</tr>\n</tbody>\n</table>\n<p>选择前还要检查数据范围和边权条件。Dijkstra 不能直接处理负权边；Floyd 的 <code>O(n³)</code> 时间也不适合很大的 <code>n</code>。</p>\n<h2 id="kp-04">04 连通、路径与代价</h2>\n<p>“连通”只说明能否从一个顶点到达另一个顶点；“路径”是一串首尾相接的边；“路径代价”是路径上各边权值之和。三者不能混为一谈。</p>\n<p>如果题目只问是否可达，就不必计算最短距离；如果要求最低费用，就不能只找到任意一条路径。最小生成树求的是连接全部顶点的最小总边权，最短路求的是指定顶点之间的最小路径代价，两者目标不同。</p>\n<p><strong>问题 → 模型 → 算法的完整推理：</strong></p>\n<table>\n<thead>\n<tr>\n<th>实际问题</th>\n<th>顶点与边</th>\n<th>目标</th>\n<th>合适算法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>校园路线中找教学楼 A 到食堂的最短步行距离</td>\n<td>地点为顶点，通道为带长度的无向边</td>\n<td>固定起点到终点最短路</td>\n<td>非负权时用 Dijkstra</td>\n</tr>\n<tr>\n<td>通信网络用最低总费用连通全部基站</td>\n<td>基站为顶点，可建线路为无向带权边</td>\n<td>全部连通且总费用最小</td>\n<td>Kruskal 或 Prim</td>\n</tr>\n<tr>\n<td>社交网络中统计互不相连的朋友圈</td>\n<td>用户为顶点，好友关系为无向边</td>\n<td>统计连通块</td>\n<td>DFS、BFS 或并查集</td>\n</tr>\n</tbody>\n</table>\n<p>建模后的自检顺序是：方向是否正确、重边如何处理、边权是否满足算法条件、数据规模是否允许该存储与复杂度。先完成这四项，再写算法。</p>\n<p><strong>逐段具体推演（对应 01—04）。</strong></p>\n<p><strong>01 先找对象和关系。</strong> 校园有地点 A、B、C、D，道路为 A—B、A—C、C—D。模型中的顶点是 <code>{A,B,C,D}</code>，边是这 3 条道路；“A 能否到 D”被转成“图中是否存在 A 到 D 的路径”，答案是 A—C—D。</p>\n<p><strong>02 决定方向、权值和存储。</strong> 若 A 到 B 只允许单向通行，就只建 <code>A→B</code>；双向道路要同时表达两个方向。道路长度分别为 5、2、4 时，权值保存这些长度。4 个点的示例可用邻接矩阵，点多边少时邻接表更节省空间。</p>\n<p><strong>03 根据目标选择算法。</strong> 同一张图若问“从 A 到 D 最少花费”，且边权非负，选择 Dijkstra；若问“修哪些道路让四地全部连通且总造价最低”，选择最小生成树；若问很多组任意两点最短路且点数较小，才考虑 Floyd。</p>\n<p><strong>04 把答案翻译回题意。</strong> 在 A—B、A—C、C—D 中，A 与 D 连通，路径 A—C—D 经过 2 条边；若边权为 2 和 4，路径代价是 6。连通、边数最少和权值和最小是三个不同目标，必须按题目要求输出。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>题意关键词</th>\n<th>先确认</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>单向、先后、依赖</td>\n<td>有向边</td>\n</tr>\n<tr>\n<td>双向、互通</td>\n<td>无向边</td>\n</tr>\n<tr>\n<td>距离、费用、耗时</td>\n<td>边权</td>\n</tr>\n<tr>\n<td>全部连通且总费用最小</td>\n<td>最小生成树</td>\n</tr>\n<tr>\n<td>从固定起点出发的最低费用</td>\n<td>单源最短路</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>有 <code>n</code> 个城市和 <code>m</code> 条双向道路，每条道路有费用。若要求从城市 <code>1</code> 到城市 <code>n</code> 的最低费用，应把城市建成顶点、道路建成无向带权边，目标是固定起点到终点的最短路。若所有费用非负，可选择 Dijkstra；不能因为题目出现“连接城市”就误选最小生成树。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>模型选错反例。</strong> 题目要求“用最低总造价连接所有村庄”，若误用 Dijkstra 从 1 号村求最短路树，在边 <code>(1,2,2)、(1,3,2)、(2,3,1)</code> 中会选两条代价 2 的边，总价 4；最小生成树应选权值 1 和 2，总价 3。两者目标不同，不能只因都出现“最小”就混用。</p>\n<ul>\n<li>把双向道路只存一次，导致只能单向到达。</li>\n<li>只看“最小费用”四个字，不区分最小生成树与最短路。</li>\n<li>忽略边权是否为负，直接使用 Dijkstra。</li>\n<li>顶点编号是 <code>1～n</code>，数组却只开到 <code>n-1</code>。</li>\n<li>没有结合 <code>n</code>、<code>m</code> 判断邻接矩阵、邻接表和算法复杂度是否可行。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/minimum-spanning-tree/">最小生成树</a>：解决“全部连通且总边权最小”。</li>\n<li><a href="/gesp/level8/shortest-path/">单源最短路</a>：解决起点到其他顶点的最短距离。</li>\n<li><a href="/gesp/level8/time-efficiency/">时间效率分析</a>：用数据范围检验算法是否可行。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求掌握最小生成树问题，理解 Kruskal 和 Prim 算法，并能够分析、比较它们的适用场景。两种算法都用于无向带权连通图，目标是用 <code>n-1</code> 条边连接全部 <code>n</code> 个顶点，并使边权总和最小。</p>\n<h2 id="kp-01">01 生成树与最小生成树</h2>\n<p>连通无向图的一棵生成树包含全部顶点、保持连通、没有环，并且恰好有 <code>n-1</code> 条边。最小生成树是在所有生成树中边权总和最小的一棵。</p>\n<p>“最小”指整棵树的总边权最小，不保证任意两个顶点之间的路径都最短。如果原图不连通，就不存在覆盖全部顶点的生成树。</p>\n<h2 id="kp-02">02 并查集基础</h2>\n<p>Kruskal 需要快速判断一条边的两个端点是否已经连通。并查集为每个连通块维护一个代表元：</p>\n<ul>\n<li><code>find(x)</code> 找到 <code>x</code> 所属集合的代表元；</li>\n<li>若 <code>find(u) != find(v)</code>，连接 <code>u</code>、<code>v</code> 不会成环，可以合并两个集合；</li>\n<li>路径压缩让后续查找更快。</li>\n</ul>\n<p>初始化时每个顶点单独成集合，即 <code>parent[i] = i</code>。</p>\n<h2 id="kp-03">03 Kruskal 算法</h2>\n<p>Kruskal 从边出发：</p>\n<ol>\n<li>把所有边按权值从小到大排序；</li>\n<li>依次检查每条边；</li>\n<li>若两个端点尚未连通，就选择这条边并合并集合；</li>\n<li>选满 <code>n-1</code> 条边后结束。</li>\n</ol>\n<p>排序是主要耗时，常见复杂度为 <code>O(m log m)</code>。它只保存边集，通常适合边较少的稀疏图。</p>\n<h2 id="kp-04">04 Prim 算法</h2>\n<p>Prim 从顶点出发。先选择任意顶点加入生成树，每次在“已加入顶点”和“未加入顶点”之间选择权值最小的边，把新顶点加入。</p>\n<p>使用邻接矩阵和线性寻找最小值的朴素 Prim，时间复杂度为 <code>O(n²)</code>，适合顶点不多或边很密的图。每轮选择的是连接当前生成树与外部顶点的最小边，不是全图尚未使用的最小边。</p>\n<h2 id="kp-05">05 选择与边界</h2>\n<table>\n<thead>\n<tr>\n<th>比较</th>\n<th>Kruskal</th>\n<th>朴素 Prim</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>主要视角</td>\n<td>按边从小到大</td>\n<td>从一个顶点逐步扩展</td>\n</tr>\n<tr>\n<td>常见存储</td>\n<td>边集</td>\n<td>邻接矩阵</td>\n</tr>\n<tr>\n<td>常见复杂度</td>\n<td><code>O(m log m)</code></td>\n<td><code>O(n²)</code></td>\n</tr>\n<tr>\n<td>常见场景</td>\n<td>稀疏图</td>\n<td>顶点较少或稠密图</td>\n</tr>\n</tbody>\n</table>\n<p>边权可以为负，最小生成树仍然成立。存在相同权值时，最小生成树可能不唯一，但最小总权值相同。实现结束后必须检查是否选到了 <code>n-1</code> 条边，否则图不连通。</p>\n<p><strong>Kruskal 选边追踪。</strong> 四个城市的候选道路按权值排序为 <code>AB=1、BC=2、AC=3、CD=4</code>：</p>\n<table>\n<thead>\n<tr>\n<th>检查边</th>\n<th>两端是否已连通</th>\n<th>决定</th>\n<th>已选边数</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>AB=1</code></td>\n<td>否</td>\n<td>选择并合并 A、B</td>\n<td>1</td>\n</tr>\n<tr>\n<td><code>BC=2</code></td>\n<td>否</td>\n<td>选择并合并 <code>{A,B}</code> 与 C</td>\n<td>2</td>\n</tr>\n<tr>\n<td><code>AC=3</code></td>\n<td>是</td>\n<td>跳过，避免成环</td>\n<td>2</td>\n</tr>\n<tr>\n<td><code>CD=4</code></td>\n<td>否</td>\n<td>选择并合并 D</td>\n<td>3</td>\n</tr>\n</tbody>\n</table>\n<p>总权值为 <code>1+2+4=7</code>，选满 <code>n-1=3</code> 条边后停止。</p>\n<p><strong>Prim 扩展追踪。</strong> 仍从 A 开始：树内为 <code>{A}</code> 时选跨边 <code>AB=1</code>；树内为 <code>{A,B}</code> 时选 <code>BC=2</code>；树内为 <code>{A,B,C}</code> 时选 <code>CD=4</code>。每轮只比较“树内到树外”的边。单顶点图 <code>n=1</code> 需要选择 0 条边，总权值为 0；不连通图最终无法完成 <code>n-1</code> 次加入。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 生成树的目标。</strong> 4 个点全部连通且无环时必须恰好选 3 条边。若选 2 条，至少有点未连上；若在 4 个点上选 4 条并仍连通，就一定形成环。最小生成树是在所有这种 3 边方案中比较总权值。</p>\n<p><strong>02 并查集状态。</strong> 开始时集合从 <code>{1}{2}{3}{4}</code> 出发。合并 1—2 后是 <code>{1,2}{3}{4}</code>；再合并 2—3 后是 <code>{1,2,3}{4}</code>。此时检查 1—3，两个端点根相同，加入它会成环，所以跳过。</p>\n<p><strong>03 Kruskal 选边。</strong> 边按权值排序为 <code>(2,3,1)、(1,2,2)、(1,3,3)、(3,4,4)</code>。依次选前两条后集合为 <code>{1,2,3}{4}</code>；第三条成环跳过；选择第四条后全部连通，总权值 <code>1+2+4=7</code>。</p>\n<p><strong>04 Prim 扩展。</strong> 从 1 开始，已选集合 <code>{1}</code>，到 2、3 的最小连接代价为 2、3；先加入 2，并通过边 2—3 把 3 的候选从 3 更新为 1；再加入 3，最后用 3—4 的 4 加入 4，总价仍是 7。</p>\n<p><strong>05 边界与选择。</strong> 单顶点图 <code>n=1、m=0</code> 已经连通，需要 0 条边，最小生成树权值为 0。边多时 Kruskal 的边排序直观；点少且图稠密时，邻接矩阵 Prim 的 <code>O(n²)</code> 更直接，但两者都要求无向、带权、连通。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>检查项</th>\n<th>结论</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>图类型</td>\n<td>无向带权图</td>\n</tr>\n<tr>\n<td>选边数量</td>\n<td><code>n-1</code></td>\n</tr>\n<tr>\n<td>是否允许成环</td>\n<td>不允许</td>\n</tr>\n<tr>\n<td>Kruskal 判环</td>\n<td>并查集</td>\n</tr>\n<tr>\n<td>Prim 当前范围</td>\n<td>已加入与未加入顶点之间</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>下面程序使用 Kruskal 求最小生成树权值；若图不连通，输出 <code>Impossible</code>。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nstruct Edge {    int u;    int v;    int w;};\nbool cmp(Edge a, Edge b) {    return a.w &lt; b.w;}\nint parent[100005];\nint findRoot(int x) {    if (parent[x] == x) {        return x;    }    parent[x] = findRoot(parent[x]);    return parent[x];}\nint main() {    int n, m;    cin &gt;&gt; n &gt;&gt; m;    vector&lt;Edge&gt; edges(m);    for (int i = 0; i &lt; m; i++) {        cin &gt;&gt; edges[i].u &gt;&gt; edges[i].v &gt;&gt; edges[i].w;    }\n    for (int i = 1; i &lt;= n; i++) {        parent[i] = i;    }    // Kruskal 必须按边权从小到大检查。    sort(edges.begin(), edges.end(), cmp);\n    long long answer = 0;    int chosen = 0;    for (int i = 0; i &lt; m; i++) {        int rootU = findRoot(edges[i].u);        int rootV = findRoot(edges[i].v);        // 两端不在同一集合时，加入这条边不会形成环。        if (rootU != rootV) {            parent[rootU] = rootV;            answer += edges[i].w;            chosen++;            // 生成树已经有 n-1 条边，可以提前结束。            if (chosen == n - 1) {                break;            }        }    }\n    if (chosen == n - 1) {        cout &lt;&lt; answer &lt;&lt; \'\\n\';    } else {        cout &lt;&lt; "Impossible\\n";    }    return 0;}</code></pre>\n<p>Prim 的对应过程是维护每个未加入顶点到当前生成树的最小边权 <code>dist</code>，每轮选择最小的 <code>dist</code> 并更新其他顶点。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nconst int INF = 0x3f3f3f3f;int graph[1005][1005];int distToTree[1005];bool used[1005];\nint main() {    int n, m;    cin &gt;&gt; n &gt;&gt; m;    memset(graph, 0x3f, sizeof(graph));    for (int i = 1; i &lt;= n; i++) {        graph[i][i] = 0;    }    for (int i = 0; i &lt; m; i++) {        int u, v, w;        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;        graph[u][v] = min(graph[u][v], w);        graph[v][u] = min(graph[v][u], w);    }\n    for (int i = 1; i &lt;= n; i++) {        distToTree[i] = INF;    }    distToTree[1] = 0;\n    long long answer = 0;    // 每轮向当前生成树加入一个连接代价最小的顶点。    for (int round = 1; round &lt;= n; round++) {        int u = -1;        for (int i = 1; i &lt;= n; i++) {            if (!used[i] &amp;&amp; (u == -1 || distToTree[i] &lt; distToTree[u])) {                u = i;            }        }        // 还有顶点却找不到有限连接边，说明原图不连通。        if (u == -1 || distToTree[u] == INF) {            cout &lt;&lt; "Impossible\\n";            return 0;        }\n        used[u] = true;        answer += distToTree[u];        // 用新加入顶点的边更新其余顶点的最小连接代价。        for (int v = 1; v &lt;= n; v++) {            if (!used[v]) {                distToTree[v] = min(distToTree[v], graph[u][v]);            }        }    }\n    cout &lt;&lt; answer &lt;&lt; \'\\n\';    return 0;}</code></pre>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>忘记判环反例。</strong> 三角形三条边权分别为 1、2、3，若 Kruskal 只按从小到大拿边却不检查并查集，会把三条都选中，总价 6 且形成环。正确算法选权值 1、2 的两条边后已经连接 3 个点，第三条必须跳过，总价是 3。</p>\n<ul>\n<li>把最小生成树误当成从某个起点出发的最短路。</li>\n<li>Kruskal 选边前没有排序，或忘记用并查集判环。</li>\n<li>Prim 选择了全图最小边，而不是跨越当前生成树边界的最小边。</li>\n<li>忽略重边，没有保留更小权值。</li>\n<li>图不连通时仍输出部分森林的权值。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/graph-modeling/">图论综合应用</a>：确认题目目标确实是连接全部顶点。</li>\n<li><a href="/gesp/level8/shortest-path/">单源最短路</a>：辨析“总连接代价”与“路径代价”。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求掌握单源最短路，理解 Dijkstra 算法，并能比较 Dijkstra 与 Floyd 的适用场景。Dijkstra 解决一个起点到其他顶点的最短距离，要求边权非负；Floyd 求所有顶点对之间的最短距离，适合顶点较少的图。</p>\n<h2 id="kp-01">01 最短路模型与松弛</h2>\n<p>路径长度是路径上所有边权之和，最短路是在可达路径中总权值最小的一条。<code>dist[v]</code> 表示当前已知从起点到 <code>v</code> 的最短距离上界。</p>\n<p>若已知到 <code>u</code> 的距离为 <code>dist[u]</code>，边 <code>u → v</code> 权值为 <code>w</code>，且 <code>dist[u]+w &lt; dist[v]</code>，就更新：</p>\n<p><code>dist[v] = dist[u] + w</code></p>\n<p>这个更新叫松弛。初始化时起点距离为 0，其他顶点为足够大的无穷值。</p>\n<h2 id="kp-02">02 Dijkstra 算法</h2>\n<p>朴素 Dijkstra 每轮：</p>\n<ol>\n<li>在尚未确定的顶点中选 <code>dist</code> 最小的顶点 <code>u</code>；</li>\n<li>标记 <code>u</code> 的最短距离已经确定；</li>\n<li>用 <code>u</code> 松弛所有相邻顶点。</li>\n</ol>\n<p>非负边权保证一旦 <code>u</code> 是未确定顶点中距离最小的顶点，以后就不会出现更短路径。邻接矩阵实现的时间复杂度为 <code>O(n²)</code>。Dijkstra 不能直接处理负权边。</p>\n<h2 id="kp-03">03 Floyd 算法</h2>\n<p>Floyd 维护 <code>dist[i][j]</code>，表示当前允许经过一部分中间顶点时，<code>i</code> 到 <code>j</code> 的最短距离。依次允许顶点 <code>k</code> 作为中间点：</p>\n<p><code>dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])</code></p>\n<p>循环顺序必须让 <code>k</code> 在最外层。时间复杂度为 <code>O(n³)</code>，空间复杂度为 <code>O(n²)</code>。它一次得到所有顶点对的答案，适合顶点较少、询问很多的情形。</p>\n<h2 id="kp-04">04 选择、复杂度与边界</h2>\n<table>\n<thead>\n<tr>\n<th>比较</th>\n<th>Dijkstra</th>\n<th>Floyd</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>目标</td>\n<td>一个起点到其他顶点</td>\n<td>所有顶点对</td>\n</tr>\n<tr>\n<td>边权要求</td>\n<td>非负</td>\n<td>可有负边，但不能有负环</td>\n</tr>\n<tr>\n<td>朴素时间</td>\n<td><code>O(n²)</code></td>\n<td><code>O(n³)</code></td>\n</tr>\n<tr>\n<td>常见空间</td>\n<td><code>O(n²)</code> 或邻接表</td>\n<td><code>O(n²)</code></td>\n</tr>\n</tbody>\n</table>\n<p>两点不可达时应输出题目规定的特殊值。多个重边只保留最小权值。做加法前要确认两个距离都不是无穷值，并用足够大的整数类型避免溢出。</p>\n<p><strong>Dijkstra 松弛追踪。</strong> 有边 <code>1→2=4、1→3=1、3→2=2</code>，从 1 出发：</p>\n<table>\n<thead>\n<tr>\n<th>轮次</th>\n<th>本轮确定</th>\n<th>松弛后 <code>dist[1..3]</code></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>初始化</td>\n<td>1</td>\n<td><code>0, 4, 1</code></td>\n</tr>\n<tr>\n<td>第 2 轮</td>\n<td>3</td>\n<td><code>0, 3, 1</code></td>\n</tr>\n<tr>\n<td>第 3 轮</td>\n<td>2</td>\n<td><code>0, 3, 1</code></td>\n</tr>\n</tbody>\n</table>\n<p>顶点 2 虽有直接边权 4，但经过 3 的路径权值是 <code>1+2=3</code>，因此被更新。</p>\n<p><strong>Floyd 矩阵追踪。</strong> 若初始有 <code>1→2=4、2→3=2、1→3=10</code>，允许顶点 2 作为中间点后，<code>dist[1][3]</code> 从 10 更新为 <code>4+2=6</code>。每个 <code>k</code> 都代表“新允许一个中间点”的完整阶段，所以 <code>k</code> 必须在最外层。</p>\n<p>边界案例包括起点到自身为 0、不可达保持无穷、重边取最小值。Floyd 可以处理负边，但若最终出现 <code>dist[i][i]&lt;0</code>，说明存在负环，普通最短距离不再有有限答案。</p>\n<p><strong>逐段具体推演（对应 01—04）。</strong></p>\n<p><strong>01 建模与松弛。</strong> 有向边 <code>1→2(2)、1→3(8)、2→3(3)</code>，从 1 出发先得到 <code>dist=[0,2,8,∞]</code>（按点 1—4 写）。经过 2 到 3 的候选是 <code>2+3=5</code>，比原来的 8 小，更新后 <code>dist=[0,2,5,∞]</code>；这一次比较和替换就是松弛。</p>\n<p><strong>02 Dijkstra 每轮状态。</strong> 初始只确定点 1；第一轮更新后选距离最小的点 2，使用它把点 3 从 8 降为 5；下一轮确定点 3。若点 4 没有入边，最终仍为无穷大，按题意输出不可达标记。</p>\n<p><strong>03 Floyd 的阶段。</strong> 初始 <code>dist[1][3]=8</code>。当允许点 2 作为中间点时，比较直达 8 与 <code>dist[1][2]+dist[2][3]=2+3=5</code>，矩阵中的 <code>[1][3]</code> 更新为 5。<code>k</code> 放最外层表示“本阶段新允许哪个中间点”。</p>\n<p><strong>04 选择与边界。</strong> 固定一个起点且边权非负，优先 Dijkstra；需要所有点对答案、<code>n</code> 较小且 <code>O(n³)</code> 可接受，使用 Floyd。Dijkstra 遇到负边会失去正确性；Floyd 若要判断负环，可在计算后检查某个 <code>dist[i][i]&lt;0</code>。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>关键词</th>\n<th>处理</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>固定起点、边权非负</td>\n<td>Dijkstra</td>\n</tr>\n<tr>\n<td>任意两点、多次询问、顶点较少</td>\n<td>Floyd</td>\n</tr>\n<tr>\n<td>更新更短路径</td>\n<td>松弛</td>\n</tr>\n<tr>\n<td>Dijkstra 已确定顶点</td>\n<td>不再重复选择</td>\n</tr>\n<tr>\n<td>Floyd 中间点</td>\n<td><code>k</code> 放最外层</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>下面用邻接矩阵实现朴素 Dijkstra，求起点 <code>s</code> 到各顶点的最短距离：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nconst long long INF = 4e18;long long graph[1005][1005];long long distFromStart[1005];bool used[1005];\nint main() {    int n, m, s;    cin &gt;&gt; n &gt;&gt; m &gt;&gt; s;    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= n; j++) {            graph[i][j] = (i == j ? 0 : INF);        }    }    for (int i = 0; i &lt; m; i++) {        int u, v;        long long w;        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;        graph[u][v] = min(graph[u][v], w);    }\n    for (int i = 1; i &lt;= n; i++) {        distFromStart[i] = INF;    }    distFromStart[s] = 0;\n    // 每轮确定一个当前距离最小且尚未确定的顶点。    for (int round = 1; round &lt;= n; round++) {        int u = -1;        for (int i = 1; i &lt;= n; i++) {            if (!used[i] &amp;&amp; (u == -1 || distFromStart[i] &lt; distFromStart[u])) {                u = i;            }        }        // 剩余顶点都不可达时，不再继续松弛。        if (u == -1 || distFromStart[u] == INF) {            break;        }        used[u] = true;\n        // 尝试经过 u 缩短起点到每个 v 的距离。        for (int v = 1; v &lt;= n; v++) {            if (graph[u][v] != INF) {                distFromStart[v] = min(                    distFromStart[v],                    distFromStart[u] + graph[u][v]                );            }        }    }\n    for (int i = 1; i &lt;= n; i++) {        if (distFromStart[i] == INF) {            cout &lt;&lt; -1;        } else {            cout &lt;&lt; distFromStart[i];        }        cout &lt;&lt; (i == n ? \'\\n\' : \' \');    }    return 0;}</code></pre>\n<p>Floyd 的完整核心程序如下，输入为有向图，最后输出任意两点最短距离：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nconst long long INF = 4e18;long long dist[505][505];\nint main() {    int n, m;    cin &gt;&gt; n &gt;&gt; m;    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= n; j++) {            dist[i][j] = (i == j ? 0 : INF);        }    }    for (int i = 0; i &lt; m; i++) {        int u, v;        long long w;        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;        dist[u][v] = min(dist[u][v], w);    }\n    // 第 k 阶段允许把顶点 k 作为路径中间点。    for (int k = 1; k &lt;= n; k++) {        for (int i = 1; i &lt;= n; i++) {            for (int j = 1; j &lt;= n; j++) {                // 两段都可达时才能相加，避免 INF 参与运算。                if (dist[i][k] != INF &amp;&amp; dist[k][j] != INF) {                    dist[i][j] = min(                        dist[i][j],                        dist[i][k] + dist[k][j]                    );                }            }        }    }\n    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= n; j++) {            if (dist[i][j] == INF) {                cout &lt;&lt; -1;            } else {                cout &lt;&lt; dist[i][j];            }            cout &lt;&lt; (j == n ? \'\\n\' : \' \');        }    }    return 0;}</code></pre>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>INF 相加反例。</strong> 若用很大的数表示不可达，<code>dist[i][k]</code> 本身是 INF 时仍计算 <code>INF+w</code>，固定宽度整数可能溢出成负数，程序反而把它当成更短路径。正确做法是在相加前确认两段都可达，并让 INF 与数据上界留有安全余量。</p>\n<ul>\n<li>有负权边仍直接使用 Dijkstra。</li>\n<li>Dijkstra 忘记选择尚未确定且距离最小的顶点。</li>\n<li>Floyd 把 <code>k</code> 放在内层，破坏阶段含义。</li>\n<li>重边直接覆盖，未保留最小权值。</li>\n<li>把无穷值相加，导致溢出或产生假路径。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/graph-modeling/">图论综合应用</a>：从题意辨认单源或多源任务。</li>\n<li><a href="/gesp/level8/time-efficiency/">时间效率分析</a>：根据 <code>n</code> 判断 <code>O(n²)</code> 或 <code>O(n³)</code> 是否可行。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求理解算法的时间效率，能够分析循环、递归和常见算法的时间复杂度，并根据数据规模判断程序是否可行。复杂度描述的是输入规模增大时操作次数的增长速度，不等于程序实际运行的秒数。</p>\n<h2 id="kp-01">01 常见时间复杂度</h2>\n<p>常见增长速度从慢到快大致为：</p>\n<p><code>O(1) &lt; O(log n) &lt; O(n) &lt; O(n log n) &lt; O(n²) &lt; O(n³) &lt; O(2ⁿ) &lt; O(n!)</code></p>\n<p>分析时保留增长最快的项并省略常数。例如 <code>3n²+10n+5</code> 记为 <code>O(n²)</code>。这不是说低次项不存在，而是当 <code>n</code> 很大时，最高次项决定主要增长趋势。</p>\n<h2 id="kp-02">02 顺序、分支与循环</h2>\n<p>顺序执行的几段代码，复杂度取其中增长最快的一段；<code>if...else</code> 通常看最坏情况下较慢的分支。</p>\n<ul>\n<li>单层循环执行约 <code>n</code> 次，通常是 <code>O(n)</code>。</li>\n<li>两层循环都执行约 <code>n</code> 次，通常是 <code>O(n²)</code>。</li>\n<li>循环变量每次乘 2，如 <code>i = i * 2</code>，通常执行约 <code>log₂n</code> 次。</li>\n<li>嵌套循环不能只看层数，还要看每层的实际边界。例如内层执行 <code>i</code> 次，总次数是 <code>1+2+...+n</code>，仍为 <code>O(n²)</code>。</li>\n</ul>\n<h2 id="kp-03">03 数据范围与可行性</h2>\n<p>同一个算法在不同数据范围下结论不同。若 <code>n=1000</code>，<code>n²</code> 约为一百万，常常可以接受；若 <code>n=100000</code>，<code>n²</code> 达到一百亿，通常不可行。</p>\n<p>判断时还要考虑常数、输入输出、语言和评测环境，因此不能把某个操作次数当作绝对边界。稳妥做法是先估算数量级，再给速度较慢的操作留出余量。</p>\n<h2 id="kp-04">04 常见算法的复杂度</h2>\n<table>\n<thead>\n<tr>\n<th>算法或操作</th>\n<th>常见时间复杂度</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>数组按下标访问</td>\n<td><code>O(1)</code></td>\n</tr>\n<tr>\n<td>扫描整个数组</td>\n<td><code>O(n)</code></td>\n</tr>\n<tr>\n<td>二分查找</td>\n<td><code>O(log n)</code></td>\n</tr>\n<tr>\n<td>常见高效排序</td>\n<td><code>O(n log n)</code></td>\n</tr>\n<tr>\n<td>邻接矩阵 Dijkstra</td>\n<td><code>O(n²)</code></td>\n</tr>\n<tr>\n<td>Floyd</td>\n<td><code>O(n³)</code></td>\n</tr>\n<tr>\n<td>枚举全部子集</td>\n<td><code>O(2ⁿ)</code></td>\n</tr>\n</tbody>\n</table>\n<p>表中的结果要结合具体实现使用。例如使用不同图存储结构时，Dijkstra 的复杂度也会不同。</p>\n<p><strong>操作次数追踪。</strong> 外层让 <code>i</code> 从 1 增加到 <code>n</code>，内层让 <code>j</code> 从 1 增加到当前的 <code>i</code>。内层次数依次为 <code>1、2、…、n</code>，总次数是 <code>n(n+1)/2</code>，所以是 <code>O(n²)</code>，不是看到两个循环就机械写 <code>n×n</code>。</p>\n<p>规模判断要落到数量级：<code>n=20</code> 时枚举全部子集约有 <code>2^20≈10^6</code> 个，可能可行；<code>n=100000</code> 时 <code>O(n²)</code> 约为 <code>10^10</code> 次，通常不可行。对同一张 <code>n=300</code> 的稠密图，朴素 Dijkstra 约按 <code>n²</code> 增长，而 Floyd 按 <code>n³</code> 增长，是否需要任意两点答案会直接影响选择。</p>\n<p><strong>逐段具体推演（对应 01—04）。</strong></p>\n<p><strong>01 增长速度。</strong> <code>n=5</code> 时，线性操作约 5 次，平方约 25 次，枚举子集约 <code>2⁵=32</code> 次，全排列约 <code>5!=120</code> 次。<code>n=10</code> 时 <code>10!=3628800</code>，阶乘增长很快，通常只适合很小的 <code>n</code>。</p>\n<p><strong>02 代码结构。</strong> 两个先后执行的循环各跑 <code>n</code> 次，总次数是 <code>n+n=2n</code>，所以为 <code>O(n)</code>；两层完整嵌套才是 <code>n×n=n²</code>。若内层只跑到 <code>i</code>，总次数为 <code>1+2+...+n=n(n+1)/2</code>，仍是 <code>O(n²)</code>。</p>\n<p><strong>03 数据范围。</strong> <code>n=1000</code> 时，<code>n²=10⁶</code>；<code>n=100000</code> 时，<code>n²=10¹⁰</code>。同一段双循环从可能可行变成通常不可行，判断依据是操作数量级，不是代码行数。</p>\n<p><strong>04 常见算法。</strong> 对 <code>n=300</code> 的稠密图，邻接矩阵 Dijkstra 约做 <code>300²=90000</code> 级别的扫描；Floyd 约做 <code>300³=27000000</code> 次状态检查。只求一个起点时前者更合适，需要任意两点答案时才考虑后者。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>代码结构</th>\n<th>估算方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>多段顺序代码</td>\n<td>取增长最快的一段</td>\n</tr>\n<tr>\n<td>两个独立循环</td>\n<td>次数相加</td>\n</tr>\n<tr>\n<td>两层嵌套循环</td>\n<td>次数相乘或求和</td>\n</tr>\n<tr>\n<td>每次规模减半</td>\n<td>约 <code>log n</code> 层</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>下面程序中，第一段循环执行 <code>n</code> 次，第二段双重循环执行 <code>n²</code> 次，总次数约为 <code>n+n²</code>，所以时间复杂度为 <code>O(n²)</code>。若 <code>n=100000</code>，就应寻找只扫描一次或 <code>O(n log n)</code> 的方法。</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    cin &gt;&gt; n;    long long count = 0;\n    // 第一段独立循环执行 n 次。    for (int i = 1; i &lt;= n; i++) {        count++;    }    // 第二段是完整双重循环，共执行 n×n 次。    for (int i = 1; i &lt;= n; i++) {        for (int j = 1; j &lt;= n; j++) {            count++;        }    }\n    cout &lt;&lt; count &lt;&lt; \'\\n\';    return 0;}</code></pre>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>顺序循环相乘反例。</strong> 先扫描 <code>n</code> 个数求和，再扫描 <code>n</code> 个数求最大值，两段各执行 n 次。错误写成 <code>O(n²)</code> 是把先后执行误当嵌套；实际总次数约 <code>2n</code>，省略常数后为 <code>O(n)</code>。</p>\n<ul>\n<li>只数循环层数，不检查循环边界和变量变化。</li>\n<li>把两个先后执行的循环复杂度相乘。</li>\n<li>认为 <code>O(n²)</code> 在任何数据范围下都一定超时。</li>\n<li>忽略递归分支数和递归深度。</li>\n<li>把复杂度写成实际运行秒数，或认为同复杂度的程序一定同样快。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/space-efficiency/">空间效率分析</a>：同时检查内存是否足够。</li>\n<li><a href="/gesp/level8/algorithm-optimization/">算法优化</a>：定位瓶颈后选择优化方向。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求理解算法的空间效率，能够估算变量、数组、递归栈等占用的空间，并结合内存限制判断方案是否可行。空间复杂度关注随输入规模增长而增加的存储量，通常记额外空间，不把程序代码本身机械地算进去。</p>\n<h2 id="kp-01">01 变量与一维数组</h2>\n<p>少量固定变量占用 <code>O(1)</code> 空间；长度与 <code>n</code> 成正比的一维数组占用 <code>O(n)</code> 空间。在常见 GCC/OJ 环境中，<code>int</code> 通常占 4 字节，<code>long long</code> 通常占 8 字节，但实际字节数仍以编译器和平台为准。</p>\n<p>例如 <code>int a[1000000]</code> 通常约占 <code>4,000,000</code> 字节，约为 <code>3.8 MiB</code>。估算时既要写复杂度，也要把字节数与题目的内存限制比较。</p>\n<h2 id="kp-02">02 二维数组与状态表</h2>\n<p><code>n×n</code> 的二维数组占用 <code>O(n²)</code> 空间。若 <code>n=10000</code>，一个 <code>int</code> 矩阵约需 400 MB，可能超过内存限制；这也是大规模稀疏图不宜使用邻接矩阵的原因。</p>\n<p>动态规划状态表也常占二维空间。若当前行只依赖上一行，就不必保留所有历史行，可以只保留两行或一行。</p>\n<h2 id="kp-03">03 图的存储空间</h2>\n<p>邻接矩阵占用 <code>O(n²)</code>；邻接表占用 <code>O(n+m)</code>，其中 <code>m</code> 是边数。对于边很少的稀疏图，邻接表通常节省大量空间；对于顶点少、边很密且需要频繁查询边的图，邻接矩阵可能更直接。</p>\n<p>无向图用邻接表存储时，每条无向边通常会保存两个方向，因此实际记录数约为 <code>2m</code>，但空间复杂度仍是 <code>O(n+m)</code>。</p>\n<h2 id="kp-04">04 递归调用栈</h2>\n<p>递归每深入一层，都要保存参数、局部变量和返回位置。递归深度为 <code>n</code> 时，调用栈空间通常为 <code>O(n)</code>。即使没有显式创建大数组，过深递归也可能造成栈溢出。</p>\n<p>分析递归空间时看“同时存在的调用层数”，不是总调用次数。二叉递归可能调用很多次，但某一时刻调用栈深度仍由最长路径决定。</p>\n<h2 id="kp-05">05 空间优化</h2>\n<p>常见方法包括：</p>\n<ul>\n<li>用滚动数组只保留仍会被使用的状态；</li>\n<li>稀疏图使用邻接表，不保存不存在的边；</li>\n<li>只保存必要信息，及时复用不再需要的数组；</li>\n<li>在递归过深时，评估能否改用显式队列或栈。</li>\n</ul>\n<p>空间优化不能破坏依赖关系。把二维状态压成一维时，要特别检查更新顺序，避免本轮新值覆盖本轮仍要读取的旧值。</p>\n<p><strong>内存估算追踪。</strong> <code>long long table[2000][2000]</code> 在常见环境中每个元素约 8 字节：</p>\n<ol>\n<li>元素数：<code>2000×2000=4,000,000</code>；</li>\n<li>字节数：<code>4,000,000×8=32,000,000</code>；</li>\n<li>字节 → MiB：<code>32,000,000÷1024÷1024≈30.5 MiB</code>。</li>\n</ol>\n<p>这还没有计入其他数组和运行开销。边界检查既要看静态数组大小，也要看递归深度；例如链式图上的递归 DFS 深度可能达到 <code>n</code>。滚动数组把空间降下来后，还必须用小样例验证更新顺序没有覆盖旧状态。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 变量与一维数组。</strong> 在常见 OJ 环境中按 <code>int</code> 4 字节估算，1000000 个元素约 4000000 字节，约 3.8 MiB。几个固定变量无论 <code>n</code> 多大都只占常数空间，而长度随 <code>n</code> 增长的数组是 <code>O(n)</code>。</p>\n<p><strong>02 二维状态。</strong> <code>1000×1000</code> 个 <code>long long</code> 元素按每个 8 字节估算，需要 <code>1000×1000×8=8000000</code> 字节，约 7.6 MiB。若开成 <code>10000×10000</code>，就约 763 MiB，可能超过内存限制。</p>\n<p><strong>03 图存储。</strong> 10000 个点、20000 条无向边，邻接表保存点入口和约 40000 条方向记录，空间随 <code>n+m</code> 增长；邻接矩阵要保存一亿个位置，即使每格 1 字节也约 95 MiB。</p>\n<p><strong>04 递归栈。</strong> 链式 DFS 深度为 4 时，调用栈依次是 <code>[dfs(1)]</code>、<code>[dfs(1),dfs(2)]</code>，直到同时保存 4 层；返回时才逐层释放。若深度扩大到 100000，即使每层变量很少也可能栈溢出。</p>\n<p><strong>05 滚动数组。</strong> 若 <code>dp[i][j]</code> 只依赖上一行，计算第 3 行时只需要第 2 行，不再需要第 1 行。保留 <code>previous</code> 和 <code>current</code> 两行就能把 <code>n×m</code> 个状态降为约 <code>2m</code> 个；更新顺序必须保证旧值还没被覆盖。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>存储对象</th>\n<th>常见空间</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>固定数量变量</td>\n<td><code>O(1)</code></td>\n</tr>\n<tr>\n<td>长度为 <code>n</code> 的数组</td>\n<td><code>O(n)</code></td>\n</tr>\n<tr>\n<td><code>n×n</code> 矩阵</td>\n<td><code>O(n²)</code></td>\n</tr>\n<tr>\n<td>邻接表</td>\n<td><code>O(n+m)</code></td>\n</tr>\n<tr>\n<td>深度为 <code>n</code> 的递归栈</td>\n<td><code>O(n)</code></td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>内存限制为 128 MiB，<code>n=10000</code>，图只有 <code>20000</code> 条边。邻接矩阵若使用 <code>int</code>，约需 <code>10000×10000×4</code> 字节，约 381 MiB，明显不可行；邻接表只保存顶点表和约 <code>40000</code> 条方向记录，更适合这个稀疏图。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>只看变量名反例。</strong> <code>long long a[1000][1000]</code> 看起来只有一个变量名 <code>a</code>，但实际包含一百万个元素，约占 7.6 MiB。空间分析要数元素和字节，不能数源代码中写了几个名字。</p>\n<ul>\n<li>只写 <code>O(n²)</code>，不与实际内存限制比较。</li>\n<li>忘记 <code>long long</code> 通常比 <code>int</code> 占用更多字节。</li>\n<li>认为没有大数组就一定不会超内存，忽略递归栈。</li>\n<li>无向图邻接表只记录一个方向，造成遍历错误。</li>\n<li>滚动数组更新顺序错误，覆盖了仍要使用的旧状态。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/graph-modeling/">图论综合应用</a>：按图的稀疏程度选择存储结构。</li>\n<li><a href="/gesp/level8/algorithm-optimization/">算法优化</a>：在保证正确性的前提下降低空间。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
        '<h2 id="知识范围与官方定位">知识范围与官方定位</h2>\n<p>官方八级要求理解常见算法优化思路，能够从时间和空间角度改进算法。优化的前提是结果正确：先写清基础解法、分析瓶颈，再选择有依据的改进方法，并重新验证边界和复杂度。</p>\n<h2 id="kp-01">01 基础解法与瓶颈</h2>\n<p>先写出能保证正确的直接方法，再分析最耗时或最占空间的部分。瓶颈可能来自重复计算、无效枚举、不合适的数据结构、过大的状态表或不必要的高复杂度算法。</p>\n<p>优化目标必须具体。例如把两层枚举的 <code>O(n²)</code> 降为排序后的 <code>O(n log n)</code>，或者把只依赖上一行的 <code>O(n²)</code> 状态表压缩成 <code>O(n)</code> 空间。</p>\n<h2 id="kp-02">02 剪枝</h2>\n<p>剪枝是在搜索过程中提前停止不可能得到合法答案或更优答案的分支。常见条件有：</p>\n<ul>\n<li>当前选择已经违反限制；</li>\n<li>即使使用剩余全部资源也无法达到目标；</li>\n<li>当前代价已经不小于已知最优答案；</li>\n<li>相同状态已经处理过，不必重复搜索。</li>\n</ul>\n<p>剪枝必须有严格理由。错误地删除仍可能产生答案的分支，会让程序更快地得到错误结果。</p>\n<h2 id="kp-03">03 预处理与数学规律</h2>\n<p>当许多询问反复使用相同信息时，可以先统一计算一次。例如前缀和把每次区间求和从 <code>O(n)</code> 降为 <code>O(1)</code>；杨辉三角预处理后可以多次查询组合数。</p>\n<p>发现数学规律也能减少枚举。使用公式前必须核对适用条件、整数范围和取模要求，不能为了“看起来更快”直接套用未经证明的结论。</p>\n<h2 id="kp-04">04 数据结构与算法替换</h2>\n<p>如果基础方法的瓶颈是反复查找，可以选择更合适的查找方式；如果图很稀疏，可以用邻接表替代邻接矩阵；如果问题目标已经有成熟算法，应使用与条件匹配的算法，而不是继续暴力枚举。</p>\n<p>例如在有序数组中寻找和为目标值的一对数，可以用左右指针从两端向中间移动，把两层枚举的 <code>O(n²)</code> 降为排序后的 <code>O(n log n)</code>。</p>\n<h2 id="kp-05">05 时间与空间的权衡</h2>\n<p>有些优化用更多空间换时间，例如预处理表；有些优化用更多计算换空间，例如不保存全部中间结果。选择时要同时满足时间限制和内存限制。</p>\n<p>滚动数组可以降低空间，但可能让恢复完整方案变得困难；预处理可以加快询问，但若只查询一次，预处理成本未必划算。优化不是越复杂越好，而是让整体方案在题目约束下稳定通过。</p>\n<p><strong>优化前后追踪。</strong> “判断数组中是否存在两个不同位置的数之和为目标值”的基线方案枚举所有数对，最多检查 <code>n(n-1)/2</code> 对，时间为 <code>O(n²)</code>。优化方案先排序，再用左右指针：</p>\n<table>\n<thead>\n<tr>\n<th>当前和与目标的关系</th>\n<th>为什么这样移动</th>\n<th>排除的范围</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>相等</td>\n<td>已找到答案</td>\n<td>结束</td>\n</tr>\n<tr>\n<td>小于目标</td>\n<td>左端太小，左指针右移</td>\n<td>当前左端与更小右端的组合</td>\n</tr>\n<tr>\n<td>大于目标</td>\n<td>右端太大，右指针左移</td>\n<td>当前右端与更大左端的组合</td>\n</tr>\n</tbody>\n</table>\n<p>排序后扫描只移动至多 <code>n-1</code> 次，总时间变为 <code>O(n log n)</code>。优化后必须做反例验证：包含重复元素、全为负数、只有一个元素、答案不存在；还要检查排序是否会破坏题目要求保留原下标的附加条件。</p>\n<p><strong>逐段具体推演（对应 01—05）。</strong></p>\n<p><strong>01 先量化基线。</strong> 对 <code>n=100000、q=100000</code> 的区间和查询，每次扫描整个区间最坏约做 <code>nq=10¹⁰</code> 次加法。结果虽然正确，但数量级说明瓶颈就在重复扫描，而不是输入输出格式。</p>\n<p><strong>02 剪枝。</strong> 从正整数中搜索和为 10 的方案，当前和已经是 12 时，后面继续加入正数只会更大，这条分支可以立刻返回。若允许负数，就不能用同一条件剪枝，因为后面可能用负数把和降回 10。</p>\n<p><strong>03 预处理、公式与差分数组。</strong> 静态区间和可先建前缀和，把一次查询从 <code>O(n)</code> 降为 <code>O(1)</code>。若有多次区间加法，可在差分数组中对区间 <code>[l,r]</code> 执行 <code>diff[l]+=x、diff[r+1]-=x</code>，最后一次前缀累加还原全部值；素数查询很多时则可先筛出素数表。</p>\n<p><strong>04 更换数据结构或算法。</strong> 有序数组中查一个数，顺序扫描最多比较 n 次，二分查找每次排除一半，只需约 <code>log₂n</code> 次。两数之和从双重枚举 <code>O(n²)</code> 改为排序加双指针后是 <code>O(n log n)</code>，但必须保留“两个不同位置”等边界。</p>\n<p><strong>05 时间与空间权衡。</strong> 长度 n 的前缀和多用 <code>O(n)</code> 空间，换来每次 <code>O(1)</code> 查询；滚动数组则在依赖允许时减少空间。优化后要用普通、边界和极限数据比较新旧答案，再确认复杂度确实下降。</p>\n<h2 id="知识点速查表">知识点速查表</h2>\n<table>\n<thead>\n<tr>\n<th>瓶颈</th>\n<th>常见方向</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>重复计算</td>\n<td>预处理、记忆结果</td>\n</tr>\n<tr>\n<td>大量无效搜索</td>\n<td>剪枝</td>\n</tr>\n<tr>\n<td>反复线性查找</td>\n<td>排序、二分或合适的数据结构</td>\n</tr>\n<tr>\n<td>稀疏图矩阵过大</td>\n<td>邻接表</td>\n</tr>\n<tr>\n<td>状态表只依赖少量前驱</td>\n<td>滚动数组</td>\n</tr>\n</tbody>\n</table>\n<h2 id="typical-example">典型例题</h2>\n<p>输入 <code>n</code> 个整数和目标值，判断是否存在两个不同位置的数之和等于目标值。先排序，再用左右指针判断：</p>\n<pre><code>#include &lt;bits/stdc++.h&gt;using namespace std;\nint main() {    int n;    long long target;    cin &gt;&gt; n &gt;&gt; target;\n    vector&lt;long long&gt; a(n);    for (int i = 0; i &lt; n; i++) {        cin &gt;&gt; a[i];    }    // 双指针依赖单调性，必须先排序。    sort(a.begin(), a.end());\n    int left = 0;    int right = n - 1;    bool found = false;    while (left &lt; right) {        long long sum = a[left] + a[right];        if (sum == target) {            found = true;            break;        } else if (sum &lt; target) {            // 当前和偏小，左端右移才可能增大总和。            left++;        } else {            // 当前和偏大，右端左移才可能减小总和。            right--;        }    }\n    if (found) {        cout &lt;&lt; "YES\\n";    } else {        cout &lt;&lt; "NO\\n";    }    return 0;}</code></pre>\n<p>排序为 <code>O(n log n)</code>，双指针扫描为 <code>O(n)</code>，总时间复杂度为 <code>O(n log n)</code>。</p>\n<h2 id="common-errors">常见错误</h2>\n<p><strong>优化后漏解反例。</strong> 两数之和目标为 6，数组是 <code>[3,3]</code>。若优化后错误规定“两个数值不能相等”，就会漏掉两个不同位置上的 3；正确限制是下标不同，而不是数值不同。新算法必须与基线在这个反例上都返回存在解。</p>\n<ul>\n<li>未证明剪枝条件正确就删除搜索分支。</li>\n<li>只比较某一行代码，不分析整体复杂度。</li>\n<li>优化后忘记重新检查边界、重复元素和整数溢出。</li>\n<li>为一次查询做代价很大的预处理，反而更慢。</li>\n<li>用超出内存限制的大表换取速度。</li>\n</ul>\n<h2 id="关联学习">关联学习</h2>\n<ul>\n<li><a href="/gesp/level8/time-efficiency/">时间效率分析</a>：定位操作次数增长最快的部分。</li>\n<li><a href="/gesp/level8/space-efficiency/">空间效率分析</a>：检查优化后的额外存储。</li>\n</ul>\n<h2 id="core-check">核心内容检查</h2><div id="page-test"></div></div><div></div><div hidden data-group-result aria-live="polite"><strong data-group-result-title></strong><p>只保留本页最近一次完整提交结果；重新测试不会改变“已复习”状态。</p></div></gesp-course-question-group></div><footer',
    ],
}
