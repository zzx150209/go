#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Showball 信息学 —— 站点数据
依据 GESP / CSP 官方大纲组织，内容自行撰写。
"""

SITE_NAME = "Showball 信息学"
SITE_SUB  = "教练型信息学学习工作台"

# ============ GESP 八级 ============
# 每级: (短标签, 长介绍, [(编号, 标题, 描述, [标签])], 卡片描述)
LEVELS = {
1: ("编程入门知识字典",
 "GESP 一级要求学生了解计算机与开发环境，掌握 C++ 程序结构、输入输出、基本数据类型、基本运算，以及顺序、分支、循环三种结构。学完后应能读懂并编写单层程序，结合编译信息、样例和变量变化检查错误。",
 "输入输出、变量、分支与循环，写出第一个完整程序。",
 [("计算机基础知识","认识计算机的发展、组成、软硬件、操作系统与文件目录。",["计算机组成","软硬件"]),
  ("集成开发环境","完成编辑、编译、运行和调试，判断常见程序错误。",["IDE","编译调试"]),
  ("程序基本结构","理解头文件、命名空间、main 函数、语句、代码块和注释。",["main函数","程序框架"]),
  ("输入输出语句","使用 cin、cout、scanf、printf，并按题目要求控制输出格式。",["cin-cout","scanf-printf"]),
  ("程序基本概念","掌握标识符、关键字、常量、变量、赋值与表达式。",["变量常量","标识符表达式"]),
  ("基本数据类型","认识整型、浮点、字符和布尔类型的范围与选择方法。",["int-double","char-bool"]),
  ("基本运算","掌握算术、关系、逻辑、赋值、自增自减及优先级规则。",["运算符","优先级"]),
  ("顺序结构","按顺序完成输入、计算和输出，并通过变量变化检查。",["顺序执行","变量追踪"]),
  ("分支结构","使用 if、if-else、else-if、switch 和三元运算处理不同情况。",["if-else","switch-case"]),
  ("循环结构","掌握三种循环与循环控制，解决累加、最值、位数、数列和枚举问题。",["for-while","循环控制"]),
 ]),
2: ("编程基础与流程图",
 "GESP 二级在基本语法之上引入存储与网络常识、程序设计语言的分类与编译运行过程、流程图表示法，以及数据类型转换、多分支、循环嵌套和常用数学函数。重点是读懂流程图并把它与代码互相翻译。",
 "存储与网络、编译过程、流程图与代码互译。",
 [("计算机存储与网络","了解存储单位换算，认识网络协议与常见网络概念。",["存储与单位","网络与协议"]),
  ("程序设计语言","区分机器语言、汇编语言与高级语言，了解编译与运行过程。",["语言分类","编译过程"]),
  ("流程图","识别流程图符号，追踪流程走向，并在流程图与代码之间互相翻译。",["符号辨认","流程追踪"]),
  ("数据类型转换","掌握隐式转换与强制转换，注意整数除法与精度丢失。",["隐式转换","强制转换"]),
  ("多分支判断","处理分支嵌套与条件边界，避免遗漏情况。",["分支嵌套","条件边界"]),
  ("循环嵌套","用双重循环处理行列、图形与枚举问题，控制内层跳转。",["行列问题","内层跳转"]),
  ("常用数学函数","使用绝对值、平方根、最大最小值与随机数，注意取值范围。",["绝对值平方根","最大最小值"]),
  ("程序调试","根据编译信息与运行结果定位错误，学会输出中间变量。",["编译信息","中间输出"]),
 ]),
3: ("数据编码与算法初步",
 "GESP 三级进入数据表示与算法描述：理解数据编码与进制转换、位运算，掌握算法的概念与描述方式，并开始系统使用数组、字符串和枚举，能估算简单程序的操作次数。",
 "进制转换、位运算、数组字符串与枚举模拟。",
 [("数据编码","确认每个数使用的二进制位数，判断能表示的整数范围。",["二进制位数","整数范围"]),
  ("进制转换","掌握任意进制与十进制之间的转换方法，并检查数字合法性。",["按权展开","除基取余"]),
  ("位运算","掌握与、或、异或、取反和移位，安全地进行位操作。",["按位运算","移位"]),
  ("算法的概念与描述","判断步骤是否明确且能结束，按接近代码的步骤推断输出。",["步骤明确","结果推断"]),
  ("一维数组","按位置编号访问元素，完成统计、查找和最值更新，并检查下标范围。",["下标访问","范围检查"]),
  ("字符串处理","按位置访问字符，查找与截取连续子串，完成连接与替换。",["字符访问","子串截取"]),
  ("枚举算法","确定枚举范围与判断条件，逐个检查并统计或更新最优解。",["枚举范围","条件判断"]),
  ("模拟算法","按题目规则保存状态并逐步推进，得到最终结果。",["状态保存","规则推进"]),
 ]),
4: ("函数、指针与结构体",
 "GESP 四级引入函数与作用域、指针与引用、结构体与二维数组，并开始关注递推、排序算法和复杂度估算，同时了解文件操作与异常处理的基本写法。",
 "函数、作用域、指针引用、结构体与排序。",
 [("函数基础","掌握函数声明与定义、调用与返回值，把重复任务拆分成函数。",["声明定义","返回值"]),
  ("参数与作用域","区分形参与实参，理解局部与全局变量、作用域与遮蔽。",["形参实参","作用域"]),
  ("指针基础","获取变量地址、解引用指针，并做空指针检查。",["取地址","解引用"]),
  ("引用与参数传递","区分值传递与引用传递，掌握数组作为参数的行为。",["值传递","引用参数"]),
  ("结构体","定义结构体类型与对象，访问成员，使用结构体数组。",["成员访问","结构体数组"]),
  ("二维数组","定义二维数组，完成行列遍历与矩阵基本操作。",["行列遍历","矩阵操作"]),
  ("递推","确定初始状态与递推关系，按顺序计算得到结果。",["初始状态","递推关系"]),
  ("排序算法","掌握冒泡、插入、选择排序的过程与稳定性。",["冒泡排序","选择排序"]),
  ("复杂度估算","估算操作次数，理解时间复杂度与空间复杂度的含义。",["时间复杂度","空间复杂度"]),
  ("文件与异常","完成文件重定向与文件流读写，处理输入校验与异常捕获。",["文件流","异常处理"]),
 ]),
5: ("数论、链表与分治",
 "GESP 五级进入初等数论、高精度运算与线性数据结构，掌握链表与数组模拟链表、顺序查找与二分查找、二分答案、贪心与分治排序，并能根据数据范围判断复杂度是否可行。",
 "数论、高精度、链表、二分、贪心与分治。",
 [("初等数论基础","掌握整除与约数、素数与互质、同余与余数。",["整除约数","同余"]),
  ("最大公约数","用欧几里得算法求最大公约数，并推导最小公倍数与分数约分。",["辗转相除","最小公倍数"]),
  ("素数筛法","掌握埃氏筛与线性筛，实现素数表查询。",["埃氏筛","线性筛"]),
  ("质因数分解","写出标准分解式，求约数个数与约数和。",["分解式","约数个数"]),
  ("高精度运算","用数组存储大整数，处理进位借位以及与普通整数的乘除。",["进位借位","大整数"]),
  ("链表","掌握单向与双向链表的插入、删除与反转，会用数组模拟。",["链表操作","数组模拟"]),
  ("顺序查找","实现精确查找与左右边界查找，统计出现次数。",["精确查找","左右边界"]),
  ("二分答案","判断单调性，求最小可行值与最大可行值。",["单调判定","边界取值"]),
  ("贪心算法","完成贪心选择、排序后构造，并用交换论证说明正确性。",["贪心选择","交换论证"]),
  ("分治与排序","掌握分治三步，实现归并排序与快速排序。",["分治三步","归并快排"]),
  ("递归","明确递归出口，理解调用与返回过程，完成递归枚举。",["递归出口","递归枚举"]),
  ("复杂度与数据范围","根据数据范围判断算法是否可行，选择合适复杂度。",["数据范围","复杂度判断"]),
 ]),
6: ("树结构与编码",
 "GESP 六级围绕树展开：树的构造与各种遍历、特殊二叉树、哈夫曼编码与格雷码，以及迷宫路径枚举与回溯，强调从层次结构走向系统化的问题求解。",
 "树的遍历、哈夫曼编码、格雷码与回溯搜索。",
 [("树的基础知识","理解结点、边、根、子树、深度与度的概念。",["结点关系","树的术语"]),
  ("树的构造与遍历","根据输入构造树，掌握先序、中序、后序遍历。",["构造树","三种遍历"]),
  ("层序遍历","用队列按层访问结点，处理按层统计问题。",["队列实现","按层访问"]),
  ("遍历序列还原","根据两种遍历序列还原二叉树，并推断第三种序列。",["序列还原","结构推断"]),
  ("特殊二叉树","掌握二叉搜索树、完全二叉树与满二叉树的性质。",["二叉搜索树","完全二叉树"]),
  ("哈夫曼编码","构造哈夫曼树，计算带权路径长度并生成编码。",["哈夫曼树","带权路径"]),
  ("格雷码","理解格雷码的生成规则与相邻码字只差一位的性质。",["生成规则","相邻码字"]),
  ("路径枚举与回溯","在迷宫中枚举路径，处理状态标记与撤销。",["状态标记","回溯撤销"]),
  ("深度优先搜索","用递归实现 DFS，处理连通性与路径问题。",["递归搜索","连通性"]),
  ("广度优先搜索","用队列实现 BFS，求最短步数与层次信息。",["队列搜索","最短步数"]),
 ]),
7: ("数学工具与图论入门",
 "GESP 七级进入数学库函数、复杂与二维动态规划、图的基本概念与存储、图的遍历，以及哈希表等关联容器的使用，为综合问题求解打基础。",
 "数学函数、动态规划、图论基础与哈希表。",
 [("数学库常用函数","处理角度与弧度换算，检查函数定义域，按题意处理浮点结果。",["角度弧度","浮点处理"]),
  ("复杂动态规划","增加状态维度，按阶段推进状态，按区间长度完成转移。",["状态维度","阶段推进"]),
  ("二维动态规划","处理网格最值、最长公共子序列与编辑距离。",["网格最值","编辑距离"]),
  ("线性动态规划","掌握最大连续和、最长上升子序列与背包空间优化。",["最大连续和","空间优化"]),
  ("图的基本概念","识别图的组成，计算顶点的度，判断路径、环与连通性。",["顶点与边","连通性"]),
  ("图的存储","掌握邻接矩阵、邻接表与链式前向星。",["邻接矩阵","邻接表"]),
  ("图的遍历","实现 DFS 与 BFS 遍历，处理非连通图。",["DFS遍历","非连通图"]),
  ("网格搜索","检查网格边界，使用方向数组扩展，标记并统计连通区域。",["方向数组","连通区域"]),
  ("哈希表与集合","统计元素出现次数，判断存在性，比较有序与无序关联容器。",["键值统计","关联容器"]),
  ("综合应用","把图论、动态规划与数据结构组合起来解决问题。",["算法组合","建模"]),
 ]),
8: ("计数、图论与复杂度",
 "GESP 八级从准确建模走向复杂度可控的综合求解：计数原理与组合数、倍增与二进制拆分、数列与几何计算，以及最小生成树、最短路和常见优化手段。",
 "计数原理、组合数、倍增、生成树与最短路。",
 [("计数原理","掌握加法原理与乘法原理，区分分类与分步。",["加法原理","乘法原理"]),
  ("排列与组合","计算排列数与阶乘，理解组合数的意义与性质。",["排列数","组合数"]),
  ("杨辉三角","处理边界条件，用递推与预处理求组合数。",["边界条件","组合数递推"]),
  ("倍增","用二进制拆分与倍增预处理实现快速查询。",["二进制拆分","倍增查询"]),
  ("数列与代数","对表达式化简，处理等差数列与等比数列。",["数列求和","表达式化简"]),
  ("几何计算","计算面积与周长、坐标距离，注意退化图形。",["坐标距离","退化图形"]),
  ("图算法选择","根据顶点、边、方向与权值选择合适的图算法。",["图建模","算法选择"]),
  ("最小生成树","理解生成树与最小生成树，实现 Prim 与 Kruskal。",["Prim","Kruskal"]),
  ("单源最短路","理解松弛操作，实现 Dijkstra 与负权处理。",["松弛操作","Dijkstra"]),
  ("复杂度分析","分析常见算法复杂度，按数据范围选择算法。",["复杂度","范围判断"]),
  ("空间优化","优化数组空间与图存储，使用滚动数组。",["滚动数组","图存储"]),
  ("算法优化","在暴力与剪枝、预处理与数学优化之间做出选择。",["暴力剪枝","预处理"]),
 ]),
}

# ============ CSP ============
CSP = [
 ("01","CSP-J 初赛","计算机基础、C++ 语法、算法阅读与选择填空。",
  "先把握题目和选项的读法","#5b8def",
  ["计算机基础","进制与编码","C++ 语法","逻辑运算","算法阅读","数据结构常识"]),
 ("02","CSP-J 复赛","模拟、枚举、排序、搜索、简单动态规划与代码规范。",
  "把思路写成完整程序","#22b8a0",
  ["模拟","枚举","排序","搜索","简单 DP","代码规范"]),
 ("03","CSP-S 进阶","图论、树结构、数论、复杂动态规划与复杂度控制。",
  "进入更综合的算法训练","#a56bf0",
  ["图论","树结构","数论","复杂 DP","复杂度控制","综合建模"]),
]

# ============ 程序模板分类 ============
# (图标, 标题, 数量, 说明, [关键词], 是否标志)
TEMPLATE_CATS = [
 ("→","基础算法",12,"输入输出、循环枚举、数组处理与基础写法。",["数组统计计数","递推转移","一维前缀和"],False),
 ("↕","排序算法",7,"按需选择排序方式统一序列。",["基础排序三选一","计数排序","堆排序"],False),
 ("⌕","查找与二分",6,"查找、二分查找与二分答案。",["二分答案","二分查找","浮点二分"],True),
 ("⬚","搜索算法",18,"枚举、深度优先、广度优先与剪枝。",["A* 启发","BFS 最短路","DFS 回溯"],True),
 ("▦","动态规划",10,"常见状态设计、转移和背包模型。",["线性 DP","二维网格与不同计数","状态压缩 DP / 子集子段","背包问题进阶"],False),
 ("网","图论算法",10,"图的遍历、最短路和生成树等写法。",["Floyd 最短路","Dijkstra 最短路","二分图判定（染色）"],False),
 ("≣","数据结构",10,"栈、队列、树、并查集等常用结构。",["二维前缀和","树状数组（BIT）","线段树与区间操作"],False),
 ("∑","数与数学",12,"质数、最大公约数、组合计数等方法。",["异或基础数","快速幂与逆元","质数分解"],False),
 ("1↔","高精度运算",8,"大整数的加、减、乘、除及比较。",["高精度乘法","高精度单精度乘输","高精度除法"],False),
 ("T","字符串算法",4,"字符串处理、匹配与常用技巧。",["AC 自动机","KMP 算法","Manacher 算法"],False),
]

COMMON_TEMPLATES = [
 ("快读快写","大数据量输入输出加速，竞赛必备基础优化。",["快读快写","输入加速","格式化输出"],
  """ios::sync_with_stdio(false);
cin.tie(nullptr);

int n;
cin >> n;"""),
 ("二分答案","在有序数据中查找目标值的二分模板。",["有序数组","边界处理","O(log n)"],
  """int l = 0, r = n - 1;
while (l <= r) {
    int mid = l + r >> 1;
    if (check(mid)) ans = mid, r = mid - 1;
    else l = mid + 1;
}"""),
 ("并查集","路径压缩 + 按秩合并，处理连通性问题。",["路径压缩","按秩合并","近似 O(1)"],
  """int find(int x) {
    while (fa[x] != x) { fa[x] = fa[fa[x]]; x = fa[x]; }
    return x;
}

void unite(int a, int b) {
    a = find(a); b = find(b);
    if (a == b) return;
    if (rk[a] < rk[b]) swap(a, b);
    fa[b] = a;
    if (rk[a] == rk[b]) rk[a]++;
}"""),
]

# ============ 题解页 ============
SOLUTION_STEPS = [
 ("01","看不懂题目的知识","对应等级的概念、语法和算法，把题意先读懂。","去查 GESP 知识","gesp.html"),
 ("02","有思路，但不会写成代码","找一份可以直接修改的程序框架，对照补上关键步骤。","去找程序模板","templates.html"),
 ("03","想继续找题练习","进入 OJ 与学习资源，挑一道合适的题继续独立完成。","去 OJ 与资源","resources.html"),
]

# ============ 资源页分组 ============
RESOURCE_GROUPS = [
 ("今天刷题","固定训练优先用穷狼 OJ；需要更多公开题目和题单时再用洛谷。", None, [
   ("穷","穷狼 OJ","刷题","日常训练专用题库，适合按教练安排完成训练。","#e8b04b"),
   ("洛","洛谷","公开题库","公开题库、题单和比赛平台，适合补充练习与自主刷题。","#3f8fd6"),
 ]),
 ("查官方考试信息","报名、考试规则和时间以主办方官网为准，不依赖转载消息。", None, [
   ("G","GESP 官方网站","官方","查询 GESP 报名、认证安排、考试大纲和官方通知。","#5b8def"),
   ("N","NOI 官方网站","官方","查询 CSP-J/S、NOI 系列活动和竞赛相关通知。","#5b8def"),
 ]),
 ("学知识或看资料","先用中文资料弄清概念，再配合动画或示例理解算法过程。", None, [
   ("O","OI Wiki","资料","系统查询信息学竞赛知识、算法、数据结构和常见实现。","#22b8a0"),
   ("H","Hello 算法","资料","使用插图和解说理解数据结构和算法。","#22b8a0"),
 ]),
 ("配置编程环境","先把编译和运行环境配好；遇到语法细节时再查阅参考文档。", None, [
   ("V","VS Code 配置 C++","手册","按官方介绍完成本地 IDE 编辑、编译和调试环境。","#a56bf0"),
   ("c","cppreference","手册","查询 C++ 标准库、容器、函数和语言细节。","#a56bf0"),
 ]),
]

# ============ 知识点详细内容 ============
# 每个知识点: {"points": [要点...], "code": 代码示例或None, "tips": [注意...]}
KP_DETAILS = {
# ---- 1级 ----
"计算机基础知识": {
 "points": [
   "硬件是计算机的实体部分，包括 CPU、内存、硬盘、主板、输入输出设备等，看得见摸得着。",
   "软件是指挥硬件工作的程序和数据，分为系统软件（操作系统）和应用软件（浏览器、编辑器等）。",
   "CPU 负责运算和控制，内存临时保存正在运行的程序和数据，硬盘长期保存文件。",
   "输入设备把外部信息送入计算机（键盘、鼠标、麦克风），输出设备把结果呈现出来（显示器、打印机、扬声器）。",
   "计算机内部用二进制（0 和 1）表示所有信息，因为电子元件只有通电和断电两种稳定状态。",
 ],
 "code": None,
 "tips": [
   "触摸屏既是输入设备也是输出设备，判断时看信息流向。",
   "改文件扩展名不会改变文件内容，.cpp 源文件必须编译后才能运行。",
 ],
},
"集成开发环境": {
 "points": [
   "IDE（集成开发环境）把编辑、编译、运行、调试四个功能整合在一个软件中。",
   "写代码用编辑器，保存后编译器把 .cpp 源文件翻译成可执行程序（.exe），再由运行环境执行。",
   "常见错误有：语法错误（编译时报错）、链接错误（找不到函数）、运行时错误（程序崩溃）、逻辑错误（结果不对）。",
   "调试时可以在关键位置打断点，逐行执行并查看变量值，定位逻辑问题。",
 ],
 "code": None,
 "tips": [
   "编译错误看行号和提示，先改第一个错误，后面的可能是连锁反应。",
   "程序运行结果不对时，用 cout 输出中间变量值来追踪。",
 ],
},
"程序基本结构": {
 "points": [
   "一个最简单的 C++ 程序包含：头文件包含（#include）、命名空间声明（using namespace std）、main 函数。",
   "main 函数是程序的入口，程序从 main 的第一条语句开始执行，到 return 0 结束。",
   "每条语句以分号结尾，大括号 { } 表示一个代码块。",
   "注释有两种：// 单行注释 和 /* 多行注释 */，编译器忽略注释内容。",
 ],
 "code": """#include <iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}""",
 "tips": [
   "return 0 表示程序正常结束，main 函数必须有返回值类型 int。",
   "cout 输出时需要换行用 endl 或 '\\n'。",
 ],
},
"输入输出语句": {
 "points": [
   "cin 用于从键盘读取数据，cout 用于向屏幕输出数据，都需要 #include <iostream>。",
   "cin >> 变量 会跳过空格和换行，依次读取多个值。",
   "cout << 内容 可以连续输出，用 endl 换行。",
   "scanf/printf 是 C 风格输入输出，格式化输出用 %d（整数）、%f（小数）、%c（字符）、%s（字符串）。",
 ],
 "code": """int a, b;
cin >> a >> b;              // 输入两个整数
cout << a + b << endl;      // 输出和

printf("%d %d\\n", a, b);   // C 风格输出""",
 "tips": [
   "cin 和 cin 不要混用 scanf/printf，避免不同步问题。",
   "输出格式要严格按题目要求，包括空格、换行和小数位数。",
 ],
},
"程序基本概念": {
 "points": [
   "标识符是程序员自己起的名字（变量名、函数名），只能由字母、数字、下划线组成，不能以数字开头。",
   "关键字是 C++ 保留的单词（int、if、for 等），不能用作标识符。",
   "常量是程序运行中不改变的值（如 3.14、'hello'），变量是内存中可以改变的数据。",
   "赋值语句 x = 5; 把右边的值存入左边的变量，等号是赋值不是数学等式。",
 ],
 "code": """int a = 10;      // 定义变量a并赋初值
int b = a + 5;   // b = 15
a = a + 1;       // a = 11（自增）
const double PI = 3.14159;  // 常量""",
 "tips": [
   "变量必须先定义再使用，未初始化的变量值是随机的。",
   "C++ 区分大小写，Age 和 age 是两个不同的变量。",
 ],
},
"基本数据类型": {
 "points": [
   "int 存储整数（约 -21 亿到 21 亿），double 存储小数（约 15 位有效数字）。",
   "char 存储单个字符（如 'A'），bool 存储真（true）或假（false）。",
   "整数除法会截断小数部分：7 / 2 = 3 而不是 3.5。",
   "选择数据类型时看数值范围：小数用 double，超大整数用 long long。",
 ],
 "code": """int a = 7, b = 2;
double c = 7.0 / 2.0;   // c = 3.5
double d = a / b;        // d = 3.0（整数除法先执行）
char ch = 'A';           // 字符用单引号
bool ok = true;          // 布尔值""",
 "tips": [
   "整数除以整数结果还是整数，要得到小数至少有一个操作数是小数。",
   "char 本质是整数，'A' 的 ASCII 值是 65。",
 ],
},
"基本运算": {
 "points": [
   "算术运算符：+、-、*、/、%（取余），注意整数除法和取余只对整数有效。",
   "关系运算符：>、<、>=、<=、==（等于）、!=（不等于），结果为 true 或 false。",
   "逻辑运算符：&&（并且）、||（或者）、!（非），优先级 ! > && > ||。",
   "赋值运算符：=、+=、-=、*=、/=，自增 ++ 和自减 --。",
 ],
 "code": """int a = 10, b = 3;
a + b   // 13
a / b   // 3（整数除法）
a % b   // 1（10除以3余1）
(a > 5) && (b < 5)  // true
a++     // a变成11，先使用再加1
++a     // a先加1再使用""",
 "tips": [
   "== 是判断相等，= 是赋值，不要混淆。",
   "负数取余的结果符号跟被除数有关，C++ 中 -7 % 3 = -1。",
 ],
},
"顺序结构": {
 "points": [
   "程序默认按语句书写顺序从上到下依次执行。",
   "解题时通常三步：输入数据 → 计算处理 → 输出结果。",
   "通过输出中间变量值可以追踪每一步发生了什么，帮助找错。",
   "变量的最终值取决于最后一次赋值，之前的值会被覆盖。",
 ],
 "code": """// 计算长方形面积和周长
int length, width;
cin >> length >> width;
int area = length * width;       // 计算面积
int peri = 2 * (length + width); // 计算周长
cout << area << " " << peri << endl;""",
 "tips": [
   "调试时在关键步骤加 cout 输出，确认变量值是否符合预期。",
   "语句顺序很重要，先计算再输出，不要搞反。",
 ],
},
"分支结构": {
 "points": [
   "if (条件) { ... } 条件为真时执行大括号内的语句。",
   "if-else 二选一：条件为真执行 if 块，否则执行 else 块。",
   "else if 可以串联多个条件，从上到下依次判断，满足一个就执行。",
   "switch 适合单值多分支判断，每个 case 后记得 break。",
 ],
 "code": """int score;
cin >> score;
if (score >= 90) cout << "A" << endl;
else if (score >= 80) cout << "B" << endl;
else if (score >= 60) cout << "C" << endl;
else cout << "D" << endl;""",
 "tips": [
   "if 后不要加分号，否则大括号内容不再受条件控制。",
   "switch 中 case 不写 break 会继续执行后面的 case（穿透）。",
 ],
},
"循环结构": {
 "points": [
   "for 循环适合已知次数：for(初始化; 条件; 更新) { ... }。",
   "while 循环适合未知次数：while(条件) { ... }，条件为真时反复执行。",
   "do-while 先执行一次再判断条件，至少执行一次。",
   "break 跳出当前循环，continue 跳过本次循环直接进入下一次。",
 ],
 "code": """// 计算 1 到 100 的和
int sum = 0;
for (int i = 1; i <= 100; i++) {
    sum += i;
}
cout << sum << endl;  // 5050

// 累加输入直到输入0
int x, total = 0;
while (cin >> x && x != 0) {
    total += x;
}""",
 "tips": [
   "for 循环中循环变量建议在初始化处定义（for(int i=...）。",
   "死循环：while(true) 需要用 break 退出，注意不要忘记出口。",
 ],
},
# ---- 2级 ----
"计算机存储与网络": {
 "points": [
   "存储单位：1 Byte = 8 bit，1 KB = 1024 B，1 MB = 1024 KB，1 GB = 1024 MB。",
   "内存是临时存储（断电丢失），硬盘/SSD 是永久存储（断电保留）。",
   "网络中数据通过协议传输，常见协议 HTTP/HTTPS 用于网页，TCP 用于可靠传输。",
   "IP 地址标识网络中的设备，域名便于记忆，DNS 将域名解析为 IP。",
 ],
 "code": None,
 "tips": [
   "换算时注意 1024 而不是 1000，存储容量厂商常用 1000。",
   "bit 是最小单位（0或1），Byte 是基本寻址单位。",
 ],
},
"程序设计语言": {
 "points": [
   "机器语言由 0 和 1 组成，CPU 直接执行，但人很难读写。",
   "汇编语言用助记符表示指令，需要汇编器翻译为机器码。",
   "高级语言（C++、Python）接近自然语言，需要编译器或解释器翻译。",
   "编译型语言（C++）先整体翻译为可执行文件再运行；解释型语言（Python）逐行翻译执行。",
 ],
 "code": None,
 "tips": [
   "C++ 是编译型语言，运行前需要编译，编译后运行速度快。",
   "编译器报错信息中的行号对应源文件，不是翻译后的机器码。",
 ],
},
"流程图": {
 "points": [
   "流程图用标准符号表示算法步骤：椭圆表示开始/结束，矩形表示处理，菱形表示判断，平行四边形表示输入输出。",
   "箭头表示执行方向，从开始符号出发，沿箭头执行直到结束符号。",
   "流程图与代码可以互相翻译：顺序对应顺序结构，菱形对应 if 判断，回箭头对应循环。",
 ],
 "code": None,
 "tips": [
   "判断框有两个出口（是/否），不要漏掉任何一条路径。",
   "循环体在流程图中表现为从判断框回到上方的箭头。",
 ],
},
"数据类型转换": {
 "points": [
   "隐式转换：小类型自动转大类型，如 int 转 double，不丢失精度。",
   "强制转换：(double)a 把 a 当 double 处理，(int)3.14 得到 3（截断小数）。",
   "整数除法问题：7/2=3，要得到 3.5 需要写成 7.0/2 或 (double)7/2。",
   "字符与整数互转：char 本质是 ASCII 码，'A'+1 得到 'B'。",
 ],
 "code": """int a = 7, b = 2;
double c = (double)a / b;  // 3.5（先转a再除）
int x = (int)3.99;         // 3（截断，不是四舍五入）
char ch = 'A' + 1;          // 'B'""",
 "tips": [
   "强制转换是截断不是四舍五入，3.99 转 int 是 3 不是 4。",
   "混合运算时 int 和 double 一起计算，int 自动提升为 double。",
 ],
},
"多分支判断": {
 "points": [
   "if-else if-else 链从上到下判断，满足第一个就执行对应块并跳出。",
   "分支嵌套：if 里面再写 if，注意 else 与最近的未配对 if 结合。",
   "处理区间判断时要注意边界，分数 60 分及格不能写成 score>60。",
   "多分支用 switch 时表达式必须是整数或字符类型。",
 ],
 "code": """int x;
cin >> x;
if (x > 0) {
    if (x >= 100) cout << "大数" << endl;
    else cout << "正数" << endl;
} else if (x == 0) {
    cout << "零" << endl;
} else {
    cout << "负数" << endl;
}""",
 "tips": [
   "嵌套 if 的 else 属于最近的 if，加括号明确逻辑更安全。",
   "区间判断用 >= 和 <= 时注意不要漏掉边界值。",
 ],
},
"循环嵌套": {
 "points": [
   "双重循环：外层循环控制行，内层循环控制列，常用于输出图形和枚举。",
   "外层每执行一次，内层完整执行一遍。",
   "内层循环的 break 只跳出内层，不影响外层。",
   "注意循环变量不要同名，避免互相覆盖。",
 ],
 "code": """// 输出九九乘法表
for (int i = 1; i <= 9; i++) {
    for (int j = 1; j <= i; j++) {
        cout << j << "*" << i << "=" << i*j << "\\t";
    }
    cout << endl;
}""",
 "tips": [
   "双重循环总次数是外层次数乘以内层次数，数据量大时会很慢。",
   "输出图形时注意每行末尾要换行。",
 ],
},
"常用数学函数": {
 "points": [
   "abs(x) 求整数绝对值，fabs(x) 求浮点数绝对值，需要 #include <cmath>。",
   "sqrt(x) 求平方根，pow(a,b) 求 a 的 b 次方。",
   "max(a,b) 和 min(a,b) 求最大最小值，C++11 起支持。",
   "rand() 生成伪随机数，需要 srand(time(0)) 设置种子。",
 ],
 "code": """#include <cmath>
double a = -3.14;
cout << fabs(a) << endl;    // 3.14
cout << sqrt(16) << endl;   // 4
cout << pow(2, 10) << endl; // 1024
int r = rand() % 100;       // 0到99的随机数""",
 "tips": [
   "sqrt 参数必须非负，负数会返回 NaN。",
   "rand() 范围是 0 到 RAND_MAX，%n 得到 0 到 n-1。",
 ],
},
"程序调试": {
 "points": [
   "编译错误：语法不对，编译器给出行号和原因，逐条修改。",
   "运行时错误：程序崩溃，常见原因是数组越界、除零、死递归。",
   "逻辑错误：程序能运行但结果不对，需要输出中间变量排查。",
   "常用调试方法：在关键位置插入 cout 输出变量值，对比预期结果。",
 ],
 "code": """// 调试：输出中间值
int sum = 0;
for (int i = 1; i <= n; i++) {
    sum += i;
    cout << "i=" << i << " sum=" << sum << endl;  // 追踪
}""",
 "tips": [
   "先修第一个编译错误，后面的错误可能是它引起的连锁反应。",
   "运行超时通常是死循环或算法复杂度太高。",
 ],
},
# ---- 3级 ----
"数据编码": {
 "points": [
   "n 位二进制能表示 2^n 个不同状态，范围是 0 到 2^n-1。",
   "有符号整数用最高位表示符号（0正1负），n 位有符号数范围是 -2^(n-1) 到 2^(n-1)-1。",
   "字符编码：ASCII 用 7 位表示 128 个字符，'0'=48，'A'=65，'a'=97。",
   "中文在 Unicode/UTF-8 中占多个字节，GESP 阶段不深入。",
 ],
 "code": None,
 "tips": [
   "8 位无符号数范围 0~255，8 位有符号数范围 -128~127。",
   "不要搞混 ASCII 值和字符：'0' 不是数字 0，而是 48。",
 ],
},
"进制转换": {
 "points": [
   "R 进制转十进制：按权展开求和，每一位乘以 R 的幂次。",
   "十进制转 R 进制：除 R 取余，直到商为 0，余数逆序排列。",
   "二进制与八进制：每 3 位二进制对应 1 位八进制。",
   "二进制与十六进制：每 4 位二进制对应 1 位十六进制（0-9, A-F）。",
 ],
 "code": """// 十进制转二进制（短除法）
int n = 13;
string s;
while (n > 0) {
    s = char('0' + n % 2) + s;
    n /= 2;
}
// s = "1101"

// 二进制转十进制
int dec = 0;
for (char c : s) dec = dec * 2 + (c - '0');""",
 "tips": [
   "除基取余时先得到的是低位，结果要逆序排列。",
   "十六进制中 A=10, B=11, ..., F=15，不区分大小写。",
 ],
},
"位运算": {
 "points": [
   "& 按位与：两位都为 1 结果才为 1；| 按位或：有一个为 1 就为 1。",
   "^ 按位异或：两位不同为 1，相同为 0；~ 按位取反：0 变 1，1 变 0。",
   "<< 左移：左移 n 位等于乘以 2^n；>> 右移 n 位等于除以 2^n（整数）。",
   "常用技巧：x & (x-1) 去掉最低位的 1，可用于统计二进制中 1 的个数。",
 ],
 "code": """int a = 5;   // 101
int b = 3;   // 011
a & b        // 001 = 1
a | b        // 111 = 7
a ^ b        // 110 = 6
a << 1       // 1010 = 10（乘2）
a >> 1       // 10 = 2（除2）""",
 "tips": [
   "异或同一个数两次等于原值：a ^ b ^ b = a。",
   "位运算优先级低于加减乘除，建议加括号。",
 ],
},
"算法的概念与描述": {
 "points": [
   "算法是解决问题的一系列明确步骤，具有有穷性、确定性、可行性、输入和输出。",
   "有穷性：算法必须在有限步后结束。",
   "确定性：每一步都有明确含义，不能有二义性。",
   "常用自然语言、流程图或伪代码描述算法，再翻译成程序。",
 ],
 "code": None,
 "tips": [
   "判断算法是否正确，要检查边界情况和特殊输入。",
   "好的算法不仅要正确，还要效率高、容易理解。",
 ],
},
"一维数组": {
 "points": [
   "数组是相同类型数据的集合，下标从 0 开始，定义 int a[100] 表示 100 个 int。",
   "访问元素 a[i]，注意下标范围 0 到 n-1，越界会导致未定义行为。",
   "常用操作：遍历统计、查找特定值、求最大值/最小值并记录位置。",
   "数组名是首元素地址，sizeof(a)/sizeof(a[0]) 得到元素个数。",
 ],
 "code": """int a[1005], n;
cin >> n;
for (int i = 0; i < n; i++) cin >> a[i];

int mx = a[0], pos = 0;
for (int i = 1; i < n; i++) {
    if (a[i] > mx) { mx = a[i]; pos = i; }
}
cout << mx << " " << pos << endl;""",
 "tips": [
   "数组越界是常见错误，定义大一点（如 1005 防止越界 1 个）。",
   "全局数组默认初始化为 0，局部数组需要手动初始化。",
 ],
},
"字符串处理": {
 "points": [
   "C++ 中 string 类型存储字符串，s.length() 或 s.size() 获取长度。",
   "s[i] 访问第 i 个字符，下标从 0 开始。",
   "substr(pos, len) 截取子串，find() 查找子串位置。",
   "字符串比较直接用 < > ==，按字典序比较。",
 ],
 "code": """string s;
cin >> s;
cout << s.length() << endl;     // 长度
cout << s[0] << endl;           // 第一个字符
cout << s.substr(0, 3) << endl; // 前3个字符
int p = s.find("abc");          // 查找位置，找不到返回 string::npos
string t = s + "!";             // 拼接""",
 "tips": [
   "cin >> s 遇到空格就停止，读整行用 getline(cin, s)。",
   "下标越界同样危险，s.length() 是长度，最后一个字符下标是 length()-1。",
 ],
},
"枚举算法": {
 "points": [
   "枚举法：把所有可能的情况逐一列举，逐个检查是否满足条件。",
   "关键是确定枚举范围和判断条件，范围不能太大导致超时。",
   "常见枚举：枚举数字、枚举位置、枚举选择（是/否）。",
   "优化枚举范围：利用数学性质缩小上下界，减少不必要的情况。",
 ],
 "code": """// 找出 1 到 n 中所有能被 3 整除的数
int n;
cin >> n;
for (int i = 1; i <= n; i++) {
    if (i % 3 == 0) cout << i << " ";
}

// 百钱百鸡
for (int x = 0; x <= 20; x++)
    for (int y = 0; y <= 33; y++) {
        int z = 100 - x - y;
        if (5*x + 3*y + z/3 == 100 && z % 3 == 0)
            cout << x << " " << y << " " << z << endl;
    }""",
 "tips": [
   "枚举范围要算好上界，太大可能超时。",
   "枚举时注意题目是否要求去重或顺序。",
 ],
},
"模拟算法": {
 "points": [
   "模拟法：按照题目描述的规则，一步一步模拟过程，保存状态变化。",
   "关键是用变量保存当前状态，每一步按规则更新。",
   "常见模拟：日期推算、排队过程、游戏规则、字符替换。",
   "注意边界条件和循环终止条件。",
 ],
 "code": """// 约瑟夫问题：n个人报数，数到3出列
int n, k = 3;
cin >> n;
bool out[1005] = {false};
int cnt = 0, cur = 0, num = 0;
while (cnt < n) {
    if (!out[cur]) {
        num++;
        if (num == k) {
            out[cur] = true;
            cout << cur+1 << " ";
            cnt++; num = 0;
        }
    }
    cur = (cur + 1) % n;
}""",
 "tips": [
   "模拟题代码通常较长，注意分步写函数。",
   "方向变化、边界折返等细节要仔细读题。",
 ],
},
# ---- 4级 ----
"函数基础": {
 "points": [
   "函数是完成特定任务的一段代码，有返回值类型、函数名、参数列表和函数体。",
   "函数先声明（原型）再调用，或把定义放在调用之前。",
   "return 值; 把结果返回给调用处，void 函数不需要 return 值。",
   "把重复出现的代码写成函数，减少重复、方便修改。",
 ],
 "code": """int add(int a, int b) {
    return a + b;
}

int main() {
    cout << add(3, 5) << endl;  // 8
    cout << add(10, 20) << endl; // 30
    return 0;
}""",
 "tips": [
   "函数名和变量名命名规则相同，不能与关键字冲突。",
   "return 后的表达式类型要与函数返回值类型匹配。",
 ],
},
"参数与作用域": {
 "points": [
   "形参是函数定义中的变量，实参是调用时传入的值。",
   "值传递：形参是实参的副本，函数内修改不影响外部变量。",
   "局部变量在函数内定义，函数结束后销毁；全局变量在所有函数外定义。",
   "同名局部变量会遮蔽全局变量，使用 :: 可访问全局变量。",
 ],
 "code": """int x = 10;  // 全局变量

void func(int a) {
    a = 20;      // 修改的是形参副本
    int x = 5;   // 局部x遮蔽全局x
    cout << x;   // 5
}

int main() {
    func(x);
    cout << x;   // 仍然是10
}""",
 "tips": [
   "值传递不会改变实参，想改变实参要用引用传递。",
   "尽量少用全局变量，避免不同函数互相干扰。",
 ],
},
"指针基础": {
 "points": [
   "指针是存储内存地址的变量，int* p 表示 p 指向一个 int。",
   "& 变量 取变量地址，* 指针 取指针指向的值（解引用）。",
   "指针必须初始化后才能使用，空指针用 nullptr 表示。",
   "数组名就是首元素的地址，int a[5]; a 等价于 &a[0]。",
 ],
 "code": """int x = 42;
int* p = &x;    // p 指向 x
cout << *p;     // 42（解引用）
*p = 100;       // 通过指针修改 x
cout << x;      // 100

int* q = nullptr; // 空指针，不能解引用""",
 "tips": [
   "未初始化的指针值是随机的，解引用会崩溃。",
   "指针类型要匹配，int* 不能指向 double。",
 ],
},
"引用与参数传递": {
 "points": [
   "引用是变量的别名，int& r = x; r 和 x 代表同一变量。",
   "引用传递：函数参数用引用类型，函数内修改会影响实参。",
   "数组作为参数时自动退化为指针，函数内不知道数组长度。",
   "const 引用可以避免拷贝大对象，同时防止修改。",
 ],
 "code": """void swap(int& a, int& b) {
    int t = a; a = b; b = t;
}

int main() {
    int x = 3, y = 5;
    swap(x, y);  // x=5, y=3
}

// 数组参数：长度需要额外传入
int sum(int a[], int n) {
    int s = 0;
    for (int i = 0; i < n; i++) s += a[i];
    return s;
}""",
 "tips": [
   "引用必须在定义时初始化，不能事后更改引用对象。",
   "值传递适合小数据，引用传递适合大数据或需要修改实参。",
 ],
},
"结构体": {
 "points": [
   "struct 把不同类型的数据组合成一个新类型，如学生（姓名、年龄、成绩）。",
   "定义结构体后，可以声明变量、数组和指针。",
   "访问成员用 .（结构体变量）或 ->（结构体指针）。",
   "结构体数组适合存储多条记录，如全班学生信息。",
 ],
 "code": """struct Student {
    string name;
    int age;
    double score;
};

Student s;
cin >> s.name >> s.age >> s.score;

Student cls[50];
for (int i = 0; i < n; i++) {
    cin >> cls[i].name >> cls[i].score;
}""",
 "tips": [
   "结构体定义末尾的分号不能漏。",
   "结构体可以嵌套，如学生里包含出生日期结构体。",
 ],
},
"二维数组": {
 "points": [
   "二维数组 int a[m][n] 表示 m 行 n 列，a[i][j] 访问第 i 行第 j 列。",
   "内存中按行存储，第一维可以省略（初始化时），第二维不能省略。",
   "常用操作：行列遍历、矩阵加法/转置、按行/列求和、找最值。",
   "作为函数参数时，第二维必须指定大小。",
 ],
 "code": """int a[105][105], m, n;
cin >> m >> n;
for (int i = 0; i < m; i++)
    for (int j = 0; j < n; j++)
        cin >> a[i][j];

// 按行求和
for (int i = 0; i < m; i++) {
    int rowSum = 0;
    for (int j = 0; j < n; j++) rowSum += a[i][j];
    cout << rowSum << endl;
}""",
 "tips": [
   "二维数组下标都是从 0 开始，注意行列不要搞反。",
   "大二维数组建议定义为全局变量，避免栈溢出。",
 ],
},
"递推": {
 "points": [
   "递推：从已知的初始条件出发，按固定关系逐步推出结果。",
   "关键是找到递推公式和初始条件。",
   "经典例子：斐波那契数列 f(n)=f(n-1)+f(n-2)，f(1)=f(2)=1。",
   "递推用循环实现，从小到大计算，避免重复计算。",
 ],
 "code": """// 斐波那契数列
long long f[100];
f[1] = f[2] = 1;
for (int i = 3; i <= 90; i++) {
    f[i] = f[i-1] + f[i-2];
}

// 爬楼梯：每次走1或2步，到第n阶有几种走法
// f(n) = f(n-1) + f(n-2), f(1)=1, f(2)=2""",
 "tips": [
   "数据增长快时用 long long 防止溢出。",
   "递推方向要对：从已知推向未知，不要倒着算。",
 ],
},
"排序算法": {
 "points": [
   "冒泡排序：相邻元素比较交换，每轮把最大值冒到末尾，O(n²)。",
   "选择排序：每轮选最小值放到前面，O(n²)。",
   "插入排序：把当前元素插入前面已排好序的部分，O(n²)，接近有序时很快。",
   "稳定性：相等元素排序后相对位置不变，冒泡和插入稳定，选择不稳定。",
 ],
 "code": """// 冒泡排序
for (int i = 0; i < n-1; i++) {
    for (int j = 0; j < n-1-i; j++) {
        if (a[j] > a[j+1]) swap(a[j], a[j+1]);
    }
}

// C++ 自带排序
#include <algorithm>
sort(a, a + n);  // 升序
sort(a, a + n, greater<int>());  // 降序""",
 "tips": [
   "实际竞赛直接用 sort()，O(n log n)，比手写 O(n²) 快得多。",
   "结构体排序用自定义比较函数。",
 ],
},
"复杂度估算": {
 "points": [
   "时间复杂度用大 O 表示，描述操作次数随 n 增长的趋势。",
   "常见：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)。",
   "循环嵌套层数决定复杂度：一层 O(n)，两层 O(n²)，三层 O(n³)。",
   "根据数据范围判断：n≤10⁶ 可用 O(n log n)，n≤100 可用 O(n³)。",
 ],
 "code": None,
 "tips": [
   "1 秒大约能处理 10⁷~10⁸ 次基本操作。",
   "n=100000 时 O(n²) 是 10¹⁰，会超时，必须优化。",
 ],
},
"文件与异常": {
 "points": [
   "freopen('input.txt','r',stdin) 把标准输入重定向到文件。",
   "fstream 库支持文件读写：ifstream 读，ofstream 写。",
   "输入校验：读取数据前检查是否成功打开。",
   "try-catch 用于捕获异常，GESP 阶段了解基本写法即可。",
 ],
 "code": """#include <fstream>
ifstream fin("input.txt");
ofstream fout("output.txt");
int x;
fin >> x;
fout << x * 2 << endl;
fin.close();
fout.close();

// 或用 freopen
freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);""",
 "tips": [
   "文件名注意路径和拼写，文件不存在会导致读入失败。",
   "竞赛中在线评测不用文件操作，直接用 cin/cout。",
 ],
},
# ---- 5级 ----
"初等数论基础": {
 "points": [
   "整除：a 能被 b 整除（b|a）意味着 a = b×k 没有余数。",
   "约数：如果 b|a，b 就是 a 的约数；倍数：a 是 b 的倍数。",
   "素数（质数）：大于 1 且只有 1 和自身两个约数的数。",
   "互质：两个数的最大公约数为 1。同余：a 和 b 除以 m 余数相同。",
 ],
 "code": None,
 "tips": [
   "1 不是素数，2 是最小的素数也是唯一的偶素数。",
   "判断素数只需试除到 √n，因为因数成对出现。",
 ],
},
"最大公约数": {
 "points": [
   "欧几里得算法（辗转相除法）：gcd(a,b) = gcd(b, a%b)，直到 b=0。",
   "最小公倍数 lcm(a,b) = a / gcd(a,b) * b（先除后乘防止溢出）。",
   "分数约分：分子分母同除以 gcd。",
   "C++17 有 std::gcd 和 std::lcm，需要 <numeric>。",
 ],
 "code": """int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}

int lcm(int a, int b) {
    return a / gcd(a, b) * b;
}""",
 "tips": [
   "a 和 b 顺序不影响结果，gcd(a,b)=gcd(b,a)。",
   "用 long long 防止乘法溢出。",
 ],
},
"素数筛法": {
 "points": [
   "埃氏筛：从 2 开始，把每个素数的倍数标记为合数，O(n log log n)。",
   "线性筛（欧拉筛）：每个合数只被最小质因子筛一次，O(n)。",
   "筛法预处理后，O(1) 查询一个数是否为素数。",
   "bool isPrime[N] 数组标记，初始全 true，0 和 1 标 false。",
 ],
 "code": """const int N = 1000005;
bool isPrime[N];
vector<int> primes;

void sieve(int n) {
    fill(isPrime, isPrime+n+1, true);
    isPrime[0] = isPrime[1] = false;
    for (int i = 2; i <= n; i++) {
        if (isPrime[i]) {
            primes.push_back(i);
            for (long long j = 1LL*i*i; j <= n; j += i)
                isPrime[j] = false;
        }
    }
}""",
 "tips": [
   "筛法从 i*i 开始标记更小倍数已被前面筛过。",
   "i*i 可能溢出 int，用 long long。",
 ],
},
"质因数分解": {
 "points": [
   "任何大于 1 的整数都可以唯一分解为素数的乘积：n = p1^e1 × p2^e2 × ...。",
   "分解方法：从最小素数开始试除，记录每个素因子的指数。",
   "约数个数：(e1+1)(e2+1)...，每个质因子可取 0 到 ei 次。",
   "约数和：(1+p1+...+p1^e1)(1+p2+...+p2^e2)...。",
 ],
 "code": """map<int,int> factorize(int n) {
    map<int,int> factors;
    for (int i = 2; i*i <= n; i++) {
        while (n % i == 0) {
            factors[i]++;
            n /= i;
        }
    }
    if (n > 1) factors[n]++;
    return factors;
}""",
 "tips": [
   "试除到 √n，如果最后 n>1 说明 n 本身是素数。",
   "约数个数可能很大，用 long long。",
 ],
},
"高精度运算": {
 "points": [
   "超过 long long 范围的大整数用数组（或 string）逐位存储。",
   "加法：从低位到高位逐位相加，处理进位。",
   "乘法：大数乘以一位数，逐位乘加进位；大整数相乘用双重循环。",
   "存储时通常低位在前（a[0] 存个位），方便进位。",
 ],
 "code": """// 大数加法（低位在前）
vector<int> add(vector<int>& a, vector<int>& b) {
    vector<int> c;
    int carry = 0;
    for (int i = 0; i < max(a.size(), b.size()) || carry; i++) {
        if (i < a.size()) carry += a[i];
        if (i < b.size()) carry += b[i];
        c.push_back(carry % 10);
        carry /= 10;
    }
    return c;
}""",
 "tips": [
   "低位在前存储，输出时倒序打印。",
   "注意前导零问题和负数处理。",
 ],
},
"链表": {
 "points": [
   "链表通过指针把节点串联，每个节点包含数据和指向下一节点的指针。",
   "插入删除只需修改指针，不需要移动元素，但查找需从头遍历。",
   "双向链表每个节点有前驱和后继指针。",
   "数组模拟链表：用数组存数据和 next 指针，避免动态分配。",
 ],
 "code": """struct Node {
    int val;
    Node* next;
    Node(int v) : val(v), next(nullptr) {}
};

// 在头部插入
Node* head = nullptr;
void insertFront(int v) {
    Node* p = new Node(v);
    p->next = head;
    head = p;
}

// 数组模拟链表
int val[100005], nxt[100005], cnt = 0;""",
 "tips": [
   "动态分配的节点要手动 delete，否则内存泄漏。",
   "竞赛中推荐用数组模拟链表，更快更安全。",
 ],
},
"顺序查找": {
 "points": [
   "顺序查找：从头到尾逐个比较，O(n)，适合无序或小数据。",
   "统计出现次数：遍历一遍计数。",
   "左右边界查找：找到第一个 >= x 的位置和第一个 > x 的位置。",
   "有序数组可用二分查找把 O(n) 降到 O(log n)。",
 ],
 "code": """int search(int a[], int n, int x) {
    for (int i = 0; i < n; i++)
        if (a[i] == x) return i;
    return -1; // 未找到
}""",
 "tips": [
   "顺序查找简单但效率低，大数据量用二分。",
   "找到后不要忘记 break 或 return。",
 ],
},
"二分答案": {
 "points": [
   "二分答案适用于：答案具有单调性（满足条件后更大/更小也满足）。",
   "二分范围 [l, r]，取 mid 判断 check(mid)，根据结果收缩区间。",
   "求最小可行值：check(mid) 为真时尝试更小（r=mid-1），否则 l=mid+1。",
   "check 函数是核心，要正确实现判定逻辑。",
 ],
 "code": """bool check(int x) { /* 判断 x 是否可行 */ }

int binaryAnswer(int lo, int hi) {
    int ans = lo;
    while (lo <= hi) {
        int mid = (lo + hi) / 2;
        if (check(mid)) {
            ans = mid;
            hi = mid - 1;  // 尝试更小
        } else {
            lo = mid + 1;
        }
    }
    return ans;
}""",
 "tips": [
   "上下界要根据题目确定好，答案可能是浮点型（浮点二分）。",
   "mid = (l+r)/2 可能溢出，用 l+(r-l)/2。",
 ],
},
"贪心算法": {
 "points": [
   "贪心：每步选当前最优的选择，期望得到全局最优。",
   "关键是证明贪心策略正确（交换论证：换一种选择不会更好）。",
   "常见策略：按某关键字排序后依次选择，如区间调度、分数背包。",
   "贪心不一定正确，必须证明或反例验证。",
 ],
 "code": """// 区间调度：最多选多少个不重叠区间
struct Interval { int l, r; };
bool cmp(Interval a, Interval b) { return a.r < b.r; }

int maxIntervals(Interval v[], int n) {
    sort(v, v+n, cmp);
    int cnt = 0, lastEnd = -1e9;
    for (int i = 0; i < n; i++) {
        if (v[i].l >= lastEnd) {
            cnt++;
            lastEnd = v[i].r;
        }
    }
    return cnt;
}""",
 "tips": [
   "贪心题先排序再决策，想清楚按什么关键字排序。",
   "如果不确定贪心是否正确，考虑动态规划。",
 ],
},
"分治与排序": {
 "points": [
   "分治三步：分解（把问题分成子问题）→ 解决（递归求解）→ 合并（合并结果）。",
   "归并排序：分成两半分别排序，再合并两个有序数组，O(n log n)，稳定。",
   "快速排序：选基准元素，小于放左边大于放右边，递归处理，平均 O(n log n)。",
   "分治的关键是合并步骤要高效。",
 ],
 "code": """// 归并排序核心
void mergeSort(int a[], int l, int r) {
    if (l >= r) return;
    int mid = (l + r) / 2;
    mergeSort(a, l, mid);
    mergeSort(a, mid+1, r);
    // 合并 a[l..mid] 和 a[mid+1..r]
    int i = l, j = mid+1, k = 0;
    while (i <= mid && j <= r) {
        if (a[i] <= a[j]) tmp[k++] = a[i++];
        else tmp[k++] = a[j++];
    }
    while (i <= mid) tmp[k++] = a[i++];
    while (j <= r) tmp[k++] = a[j++];
    for (i = 0; i < k; i++) a[l+i] = tmp[i];
}""",
 "tips": [
   "归并排序需要 O(n) 额外空间存临时数组。",
   "快排在最坏情况（已排序+选端点）退化为 O(n²)。",
 ],
},
"递归": {
 "points": [
   "递归：函数直接或间接调用自己，必须有递归出口（base case）。",
   "递归三要素：出口条件、递归关系、返回值。",
   "递归过程：逐层调用 → 到达出口 → 逐层返回计算结果。",
   "经典：阶乘、斐波那契、汉诺塔、二叉树遍历（后续学）。",
 ],
 "code": """int factorial(int n) {
    if (n <= 1) return 1;       // 出口
    return n * factorial(n - 1); // 递归
}

// 汉诺塔
void hanoi(int n, char a, char b, char c) {
    if (n == 0) return;
    hanoi(n-1, a, c, b);
    cout << a << "->" << c << endl;
    hanoi(n-1, b, a, c);
}""",
 "tips": [
   "没有出口或出口错误会导致栈溢出（无限递归）。",
   "递归深度太大（如 10^6）会爆栈，改用循环。",
 ],
},
"复杂度与数据范围": {
 "points": [
   "根据数据范围选择算法：n≤20 可用 O(2ⁿ) 枚举，n≤1000 可用 O(n²)，n≤10⁶ 用 O(n log n)。",
   "空间复杂度也要考虑：数组大小不能超过内存限制（通常 256MB）。",
   "long long 比 int 占一倍空间，注意数组大小。",
   "常数因子也重要：O(n) 的沉重实现可能比 O(n log n) 慢。",
 ],
 "code": None,
 "tips": [
   "1秒时限：O(n) 可到 10⁷，O(n²) 可到 10000，O(n³) 可到 500。",
   "内存 256MB 约可开 3×10⁷ 个 int 数组。",
 ],
},
# ---- 6级 ----
"树的基础知识": {
 "points": [
   "树是 n 个结点的有限集合，有且仅有一个根结点，其余结点分成若干子树。",
   "结点的度：子树个数；树的度：最大结点度。",
   "深度：从根到该结点的层数；高度：结点到最远叶结点的路径长度。",
   "叶结点：度为 0 的结点；父结点/子结点/兄弟结点按层次关系命名。",
 ],
 "code": None,
 "tips": [
   "树中 n 个结点有 n-1 条边。",
   "根结点没有父结点，叶结点没有子结点。",
 ],
},
"树的构造与遍历": {
 "points": [
   "先序遍历：根 → 左 → 右；中序：左 → 根 → 右；后序：左 → 右 → 根。",
   "根据输入构造二叉树：常见格式有括号表示法、层序数组。",
   "遍历用递归实现最简洁，也可以用栈模拟迭代版本。",
   "已知先序+中序可以唯一确定二叉树，后序+中序也可以。",
 ],
 "code": """struct TreeNode {
    int val;
    TreeNode *left, *right;
    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}
};

void preorder(TreeNode* root) {
    if (!root) return;
    cout << root->val;
    preorder(root->left);
    preorder(root->right);
}

void inorder(TreeNode* root) {
    if (!root) return;
    inorder(root->left);
    cout << root->val;
    inorder(root->right);
}""",
 "tips": [
   "先序+后序不能唯一确定一棵二叉树。",
   "空指针要判断，递归函数首先 if(!root) return。",
 ],
},
"层序遍历": {
 "points": [
   "层序遍历按从上到下、从左到右顺序访问，用队列实现。",
   "根入队，每次出队一个结点并访问，把它的左右孩子入队。",
   "层序遍历可用来按层统计结点数、求树的宽度。",
   "每次处理一层时记录当前队列大小，可以逐层处理。",
 ],
 "code": """#include <queue>
void levelOrder(TreeNode* root) {
    if (!root) return;
    queue<TreeNode*> q;
    q.push(root);
    while (!q.empty()) {
        TreeNode* node = q.front();
        q.pop();
        cout << node->val;
        if (node->left) q.push(node->left);
        if (node->right) q.push(node->right);
    }
}""",
 "tips": [
   "层序遍历必用队列，不是栈。",
   "求深度可以在每层处理时计数。",
 ],
},
"遍历序列还原": {
 "points": [
   "先序第一个元素是根，中序中根左边是左子树、右边是右子树。",
   "递归地对左右子树重复此过程，即可还原整棵树。",
   "同理，后序最后一个元素是根。",
   "还原后可以推出第三种遍历序列。",
 ],
 "code": """// 先序+中序还原（简述）
TreeNode* build(int preL, int preR, int inL, int inR,
                vector<int>& pre, vector<int>& in) {
    if (preL > preR) return nullptr;
    int rootVal = pre[preL];
    int k = inL;
    while (in[k] != rootVal) k++;
    int leftSize = k - inL;
    TreeNode* node = new TreeNode(rootVal);
    node->left = build(preL+1, preL+leftSize, inL, k-1, pre, in);
    node->right = build(preL+leftSize+1, preR, k+1, inR, pre, in);
    return node;
}""",
 "tips": [
   "注意计算左右子树大小的偏移量，这是最容易出错的地方。",
   "元素必须互不相同，否则中序中找不到唯一位置。",
 ],
},
"特殊二叉树": {
 "points": [
   "二叉搜索树（BST）：左子树所有结点 < 根 < 右子树所有结点。",
   "BST 的中序遍历是升序序列。",
   "完全二叉树：除最后一层外全满，最后一层靠左排列。",
   "满二叉树：所有层都满，结点数 2^h - 1。",
 ],
 "code": None,
 "tips": [
   "完全二叉树用数组存储：i 的左孩子是 2i，右孩子是 2i+1。",
   "BST 查找时间平均 O(log n)，最坏 O(n)（链状）。",
 ],
},
"哈夫曼编码": {
 "points": [
   "哈夫曼树：带权路径长度（WPL）最小的二叉树。",
   "构造：每次选两个权值最小的结点合并，新结点权值为两者之和。",
   "哈夫曼编码：左分支 0 右分支 1，频率高的字符编码短。",
   "WPL = Σ(字符频率 × 编码长度)，也等于所有非叶结点权值之和。",
 ],
 "code": """// 用优先队列构造哈夫曼树（简述）
priority_queue<int, vector<int>, greater<int>> pq;
for (int w : weights) pq.push(w);
int wpl = 0;
while (pq.size() > 1) {
    int a = pq.top(); pq.pop();
    int b = pq.top(); pq.pop();
    wpl += a + b;
    pq.push(a + b);
}""",
 "tips": [
   "哈夫曼树不唯一（等权结点合并顺序不同），但 WPL 唯一。",
   "哈夫曼编码是前缀编码，不会一个编码是另一个的前缀。",
 ],
},
"格雷码": {
 "points": [
   "格雷码：相邻两个数的二进制表示只有一位不同。",
   "n 位格雷码有 2^n 个编码。",
   "构造方法：n 位格雷码 = (n-1)位格雷码正序 + (n-1)位格雷码逆序，前面补 0/1。",
   "二进制转格雷码：g = b ^ (b >> 1)。",
 ],
 "code": """// 二进制转格雷码
int binaryToGray(int b) {
    return b ^ (b >> 1);
}

// 生成 n 位格雷码序列（简述）
vector<int> gray(int n) {
    if (n == 1) return {0, 1};
    vector<int> v = gray(n-1), r;
    for (int x : v) r.push_back(x);
    for (int i = v.size()-1; i >= 0; i--)
        r.push_back((1 << (n-1)) | v[i]);
    return r;
}""",
 "tips": [
   "格雷码首尾两个编码也只差一位（循环格雷码）。",
   "常用于电机控制和错误校正。",
 ],
},
"路径枚举与回溯": {
 "points": [
   "回溯：按决策树深度优先搜索，走不通就回退，撤销选择继续试。",
   "在迷宫中：标记访问过的格子，DFS 搜索，递归返回后取消标记。",
   "回溯三步：做选择 → 递归 → 撤销选择。",
   "适用于：全排列、子集、迷宫路径、N 皇后等。",
 ],
 "code": """int maze[15][15];
bool visited[15][15];
int dx[] = {0, 1, 0, -1};
int dy[] = {1, 0, -1, 0};

void dfs(int x, int y, int n, int m) {
    if (x < 0 || x >= n || y < 0 || y >= m) return;
    if (maze[x][y] == 1 || visited[x][y]) return;
    visited[x][y] = true;
    // 到达终点处理
    for (int d = 0; d < 4; d++)
        dfs(x+dx[d], y+dy[d], n, m);
    visited[x][y] = false;  // 撤销
}""",
 "tips": [
   "回溯必须撤销选择（恢复现场），否则其他路径会误判。",
   "方向数组简化四方向扩展。",
 ],
},
"深度优先搜索": {
 "points": [
   "DFS：一条路走到黑，走不通就回溯，用递归或栈实现。",
   "连通性问题：从一个点出发 DFS，能到达的所有点在同一连通分量。",
   "路径问题：记录当前路径，到达目标时处理。",
   "visited 数组防止重复访问。",
 ],
 "code": """vector<int> adj[100005];
bool vis[100005];

void dfs(int u) {
    vis[u] = true;
    for (int v : adj[u]) {
        if (!vis[v]) dfs(v);
    }
}

// 统计连通分量数
int cnt = 0;
for (int i = 1; i <= n; i++) {
    if (!vis[i]) { dfs(i); cnt++; }
}""",
 "tips": [
   "DFS 递归深度不能太大，可能爆栈。",
   "无向图 DFS 不需要记录父结点来避免走回去。",
 ],
},
"广度优先搜索": {
 "points": [
   "BFS：按层扩展，用队列实现，第一次到达目标时步数最少。",
   "求最短路径（边权为 1）：BFS 天然保证最短。",
   "BFS 用 dist 数组记录距离，初始 -1 表示未访问。",
   "每层处理完后步数加一。",
 ],
 "code": """int dist[100005];
memset(dist, -1, sizeof(dist));
queue<int> q;
dist[start] = 0;
q.push(start);
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int v : adj[u]) {
        if (dist[v] == -1) {
            dist[v] = dist[u] + 1;
            q.push(v);
        }
    }
}""",
 "tips": [
   "BFS 求最短路只适用于边权相同（或为1）的图。",
   "网格 BFS 同样用队列和方向数组。",
 ],
},
# ---- 7级 ----
"数学库常用函数": {
 "points": [
   "三角函数 sin/cos/tan 使用弧度，角度转弧度：rad = deg * PI / 180。",
   "asin/acos/atan 返回弧度，注意定义域：asin(x) 要求 |x|≤1。",
   "pow(a,b) 求幂，sqrt(x) 求平方根，log(x) 自然对数，log10(x) 常用对数。",
   "浮点数比较不要用 ==，用 fabs(a-b) < 1e-9。",
 ],
 "code": """#include <cmath>
const double PI = acos(-1.0);
double deg = 90;
double rad = deg * PI / 180;
cout << sin(rad);  // 1.0

double a = 0.1 + 0.2;
if (fabs(a - 0.3) < 1e-9) cout << "equal";""",
 "tips": [
   "PI 用 acos(-1.0) 获取比手写 3.14159... 更精确。",
   "浮点误差累积，比较时设一个 epsilon（如 1e-9）。",
 ],
},
"复杂动态规划": {
 "points": [
   "DP 三步：定义状态、推导转移方程、确定初始值和遍历顺序。",
   "状态维度增加：如同时记录是否选过、是否处于某种状态。",
   "区间 DP：按区间长度从小到大枚举，dp[i][j] 表示区间 [i,j] 的最优解。",
   "枚举断点 k 把区间分成 [i,k] 和 [k+1,j]。",
 ],
 "code": """// 区间DP：石子合并（简述）
int dp[305][305], sum[305];
for (int len = 2; len <= n; len++) {
    for (int i = 1; i + len - 1 <= n; i++) {
        int j = i + len - 1;
        dp[i][j] = INF;
        for (int k = i; k < j; k++) {
            dp[i][j] = min(dp[i][j],
                dp[i][k] + dp[k+1][j] + sum[j] - sum[i-1]);
        }
    }
}""",
 "tips": [
   "区间 DP 必须按区间长度从小到大枚举。",
   "注意初始化：dp[i][i] = 0，其他设为无穷大。",
 ],
},
"二维动态规划": {
 "points": [
   "网格 DP：dp[i][j] 表示从起点到 (i,j) 的最优值，从左/上转移。",
   "最长公共子序列（LCS）：dp[i][j] 表示 A 前 i 个和 B 前 j 个的 LCS。",
   "编辑距离：插入、删除、替换三种操作的最小编辑次数。",
   "转移方程根据题目具体分析。",
 ],
 "code": """// LCS
int dp[1005][1005];
for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= m; j++) {
        if (a[i] == b[j])
            dp[i][j] = dp[i-1][j-1] + 1;
        else
            dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
    }
}
// dp[n][m] 就是 LCS 长度""",
 "tips": [
   "网格 DP 注意第一行第一列的初始化。",
   "LCS 不要求连续，子串才要求连续。",
 ],
},
"线性动态规划": {
 "points": [
   "最大连续子段和：dp[i] 表示以 i 结尾的最大连续和，dp[i]=max(a[i], dp[i-1]+a[i])。",
   "最长上升子序列（LIS）：O(n²) DP，O(n log n) 用二分优化。",
   "01 背包：dp[j] 表示容量 j 时的最大价值，倒序枚举容量。",
   "完全背包：正序枚举容量，每个物品可选多次。",
 ],
 "code": """// 最大连续子段和
int dp[100005], ans = -INF;
dp[1] = a[1];
for (int i = 2; i <= n; i++) {
    dp[i] = max(a[i], dp[i-1] + a[i]);
    ans = max(ans, dp[i]);
}

// 01背包
for (int i = 1; i <= n; i++)
    for (int j = W; j >= w[i]; j--)
        dp[j] = max(dp[j], dp[j-w[i]] + v[i]);""",
 "tips": [
   "01 背包必须倒序枚举容量，正序就变成完全背包了。",
   "LIS 二分优化用 lower_bound 找替换位置。",
 ],
},
"图的基本概念": {
 "points": [
   "图由顶点集 V 和边集 E 组成，记为 G=(V,E)。",
   "无向图边没有方向；有向图边有方向（弧）。",
   "度：无向图中与该顶点相连的边数；有向图分入度和出度。",
   "路径：顶点序列；环：起点和终点相同的路径；连通：任意两点间有路径。",
 ],
 "code": None,
 "tips": [
   "n 个顶点的无向完全图有 n(n-1)/2 条边。",
   "树是无环连通图，n 个顶点 n-1 条边。",
 ],
},
"图的存储": {
 "points": [
   "邻接矩阵：g[i][j] 表示 i 到 j 是否有边，适合稠密图，O(n²) 空间。",
   "邻接表：vector<int> adj[n+1]，每个顶点存邻居列表，适合稀疏图。",
   "邻接表方便遍历所有出边，矩阵方便 O(1) 查询两点是否相邻。",
   "带权图邻接表存 pair<int,int>（邻接点, 权值）。",
 ],
 "code": """// 邻接表
vector<int> adj[100005];
void addEdge(int u, int v) {
    adj[u].push_back(v);
    adj[v].push_back(u);  // 无向图
}

// 带权邻接表
vector<pair<int,int>> adj[100005];
adj[u].push_back({v, w});""",
 "tips": [
   "顶点编号从 0 或 1 开始，保持统一。",
   "链式前向星用数组模拟邻接表，竞赛常用。",
 ],
},
"图的遍历": {
 "points": [
   "DFS 遍历图：从起点出发，递归访问未访问的邻居。",
   "BFS 遍历图：用队列逐层扩展。",
   "非连通图需要枚举所有起点，visited 数组标记。",
   "遍历顺序不同但都能访问所有可达结点。",
 ],
 "code": """vector<int> adj[100005];
bool vis[100005];

void dfs(int u) {
    vis[u] = true;
    for (int v : adj[u])
        if (!vis[v]) dfs(v);
}

int components = 0;
for (int i = 1; i <= n; i++) {
    if (!vis[i]) {
        dfs(i);
        components++;
    }
}""",
 "tips": [
   "有向图的连通性要分强连通和弱连通。",
   "遍历顺序影响具体结果，但不影响连通性判定。",
 ],
},
"网格搜索": {
 "points": [
   "把网格看成图，每个格子是一个结点，相邻格子是边。",
   "方向数组 dx[] = {-1,1,0,0}, dy[] = {0,0,-1,1} 表示上下左右。",
   "检查边界：坐标不能越出 0~n-1 和 0~m-1。",
   "统计连通区域：DFS/BFS  flood fill 算法。",
 ],
 "code": """int grid[105][105], n, m;
int dx[] = {-1, 1, 0, 0};
int dy[] = {0, 0, -1, 1};

void floodFill(int x, int y, int oldColor, int newColor) {
    if (x < 0 || x >= n || y < 0 || y >= m) return;
    if (grid[x][y] != oldColor) return;
    grid[x][y] = newColor;
    for (int d = 0; d < 4; d++)
        floodFill(x+dx[d], y+dy[d], oldColor, newColor);
}""",
 "tips": [
   "网格 BFS 求最短步数比 DFS 更可靠。",
   "注意障碍物和边界的判断顺序。",
 ],
},
"哈希表与集合": {
 "points": [
   "unordered_map<key,value>：键值对存储，平均 O(1) 查找插入。",
   "unordered_set：只存键不存值，O(1) 判断存在性。",
   "map/set：有序（红黑树），O(log n) 查找，按键排序。",
   "统计频率：用 map<int,int> 或 unordered_map<int,int>。",
 ],
 "code": """#include <unordered_map>
unordered_map<string, int> cnt;
cnt["apple"]++;
cnt["banana"]++;
cout << cnt["apple"];  // 1

unordered_set<int> seen;
seen.insert(5);
if (seen.count(5)) cout << "exists";

// 遍历
for (auto& [key, val] : cnt)
    cout << key << ":" << val;""",
 "tips": [
   "unordered_map 最坏 O(n)（哈希冲突），map 稳定 O(log n)。",
   "自定义类型作为 key 需要定义哈希函数或比较函数。",
 ],
},
"综合应用": {
 "points": [
   "综合题通常需要组合多个数据结构和算法。",
   "解题步骤：建模（把问题转化为图/数组/区间）→ 选算法 → 写代码。",
   "先写暴力解法确保正确，再想优化。",
   "注意数据范围，选择合适复杂度的算法。",
 ],
 "code": None,
 "tips": [
   "不要一上来就写最复杂的算法，先确保思路正确。",
   "复杂题分步测试，每完成一部分就验证。",
 ],
},
# ---- 8级 ----
"计数原理": {
 "points": [
   "加法原理：完成一件事有多种互斥方案，方案数相加。",
   "乘法原理：完成一件事分多个步骤，每步方法数相乘。",
   "区分分类与分步：分类是'或'的关系，分步是'且'的关系。",
   "容斥原理：|A∪B| = |A| + |B| - |A∩B|。",
 ],
 "code": None,
 "tips": [
   "先分类再分步，确保不重不漏。",
   "题目有'至少''至多'时考虑容斥。",
 ],
},
"排列与组合": {
 "points": [
   "排列 P(n,k) = n! / (n-k)!，从 n 个中选 k 个排列。",
   "组合 C(n,k) = n! / (k!(n-k)!)，从 n 个中选 k 个不排序。",
   "组合性质：C(n,k) = C(n,n-k)，C(n,k) = C(n-1,k-1) + C(n-1,k)。",
   "阶乘增长极快，n=20 就超过 int，n=100 需高精度或模运算。",
 ],
 "code": """// 用递推公式求组合数（杨辉三角）
long long C[105][105];
for (int i = 0; i <= n; i++) {
    C[i][0] = 1;
    for (int j = 1; j <= i; j++)
        C[i][j] = C[i-1][j-1] + C[i-1][j];
}""",
 "tips": [
   "n 不大时用递推，n 大时用乘法逆元求组合数。",
   "C(n,0)=1, C(n,n)=1, C(n,1)=n。",
 ],
},
"杨辉三角": {
 "points": [
   "杨辉三角第 i 行第 j 列等于 C(i,j)。",
   "递推公式：C[i][j] = C[i-1][j-1] + C[i-1][j]。",
   "边界：C[i][0] = C[i][i] = 1。",
   "预处理后 O(1) 查询组合数。",
 ],
 "code": """const int MOD = 1e9 + 7;
long long C[1005][1005];
for (int i = 0; i <= 1000; i++) {
    C[i][0] = 1;
    for (int j = 1; j <= i; j++)
        C[i][j] = (C[i-1][j-1] + C[i-1][j]) % MOD;
}""",
 "tips": [
   "模运算要在每次加法后取模，防止溢出。",
   "空间优化：每行只依赖上一行，可用一维数组倒序更新。",
 ],
},
"倍增": {
 "points": [
   "倍增：把区间长度按 2 的幂次分解，预处理跳 2^k 步的结果。",
   "经典应用：快速求 LCA（最近公共祖先）、ST 表求区间最值。",
   "预处理 O(n log n)，查询 O(log n)。",
   "二进制拆分：任何数都可表示为若干 2 的幂之和。",
 ],
 "code": """// ST表求区间最大值（简述）
int st[18][100005];
for (int i = 1; i <= n; i++) st[0][i] = a[i];
for (int k = 1; (1<<k) <= n; k++)
    for (int i = 1; i + (1<<k) - 1 <= n; i++)
        st[k][i] = max(st[k-1][i], st[k-1][i+(1<<(k-1))]);

int queryMax(int l, int r) {
    int k = log2(r - l + 1);
    return max(st[k][l], st[k][r-(1<<k)+1]);
}""",
 "tips": [
   "ST 表适用于静态数组（不修改）的区间查询。",
   "log2 用 __lg() 或循环计算，精度问题注意。",
 ],
},
"数列与代数": {
 "points": [
   "等差数列：a_n = a_1 + (n-1)d，和 S_n = n(a_1+a_n)/2。",
   "等比数列：a_n = a_1 * q^(n-1)，和 S_n = a_1(1-q^n)/(1-q)（q≠1）。",
   "常见求和公式：Σi = n(n+1)/2，Σi² = n(n+1)(2n+1)/6。",
   "代数化简：先化简表达式再计算，避免大数中间结果。",
 ],
 "code": None,
 "tips": [
   "等比数列求和 q=1 时单独处理，就是 n*a1。",
   "取模除法要用乘法逆元。",
 ],
},
"几何计算": {
 "points": [
   "两点距离：√((x2-x1)² + (y2-y1)²)，不开方用平方距离比较。",
   "三角形面积：叉积公式 |(B-A)×(C-A)| / 2。",
   "多边形面积： shoelace 公式，按顶点顺序累加叉积。",
   "注意退化情况：三点共线面积为 0，点在边上。",
 ],
 "code": """struct Point { double x, y; };
double dist(Point a, Point b) {
    return sqrt((a.x-b.x)*(a.x-b.x) + (a.y-b.y)*(a.y-b.y));
}
double cross(Point a, Point b, Point c) {
    return (b.x-a.x)*(c.y-a.y) - (b.y-a.y)*(c.x-a.x);
}
double triangleArea(Point a, Point b, Point c) {
    return fabs(cross(a,b,c)) / 2.0;
}""",
 "tips": [
   "浮点计算注意精度，用 epsilon 判断相等。",
   "叉积正负表示方向（左转/右转）。",
 ],
},
"图算法选择": {
 "points": [
   "根据问题选算法：连通性→DFS/BFS，最短路→Dijkstra/SPFA/Floyd。",
   "最小生成树→Prim（稠密图）或 Kruskal（稀疏图）。",
   "边权正负决定算法：有负权用 SPFA，无负权用 Dijkstra。",
   "顶点数小（n≤500）可用 Floyd O(n³)，顶点多用邻接表优化。",
 ],
 "code": None,
 "tips": [
   "先分析数据范围再选算法，不要套模板不思考。",
   "建图时注意有向/无向、是否有权重。",
 ],
},
"最小生成树": {
 "points": [
   "生成树：连通所有顶点且无环，n 个顶点 n-1 条边。",
   "最小生成树（MST）：所有生成树中边权之和最小的。",
   "Kruskal：把边按权排序，依次加入不形成环的边（并查集）。",
   "Prim：从一个点出发，每次选距离树最近的点加入。",
 ],
 "code": """// Kruskal
struct Edge { int u, v, w; };
bool cmp(Edge a, Edge b) { return a.w < b.w; }

int kruskal(vector<Edge>& edges, int n) {
    sort(edges.begin(), edges.end(), cmp);
    init(n);
    int total = 0, cnt = 0;
    for (auto& e : edges) {
        if (find(e.u) != find(e.v)) {
            unite(e.u, e.v);
            total += e.w;
            if (++cnt == n-1) break;
        }
    }
    return total;
}""",
 "tips": [
   "Kruskal 适合稀疏图，Prim 适合稠密图。",
   "MST 不唯一但权值和唯一。",
 ],
},
"单源最短路": {
 "points": [
   "Dijkstra：边权非负，用优先队列每次取最近点松弛，O((n+m)log n)。",
   "松弛操作：若 dist[u]+w(u,v) < dist[v]，则更新 dist[v]。",
   "有负权边用 Bellman-Ford 或 SPFA。",
   "多源最短路用 Floyd O(n³)，或把所有源点加入队列。",
 ],
 "code": """// Dijkstra
vector<pair<int,int>> adj[MAXN];
int dist[MAXN];
void dijkstra(int s) {
    fill(dist, dist+MAXN, INF);
    dist[s] = 0;
    priority_queue<pair<int,int>, vector<pair<int,int>>, greater<>> pq;
    pq.push({0, s});
    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d > dist[u]) continue;
        for (auto [v, w] : adj[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }
}""",
 "tips": [
   "Dijkstra 不能处理负权边，会死循环或结果错误。",
   "优先队列里的旧条目用 if(d > dist[u]) continue 跳过。",
 ],
},
"复杂度分析": {
 "points": [
   "常见图算法复杂度：Dijkstra O((n+m)log n)，Floyd O(n³)，Kruskal O(m log m)。",
   "根据 n 和 m 选算法：n≤500 可用 Floyd，m 大用 Kruskal。",
   "优化常数：邻接表比矩阵快，优先队列比朴素实现快。",
   "空间复杂度也要考虑：邻接矩阵 O(n²)，邻接表 O(n+m)。",
 ],
 "code": None,
 "tips": [
   "时间紧时先写正确的暴力版，再想优化。",
   "注意递归 DFS 的栈空间限制。",
 ],
},
"空间优化": {
 "points": [
   "滚动数组：DP 只依赖前一行/前一层时，用两个数组交替。",
   "01 背包的一维数组本身就是空间优化（O(W)）。",
   "图存储：链式前向星比邻接表数组更紧凑。",
   "不必要的数组及时释放或用局部变量。",
 ],
 "code": """// 滚动数组优化 DP
int dp[2][MAXN];
for (int i = 1; i <= n; i++) {
    int cur = i % 2, prev = 1 - cur;
    for (int j = 1; j <= m; j++) {
        dp[cur][j] = max(dp[prev][j], dp[cur][j-1]) + a[i][j];
    }
}""",
 "tips": [
   "滚动数组时注意当前行和上一行的交替关系。",
   "内存不够时考虑用 short 或 char 代替 int。",
 ],
},
"算法优化": {
 "points": [
   "暴力→剪枝：提前排除不可能的情况，如 DFS 中当前解已劣于最优解则剪枝。",
   "预处理→离线：把需要多次查询的量预处理出来。",
   "数学优化：用公式或数论结论替代暴力枚举。",
   "位运算优化：用位操作替代计数/标记。",
 ],
 "code": None,
 "tips": [
   "优化前先确认瓶颈在哪里，不要盲目优化。",
   "常数优化：用快读、减少模运算次数、循环展开。",
 ],
},
}
